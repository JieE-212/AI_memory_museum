const STORAGE_KEY = "memory-museum-items";
const PROFILE_KEY = "memory-museum-local-profile";
const ASSET_AUDIT_KEY = "memory-museum-asset-audit";
const ASSET_SNAPSHOT_KEY = "memory-museum-asset-snapshots";
const PHASE16_SYNC_AUDIT_KEY = "memory-museum-phase16-sync-audit";
const PHASE17_DEVICE_REGISTRY_KEY = "memory-museum-phase17-device-registry";
const PHASE17_SYNC_QUEUE_KEY = "memory-museum-phase17-sync-queue";
const PHASE17_PRIVATE_CLOUD_KEY = "memory-museum-phase17-private-cloud-boundary";
const PHASE18_AGENT_FEEDBACK_KEY = "memory-museum-phase18-agent-feedback";
const PHASE18_REPAIR_DRAFT_KEY = "memory-museum-phase18-repair-drafts";
const PHASE18_TASK_QUEUE_KEY = "memory-museum-phase18-task-queue";
const PHASE18_TASK_AUDIT_KEY = "memory-museum-phase18-task-audit";
const PHASE18_NOISE_RULE_KEY = "memory-museum-phase18-noise-rules";
const PHASE19_IMPORT_PREVIEW_KEY = "memory-museum-phase19-import-preview";
const PHASE19_IMPORT_BATCHES_KEY = "memory-museum-phase19-import-batches";
const PHASE19_CUSTOM_TEMPLATES_KEY = "memory-museum-phase19-custom-templates";
const SCHEMA_VERSION = 2;
const API_MEMORIES = "/api/memories";
const API_WORKFLOWS = "/api/workflows";
const API_PRIVACY = "/api/privacy";
const API_VERSION = "/api/version";
const API_OPERATIONS = "/api/operations";
const API_ASSETS = "/api/assets";
const API_EXHIBITIONS = "/api/exhibitions";
const API_REPORT_DRAFTS = "/api/report-drafts";

const emotionOptions = [
  "怀念",
  "快乐",
  "遗憾",
  "平静",
  "荒诞",
  "感动",
  "兴奋",
  "紧张",
  "孤独",
  "委屈",
  "愤怒",
  "害怕",
  "释然",
  "期待",
  "温暖",
  "迷茫"
];

const sourceTypes = ["日记", "聊天片段", "照片描述", "旅行片段", "梦境", "物品", "图片", "截图", "语音转写", "其他"];
const importanceLabels = ["普通展品", "值得一看", "重要展品", "珍贵展品", "镇馆级"];
const attachmentTypeOrder = ["图片", "OCR", "语音", "文档", "视频", "其他"];
const AI_BUTTON_TEXT = "Agent 整理";
const AI_BUTTON_PENDING_TEXT = "整理中...";
const workflowTemplates = [
  {
    id: "memory-curation",
    name: "展品整理工作流",
    purpose: "原始记忆到展品草稿",
    entry: "/api/analyze",
    nodes: ["档案员", "策展人", "编辑", "讲解员"],
    controls: ["确认", "驳回", "重试", "保存"],
    statusLabel: "可运行",
    maturity: "active",
    pausePoints: ["待复核", "已确认", "已驳回"],
    riskSignals: ["缺少时间", "缺少人物地点", "说明过短"],
    nextActions: ["补齐复核项", "保存快照", "回看历史"],
    persistence: "运行历史 + 展品关联",
    warmCue: "像把一张旧票夹进相册：每一步都留下来历。"
  },
  {
    id: "insight-report",
    name: "回忆报告工作流",
    purpose: "展品集合到时间线、主题展和报告",
    entry: "/api/insights",
    nodes: ["时间线", "主题展", "报告"],
    controls: ["范围", "筛选", "引用", "导出"],
    statusLabel: "可预览",
    maturity: "draft",
    pausePoints: ["范围选择", "主题复核", "报告导出"],
    riskSignals: ["缺少时间范围", "主题来源过少", "报告不可保存"],
    nextActions: ["保存报告草稿", "主题展命名", "编辑历史"],
    persistence: "洞察快照 + 导出备份",
    warmCue: "把散落灯光排成一条能回看的路。"
  },
  {
    id: "guided-tour",
    name: "讲解检索工作流",
    purpose: "提问到带引用的讲解回答",
    entry: "/api/guide",
    nodes: ["提问理解", "混合检索", "引用回答"],
    controls: ["提问", "引用", "追问"],
    statusLabel: "可提问",
    maturity: "active",
    pausePoints: ["弱证据", "继续追问"],
    riskSignals: ["召回弱证据", "问题锚点少", "引用不足"],
    nextActions: ["记录问答历史", "收藏引用", "导览路线"],
    persistence: "检索依据 + 可信度",
    warmCue: "讲解员会说明它为什么想起这些展品。"
  }
];
const fieldLimits = {
  title: 80,
  rawContent: 2000,
  exhibitText: 600,
  coverImage: 300,
  mediaNote: 800,
  attachmentName: 80,
  attachmentNote: 180,
  date: 30,
  location: 80,
  listItem: 30,
  listLength: 16
};

const halls = [
  { id: "all", name: "全部展品", description: "查看所有被保存下来的记忆。" },
  { id: "youth", name: "青春展厅", description: "校园、毕业、成长和那些没说完的话。" },
  { id: "friends", name: "朋友展厅", description: "朋友、室友、群聊和共同经历。" },
  { id: "family", name: "家庭展厅", description: "家人、饭桌、节日和被照顾的瞬间。" },
  { id: "low", name: "低谷展厅", description: "挫折、疲惫、失眠和重新站起来。" },
  { id: "strange", name: "奇怪瞬间展厅", description: "荒诞、尴尬、离谱但很难忘。" },
  { id: "daily", name: "日常展厅", description: "普通日子里值得留下的细节。" }
];

const seedMemories = [
  {
    id: createId(),
    title: "操场尽头的告别",
    hall: "youth",
    rawContent: "高三毕业那天晚上，我们几个人在操场坐到很晚，谁也没说以后还会不会见面。",
    exhibitText: "这件展品记录了一次没有正式说出口的告别。夜晚、操场和沉默一起保存了青春快结束时的重量。",
    date: "",
    location: "学校操场",
    people: ["同学", "朋友"],
    tags: ["毕业", "夜晚", "校园"],
    emotions: ["怀念", "遗憾"],
    emotionIntensity: 4,
    sourceType: "日记",
    importance: 4,
    favorite: true,
    createdAt: new Date().toISOString()
  },
  {
    id: createId(),
    title: "洒掉的外卖和没坏掉的一天",
    hall: "daily",
    rawContent: "今天起床晚了，外卖还洒了一半，但晚上散步的时候风很舒服。",
    exhibitText: "这件展品属于普通生活的修复时刻。它提醒参观者，一天可以从混乱开始，也可以被一阵晚风重新整理。",
    date: "",
    location: "宿舍附近",
    people: [],
    tags: ["日常", "晚风", "倒霉"],
    emotions: ["平静"],
    emotionIntensity: 2,
    sourceType: "日记",
    importance: 2,
    favorite: false,
    createdAt: new Date().toISOString()
  }
];

const samplePrompts = [
  "毕业那天晚上，我们在操场绕了一圈又一圈，谁都没有先说再见。",
  "妈妈把剩下的菜装进保鲜盒，非要让我带回去。地铁上我突然觉得那个袋子很重。",
  "群聊里朋友发了一张很糊的合照，大家开始翻旧账，笑到凌晨一点。",
  "昨晚梦到自己在一个没有出口的车站，醒来以后还记得广播声。",
  "今天本来很崩溃，晚上路过一家小店，老板多送了一个茶叶蛋。",
  "旅行回来的车上，窗外一直下雨，我突然不想那么快到站。",
  "拍照的时候所有人都在笑，只有我盯着镜头旁边那盏坏掉的灯。",
  "那天发生了一件特别离谱的事，我在电梯里把外卖递给了完全不认识的人。"
];

let memories = loadMemories();
let activeHall = "all";
let searchKeyword = "";
let sortMode = "newest";
let emotionFilter = "all";
let editingId = null;
let isAnalyzing = false;
let isPersisting = false;
let isGuideAsking = false;
let databaseAvailable = false;
let databaseNeedsMigration = false;
let backendStats = null;
let backendAiConfigured = false;
let latestAgentDraft = null;
let latestAgentWorkflow = null;
let insightScope = "all";
let backendWorkflowBlueprint = null;
let workflowBlueprintSource = "local";
let activeWorkflowTemplateId = "memory-curation";
let localProfile = loadLocalProfile();
let privacyPolicy = null;
let privacyPolicySource = "local";
let versionInfo = null;
let operationsSource = "local";
let assetCollection = null;
let assetSource = "local";
let activeAssetDialog = null;
let assetAuditSearchTerm = "";
let selectedAssetSnapshotId = "";
let pendingSyncImportPlan = null;
let phase16AuditFilter = "all";
let phase18TaskFilter = "active";
let phase18SelectedTaskIds = new Set();
let phase18GraphFilter = "all";
let phase19ImportPreview = null;
let phase19ImportBatches = loadPhase19ImportBatches();
let activePhase19BatchId = "";
let phase19BatchFilter = "all";
let phase19AuditSearch = "";
let phase19CustomTemplates = loadPhase19CustomMappingTemplates();
let activeFeaturePanel = "home";
const featurePanelIds = ["insightsSection", "phase19ImportSection", "workflowOrchestration", "privacySection", "operationsSection", "phase20PlatformSection"];

const $ = (selector) => document.querySelector(selector);
const elements = {
  hallNav: $("#hallNav"),
  hallSelect: $("#hallSelect"),
  memoryForm: $("#memoryForm"),
  rawContent: $("#rawContent"),
  rawCount: $("#rawCount"),
  titleInput: $("#titleInput"),
  memoryDate: $("#memoryDate"),
  locationInput: $("#locationInput"),
  peopleInput: $("#peopleInput"),
  exhibitText: $("#exhibitText"),
  coverImageInput: $("#coverImageInput"),
  mediaNoteInput: $("#mediaNoteInput"),
  attachmentsInput: $("#attachmentsInput"),
  tagsInput: $("#tagsInput"),
  sampleButton: $("#sampleButton"),
  mockAiButton: $("#mockAiButton"),
  aiStatus: $("#aiStatus"),
  useAgentSuggestionButton: $("#useAgentSuggestionButton"),
  clearAgentWorkflowButton: $("#clearAgentWorkflowButton"),
  resetButton: $("#resetButton"),
  cancelEditButton: $("#cancelEditButton"),
  saveButton: $("#saveButton"),
  formModeHint: $("#formModeHint"),
  memoryGrid: $("#memoryGrid"),
  memoryCount: $("#memoryCount"),
  hallCount: $("#hallCount"),
  tagCount: $("#tagCount"),
  emotionCount: $("#emotionCount"),
  mediaCount: $("#mediaCount"),
  agentRunCount: $("#agentRunCount"),
  timelineCount: $("#timelineCount"),
  timelineList: $("#timelineList"),
  themeCount: $("#themeCount"),
  themeList: $("#themeList"),
  reportMeta: $("#reportMeta"),
  reportPanel: $("#reportPanel"),
  assetSummaryMeta: $("#assetSummaryMeta"),
  savedAssetsPanel: $("#savedAssetsPanel"),
  phase18AgentPanel: $("#phase18AgentPanel"),
  phase19ImportSection: $("#phase19ImportSection"),
  phase19ImportFormat: $("#phase19ImportFormat"),
  phase19DefaultSource: $("#phase19DefaultSource"),
  phase19DefaultHall: $("#phase19DefaultHall"),
  phase19CleanupMode: $("#phase19CleanupMode"),
  phase19MappingTemplate: $("#phase19MappingTemplate"),
  phase19TemplateTags: $("#phase19TemplateTags"),
  phase19TemplatePeople: $("#phase19TemplatePeople"),
  phase19TemplateDateRule: $("#phase19TemplateDateRule"),
  phase19TemplateAliases: $("#phase19TemplateAliases"),
  phase19BatchName: $("#phase19BatchName"),
  phase19AuditSearch: $("#phase19AuditSearch"),
  phase19BatchFilter: $("#phase19BatchFilter"),
  phase19ImportText: $("#phase19ImportText"),
  phase19SampleButton: $("#phase19SampleButton"),
  phase19SaveTemplateButton: $("#phase19SaveTemplateButton"),
  phase19PreviewButton: $("#phase19PreviewButton"),
  phase19ApplyButton: $("#phase19ApplyButton"),
  phase19ImportPreview: $("#phase19ImportPreview"),
  insightScopeSelect: $("#insightScopeSelect"),
  insightScopeMeta: $("#insightScopeMeta"),
  workflowOrchestration: $("#workflowOrchestration"),
  workflowSummaryMeta: $("#workflowSummaryMeta"),
  workflowReadinessGrid: $("#workflowReadinessGrid"),
  workflowGapList: $("#workflowGapList"),
  phase12ReadinessPanel: $("#phase12ReadinessPanel"),
  workflowDetailPanel: $("#workflowDetailPanel"),
  workflowTemplateList: $("#workflowTemplateList"),
  workflowCapabilityList: $("#workflowCapabilityList"),
  privacySection: $("#privacySection"),
  profileNameInput: $("#profileNameInput"),
  profileDeviceInput: $("#profileDeviceInput"),
  profileSyncSelect: $("#profileSyncSelect"),
  profileAiConsentInput: $("#profileAiConsentInput"),
  saveProfileButton: $("#saveProfileButton"),
  privacySummary: $("#privacySummary"),
  dataLocationList: $("#dataLocationList"),
  aiScopePanel: $("#aiScopePanel"),
  sensitivityPanel: $("#sensitivityPanel"),
  userControlList: $("#userControlList"),
  exportRedactedButton: $("#exportRedactedButton"),
  purgeDatabaseButton: $("#purgeDatabaseButton"),
  phase16SyncPanel: $("#phase16SyncPanel"),
  phase13ReadinessPanel: $("#phase13ReadinessPanel"),
  operationsSection: $("#operationsSection"),
  operationsSummaryGrid: $("#operationsSummaryGrid"),
  runtimePanel: $("#runtimePanel"),
  deploymentPanel: $("#deploymentPanel"),
  checksPanel: $("#checksPanel"),
  engineeringPanel: $("#engineeringPanel"),
  releaseChecklistPanel: $("#releaseChecklistPanel"),
  runbookPanel: $("#runbookPanel"),
  backupPolicyPanel: $("#backupPolicyPanel"),
  riskPanel: $("#riskPanel"),
  operationEventsPanel: $("#operationEventsPanel"),
  releaseHistoryPanel: $("#releaseHistoryPanel"),
  logArchivePanel: $("#logArchivePanel"),
  demoKitPanel: $("#demoKitPanel"),
  phase14Panel: $("#phase14Panel"),
  moduleBoundaryPanel: $("#moduleBoundaryPanel"),
  phase15Panel: $("#phase15Panel"),
  phase15AssetPanel: $("#phase15AssetPanel"),
  phase20PlatformSection: $("#phase20PlatformSection"),
  phase20SummaryGrid: $("#phase20SummaryGrid"),
  phase20ExtensionPanel: $("#phase20ExtensionPanel"),
  phase20PluginPanel: $("#phase20PluginPanel"),
  phase20RegistryPanel: $("#phase20RegistryPanel"),
  phase20ManifestPanel: $("#phase20ManifestPanel"),
  phase20PermissionPanel: $("#phase20PermissionPanel"),
  phase20AuditPanel: $("#phase20AuditPanel"),
  phase20ContractPanel: $("#phase20ContractPanel"),
  phase20SandboxPanel: $("#phase20SandboxPanel"),
  phase20TemplatePanel: $("#phase20TemplatePanel"),
  phase20FixturePanel: $("#phase20FixturePanel"),
  phase20SignaturePanel: $("#phase20SignaturePanel"),
  phase20InstallPanel: $("#phase20InstallPanel"),
  phase20SecurityPanel: $("#phase20SecurityPanel"),
  phase20MilestonePanel: $("#phase20MilestonePanel"),
  phase25SurfacePanel: $("#phase25SurfacePanel"),
  phase26ValidationPanel: $("#phase26ValidationPanel"),
  phase27GovernancePanel: $("#phase27GovernancePanel"),
  phase28ClearanceReviewPanel: $("#phase28ClearanceReviewPanel"),
  phase29ReleaseGovernancePanel: $("#phase29ReleaseGovernancePanel"),
  draftPreview: $("#draftPreview"),
  guideCopy: $("#guideCopy"),
  agentWorkflow: $("#agentWorkflow"),
  guideAskForm: $("#guideAskForm"),
  guideQuestionInput: $("#guideQuestionInput"),
  guideAskButton: $("#guideAskButton"),
  searchInput: $("#searchInput"),
  sortSelect: $("#sortSelect"),
  emotionFilterSelect: $("#emotionFilterSelect"),
  clearFiltersButton: $("#clearFiltersButton"),
  storageStatus: $("#storageStatus"),
  syncDatabaseButton: $("#syncDatabaseButton"),
  migrateLocalButton: $("#migrateLocalButton"),
  emotionOptions: $("#emotionOptions"),
  emotionIntensity: $("#emotionIntensity"),
  emotionIntensityOutput: $("#emotionIntensityOutput"),
  sourceType: $("#sourceType"),
  importanceSelect: $("#importanceSelect"),
  favoriteInput: $("#favoriteInput"),
  exportButton: $("#exportButton"),
  importFile: $("#importFile"),
  memoryDialog: $("#memoryDialog"),
  dialogContent: $("#dialogContent"),
  closeDialog: $("#closeDialog")
};

function setActiveFeaturePanel(panelId = "home", scrollTargetId = "", shouldScroll = true) {
  const nextPanel = featurePanelIds.includes(panelId) ? panelId : "home";
  activeFeaturePanel = nextPanel;
  featurePanelIds.forEach((id) => {
    const panel = document.getElementById(id);
    if (!panel) return;
    const isVisible = id === nextPanel;
    panel.classList.toggle("is-visible", isVisible);
    panel.setAttribute("aria-hidden", isVisible ? "false" : "true");
  });
  document.querySelectorAll("[data-feature-target]").forEach((button) => {
    button.classList.toggle("is-active", button.dataset.featureTarget === nextPanel);
  });
  const target = document.getElementById(scrollTargetId || (nextPanel === "home" ? "homeGateway" : nextPanel));
  if (shouldScroll && target) target.scrollIntoView({ behavior: "smooth", block: "start" });
}

function scrollHomeTarget(targetId) {
  setActiveFeaturePanel("home", targetId);
}

function createId() {
  return globalThis.crypto?.randomUUID ? globalThis.crypto.randomUUID() : `memory-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}

function normalizeId(value) {
  const id = String(value || "").trim();
  return /^[a-zA-Z0-9_-]{8,80}$/.test(id) ? id : createId();
}

function persistMemories(nextMemories) {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(nextMemories));
    return true;
  } catch {
    return false;
  }
}

function loadLocalProfile() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PROFILE_KEY) || "{}");
    return normalizeLocalProfile(parsed);
  } catch {
    return normalizeLocalProfile({});
  }
}

function normalizeLocalProfile(profile = {}) {
  return {
    deviceId: normalizeId(profile.deviceId || `device-${createId()}`),
    displayName: limitText(profile.displayName, 40) || "本地馆主",
    deviceLabel: limitText(profile.deviceLabel, 60) || "这台设备",
    syncPreference: ["manual-json", "local-only"].includes(profile.syncPreference) ? profile.syncPreference : "manual-json",
    aiConsent: profile.aiConsent === true
  };
}

function persistLocalProfile(profile) {
  localProfile = normalizeLocalProfile(profile);
  try {
    localStorage.setItem(PROFILE_KEY, JSON.stringify(localProfile));
    return true;
  } catch {
    return false;
  }
}

function loadPhase16SyncAudit() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE16_SYNC_AUDIT_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 30) : [];
  } catch {
    return [];
  }
}

function recordPhase16SyncAuditEvent(event = {}) {
  const entry = {
    id: createId(),
    at: new Date().toISOString(),
    phase: 16,
    deviceId: localProfile.deviceId,
    deviceLabel: localProfile.deviceLabel,
    action: event.action || "sync",
    label: limitText(event.label, 80) || "同步事件",
    detail: limitText(event.detail, 180),
    batchId: limitText(event.batchId, 120),
    summary: event.summary || null
  };
  try {
    const current = loadPhase16SyncAudit();
    localStorage.setItem(PHASE16_SYNC_AUDIT_KEY, JSON.stringify([entry, ...current].slice(0, 30)));
  } catch {
    return entry;
  }
  return entry;
}

function filterPhase16SyncAudit(entries = loadPhase16SyncAudit()) {
  if (phase16AuditFilter === "all") return entries;
  const groups = {
    export: ["export"],
    preview: ["preview"],
    drill: ["drill-export"],
    apply: ["apply", "apply-empty"],
    cancel: ["cancel-preview"]
  };
  const actions = groups[phase16AuditFilter] || [];
  return entries.filter((entry) => actions.includes(entry.action));
}

function loadPhase17DeviceRegistry() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE17_DEVICE_REGISTRY_KEY) || "[]");
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
}

function savePhase17DeviceRegistry(devices = []) {
  try {
    localStorage.setItem(PHASE17_DEVICE_REGISTRY_KEY, JSON.stringify(devices.slice(0, 12)));
  } catch {
    return false;
  }
  return true;
}

function normalizePhase17Device(device = {}) {
  const trust = ["trusted", "review", "blocked"].includes(device.status) ? device.status : "review";
  return {
    id: limitText(device.id || createId(), 80),
    label: limitText(device.label || "未命名设备", 80),
    owner: limitText(device.owner || localProfile.displayName || "本地馆主", 80),
    role: limitText(device.role || "device", 40),
    status: trust,
    trustLevel: trust === "trusted" ? "可信" : trust === "blocked" ? "已阻止" : "待复核",
    note: limitText(device.note || "", 120),
    firstSeenAt: device.firstSeenAt || new Date().toISOString(),
    lastSeenAt: device.lastSeenAt || device.firstSeenAt || new Date().toISOString(),
    syncMode: ["manual-json", "local-only"].includes(device.syncMode) ? device.syncMode : "manual-json"
  };
}

function registerPhase17CurrentDevice() {
  const now = new Date().toISOString();
  const current = normalizePhase17Device({
    id: localProfile.deviceId,
    label: localProfile.deviceLabel,
    owner: localProfile.displayName,
    role: "primary-local",
    status: "trusted",
    firstSeenAt: now,
    lastSeenAt: now,
    syncMode: localProfile.syncPreference || "manual-json"
  });
  const devices = loadPhase17DeviceRegistry().map(normalizePhase17Device);
  const existing = devices.find((device) => device.id === current.id);
  const next = existing
    ? devices.map((device) => device.id === current.id ? { ...device, ...current, firstSeenAt: device.firstSeenAt || now } : device)
    : [current, ...devices];
  savePhase17DeviceRegistry(next);
  return next;
}

function addPhase17ReviewDevice() {
  const now = new Date().toISOString();
  const devices = registerPhase17CurrentDevice().map(normalizePhase17Device);
  const reviewCount = devices.filter((device) => device.status === "review").length + 1;
  const device = normalizePhase17Device({
    id: `review-device-${simpleChecksum(`${now}:${reviewCount}`).slice(0, 8)}`,
    label: `待复核设备 ${reviewCount}`,
    owner: localProfile.displayName,
    role: "peer-candidate",
    status: "review",
    note: "手动登记的跨设备同步候选，进入真实交换前需要确认来源。",
    firstSeenAt: now,
    lastSeenAt: now,
    syncMode: "manual-json"
  });
  savePhase17DeviceRegistry([device, ...devices].slice(0, 12));
  return device;
}

function updatePhase17DeviceTrust(id, nextStatus) {
  if (!["trusted", "review", "blocked"].includes(nextStatus)) return false;
  const devices = registerPhase17CurrentDevice().map(normalizePhase17Device);
  let changed = false;
  const next = devices.map((device) => {
    if (device.id !== id || device.id === localProfile.deviceId) return device;
    changed = true;
    return {
      ...device,
      status: nextStatus,
      trustLevel: nextStatus === "trusted" ? "可信" : nextStatus === "blocked" ? "已阻止" : "待复核",
      lastSeenAt: new Date().toISOString()
    };
  });
  if (changed) savePhase17DeviceRegistry(next);
  return changed;
}

function buildPhase17DeviceTrustPolicy(devices = []) {
  const normalized = devices.map(normalizePhase17Device);
  return {
    mode: "explicit-trust-required",
    trusted: normalized.filter((device) => device.status === "trusted").length,
    review: normalized.filter((device) => device.status === "review").length,
    blocked: normalized.filter((device) => device.status === "blocked").length,
    rules: [
      "本机设备自动可信，但不能代表其他设备。",
      "历史候选设备默认进入待复核，不参与自动导入。",
      "已阻止设备不会出现在局域网握手候选里。",
      "任何跨设备写入仍需第十六阶段风险确认。"
    ]
  };
}

function loadPhase17SyncQueue() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE17_SYNC_QUEUE_KEY) || "[]");
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
}

function savePhase17SyncQueue(queue = []) {
  try {
    localStorage.setItem(PHASE17_SYNC_QUEUE_KEY, JSON.stringify(queue.slice(0, 40)));
  } catch {
    return false;
  }
  return true;
}

function normalizePhase17QueueTask(task = {}) {
  const now = new Date().toISOString();
  const allowedStatuses = ["queued", "reviewing", "ready", "exported", "imported", "failed", "resolved", "cancelled"];
  const allowedChannels = ["manual-json", "lan-bridge", "private-cloud"];
  const status = allowedStatuses.includes(task.status) ? task.status : "queued";
  const channel = allowedChannels.includes(task.channel) ? task.channel : "manual-json";
  return {
    id: task.id || createId(),
    phase: 17,
    status,
    channel,
    action: limitText(task.action || "sync-preview", 60),
    label: limitText(task.label, 80) || "同步任务",
    detail: limitText(task.detail, 180),
    risk: ["low", "review", "blocked"].includes(task.risk) ? task.risk : (channel === "manual-json" ? "low" : "review"),
    batchId: limitText(task.batchId || "", 80),
    deviceId: task.deviceId || localProfile.deviceId,
    deviceLabel: task.deviceLabel || localProfile.deviceLabel,
    createdAt: task.createdAt || now,
    updatedAt: task.updatedAt || task.createdAt || now
  };
}

function enqueuePhase17SyncTask(task = {}) {
  const entry = normalizePhase17QueueTask(task);
  const queue = [entry, ...loadPhase17SyncQueue().map(normalizePhase17QueueTask)].slice(0, 40);
  savePhase17SyncQueue(queue);
  return entry;
}

function updatePhase17QueueTaskStatus(id, nextStatus) {
  const allowedStatuses = ["reviewing", "ready", "exported", "imported", "failed", "resolved", "cancelled"];
  if (!allowedStatuses.includes(nextStatus)) return false;
  const queue = loadPhase17SyncQueue().map(normalizePhase17QueueTask);
  const next = queue.map((item) => item.id === id ? { ...item, status: nextStatus, updatedAt: new Date().toISOString() } : item);
  savePhase17SyncQueue(next);
  return next.some((item) => item.id === id);
}

function buildPhase17QueueMetrics(queue = loadPhase17SyncQueue()) {
  const normalized = queue.map(normalizePhase17QueueTask);
  const byStatus = normalized.reduce((acc, item) => {
    acc[item.status] = (acc[item.status] || 0) + 1;
    return acc;
  }, {});
  const byChannel = normalized.reduce((acc, item) => {
    acc[item.channel] = (acc[item.channel] || 0) + 1;
    return acc;
  }, {});
  return {
    total: normalized.length,
    pending: normalized.filter((item) => ["queued", "reviewing", "ready"].includes(item.status)).length,
    failures: byStatus.failed || 0,
    resolved: (byStatus.resolved || 0) + (byStatus.imported || 0),
    byStatus,
    byChannel,
    recent: normalized.slice(0, 8)
  };
}

function getPhase17QueueStatusLabel(status = "queued") {
  const labels = {
    queued: "等待处理",
    reviewing: "复核中",
    ready: "可同步",
    exported: "已导出",
    imported: "已导入",
    failed: "失败",
    resolved: "已完成",
    cancelled: "已取消"
  };
  return labels[status] || status;
}

function getPhase17QueueNextActions(status = "queued") {
  if (status === "queued") {
    return [
      { status: "reviewing", label: "复核" },
      { status: "failed", label: "标记失败" }
    ];
  }
  if (status === "reviewing") {
    return [
      { status: "ready", label: "通过" },
      { status: "cancelled", label: "取消" }
    ];
  }
  if (status === "ready") {
    return [
      { status: "exported", label: "已导出" },
      { status: "failed", label: "失败" }
    ];
  }
  if (status === "exported" || status === "imported" || status === "failed") {
    return [{ status: "resolved", label: "完成" }];
  }
  return [];
}

function buildPhase17LanHandshake(devices = []) {
  const localFingerprint = simpleChecksum(`${localProfile.deviceId}:${localProfile.deviceLabel}:${localProfile.displayName}`).slice(0, 12);
  const peerCandidates = devices.filter((device) => device.id !== localProfile.deviceId && device.status !== "blocked").slice(0, 3);
  return {
    channel: "lan-bridge",
    status: peerCandidates.length ? "review" : "standby",
    mode: "read-only-handshake-simulation",
    localEndpoint: "browser-localhost",
    localFingerprint,
    peerCandidates: peerCandidates.map((device) => ({
      id: device.id,
      label: device.label,
      status: device.status,
      fingerprint: simpleChecksum(`${device.id}:${device.label}`).slice(0, 12)
    })),
    checks: [
      { id: "same-network", label: "同网段发现", status: "simulated", detail: "当前只展示握手模型，不扫描局域网。" },
      { id: "data-transfer", label: "私人数据传输", status: "blocked", detail: "第二版不传输展品正文、附件或专题资产。" },
      { id: "manual-review", label: "人工确认", status: "required", detail: "进入真实交换前必须回到第十六阶段风险确认。" }
    ]
  };
}

function loadPhase17PrivateCloudBoundary() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE17_PRIVATE_CLOUD_KEY) || "{}");
    return parsed && typeof parsed === "object" ? parsed : {};
  } catch {
    return {};
  }
}

function savePhase17PrivateCloudBoundary(config = {}) {
  try {
    localStorage.setItem(PHASE17_PRIVATE_CLOUD_KEY, JSON.stringify(config));
  } catch {
    return false;
  }
  return true;
}

function createPhase17PrivateCloudDraft() {
  const draft = {
    provider: "webdav-or-s3",
    endpoint: "not-configured",
    enabled: false,
    credentialStored: false,
    createdAt: new Date().toISOString(),
    note: "第五版只保存适配边界草案，不保存密钥，不连接云端。"
  };
  savePhase17PrivateCloudBoundary(draft);
  return draft;
}

function clearPhase17PrivateCloudDraft() {
  savePhase17PrivateCloudBoundary({});
}

function buildPhase17PrivateCloudBoundary() {
  const draft = loadPhase17PrivateCloudBoundary();
  const hasDraft = Boolean(draft.provider);
  return {
    channel: "private-cloud",
    status: hasDraft ? "draft" : "not-configured",
    provider: draft.provider || "none",
    endpoint: draft.endpoint || "not-configured",
    enabled: false,
    credentialStored: false,
    policy: "configuration-boundary-only",
    checks: [
      { id: "auto-upload", label: "自动上传", status: "blocked", detail: "第五版不允许任何自动上传。" },
      { id: "credential", label: "凭据保存", status: "blocked", detail: "本地不保存 WebDAV/S3/API 密钥。" },
      { id: "manual-export", label: "手动导出", status: "required", detail: "需要先生成第十六阶段同步包并人工确认。" },
      { id: "redaction", label: "脱敏边界", status: "recommended", detail: "外部通道优先使用脱敏导出包。" }
    ],
    updatedAt: draft.createdAt || null
  };
}

function buildPhase17SyncHealth({ devices = [], queueMetrics = {}, lanHandshake = {}, deviceTrustPolicy = {}, privateCloudBoundary = {} } = {}) {
  const failedTasks = queueMetrics.failures || 0;
  const reviewDevices = deviceTrustPolicy.review || 0;
  const blockedDevices = deviceTrustPolicy.blocked || 0;
  const pendingTasks = queueMetrics.pending || 0;
  const checks = [
    { id: "phase16-risk-gate", label: "第十六阶段风险门禁", status: "ready", detail: "冲突决策、恢复演练和导入前确认继续保留。" },
    { id: "device-trust", label: "设备信任", status: reviewDevices ? "review" : "ready", detail: reviewDevices ? `${reviewDevices} 台设备仍需复核。` : "暂无待复核设备。" },
    { id: "blocked-device", label: "阻止设备", status: blockedDevices ? "review" : "ready", detail: blockedDevices ? `${blockedDevices} 台设备已阻止，不参与握手。` : "暂无已阻止设备。" },
    { id: "queue-failures", label: "失败任务", status: failedTasks ? "review" : "ready", detail: failedTasks ? `${failedTasks} 个任务需要处理。` : "暂无失败任务。" },
    { id: "queue-pending", label: "待处理任务", status: pendingTasks > 8 ? "review" : "ready", detail: pendingTasks ? `${pendingTasks} 个任务等待推进。` : "队列当前清爽。" },
    { id: "lan-handshake", label: "局域网握手", status: lanHandshake.status === "review" ? "review" : "ready", detail: "只读握手模拟已具备，不传输私人数据。" },
    { id: "private-cloud-boundary", label: "私有云边界", status: privateCloudBoundary.status === "draft" ? "review" : "ready", detail: privateCloudBoundary.status === "draft" ? "已有草案，仍保持关闭。" : "未配置外部通道。" }
  ];
  const reviewCount = checks.filter((item) => item.status === "review").length;
  const readyCount = checks.filter((item) => item.status === "ready").length;
  const score = Math.round((readyCount / checks.length) * 100);
  return {
    status: reviewCount ? "review" : "ready",
    score,
    deviceCount: devices.length,
    pendingTasks,
    failedTasks,
    checks,
    recommendation: reviewCount
      ? "继续留在第十七阶段处理设备复核、队列失败和外部通道边界。"
      : "第十七阶段同步适配层已具备进入第十八阶段评估的基础。"
  };
}

function buildPhase17HealthExplanation(syncHealth = {}) {
  const checks = Array.isArray(syncHealth.checks) ? syncHealth.checks : [];
  const reviewChecks = checks.filter((item) => item.status === "review");
  return {
    score: syncHealth.score || 0,
    reviewCount: reviewChecks.length,
    reasons: reviewChecks.map((item) => ({
      id: item.id,
      label: item.label,
      impact: item.id === "queue-failures" ? "优先处理失败任务，避免下一次手动同步重复出错。"
        : item.id === "device-trust" ? "确认设备来源后再交换同步包。"
          : item.id === "private-cloud-boundary" ? "外部通道仍保持关闭，先使用手动同步包。"
            : item.detail
    })),
    summary: reviewChecks.length
      ? `健康度有 ${reviewChecks.length} 项需要复核。`
      : "同步健康度稳定，可以继续作为第十八阶段长期助理的同步底座。"
  };
}

function buildPhase17FailureRecovery(queue = loadPhase17SyncQueue()) {
  const failed = queue.map(normalizePhase17QueueTask).filter((item) => item.status === "failed");
  return {
    failedCount: failed.length,
    actions: failed.slice(0, 5).map((item) => ({
      id: item.id,
      label: item.label,
      action: item.channel === "private-cloud" ? "保持外部通道关闭，改用手动 JSON 同步包重试。"
        : item.channel === "lan-bridge" ? "重新生成只读握手，确认设备后再导出同步包。"
          : "重新导出第十六阶段同步包，并重新做导入前风险确认。",
      risk: item.risk
    })),
    recommendation: failed.length
      ? "先逐条完成或取消失败任务，再继续添加新的同步任务。"
      : "暂无失败任务；可保留当前同步节奏。"
  };
}

function buildPhase17Phase18SyncAdvisory() {
  const collection = getAssetCollection();
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reports = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const phase18Tasks = loadPhase18TaskQueue();
  const repairDrafts = loadPhase18RepairDrafts();
  const assetCount = exhibitions.length + reports.length;
  const previewDrafts = repairDrafts.filter((item) => item.status === "preview").length;
  return {
    assetCount,
    exhibitions: exhibitions.length,
    reports: reports.length,
    activePhase18Tasks: phase18Tasks.filter((item) => ["queued", "reviewing", "failed"].includes(item.status)).length,
    previewDrafts,
    readiness: previewDrafts ? "review" : assetCount ? "asset-ready" : "waiting",
    recommendation: previewDrafts
      ? "仍有第十八阶段修复草案未应用，建议先复核后再同步资产。"
      : assetCount
        ? "专题展和报告草稿可进入第十六阶段同步包预览，但仍不静默合并。"
        : "第十八阶段还没有可同步的专题资产或报告草稿。"
  };
}

function buildPhase18AssetSyncState(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reports = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const phase18Tasks = loadPhase18TaskQueue();
  const repairDrafts = loadPhase18RepairDrafts();
  const previewDrafts = repairDrafts.filter((item) => item.status === "preview").length;
  const activeTasks = phase18Tasks.filter((item) => ["queued", "reviewing", "failed"].includes(item.status)).length;
  const buildItem = (type, item) => {
    const issues = getAssetIssueList(type, item);
    const linkCount = type === "exhibition"
      ? (Array.isArray(item.memoryIds) ? item.memoryIds.length : 0)
      : (Array.isArray(item.references) ? item.references.length : 0);
    const status = item.status || "draft";
    const syncStatus = issues.length ? "risk" : status === "published" ? "ready" : status === "archived" ? "archived" : "review";
    return {
      id: item.id,
      type,
      title: item.title || (type === "exhibition" ? "未命名专题展" : "未命名报告草稿"),
      status,
      syncStatus,
      linkCount,
      issueCount: issues.length,
      includedInManualPackage: ["ready", "review"].includes(syncStatus),
      recommendation: issues.length
        ? "先修复资产字段和引用问题，再加入手动同步包。"
        : status === "published"
          ? "适合加入第十六阶段手动同步包，并交由第十七阶段设备复核。"
          : "可加入同步包预览，但建议先复核发布状态。"
    };
  };
  const items = [
    ...exhibitions.map((item) => buildItem("exhibition", item)),
    ...reports.map((item) => buildItem("report", item))
  ];
  const ready = items.filter((item) => item.syncStatus === "ready").length;
  const review = items.filter((item) => item.syncStatus === "review").length;
  const risk = items.filter((item) => item.syncStatus === "risk").length;
  const packageCandidates = items.filter((item) => item.includedInManualPackage).length;
  return {
    phase: 18,
    mode: "phase17-manual-sync-link",
    total: items.length,
    exhibitions: exhibitions.length,
    reports: reports.length,
    ready,
    review,
    risk,
    packageCandidates,
    activePhase18Tasks: activeTasks,
    previewDrafts,
    items: items.slice(0, 8),
    readiness: previewDrafts || risk ? "review" : items.length ? "asset-ready" : "waiting",
    recommendation: previewDrafts
      ? "仍有第十八阶段修复草案未应用，建议先复核后再同步资产。"
      : risk
        ? "部分资产存在字段或引用风险，建议先修复再加入同步包。"
        : items.length
          ? "专题展和报告草稿可进入第十六阶段同步包预览，并由第十七阶段设备复核。"
          : "第十八阶段还没有可同步的专题资产或报告草稿。"
  };
}

function buildPhase17Phase18SyncAdvisory() {
  const assetSyncState = buildPhase18AssetSyncState();
  return {
    assetCount: assetSyncState.total,
    exhibitions: assetSyncState.exhibitions,
    reports: assetSyncState.reports,
    activePhase18Tasks: assetSyncState.activePhase18Tasks,
    previewDrafts: assetSyncState.previewDrafts,
    readiness: assetSyncState.readiness,
    packageCandidates: assetSyncState.packageCandidates,
    riskAssets: assetSyncState.risk,
    readyAssets: assetSyncState.ready,
    assetSyncState,
    recommendation: assetSyncState.recommendation
  };
}

function loadPhase18AgentFeedback() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE18_AGENT_FEEDBACK_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 40) : [];
  } catch {
    return [];
  }
}

function savePhase18AgentFeedback(entries = []) {
  try {
    localStorage.setItem(PHASE18_AGENT_FEEDBACK_KEY, JSON.stringify(entries.slice(0, 40)));
  } catch {
    return false;
  }
  return true;
}

function recordPhase18AgentFeedback(action, targetId, label = "") {
  const entry = {
    id: createId(),
    phase: 18,
    action: ["accepted", "dismissed"].includes(action) ? action : "accepted",
    targetId: limitText(targetId, 100),
    label: limitText(label, 120),
    at: new Date().toISOString()
  };
  savePhase18AgentFeedback([entry, ...loadPhase18AgentFeedback()]);
  return entry;
}

function loadPhase18TaskAudit() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE18_TASK_AUDIT_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 120) : [];
  } catch {
    return [];
  }
}

function savePhase18TaskAudit(entries = []) {
  try {
    localStorage.setItem(PHASE18_TASK_AUDIT_KEY, JSON.stringify(entries.slice(0, 120)));
  } catch {
    return false;
  }
  return true;
}

function recordPhase18TaskAudit(action, targetId, detail = "", meta = {}) {
  const entry = {
    id: createId(),
    phase: 18,
    action: limitText(action, 80),
    targetId: limitText(targetId, 120),
    detail: limitText(detail, 220),
    meta,
    at: new Date().toISOString()
  };
  savePhase18TaskAudit([entry, ...loadPhase18TaskAudit()]);
  return entry;
}

function loadPhase18RepairDrafts() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE18_REPAIR_DRAFT_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 80) : [];
  } catch {
    return [];
  }
}

function savePhase18RepairDrafts(drafts = []) {
  try {
    localStorage.setItem(PHASE18_REPAIR_DRAFT_KEY, JSON.stringify(drafts.slice(0, 80)));
  } catch {
    return false;
  }
  return true;
}

function buildPhase18RepairDraftsForSuggestion(suggestionId) {
  const targets = getPhase18SuggestionTargets(suggestionId);
  const now = new Date().toISOString();
  return targets.map((memory) => {
    const patch = buildPhase18RepairPatch(suggestionId, memory);
    return {
      id: `repair-${suggestionId}-${memory.id}`,
      phase: 18,
      suggestionId,
      memoryId: memory.id,
      memoryTitle: memory.title,
      status: "preview",
      patch,
      reason: getPhase18RepairReason(suggestionId),
      createdAt: now,
      updatedAt: now
    };
  }).filter((draft) => Object.keys(draft.patch).length > 0);
}

function getPhase18SuggestionTargets(suggestionId) {
  const normalized = memories.map(normalizeMemory);
  if (suggestionId === "missing-date") return normalized.filter((memory) => !memory.date);
  if (suggestionId === "missing-people") return normalized.filter((memory) => !memory.people?.length);
  if (suggestionId === "missing-location") return normalized.filter((memory) => !memory.location);
  if (suggestionId === "missing-tags") return normalized.filter((memory) => !memory.tags?.length);
  if (suggestionId === "high-value-media") {
    return normalized.filter((memory) => (memory.favorite || memory.importance >= 4) && !memory.coverImage && !memory.mediaNote && !normalizeAttachments(memory.attachments).length);
  }
  return [];
}

function buildPhase18RepairPatch(suggestionId, memory) {
  if (suggestionId === "missing-date") {
    const fallbackDate = String(memory.createdAt || new Date().toISOString()).slice(0, 10);
    return fallbackDate ? { date: fallbackDate } : {};
  }
  if (suggestionId === "missing-people") return { people: ["待确认人物"] };
  if (suggestionId === "missing-location") return { location: "待确认地点" };
  if (suggestionId === "missing-tags") {
    const baseTag = memory.hall ? getHallName(memory.hall).replace("展厅", "") : "待整理";
    return { tags: Array.from(new Set([...(memory.tags || []), baseTag, "待复核"].filter(Boolean))).slice(0, fieldLimits.listLength) };
  }
  if (suggestionId === "high-value-media") return { mediaNote: "待补充图片、OCR、语音或附件线索。" };
  return {};
}

function getPhase18RepairReason(suggestionId) {
  const reasons = {
    "missing-date": "使用创建时间作为时间线草案，应用后仍建议人工复核。",
    "missing-people": "补入占位人物，提醒后续人工替换为真实人物。",
    "missing-location": "补入占位地点，提醒后续人工替换为真实地点。",
    "missing-tags": "根据展厅补充轻量标签，并标记待复核。",
    "high-value-media": "为重点展品添加多模态补充提示，不生成虚假附件。"
  };
  return reasons[suggestionId] || "长期助理生成的修复草案，需要人工确认。";
}

function queuePhase18RepairDrafts(suggestionId) {
  const drafts = buildPhase18RepairDraftsForSuggestion(suggestionId);
  if (!drafts.length) return [];
  const existing = loadPhase18RepairDrafts();
  const existingIds = new Set(existing.map((item) => item.id));
  const next = [...drafts.filter((draft) => !existingIds.has(draft.id)), ...existing];
  savePhase18RepairDrafts(next);
  recordPhase18TaskAudit("repair-drafts-generated", suggestionId, `生成 ${drafts.length} 条修复草案。`, {
    draftCount: drafts.length,
    fields: Array.from(new Set(drafts.flatMap((draft) => Object.keys(draft.patch || {}))))
  });
  return drafts;
}

function applyPhase18RepairDraft(draftId, options = {}) {
  const drafts = loadPhase18RepairDrafts();
  const draft = drafts.find((item) => item.id === draftId);
  if (!draft || draft.status === "applied") return false;
  const index = memories.findIndex((memory) => memory.id === draft.memoryId);
  if (index < 0) return false;
  const nextMemory = normalizeMemory({
    ...memories[index],
    ...draft.patch,
    updatedAt: new Date().toISOString()
  });
  memories = memories.map((memory) => memory.id === nextMemory.id ? nextMemory : memory);
  saveMemories(memories);
  savePhase18RepairDrafts(drafts.map((item) => item.id === draftId ? { ...item, status: "applied", updatedAt: new Date().toISOString() } : item));
  if (!options.skipAudit) {
    recordPhase18TaskAudit("repair-draft-applied", draftId, `应用到《${draft.memoryTitle || draft.memoryId}》。`, {
      memoryId: draft.memoryId,
      fields: Object.keys(draft.patch || {})
    });
  }
  return true;
}

function clearPhase18RepairDrafts() {
  const total = loadPhase18RepairDrafts().length;
  savePhase18RepairDrafts([]);
  recordPhase18TaskAudit("repair-drafts-cleared", "all", `清空 ${total} 条修复草案。`, { total });
}

function buildPhase18RepairBatchReview(drafts = loadPhase18RepairDrafts()) {
  const previewDrafts = drafts.filter((draft) => draft.status === "preview");
  const fields = countValues(previewDrafts.flatMap((draft) => Object.keys(draft.patch || {})));
  return {
    ready: previewDrafts.length > 0,
    previewCount: previewDrafts.length,
    targetCount: new Set(previewDrafts.map((draft) => draft.memoryId)).size,
    fields: toTopEntries(fields, 8),
    highRiskCount: previewDrafts.filter((draft) => Object.values(draft.patch || {}).some((value) => String(Array.isArray(value) ? value.join(" ") : value).includes("待确认"))).length,
    recent: previewDrafts.slice(0, 5).map((draft) => ({
      id: draft.id,
      memoryTitle: draft.memoryTitle || draft.memoryId,
      fields: Object.keys(draft.patch || {}),
      reason: draft.reason || ""
    })),
    policy: "逐条预览，批量应用前需要浏览器确认；所有变更写入本地审计记录。"
  };
}

function applyPhase18RepairDraftBatch() {
  const drafts = loadPhase18RepairDrafts().filter((draft) => draft.status === "preview");
  let applied = 0;
  drafts.forEach((draft) => {
    if (applyPhase18RepairDraft(draft.id, { skipAudit: true })) applied += 1;
  });
  recordPhase18TaskAudit("repair-drafts-batch-applied", "preview-drafts", `批量应用 ${applied} / ${drafts.length} 条修复草案。`, {
    requested: drafts.length,
    applied,
    fields: Array.from(new Set(drafts.flatMap((draft) => Object.keys(draft.patch || {}))))
  });
  return applied;
}

function loadPhase18TaskQueue() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE18_TASK_QUEUE_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.map(normalizePhase18Task).slice(0, 80) : [];
  } catch {
    return [];
  }
}

function savePhase18TaskQueue(tasks = []) {
  try {
    localStorage.setItem(PHASE18_TASK_QUEUE_KEY, JSON.stringify(tasks.map(normalizePhase18Task).slice(0, 80)));
  } catch {
    return false;
  }
  return true;
}

function normalizePhase18Task(task = {}) {
  const now = new Date().toISOString();
  const statuses = ["queued", "reviewing", "applied", "dismissed", "failed"];
  const status = statuses.includes(task.status) ? task.status : "queued";
  return {
    id: limitText(task.id || createId(), 120),
    phase: 18,
    status,
    label: limitText(task.label || "长期助理任务", 120),
    source: limitText(task.source || "suggestion", 60),
    priority: ["high", "medium", "low"].includes(task.priority) ? task.priority : "medium",
    memoryIds: Array.isArray(task.memoryIds) ? task.memoryIds.slice(0, 12).map((id) => limitText(id, 100)) : [],
    detail: limitText(task.detail || "", 180),
    createdAt: task.createdAt || now,
    updatedAt: task.updatedAt || task.createdAt || now
  };
}

function syncPhase18TaskQueue(suggestions = []) {
  const existing = loadPhase18TaskQueue();
  const existingIds = new Set(existing.map((task) => task.id));
  const generated = suggestions.slice(0, 12).map((item) => normalizePhase18Task({
    id: `phase18-task-${item.id}`,
    status: item.priority === "high" ? "reviewing" : "queued",
    label: item.title,
    source: item.type,
    priority: item.priority,
    memoryIds: item.memoryIds || [],
    detail: item.detail
  })).filter((task) => !existingIds.has(task.id));
  const next = [...generated, ...existing].slice(0, 80);
  if (generated.length) savePhase18TaskQueue(next);
  return next;
}

function updatePhase18TaskStatus(taskId, nextStatus) {
  if (!["queued", "reviewing", "applied", "dismissed", "failed"].includes(nextStatus)) return false;
  const tasks = loadPhase18TaskQueue();
  let changed = false;
  let previousStatus = "";
  let targetLabel = "";
  const next = tasks.map((task) => {
    if (task.id !== taskId) return task;
    changed = true;
    previousStatus = task.status;
    targetLabel = task.label;
    return { ...task, status: nextStatus, updatedAt: new Date().toISOString() };
  });
  if (changed) {
    savePhase18TaskQueue(next);
    recordPhase18TaskAudit("task-status-changed", taskId, `${targetLabel}：${getPhase18TaskStatusLabel(previousStatus)} -> ${getPhase18TaskStatusLabel(nextStatus)}`, {
      from: previousStatus,
      to: nextStatus
    });
  }
  return changed;
}

function clearResolvedPhase18Tasks() {
  const tasks = loadPhase18TaskQueue();
  const next = tasks.filter((task) => !["applied", "dismissed"].includes(task.status));
  savePhase18TaskQueue(next);
  recordPhase18TaskAudit("resolved-tasks-cleared", "task-queue", `清理 ${tasks.length - next.length} 条已完成或已忽略任务。`, {
    cleared: tasks.length - next.length
  });
}

function prunePhase18TaskSelection(tasks = loadPhase18TaskQueue()) {
  const validIds = new Set(tasks.map((task) => task.id));
  phase18SelectedTaskIds = new Set(Array.from(phase18SelectedTaskIds).filter((id) => validIds.has(id)));
  return phase18SelectedTaskIds;
}

function togglePhase18TaskSelection(taskId, selected) {
  if (!taskId) return phase18SelectedTaskIds;
  if (selected) {
    phase18SelectedTaskIds.add(taskId);
  } else {
    phase18SelectedTaskIds.delete(taskId);
  }
  return phase18SelectedTaskIds;
}

function selectVisiblePhase18Tasks(tasks = loadPhase18TaskQueue()) {
  filterPhase18Tasks(tasks).slice(0, 10).forEach((task) => phase18SelectedTaskIds.add(task.id));
  return phase18SelectedTaskIds;
}

function clearPhase18TaskSelection() {
  phase18SelectedTaskIds = new Set();
  return phase18SelectedTaskIds;
}

function getPhase18SuggestionIdFromTask(task = {}) {
  const id = String(task.id || "");
  if (id.startsWith("phase18-task-")) return id.replace(/^phase18-task-/, "");
  return "";
}

function buildPhase18TaskBatchReview(tasks = loadPhase18TaskQueue(), selectedIds = Array.from(phase18SelectedTaskIds)) {
  const selectedSet = new Set(selectedIds);
  const selected = tasks.map(normalizePhase18Task).filter((task) => selectedSet.has(task.id));
  const statusCounts = selected.reduce((acc, task) => {
    acc[task.status] = (acc[task.status] || 0) + 1;
    return acc;
  }, {});
  const sources = Array.from(new Set(selected.map((task) => task.source).filter(Boolean)));
  const memoryIds = Array.from(new Set(selected.flatMap((task) => task.memoryIds || [])));
  const suggestionIds = Array.from(new Set(selected.map(getPhase18SuggestionIdFromTask).filter(Boolean)));
  const draftableSuggestions = suggestionIds.filter((id) => buildPhase18RepairDraftsForSuggestion(id).length);
  return {
    selectedCount: selected.length,
    activeCount: selected.filter((task) => ["queued", "reviewing", "failed"].includes(task.status)).length,
    memoryCount: memoryIds.length,
    sources,
    statusCounts,
    draftableSuggestions,
    draftableCount: draftableSuggestions.length,
    canBatch: selected.length > 0,
    recommendation: selected.length
      ? `已选择 ${selected.length} 条任务，可批量流转状态或生成 ${draftableSuggestions.length} 组修复草案。`
      : "先选择任务，再进行批量复核、完成、忽略、失败或生成修复草案。"
  };
}

function updatePhase18SelectedTasksStatus(nextStatus) {
  if (!["queued", "reviewing", "applied", "dismissed", "failed"].includes(nextStatus)) return { changed: 0 };
  const tasks = loadPhase18TaskQueue();
  prunePhase18TaskSelection(tasks);
  const selected = new Set(phase18SelectedTaskIds);
  let changed = 0;
  const statusBefore = {};
  const next = tasks.map((task) => {
    if (!selected.has(task.id) || task.status === nextStatus) return task;
    changed += 1;
    statusBefore[task.status] = (statusBefore[task.status] || 0) + 1;
    return { ...task, status: nextStatus, updatedAt: new Date().toISOString() };
  });
  if (changed) {
    savePhase18TaskQueue(next);
    recordPhase18TaskAudit("task-batch-status-changed", "selected-tasks", `批量更新 ${changed} 条长期任务为 ${getPhase18TaskStatusLabel(nextStatus)}。`, {
      changed,
      to: nextStatus,
      from: statusBefore,
      selected: Array.from(selected).slice(0, 20)
    });
  }
  clearPhase18TaskSelection();
  return { changed, status: nextStatus };
}

function queuePhase18RepairDraftsForSelectedTasks() {
  const tasks = loadPhase18TaskQueue();
  prunePhase18TaskSelection(tasks);
  const selected = tasks.filter((task) => phase18SelectedTaskIds.has(task.id));
  const suggestionIds = Array.from(new Set(selected.map(getPhase18SuggestionIdFromTask).filter(Boolean)));
  const drafts = suggestionIds.flatMap((id) => buildPhase18RepairDraftsForSuggestion(id));
  if (!drafts.length) return { generated: 0, suggestionCount: suggestionIds.length };
  const existing = loadPhase18RepairDrafts();
  const existingIds = new Set(existing.map((item) => item.id));
  const fresh = drafts.filter((draft) => !existingIds.has(draft.id));
  savePhase18RepairDrafts([...fresh, ...existing]);
  recordPhase18TaskAudit("task-batch-repair-drafts-generated", "selected-tasks", `从 ${selected.length} 条任务批量生成 ${fresh.length} 条修复草案。`, {
    selectedTasks: selected.length,
    suggestionCount: suggestionIds.length,
    generated: fresh.length,
    fields: Array.from(new Set(fresh.flatMap((draft) => Object.keys(draft.patch || {}))))
  });
  clearPhase18TaskSelection();
  return { generated: fresh.length, suggestionCount: suggestionIds.length };
}

function createPhase18ReviewDashboardTask() {
  const agent = buildPhase18LongTermAgent();
  const dashboard = agent.reviewDashboard || {};
  const task = normalizePhase18Task({
    id: `phase18-review-dashboard-${simpleChecksum(`${dashboard.focus || ""}:${dashboard.latestAudit || ""}`)}`,
    status: "reviewing",
    label: dashboard.focus || "长期助理复盘任务",
    source: "review-dashboard",
    priority: dashboard.quietSuggestions || dashboard.unresolvedDrafts ? "high" : "medium",
    memoryIds: [],
    detail: dashboard.recommendation || "根据长期助理复盘结果生成的下一步任务。"
  });
  const tasks = loadPhase18TaskQueue();
  const exists = tasks.some((item) => item.id === task.id);
  savePhase18TaskQueue(exists ? tasks.map((item) => item.id === task.id ? { ...item, ...task, updatedAt: new Date().toISOString() } : item) : [task, ...tasks]);
  recordPhase18TaskAudit("review-dashboard-task-created", task.id, `由复盘面板生成任务：${task.label}`, {
    priority: task.priority,
    focus: dashboard.focus || ""
  });
  return task;
}

function createPhase18DigestTask() {
  const agent = buildPhase18LongTermAgent();
  const digest = agent.agentDigest || {};
  const signalKey = `${digest.todayFocus || ""}:${digest.weeklyFocus || ""}:${digest.recommendation || ""}`;
  const task = normalizePhase18Task({
    id: `phase18-agent-digest-${simpleChecksum(signalKey)}`,
    status: "reviewing",
    label: digest.todayFocus || "长期助理摘要任务",
    source: "agent-digest",
    priority: digest.readiness === "needs-attention" ? "high" : "medium",
    memoryIds: Array.from(new Set((digest.topActions || []).flatMap((item) => item.memoryIds || []))).slice(0, 8),
    detail: digest.recommendation || "根据长期助理每日/每周摘要生成的跟进任务。"
  });
  const tasks = loadPhase18TaskQueue();
  const exists = tasks.some((item) => item.id === task.id);
  savePhase18TaskQueue(exists ? tasks.map((item) => item.id === task.id ? { ...item, ...task, updatedAt: new Date().toISOString() } : item) : [task, ...tasks]);
  recordPhase18TaskAudit("agent-digest-task-created", task.id, `由长期助理摘要生成任务：${task.label}`, {
    priority: task.priority,
    readiness: digest.readiness || "unknown"
  });
  return task;
}

function buildPhase18TaskMetrics(tasks = []) {
  const normalized = tasks.map(normalizePhase18Task);
  const byStatus = normalized.reduce((acc, task) => {
    acc[task.status] = (acc[task.status] || 0) + 1;
    return acc;
  }, {});
  const active = normalized.filter((task) => ["queued", "reviewing", "failed"].includes(task.status));
  return {
    total: normalized.length,
    active: active.length,
    queued: byStatus.queued || 0,
    reviewing: byStatus.reviewing || 0,
    applied: byStatus.applied || 0,
    dismissed: byStatus.dismissed || 0,
    failed: byStatus.failed || 0,
    byStatus,
    recent: normalized.slice(0, 12)
  };
}

function filterPhase18Tasks(tasks = [], filter = phase18TaskFilter) {
  const normalized = tasks.map(normalizePhase18Task);
  if (filter === "all") return normalized;
  if (filter === "active") return normalized.filter((task) => ["queued", "reviewing", "failed"].includes(task.status));
  return normalized.filter((task) => task.status === filter);
}

function getPhase18TaskStatusLabel(status = "queued") {
  const labels = {
    queued: "等待中",
    reviewing: "复核中",
    applied: "已应用",
    dismissed: "已忽略",
    failed: "失败"
  };
  return labels[status] || status;
}

function getPhase18TaskNextActions(status = "queued") {
  if (status === "queued") return [{ status: "reviewing", label: "复核" }, { status: "dismissed", label: "忽略" }];
  if (status === "reviewing") return [{ status: "applied", label: "完成" }, { status: "failed", label: "失败" }];
  if (status === "failed") return [{ status: "reviewing", label: "重试" }, { status: "dismissed", label: "忽略" }];
  return [];
}

function buildPhase18LongTermAgent(sourceMemories = memories) {
  const structured = sourceMemories.map(normalizeMemory);
  const feedback = loadPhase18AgentFeedback();
  const repairDrafts = loadPhase18RepairDrafts();
  const taskAudit = loadPhase18TaskAudit();
  const repairBatchReview = buildPhase18RepairBatchReview(repairDrafts);
  const suggestions = buildPhase18ProactiveSuggestions(structured);
  const relationships = buildPhase18RelationshipMap(structured);
  const periodicReviews = enrichPhase18PeriodicReviewsWithAssets(buildPhase18PeriodicReviews(structured));
  const taskQueue = syncPhase18TaskQueue(suggestions);
  prunePhase18TaskSelection(taskQueue);
  const taskMetrics = buildPhase18TaskMetrics(taskQueue);
  const taskBatchReview = buildPhase18TaskBatchReview(taskQueue);
  const agentQuality = buildPhase18AgentQuality(feedback, taskMetrics, repairDrafts, taskAudit);
  const suggestionNoise = buildPhase18SuggestionNoisePolicy(suggestions, feedback);
  const assetSyncState = buildPhase18AssetSyncState();
  const reviewDashboard = buildPhase18ReviewDashboard({ suggestions, relationships, periodicReviews, taskMetrics, agentQuality, repairDrafts, taskAudit, suggestionNoise });
  const agentDigest = buildPhase18AgentDigest({ structured, suggestions, relationships, periodicReviews, taskMetrics, repairDrafts, taskAudit, agentQuality, assetSyncState, suggestionNoise });
  const readinessChecks = [
    { id: "suggestions", label: "主动整理建议", status: suggestions.length ? "ready" : "needs-sample", detail: suggestions.length ? `${suggestions.length} 条建议` : "需要更多展品生成建议。" },
    { id: "relationships", label: "跨展品关系", status: relationships.clusters.length ? "ready" : "needs-sample", detail: relationships.clusters.length ? `${relationships.clusters.length} 组关系` : "需要人物、地点、标签或情绪线索。" },
    { id: "periodic-review", label: "周期回顾", status: periodicReviews.length ? "ready" : "needs-date", detail: periodicReviews.length ? `${periodicReviews.length} 个回顾候选` : "需要带日期的展品。" },
    { id: "feedback-loop", label: "反馈闭环", status: feedback.length ? "learning" : "ready", detail: feedback.length ? `${feedback.length} 条采纳/忽略记录` : "已具备反馈记录入口。" },
    { id: "task-audit", label: "任务审计", status: taskAudit.length ? "learning" : "ready", detail: taskAudit.length ? `${taskAudit.length} 条任务审计` : "状态流转和批量修复会写入本地审计。" },
    { id: "review-assets", label: "回顾资产", status: periodicReviews.length ? "ready" : "needs-date", detail: periodicReviews.length ? "周期回顾可保存为专题展草稿。" : "需要月度回顾候选。" },
    { id: "review-reports", label: "回顾报告", status: periodicReviews.length ? "ready" : "needs-date", detail: periodicReviews.length ? "周期回顾可保存为报告草稿。" : "需要月度回顾候选。" }
  ];
  return {
    phase: 18,
    phaseName: "Agent 能力进阶和长期记忆助理版",
    buildLabel: "phase18-agent-digest-thirteenth-edition",
    mode: "local-long-term-memory-assistant",
    generatedAt: new Date().toISOString(),
    sourceCount: structured.length,
    suggestions,
    visibleSuggestions: suggestionNoise.visible,
    suggestionNoise,
    relationships,
    periodicReviews,
    taskQueue: {
      ...taskMetrics,
      filter: phase18TaskFilter,
      selectedIds: Array.from(phase18SelectedTaskIds),
      batchReview: taskBatchReview,
      visible: filterPhase18Tasks(taskQueue).slice(0, 10)
    },
    feedbackLoop: {
      total: feedback.length,
      accepted: feedback.filter((item) => item.action === "accepted").length,
      dismissed: feedback.filter((item) => item.action === "dismissed").length,
      recent: feedback.slice(0, 6)
    },
    agentQuality,
    agentDigest,
    reviewDashboard,
    repairDrafts: {
      total: repairDrafts.length,
      preview: repairDrafts.filter((item) => item.status === "preview").length,
      applied: repairDrafts.filter((item) => item.status === "applied").length,
      recent: repairDrafts.slice(0, 8),
      batchReview: repairBatchReview
    },
    taskAudit: {
      total: taskAudit.length,
      recent: taskAudit.slice(0, 8)
    },
    periodicAssetPlan: {
      ready: periodicReviews.length > 0,
      candidates: periodicReviews.length,
      persistence: databaseAvailable ? "sqlite-saved-exhibitions" : "requires-node-sqlite"
    },
    periodicReportPlan: {
      ready: periodicReviews.length > 0,
      candidates: periodicReviews.length,
      persistence: databaseAvailable ? "sqlite-report-drafts" : "requires-node-sqlite"
    },
    assetSyncState,
    suggestionQuality: {
      high: suggestions.filter((item) => item.quality?.tier === "A").length,
      medium: suggestions.filter((item) => item.quality?.tier === "B").length,
      low: suggestions.filter((item) => item.quality?.tier === "C").length,
      averageScore: suggestions.length ? Math.round(suggestions.reduce((sum, item) => sum + (item.quality?.score || 0), 0) / suggestions.length) : 0
    },
    readinessChecks,
    nextStep: agentDigest.recommendation || (suggestions.length || relationships.clusters.length
      ? "优先处理高优先级建议，再把稳定月度回顾保存为专题资产。"
      : "先继续保存更多带时间、人物、地点和标签的展品。")
  };
}

function buildPhase18AgentQuality(feedback = [], taskMetrics = {}, repairDrafts = [], taskAudit = []) {
  const accepted = feedback.filter((item) => item.action === "accepted").length;
  const dismissed = feedback.filter((item) => item.action === "dismissed").length;
  const feedbackTotal = accepted + dismissed;
  const appliedDrafts = repairDrafts.filter((item) => item.status === "applied").length;
  const previewDrafts = repairDrafts.filter((item) => item.status === "preview").length;
  const statusChanges = taskAudit.filter((item) => item.action === "task-status-changed");
  const failedChanges = statusChanges.filter((item) => item.meta?.to === "failed");
  return {
    feedbackTotal,
    acceptanceRate: feedbackTotal ? Math.round((accepted / feedbackTotal) * 100) : 0,
    dismissalRate: feedbackTotal ? Math.round((dismissed / feedbackTotal) * 100) : 0,
    taskResolvedRate: taskMetrics.total ? Math.round(((taskMetrics.applied + taskMetrics.dismissed) / taskMetrics.total) * 100) : 0,
    repairApplyRate: repairDrafts.length ? Math.round((appliedDrafts / repairDrafts.length) * 100) : 0,
    appliedDrafts,
    previewDrafts,
    failedTasks: taskMetrics.failed || 0,
    failedTransitions: failedChanges.length,
    recommendation: failedChanges.length
      ? "先复盘失败任务，再继续批量修复。"
      : previewDrafts
        ? "可从预览草案中选择低风险字段批量应用。"
        : "继续积累反馈，质量统计会更稳定。"
  };
}

function buildPhase18SuggestionNoisePolicy(suggestions = [], feedback = []) {
  const dismissedTargets = new Set(feedback.filter((item) => item.action === "dismissed").map((item) => item.targetId));
  const quiet = suggestions.filter((item) => item.quality?.tier === "C" || dismissedTargets.has(item.id));
  const visible = suggestions.filter((item) => !quiet.some((quietItem) => quietItem.id === item.id));
  const sortedVisible = visible.sort((a, b) => (b.quality?.score || 0) - (a.quality?.score || 0));
  return {
    mode: "auto-tier-and-feedback",
    quietCount: quiet.length,
    visible: sortedVisible.length ? sortedVisible : suggestions.slice().sort((a, b) => (b.quality?.score || 0) - (a.quality?.score || 0)),
    quiet: quiet.map((item) => ({
      id: item.id,
      title: item.title,
      tier: item.quality?.tier || "C",
      reason: dismissedTargets.has(item.id) ? "用户已暂不处理" : "质量分层较低，自动降噪"
    })),
    recommendation: quiet.length
      ? `已自动降噪 ${quiet.length} 条低优先级或被忽略建议。`
      : "当前建议质量稳定，无需降噪。"
  };
}

function getDefaultPhase18NoiseRuleConfig() {
  return {
    mode: "configurable-tier-feedback",
    hideLowTier: true,
    hideDismissed: true,
    hideWeakEvidence: false,
    minVisibleScore: 48,
    keepHighPriority: true
  };
}

function normalizePhase18NoiseRuleConfig(config = {}) {
  const defaults = getDefaultPhase18NoiseRuleConfig();
  const score = Number(config.minVisibleScore);
  return {
    ...defaults,
    ...config,
    minVisibleScore: Number.isFinite(score) ? Math.min(90, Math.max(0, Math.round(score))) : defaults.minVisibleScore,
    hideLowTier: config.hideLowTier !== false,
    hideDismissed: config.hideDismissed !== false,
    hideWeakEvidence: config.hideWeakEvidence === true,
    keepHighPriority: config.keepHighPriority !== false
  };
}

function loadPhase18NoiseRuleConfig() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE18_NOISE_RULE_KEY) || "{}");
    return normalizePhase18NoiseRuleConfig(parsed);
  } catch (error) {
    return getDefaultPhase18NoiseRuleConfig();
  }
}

function savePhase18NoiseRuleConfig(config) {
  const normalized = normalizePhase18NoiseRuleConfig(config);
  localStorage.setItem(PHASE18_NOISE_RULE_KEY, JSON.stringify(normalized));
  return normalized;
}

function updatePhase18NoiseRuleSetting(key, value) {
  const current = loadPhase18NoiseRuleConfig();
  return savePhase18NoiseRuleConfig({ ...current, [key]: value });
}

function resetPhase18NoiseRuleConfig() {
  localStorage.removeItem(PHASE18_NOISE_RULE_KEY);
  return getDefaultPhase18NoiseRuleConfig();
}

function buildPhase18NoiseRulePreview({ suggestions = [], quiet = [], visible = [], config = {} } = {}) {
  const quietIds = new Set(quiet.map((item) => item.id));
  const recoverable = suggestions.filter((item) => quietIds.has(item.id) && (
    item.quality?.tier === "B" ||
    item.priority === "high" ||
    (item.quality?.score || 0) >= Math.max(0, (config.minVisibleScore || 0) - 8)
  ));
  return {
    total: suggestions.length,
    visibleCount: visible.length,
    quietCount: quiet.length,
    recoverableCount: recoverable.length,
    hiddenByTier: quiet.filter((item) => item.noiseReason === "low-tier").length,
    hiddenByFeedback: quiet.filter((item) => item.noiseReason === "dismissed").length,
    hiddenByWeakEvidence: quiet.filter((item) => item.noiseReason === "weak-evidence").length,
    hiddenByScore: quiet.filter((item) => item.noiseReason === "below-score").length,
    recoverable: recoverable.slice(0, 4).map((item) => ({
      id: item.id,
      title: item.title,
      tier: item.quality?.tier || "C",
      score: item.quality?.score || 0
    })),
    recommendation: quiet.length
      ? "当前规则已收起低置信或暂不处理的建议，可在规则面板临时放宽后复核。"
      : "当前规则没有收起建议，可以继续保持自动复盘。"
  };
}

function buildPhase18SuggestionNoisePolicy(suggestions = [], feedback = [], configInput = loadPhase18NoiseRuleConfig()) {
  const config = normalizePhase18NoiseRuleConfig(configInput);
  const dismissedTargets = new Set(feedback.filter((item) => item.action === "dismissed").map((item) => item.targetId));
  const scored = suggestions.map((item) => {
    const score = item.quality?.score || 0;
    let noiseReason = "";
    if (config.hideDismissed && dismissedTargets.has(item.id)) noiseReason = "dismissed";
    if (!noiseReason && config.hideLowTier && item.quality?.tier === "C") noiseReason = "low-tier";
    if (!noiseReason && config.hideWeakEvidence && (item.quality?.coverage || 0) < 35) noiseReason = "weak-evidence";
    if (!noiseReason && score < config.minVisibleScore) noiseReason = "below-score";
    if (config.keepHighPriority && item.priority === "high" && noiseReason !== "dismissed") noiseReason = "";
    return { ...item, noiseReason };
  });
  const quiet = scored.filter((item) => item.noiseReason);
  const visible = scored.filter((item) => !item.noiseReason);
  const sortedVisible = visible.sort((a, b) => (b.quality?.score || 0) - (a.quality?.score || 0));
  const fallbackVisible = suggestions.slice().sort((a, b) => (b.quality?.score || 0) - (a.quality?.score || 0));
  const preview = buildPhase18NoiseRulePreview({ suggestions: scored, quiet, visible: sortedVisible, config });
  return {
    mode: config.mode,
    config,
    preview,
    quietCount: quiet.length,
    visible: sortedVisible.length ? sortedVisible : fallbackVisible,
    quiet: quiet.map((item) => ({
      id: item.id,
      title: item.title,
      tier: item.quality?.tier || "C",
      score: item.quality?.score || 0,
      noiseReason: item.noiseReason,
      reason: item.noiseReason === "dismissed"
        ? "用户已标记暂不处理"
        : item.noiseReason === "weak-evidence"
          ? "证据覆盖不足，已按规则收起"
          : item.noiseReason === "below-score"
            ? "低于当前显示分数阈值"
            : "质量分层较低，已按规则收起"
    })),
    recommendation: quiet.length
      ? `已按规则收起 ${quiet.length} 条建议，当前显示 ${sortedVisible.length || fallbackVisible.length} 条。`
      : "当前建议质量稳定，没有被规则收起的建议。"
  };
}

function buildPhase18ReviewDashboard({ suggestions = [], relationships = {}, periodicReviews = [], taskMetrics = {}, agentQuality = {}, repairDrafts = [], taskAudit = [], suggestionNoise = {} } = {}) {
  const savedReviews = periodicReviews.filter((item) => item.assetLink?.exists || item.reportLink?.exists).length;
  const highTier = suggestions.filter((item) => item.quality?.tier === "A").length;
  const latestAudit = taskAudit[0]?.detail || "暂无审计记录";
  return {
    status: suggestions.length || relationships.clusterCount || periodicReviews.length ? "active" : "waiting",
    focus: highTier ? "优先处理 A 级建议" : periodicReviews.length ? "优先沉淀周期回顾" : "继续补充展品线索",
    savedReviews,
    reviewCoverage: periodicReviews.length ? Math.round((savedReviews / periodicReviews.length) * 100) : 0,
    graphCoverage: relationships.graph?.nodeCount || 0,
    quietSuggestions: suggestionNoise.quietCount || 0,
    openTasks: taskMetrics.active || 0,
    unresolvedDrafts: repairDrafts.filter((item) => item.status === "preview").length,
    qualityScore: Math.round(((agentQuality.acceptanceRate || 0) + (agentQuality.taskResolvedRate || 0) + (agentQuality.repairApplyRate || 0)) / 3),
    latestAudit,
    recommendation: suggestionNoise.quietCount
      ? "先处理未降噪的 A/B 级建议，再复盘被忽略建议。"
      : "继续把稳定回顾保存为专题资产或报告草稿。"
  };
}

function buildPhase18AgentDigest({
  structured = [],
  suggestions = [],
  relationships = {},
  periodicReviews = [],
  taskMetrics = {},
  repairDrafts = [],
  taskAudit = [],
  agentQuality = {},
  assetSyncState = {},
  suggestionNoise = {}
} = {}) {
  const highSuggestions = suggestions.filter((item) => item.priority === "high" || item.quality?.tier === "A");
  const previewDrafts = repairDrafts.filter((item) => item.status === "preview");
  const savedReviews = periodicReviews.filter((item) => item.assetLink?.exists || item.reportLink?.exists);
  const riskAssets = assetSyncState.items?.filter((item) => item.syncStatus === "risk") || [];
  const relationClusters = relationships.clusters || [];
  const activeTasks = taskMetrics.active || 0;
  const quietSuggestions = suggestionNoise.quietCount || 0;
  const reviewCandidates = periodicReviews.length;
  const todayFocus = highSuggestions[0]?.title
    || (previewDrafts.length ? "复核待应用修复草案" : "")
    || (activeTasks ? "推进长期任务队列" : "")
    || (reviewCandidates ? "沉淀一个周期回顾" : "")
    || "补充展品的时间、人物、地点和标签线索";
  const weeklyFocus = relationClusters.length >= 2
    ? "把稳定关系簇整理成专题展或报告草稿"
    : reviewCandidates
      ? "选择一个月度回顾保存为长期资产"
      : "持续积累可用于长期助理学习的展品样本";
  const topActions = [
    highSuggestions[0] ? {
      id: `suggestion-${highSuggestions[0].id}`,
      label: highSuggestions[0].title,
      detail: highSuggestions[0].detail,
      memoryIds: highSuggestions[0].memoryIds || []
    } : null,
    previewDrafts.length ? {
      id: "repair-preview",
      label: "复核修复草案",
      detail: `当前有 ${previewDrafts.length} 条草案等待确认，适合先处理低风险字段。`,
      memoryIds: previewDrafts.map((item) => item.memoryId).filter(Boolean)
    } : null,
    periodicReviews[0] ? {
      id: `review-${periodicReviews[0].id}`,
      label: periodicReviews[0].label,
      detail: `${periodicReviews[0].count || 0} 件展品可沉淀为周期回顾。`,
      memoryIds: periodicReviews[0].memories?.map((item) => item.id).filter(Boolean) || []
    } : null,
    relationClusters[0] ? {
      id: `relation-${relationClusters[0].id}`,
      label: `${relationClusters[0].type}：${relationClusters[0].value}`,
      detail: relationClusters[0].evidence || "关系簇可继续整理成讲解线索。",
      memoryIds: relationClusters[0].memories?.map((item) => item.id).filter(Boolean) || []
    } : null
  ].filter(Boolean).slice(0, 3);
  const readiness = riskAssets.length || agentQuality.failedTasks || agentQuality.failedTransitions
    ? "needs-attention"
    : (activeTasks || highSuggestions.length || previewDrafts.length || reviewCandidates ? "ready" : "warming-up");
  const recommendation = readiness === "needs-attention"
    ? "先处理同步风险、失败任务或高风险草案，再继续推进新整理。"
    : topActions.length
      ? "今天先完成摘要中的第一项动作，本周再把稳定关系或周期回顾沉淀为资产。"
      : "继续补充展品线索，长期助理会在样本变丰富后生成更明确摘要。";
  return {
    mode: "daily-weekly-memory-assistant-digest",
    generatedAt: new Date().toISOString(),
    todayFocus,
    weeklyFocus,
    topActions,
    signals: {
      memories: structured.length,
      activeTasks,
      quietSuggestions,
      relationClusters: relationClusters.length,
      reviewCandidates,
      savedReviews: savedReviews.length,
      previewDrafts: previewDrafts.length,
      assetSyncRisk: riskAssets.length,
      latestAudit: taskAudit[0]?.detail || "暂无审计记录"
    },
    cadence: {
      daily: "打开页面后先看摘要焦点，再处理一条高优先级任务。",
      weekly: "每周选择一个稳定关系簇或周期回顾，保存为专题展或报告草稿。"
    },
    readiness,
    recommendation
  };
}

function buildPhase18ProactiveSuggestions(items = []) {
  const suggestions = [];
  const missingDate = items.filter((memory) => !memory.date);
  const missingPeople = items.filter((memory) => !memory.people?.length);
  const missingLocation = items.filter((memory) => !memory.location);
  const missingTags = items.filter((memory) => !memory.tags?.length);
  const highValueNoMedia = items.filter((memory) => (memory.favorite || memory.importance >= 4) && !memory.coverImage && !memory.mediaNote && !normalizeAttachments(memory.attachments).length);
  if (missingDate.length) suggestions.push(buildPhase18Suggestion("missing-date", "补全时间线日期", "timeline", "high", missingDate, `有 ${missingDate.length} 件展品缺少日期，周期回顾会受影响。`));
  if (missingPeople.length) suggestions.push(buildPhase18Suggestion("missing-people", "补充相关人物", "relationship", "medium", missingPeople, `有 ${missingPeople.length} 件展品缺少人物线索，跨展品关系较难发现。`));
  if (missingLocation.length) suggestions.push(buildPhase18Suggestion("missing-location", "补充地点线索", "relationship", "medium", missingLocation, `有 ${missingLocation.length} 件展品缺少地点，可补成城市、房间或场景。`));
  if (missingTags.length) suggestions.push(buildPhase18Suggestion("missing-tags", "补充主题标签", "theme", "medium", missingTags, `有 ${missingTags.length} 件展品缺少标签，主题展候选会偏弱。`));
  if (highValueNoMedia.length) suggestions.push(buildPhase18Suggestion("high-value-media", "为重点展品补附件说明", "multimodal", "high", highValueNoMedia, `有 ${highValueNoMedia.length} 件重点展品缺少图片、OCR、语音或附件线索。`));
  return suggestions;
}

function buildPhase18Suggestion(id, title, type, priority, matches, detail) {
  const quality = buildPhase18SuggestionQuality(type, priority, matches);
  return {
    id,
    title,
    type,
    priority,
    quality,
    detail,
    count: matches.length,
    memoryIds: matches.slice(0, 6).map((memory) => memory.id),
    examples: matches.slice(0, 3).map(buildMemoryReference)
  };
}

function buildPhase18SuggestionQuality(type, priority, matches = []) {
  const count = matches.length;
  const evidenceRich = matches.filter((memory) => (
    memory.date ||
    memory.location ||
    memory.people?.length ||
    memory.tags?.length ||
    memory.emotions?.length ||
    memory.mediaNote ||
    normalizeAttachments(memory.attachments).length
  )).length;
  const coverage = count ? Math.round((evidenceRich / count) * 100) : 0;
  const urgency = priority === "high" ? 34 : priority === "medium" ? 22 : 12;
  const breadth = Math.min(34, count * 6);
  const score = Math.min(100, urgency + breadth + Math.round(coverage / 4));
  const tier = score >= 72 ? "A" : score >= 48 ? "B" : "C";
  return {
    tier,
    score,
    coverage,
    evidenceRich,
    reason: tier === "A"
      ? "高价值且影响面较大，建议优先复核。"
      : tier === "B"
        ? "价值稳定，可排入常规整理。"
        : "线索较轻，适合后续批量处理。"
  };
}

function buildPhase18RelationshipMap(items = []) {
  const dimensions = [
    { id: "people", label: "共同人物", values: (memory) => memory.people || [] },
    { id: "locations", label: "重复地点", values: (memory) => memory.location ? [memory.location] : [] },
    { id: "tags", label: "相同标签", values: (memory) => memory.tags || [] },
    { id: "emotions", label: "相似情绪", values: (memory) => memory.emotions || [] }
  ];
  const clusters = dimensions.flatMap((dimension) => {
    const map = new Map();
    items.forEach((memory) => {
      dimension.values(memory).forEach((value) => {
        const key = limitText(value, 40);
        if (!key) return;
        if (!map.has(key)) map.set(key, []);
        map.get(key).push(memory);
      });
    });
    return [...map.entries()]
      .filter(([, memoriesInCluster]) => memoriesInCluster.length >= 2)
      .map(([value, memoriesInCluster]) => ({
        id: `${dimension.id}-${simpleChecksum(value)}`,
        type: dimension.label,
        value,
        count: memoriesInCluster.length,
        strength: memoriesInCluster.length >= 5 ? "strong" : memoriesInCluster.length >= 3 ? "medium" : "light",
        evidence: `${dimension.label}“${value}”连接了 ${memoriesInCluster.length} 件展品，可作为专题展或讲解线索。`,
        memories: memoriesInCluster.slice(0, 5).map(buildMemoryReference)
      }));
  }).sort((a, b) => b.count - a.count).slice(0, 8);
  const assetNavigation = buildPhase18AssetNavigationIndex(clusters);
  const enrichedClusters = clusters.map((cluster) => ({
    ...cluster,
    assetLinks: assetNavigation.byCluster[cluster.id]?.assets || [],
    reportLinks: assetNavigation.byCluster[cluster.id]?.reports || []
  }));
  return {
    clusterCount: enrichedClusters.length,
    clusters: enrichedClusters,
    assetNavigation,
    graph: buildPhase18RelationshipGraph(enrichedClusters)
  };
}

function getReportReferenceIds(report = {}) {
  return (Array.isArray(report.references) ? report.references : [])
    .map((ref) => ref.memoryId || ref.id || ref.memory_id || "")
    .filter(Boolean);
}

function buildPhase18AssetNavigationIndex(clusters = [], collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reports = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const byCluster = {};
  const assetLookup = {};
  const reportLookup = {};
  clusters.forEach((cluster) => {
    const clusterMemoryIds = new Set((cluster.memories || []).map((memory) => memory.id).filter(Boolean));
    const assets = exhibitions.map((asset) => {
      const ids = Array.isArray(asset.memoryIds) ? asset.memoryIds : [];
      const overlap = ids.filter((id) => clusterMemoryIds.has(id));
      return overlap.length ? {
        id: asset.id,
        title: asset.title || "未命名专题展",
        type: "exhibition",
        overlapCount: overlap.length,
        status: asset.status || "draft"
      } : null;
    }).filter(Boolean).slice(0, 3);
    const linkedReports = reports.map((report) => {
      const ids = getReportReferenceIds(report);
      const overlap = ids.filter((id) => clusterMemoryIds.has(id));
      return overlap.length ? {
        id: report.id,
        title: report.title || "未命名报告草稿",
        type: "report",
        overlapCount: overlap.length,
        status: report.status || "draft"
      } : null;
    }).filter(Boolean).slice(0, 3);
    byCluster[cluster.id] = { assets, reports: linkedReports };
    assets.forEach((asset) => {
      assetLookup[asset.id] = [...(assetLookup[asset.id] || []), {
        id: cluster.id,
        label: `${cluster.type}:${cluster.value}`,
        overlapCount: asset.overlapCount
      }].slice(0, 4);
    });
    linkedReports.forEach((report) => {
      reportLookup[report.id] = [...(reportLookup[report.id] || []), {
        id: cluster.id,
        label: `${cluster.type}:${cluster.value}`,
        overlapCount: report.overlapCount
      }].slice(0, 4);
    });
  });
  const linkedAssetCount = Object.keys(assetLookup).length;
  const linkedReportCount = Object.keys(reportLookup).length;
  return {
    byCluster,
    assetLookup,
    reportLookup,
    linkedAssetCount,
    linkedReportCount,
    recommendation: linkedAssetCount || linkedReportCount
      ? "关系图谱已能跳转到相关专题展和报告草稿，也可从资产卡片回看关系来源。"
      : "保存包含这些展品的专题展或报告后，关系图谱会自动出现资产跳转。"
  };
}

function buildPhase18RelationshipGraph(clusters = []) {
  const nodes = [];
  const edges = [];
  const nodeIds = new Set();
  const addNode = (id, label, type, weight = 1) => {
    if (nodeIds.has(id)) return;
    nodeIds.add(id);
    nodes.push({ id, label: limitText(label, 60), type, weight });
  };
  clusters.slice(0, 6).forEach((cluster) => {
    const clusterId = `cluster-${cluster.id}`;
    addNode(clusterId, `${cluster.type}:${cluster.value}`, "cluster", cluster.count);
    (cluster.memories || []).slice(0, 4).forEach((memory) => {
      const memoryId = `memory-${memory.id}`;
      addNode(memoryId, memory.title, "memory", 1);
      edges.push({
        from: clusterId,
        to: memoryId,
        label: cluster.type,
        strength: cluster.strength || "light"
      });
    });
    (cluster.assetLinks || []).slice(0, 2).forEach((asset) => {
      const assetId = `asset-${asset.id}`;
      addNode(assetId, asset.title, "asset", asset.overlapCount || 1);
      edges.push({
        from: clusterId,
        to: assetId,
        label: "专题展",
        strength: asset.overlapCount >= 3 ? "medium" : "light"
      });
    });
    (cluster.reportLinks || []).slice(0, 2).forEach((report) => {
      const reportId = `report-${report.id}`;
      addNode(reportId, report.title, "report", report.overlapCount || 1);
      edges.push({
        from: clusterId,
        to: reportId,
        label: "报告",
        strength: report.overlapCount >= 3 ? "medium" : "light"
      });
    });
  });
  return {
    nodeCount: nodes.length,
    edgeCount: edges.length,
    nodes: nodes.slice(0, 18),
    edges: edges.slice(0, 24),
    summary: nodes.length ? `${nodes.length} 个节点 / ${edges.length} 条关系` : "等待更多关系线索"
  };
}

function filterPhase18RelationshipGraph(graph = {}, filter = phase18GraphFilter) {
  const nodes = Array.isArray(graph.nodes) ? graph.nodes : [];
  const edges = Array.isArray(graph.edges) ? graph.edges : [];
  if (filter === "all") return { ...graph, filter, visibleNodes: nodes.slice(0, 12), visibleEdges: edges.slice(0, 8) };
  const visibleNodes = nodes.filter((node) => node.type === filter).slice(0, 12);
  const visibleIds = new Set(visibleNodes.map((node) => node.id));
  const visibleEdges = edges.filter((edge) => visibleIds.has(edge.from) || visibleIds.has(edge.to)).slice(0, 8);
  return { ...graph, filter, visibleNodes, visibleEdges };
}

function buildPhase18PeriodicReviews(items = []) {
  const byMonth = items.reduce((map, memory) => {
    const sourceDate = memory.date || memory.createdAt || "";
    const month = String(sourceDate).slice(0, 7);
    if (!/^\d{4}-\d{2}$/.test(month)) return map;
    if (!map.has(month)) map.set(month, []);
    map.get(month).push(memory);
    return map;
  }, new Map());
  return [...byMonth.entries()]
    .filter(([, monthItems]) => monthItems.length >= 2)
    .sort((a, b) => b[0].localeCompare(a[0]))
    .slice(0, 6)
    .map(([month, monthItems]) => ({
      id: `review-${month}`,
      period: month,
      label: `${month} 月度回顾`,
      count: monthItems.length,
      topEmotions: toTopEntries(countValues(monthItems.flatMap((memory) => memory.emotions || [])), 3),
      memories: monthItems.slice(0, 5).map(buildMemoryReference),
      assetCandidate: {
        title: `${month} 月度回顾专题展`,
        memoryIds: monthItems.slice(0, 12).map((memory) => memory.id),
        tags: ["周期回顾", month, ...toTopEntries(countValues(monthItems.flatMap((memory) => memory.emotions || [])), 3).map((item) => item.label)].filter(Boolean).slice(0, 8)
      }
    }));
}

function enrichPhase18PeriodicReviewsWithAssets(reviews = []) {
  const collection = getAssetCollection();
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reports = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  return reviews.map((review) => {
    const assetId = `phase18-review-${review.period}`;
    const reportId = `phase18-review-report-${review.period}`;
    const asset = exhibitions.find((item) => item.id === assetId);
    const report = reports.find((item) => item.id === reportId);
    return {
      ...review,
      assetLink: {
        id: assetId,
        exists: Boolean(asset),
        title: asset?.title || review.assetCandidate?.title || `${review.label}专题展`
      },
      reportLink: {
        id: reportId,
        exists: Boolean(report),
        title: report?.title || `${review.label}报告草稿`
      }
    };
  });
}

async function savePhase18PeriodicReviewAsset(reviewId) {
  const agent = buildPhase18LongTermAgent();
  const review = agent.periodicReviews.find((item) => item.id === reviewId);
  if (!review) {
    setStorageStatus("没有找到这条第十八阶段周期回顾。", "warning");
    return;
  }
  if (!databaseAvailable) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能把周期回顾保存为专题展资产。", "warning");
    renderAssetCollectionPanel();
    return;
  }
  const memoryIds = review.assetCandidate?.memoryIds?.length
    ? review.assetCandidate.memoryIds
    : review.memories.map((memory) => memory.id);
  const title = review.assetCandidate?.title || `${review.label}专题展`;
  const topEmotionText = review.topEmotions.map((item) => `${item.label} ${item.count}`).join(" / ") || "情绪待补充";
  const payload = {
    id: `phase18-review-${review.period}`,
    title,
    intro: `${review.label}包含 ${review.count} 件展品，适合整理成一条温和的月度回顾线。`,
    status: "draft",
    coverMemoryId: memoryIds[0] || "",
    memoryIds,
    sort: memoryIds,
    guideText: `这组回顾来自第十八阶段长期记忆助理。建议从时间、人物、地点和情绪线索切入，当前情绪线索为：${topEmotionText}。`,
    tags: review.assetCandidate?.tags || ["周期回顾", "第十八阶段"]
  };
  setStorageStatus(`正在保存周期回顾专题展：${title}`, "loading");
  try {
    const response = await requestJson(API_EXHIBITIONS, {
      method: "POST",
      body: JSON.stringify(payload)
    });
    const saved = response.savedExhibition;
    recordPhase18TaskAudit("periodic-review-asset-saved", review.id, `保存周期回顾专题展《${saved?.title || title}》。`, {
      assetId: saved?.id || payload.id,
      memoryCount: memoryIds.length
    });
    recordAssetAuditEvent({
      action: "phase18-review-asset",
      label: `第十八阶段周期回顾生成专题展《${saved?.title || title}》`,
      detail: `${review.label} / ${memoryIds.length} 件展品`
    });
    await syncAssetCollection({ quiet: true });
    renderInsights();
    setStorageStatus(`已保存周期回顾专题展：${saved?.title || title}`, "success");
  } catch (error) {
    recordPhase18TaskAudit("periodic-review-asset-failed", review.id, `周期回顾专题展保存失败：${error.message}`, { memoryCount: memoryIds.length });
    setStorageStatus(`周期回顾专题展保存失败：${error.message}`, "warning");
  }
}

function buildPhase18PeriodicReviewReportPayload(review) {
  const memoryIds = review.assetCandidate?.memoryIds?.length
    ? review.assetCandidate.memoryIds
    : review.memories.map((memory) => memory.id);
  const emotionText = review.topEmotions.map((item) => `${item.label} ${item.count}`).join(" / ") || "情绪待补充";
  const references = review.memories.slice(0, 8).map((memory, index) => ({
    memoryId: memory.id,
    title: memory.title,
    role: index === 0 ? "开篇展品" : "回顾引用"
  }));
  return {
    id: `phase18-review-report-${review.period}`,
    title: `${review.label}报告草稿`,
    status: "draft",
    scope: {
      source: "phase18-periodic-review",
      period: review.period,
      memoryIds
    },
    sections: [
      {
        title: "报告开头",
        text: `${review.label}收纳了 ${review.count} 件展品，可以从时间、情绪和代表展品三个角度打开这段记忆。`
      },
      {
        title: "情绪线索",
        text: `当前最明显的情绪线索是：${emotionText}。这些词适合作为后续人工润色报告时的语气锚点。`
      },
      {
        title: "后续整理建议",
        text: "建议先复核日期、人物、地点和附件线索，再把稳定展品推进到专题展或正式回忆报告。"
      }
    ],
    references,
    sourceInsights: {
      phase: 18,
      type: "periodic-review",
      reviewId: review.id,
      generatedAt: new Date().toISOString()
    }
  };
}

async function savePhase18PeriodicReviewReport(reviewId) {
  const agent = buildPhase18LongTermAgent();
  const review = agent.periodicReviews.find((item) => item.id === reviewId);
  if (!review) {
    setStorageStatus("没有找到这条第十八阶段周期回顾。", "warning");
    return;
  }
  if (!databaseAvailable) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能把周期回顾保存为报告草稿。", "warning");
    renderAssetCollectionPanel();
    return;
  }
  const payload = buildPhase18PeriodicReviewReportPayload(review);
  setStorageStatus(`正在保存周期回顾报告草稿：${payload.title}`, "loading");
  try {
    const response = await requestJson(API_REPORT_DRAFTS, {
      method: "POST",
      body: JSON.stringify(payload)
    });
    const saved = response.reportDraft;
    recordPhase18TaskAudit("periodic-review-report-saved", review.id, `保存周期回顾报告草稿《${saved?.title || payload.title}》。`, {
      reportId: saved?.id || payload.id,
      sectionCount: payload.sections.length,
      referenceCount: payload.references.length
    });
    recordAssetAuditEvent({
      action: "phase18-review-report",
      label: `第十八阶段周期回顾生成报告草稿《${saved?.title || payload.title}》`,
      detail: `${review.label} / ${payload.references.length} 条引用`
    });
    await syncAssetCollection({ quiet: true });
    renderInsights();
    setStorageStatus(`已保存周期回顾报告草稿：${saved?.title || payload.title}`, "success");
  } catch (error) {
    recordPhase18TaskAudit("periodic-review-report-failed", review.id, `周期回顾报告草稿保存失败：${error.message}`, {
      sectionCount: payload.sections.length,
      referenceCount: payload.references.length
    });
    setStorageStatus(`周期回顾报告草稿保存失败：${error.message}`, "warning");
  }
}

function escapeHtml(value = "") {
  return String(value)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;");
}

function limitText(value, maxLength) {
  return String(value || "").trim().slice(0, maxLength);
}

function parseBoolean(value) {
  if (value === true || value === false) return value;
  if (typeof value === "string") {
    const normalized = value.trim().toLowerCase();
    if (["true", "1", "yes", "y"].includes(normalized)) return true;
    if (["false", "0", "no", "n", ""].includes(normalized)) return false;
  }
  if (typeof value === "number") return value === 1;
  return false;
}

function normalizeList(value) {
  if (Array.isArray(value)) {
    return value
      .map((item) => limitText(item, fieldLimits.listItem))
      .filter(Boolean)
      .slice(0, fieldLimits.listLength);
  }
  if (typeof value === "string") return splitList(value);
  return [];
}

function normalizeMemory(memory) {
  const hallId = typeof memory.hall === "object" ? memory.hall?.id : memory.hall;
  const hallExists = halls.some((hall) => hall.id === hallId && hall.id !== "all");
  const now = new Date().toISOString();
  const rawContent = limitText(memory.rawContent, fieldLimits.rawContent);
  const title = limitText(memory.title, fieldLimits.title);
  const emotionIntensity = Math.min(5, Math.max(1, Number(memory.emotionIntensity) || 3));
  const importance = Math.min(5, Math.max(1, Number(memory.importance) || 1));
  const sourceType = sourceTypes.includes(memory.sourceType) ? memory.sourceType : "日记";

  return {
    schemaVersion: SCHEMA_VERSION,
    id: normalizeId(memory.id),
    title: title || "未命名展品",
    hall: hallExists ? hallId : "daily",
    rawContent,
    exhibitText: limitText(memory.exhibitText || rawContent || "这件展品还没有说明。", fieldLimits.exhibitText),
    date: limitText(memory.date, fieldLimits.date),
    location: limitText(memory.location, fieldLimits.location),
    people: normalizeList(memory.people),
    tags: normalizeList(memory.tags),
    emotions: normalizeList(memory.emotions),
    emotionIntensity,
    sourceType,
    importance,
    favorite: parseBoolean(memory.favorite),
    coverImage: limitText(memory.coverImage, fieldLimits.coverImage),
    mediaNote: limitText(memory.mediaNote, fieldLimits.mediaNote),
    attachments: normalizeAttachments(memory.attachments),
    agentRunId: limitText(memory.agentRunId, 120),
    createdAt: memory.createdAt || now,
    updatedAt: memory.updatedAt || ""
  };
}

function normalizeAttachments(value) {
  const items = Array.isArray(value)
    ? value
    : typeof value === "string"
      ? value.split(/\n+/).map((line) => {
        const [name = "", type = "", note = ""] = line.split("|").map((part) => part.trim());
        return { name, type, note };
      })
      : [];
  return items.map((item) => {
    if (typeof item === "string") return { name: limitText(item, fieldLimits.attachmentName), type: "附件", note: "" };
    return {
      name: limitText(item.name || item.filename || item.title, fieldLimits.attachmentName),
      type: limitText(item.type || item.kind || "附件", 30),
      note: limitText(item.note || item.description || item.text, fieldLimits.attachmentNote)
    };
  }).filter((item) => item.name).slice(0, 8);
}

function formatAttachmentsInput(attachments = []) {
  return normalizeAttachments(attachments).map((item) => [item.name, item.type, item.note].filter(Boolean).join(" | ")).join("\n");
}

function detectPhase19ImportFormat(text = "", requested = "auto") {
  if (requested && requested !== "auto") return requested;
  const trimmed = String(text || "").trim();
  if (!trimmed) return "text";
  if ((trimmed.startsWith("[") || trimmed.startsWith("{")) && /"?(memories|items|rawContent|content|text)"?\s*:/.test(trimmed)) return "json";
  if (/^#{1,3}\s+/m.test(trimmed)) return "markdown";
  if (/^.+,.+\n.+,.+/m.test(trimmed)) return "csv";
  if (/^\s*(?:\[[^\]]+\]\s*)?[^：:\n]{1,16}[：:]/m.test(trimmed)) return "chat";
  return "text";
}

function getPhase19CsvDelimiter(line = "") {
  const delimiters = [",", "\t", ";", "，"];
  return delimiters
    .map((delimiter) => ({ delimiter, count: String(line || "").split(delimiter).length - 1 }))
    .sort((a, b) => b.count - a.count)[0]?.delimiter || ",";
}

function splitPhase19CsvLine(line = "", delimiter = ",") {
  const cells = [];
  let current = "";
  let quoted = false;
  String(line || "").split("").forEach((char) => {
    if (char === "\"") {
      quoted = !quoted;
      return;
    }
    if (char === delimiter && !quoted) {
      cells.push(current.trim());
      current = "";
      return;
    }
    current += char;
  });
  cells.push(current.trim());
  return cells;
}

function parsePhase19CsvRows(text = "") {
  const lines = String(text || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  if (lines.length < 2) return [];
  const delimiter = getPhase19CsvDelimiter(lines[0]);
  const headers = splitPhase19CsvLine(lines[0], delimiter);
  return lines.slice(1).map((line) => {
    const cells = splitPhase19CsvLine(line, delimiter);
    return headers.reduce((row, header, index) => {
      row[header] = cells[index] || "";
      return row;
    }, { _delimiter: delimiter });
  });
}

function parsePhase19ChatSegments(text = "", cleanupMode = "balanced") {
  const lines = String(text || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  const groups = [];
  let active = null;
  lines.forEach((line) => {
    const match = line.match(/^(?:\[?(\d{4}[-/]\d{1,2}[-/]\d{1,2}|\d{1,2}[:：]\d{2})\]?\s*)?([^：:\n]{1,18})[：:]\s*(.+)$/);
    if (!match) {
      if (active) active.lines.push(line);
      else groups.push({ speaker: "片段", time: "", lines: [line] });
      return;
    }
    const [, time = "", speaker = "片段", content = ""] = match;
    const shouldMerge = active
      && active.speaker === speaker
      && (cleanupMode === "compact" || active.lines.length < 4);
    if (!shouldMerge) {
      active = { speaker, time, lines: [] };
      groups.push(active);
    }
    active.lines.push(content);
  });
  return groups.map((group, index) => ({
    title: `${group.speaker}的聊天片段 ${index + 1}`,
    rawContent: group.lines.join("\n"),
    people: [group.speaker].filter((item) => item && item !== "片段"),
    date: /^\d{4}[-/]\d{1,2}[-/]\d{1,2}$/.test(group.time) ? group.time.replace(/\//g, "-") : "",
    sourceType: "聊天片段",
    sourceTrace: `聊天记录 / ${group.speaker}${group.time ? ` / ${group.time}` : ""}`
  }));
}

function splitPhase19ImportText(text = "", format = "text", cleanupMode = "balanced") {
  const trimmed = String(text || "").trim();
  if (!trimmed) return [];
  if (format === "json") {
    try {
      const parsed = JSON.parse(trimmed);
      const rows = Array.isArray(parsed) ? parsed : Array.isArray(parsed.memories) ? parsed.memories : Array.isArray(parsed.items) ? parsed.items : [parsed];
      return rows.map((item, index) => ({
        title: item.title || item.name || `导入片段 ${index + 1}`,
        rawContent: item.rawContent || item.content || item.text || item.body || item.exhibitText || "",
        date: item.date || item.memoryDate || "",
        location: item.location || "",
        people: item.people || item.person || "",
        tags: item.tags || "",
        sourceType: item.sourceType || item.source || "",
        fieldSource: item,
        sourceTrace: `JSON 第 ${index + 1} 项`
      })).filter((item) => item.rawContent || item.title);
    } catch {
      return [{ title: "JSON 解析失败的原始片段", rawContent: trimmed }];
    }
  }
  if (format === "csv") {
    return parsePhase19CsvRows(trimmed).map((row, index) => ({
      title: row.title || row.标题 || row.name || `CSV 片段 ${index + 1}`,
      rawContent: row.rawContent || row.content || row.text || row.正文 || row.内容 || Object.values(row).join(" / "),
      date: row.date || row.日期 || "",
      location: row.location || row.地点 || "",
      people: row.people || row.人物 || "",
      tags: row.tags || row.标签 || "",
      sourceType: row.sourceType || row.来源 || "",
      sourceTrace: `CSV 第 ${index + 2} 行 / 分隔符 ${row._delimiter === "\t" ? "Tab" : row._delimiter}`
    }));
  }
  if (format === "markdown") {
    const blocks = trimmed.split(/\n(?=#{1,3}\s+)/).map((block) => block.trim()).filter(Boolean);
    return blocks.map((block, index) => {
      const heading = block.match(/^#{1,3}\s+(.+)$/m)?.[1] || `Markdown 片段 ${index + 1}`;
      return { title: heading, rawContent: block.replace(/^#{1,3}\s+.+$/m, "").trim() || block, sourceTrace: `Markdown 标题：${heading}` };
    });
  }
  if (format === "chat") {
    return parsePhase19ChatSegments(trimmed, cleanupMode);
  }
  return trimmed.split(/\n{2,}|-{3,}/).map((block, index) => ({
    title: `文本片段 ${index + 1}`,
    rawContent: block.trim(),
    sourceTrace: `文本段落 ${index + 1}`
  })).filter((item) => item.rawContent);
}

function getPhase19MappingTemplate(template = "auto", detectedFormat = "text") {
  const templates = {
    diary: { label: "日记整理", sourceType: "日记", tags: ["日记", "个人记录"], hall: "" },
    chat: { label: "聊天归档", sourceType: "聊天片段", tags: ["聊天", "关系线索"], hall: "friends" },
    album: { label: "相册批注", sourceType: "照片描述", tags: ["相册", "照片线索"], hall: "family" },
    travel: { label: "旅行记录", sourceType: "旅行片段", tags: ["旅行", "地点线索"], hall: "travel" },
    auto: { label: "自动模板", sourceType: "", tags: [detectedFormat], hall: "" }
  };
  const custom = phase19CustomTemplates.find((item) => item.id === template);
  return custom || templates[template] || templates.auto;
}

function loadPhase19CustomMappingTemplates() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE19_CUSTOM_TEMPLATES_KEY) || "[]");
    return Array.isArray(parsed)
      ? parsed.map(normalizePhase19CustomMappingTemplate).filter(Boolean).slice(0, 8)
      : [];
  } catch {
    return [];
  }
}

function normalizePhase19CustomMappingTemplate(template = {}) {
  const label = limitText(template.label || template.name, 30);
  if (!label) return null;
  return {
    id: limitText(template.id || `custom-${simpleChecksum(label).slice(0, 8)}`, 40),
    label,
    sourceType: sourceTypes.includes(template.sourceType) ? template.sourceType : "",
    hall: halls.some((hall) => hall.id === template.hall && hall.id !== "all") ? template.hall : "",
    tags: normalizeList(template.tags).slice(0, 6),
    people: normalizeList(template.people).slice(0, 6),
    dateRule: limitText(template.dateRule, 40),
    fieldAliases: normalizePhase19FieldAliases(template.fieldAliases),
    createdAt: template.createdAt || new Date().toISOString()
  };
}

function parsePhase19FieldAliasInput(value = "") {
  return String(value || "").split(";").reduce((rules, chunk) => {
    const [field, aliases] = chunk.split("=").map((part) => part.trim());
    if (!field || !aliases) return rules;
    rules[field] = aliases.split(/[,，、|]/).map((item) => item.trim()).filter(Boolean).slice(0, 8);
    return rules;
  }, {});
}

function normalizePhase19FieldAliases(value = {}) {
  const source = typeof value === "string" ? parsePhase19FieldAliasInput(value) : value;
  const allowed = ["title", "rawContent", "date", "location", "people", "tags", "sourceType"];
  return allowed.reduce((rules, field) => {
    const aliases = Array.isArray(source?.[field]) ? source[field] : typeof source?.[field] === "string" ? source[field].split(/[,，、|]/) : [];
    rules[field] = aliases.map((item) => limitText(item, 24)).filter(Boolean).slice(0, 8);
    return rules;
  }, {});
}

function getPhase19FieldSources(text = "", format = "text") {
  if (format === "csv") return parsePhase19CsvRows(text);
  if (format === "json") {
    try {
      const parsed = JSON.parse(String(text || "").trim());
      return Array.isArray(parsed) ? parsed : Array.isArray(parsed.memories) ? parsed.memories : Array.isArray(parsed.items) ? parsed.items : [parsed];
    } catch {
      return [];
    }
  }
  return [];
}

function readPhase19AliasValue(source = {}, aliases = []) {
  if (!source || typeof source !== "object") return "";
  const keys = Object.keys(source);
  const key = aliases.find((alias) => keys.some((item) => item.toLowerCase() === String(alias).toLowerCase()));
  if (!key) return "";
  const actual = keys.find((item) => item.toLowerCase() === String(key).toLowerCase());
  return source[actual];
}

function applyPhase19FieldAliases(segment = {}, template = {}) {
  const source = segment.fieldSource;
  if (!source) return segment;
  const rules = normalizePhase19FieldAliases(template.fieldAliases);
  return ["title", "rawContent", "date", "location", "people", "tags", "sourceType"].reduce((next, field) => {
    const value = readPhase19AliasValue(source, rules[field]);
    return value ? { ...next, [field]: value } : next;
  }, segment);
}

function persistPhase19CustomMappingTemplates() {
  localStorage.setItem(PHASE19_CUSTOM_TEMPLATES_KEY, JSON.stringify(phase19CustomTemplates.slice(0, 8)));
}

function renderPhase19MappingTemplateOptions() {
  if (!elements.phase19MappingTemplate) return;
  const selected = elements.phase19MappingTemplate.value || "auto";
  const defaults = [
    ["auto", "自动模板"],
    ["diary", "日记整理"],
    ["chat", "聊天归档"],
    ["album", "相册批注"],
    ["travel", "旅行记录"]
  ];
  const values = [...defaults.map(([value]) => value), ...phase19CustomTemplates.map((item) => item.id)];
  elements.phase19MappingTemplate.innerHTML = [
    ...defaults.map(([value, label]) => `<option value="${escapeHtml(value)}">${escapeHtml(label)}</option>`),
    ...phase19CustomTemplates.map((template) => `<option value="${escapeHtml(template.id)}">${escapeHtml(template.label)}</option>`)
  ].join("");
  elements.phase19MappingTemplate.value = values.includes(selected) ? selected : "auto";
}

function savePhase19CustomMappingTemplate() {
  const label = limitText(elements.phase19BatchName?.value || elements.phase19TemplateTags?.value || `导入模板 ${phase19CustomTemplates.length + 1}`, 30);
  const tags = normalizeList(elements.phase19TemplateTags?.value || label);
  const id = `custom-${Date.now().toString(36)}-${simpleChecksum(`${label}:${tags.join("|")}`).slice(0, 6)}`;
  const template = normalizePhase19CustomMappingTemplate({
    id,
    label,
    sourceType: elements.phase19DefaultSource?.value || "",
    hall: elements.phase19DefaultHall?.value === "auto" ? "" : elements.phase19DefaultHall?.value,
    tags,
    people: elements.phase19TemplatePeople?.value || "",
    dateRule: elements.phase19TemplateDateRule?.value || "",
    fieldAliases: parsePhase19FieldAliasInput(elements.phase19TemplateAliases?.value || ""),
    createdAt: new Date().toISOString()
  });
  if (!template) return;
  phase19CustomTemplates = [template, ...phase19CustomTemplates.filter((item) => item.label !== template.label)].slice(0, 8);
  persistPhase19CustomMappingTemplates();
  renderPhase19MappingTemplateOptions();
  elements.phase19MappingTemplate.value = template.id;
  setStorageStatus(`已保存第十九阶段自定义映射模板：${template.label}`, "success");
}

function applyPhase19MappingTemplate(draft = {}, templateId = "auto", detectedFormat = "text") {
  const template = getPhase19MappingTemplate(templateId, detectedFormat);
  const sourceType = template.sourceType && sourceTypes.includes(template.sourceType) ? template.sourceType : draft.sourceType;
  const hall = template.hall && halls.some((item) => item.id === template.hall) ? template.hall : draft.hall;
  const date = draft.date || template.dateRule || "";
  return normalizeMemory({
    ...draft,
    sourceType,
    hall,
    date,
    people: [...normalizeList(draft.people), ...normalizeList(template.people)].filter(Boolean),
    tags: [...normalizeList(draft.tags), ...normalizeList(template.tags), template.label].filter(Boolean)
  });
}

function buildPhase19DuplicateRisk(draft = {}, items = memories) {
  const title = String(draft.title || "").trim().toLowerCase();
  const raw = String(draft.rawContent || "").trim();
  const rawHash = simpleChecksum(raw.slice(0, 360));
  const candidate = items.find((memory) => {
    const sameTitle = title && String(memory.title || "").trim().toLowerCase() === title;
    const sameRaw = raw && simpleChecksum(String(memory.rawContent || "").trim().slice(0, 360)) === rawHash;
    return sameTitle || sameRaw;
  });
  return candidate
    ? { level: "high", memoryId: candidate.id, title: candidate.title, reason: "标题或正文片段与现有展品接近" }
    : { level: "low", memoryId: "", title: "", reason: "未发现明显重复" };
}

function annotatePhase19DuplicateRisks(drafts = []) {
  return drafts.map((draft) => ({
    ...draft,
    duplicateRisk: buildPhase19DuplicateRisk(draft)
  })).map((draft) => ({
    ...draft,
    duplicateAction: draft.duplicateRisk?.level === "high" ? "skip" : "new",
    selected: draft.duplicateRisk?.level === "high" ? false : draft.selected
  }));
}

function buildPhase19ConflictPreview(plan = phase19ImportPreview) {
  const drafts = plan?.drafts || [];
  const risky = drafts.filter((draft) => draft.duplicateRisk?.level === "high");
  const decisionCounts = drafts.reduce((acc, draft) => {
    const action = draft.duplicateAction || "new";
    acc[action] = (acc[action] || 0) + 1;
    return acc;
  }, {});
  return {
    total: risky.length,
    skip: decisionCounts.skip || 0,
    asNew: decisionCounts.new || 0,
    review: decisionCounts.review || 0,
    items: risky.slice(0, 6).map((draft) => ({
      title: draft.title,
      matchedTitle: draft.duplicateRisk?.title || "",
      action: draft.duplicateAction || "skip"
    }))
  };
}

function renderPhase19ConflictPreview(plan = phase19ImportPreview) {
  const preview = buildPhase19ConflictPreview(plan);
  if (!preview.total) return "";
  return `
    <div class="phase19-conflict-preview">
      <span><b>${escapeHtml(preview.total)}</b><small>疑似重复</small></span>
      <span><b>${escapeHtml(preview.skip)}</b><small>跳过</small></span>
      <span><b>${escapeHtml(preview.asNew)}</b><small>作为新展品</small></span>
      <span><b>${escapeHtml(preview.review)}</b><small>待复核</small></span>
      <small>${preview.items.map((item) => `${item.title} -> ${item.matchedTitle || item.action}`).join(" / ")}</small>
    </div>
  `;
}

function buildPhase19BatchComparison(batches = phase19ImportBatches) {
  const active = batches.filter((batch) => batch.status !== "failed").slice(0, 2);
  if (active.length < 2) return null;
  const [latest, previous] = active;
  const latestTitles = new Set((latest.titles || []).map((title) => String(title).toLowerCase()));
  const overlap = (previous.titles || []).filter((title) => latestTitles.has(String(title).toLowerCase()));
  return {
    latestName: latest.name || latest.id,
    previousName: previous.name || previous.id,
    scoreDelta: (latest.quality?.averageScore || 0) - (previous.quality?.averageScore || 0),
    countDelta: (latest.count || 0) - (previous.count || 0),
    overlap
  };
}

function getPhase19FilteredBatches() {
  const value = phase19BatchFilter || elements.phase19BatchFilter?.value || "all";
  const searched = phase19ImportBatches.filter((batch) => {
    const keyword = String(phase19AuditSearch || elements.phase19AuditSearch?.value || "").trim().toLowerCase();
    if (!keyword) return true;
    const haystack = [
      batch.id,
      batch.name,
      batch.format,
      ...(batch.titles || []),
      ...(batch.draftSummaries || []).map((item) => item.title),
      ...(batch.cleanupQueue || []).map((item) => `${item.title} ${item.reason}`),
      ...(batch.conflictReviewItems || []).map((item) => `${item.title} ${item.matchedTitle}`)
    ].join(" ").toLowerCase();
    return haystack.includes(keyword);
  });
  if (value === "active") return searched.filter((batch) => batch.status === "active");
  if (value === "failed") return searched.filter((batch) => batch.status === "failed");
  if (value === "needs-review") return searched.filter((batch) => (batch.quality?.needsReviewCount || 0) > 0);
  if (value === "duplicate") return searched.filter((batch) => (batch.conflictSummary?.duplicateRiskCount || 0) > 0);
  return searched;
}

function renderPhase19BatchFilters(filtered = getPhase19FilteredBatches()) {
  return `
    <div class="phase19-batch-filters">
      <span><b>${escapeHtml(filtered.length)}</b><small>当前批次</small></span>
      <span><b>${escapeHtml(phase19ImportBatches.filter((batch) => batch.status === "active").length)}</b><small>有效</small></span>
      <span><b>${escapeHtml(phase19ImportBatches.filter((batch) => (batch.conflictSummary?.duplicateRiskCount || 0) > 0).length)}</b><small>含重复风险</small></span>
      <small>筛选：${escapeHtml(phase19BatchFilter || "all")}</small>
    </div>
  `;
}

function renderPhase19BatchComparison(batches = phase19ImportBatches) {
  const comparison = buildPhase19BatchComparison(batches);
  if (!comparison) return "";
  const scoreLabel = comparison.scoreDelta > 0 ? `+${comparison.scoreDelta}` : String(comparison.scoreDelta);
  const countLabel = comparison.countDelta > 0 ? `+${comparison.countDelta}` : String(comparison.countDelta);
  return `
    <div class="phase19-batch-comparison">
      <span><b>${escapeHtml(scoreLabel)}</b><small>质量变化</small></span>
      <span><b>${escapeHtml(countLabel)}</b><small>数量变化</small></span>
      <span><b>${escapeHtml(comparison.overlap.length)}</b><small>标题重叠</small></span>
      <small>${escapeHtml(comparison.latestName)} 对比 ${escapeHtml(comparison.previousName)}</small>
    </div>
  `;
}

function buildPhase19ImportReport(batches = getPhase19FilteredBatches()) {
  const active = batches.filter((batch) => batch.status !== "reverted");
  const imported = active.reduce((sum, batch) => sum + (batch.count || batch.importedIds?.length || 0), 0);
  const reviewItems = active.reduce((sum, batch) => sum + (batch.conflictReviewItems || []).filter((item) => item.status !== "resolved" && item.status !== "ignored").length, 0);
  const cleanupItems = active.reduce((sum, batch) => sum + (batch.cleanupQueue || []).filter((item) => item.status !== "done").length, 0);
  const avgQuality = active.length
    ? Math.round(active.reduce((sum, batch) => sum + (batch.quality?.averageScore || 0), 0) / active.length)
    : 0;
  return {
    batchCount: active.length,
    imported,
    avgQuality,
    reviewItems,
    cleanupItems,
    failed: batches.filter((batch) => batch.status === "failed").length,
    recommendation: reviewItems || cleanupItems
      ? "建议先处理冲突复核和导入后整理队列，再继续导入下一批资料。"
      : "当前筛选范围内导入状态较稳定，可以继续导入或导出审计包。"
  };
}

function renderPhase19ImportReport(batches = getPhase19FilteredBatches()) {
  if (!phase19ImportBatches.length) return "";
  const report = buildPhase19ImportReport(batches);
  return `
    <div class="phase19-import-report">
      <span><b>${escapeHtml(report.batchCount)}</b><small>报告批次</small></span>
      <span><b>${escapeHtml(report.imported)}</b><small>导入展品</small></span>
      <span><b>${escapeHtml(report.avgQuality)}</b><small>平均质量</small></span>
      <span><b>${escapeHtml(report.reviewItems)}</b><small>复核项</small></span>
      <span><b>${escapeHtml(report.cleanupItems)}</b><small>整理项</small></span>
      <small>${escapeHtml(report.recommendation)}</small>
    </div>
  `;
}

function buildPhase19ConflictReviewDesk(batches = phase19ImportBatches) {
  const items = batches.flatMap((batch) => (batch.conflictReviewItems || []).map((item) => ({
    ...item,
    batchId: batch.id,
    batchName: batch.name || batch.id,
    batchStatus: batch.status || "active"
  })));
  const pending = items.filter((item) => ["review", "skip"].includes(item.action) && item.status !== "resolved");
  return {
    total: items.length,
    pendingCount: pending.length,
    importedAsNew: items.filter((item) => item.action === "new").length,
    skipped: items.filter((item) => item.action === "skip").length,
    review: items.filter((item) => item.action === "review").length,
    items: pending.slice(0, 6)
  };
}

function renderPhase19ConflictReviewDesk() {
  const desk = buildPhase19ConflictReviewDesk();
  if (!desk.total) return "";
  return `
    <div class="phase19-review-desk">
      <div>
        <strong>冲突复核台</strong>
        <small>集中回看被跳过或保留待复核的疑似重复资料。</small>
      </div>
      <div class="phase19-review-strip">
        <span><b>${escapeHtml(desk.pendingCount)}</b><small>待复核</small></span>
        <span><b>${escapeHtml(desk.importedAsNew)}</b><small>作为新展品</small></span>
        <span><b>${escapeHtml(desk.skipped)}</b><small>跳过</small></span>
        <span><b>${escapeHtml(desk.review)}</b><small>保留</small></span>
      </div>
      ${desk.items.map((item) => `
        <article>
          <b>${escapeHtml(item.title || "未命名资料")}</b>
          <small>${escapeHtml(item.batchName)} / 匹配：${escapeHtml(item.matchedTitle || item.matchedId || "现有展品")}</small>
          <span>${escapeHtml(item.action === "skip" ? "已跳过，建议确认是否需要重新导入" : "保留待复核，建议补充字段后再导入")}</span>
          <div class="phase19-review-actions">
            <button type="button" data-phase19-review-status="${escapeHtml(item.id)}" data-phase19-review-batch="${escapeHtml(item.batchId)}" data-phase19-review-next="resolved">已处理</button>
            <button type="button" data-phase19-review-status="${escapeHtml(item.id)}" data-phase19-review-batch="${escapeHtml(item.batchId)}" data-phase19-review-next="ignored">忽略</button>
            <button type="button" data-phase19-review-status="${escapeHtml(item.id)}" data-phase19-review-batch="${escapeHtml(item.batchId)}" data-phase19-review-next="queued">加入整理</button>
          </div>
        </article>
      `).join("")}
    </div>
  `;
}

function updatePhase19ConflictReviewStatus(batchId, itemId, status) {
  const batch = phase19ImportBatches.find((item) => item.id === batchId);
  if (!batch) return false;
  const nextStatus = ["resolved", "ignored", "queued"].includes(status) ? status : "resolved";
  let target = null;
  batch.conflictReviewItems = (batch.conflictReviewItems || []).map((item) => {
    if (item.id !== itemId) return item;
    target = item;
    return { ...item, status: nextStatus, resolvedAt: new Date().toISOString() };
  });
  if (target && nextStatus === "queued") {
    batch.cleanupQueue = [
      ...(batch.cleanupQueue || []),
      {
        id: `phase19-cleanup-conflict-${simpleChecksum(`${batchId}:${itemId}`).slice(0, 10)}`,
        memoryId: target.id,
        title: target.title,
        reason: "冲突复核",
        priority: "high",
        status: "queued",
        recommendation: "该资料曾被判定为疑似重复，建议人工确认后再决定是否重新导入。"
      }
    ].slice(0, 16);
  }
  persistPhase19ImportBatches();
  renderPhase19ImportPreview(phase19ImportPreview);
  return Boolean(target);
}

function buildPhase19CleanupQueue(batches = phase19ImportBatches) {
  const items = batches.flatMap((batch) => (batch.cleanupQueue || []).map((item) => ({
    ...item,
    batchId: batch.id,
    batchName: batch.name || batch.id,
    batchStatus: batch.status || "active"
  })));
  const active = items.filter((item) => item.status !== "done" && item.batchStatus !== "reverted");
  return {
    total: items.length,
    activeCount: active.length,
    highCount: active.filter((item) => item.priority === "high").length,
    byReason: active.reduce((acc, item) => {
      acc[item.reason] = (acc[item.reason] || 0) + 1;
      return acc;
    }, {}),
    items: active.slice(0, 6)
  };
}

function renderPhase19CleanupQueue() {
  const queue = buildPhase19CleanupQueue();
  if (!queue.total) return "";
  return `
    <div class="phase19-cleanup-queue">
      <div>
        <strong>导入后整理队列</strong>
        <small>把缺字段、低分草稿和冲突复核集中成下一步整理清单。</small>
      </div>
      <div class="phase19-review-strip">
        <span><b>${escapeHtml(queue.activeCount)}</b><small>待整理</small></span>
        <span><b>${escapeHtml(queue.highCount)}</b><small>高优先级</small></span>
        <span><b>${escapeHtml(Object.keys(queue.byReason).length)}</b><small>问题类型</small></span>
      </div>
      ${queue.items.map((item) => `
        <article>
          <b>${escapeHtml(item.title || item.memoryId || "未命名展品")}</b>
          <small>${escapeHtml(item.batchName)} / ${escapeHtml(item.reason || "待补全")}</small>
          <span>${escapeHtml(item.recommendation || "交给第十八阶段长期助理继续补全。")}</span>
        </article>
      `).join("")}
    </div>
  `;
}

function buildPhase19BatchAuditPackage(batch) {
  if (!batch) return null;
  return {
    schemaVersion: SCHEMA_VERSION,
    phase: 19,
    exportedAt: new Date().toISOString(),
    auditType: "phase19-import-batch",
    batch: {
      id: batch.id,
      name: batch.name,
      createdAt: batch.createdAt,
      status: batch.status,
      storage: batch.storage,
      format: batch.format,
      cleanupMode: batch.cleanupMode,
      count: batch.count,
      importedIds: batch.importedIds || [],
      titles: batch.titles || [],
      quality: batch.quality || {},
      conflictSummary: batch.conflictSummary || {},
      draftSummaries: batch.draftSummaries || [],
      conflictReviewItems: batch.conflictReviewItems || [],
      cleanupQueue: batch.cleanupQueue || [],
      itemRollbacks: batch.itemRollbacks || [],
      failedItems: batch.failedItems || [],
      followupTaskIds: batch.followupTaskIds || []
    },
    recommendation: "用于复盘某次外部资料导入的质量、冲突处理、撤销和后续整理任务。"
  };
}

function exportPhase19BatchAudit(batchId) {
  const batch = phase19ImportBatches.find((item) => item.id === batchId);
  const payload = buildPhase19BatchAuditPackage(batch);
  if (!payload) {
    setStorageStatus("未找到可导出的第十九阶段导入批次。", "warning");
    return;
  }
  downloadJsonPayload(payload, `memory-museum-phase19-audit-${batch.id}.json`);
  setStorageStatus(`已导出第十九阶段批次审计包：${batch.name || batch.id}`, "success");
}

function buildPhase19ImportPreview({ text = "", format = "auto", defaultSource = "日记", defaultHall = "auto", cleanupMode = "balanced", mappingTemplate = "auto" } = {}) {
  const detectedFormat = detectPhase19ImportFormat(text, format);
  const template = getPhase19MappingTemplate(mappingTemplate, detectedFormat);
  const segments = splitPhase19ImportText(text, detectedFormat, cleanupMode).slice(0, 30);
  const fieldSources = getPhase19FieldSources(text, detectedFormat);
  const drafts = annotatePhase19DuplicateRisks(segments.map((segment, index) => {
    const mappedSegment = applyPhase19FieldAliases({ ...segment, fieldSource: segment.fieldSource || fieldSources[index] }, template);
    const rawContent = limitText(mappedSegment.rawContent || mappedSegment.title, fieldLimits.rawContent);
    const analysis = mockAnalyzeMemory(rawContent || mappedSegment.title || "");
    const sourceType = sourceTypes.includes(mappedSegment.sourceType) ? mappedSegment.sourceType : defaultSource;
    const hall = defaultHall && defaultHall !== "auto" ? defaultHall : analysis.hall;
    Object.assign(segment, {
      title: mappedSegment.title,
      rawContent: mappedSegment.rawContent,
      date: mappedSegment.date,
      location: mappedSegment.location,
      people: mappedSegment.people,
      tags: mappedSegment.tags,
      sourceType: mappedSegment.sourceType,
      sourceTrace: mappedSegment.sourceTrace
    });
    return normalizeMemory({
      id: `phase19-import-${Date.now()}-${index}-${simpleChecksum(`${mappedSegment.title}:${rawContent}`).slice(0, 8)}`,
      title: limitText(segment.title && !/^(文本片段|聊天片段|CSV 片段|Markdown 片段)/.test(segment.title) ? segment.title : analysis.title, fieldLimits.title),
      hall,
      rawContent,
      exhibitText: analysis.exhibitText,
      date: segment.date || "",
      location: segment.location || analysis.location || "",
      people: segment.people || analysis.people || [],
      tags: [...normalizeList(segment.tags || analysis.tags), "外部导入", detectedFormat].filter(Boolean),
      emotions: analysis.emotions,
      emotionIntensity: analysis.emotionIntensity,
      sourceType,
      importance: analysis.importance,
      favorite: analysis.favorite,
      mediaNote: [analysis.mediaNote, `第十九阶段导入来源：${segment.sourceTrace || `${detectedFormat} 第 ${index + 1} 段`}`].filter(Boolean).join("\n")
    });
    return {
      ...applyPhase19MappingTemplate(draft, mappingTemplate, detectedFormat),
      selected: true,
      importTrace: segment.sourceTrace || `${detectedFormat} 第 ${index + 1} 段`
    };
  }));
  const missingContent = segments.filter((item) => !String(item.rawContent || "").trim()).length;
  const duplicateTitles = drafts.length - new Set(drafts.map((item) => item.title)).size;
  const plan = {
    phase: 19,
    mode: "external-import-preview",
    detectedFormat,
    cleanupMode,
    sourceLength: String(text || "").length,
    segmentCount: segments.length,
    draftCount: drafts.length,
    drafts,
    mapping: {
      defaultSource,
      defaultHall,
    cleanupMode,
    mappingTemplate,
    templateLabel: template.label,
      templateRules: {
        people: normalizeList(template.people),
        dateRule: template.dateRule || "",
        tags: normalizeList(template.tags)
      },
      title: "title/name/标题 或自动生成",
      rawContent: "rawContent/content/text/正文/内容 或段落正文",
      date: "date/日期",
      location: "location/地点",
      people: "people/人物",
      tags: "tags/标签"
    },
    quality: {
      ready: drafts.length > 0 && !missingContent,
      missingContent,
      duplicateTitles,
      duplicateRiskCount: drafts.filter((item) => item.duplicateRisk?.level === "high").length,
      selectedCount: drafts.filter((item) => item.selected !== false).length,
      recommendation: drafts.length
        ? "先检查标题、日期、人物和地点，再导入为展品草稿；导入后第十八阶段长期助理会继续给出补全建议。"
        : "粘贴日记、Markdown、CSV、JSON 或聊天记录后再生成预览。"
    }
  };
  localStorage.setItem(PHASE19_IMPORT_PREVIEW_KEY, JSON.stringify({ ...plan, drafts: drafts.slice(0, 10) }));
  return plan;
}

function buildPhase19ImportPlan(items = memories) {
  return {
    phase: 19,
    phaseName: "个人知识生态和外部导入版",
    mode: "external-source-preview-first",
    supportedFormats: ["text", "markdown", "csv", "json", "chat"],
    cleanupFeatures: ["delimiter-detection", "chat-speaker-grouping", "draft-selection", "draft-field-editing", "field-mapping-template", "custom-mapping-template", "mapping-template-persistence", "template-rule-defaults", "field-alias-rules", "batch-naming", "duplicate-precheck", "duplicate-decision", "conflict-preview", "conflict-review-desk", "review-status-flow", "cross-batch-compare", "batch-filter-compare", "audit-search", "import-report-view", "post-import-cleanup-queue", "batch-audit-export", "import-quality-score", "batch-detail", "source-trace", "import-batch-history", "batch-rollback", "item-rollback", "failed-item-retention", "phase18-followup-task", "followup-task-status", "quality-trend"],
    importEndpoint: "/api/imports/preview",
    applyEndpoint: "/api/memories/import",
    currentMemories: items.length,
    safety: {
      previewBeforeWrite: true,
      silentOverwrite: false,
      inheritsPhase16ConflictPolicy: true,
      afterImportAssistant: "phase18LongTermAgent"
    },
    recommendation: "先用导入预览拆分外部资料，确认字段后再写入展品库；写入后交给第十八阶段长期助理继续补全。"
  };
}

function loadPhase19ImportBatches() {
  try {
    const parsed = JSON.parse(localStorage.getItem(PHASE19_IMPORT_BATCHES_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 12) : [];
  } catch {
    return [];
  }
}

function persistPhase19ImportBatches() {
  localStorage.setItem(PHASE19_IMPORT_BATCHES_KEY, JSON.stringify(phase19ImportBatches.slice(0, 12)));
}

function updatePhase19ImportPreviewStorage() {
  if (!phase19ImportPreview) return;
  localStorage.setItem(PHASE19_IMPORT_PREVIEW_KEY, JSON.stringify({
    ...phase19ImportPreview,
    drafts: phase19ImportPreview.drafts.slice(0, 10)
  }));
}

function normalizePhase19DraftField(field, value) {
  if (["people", "tags", "emotions"].includes(field)) return normalizeList(value);
  if (field === "hall") return halls.some((hall) => hall.id === value) ? value : "daily";
  if (field === "emotionIntensity" || field === "importance") return Math.min(5, Math.max(1, Number(value) || 1));
  if (field === "favorite") return parseBoolean(value);
  return String(value || "").trim();
}

function updatePhase19DraftField(index, field, value) {
  if (!phase19ImportPreview?.drafts?.[index]) return;
  const previous = phase19ImportPreview.drafts[index];
  const next = normalizeMemory({
    ...previous,
    [field]: normalizePhase19DraftField(field, value)
  });
  next.selected = previous.selected !== false;
  next.importTrace = previous.importTrace || `${phase19ImportPreview.detectedFormat} 第 ${index + 1} 段`;
  phase19ImportPreview.drafts[index] = next;
  updatePhase19ImportPreviewStorage();
}

function buildPhase19DraftQuality(draft = {}) {
  const checks = [
    { id: "title", label: "标题", ok: Boolean(String(draft.title || "").trim()) },
    { id: "rawContent", label: "正文", ok: String(draft.rawContent || draft.exhibitText || "").trim().length >= 12 },
    { id: "date", label: "日期", ok: Boolean(String(draft.date || "").trim()) },
    { id: "location", label: "地点", ok: Boolean(String(draft.location || "").trim()) },
    { id: "people", label: "人物", ok: Array.isArray(draft.people) && draft.people.length > 0 },
    { id: "tags", label: "标签", ok: Array.isArray(draft.tags) && draft.tags.length >= 2 }
  ];
  const missing = checks.filter((item) => !item.ok);
  const score = Math.round(((checks.length - missing.length) / checks.length) * 100);
  return {
    score,
    status: score >= 84 ? "ready" : score >= 60 ? "review" : "needs-work",
    missing: missing.map((item) => item.id),
    labels: missing.map((item) => item.label),
    recommendation: missing.length
      ? `建议补全：${missing.map((item) => item.label).join("、")}`
      : "字段完整度较好，可导入后继续交给长期助理维护。"
  };
}

function summarizePhase19DraftQuality(drafts = []) {
  const qualities = drafts.map(buildPhase19DraftQuality);
  const averageScore = qualities.length
    ? Math.round(qualities.reduce((sum, item) => sum + item.score, 0) / qualities.length)
    : 0;
  const needsReviewCount = qualities.filter((item) => item.status !== "ready").length;
  const issueCounts = qualities.flatMap((item) => item.labels).reduce((acc, label) => {
    acc[label] = (acc[label] || 0) + 1;
    return acc;
  }, {});
  return {
    averageScore,
    needsReviewCount,
    readyCount: qualities.filter((item) => item.status === "ready").length,
    issueCounts,
    topIssues: Object.entries(issueCounts).sort((a, b) => b[1] - a[1]).slice(0, 4).map(([label, count]) => ({ label, count }))
  };
}

function createPhase19FollowupTasks(batch, drafts = []) {
  if (!batch?.id) return [];
  const importedIdSet = new Set(batch.importedIds || []);
  const tasks = drafts
    .map((draft, index) => {
      const quality = buildPhase19DraftQuality(draft);
      const memoryId = batch.importedIds?.[index] || draft.id;
      if (quality.status === "ready" || !importedIdSet.has(memoryId)) return null;
      return normalizePhase18Task({
        id: `phase18-task-phase19-${batch.id}-${simpleChecksum(memoryId).slice(0, 8)}`,
        status: quality.status === "needs-work" ? "reviewing" : "queued",
        label: `补全导入展品：${draft.title || memoryId}`,
        source: "phase19-import",
        priority: quality.status === "needs-work" ? "high" : "medium",
        memoryIds: [memoryId],
        detail: `${quality.recommendation}；来源批次 ${batch.id}`
      });
    })
    .filter(Boolean);
  if (!tasks.length) return [];
  const existing = loadPhase18TaskQueue();
  const existingIds = new Set(existing.map((task) => task.id));
  const fresh = tasks.filter((task) => !existingIds.has(task.id));
  if (fresh.length) {
    savePhase18TaskQueue([...fresh, ...existing].slice(0, 80));
    recordPhase18TaskAudit("phase19-import-followup-created", batch.id, `第十九阶段导入后生成 ${fresh.length} 条补全任务。`, {
      batchId: batch.id,
      taskCount: fresh.length,
      memoryIds: fresh.flatMap((task) => task.memoryIds || [])
    });
  }
  return fresh;
}

function getPhase19FollowupTaskSummary(batch = {}) {
  const ids = new Set(batch.followupTaskIds || []);
  const tasks = loadPhase18TaskQueue().filter((task) => ids.has(task.id));
  const byStatus = tasks.reduce((acc, task) => {
    acc[task.status] = (acc[task.status] || 0) + 1;
    return acc;
  }, {});
  return {
    total: tasks.length,
    active: tasks.filter((task) => ["queued", "reviewing", "failed"].includes(task.status)).length,
    completed: tasks.filter((task) => task.status === "applied").length,
    dismissed: tasks.filter((task) => task.status === "dismissed").length,
    byStatus
  };
}

function buildPhase19QualityTrend(batches = phase19ImportBatches) {
  const scored = batches.filter((batch) => typeof batch.quality?.averageScore === "number");
  if (!scored.length) return null;
  const active = scored.filter((batch) => batch.status !== "failed");
  const latest = active[0] || scored[0];
  const previous = active[1];
  const average = Math.round(active.reduce((sum, batch) => sum + (batch.quality?.averageScore || 0), 0) / Math.max(1, active.length));
  const delta = previous ? (latest.quality.averageScore || 0) - (previous.quality.averageScore || 0) : 0;
  return {
    latestScore: latest.quality?.averageScore || 0,
    averageScore: average,
    delta,
    failedCount: batches.filter((batch) => batch.status === "failed").length,
    reviewCount: active.reduce((sum, batch) => sum + (batch.quality?.needsReviewCount || 0), 0)
  };
}

function renderPhase19QualityTrend() {
  const trend = buildPhase19QualityTrend();
  if (!trend) return "";
  const deltaLabel = trend.delta > 0 ? `+${trend.delta}` : String(trend.delta);
  return `
    <div class="phase19-quality-trend">
      <span><b>${escapeHtml(trend.latestScore)}</b><small>最近批次</small></span>
      <span><b>${escapeHtml(trend.averageScore)}</b><small>平均分</small></span>
      <span><b>${escapeHtml(deltaLabel)}</b><small>较上批</small></span>
      <span><b>${escapeHtml(trend.reviewCount)}</b><small>待补全</small></span>
      <span><b>${escapeHtml(trend.failedCount)}</b><small>失败批次</small></span>
    </div>
  `;
}

function togglePhase19BatchDetail(batchId) {
  activePhase19BatchId = activePhase19BatchId === batchId ? "" : batchId;
  renderPhase19ImportPreview(phase19ImportPreview);
}

function createPhase19ImportBatchRecord(drafts = [], result = {}) {
  const importedIds = Array.isArray(result.importedIds) && result.importedIds.length
    ? result.importedIds
    : drafts.map((draft) => draft.id).filter(Boolean);
  const quality = summarizePhase19DraftQuality(drafts);
  const createdAt = new Date().toISOString();
  const previewDrafts = phase19ImportPreview?.drafts || drafts;
  const conflictReviewItems = previewDrafts
    .filter((draft) => draft.duplicateRisk?.level === "high")
    .map((draft) => ({
      id: draft.id,
      title: draft.title,
      matchedId: draft.duplicateRisk?.memoryId || "",
      matchedTitle: draft.duplicateRisk?.title || "",
      action: draft.duplicateAction || "skip",
      status: draft.duplicateAction === "new" ? "imported" : "pending",
      reason: draft.duplicateRisk?.reason || ""
    })).slice(0, 12);
  const cleanupQueue = drafts.map((draft, index) => {
    const itemQuality = buildPhase19DraftQuality(draft);
    if (itemQuality.status === "ready") return null;
    return {
      id: `phase19-cleanup-${simpleChecksum(`${importedIds[index] || draft.id}:${itemQuality.score}`).slice(0, 10)}`,
      memoryId: importedIds[index] || draft.id,
      title: draft.title,
      reason: itemQuality.labels.join("、") || "字段待补全",
      priority: itemQuality.status === "needs-work" ? "high" : "medium",
      status: "queued",
      recommendation: itemQuality.recommendation
    };
  }).filter(Boolean).slice(0, 12);
  return {
    id: `phase19-batch-${createdAt.replace(/[-:.TZ]/g, "").slice(0, 14)}-${simpleChecksum(importedIds.join("|")).slice(0, 6)}`,
    name: limitText(elements.phase19BatchName?.value || `导入批次 ${createdAt.slice(0, 10)}`, 60),
    createdAt,
    status: "active",
    storage: databaseAvailable ? "sqlite" : "browser",
    format: phase19ImportPreview?.detectedFormat || "unknown",
    cleanupMode: phase19ImportPreview?.cleanupMode || "balanced",
    count: importedIds.length,
    importedIds,
    titles: drafts.map((draft) => draft.title).filter(Boolean).slice(0, 6),
    quality,
    conflictSummary: {
      duplicateRiskCount: conflictReviewItems.length,
      importedAsNewCount: drafts.filter((draft) => draft.duplicateAction === "new" && draft.duplicateRisk?.level === "high").length,
      skippedCount: previewDrafts.filter((draft) => draft.duplicateAction === "skip").length || 0,
      reviewCount: previewDrafts.filter((draft) => draft.duplicateAction === "review").length || 0
    },
    draftSummaries: drafts.map((draft, index) => ({
      id: importedIds[index] || draft.id,
      title: draft.title,
      quality: buildPhase19DraftQuality(draft),
      importTrace: draft.importTrace || "",
      duplicateAction: draft.duplicateAction || "new",
      status: "active"
    })).slice(0, 12),
    failedItems: [],
    itemRollbacks: [],
    conflictReviewItems,
    cleanupQueue,
    followupTaskIds: []
  };
}

function createPhase19FailedImportBatch(drafts = [], error = {}) {
  const createdAt = new Date().toISOString();
  const conflictReviewItems = drafts
    .filter((draft) => draft.duplicateRisk?.level === "high")
    .map((draft) => ({
      id: draft.id,
      title: draft.title,
      matchedId: draft.duplicateRisk?.memoryId || "",
      matchedTitle: draft.duplicateRisk?.title || "",
      action: draft.duplicateAction || "review",
      status: "pending",
      reason: draft.duplicateRisk?.reason || ""
    })).slice(0, 12);
  return {
    id: `phase19-failed-${createdAt.replace(/[-:.TZ]/g, "").slice(0, 14)}-${simpleChecksum(`${error.message || "failed"}:${drafts.length}`).slice(0, 6)}`,
    name: limitText(elements.phase19BatchName?.value || `失败批次 ${createdAt.slice(0, 10)}`, 60),
    createdAt,
    status: "failed",
    storage: databaseAvailable ? "sqlite" : "browser",
    format: phase19ImportPreview?.detectedFormat || "unknown",
    cleanupMode: phase19ImportPreview?.cleanupMode || "balanced",
    count: 0,
    importedIds: [],
    titles: drafts.map((draft) => draft.title).filter(Boolean).slice(0, 6),
    quality: summarizePhase19DraftQuality(drafts),
    conflictSummary: {
      duplicateRiskCount: drafts.filter((draft) => draft.duplicateRisk?.level === "high").length,
      importedAsNewCount: 0,
      skippedCount: drafts.filter((draft) => draft.duplicateAction === "skip").length,
      reviewCount: drafts.filter((draft) => draft.duplicateAction === "review").length
    },
    draftSummaries: drafts.map((draft) => ({
      id: draft.id,
      title: draft.title,
      quality: buildPhase19DraftQuality(draft),
      importTrace: draft.importTrace || "",
      status: "failed",
      error: error.message || "import failed"
    })).slice(0, 12),
    failedItems: drafts.map((draft) => ({ id: draft.id, title: draft.title, error: error.message || "import failed" })).slice(0, 12),
    itemRollbacks: [],
    conflictReviewItems,
    cleanupQueue: drafts.map((draft) => ({
      id: `phase19-cleanup-failed-${simpleChecksum(draft.id).slice(0, 10)}`,
      memoryId: draft.id,
      title: draft.title,
      reason: "导入失败",
      priority: "high",
      status: "queued",
      recommendation: error.message || "保留失败项，修复后重新导入。"
    })).slice(0, 12),
    followupTaskIds: []
  };
}

function recordPhase19ImportBatch(record) {
  if (!record?.importedIds?.length && !record?.failedItems?.length) return;
  phase19ImportBatches = [record, ...phase19ImportBatches.filter((batch) => batch.id !== record.id)].slice(0, 12);
  persistPhase19ImportBatches();
}

function renderPhase19BatchHistory() {
  if (!phase19ImportBatches.length) return "";
  return `
    <div class="phase19-batch-history">
      <div class="phase19-batch-heading">
        <strong>最近导入批次</strong>
        <small>可撤销仍存在的批次展品</small>
      </div>
      ${phase19ImportBatches.slice(0, 4).map((batch) => `
        <article data-status="${escapeHtml(batch.status || "active")}">
          <span>
            <b>${escapeHtml(batch.count || batch.importedIds?.length || 0)} 件</b>
            <small>${escapeHtml(batch.format || "unknown")} / ${escapeHtml(batch.cleanupMode || "balanced")}</small>
          </span>
          <small>${escapeHtml((batch.titles || []).join("、") || batch.id)}</small>
          <button type="button" data-phase19-rollback="${escapeHtml(batch.id)}" ${batch.status === "reverted" ? "disabled" : ""}>${batch.status === "reverted" ? "已撤销" : "撤销批次"}</button>
        </article>
      `).join("")}
    </div>
  `;
}

function renderPhase19BatchHistoryV4() {
  if (!phase19ImportBatches.length) return "";
  return `
    <div class="phase19-batch-history">
      <div class="phase19-batch-heading">
        <strong>最近导入批次</strong>
        <small>查看质量、补全任务和撤销状态</small>
      </div>
      ${phase19ImportBatches.slice(0, 4).map((batch) => `
        <article data-status="${escapeHtml(batch.status || "active")}">
          <span>
            <b>${escapeHtml(batch.count || batch.importedIds?.length || 0)} 件</b>
            <small>${escapeHtml(batch.format || "unknown")} / ${escapeHtml(batch.cleanupMode || "balanced")}</small>
          </span>
          <span>
            <b>${escapeHtml(batch.quality?.averageScore ?? "-")} 分</b>
            <small>${escapeHtml(batch.quality?.needsReviewCount || 0)} 件待补全</small>
          </span>
          <small>${escapeHtml((batch.titles || []).join("、") || batch.id)}</small>
          <button type="button" data-phase19-batch-toggle="${escapeHtml(batch.id)}">${activePhase19BatchId === batch.id ? "收起详情" : "查看详情"}</button>
          <button type="button" data-phase19-rollback="${escapeHtml(batch.id)}" ${batch.status === "reverted" ? "disabled" : ""}>${batch.status === "reverted" ? "已撤销" : "撤销批次"}</button>
        </article>
        ${activePhase19BatchId === batch.id ? `
          <div class="phase19-batch-detail">
            ${(batch.draftSummaries || []).map((item) => `
              <span data-quality="${escapeHtml(item.quality?.status || "review")}">
                <b>${escapeHtml(item.title || item.id)}</b>
                <small>${escapeHtml(item.quality?.score ?? "-")} 分 / ${escapeHtml(item.quality?.recommendation || "等待复核")}</small>
              </span>
            `).join("")}
            ${(batch.followupTaskIds || []).length ? `<small>已生成第十八阶段补全任务：${escapeHtml(batch.followupTaskIds.length)} 条</small>` : ""}
          </div>
        ` : ""}
      `).join("")}
    </div>
  `;
}

function renderPhase19BatchHistoryV5() {
  if (!phase19ImportBatches.length) return "";
  const filteredBatches = getPhase19FilteredBatches();
  return `
    <div class="phase19-batch-history">
      <div class="phase19-batch-heading">
        <strong>最近导入批次</strong>
        <small>质量趋势、失败项、补全任务和撤销状态</small>
      </div>
      ${renderPhase19BatchFilters(filteredBatches)}
      ${renderPhase19ImportReport(filteredBatches)}
      ${renderPhase19QualityTrend()}
      ${renderPhase19BatchComparison(filteredBatches)}
      ${renderPhase19CleanupQueue()}
      ${renderPhase19ConflictReviewDesk()}
      ${filteredBatches.slice(0, 4).map((batch) => {
        const taskSummary = getPhase19FollowupTaskSummary(batch);
        return `
          <article data-status="${escapeHtml(batch.status || "active")}">
            <span>
              <b>${escapeHtml(batch.count || batch.importedIds?.length || 0)} 件</b>
              <small>${escapeHtml(batch.format || "unknown")} / ${escapeHtml(batch.cleanupMode || "balanced")}</small>
            </span>
            <span>
              <b>${escapeHtml(batch.quality?.averageScore ?? "-")} 分</b>
              <small>${escapeHtml(batch.quality?.needsReviewCount || 0)} 件待补全</small>
            </span>
            <span>
              <b>${escapeHtml(taskSummary.active)}</b>
              <small>活跃补全任务</small>
            </span>
            <span>
              <b>${escapeHtml(batch.conflictSummary?.duplicateRiskCount || 0)}</b>
              <small>重复风险</small>
            </span>
            <small>${escapeHtml(batch.name || (batch.titles || []).join("、") || batch.id)}</small>
            <button type="button" data-phase19-batch-toggle="${escapeHtml(batch.id)}">${activePhase19BatchId === batch.id ? "收起详情" : "查看详情"}</button>
            <button type="button" data-phase19-export-audit="${escapeHtml(batch.id)}">导出审计</button>
            <button type="button" data-phase19-rollback="${escapeHtml(batch.id)}" ${batch.status === "reverted" || batch.status === "failed" ? "disabled" : ""}>${batch.status === "reverted" ? "已撤销" : batch.status === "failed" ? "导入失败" : "撤销批次"}</button>
          </article>
          ${activePhase19BatchId === batch.id ? `
            <div class="phase19-batch-detail">
              ${batch.status === "failed" ? `<small>失败项已保留：${escapeHtml((batch.failedItems || []).map((item) => item.title || item.id).join("、") || "无标题草稿")}</small>` : ""}
              ${batch.conflictSummary?.duplicateRiskCount ? `<small>重复处理：跳过 ${escapeHtml(batch.conflictSummary.skippedCount || 0)} / 作为新展品 ${escapeHtml(batch.conflictSummary.importedAsNewCount || 0)} / 待复核 ${escapeHtml(batch.conflictSummary.reviewCount || 0)}</small>` : ""}
              ${(batch.cleanupQueue || []).length ? `<small>整理队列：${escapeHtml((batch.cleanupQueue || []).filter((item) => item.status !== "done").length)} 条待处理</small>` : ""}
              ${(batch.conflictReviewItems || []).length ? `<small>冲突复核：${escapeHtml((batch.conflictReviewItems || []).filter((item) => item.status !== "resolved").length)} 条未关闭</small>` : ""}
              ${(batch.draftSummaries || []).map((item) => `
                <span data-quality="${escapeHtml(item.quality?.status || "review")}" data-status="${escapeHtml(item.status || "active")}">
                  <b>${escapeHtml(item.title || item.id)}</b>
                  <small>${escapeHtml(item.quality?.score ?? "-")} 分 / ${escapeHtml(item.error || item.quality?.recommendation || "等待复核")}</small>
                  ${batch.status !== "failed" ? `<button type="button" data-phase19-item-rollback="${escapeHtml(item.id)}" data-phase19-item-batch="${escapeHtml(batch.id)}" ${item.status === "reverted" ? "disabled" : ""}>${item.status === "reverted" ? "已撤销" : "撤销单项"}</button>` : ""}
                </span>
              `).join("")}
              ${(batch.followupTaskIds || []).length ? `<small>第十八阶段补全任务：${escapeHtml(taskSummary.active)} 条活跃 / ${escapeHtml(taskSummary.completed)} 条完成 / ${escapeHtml(taskSummary.dismissed)} 条忽略</small>` : ""}
              ${(batch.itemRollbacks || []).length ? `<small>已单项撤销：${escapeHtml((batch.itemRollbacks || []).map((item) => item.title || item.id).join("、"))}</small>` : ""}
            </div>
          ` : ""}
        `;
      }).join("")}
    </div>
  `;
}

async function rollbackPhase19ImportBatch(batchId) {
  const batch = phase19ImportBatches.find((item) => item.id === batchId);
  if (!batch || batch.status === "reverted") {
    setStorageStatus("没有可撤销的第十九阶段导入批次。", "warning");
    return;
  }
  const ids = Array.isArray(batch.importedIds) ? batch.importedIds.filter(Boolean) : [];
  if (!ids.length) {
    setStorageStatus("该导入批次没有记录展品 ID，无法撤销。", "warning");
    return;
  }
  const confirmed = window.confirm(`即将撤销第十九阶段导入批次中的 ${ids.length} 件展品。是否继续？`);
  if (!confirmed) return;
  try {
    if (batch.storage === "sqlite") {
      if (!databaseAvailable) {
        setStorageStatus("该批次写入 SQLite，但当前数据库未连接，暂时无法撤销。", "warning");
        return;
      }
      for (const id of ids) await deleteMemoryFromDatabase(id);
      await loadMemoriesFromDatabase({ silent: true });
    } else {
      const idSet = new Set(ids);
      memories = memories.filter((memory) => !idSet.has(memory.id));
      persistMemories(memories);
    }
    batch.status = "reverted";
    batch.revertedAt = new Date().toISOString();
    persistPhase19ImportBatches();
    render();
    setStorageStatus(`已撤销第十九阶段导入批次：移除 ${ids.length} 件展品。`, "success");
  } catch (error) {
    setStorageStatus(`撤销第十九阶段导入批次失败：${error.message}`, "warning");
  }
}

async function rollbackPhase19ImportItem(batchId, itemId) {
  const batch = phase19ImportBatches.find((item) => item.id === batchId);
  if (!batch || batch.status === "reverted" || batch.status === "failed") {
    setStorageStatus("该第十九阶段批次没有可撤销的单项展品。", "warning");
    return;
  }
  const target = (batch.draftSummaries || []).find((item) => item.id === itemId);
  if (!target || target.status === "reverted") {
    setStorageStatus("该展品已经撤销或不在批次中。", "warning");
    return;
  }
  const confirmed = window.confirm(`即将从当前导入批次中撤销《${target.title || itemId}》。是否继续？`);
  if (!confirmed) return;
  try {
    if (batch.storage === "sqlite") {
      if (!databaseAvailable) {
        setStorageStatus("该单项写入 SQLite，但当前数据库未连接，暂时无法撤销。", "warning");
        return;
      }
      await deleteMemoryFromDatabase(itemId);
      await loadMemoriesFromDatabase({ silent: true });
    } else {
      memories = memories.filter((memory) => memory.id !== itemId);
      persistMemories(memories);
    }
    const now = new Date().toISOString();
    batch.draftSummaries = (batch.draftSummaries || []).map((item) => item.id === itemId ? { ...item, status: "reverted", revertedAt: now } : item);
    batch.itemRollbacks = [...(batch.itemRollbacks || []), { id: itemId, title: target.title || itemId, revertedAt: now }].slice(-20);
    const activeCount = (batch.draftSummaries || []).filter((item) => item.status !== "reverted").length;
    if (!activeCount) {
      batch.status = "reverted";
      batch.revertedAt = now;
    }
    persistPhase19ImportBatches();
    render();
    setStorageStatus(`已撤销第十九阶段批次内展品：${target.title || itemId}。`, "success");
  } catch (error) {
    setStorageStatus(`撤销第十九阶段批次单项失败：${error.message}`, "warning");
  }
}

function renderPhase19ImportPreview(plan = phase19ImportPreview) {
  if (!elements.phase19ImportPreview) return;
  if (!plan?.drafts?.length) {
    elements.phase19ImportPreview.innerHTML = `
      <div class="phase19-empty">
        <strong>等待外部资料</strong>
        <small>第十九阶段会先生成导入预览，不会静默写入展品库。</small>
      </div>
      ${renderPhase19BatchHistoryV5()}
    `;
    if (elements.phase19ApplyButton) elements.phase19ApplyButton.disabled = true;
    return;
  }
  if (elements.phase19ApplyButton) elements.phase19ApplyButton.disabled = false;
  const selectedCount = plan.drafts.filter((draft) => draft.selected !== false).length;
  elements.phase19ImportPreview.innerHTML = `
    <div class="phase19-import-summary">
      <span><b>${escapeHtml(plan.detectedFormat)}</b><small>识别格式</small></span>
      <span><b>${escapeHtml(plan.draftCount)}</b><small>展品草稿</small></span>
      <span><b>${escapeHtml(selectedCount)}</b><small>已选择</small></span>
      <span><b>${escapeHtml(plan.quality?.duplicateTitles || 0)}</b><small>重名标题</small></span>
      <span><b>${escapeHtml(plan.quality?.duplicateRiskCount || 0)}</b><small>疑似重复</small></span>
    </div>
    <div class="phase19-selection-tools">
      <button type="button" data-phase19-select-all>全选草稿</button>
      <button type="button" data-phase19-select-none>清空选择</button>
      <small>${escapeHtml(plan.mapping?.templateLabel || "自动模板")} / ${escapeHtml(plan.mapping?.cleanupMode || "balanced")} / ${escapeHtml(plan.quality?.missingContent || 0)} 条空内容</small>
    </div>
    <div class="phase19-template-meta">
      <span><b>${escapeHtml(phase19CustomTemplates.length)}</b><small>自定义模板</small></span>
      <small>当前模板会影响来源、展厅和标签；可用“模板标签 + 批次名称”保存下一次复用。</small>
    </div>
    ${renderPhase19ConflictPreview(plan)}
    <small>${escapeHtml(plan.quality?.recommendation || "")}</small>
    <div class="phase19-draft-list">
      ${plan.drafts.slice(0, 8).map((draft, index) => `
        <article data-selected="${draft.selected !== false ? "true" : "false"}">
          <label class="phase19-draft-select">
            <input type="checkbox" data-phase19-draft-select="${escapeHtml(index)}" ${draft.selected !== false ? "checked" : ""}>
            <strong>${escapeHtml(draft.title)}</strong>
          </label>
          <small>${escapeHtml(getHallName(draft.hall))} / ${escapeHtml(draft.sourceType)} / ${escapeHtml(draft.date || "日期待补")}</small>
          <small>${escapeHtml(draft.importTrace || "来源待确认")}</small>
          ${draft.duplicateRisk?.level === "high" ? `<small class="phase19-duplicate-risk">疑似重复：${escapeHtml(draft.duplicateRisk.title || draft.duplicateRisk.reason)}</small>` : ""}
          ${draft.duplicateRisk?.level === "high" ? `
            <label class="phase19-duplicate-action">重复处理
              <select data-phase19-duplicate-action="${escapeHtml(index)}">
                <option value="skip" ${draft.duplicateAction === "skip" ? "selected" : ""}>跳过导入</option>
                <option value="new" ${draft.duplicateAction === "new" ? "selected" : ""}>作为新展品</option>
                <option value="review" ${draft.duplicateAction === "review" ? "selected" : ""}>保留待复核</option>
              </select>
            </label>
          ` : ""}
          <div class="phase19-quality" data-quality="${escapeHtml(buildPhase19DraftQuality(draft).status)}">
            <b>${escapeHtml(buildPhase19DraftQuality(draft).score)} 分</b>
            <small>${escapeHtml(buildPhase19DraftQuality(draft).recommendation)}</small>
          </div>
          <div class="phase19-draft-editor">
            <label>标题<input data-phase19-draft-field="title" data-phase19-draft-index="${escapeHtml(index)}" value="${escapeHtml(draft.title)}" maxlength="80"></label>
            <label>日期<input data-phase19-draft-field="date" data-phase19-draft-index="${escapeHtml(index)}" value="${escapeHtml(draft.date || "")}" maxlength="20"></label>
            <label>地点<input data-phase19-draft-field="location" data-phase19-draft-index="${escapeHtml(index)}" value="${escapeHtml(draft.location || "")}" maxlength="80"></label>
            <label>人物<input data-phase19-draft-field="people" data-phase19-draft-index="${escapeHtml(index)}" value="${escapeHtml((draft.people || []).join("、"))}" maxlength="120"></label>
            <label class="wide">标签<input data-phase19-draft-field="tags" data-phase19-draft-index="${escapeHtml(index)}" value="${escapeHtml((draft.tags || []).join("、"))}" maxlength="160"></label>
            <label class="wide">正文<textarea data-phase19-draft-field="rawContent" data-phase19-draft-index="${escapeHtml(index)}" maxlength="3000">${escapeHtml(draft.rawContent || draft.exhibitText || "")}</textarea></label>
          </div>
          <div>${draft.tags.slice(0, 4).map((tag) => `<span>${escapeHtml(tag)}</span>`).join("")}</div>
        </article>
      `).join("")}
    </div>
    ${renderPhase19BatchHistoryV5()}
  `;
}

function setPhase19DraftSelection(index, selected) {
  if (!phase19ImportPreview?.drafts?.[index]) return;
  phase19ImportPreview.drafts[index].selected = selected;
  updatePhase19ImportPreviewStorage();
  renderPhase19ImportPreview(phase19ImportPreview);
}

function setAllPhase19DraftSelection(selected) {
  if (!phase19ImportPreview?.drafts?.length) return;
  phase19ImportPreview.drafts = phase19ImportPreview.drafts.map((draft) => ({ ...draft, selected }));
  updatePhase19ImportPreviewStorage();
  renderPhase19ImportPreview(phase19ImportPreview);
}

function setPhase19DuplicateAction(index, action) {
  if (!phase19ImportPreview?.drafts?.[index]) return;
  const nextAction = ["skip", "new", "review"].includes(action) ? action : "new";
  phase19ImportPreview.drafts[index] = {
    ...phase19ImportPreview.drafts[index],
    duplicateAction: nextAction,
    selected: nextAction === "new"
  };
  updatePhase19ImportPreviewStorage();
  renderPhase19ImportPreview(phase19ImportPreview);
}

function renderPhase19ImportLab() {
  if (!elements.phase19ImportSection) return;
  renderPhase19MappingTemplateOptions();
  if (elements.phase19BatchFilter) elements.phase19BatchFilter.value = phase19BatchFilter;
  renderPhase19ImportPreview(phase19ImportPreview);
}

function loadPhase19Sample() {
  if (!elements.phase19ImportText) return;
  elements.phase19ImportFormat.value = "markdown";
  elements.phase19DefaultSource.value = "日记";
  elements.phase19ImportText.value = [
    "# 外婆家的晚饭",
    "2021 年冬天，外婆把热汤端到桌上，窗外很冷，但屋里都是饭菜的香气。",
    "",
    "# 毕业前的操场",
    "我们在操场边拍了最后一张照片，大家都在笑，但心里知道以后很难再这样聚齐。"
  ].join("\n");
  setStorageStatus("已放入第十九阶段导入示例。", "success");
}

function previewPhase19Import() {
  const text = elements.phase19ImportText?.value || "";
  phase19ImportPreview = buildPhase19ImportPreview({
    text,
    format: elements.phase19ImportFormat?.value || "auto",
    defaultSource: elements.phase19DefaultSource?.value || "日记",
    defaultHall: elements.phase19DefaultHall?.value || "auto",
    cleanupMode: elements.phase19CleanupMode?.value || "balanced",
    mappingTemplate: elements.phase19MappingTemplate?.value || "auto"
  });
  renderPhase19ImportPreview(phase19ImportPreview);
  setStorageStatus(`已生成第十九阶段导入预览：${phase19ImportPreview.draftCount} 件草稿。`, phase19ImportPreview.draftCount ? "success" : "warning");
}

async function applyPhase19ImportPreview() {
  if (!phase19ImportPreview?.drafts?.length) {
    setStorageStatus("请先生成第十九阶段导入预览。", "warning");
    return;
  }
  const drafts = phase19ImportPreview.drafts
    .filter((draft) => draft.selected !== false && !["skip", "review"].includes(draft.duplicateAction))
    .map((draft) => ({
      ...normalizeMemory(draft),
      duplicateRisk: draft.duplicateRisk,
      duplicateAction: draft.duplicateAction || "new",
      importTrace: draft.importTrace || ""
    }));
  if (!drafts.length) {
    setStorageStatus("请至少选择一件第十九阶段导入草稿。", "warning");
    return;
  }
  try {
    let importResult = { importedIds: drafts.map((draft) => draft.id).filter(Boolean) };
    if (databaseAvailable) {
      const result = await importMemoriesToDatabase(drafts);
      importResult = result;
      memories = result.memories;
    } else {
      memories = [...drafts, ...memories].map(normalizeMemory);
    }
    const batchRecord = createPhase19ImportBatchRecord(drafts, importResult);
    const followupTasks = createPhase19FollowupTasks(batchRecord, drafts);
    batchRecord.followupTaskIds = followupTasks.map((task) => task.id);
    recordPhase19ImportBatch(batchRecord);
    persistMemories(memories);
    phase19ImportPreview = null;
    localStorage.removeItem(PHASE19_IMPORT_PREVIEW_KEY);
    render();
    setStorageStatus(`第十九阶段导入完成：写入 ${drafts.length} 件展品草稿。`, "success");
  } catch (error) {
    recordPhase19ImportBatch(createPhase19FailedImportBatch(drafts, error));
    renderPhase19ImportPreview(phase19ImportPreview);
    setStorageStatus(`第十九阶段导入失败：${error.message}`, "warning");
  }
}

function getAttachmentCategory(item = {}) {
  const text = `${item.type || ""} ${item.name || ""} ${item.note || ""}`.toLowerCase();
  if (/图片|照片|截图|合照|相册|image|photo|png|jpe?g|webp|gif|heic/.test(text)) return "图片";
  if (/ocr|扫描|截图文字|识别文字|文字提取|scan/.test(text)) return "OCR";
  if (/语音|录音|转写|音频|voice|audio|mp3|wav|m4a/.test(text)) return "语音";
  if (/文档|报告|笔记|pdf|docx?|txt|md|markdown/.test(text)) return "文档";
  if (/视频|录像|video|mp4|mov|avi/.test(text)) return "视频";
  return "其他";
}

function buildAttachmentTypeCounts(attachments = []) {
  return normalizeAttachments(attachments).reduce((counts, item) => {
    const category = getAttachmentCategory(item);
    counts[category] = (counts[category] || 0) + 1;
    return counts;
  }, {});
}

function sortAttachmentTypeCounts(counts = {}) {
  return Object.entries(counts).sort((a, b) => {
    const orderDelta = attachmentTypeOrder.indexOf(a[0]) - attachmentTypeOrder.indexOf(b[0]);
    return orderDelta || b[1] - a[1] || a[0].localeCompare(b[0]);
  });
}

function formatAttachmentTypeSummary(attachments = [], maxItems = 3) {
  const counts = buildAttachmentTypeCounts(attachments);
  return sortAttachmentTypeCounts(counts)
    .slice(0, maxItems)
    .map(([type, count]) => `${type} ${count}`)
    .join(" / ");
}

function loadMemories() {
  let stored = null;
  try {
    stored = localStorage.getItem(STORAGE_KEY);
  } catch {
    return seedMemories.map(normalizeMemory);
  }

  if (!stored) {
    const seeded = seedMemories.map(normalizeMemory);
    persistMemories(seeded);
    return seeded;
  }

  try {
    const parsed = JSON.parse(stored);
    if (!Array.isArray(parsed)) throw new Error("Stored memories must be an array.");
    const normalized = parsed.map(normalizeMemory);
    persistMemories(normalized);
    return normalized;
  } catch {
    const seeded = seedMemories.map(normalizeMemory);
    persistMemories(seeded);
    return seeded;
  }
}

function saveMemories(nextMemories = memories) {
  if (persistMemories(nextMemories)) return true;

  alert("保存失败：浏览器本地存储空间可能已满。可以先导出备份，再删除一些展品。");
  return false;
}

function splitList(value) {
  return value
    .split(/[,，、]/)
    .map((item) => limitText(item, fieldLimits.listItem))
    .filter(Boolean)
    .slice(0, fieldLimits.listLength);
}

function selectedEmotions() {
  return Array.from(document.querySelectorAll('input[name="emotion"]:checked')).map((item) => item.value);
}

function setSelectedEmotions(emotions) {
  document.querySelectorAll('input[name="emotion"]').forEach((item) => {
    item.checked = emotions.includes(item.value);
  });
}

function getHallName(hallId) {
  return halls.find((hall) => hall.id === hallId)?.name || "日常展厅";
}

function getImportanceLabel(value) {
  return importanceLabels[(Number(value) || 1) - 1] || importanceLabels[0];
}

function formatDate(value) {
  if (!value) return "";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "";
  return date.toLocaleDateString("zh-CN", { year: "numeric", month: "2-digit", day: "2-digit" });
}

function formatDateTime(value) {
  if (!value) return "";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "";
  return date.toLocaleString("zh-CN", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" });
}

function pillHtml(value, className = "") {
  return `<span class="pill ${className}">${escapeHtml(value)}</span>`;
}

function buildStructuredMemory(memory) {
  return {
    schemaVersion: SCHEMA_VERSION,
    id: memory.id,
    title: memory.title,
    hall: {
      id: memory.hall,
      name: getHallName(memory.hall)
    },
    sourceType: memory.sourceType,
    rawContent: memory.rawContent,
    exhibitText: memory.exhibitText,
    date: memory.date,
    location: memory.location,
    people: memory.people || [],
    emotions: memory.emotions || [],
    emotionIntensity: memory.emotionIntensity,
    tags: memory.tags || [],
    importance: memory.importance,
    importanceLabel: getImportanceLabel(memory.importance),
    favorite: memory.favorite,
    coverImage: memory.coverImage || "",
    mediaNote: memory.mediaNote || "",
    attachments: Array.isArray(memory.attachments) ? memory.attachments : [],
    agentRunId: memory.agentRunId || "",
    createdAt: memory.createdAt,
    updatedAt: memory.updatedAt || ""
  };
}

function buildCollectionExport() {
  const exportedAt = new Date().toISOString();
  return {
    app: "AI 记忆博物馆",
    schemaVersion: SCHEMA_VERSION,
    exportedAt,
    phase16Sync: buildPhase16SyncManifest(memories, { exportedAt }),
    phase17SyncAdapter: buildPhase17SyncAdapter(memories, { exportedAt }),
    phase18LongTermAgent: buildPhase18LongTermAgent(memories),
    phase19ImportPlan: buildPhase19ImportPlan(memories),
    halls: halls.filter((hall) => hall.id !== "all").map((hall) => ({
      id: hall.id,
      name: hall.name,
      description: hall.description
    })),
    emotions: emotionOptions,
    sourceTypes,
    mediaSummary: buildMediaSummary(memories),
    phase10Handoff: buildPhase10Handoff(memories),
    phase10Insights: buildPhase10Insights(memories),
    phase11WorkflowBlueprint: buildPhase11WorkflowBlueprint(memories),
    phase12Sovereignty: buildLocalPhase12Sovereignty(memories),
    ...buildPhase15AssetExportReview(),
    privacyPolicy: getPrivacyPolicy(),
    memories: memories.map(buildStructuredMemory)
  };
}

function buildPhase16SyncManifest(sourceMemories = memories, options = {}) {
  const exportedAt = options.exportedAt || new Date().toISOString();
  const assetItems = buildAssetPackageItems();
  const items = sourceMemories.map((memory) => {
    const structured = buildStructuredMemory(memory);
    return {
      id: structured.id,
      title: structured.title,
      updatedAt: structured.updatedAt || structured.createdAt || "",
      checksum: simpleChecksum(stableStringify({
        title: structured.title,
        hall: structured.hall?.id || structured.hall,
        rawContent: structured.rawContent,
        exhibitText: structured.exhibitText,
        date: structured.date,
        location: structured.location,
        people: structured.people,
        emotions: structured.emotions,
        tags: structured.tags,
        importance: structured.importance,
        attachments: structured.attachments
      }))
    };
  });
  return {
    phase: 17,
    phaseName: "真实多设备同步适配层版",
    mode: "manual-json-local-first",
    batchId: `sync-${exportedAt.replace(/[-:.TZ]/g, "").slice(0, 14)}-${simpleChecksum(localProfile.deviceId).slice(0, 4)}`,
    exportedAt,
    device: {
      id: localProfile.deviceId,
      label: localProfile.deviceLabel,
      owner: localProfile.displayName
    },
    itemCount: items.length,
    assetCount: assetItems.length,
    assetBoundary: {
      savedExhibitions: (getAssetCollection().savedExhibitions || []).length,
      reportDrafts: (getAssetCollection().reportDrafts || []).length,
      importPolicy: "phase16-v5-memory-first-assets-preview",
      note: "第十六阶段第五版先同步展品；专题展和报告草稿进入逐项合并预览，不在导入时静默合并。"
    },
    items,
    syncAudit: loadPhase16SyncAudit().slice(0, 5).map((entry) => ({
      at: entry.at,
      action: entry.action,
      label: entry.label,
      batchId: entry.batchId,
      summary: entry.summary
    })),
    conflictPolicy: {
      create: "新增展品直接写入",
      update: "导入版本较新时默认覆盖同 ID 本地展品，也可改为保留本地或复制",
      conflict: "冲突项默认保留本地并复制导入项为新展品，也支持逐项选择导入覆盖",
      skip: "完全一致或导入版本较旧时跳过"
    }
  };
}

function buildPhase17SyncAdapter(sourceMemories = memories, options = {}) {
  const exportedAt = options.exportedAt || new Date().toISOString();
  const devices = registerPhase17CurrentDevice();
  const queue = loadPhase17SyncQueue().map(normalizePhase17QueueTask);
  const queueMetrics = buildPhase17QueueMetrics(queue);
  const lanHandshake = buildPhase17LanHandshake(devices);
  const deviceTrustPolicy = buildPhase17DeviceTrustPolicy(devices);
  const privateCloudBoundary = buildPhase17PrivateCloudBoundary();
  const syncHealth = buildPhase17SyncHealth({ devices, queueMetrics, lanHandshake, deviceTrustPolicy, privateCloudBoundary });
  const healthExplanation = buildPhase17HealthExplanation(syncHealth);
  const failureRecovery = buildPhase17FailureRecovery(queue);
  const phase18SyncAdvisory = buildPhase17Phase18SyncAdvisory();
  return {
    phase: 17,
    phaseName: "真实多设备同步适配层版",
    buildLabel: "phase17-sync-health-sixth-edition",
    mode: "adapter-layer-local-first",
    generatedAt: exportedAt,
    dependsOn: ["phase16Sync", "manual-json-local-first"],
    deviceRegistry: {
      localDeviceId: localProfile.deviceId,
      deviceCount: devices.length,
      devices: devices.slice(0, 6)
    },
    adapters: [
      { id: "manual-json", label: "手动 JSON 同步", status: "active", writable: true, detail: "沿用第十六阶段同步包、冲突预览、风险确认和恢复演练。" },
      { id: "lan-bridge", label: "局域网桥接", status: "simulated", writable: false, detail: "第二版提供只读握手模拟，当前不扫描网络、不交换私人数据。" },
      { id: "private-cloud", label: "私有云适配", status: privateCloudBoundary.status === "draft" ? "draft" : "planned", writable: false, detail: "第五版只提供配置边界草案，默认关闭，不保存密钥。" }
    ],
    queue: {
      ...queueMetrics,
      recent: queueMetrics.recent
    },
    lanHandshake,
    deviceTrustPolicy,
    privateCloudBoundary,
    syncHealth,
    healthExplanation,
    failureRecovery,
    phase18SyncAdvisory,
    syncScope: {
      memories: sourceMemories.length,
      assets: buildAssetPackageItems().length,
      assetPolicy: "preview-first-no-silent-merge"
    },
    safety: {
      localFirst: true,
      autoUpload: false,
      requiresReview: true,
      inheritsPhase16RiskGate: true
    }
  };
}

function buildPhase15AssetExportReview(collection = getAssetCollection()) {
  return {
    phase15AssetQuality: buildAssetQualitySummary(collection),
    phase15AssetReferences: buildAssetReferenceGraph(collection),
    phase15RepairSuggestions: buildAssetRepairSuggestions(collection),
    phase15PackageItems: buildAssetPackageItems(collection),
    phase15ReleaseTimeline: buildAssetReleaseTimeline(collection),
    phase15PackageComparison: buildAssetPackageComparison(collection),
    phase15AssetAuditLog: loadAssetAuditLog(),
    phase15AssetSnapshots: loadAssetSnapshots(),
    phase15PackageValidation: buildAssetPackageValidationSummary(collection),
    phase15SnapshotComparison: buildAssetSnapshotComparison(collection),
    phase15AuditFilters: buildAssetAuditFilters(),
    phase15AuditSearch: buildAssetAuditSearch(),
    phase15SelectedSnapshot: getSelectedAssetSnapshot(),
    phase15SignatureRecovery: buildAssetSignatureAnomalyPlan({ checked: true, ok: true }),
    phase15PackageSignature: buildAssetPackageSignature(collection)
  };
}

function buildRedactedCollectionExport() {
  const payload = buildCollectionExport();
  const redactedMemories = memories.map(buildRedactedMemory);
  return {
    ...payload,
    exportMode: "redacted",
    redacted: true,
    phase16Sync: buildPhase16SyncManifest(redactedMemories, { exportedAt: payload.exportedAt }),
    phase17SyncAdapter: buildPhase17SyncAdapter(redactedMemories, { exportedAt: payload.exportedAt }),
    phase18LongTermAgent: buildPhase18LongTermAgent(redactedMemories),
    phase19ImportPlan: buildPhase19ImportPlan(redactedMemories),
    memories: redactedMemories,
    redaction: buildRedactionPolicy()
  };
}

function buildRedactedMemory(memory) {
  const structured = buildStructuredMemory(memory);
  return {
    ...structured,
    rawContent: redactText(structured.rawContent, "原始记忆已脱敏"),
    exhibitText: redactText(structured.exhibitText, "展品说明已脱敏"),
    location: structured.location ? "地点已脱敏" : "",
    people: structured.people.map((_, index) => `人物${index + 1}`),
    coverImage: structured.coverImage ? "封面线索已脱敏" : "",
    mediaNote: structured.mediaNote ? "多模态说明已脱敏" : "",
    attachments: structured.attachments.map((item, index) => ({
      name: `附件${index + 1}`,
      type: item.type || "其他",
      note: item.note ? "备注已脱敏" : ""
    }))
  };
}

function redactText(value, fallback) {
  if (!value) return "";
  return `${fallback}（${String(value).length} 字）`;
}

function buildRedactionPolicy() {
  return {
    mode: "redacted",
    maskedFields: ["rawContent", "exhibitText", "people", "location", "coverImage", "mediaNote", "attachments.name", "attachments.note"],
    preservedFields: ["id", "hall", "sourceType", "date", "tags", "emotions", "importance", "favorite", "createdAt", "updatedAt"],
    note: "脱敏包用于演示、排查和跨设备预览，不适合作为完整恢复备份。"
  };
}

function buildLocalPhase12Sovereignty(items = []) {
  const structured = items.map(buildStructuredMemory);
  const sensitivity = buildLocalSensitivitySummary(structured);
  return {
    phase: 18,
    phaseName: "Agent 能力进阶和长期记忆助理版",
    localUserMode: "single-user-local-profile",
    memoryCount: structured.length,
    agentRunLinkedCount: structured.filter((memory) => memory.agentRunId).length,
    sensitivity,
    exportPackage: {
      format: "json",
      modes: ["full", "redacted"],
      includes: ["memories", "phase10Insights", "phase11WorkflowBlueprint", "phase12Sovereignty", "phase16Sync", "phase17SyncAdapter", "phase18LongTermAgent", "phase19ImportPlan", "privacyPolicy"],
      excludes: ["originalAttachmentFiles", "remoteAccountCredentials"],
      portable: true,
      riskLevel: sensitivity.riskLevel,
      suggestedHandling: sensitivity.riskLevel === "high" ? "导出后请保存在受信任位置，后续建议使用加密导出包。" : "导出后仍建议保存在个人设备或可信备份位置。"
    },
    deletion: {
      singleMemory: "available",
      fullPurge: databaseAvailable ? "DELETE /api/memories/purge" : "clear local backup manually",
      keeps: ["local profile", "application source files"]
    },
    sync: {
      mode: localProfile.syncPreference,
      status: "available"
    },
    phase13Readiness: buildLocalPhase13Readiness()
  };
}

function buildLocalPhase13Readiness() {
  const checks = [
    { id: "stable-api", label: "主要 API 有 smoke test", status: "ready" },
    { id: "start-docs", label: "启动和配置文档", status: "ready" },
    { id: "data-boundary", label: "数据位置和 AI 调用边界", status: "ready" },
    { id: "backup-restore", label: "导出导入和删除控制", status: "ready" },
    { id: "redacted-demo", label: "脱敏演示/排查包", status: "ready" },
    { id: "module-boundary", label: "工程模块拆分", status: "planned" }
  ];
  const readyCount = checks.filter((item) => item.status === "ready").length;
  return {
    targetPhase: 13,
    targetName: "产品化、部署和运维",
    ready: readyCount >= 5,
    score: Math.round((readyCount / checks.length) * 100),
    checks,
    recommendation: readyCount >= 5
      ? "可以进入第十三阶段，优先处理工程拆分、运行日志、部署文档和发布流程。"
      : "继续补齐 API 检查、数据边界和导出删除控制后再进入第十三阶段。"
  };
}

function buildMediaSummary(items = []) {
  const structured = items.map(buildStructuredMemory);
  const attachmentTypeCounts = structured.reduce((counts, memory) => {
    Object.entries(buildAttachmentTypeCounts(memory.attachments)).forEach(([type, count]) => {
      counts[type] = (counts[type] || 0) + count;
    });
    return counts;
  }, {});
  return {
    withCover: structured.filter((memory) => memory.coverImage).length,
    withMediaNote: structured.filter((memory) => memory.mediaNote).length,
    withAttachments: structured.filter((memory) => memory.attachments.length > 0).length,
    attachmentCount: structured.reduce((total, memory) => total + memory.attachments.length, 0),
    attachmentTypeCounts,
    attachmentTypes: sortAttachmentTypeCounts(attachmentTypeCounts).map(([type, count]) => ({ type, count })),
    fileStorage: "metadata-only",
    note: "当前导出包含附件清单和线索，不包含原始附件文件。"
  };
}

function buildPhase10Handoff(items = []) {
  const structured = items.map(buildStructuredMemory);
  const timelineReady = structured.filter((memory) => memory.date).length;
  const themeReady = structured.filter((memory) => (
    memory.tags.length
    || memory.people.length
    || memory.location
    || memory.emotions.length
    || memory.hall?.id
    || memory.sourceType
  )).length;
  const reportReady = structured.filter((memory) => (
    memory.rawContent
    && memory.exhibitText
    && (memory.tags.length || memory.emotions.length || memory.mediaNote || memory.attachments.length)
  )).length;
  const multimodalEvidence = structured.filter(hasMultimodalMetadata).length;
  return {
    total: structured.length,
    timelineReady,
    themeReady,
    reportReady,
    multimodalEvidence,
    missingDate: Math.max(0, structured.length - timelineReady),
    readyForPhase10: structured.length > 0 && themeReady > 0 && reportReady > 0,
    note: "阶段十将基于时间、人物、地点、标签、情绪和多模态证据生成时间线、主题展和回忆报告。"
  };
}

function buildPhase11Handoff(items = []) {
  const structured = items.map(buildStructuredMemory);
  const phase10 = buildPhase10Handoff(items);
  const withAgentRun = structured.filter((memory) => memory.agentRunId).length;
  const withReviewEvidence = structured.filter((memory) => (
    memory.rawContent
    && memory.exhibitText
    && (memory.tags.length || memory.people.length || memory.emotions.length)
  )).length;
  const withGuideEvidence = structured.filter((memory) => (
    memory.exhibitText
    && (memory.tags.length || memory.mediaNote || memory.attachments.length || memory.people.length)
  )).length;
  const runCoverage = buildCoverageRatio(withAgentRun, structured.length);
  const reviewCoverage = buildCoverageRatio(withReviewEvidence, structured.length);
  const guideCoverage = buildCoverageRatio(withGuideEvidence, structured.length);
  const gaps = buildPhase11Gaps({ structured, phase10, withAgentRun, withReviewEvidence, withGuideEvidence });
  return {
    total: structured.length,
    templates: workflowTemplates.length,
    withAgentRun,
    withReviewEvidence,
    withGuideEvidence,
    runCoverage,
    reviewCoverage,
    guideCoverage,
    gaps,
    recommendedNextActions: buildPhase11NextActions(gaps),
    readyForPhase11: structured.length > 0 && phase10.readyForPhase10 && (withAgentRun > 0 || withReviewEvidence > 0),
    nextEngine: "lightweight-orchestrator"
  };
}

function buildCoverageRatio(value, total) {
  if (!total) return 0;
  return Math.round((Number(value) / Math.max(Number(total), 1)) * 100);
}

function buildPhase11Gaps({ structured, phase10, withAgentRun, withReviewEvidence, withGuideEvidence }) {
  const gaps = [];
  if (!structured.length) gaps.push({ id: "no-memories", label: "还没有展品样本", severity: "high" });
  if (phase10.missingDate > 0) gaps.push({ id: "missing-date", label: `${phase10.missingDate} 件展品缺少时间`, severity: "medium" });
  if (withAgentRun < structured.length) gaps.push({ id: "agent-run-coverage", label: `${structured.length - withAgentRun} 件展品没有整理历史`, severity: "medium" });
  if (withReviewEvidence < structured.length) gaps.push({ id: "review-evidence", label: `${structured.length - withReviewEvidence} 件展品复核依据不足`, severity: "medium" });
  if (withGuideEvidence < structured.length) gaps.push({ id: "guide-evidence", label: `${structured.length - withGuideEvidence} 件展品导览证据不足`, severity: "low" });
  if (!gaps.length) gaps.push({ id: "stable", label: "当前样本已具备轻量编排基础", severity: "low" });
  return gaps.slice(0, 5);
}

function buildPhase11NextActions(gaps = []) {
  const actions = [];
  const ids = new Set(gaps.map((gap) => gap.id));
  if (ids.has("no-memories")) actions.push("先保存 3 到 5 件带时间、人物和标签的展品");
  if (ids.has("missing-date")) actions.push("补齐关键展品时间，提升时间线和报告稳定性");
  if (ids.has("agent-run-coverage")) actions.push("优先使用 Agent 整理新展品，增加可回放运行历史");
  if (ids.has("review-evidence")) actions.push("补齐人物、地点、标签和展品说明，减少人工复核空洞");
  if (ids.has("guide-evidence")) actions.push("补充多模态线索或引用字段，提高讲解检索可信度");
  if (!actions.length) actions.push("进入可保存专题展和报告草稿的设计准备");
  return actions.slice(0, 4);
}

function buildPhase11WorkflowBlueprint(items = []) {
  const handoff = buildPhase11Handoff(items);
  const phase12Readiness = buildPhase12Readiness(items, handoff);
  return {
    phase: 18,
    phaseName: "Agent 能力进阶和长期记忆助理版",
    engine: {
      id: "memory-museum-light-orchestrator",
      name: "轻量工作流编排层",
      dependency: "none"
    },
    capabilities: {
      templates: true,
      humanReview: true,
      retryAndReject: true,
      persistedRuns: true,
      replay: true,
      citations: true,
      scopedInsights: true
    },
    qualityGates: [
      { id: "review-gate", label: "人工复核闸门", status: handoff.reviewCoverage >= 60 ? "ready" : "needs-data" },
      { id: "run-history-gate", label: "运行历史回放", status: handoff.runCoverage > 0 ? "ready" : "needs-run" },
      { id: "citation-gate", label: "引用依据闸门", status: handoff.guideCoverage >= 60 ? "ready" : "needs-evidence" },
      { id: "asset-gate", label: "专题资产沉淀", status: "planned" }
    ],
    dataSources: [
      { id: "memories", label: "展品表", count: handoff.total },
      { id: "agent-runs", label: "整理历史", count: handoff.withAgentRun },
      { id: "review-evidence", label: "复核依据", count: handoff.withReviewEvidence },
      { id: "guide-evidence", label: "导览证据", count: handoff.withGuideEvidence }
    ],
    phase12Readiness,
    handoff,
    templates: workflowTemplates
  };
}

function buildPhase12Readiness(items = [], handoff = buildPhase11Handoff(items)) {
  const checks = [
    { id: "local-first-storage", label: "本地优先存储", status: "ready", detail: "SQLite 主存储，本地备份回退。" },
    { id: "portable-export", label: "可迁移导出包", status: "ready", detail: "导出包含展品、洞察和工作流蓝图。" },
    { id: "import-restore", label: "导入恢复", status: "ready", detail: "JSON 备份可以导入并处理 ID 冲突。" },
    { id: "delete-control", label: "删除控制", status: "ready", detail: "展品支持按 ID 删除。" },
    { id: "workflow-audit", label: "工作流审计", status: handoff.withAgentRun > 0 ? "ready" : "needs-sample", detail: "整理历史可关联展品。" },
    { id: "privacy-boundary", label: "隐私边界说明", status: "planned", detail: "第十二阶段补账号、加密和 AI 调用范围确认。" }
  ];
  const readyCount = checks.filter((item) => item.status === "ready").length;
  return {
    targetPhase: 12,
    targetName: "账号、多端同步、隐私和数据主权",
    planningReady: readyCount >= 4,
    score: Math.round((readyCount / checks.length) * 100),
    checks,
    blockers: checks.filter((item) => item.status !== "ready").map((item) => item.label),
    recommendation: readyCount >= 4
      ? "可以进入第十二阶段规划与第一版实现，优先处理数据主权、导出删除边界和隐私说明。"
      : "继续补齐导入导出、删除控制和审计记录后再进入第十二阶段。"
  };
}

function getWorkflowBlueprint() {
  if (backendWorkflowBlueprint?.phase >= 15 && Array.isArray(backendWorkflowBlueprint.templates)) {
    workflowBlueprintSource = "server";
    return backendWorkflowBlueprint;
  }
  workflowBlueprintSource = databaseAvailable ? "server-fallback" : "local";
  return buildPhase11WorkflowBlueprint(memories);
}

async function syncWorkflowBlueprint({ quiet = false } = {}) {
  if (!databaseAvailable && !quiet) {
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = "local";
    renderWorkflowOrchestration();
    return null;
  }
  try {
    const response = await fetch(API_WORKFLOWS);
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    const payload = await response.json();
    if ((payload?.phase || 0) < 15 || !Array.isArray(payload.templates)) throw new Error("workflow blueprint is invalid");
    backendWorkflowBlueprint = payload;
    workflowBlueprintSource = "server";
    renderWorkflowOrchestration();
    return payload;
  } catch (error) {
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = databaseAvailable ? "server-fallback" : "local";
    if (!quiet) renderWorkflowOrchestration();
    return null;
  }
}

function buildLocalPrivacyPolicy() {
  const sovereignty = buildLocalPhase12Sovereignty(memories);
  return {
    phase: 18,
    phaseName: "Agent 能力进阶和长期记忆助理版",
    summary: {
      storageMode: databaseAvailable ? "local-first" : "browser-local",
      accountMode: "local-single-user",
      syncMode: localProfile.syncPreference,
      aiMode: databaseAvailable ? "backend-or-mock" : "frontend-local",
      databasePath: databaseAvailable ? "SQLite 后端已连接" : "未连接后端",
      memoryCount: memories.length
    },
    dataLocations: [
      { id: "browser-backup", label: "浏览器本地备份", location: `localStorage: ${STORAGE_KEY}`, contains: ["展品备份", "本地馆主配置"] },
      { id: "export-package", label: "手动同步包", location: "用户下载的 JSON 文件", contains: ["memories", "phase10Insights", "phase11WorkflowBlueprint", "phase12Sovereignty", "privacyPolicy"] }
    ],
    aiDataScope: {
      configured: false,
      baseUrl: "frontend-local",
      model: "local-mock",
      sentFields: localProfile.aiConsent ? ["rawContent when backend AI is configured"] : [],
      notSentByCurrentApp: ["originalAttachmentFiles", "fullDatabaseFile"],
      requiresUserConfirmation: true,
      note: localProfile.aiConsent
        ? "本地配置允许在后端 AI 已配置时发送原始记忆；当前策略仍以实际后端 /api/privacy 为准。"
        : "本地配置未允许 AI 数据发送；前端本地 Mock 不会外发数据。"
    },
    sensitiveData: sovereignty.sensitivity,
    userControls: [
      { id: "export", label: "完整导出 JSON 备份", status: "available" },
      { id: "redacted-export", label: "导出脱敏 JSON 包", status: "available" },
      { id: "import", label: "从 JSON 备份恢复", status: "available" },
      { id: "delete-one", label: "删除单件展品", status: "available" },
      { id: "local-profile", label: "本地用户配置", status: "browser-local" }
    ],
    productizationReadiness: sovereignty.phase13Readiness
  };
}

function buildLocalSensitivitySummary(items = []) {
  const categories = [
    { id: "people", label: "人物关系", count: items.filter((memory) => memory.people?.length).length },
    { id: "location", label: "地点线索", count: items.filter((memory) => memory.location || String(memory.rawContent || "").match(/家|学校|公司|医院|车站|地址|上海|北京|广州|深圳/)).length },
    { id: "contact", label: "联系方式", count: items.filter((memory) => String(memory.rawContent || "").match(/1[3-9]\d{9}|微信|电话|邮箱|@/)).length },
    { id: "low-mood", label: "低谷情绪", count: items.filter((memory) => (memory.emotions || []).some((emotion) => ["低谷", "孤独", "委屈", "愤怒", "害怕", "迷茫"].includes(emotion)) || memory.hall?.id === "low").length },
    { id: "attachments", label: "附件元数据", count: items.filter((memory) => memory.attachments?.length || memory.coverImage || memory.mediaNote).length }
  ];
  const matched = categories.filter((category) => category.count > 0);
  const totalSignals = matched.reduce((sum, category) => sum + category.count, 0);
  const riskLevel = matched.some((item) => item.id === "contact" && item.count > 0) || matched.length >= 4
    ? "high"
    : matched.length >= 2 ? "medium" : matched.length === 1 ? "low" : "none";
  return {
    riskLevel,
    totalSignals,
    categories,
    matchedCategories: matched.map((item) => item.label),
    recommendation: riskLevel === "high"
      ? "导出或调用 AI 前建议再次检查原文、人物、地点和联系方式。"
      : riskLevel === "medium"
        ? "建议导出前确认人物、地点和附件线索是否适合随包保存。"
        : "当前敏感线索较少，仍建议只在可信设备保存备份。"
  };
}

function getPrivacyPolicy() {
  if (privacyPolicy?.phase >= 15) {
    privacyPolicySource = "server";
    return privacyPolicy;
  }
  privacyPolicySource = "local";
  return buildLocalPrivacyPolicy();
}

async function syncPrivacyPolicy({ quiet = false } = {}) {
  if (!databaseAvailable && !quiet) {
    privacyPolicy = null;
    privacyPolicySource = "local";
    renderPrivacyPanel();
    return null;
  }
  try {
    const response = await fetch(API_PRIVACY);
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    const payload = await response.json();
    if ((payload?.phase || 0) < 15) throw new Error("privacy policy is invalid");
    privacyPolicy = payload;
    privacyPolicySource = "server";
    renderPrivacyPanel();
    return payload;
  } catch {
    privacyPolicy = null;
    privacyPolicySource = "local";
    if (!quiet) renderPrivacyPanel();
    return null;
  }
}

function buildLocalVersionInfo() {
  return {
    app: "AI 记忆博物馆",
    packageName: "ai-memory-museum",
    version: "1.9.48",
    schemaVersion: SCHEMA_VERSION,
    phase: 29,
    phaseName: "\u53d1\u5e03\u6cbb\u7406\u89c4\u5212",
    releaseChannel: "phase29-release-exit-final-archive-manifest-preview-browser-fallback",
    buildLabel: "phase29-release-exit-final-archive-manifest-preview",
    runtime: {
      node: "not-connected",
      platform: "browser",
      uptimeSeconds: 0,
      port: "not-connected",
      databasePath: databaseAvailable ? "SQLite 后端已连接" : "未连接后端",
      aiConfigured: backendAiConfigured,
      aiModel: backendAiConfigured ? "configured" : "not-configured"
    },
    deployment: {
      mode: "browser-fallback",
      staticHosting: "local-file-or-node-static",
      database: databaseAvailable ? "node:sqlite" : "localStorage",
      startCommand: "npm.cmd start",
      healthCheck: "/api/health",
      versionCheck: "/api/version",
      notes: "未连接后端时，页面会显示浏览器本地备份能力。"
    },
    operations: {
      status: databaseAvailable ? "operational" : "degraded",
      mode: databaseAvailable ? "local" : "browser-fallback",
      checks: ["syntax", "phase15-readiness", "phase16-readiness", "phase17-readiness", "phase18-readiness", "phase19-readiness", "phase20-readiness", "phase21-readiness", "phase22-readiness", "phase23-readiness", "phase24-readiness", "phase25-readiness", "phase26-readiness", "phase27-readiness", "phase28-readiness", "phase29-readiness", "api-smoke"],
      release: {
        channel: databaseAvailable ? "local-preview" : "browser-fallback",
        label: "phase29-release-exit-final-archive-manifest-preview",
        checklistReady: buildLocalReleaseChecklist().filter((item) => item.status === "ready").length,
        checklistTotal: buildLocalReleaseChecklist().length
      },
      data: {
        memories: memories.length,
        multimodal: memories.filter(hasMultimodalMetadata).length,
        agentRuns: memories.filter((memory) => memory.agentRunId).length,
        databasePath: databaseAvailable ? "SQLite 后端已连接" : "localStorage"
      },
      backup: {
        fullExport: "导出",
        redactedExport: "导出脱敏包",
        purge: "清空数据库展品"
      },
      observability: {
        health: "/api/health",
        version: "/api/version",
        operations: "/api/operations",
        logs: databaseAvailable ? "jsonl-and-memory-events" : "browser-local-summary",
        requestId: "X-Request-Id",
        recentEvents: 0
      },
      backupPolicy: buildLocalBackupPolicy(),
      readiness: {
        phase14: true,
        phase15: true,
        phase16: true,
        phase17: true,
        phase18: true,
        phase19: true,
        phase20: true,
        phase21: true,
        phase22: true,
        phase23: true,
        phase24: true,
        phase25: true,
        phase26: true,
        phase27: true,
        phase28: true,
        phase29: true,
        modularizationReady: true,
        assetModelReady: true,
        deployableLocal: databaseAvailable,
        productionReady: false,
        reason: "阶段 20 第十三版已补充 lockfile 导出、安装队列持久化模型和发布前签名闸门，继续保持运行时关闭。"
      }
    },
    checks: {
      full: "npm.cmd run check",
      smoke: "npm.cmd run smoke",
      syntax: "node --check app.js && node --check server.js && node --check database.js"
    },
    releaseChecklist: buildLocalReleaseChecklist(),
    runbook: buildLocalOperationsRunbook(),
    deploymentModes: buildLocalDeploymentModes(),
    backupPolicy: buildLocalBackupPolicy(),
    riskRegister: buildLocalRiskRegister(),
    logArchive: buildLocalLogArchive(),
    demoKit: buildLocalDemoKit(),
    phase14Readiness: buildLocalPhase14Readiness(),
    phase15Readiness: buildLocalPhase15Readiness(),
    phase15AssetPlan: buildLocalPhase15AssetPlan(),
    phase20PlatformPlan: buildLocalPhase20PlatformPlan(),
    phase21RuntimeSandboxPlan: buildLocalPhase21RuntimeSandboxPlan(),
    phase22RuntimeEvidenceReview: buildLocalPhase22RuntimeEvidenceReview(),
    phase23ReleaseReadinessReviewUi: buildLocalPhase23ReleaseReadinessReviewUi(),
    phase24RuntimeSandboxUxEntry: buildLocalPhase24RuntimeSandboxUxEntry(),
    phase25RuntimeSandboxUiSurface: buildLocalPhase25RuntimeSandboxUiSurface(),
    phase26RuntimeValidationEntry: buildLocalPhase26RuntimeValidationEntry(),
    phase27ReleaseBlockerGovernanceEntry: buildLocalPhase27ReleaseBlockerGovernanceEntry(),
    phase28ClearanceReviewEntry: buildLocalPhase28ClearanceReviewEntry(),
    phase29ReleaseGovernancePlanning: buildLocalPhase29ReleaseGovernancePlanning(),
    moduleBoundaryPlan: buildLocalModuleBoundaryPlan(),
    recentEvents: buildLocalOperationEvents(),
    releaseHistory: buildLocalReleaseHistory(),
    apiSurface: ["/api/health", "/api/version", "/api/operations", "/api/operations/export", "/api/privacy", "/api/workflows", "/api/insights", "/api/assets", "/api/exhibitions", "/api/report-drafts"],
    nextEngineeringSteps: [
      "阶段 25 第一版把 Phase 24 沙箱 UX 模型渲染为只读 UI Surface",
      "预检、权限复核、隔离队列、证据导出和发布清理预览都保持只读",
      "真实第三方插件运行时等待人工复核审计闭环完成后再启用"
    ]
  };
}

function buildLocalReleaseChecklist() {
  return [
    { id: "syntax", label: "语法检查", status: "ready", command: "node --check app.js && node --check server.js && node --check database.js && node --check src/services/operations.js && node --check src/routes/health.js && node --check src/routes/operations.js" },
    { id: "readiness", label: "第十五阶段资产检查", status: "ready", command: "node scripts/phase15-readiness.js" },
    { id: "phase16-readiness", label: "第十六阶段同步检查", status: "ready", command: "node scripts/phase16-readiness.js" },
    { id: "phase17-readiness", label: "第十七阶段适配层检查", status: "ready", command: "node scripts/phase17-readiness.js" },
    { id: "phase18-readiness", label: "第十八阶段长期助理检查", status: "ready", command: "node scripts/phase18-readiness.js" },
    { id: "phase19-readiness", label: "第十九阶段外部导入检查", status: "ready", command: "node scripts/phase19-readiness.js" },
    { id: "phase20-readiness", label: "第二十阶段平台与插件检查", status: "ready", command: "node scripts/phase20-readiness.js" },
    { id: "phase21-readiness", label: "第二十一阶段运行时沙箱检查", status: "ready", command: "node scripts/phase21-readiness.js" },
    { id: "phase22-readiness", label: "Phase 22 runtime evidence review check", status: "ready", command: "node scripts/phase22-readiness.js" },
    { id: "phase23-readiness", label: "Phase 23 closure handoff check", status: "ready", command: "node scripts/phase23-readiness.js" },
    { id: "phase24-readiness", label: "Phase 24 runtime sandbox UX entry check", status: "ready", command: "node scripts/phase24-readiness.js" },
    { id: "phase25-readiness", label: "Phase 25 runtime sandbox UI surface check", status: "ready", command: "node scripts/phase25-readiness.js" },
    { id: "phase26-readiness", label: "Phase 26 runtime validation entry check", status: "ready", command: "node scripts/phase26-readiness.js" },
    { id: "phase27-readiness", label: "Phase 27 release blocker governance entry check", status: "ready", command: "node scripts/phase27-readiness.js" },
    { id: "phase28-readiness", label: "Phase 28 clearance review entry check", status: "ready", command: "node scripts/phase28-readiness.js" },
    { id: "phase29-readiness", label: "Phase 29 release governance planning check", status: "ready", command: "node scripts/phase29-readiness.js" },
    { id: "api-smoke", label: "核心 API smoke test", status: databaseAvailable ? "ready" : "needs-backend", command: "npm.cmd run smoke" },
    { id: "operations-trace", label: "请求追踪与运行事件", status: databaseAvailable ? "ready" : "browser-local", detail: "后端在线时 API 响应带 X-Request-Id，/api/operations 可查看最近运行事件。" },
    { id: "persistent-ops-log", label: "持久化运行日志", status: databaseAvailable ? "ready" : "browser-local", detail: "后端在线时最近 API 请求会追加到 JSONL 运维日志。" },
    { id: "data-export", label: "完整与脱敏备份", status: "ready", detail: "发布前导出完整包；演示和排查优先使用脱敏包。" },
    { id: "demo-kit", label: "演示包摘要", status: memories.length > 0 ? "ready" : "needs-sample", detail: "演示包会汇总样本数量、推荐路线和隐私提示。" },
    { id: "phase14-readiness", label: "第十四阶段模块化基线", status: "ready", detail: "模块边界、迁移顺序、风险闸门和检查保护网已经声明。" },
    { id: "phase15-readiness", label: "第十五阶段准备", status: "ready", detail: "专题资产、报告草稿、引用来源、导出边界和工作流状态已形成进入计划。" },
    { id: "production-logs", label: "持久化日志与反向代理", status: "planned", detail: "生产部署前仍需补充文件日志、HTTPS 和反向代理说明。" }
  ];
}

function buildLocalOperationsRunbook() {
  return [
    { id: "start", label: "本地启动", command: "npm.cmd start", detail: "启动后打开 http://127.0.0.1:3000，并先查看 /api/health。" },
    { id: "check", label: "发布前检查", command: "npm.cmd run check", detail: "包含语法检查、Phase 15-20 readiness 和 API smoke test。" },
    { id: "backup", label: "备份", command: "GET /api/memories/export", detail: "长期保存前导出完整 JSON；对外演示或排查使用 redacted 模式。" },
    { id: "privacy", label: "隐私复核", command: "GET /api/privacy", detail: "确认 AI 调用范围、敏感线索和删除控制。" },
    { id: "recover", label: "恢复", command: "POST /api/memories/import", detail: "从 JSON 备份恢复展品，导入时处理 ID 冲突。" }
  ];
}

function buildLocalDeploymentModes() {
  return [
    { id: "local", label: "本地个人使用", status: databaseAvailable ? "ready" : "needs-backend", database: databaseAvailable ? "SQLite" : "localStorage", note: "适合单机长期整理私人记忆。" },
    { id: "demo", label: "课堂/演示模式", status: "ready", database: databaseAvailable ? "演示 SQLite" : "浏览器本地备份", note: "建议使用脱敏数据和明确的 AI 调用说明。" },
    { id: "lan", label: "局域网共享", status: "planned", database: "SQLite + 访问控制", note: "进入前需要账号、权限和备份策略。" },
    { id: "cloud", label: "云端部署", status: "planned", database: "托管数据库或卷挂载 SQLite", note: "进入前需要 HTTPS、日志、备份、反向代理和密钥管理。" }
  ];
}

function buildLocalBackupPolicy() {
  return {
    full: databaseAvailable ? "/api/memories/export" : "浏览器导出",
    redacted: databaseAvailable ? "/api/memories/export?mode=redacted" : "导出脱敏包",
    restore: databaseAvailable ? "POST /api/memories/import" : "导入 JSON",
    purge: databaseAvailable ? "DELETE /api/memories/purge with confirm=DELETE" : "清空本地备份",
    recommendedCadence: "每次集中整理后导出一次完整包；对外演示只使用脱敏包。",
    storageAdvice: "完整包建议保存到个人可信设备或加密盘，脱敏包可用于演示、排查和跨设备预览。"
  };
}

function buildLocalRiskRegister() {
  return [
    { id: "privacy", label: "隐私与敏感线索", level: memories.length > 0 ? "medium" : "low", mitigation: "导出、演示和 AI 调用前先查看隐私策略与脱敏包。" },
    { id: "backup", label: "备份恢复", level: "medium", mitigation: "当前已有 JSON 导出/导入，后续需要加密包和自动备份。" },
    { id: "observability", label: "运行观测", level: "medium", mitigation: "当前有 health/version；生产部署前补请求日志、错误日志和 request id。" },
    { id: "module-size", label: "工程模块边界", level: "medium", mitigation: "后续拆分 server、agent、rag、workflow、privacy、operations 模块。" }
  ];
}

function buildLocalOperationEvents() {
  return [
    {
      id: "browser-fallback",
      type: "local",
      method: "UI",
      path: "browser",
      statusCode: databaseAvailable ? 200 : 0,
      durationMs: 0,
      level: databaseAvailable ? "info" : "warning",
      at: new Date().toISOString(),
      error: databaseAvailable ? "" : "后端未连接，显示浏览器本地摘要。"
    }
  ];
}

function buildLocalLogArchive() {
  return {
    format: databaseAvailable ? "jsonl" : "browser-summary",
    path: databaseAvailable ? "data/operations-events.jsonl" : "localStorage/session",
    exists: databaseAvailable,
    sizeBytes: 0,
    inMemoryEvents: 0,
    exportEndpoint: "/api/operations/export",
    retention: "后端在线时保留 JSONL 运维日志；本地回退仅展示当前页面摘要。"
  };
}

function buildLocalDemoKit() {
  const withDate = memories.filter((memory) => memory.date).length;
  const withPeople = memories.filter((memory) => memory.people?.length).length;
  const withMedia = memories.filter(hasMultimodalMetadata).length;
  const withGuideText = memories.filter((memory) => memory.exhibitText && ((memory.tags || []).length || (memory.emotions || []).length)).length;
  const score = Math.round(([
    memories.length >= 3,
    withDate >= 2,
    withPeople >= 1,
    withMedia >= 1,
    withGuideText >= 2
  ].filter(Boolean).length / 5) * 100);
  return {
    ready: score >= 60,
    score,
    sampleCount: memories.length,
    checks: [
      { id: "sample-count", label: "至少 3 件展品", status: memories.length >= 3 ? "ready" : "needs-sample", count: memories.length },
      { id: "timeline", label: "时间线样本", status: withDate >= 2 ? "ready" : "needs-date", count: withDate },
      { id: "people", label: "人物关系线索", status: withPeople >= 1 ? "ready" : "needs-people", count: withPeople },
      { id: "media", label: "多模态线索", status: withMedia >= 1 ? "ready" : "needs-media", count: withMedia },
      { id: "guide", label: "讲解检索样本", status: withGuideText >= 2 ? "ready" : "needs-guide", count: withGuideText }
    ],
    storyline: [
      "录入或导入 3 到 5 件温馨记忆展品",
      "运行 Agent 整理并保留人工复核状态",
      "打开讲解员提问，展示引用证据和可信度",
      "查看时间线、主题展、隐私策略和部署与运维面板",
      "导出脱敏包用于演示或排查"
    ],
    privacyNote: "演示前优先使用脱敏导出包，避免展示真实人物、地点、联系方式和原始附件线索。"
  };
}

function buildLocalPhase20PlatformPlan() {
  return {
    phase: 20,
    phaseName: "可扩展产品平台和插件生态版",
    version: "1.0.25",
    mode: "platform-boundary-first",
    runtimePolicy: "manifest-only-no-third-party-code-execution",
    currentScope: [
      "plugin-manifest-registry",
      "plugin-manifest-schema",
      "permission-review",
      "plugin-audit-log",
      "capability-catalog",
      "extension-point-map",
      "security-boundary",
      "built-in-plugin-inventory",
      "built-in-plugin-registry",
      "extension-contract-tests",
      "plugin-sandbox-boundary",
      "no-code-template-pack",
      "signed-plugin-manifest",
      "plugin-installation-workflow",
      "template-preview-fixtures",
      "plugin-review-workbench",
      "plugin-lockfile",
      "plugin-lockfile-export",
      "plugin-install-queue-persistence",
      "plugin-release-signature-gate",
      "plugin-lockfile-import-preview",
      "plugin-pre-release-check-command",
      "plugin-signature-diff-report",
      "plugin-release-report-artifact",
      "plugin-diff-review-history",
      "plugin-release-report-validation-command",
      "plugin-signed-release-report-export",
      "plugin-review-history-ui",
      "plugin-validation-command-ci-wrapper",
      "plugin-signed-report-download",
      "plugin-ci-badge-summary",
      "plugin-download-integrity-preview",
      "plugin-release-checklist-gate",
      "phase20-readiness"
    ],
    extensionPoints: [
      { id: "importer", label: "导入器", status: "planned", contract: "preview -> draft -> reviewed import", owner: "phase19ImportPlan" },
      { id: "exporter", label: "导出器", status: "planned", contract: "collection -> package -> redaction policy", owner: "privacyPolicy" },
      { id: "agent-tool", label: "Agent 工具", status: "planned", contract: "suggestion -> human review -> auditable action", owner: "phase18LongTermAgent" },
      { id: "asset-template", label: "专题展模板", status: "planned", contract: "asset draft -> editable sections -> release package", owner: "phase15 assets" },
      { id: "sync-adapter", label: "同步适配器", status: "planned", contract: "local-first package -> conflict preview -> manual apply", owner: "phase16/17 sync" }
    ],
    builtInPlugins: [
      { id: "markdown-importer", type: "importer", status: "built-in", enabled: true, source: "phase19", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["memory.import.preview"], extensionPoint: "importer" } },
      { id: "csv-importer", type: "importer", status: "built-in", enabled: true, source: "phase19", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["memory.import.preview"], extensionPoint: "importer" } },
      { id: "chat-importer", type: "importer", status: "built-in", enabled: true, source: "phase19", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["memory.import.preview"], extensionPoint: "importer" } },
      { id: "redacted-exporter", type: "exporter", status: "built-in", enabled: true, source: "phase12", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["memory.export.redacted"], extensionPoint: "exporter" } },
      { id: "long-term-review-agent", type: "agent-tool", status: "built-in", enabled: true, source: "phase18", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["agent.suggest", "memory.read.summary"], extensionPoint: "agent-tool" } },
      { id: "manual-json-sync", type: "sync-adapter", status: "built-in", enabled: true, source: "phase16", manifest: { schemaVersion: "phase20.plugin.manifest.v1", permissions: ["sync.package.preview"], extensionPoint: "sync-adapter" } }
    ],
    builtInPluginRegistry: {
      schemaVersion: "phase20.builtIn.registry.v1",
      status: "registry-ready-runtime-disabled",
      owner: "local-platform",
      total: 6,
      enabled: 6,
      categories: ["importer", "exporter", "agent-tool", "sync-adapter"],
      entries: [
        { id: "markdown-importer", type: "importer", owner: "phase19", status: "enabled", capability: "markdown-to-memory-drafts", input: "markdown text", output: "reviewable memory drafts", contract: "preview-only" },
        { id: "csv-importer", type: "importer", owner: "phase19", status: "enabled", capability: "csv-to-memory-drafts", input: "csv text", output: "reviewable memory drafts", contract: "preview-only" },
        { id: "chat-importer", type: "importer", owner: "phase19", status: "enabled", capability: "chat-to-memory-drafts", input: "chat transcript", output: "reviewable memory drafts", contract: "preview-only" },
        { id: "redacted-exporter", type: "exporter", owner: "phase12", status: "enabled", capability: "redacted-memory-export", input: "memory collection", output: "redacted export package", contract: "redaction-required" },
        { id: "long-term-review-agent", type: "agent-tool", owner: "phase18", status: "enabled", capability: "long-term-review-suggestions", input: "memory summaries", output: "reviewable suggestions", contract: "human-confirmation" },
        { id: "manual-json-sync", type: "sync-adapter", owner: "phase16", status: "enabled", capability: "manual-json-sync-preview", input: "local-first sync package", output: "conflict preview", contract: "manual-apply-only" }
      ],
      registryChecks: ["unique-id", "known-extension-point", "manifest-attached", "permission-reviewed", "audit-sample-present"],
      runtimeExecution: false
    },
    manifestSchema: {
      schemaVersion: "phase20.plugin.manifest.v1",
      status: "schema-ready-runtime-disabled",
      requiredFields: ["id", "name", "version", "type", "extensionPoint", "permissions", "entryPolicy", "dataAccess", "audit"],
      optionalFields: ["description", "sourcePhase", "capabilities", "compatibility", "uiHints", "disabledReason"],
      permissionLabels: ["memory.import.preview", "memory.export.redacted", "memory.read.summary", "agent.suggest", "sync.package.preview"],
      extensionContracts: ["importer", "exporter", "agent-tool", "asset-template", "sync-adapter"],
      validationRules: [
        "id must be stable kebab-case",
        "extensionPoint must match a declared Phase 20 extension point",
        "permissions must use approved labels",
        "entryPolicy must be manifest-only",
        "networkAccess and secretStorage must remain false in 1.0.25"
      ]
    },
    manifestValidation: {
      status: "ready",
      runtimeExecution: false,
      builtInManifestCount: 6,
      sampleManifestIds: ["markdown-importer", "csv-importer", "chat-importer", "redacted-exporter", "long-term-review-agent", "manual-json-sync"],
      blockedUntil: ["permission-review", "sandbox-boundary"]
    },
    permissionReview: {
      status: "policy-ready",
      defaultDecision: "deny-until-reviewed",
      humanApprovalRequired: true,
      reviewScope: ["manifest.permissions", "manifest.entryPolicy", "manifest.dataAccess", "manifest.audit"],
      reviewChecklist: [
        "确认插件只声明允许的权限标签",
        "确认插件不打开第三方代码执行",
        "确认插件不请求网络访问和密钥存储",
        "确认高风险能力需要人工复核",
        "确认被禁用的原因会写入审计"
      ],
      builtInDecisions: [
        { id: "markdown-importer", decision: "approved", permissions: ["memory.import.preview"], confirmationRequired: false },
        { id: "csv-importer", decision: "approved", permissions: ["memory.import.preview"], confirmationRequired: false },
        { id: "chat-importer", decision: "approved", permissions: ["memory.import.preview"], confirmationRequired: false },
        { id: "redacted-exporter", decision: "approved", permissions: ["memory.export.redacted"], confirmationRequired: false },
        { id: "long-term-review-agent", decision: "reviewed", permissions: ["agent.suggest", "memory.read.summary"], confirmationRequired: true },
        { id: "manual-json-sync", decision: "approved", permissions: ["sync.package.preview"], confirmationRequired: false }
      ],
      permissionLabels: [
        { id: "memory.import.preview", review: "approved", scope: "导入预览" },
        { id: "memory.export.redacted", review: "approved", scope: "脱敏导出" },
        { id: "memory.read.summary", review: "reviewed", scope: "摘要读取" },
        { id: "agent.suggest", review: "reviewed", scope: "Agent 建议" },
        { id: "sync.package.preview", review: "approved", scope: "同步包预览" }
      ],
      blockedUntil: ["sandbox-boundary"],
      auditEventTypes: ["manifest-loaded", "permission-reviewed", "decision-approved", "decision-blocked", "confirmation-recorded"]
    },
    pluginAuditLog: {
      status: "audit-model-ready",
      eventSchemaVersion: "phase20.plugin.audit.v1",
      storageMode: "export-and-operations-summary",
      runtimeExecution: false,
      requiredFields: ["id", "pluginId", "eventType", "decision", "actor", "createdAt", "evidence"],
      eventTypes: ["manifest-loaded", "permission-reviewed", "decision-approved", "decision-blocked", "confirmation-recorded", "runtime-blocked"],
      sampleEvents: [
        { id: "audit-markdown-importer-manifest", pluginId: "markdown-importer", eventType: "manifest-loaded", decision: "recorded", actor: "system", evidence: ["phase20.plugin.manifest.v1", "memory.import.preview"] },
        { id: "audit-long-term-review-agent-permission", pluginId: "long-term-review-agent", eventType: "permission-reviewed", decision: "reviewed", actor: "human-review-required", evidence: ["agent.suggest", "memory.read.summary"] },
        { id: "audit-third-party-runtime-blocked", pluginId: "third-party-placeholder", eventType: "runtime-blocked", decision: "blocked", actor: "system", evidence: ["thirdPartyExecution=false", "networkAccessForPlugins=false"] }
      ],
      exportFields: ["phase20PlatformPlan.pluginAuditLog", "phase20PlatformPlan.permissionReview.auditEventTypes"],
      nextControls: ["tamper-evident-checksum", "audit-search", "reviewer-note"]
    },
    extensionContractTests: {
      schemaVersion: "phase20.extension.contract-tests.v1",
      status: "contract-tests-ready-runtime-disabled",
      runtimeExecution: false,
      coverage: ["importer", "exporter", "agent-tool", "asset-template", "sync-adapter"],
      requiredAssertions: [
        "declared-extension-point",
        "manifest-schema-version",
        "permission-labels-reviewed",
        "no-network-access",
        "no-secret-storage",
        "human-review-or-preview-output",
        "audit-event-emitted"
      ],
      contractSuites: [
        { id: "importer-contract", extensionPoint: "importer", status: "ready", samplePlugin: "markdown-importer", inputFixture: "markdown text", expectedOutput: "reviewable memory drafts", blockingFailure: "reject-import-preview" },
        { id: "exporter-contract", extensionPoint: "exporter", status: "ready", samplePlugin: "redacted-exporter", inputFixture: "memory collection", expectedOutput: "redacted export package", blockingFailure: "reject-export-package" },
        { id: "agent-tool-contract", extensionPoint: "agent-tool", status: "ready", samplePlugin: "long-term-review-agent", inputFixture: "memory summaries", expectedOutput: "reviewable suggestions", blockingFailure: "require-human-confirmation" },
        { id: "asset-template-contract", extensionPoint: "asset-template", status: "planned", samplePlugin: "asset-template-placeholder", inputFixture: "asset draft", expectedOutput: "editable sections", blockingFailure: "disable-template" },
        { id: "sync-adapter-contract", extensionPoint: "sync-adapter", status: "ready", samplePlugin: "manual-json-sync", inputFixture: "local-first sync package", expectedOutput: "conflict preview", blockingFailure: "manual-apply-only" }
      ],
      failurePolicy: "block-plugin-and-record-audit-event",
      exportFields: ["phase20PlatformPlan.extensionContractTests", "phase20PlatformPlan.extensionPoints"],
      nextControls: ["fixture-library", "negative-permission-tests", "sandbox-boundary-tests"]
    },
    sandboxBoundary: {
      schemaVersion: "phase20.plugin.sandbox-boundary.v1",
      status: "boundary-defined-runtime-disabled",
      runtimeExecution: false,
      isolationMode: "no-third-party-code-execution",
      trustZone: "built-in-manifest-only",
      blockedCapabilities: ["dynamic-code-eval", "filesystem-write", "network-request", "secret-read", "background-process", "direct-database-access"],
      allowedCapabilities: ["manifest-parse", "contract-fixture-check", "reviewable-draft-output", "redacted-export-preview", "audit-event-summary"],
      dataBoundary: {
        memoryAccess: "summary-or-explicit-draft-only",
        assetAccess: "metadata-preview-only",
        exportAccess: "redacted-package-only",
        syncAccess: "manual-preview-only"
      },
      enforcementChecks: [
        { id: "runtime-disabled", status: "ready", rule: "thirdPartyExecution=false" },
        { id: "network-blocked", status: "ready", rule: "networkAccessForPlugins=false" },
        { id: "secret-storage-blocked", status: "ready", rule: "secretStorage=false" },
        { id: "filesystem-blocked", status: "planned", rule: "no plugin filesystem write boundary before runtime" },
        { id: "database-blocked", status: "planned", rule: "plugins cannot receive direct sqlite handles" }
      ],
      handoffToRuntime: ["signed-manifest", "permission-review-approved", "contract-tests-passing", "audit-log-enabled", "sandbox-enforcer-implemented"],
      exportFields: ["phase20PlatformPlan.sandboxBoundary", "phase20PlatformPlan.securityModel"],
      nextControls: ["sandbox-enforcer", "signed-manifest-check", "resource-quota-policy"]
    },
    noCodeTemplatePack: {
      schemaVersion: "phase20.no-code.template-pack.v1",
      status: "template-pack-ready-runtime-disabled",
      runtimeExecution: false,
      owner: "local-platform",
      templateCount: 5,
      categories: ["importer", "exporter", "agent-tool", "asset-template", "sync-adapter"],
      templates: [
        { id: "memory-import-template", extensionPoint: "importer", status: "ready", input: "text-or-table", output: "reviewable memory drafts", controls: ["field-mapping", "dedupe-preview", "manual-apply"] },
        { id: "redacted-export-template", extensionPoint: "exporter", status: "ready", input: "memory collection", output: "redacted export package", controls: ["redaction-policy", "preview-only", "manual-download"] },
        { id: "review-agent-template", extensionPoint: "agent-tool", status: "ready", input: "memory summaries", output: "reviewable suggestions", controls: ["human-confirmation", "audit-event", "no-background-run"] },
        { id: "exhibition-layout-template", extensionPoint: "asset-template", status: "ready", input: "asset draft", output: "editable exhibition sections", controls: ["section-preview", "citation-required", "manual-save"] },
        { id: "sync-preview-template", extensionPoint: "sync-adapter", status: "ready", input: "local-first sync package", output: "conflict preview", controls: ["conflict-list", "per-item-decision", "manual-apply"] }
      ],
      guardrails: ["manifest-required", "permission-reviewed", "contract-tested", "sandbox-boundary-applied", "audit-summary-required"],
      authoringWorkflow: ["choose-template", "fill-metadata", "preview-fixture", "review-permissions", "export-template-json"],
      exportFields: ["phase20PlatformPlan.noCodeTemplatePack", "phase20PlatformPlan.extensionContractTests", "phase20PlatformPlan.sandboxBoundary"],
      nextControls: ["template-json-schema", "template-preview-fixtures", "template-signature"]
    },
    templatePreviewFixtures: {
      schemaVersion: "phase20.template.preview-fixtures.v1",
      status: "fixtures-ready-runtime-disabled",
      runtimeExecution: false,
      fixtureCount: 5,
      coverage: ["importer", "exporter", "agent-tool", "asset-template", "sync-adapter"],
      previewWorkflow: ["load-template", "load-fixture", "render-preview", "run-contract-assertions", "record-audit-summary", "block-or-mark-ready"],
      fixtures: [
        { id: "memory-import-fixture", templateId: "memory-import-template", extensionPoint: "importer", status: "passing", inputFixture: "two-row memory table", expectedPreview: "reviewable memory drafts", requiredAssertions: ["field-mapping-applied", "dedupe-preview-visible", "manual-apply-only"] },
        { id: "redacted-export-fixture", templateId: "redacted-export-template", extensionPoint: "exporter", status: "passing", inputFixture: "memory collection with sensitive fields", expectedPreview: "redacted export package", requiredAssertions: ["redaction-policy-applied", "download-preview-only", "audit-summary-present"] },
        { id: "review-agent-fixture", templateId: "review-agent-template", extensionPoint: "agent-tool", status: "passing", inputFixture: "memory summaries with weak signals", expectedPreview: "reviewable suggestions", requiredAssertions: ["human-confirmation-required", "no-background-run", "audit-event-emitted"] },
        { id: "exhibition-layout-fixture", templateId: "exhibition-layout-template", extensionPoint: "asset-template", status: "passing", inputFixture: "asset draft with citations", expectedPreview: "editable exhibition sections", requiredAssertions: ["citation-required", "manual-save-only", "section-preview-visible"] },
        { id: "sync-preview-fixture", templateId: "sync-preview-template", extensionPoint: "sync-adapter", status: "passing", inputFixture: "local-first sync conflict package", expectedPreview: "conflict preview", requiredAssertions: ["per-item-decision-required", "manual-apply-only", "sync-audit-summary"] }
      ],
      negativeFixtures: [
        { id: "network-request-negative", templateId: "sync-preview-template", status: "blocked", reason: "network-request", expectedDecision: "sandbox-boundary-violation" },
        { id: "missing-citation-negative", templateId: "exhibition-layout-template", status: "blocked", reason: "citation-required", expectedDecision: "template-preview-blocked" },
        { id: "auto-apply-negative", templateId: "memory-import-template", status: "blocked", reason: "manual-apply-only", expectedDecision: "contract-test-failed" }
      ],
      blockedWhen: ["fixture-missing", "expected-preview-mismatch", "required-assertion-failed", "negative-fixture-not-blocked", "audit-summary-missing"],
      exportFields: ["phase20PlatformPlan.templatePreviewFixtures", "phase20PlatformPlan.noCodeTemplatePack", "phase20PlatformPlan.extensionContractTests"],
      nextControls: ["fixture-authoring-ui", "fixture-result-history", "template-preview-diff"]
    },
    signedManifestPolicy: {
      schemaVersion: "phase20.signed.manifest-policy.v1",
      status: "signature-policy-ready-runtime-disabled",
      runtimeExecution: false,
      signatureRequired: true,
      algorithm: "sha256-manifest-digest-placeholder",
      signerTrust: "local-owner-or-built-in-only",
      signedFields: ["id", "version", "extensionPoint", "permissions", "entryPolicy", "dataAccess", "audit", "sandboxBoundary", "templatePack"],
      checksumFields: ["manifestSchema.schemaVersion", "permissionReview.defaultDecision", "extensionContractTests.schemaVersion", "sandboxBoundary.schemaVersion", "noCodeTemplatePack.schemaVersion"],
      verificationSteps: ["parse-manifest", "normalize-fields", "calculate-digest", "compare-signature", "check-signer-trust", "record-audit-event"],
      sampleSignatures: [
        { pluginId: "markdown-importer", status: "built-in-trusted", digest: "sha256:phase20-markdown-importer-manifest" },
        { pluginId: "redacted-exporter", status: "built-in-trusted", digest: "sha256:phase20-redacted-exporter-manifest" },
        { pluginId: "third-party-placeholder", status: "blocked-unsigned", digest: "missing" }
      ],
      blockedWhen: ["signature-missing", "digest-mismatch", "untrusted-signer", "manifest-mutated-after-review", "permissions-changed-after-signature"],
      exportFields: ["phase20PlatformPlan.signedManifestPolicy", "phase20PlatformPlan.manifestSchema", "phase20PlatformPlan.pluginAuditLog"],
      nextControls: ["signature-ui", "manifest-lockfile", "reviewer-countersignature"]
    },
    pluginInstallationWorkflow: {
      schemaVersion: "phase20.plugin.installation-workflow.v1",
      status: "install-workflow-ready-runtime-disabled",
      runtimeExecution: false,
      defaultDecision: "block-or-pending-review",
      installStates: ["manifest-imported", "signature-verified", "permissions-reviewed", "contract-tested", "sandbox-checked", "audit-recorded", "pending-human-review", "blocked"],
      requiredGates: ["manifest-schema-valid", "signature-trusted", "permissions-approved", "contract-tests-passing", "sandbox-boundary-passing", "audit-event-recorded"],
      workflowSteps: [
        { id: "import-manifest", status: "ready", input: "plugin manifest json", output: "normalized manifest draft", blockingFailure: "invalid-manifest" },
        { id: "verify-signature", status: "ready", input: "normalized manifest digest", output: "trusted-or-blocked signature result", blockingFailure: "signature-missing-or-mismatch" },
        { id: "review-permissions", status: "ready", input: "declared permissions", output: "approved or pending human review", blockingFailure: "permission-unreviewed" },
        { id: "run-contract-tests", status: "ready", input: "extension point fixture", output: "contract pass or block decision", blockingFailure: "contract-test-failed" },
        { id: "check-sandbox-boundary", status: "ready", input: "declared capabilities", output: "sandbox pass or blocked capability list", blockingFailure: "sandbox-boundary-violation" },
        { id: "record-install-audit", status: "ready", input: "gate decisions", output: "installation audit summary", blockingFailure: "audit-record-missing" }
      ],
      sampleDecisions: [
        { pluginId: "markdown-importer", state: "installed-built-in", decision: "approved", evidence: ["built-in-trusted", "contract-tests-passing", "audit-recorded"] },
        { pluginId: "review-agent-template", state: "pending-human-review", decision: "pending", evidence: ["agent.suggest", "confirmation-required"] },
        { pluginId: "third-party-placeholder", state: "blocked", decision: "blocked", evidence: ["signature-missing", "runtimeExecution=false"] }
      ],
      blockedWhen: ["invalid-manifest", "signature-missing-or-mismatch", "permission-unreviewed", "contract-test-failed", "sandbox-boundary-violation", "audit-record-missing"],
      exportFields: ["phase20PlatformPlan.pluginInstallationWorkflow", "phase20PlatformPlan.signedManifestPolicy", "phase20PlatformPlan.extensionContractTests", "phase20PlatformPlan.sandboxBoundary"],
      nextControls: ["installation-queue-ui", "reviewer-approval-record", "plugin-lockfile"]
    },
    pluginReviewWorkbench: {
      schemaVersion: "phase20.plugin.review-workbench.v1",
      status: "review-workbench-ready-runtime-disabled",
      runtimeExecution: false,
      queueStates: ["pending-review", "approved-disabled", "blocked", "needs-signature", "needs-contract-fix"],
      reviewActions: ["approve-built-in-disabled", "block-plugin", "request-signature", "request-contract-fix", "record-review-note"],
      reviewQueue: [
        { pluginId: "long-term-review-agent", state: "pending-review", risk: "medium", requiredAction: "confirm agent.suggest before enablement", evidence: ["agent.suggest", "memory.read.summary", "human-confirmation-required"] },
        { pluginId: "third-party-placeholder", state: "blocked", risk: "high", requiredAction: "keep blocked until signed manifest and sandbox exist", evidence: ["signature-missing", "thirdPartyExecution=false"] },
        { pluginId: "exhibition-layout-template", state: "approved-disabled", risk: "low", requiredAction: "template preview only", evidence: ["citation-required", "manual-save-only"] }
      ],
      reviewerRecord: {
        requiredFields: ["reviewer", "decision", "reason", "permissions", "createdAt", "evidence"],
        decisionTypes: ["approved-disabled", "blocked", "needs-info"],
        storageMode: "exported-plan-and-operations-summary"
      },
      exportFields: ["phase20PlatformPlan.pluginReviewWorkbench", "phase20PlatformPlan.pluginAuditLog", "phase20PlatformPlan.permissionReview"],
      nextControls: ["review-note-ui", "permission-diff-view", "review-history"]
    },
    pluginLockfile: {
      schemaVersion: "phase20.plugin.lockfile.v1",
      status: "lockfile-model-ready-runtime-disabled",
      runtimeExecution: false,
      lockfileName: "phase20-plugin-lock.json",
      lockReason: "freeze reviewed manifests before any runtime work",
      lockedPlugins: [
        { pluginId: "markdown-importer", version: "built-in", digest: "sha256:phase20-markdown-importer-manifest", decision: "approved-disabled", runtimeEnabled: false },
        { pluginId: "redacted-exporter", version: "built-in", digest: "sha256:phase20-redacted-exporter-manifest", decision: "approved-disabled", runtimeEnabled: false },
        { pluginId: "third-party-placeholder", version: "0.0.0", digest: "missing", decision: "blocked", runtimeEnabled: false }
      ],
      integrityChecks: ["unique-plugin-id", "digest-present-for-approved", "review-decision-present", "runtime-disabled", "permissions-match-reviewed-manifest"],
      blockedWhen: ["missing-lock-entry", "digest-mismatch", "permission-drift", "runtime-enabled-without-sandbox", "review-decision-missing"],
      exportFields: ["phase20PlatformPlan.pluginLockfile", "phase20PlatformPlan.signedManifestPolicy", "phase20PlatformPlan.pluginReviewWorkbench"],
      nextControls: ["lockfile-file-export", "lockfile-diff", "install-queue-persistence"]
    },
    pluginLockfileExport: {
      schemaVersion: "phase20.plugin.lockfile-export.v1",
      status: "lockfile-export-ready-runtime-disabled",
      runtimeExecution: false,
      exportName: "phase20-plugin-lock.export.json",
      exportModes: ["full-lockfile", "review-summary", "redacted-demo-lockfile"],
      exportWorkflow: ["collect-reviewed-manifests", "attach-digests", "attach-review-decisions", "verify-runtime-disabled", "emit-export-audit"],
      sampleExport: {
        lockedCount: 3,
        blockedCount: 1,
        runtimeEnabledCount: 0,
        includesReviewEvidence: true
      },
      blockedWhen: ["lockfile-missing", "approved-plugin-without-digest", "runtime-enabled-entry", "review-evidence-missing"],
      exportFields: ["phase20PlatformPlan.pluginLockfileExport", "phase20PlatformPlan.pluginLockfile", "phase20PlatformPlan.pluginAuditLog"],
      nextControls: ["download-lockfile-json", "redacted-lockfile-export", "lockfile-import-preview"]
    },
    pluginInstallQueuePersistence: {
      schemaVersion: "phase20.plugin.install-queue.v1",
      status: "install-queue-model-ready-runtime-disabled",
      runtimeExecution: false,
      storageMode: "exported-plan-local-first",
      queueStates: ["queued", "reviewing", "locked", "blocked", "archived"],
      queueItems: [
        { id: "queue-long-term-review-agent", pluginId: "long-term-review-agent", state: "reviewing", nextAction: "record reviewer note", evidence: ["pending-review", "agent.suggest"] },
        { id: "queue-exhibition-layout-template", pluginId: "exhibition-layout-template", state: "locked", nextAction: "keep preview-only", evidence: ["approved-disabled", "manual-save-only"] },
        { id: "queue-third-party-placeholder", pluginId: "third-party-placeholder", state: "blocked", nextAction: "wait for trusted signature", evidence: ["signature-missing", "runtime-disabled"] }
      ],
      persistenceChecks: ["stable-queue-id", "plugin-id-linked", "review-state-linked", "lockfile-entry-linked", "audit-event-linked"],
      exportFields: ["phase20PlatformPlan.pluginInstallQueuePersistence", "phase20PlatformPlan.pluginReviewWorkbench", "phase20PlatformPlan.pluginLockfile"],
      nextControls: ["queue-json-persistence", "queue-filter-ui", "queue-archive-policy"]
    },
    pluginReleaseSignatureGate: {
      schemaVersion: "phase20.plugin.release-signature-gate.v1",
      status: "release-signature-gate-ready-runtime-disabled",
      runtimeExecution: false,
      releaseGatePolicy: "block-release-if-signature-or-lockfile-drift",
      requiredBeforeRelease: ["signed-manifest-present", "lockfile-digest-match", "review-decision-present", "runtime-disabled", "audit-summary-exported"],
      sampleGateResults: [
        { pluginId: "markdown-importer", result: "pass", evidence: ["built-in-trusted", "digest-match", "runtime-disabled"] },
        { pluginId: "long-term-review-agent", result: "hold", evidence: ["pending-review", "reviewer-note-required"] },
        { pluginId: "third-party-placeholder", result: "block", evidence: ["signature-missing", "untrusted-signer"] }
      ],
      blockedWhen: ["signature-missing", "digest-mismatch", "lockfile-drift", "runtime-enabled", "audit-summary-missing"],
      exportFields: ["phase20PlatformPlan.pluginReleaseSignatureGate", "phase20PlatformPlan.signedManifestPolicy", "phase20PlatformPlan.pluginLockfileExport"],
      nextControls: ["release-gate-ui", "signature-diff-report", "pre-release-check-command"]
    },
    pluginLockfileImportPreview: {
      schemaVersion: "phase20.plugin.lockfile-import-preview.v1",
      status: "lockfile-import-preview-ready-runtime-disabled",
      runtimeExecution: false,
      acceptedInputs: ["phase20-plugin-lock.export.json", "redacted-demo-lockfile"],
      previewWorkflow: ["parse-lockfile", "compare-digests", "compare-permissions", "compare-review-decisions", "compare-signature-status", "emit-preview-audit"],
      diffCategories: ["new-plugin", "removed-plugin", "digest-changed", "permission-drift", "review-decision-changed", "signature-status-changed"],
      sampleDiffs: [
        { pluginId: "markdown-importer", category: "safe-match", severity: "info", decision: "allow-preview", evidence: ["digest-match", "permissions-match", "runtime-disabled"] },
        { pluginId: "long-term-review-agent", category: "permission-drift", severity: "hold", decision: "require-review", evidence: ["memory.read.summary-added", "human-review-required"] },
        { pluginId: "third-party-placeholder", category: "signature-status-changed", severity: "block", decision: "block-import", evidence: ["signature-missing", "digest-mismatch"] }
      ],
      blockedWhen: ["invalid-lockfile-schema", "runtime-enabled-entry", "permission-drift", "signature-missing", "digest-mismatch"],
      exportFields: ["phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginLockfileExport", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      nextControls: ["lockfile-import-ui", "diff-acceptance-record", "import-preview-audit-history"]
    },
    pluginPreReleaseCheckCommand: {
      schemaVersion: "phase20.plugin.pre-release-check.v1",
      status: "pre-release-check-command-ready-runtime-disabled",
      runtimeExecution: false,
      command: "npm.cmd run phase20:plugin-pre-release",
      requiredInputs: ["phase20PlatformPlan.pluginLockfileExport", "phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginReleaseSignatureGate", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginDiffReviewHistory"],
      checkWorkflow: ["load-version-payload", "verify-runtime-disabled", "verify-lockfile-import-preview", "verify-signature-gate", "verify-signature-diff-report", "verify-release-report-artifact", "verify-diff-review-history", "verify-release-history", "emit-command-summary"],
      requiredAssertions: ["version-build-label-match", "runtime-disabled", "lockfile-import-preview-ready", "permission-drift-blocked", "signature-missing-blocked", "digest-mismatch-blocked", "release-gate-blocks-drift", "signature-diff-report-ready", "release-report-artifact-ready", "diff-review-history-ready"],
      sampleResults: [
        { id: "built-in-lockfile", result: "pass", evidence: ["runtime-disabled", "digest-match", "release-history-present"] },
        { id: "permission-drift-import", result: "hold", evidence: ["permission-drift", "human-review-required"] },
        { id: "unsigned-third-party", result: "block", evidence: ["signature-missing", "digest-mismatch"] }
      ],
      blockedWhen: ["version-build-label-mismatch", "runtime-enabled-entry", "lockfile-import-preview-missing", "signature-gate-missing", "permission-drift", "signature-missing", "digest-mismatch"],
      exportFields: ["phase20PlatformPlan.pluginPreReleaseCheckCommand", "phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      nextControls: ["signature-diff-report", "release-report-artifact", "release-report-validation-command", "ci-command-wrapper"]
    },
    pluginSignatureDiffReport: {
      schemaVersion: "phase20.plugin.signature-diff-report.v1",
      status: "signature-diff-report-ready-runtime-disabled",
      runtimeExecution: false,
      reportName: "phase20-plugin-signature-diff.report.json",
      reportInputs: ["phase20-plugin-lock.export.json", "phase20PlatformPlan.signedManifestPolicy", "phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      diffAxes: ["manifest-digest", "signature-status", "permission-set", "review-decision", "lockfile-entry", "runtime-policy"],
      reportWorkflow: ["collect-lockfile-preview-diffs", "collect-signature-gate-results", "group-by-severity", "attach-review-actions", "emit-report-summary"],
      severityLevels: ["info", "hold", "block"],
      sampleFindings: [
        { id: "markdown-importer-digest", pluginId: "markdown-importer", axis: "manifest-digest", severity: "info", result: "match", releaseAction: "allow", evidence: ["digest-match", "built-in-trusted"] },
        { id: "long-term-review-permission", pluginId: "long-term-review-agent", axis: "permission-set", severity: "hold", result: "permission-drift", releaseAction: "require-review", evidence: ["memory.read.summary-added", "reviewer-note-required"] },
        { id: "third-party-signature", pluginId: "third-party-placeholder", axis: "signature-status", severity: "block", result: "signature-missing", releaseAction: "block-release", evidence: ["signature-missing", "digest-mismatch", "untrusted-signer"] }
      ],
      summary: {
        totalFindings: 3,
        info: 1,
        hold: 1,
        block: 1,
        releaseReady: false
      },
      blockedWhen: ["signature-missing", "digest-mismatch", "permission-drift-unreviewed", "lockfile-drift", "runtime-enabled-entry"],
      exportFields: ["phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginPreReleaseCheckCommand", "phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      nextControls: ["release-report-artifact", "diff-review-history", "signed-report-export"]
    },
    pluginReleaseReportArtifact: {
      schemaVersion: "phase20.plugin.release-report-artifact.v1",
      status: "release-report-artifact-ready-runtime-disabled",
      runtimeExecution: false,
      artifactName: "phase20-plugin-release.report.json",
      artifactType: "release-evidence-package",
      reportInputs: ["phase20PlatformPlan.pluginPreReleaseCheckCommand", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginLockfileImportPreview", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      includedSections: ["version-build-label", "runtime-policy", "lockfile-export", "lockfile-import-preview", "signature-diff-summary", "pre-release-command-results", "release-gate-results", "release-decision"],
      generationWorkflow: ["collect-version-payload", "attach-lockfile-export-summary", "attach-import-preview-diffs", "attach-signature-gate-results", "attach-pre-release-command-summary", "attach-signature-diff-findings", "emit-release-decision"],
      releaseDecision: {
        status: "blocked",
        releaseReady: false,
        infoFindings: 1,
        holdFindings: 1,
        blockFindings: 1,
        requiredAction: "resolve-block-findings-before-release",
        evidence: ["third-party-signature:block-release", "runtime-disabled", "pre-release-command-present"]
      },
      sampleArtifact: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        releaseReady: false,
        generatedBy: "npm.cmd run phase20:plugin-pre-release",
        blockingFindingIds: ["third-party-signature"],
        attachedReports: ["phase20-plugin-signature-diff.report.json", "phase20-plugin-lock.export.json"]
      },
      blockedWhen: ["block-finding-present", "runtime-enabled-entry", "missing-signature-diff-report", "missing-pre-release-command-summary", "release-gate-blocked"],
      exportFields: ["phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginPreReleaseCheckCommand", "phase20PlatformPlan.pluginReleaseSignatureGate"],
      nextControls: ["diff-review-history", "release-report-download", "signed-report-export"]
    },
    pluginDiffReviewHistory: {
      schemaVersion: "phase20.plugin.diff-review-history.v1",
      status: "diff-review-history-ready-runtime-disabled",
      runtimeExecution: false,
      historyName: "phase20-plugin-diff-review.history.json",
      historyInputs: ["phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginReviewWorkbench"],
      trackedFields: ["findingId", "pluginId", "axis", "severity", "reviewer", "decision", "releaseAction", "evidence", "reviewedAt", "reportArtifact"],
      reviewStates: ["pending-review", "approved", "held", "blocked", "waived"],
      decisionWorkflow: ["load-release-report-findings", "link-reviewer-record", "capture-review-decision", "attach-evidence", "update-release-action", "emit-history-entry"],
      reviewEntries: [
        { id: "review-markdown-importer-digest", findingId: "markdown-importer-digest", pluginId: "markdown-importer", axis: "manifest-digest", severity: "info", reviewer: "system", decision: "approved", releaseAction: "allow", evidence: ["digest-match", "built-in-trusted"], reviewedAt: "2026-06-25T00:00:00.000Z", reportArtifact: "phase20-plugin-release.report.json" },
        { id: "review-long-term-review-permission", findingId: "long-term-review-permission", pluginId: "long-term-review-agent", axis: "permission-set", severity: "hold", reviewer: "human-required", decision: "held", releaseAction: "require-review", evidence: ["memory.read.summary-added", "reviewer-note-required"], reviewedAt: null, reportArtifact: "phase20-plugin-release.report.json" },
        { id: "review-third-party-signature", findingId: "third-party-signature", pluginId: "third-party-placeholder", axis: "signature-status", severity: "block", reviewer: "release-gate", decision: "blocked", releaseAction: "block-release", evidence: ["signature-missing", "digest-mismatch", "untrusted-signer"], reviewedAt: "2026-06-25T00:00:00.000Z", reportArtifact: "phase20-plugin-release.report.json" }
      ],
      summary: {
        totalReviews: 3,
        approved: 1,
        held: 1,
        blocked: 1,
        waived: 0,
        unresolved: 1,
        releaseReady: false
      },
      blockedWhen: ["block-review-present", "hold-without-reviewer-note", "waiver-without-evidence", "runtime-enabled-entry", "missing-release-report-artifact"],
      exportFields: ["phase20PlatformPlan.pluginDiffReviewHistory", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginReviewWorkbench"],
      nextControls: ["release-report-validation-command", "signed-report-export", "review-history-ui"]
    },
    pluginReleaseReportValidationCommand: {
      schemaVersion: "phase20.plugin.release-report-validation-command.v1",
      status: "release-report-validation-command-ready-runtime-disabled",
      runtimeExecution: false,
      command: "npm.cmd run phase20:plugin-pre-release",
      validationTarget: "phase20-plugin-release.report.json",
      requiredInputs: ["phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginDiffReviewHistory", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.pluginPreReleaseCheckCommand"],
      validationWorkflow: ["load-release-report-artifact", "verify-version-build-label", "verify-attached-signature-diff-report", "verify-diff-review-history-linked", "verify-block-findings-block-release", "verify-hold-findings-have-reviewer-note", "verify-release-ready-consistency", "emit-validation-summary"],
      requiredAssertions: ["release-report-schema-present", "version-build-label-match", "signature-diff-report-attached", "diff-review-history-linked", "block-finding-keeps-releaseReady-false", "hold-finding-requires-reviewer-note", "waiver-requires-evidence", "runtime-disabled"],
      sampleValidationResults: [
        { id: "release-report-schema", result: "pass", evidence: ["phase20.plugin.release-report-artifact.v1", "phase20-plugin-release.report.json"] },
        { id: "hold-review-note", result: "hold", evidence: ["long-term-review-permission", "reviewer-note-required", "human-required"] },
        { id: "block-release-ready", result: "block", evidence: ["third-party-signature", "block-review-present", "releaseReady:false"] }
      ],
      summary: {
        totalAssertions: 8,
        pass: 6,
        hold: 1,
        block: 1,
        releaseReady: false
      },
      blockedWhen: ["release-report-missing", "version-build-label-mismatch", "block-finding-releaseReady-true", "hold-without-reviewer-note", "waiver-without-evidence", "runtime-enabled-entry"],
      exportFields: ["phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginDiffReviewHistory"],
      nextControls: ["signed-report-export", "validation-command-ci-wrapper", "review-history-ui"]
    },
    pluginSignedReleaseReportExport: {
      schemaVersion: "phase20.plugin.signed-release-report-export.v1",
      status: "signed-release-report-export-ready-runtime-disabled",
      runtimeExecution: false,
      exportName: "phase20-plugin-release.signed-report.json",
      exportType: "signed-release-evidence-package",
      exportInputs: ["phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginDiffReviewHistory", "phase20PlatformPlan.pluginSignatureDiffReport", "phase20PlatformPlan.signedManifestPolicy"],
      exportWorkflow: ["load-validation-summary", "attach-release-report-artifact", "attach-diff-review-history", "attach-signature-diff-report", "apply-signature-envelope", "record-export-audit", "emit-signed-report-package"],
      requiredAssertions: ["validation-summary-attached", "release-report-artifact-attached", "diff-review-history-attached", "signature-diff-report-attached", "signature-envelope-present", "runtime-disabled", "releaseReady-false-when-blocked", "export-audit-recorded"],
      signatureEnvelope: {
        algorithm: "offline-review-signature-placeholder",
        signer: "release-reviewer-required",
        digestSource: "phase20-plugin-release.report.json",
        signatureRequired: true,
        runtimeSecretsUsed: false
      },
      sampleExport: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        exportReady: false,
        releaseReady: false,
        signed: false,
        signatureState: "pending-human-signature",
        attachedReports: ["phase20-plugin-release.report.json", "phase20-plugin-diff-review.history.json", "phase20-plugin-signature-diff.report.json"]
      },
      summary: {
        totalExports: 1,
        signed: 0,
        pendingSignature: 1,
        blocked: 1,
        releaseReady: false
      },
      blockedWhen: ["validation-summary-missing", "signature-envelope-missing", "signed-while-blocked", "runtime-enabled-entry", "export-without-audit", "releaseReady-mismatch"],
      exportFields: ["phase20PlatformPlan.pluginSignedReleaseReportExport", "phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginDiffReviewHistory"],
      nextControls: ["review-history-ui", "validation-command-ci-wrapper", "signed-report-download"]
    },
    pluginReviewHistoryUi: {
      schemaVersion: "phase20.plugin.review-history-ui.v1",
      status: "review-history-ui-ready-runtime-disabled",
      runtimeExecution: false,
      panelId: "phase20-review-history-ui",
      sourceHistory: "phase20-plugin-diff-review.history.json",
      requiredInputs: ["phase20PlatformPlan.pluginDiffReviewHistory", "phase20PlatformPlan.pluginReleaseReportArtifact", "phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginSignedReleaseReportExport"],
      displayColumns: ["findingId", "pluginId", "severity", "decision", "releaseAction", "reviewer", "reviewedAt", "evidence", "reportArtifact"],
      filterControls: ["severity", "decision", "reviewer", "releaseAction", "unresolvedOnly"],
      reviewActions: ["open-evidence", "copy-finding-id", "record-review-note-placeholder", "link-signed-export", "block-release-summary"],
      stateBadges: [
        { state: "approved", tone: "success", label: "approved" },
        { state: "held", tone: "warning", label: "human review required" },
        { state: "blocked", tone: "danger", label: "blocks release" }
      ],
      sampleRows: [
        { findingId: "markdown-importer-digest", pluginId: "markdown-importer", severity: "info", decision: "approved", visible: true, primaryAction: "open-evidence" },
        { findingId: "long-term-review-permission", pluginId: "long-term-review-agent", severity: "hold", decision: "held", visible: true, primaryAction: "record-review-note-placeholder" },
        { findingId: "third-party-signature", pluginId: "third-party-placeholder", severity: "block", decision: "blocked", visible: true, primaryAction: "block-release-summary" }
      ],
      summary: {
        totalRows: 3,
        visibleBlockers: 1,
        unresolvedHolds: 1,
        signedExportLinked: true,
        releaseReady: false
      },
      blockedWhen: ["missing-diff-review-history", "missing-release-report-artifact", "blocked-row-hidden", "hold-row-without-review-action", "signed-export-link-missing", "runtime-enabled-entry"],
      exportFields: ["phase20PlatformPlan.pluginReviewHistoryUi", "phase20PlatformPlan.pluginDiffReviewHistory", "phase20PlatformPlan.pluginSignedReleaseReportExport"],
      nextControls: ["validation-command-ci-wrapper", "signed-report-download", "plugin-review-workbench-actions"]
    },
    pluginValidationCommandCiWrapper: {
      schemaVersion: "phase20.plugin.validation-command-ci-wrapper.v1",
      status: "validation-command-ci-wrapper-ready-runtime-disabled",
      runtimeExecution: false,
      command: "npm.cmd run phase20:plugin-pre-release",
      ciCommand: "npm.cmd run phase20:plugin-pre-release -- --ci",
      requiredInputs: ["phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginSignedReleaseReportExport", "phase20PlatformPlan.pluginReviewHistoryUi", "package.json.scripts.phase20:plugin-pre-release"],
      ciWorkflow: ["install-node-24", "run-syntax-checks", "run-phase20-plugin-pre-release", "collect-json-summary", "publish-ci-artifacts", "block-on-hold-or-block"],
      exitCodePolicy: [
        { result: "pass", exitCode: 0, releaseReady: true },
        { result: "hold", exitCode: 2, releaseReady: false },
        { result: "block", exitCode: 3, releaseReady: false }
      ],
      requiredArtifacts: ["phase20-plugin-pre-release.summary.json", "phase20-plugin-release.report.json", "phase20-plugin-release.signed-report.json"],
      sampleCiRun: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        command: "npm.cmd run phase20:plugin-pre-release",
        result: "block",
        exitCode: 3,
        releaseReady: false,
        artifactsPublished: ["phase20-plugin-pre-release.summary.json", "phase20-plugin-release.signed-report.json"]
      },
      summary: {
        totalSteps: 6,
        pass: 4,
        hold: 1,
        block: 1,
        releaseReady: false
      },
      blockedWhen: ["ci-command-missing", "json-summary-missing", "hold-result-exit-zero", "block-result-exit-zero", "runtime-enabled-entry", "artifact-not-published"],
      exportFields: ["phase20PlatformPlan.pluginValidationCommandCiWrapper", "phase20PlatformPlan.pluginReleaseReportValidationCommand", "phase20PlatformPlan.pluginSignedReleaseReportExport", "phase20PlatformPlan.pluginReviewHistoryUi"],
      nextControls: ["signed-report-download", "ci-badge-summary", "release-checklist-gate"]
    },
    pluginSignedReportDownload: {
      schemaVersion: "phase20.plugin.signed-report-download.v1",
      status: "signed-report-download-ready-runtime-disabled",
      runtimeExecution: false,
      downloadName: "phase20-plugin-release.signed-report.download.json",
      sourceArtifact: "phase20-plugin-release.signed-report.json",
      requiredInputs: ["phase20PlatformPlan.pluginSignedReleaseReportExport", "phase20PlatformPlan.pluginValidationCommandCiWrapper", "phase20PlatformPlan.pluginReviewHistoryUi", "phase20PlatformPlan.pluginReleaseReportArtifact"],
      downloadWorkflow: ["load-signed-report-export", "verify-ci-wrapper-result", "attach-review-history-ui-summary", "apply-redacted-download-envelope", "emit-download-metadata", "record-download-audit", "prepare-browser-download"],
      downloadEnvelope: {
        format: "json",
        redacted: true,
        includesRuntimeSecrets: false,
        checksumAlgorithm: "sha256-placeholder",
        contentDisposition: "attachment"
      },
      sampleDownload: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        fileName: "phase20-plugin-release.signed-report.download.json",
        ready: false,
        releaseReady: false,
        sourceArtifact: "phase20-plugin-release.signed-report.json",
        includedReports: ["phase20-plugin-release.report.json", "phase20-plugin-diff-review.history.json", "phase20-plugin-pre-release.summary.json"]
      },
      summary: {
        totalFiles: 3,
        downloadable: 1,
        redacted: 1,
        blocked: 1,
        releaseReady: false
      },
      blockedWhen: ["signed-report-missing", "ci-wrapper-blocked", "download-envelope-missing", "runtime-enabled-entry", "secret-field-included", "audit-record-missing"],
      exportFields: ["phase20PlatformPlan.pluginSignedReportDownload", "phase20PlatformPlan.pluginSignedReleaseReportExport", "phase20PlatformPlan.pluginValidationCommandCiWrapper"],
      nextControls: ["ci-badge-summary", "release-checklist-gate", "download-integrity-preview"]
    },
    pluginCiBadgeSummary: {
      schemaVersion: "phase20.plugin.ci-badge-summary.v1",
      status: "ci-badge-summary-ready-runtime-disabled",
      runtimeExecution: false,
      badgeName: "phase20-plugin-ci.badge-summary.json",
      requiredInputs: ["phase20PlatformPlan.pluginValidationCommandCiWrapper", "phase20PlatformPlan.pluginSignedReportDownload", "phase20PlatformPlan.pluginSignedReleaseReportExport"],
      badgeWorkflow: ["load-ci-wrapper-summary", "derive-release-state", "map-exit-code-to-badge", "attach-download-readiness", "emit-badge-json", "publish-readme-status-line", "record-badge-audit"],
      badgeStates: [
        { result: "pass", label: "plugin-ci: passing", tone: "success", releaseReady: true },
        { result: "hold", label: "plugin-ci: hold", tone: "warning", releaseReady: false },
        { result: "block", label: "plugin-ci: blocked", tone: "danger", releaseReady: false }
      ],
      sampleBadge: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        result: "block",
        label: "plugin-ci: blocked",
        exitCode: 3,
        releaseReady: false,
        downloadReady: false,
        artifact: "phase20-plugin-ci.badge-summary.json"
      },
      summary: {
        totalBadges: 3,
        published: 1,
        blocked: 1,
        releaseReady: false
      },
      blockedWhen: ["ci-wrapper-missing", "exit-code-policy-missing", "download-summary-missing", "badge-state-mismatch", "runtime-enabled-entry", "badge-audit-missing"],
      exportFields: ["phase20PlatformPlan.pluginCiBadgeSummary", "phase20PlatformPlan.pluginValidationCommandCiWrapper", "phase20PlatformPlan.pluginSignedReportDownload"],
      nextControls: ["download-integrity-preview", "release-checklist-gate", "ci-badge-ui"]
    },
    pluginDownloadIntegrityPreview: {
      schemaVersion: "phase20.plugin.download-integrity-preview.v1",
      status: "download-integrity-preview-ready-runtime-disabled",
      runtimeExecution: false,
      previewName: "phase20-plugin-release.download-integrity.preview.json",
      sourceDownload: "phase20-plugin-release.signed-report.download.json",
      requiredInputs: ["phase20PlatformPlan.pluginSignedReportDownload", "phase20PlatformPlan.pluginCiBadgeSummary", "phase20PlatformPlan.pluginSignedReleaseReportExport"],
      integrityWorkflow: ["load-download-envelope", "compute-sha256-placeholder", "compare-source-artifact-digest", "attach-ci-badge-state", "emit-integrity-preview", "record-integrity-audit", "block-on-mismatch"],
      checksumPolicy: {
        algorithm: "sha256-placeholder",
        required: true,
        redactedEnvelopeOnly: true,
        mismatchBlocksRelease: true
      },
      samplePreview: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        fileName: "phase20-plugin-release.download-integrity.preview.json",
        sourceDownload: "phase20-plugin-release.signed-report.download.json",
        checksum: "sha256-placeholder:blocked-sample",
        match: false,
        badgeResult: "block",
        releaseReady: false
      },
      summary: {
        totalChecks: 5,
        pass: 3,
        warning: 1,
        block: 1,
        releaseReady: false
      },
      blockedWhen: ["download-file-missing", "checksum-missing", "checksum-mismatch", "source-artifact-drift", "badge-result-blocked", "runtime-enabled-entry", "integrity-audit-missing"],
      exportFields: ["phase20PlatformPlan.pluginDownloadIntegrityPreview", "phase20PlatformPlan.pluginSignedReportDownload", "phase20PlatformPlan.pluginCiBadgeSummary"],
      nextControls: ["release-checklist-gate", "download-integrity-ui", "integrity-audit-export"]
    },
    pluginReleaseChecklistGate: {
      schemaVersion: "phase20.plugin.release-checklist-gate.v1",
      status: "release-checklist-gate-ready-runtime-disabled",
      runtimeExecution: false,
      gateName: "phase20-plugin-release.checklist-gate.json",
      requiredInputs: [
        "phase20PlatformPlan.pluginCiBadgeSummary",
        "phase20PlatformPlan.pluginDownloadIntegrityPreview",
        "phase20PlatformPlan.pluginSignedReleaseReportExport",
        "phase20PlatformPlan.pluginReviewHistoryUi",
        "phase20PlatformPlan.pluginReleaseSignatureGate"
      ],
      gateWorkflow: ["load-release-checklist", "collect-ci-badge", "collect-download-integrity", "verify-signed-export", "verify-review-history", "verify-runtime-disabled", "derive-final-releaseReady", "emit-checklist-gate"],
      gateItems: [
        { id: "ci-badge", source: "pluginCiBadgeSummary", result: "block", releaseReady: false },
        { id: "download-integrity", source: "pluginDownloadIntegrityPreview", result: "block", releaseReady: false },
        { id: "signed-export", source: "pluginSignedReleaseReportExport", result: "hold", releaseReady: false },
        { id: "review-history", source: "pluginReviewHistoryUi", result: "block", releaseReady: false },
        { id: "runtime-disabled", source: "securityModel", result: "pass", releaseReady: true }
      ],
      sampleGate: {
        version: "1.0.25",
        buildLabel: "phase20-release-checklist-gate",
        result: "block",
        releaseReady: false,
        blockingItems: ["ci-badge", "download-integrity", "review-history"],
        holdItems: ["signed-export"],
        artifact: "phase20-plugin-release.checklist-gate.json"
      },
      summary: {
        totalItems: 5,
        pass: 1,
        hold: 1,
        block: 3,
        releaseReady: false
      },
      blockedWhen: ["ci-badge-blocked", "download-integrity-blocked", "signed-export-hold", "review-history-blocked", "runtime-enabled-entry", "checklist-gate-audit-missing"],
      exportFields: ["phase20PlatformPlan.pluginReleaseChecklistGate", "phase20PlatformPlan.pluginCiBadgeSummary", "phase20PlatformPlan.pluginDownloadIntegrityPreview"],
      nextControls: ["release-gate-ui", "release-gate-audit-export", "runtime-sandbox-plan"]
    },
    securityModel: {
      defaultTrust: "built-in-only",
      thirdPartyExecution: false,
      networkAccessForPlugins: false,
      secretStorage: false,
      dataAccess: "explicit-export-or-reviewed-draft-only",
      requiredControls: ["manifest-review", "permission-labels", "human-confirmation", "audit-log", "redaction-before-share"]
    },
    readiness: {
      memoryCount: memories.length,
      importPlanReady: true,
      manifestSchemaReady: true,
      permissionReviewReady: true,
      pluginAuditLogReady: true,
      builtInRegistryReady: true,
      extensionContractTestsReady: true,
      sandboxBoundaryReady: true,
      noCodeTemplatePackReady: true,
      templatePreviewFixturesReady: true,
      signedManifestPolicyReady: true,
      pluginInstallationWorkflowReady: true,
      pluginReviewWorkbenchReady: true,
      pluginLockfileReady: true,
      pluginLockfileExportReady: true,
      pluginInstallQueuePersistenceReady: true,
      pluginReleaseSignatureGateReady: true,
      pluginLockfileImportPreviewReady: true,
      pluginPreReleaseCheckCommandReady: true,
      pluginSignatureDiffReportReady: true,
      pluginReleaseReportArtifactReady: true,
      pluginDiffReviewHistoryReady: true,
      pluginReleaseReportValidationCommandReady: true,
      pluginSignedReleaseReportExportReady: true,
      pluginReviewHistoryUiReady: true,
      pluginValidationCommandCiWrapperReady: true,
      pluginSignedReportDownloadReady: true,
      pluginCiBadgeSummaryReady: true,
      pluginDownloadIntegrityPreviewReady: true,
      pluginReleaseChecklistGateReady: true,
      exportBoundaryReady: true,
      syncBoundaryReady: true,
      pluginRuntimeReady: false,
      recommendation: "Phase 20 twenty-sixth edition adds release checklist gate modeling so CI badge, download integrity, signed export, review history, and runtime-disabled checks can produce a final releaseReady decision while plugin runtime stays disabled."
    },
    nextMilestones: [
      "phase20-release-gate-audit-export",
      "phase20-runtime-sandbox-plan"
    ]
  };
}

function buildLocalModuleBoundaryPlan() {
  return [
    { id: "routes", label: "HTTP routes", status: "split-active", rule: "第三版已迁移 health 与 operations 路由分发；后续继续拆 memories 和 privacy。" },
    { id: "health", label: "Health route", status: "split-stable", rule: "/api/health 已独立封装，继续保持 health smoke 覆盖。" },
    { id: "operations", label: "Operations", status: "split-stable", rule: "服务层和 route 层已分离；后续清理 server.js 旧实现并补更细的 route 测试。" },
    { id: "privacy", label: "Privacy", status: "ready-to-split", rule: "保持 /api/privacy 和数据主权导出字段兼容。" },
    { id: "agents", label: "Agent workflow", status: "split-after-routes", rule: "先用 smoke test 固定 workflow.run、steps、events 契约，再拆实现。" },
    { id: "frontend", label: "Frontend panels", status: "split-after-api", rule: "先按面板拆渲染函数，暂不引入构建工具。" }
  ];
}

function buildLocalPhase14Readiness() {
  const checks = [
    { id: "api-contract", label: "API 契约保护", status: "ready", detail: "api-smoke 已覆盖核心接口。" },
    { id: "operations-guard", label: "运维保护网", status: "ready", detail: "已有请求 ID、运行事件、日志和运维导出包。" },
    { id: "docs-route", label: "重构路线文档", status: "ready", detail: "项目规划和白皮书已声明阶段 14 模块化目标。" },
    { id: "module-plan", label: "模块边界清单", status: "ready", detail: "routes、operations、privacy、agents、frontend 边界已列出。" },
    { id: "data-safety", label: "数据安全回归", status: "ready", detail: "导入、导出、脱敏、清空、隐私策略均有回归。" },
    { id: "sample-signal", label: "演示样本信号", status: memories.length > 0 ? "ready" : "optional", detail: "无样本也可进入重构；有样本时更利于视觉回归。" }
  ];
  const readyCount = checks.filter((item) => item.status === "ready").length;
  return {
    targetPhase: 14,
    targetName: "工程模块化和服务边界重构",
    ready: readyCount >= 5,
    score: Math.round((readyCount / checks.length) * 100),
    checks,
    recommendedOrder: ["routes", "operations", "privacy", "agents", "rag", "workflow", "frontend-panels"],
    stopConditions: [
      "任一现有 API smoke 失败时暂停拆分",
      "导入导出结构变化时先补迁移说明",
      "前端面板拆分后必须保持无后端回退能力"
    ],
    recommendation: readyCount >= 5
      ? "阶段 14 第三版已完成 health 与 operations route split，并具备进入阶段 15 的基础保护网。"
      : "继续补齐 API 契约、运维导出和模块边界后再进入阶段 14。"
  };
}

function buildLocalPhase21RuntimeSandboxPlan() {
  return {
    phase: 21,
    phaseName: "受限插件运行时和沙箱执行版",
    schemaVersion: "phase21.runtime.sandbox-foundation.v1",
    status: "permission-runtime-enforcement-ready-third-party-disabled",
    version: "1.1.5",
    buildLabel: "phase21-runtime-result-quarantine",
    runtimeExecution: false,
    thirdPartyExecution: false,
    defaultMode: "kill-switch-disabled",
    allowedInitialExtensionPoints: ["importer", "exporter", "asset-template"],
    blockedInitialExtensionPoints: ["agent-tool", "sync-adapter"],
    hostApiBoundary: {
      directDatabaseAccess: false,
      directFilesystemAccess: false,
      networkAccess: false,
      environmentAccess: false,
      secretAccess: false,
      allowedApis: ["read-redacted-input", "emit-draft-output", "emit-audit-event", "read-fixture"]
    },
    permissionRuntimePolicy: {
      schemaVersion: "phase21.permission.runtime-enforcement.v1",
      status: "permission-runtime-enforcement-ready-third-party-disabled",
      enforcementMode: "pre-execution-model-only",
      defaultDecision: "deny",
      permissionSources: [
        "phase20PlatformPlan.manifestSchema.permissions",
        "phase20PlatformPlan.permissionReview",
        "phase20PlatformPlan.pluginLockfile",
        "phase20PlatformPlan.signedManifestPolicy",
        "phase20PlatformPlan.pluginReleaseChecklistGate"
      ],
      hostApiPermissions: [
        { hostApi: "read-redacted-input", requiredPermission: "memory.import.preview", allowedExtensionPoints: ["importer"], decision: "allow-if-reviewed-and-kill-switch-enabled" },
        { hostApi: "emit-draft-output", requiredPermission: "memory.import.preview", allowedExtensionPoints: ["importer", "asset-template"], decision: "allow-if-contract-valid" },
        { hostApi: "emit-audit-event", requiredPermission: "plugin.audit.emit", allowedExtensionPoints: ["importer", "exporter", "asset-template"], decision: "allow-for-all-runtime-decisions" },
        { hostApi: "read-fixture", requiredPermission: "plugin.fixture.read", allowedExtensionPoints: ["importer", "exporter", "asset-template"], decision: "allow-fixture-only" }
      ],
      requestValidation: {
        requiredFields: ["pluginId", "extensionPoint", "hostApi", "permissions", "lockfileDecision", "signatureStatus", "releaseGateResult", "killSwitch"],
        unknownHostApiDecision: "deny-and-audit",
        missingPermissionDecision: "deny-and-audit",
        extensionPointMismatchDecision: "deny-and-audit",
        runtimeEnabledDecision: "deny-while-kill-switch-disabled"
      },
      samplePermissionDecisions: [
        { pluginId: "markdown-importer", extensionPoint: "importer", hostApi: "read-redacted-input", requestedPermission: "memory.import.preview", policyDecision: "blocked", blockedReason: "kill-switch-disabled" },
        { pluginId: "third-party-placeholder", extensionPoint: "importer", hostApi: "read-redacted-input", requestedPermission: "memory.import.preview", policyDecision: "blocked", blockedReason: "lockfile-entry-blocked" },
        { pluginId: "unknown-plugin", extensionPoint: "agent-tool", hostApi: "network-request", requestedPermission: "network.request", policyDecision: "blocked", blockedReason: "unknown-host-api" }
      ],
      auditEvent: {
        eventType: "permission-runtime-decision",
        requiredFields: ["runId", "pluginId", "hostApi", "requiredPermission", "policyDecision", "blockedReason", "sourceEvidence"]
      },
      blockedWhen: [
        "unknown-host-api",
        "permission-not-granted",
        "extension-point-not-allowed",
        "lockfile-entry-missing-or-blocked",
        "signature-untrusted",
        "release-checklist-not-passed",
        "kill-switch-disabled"
      ]
    },
    sandboxRunner: {
      runnerName: "phase21-plugin-sandbox-runner",
      implementationState: "model-only-no-third-party-code",
      timeoutMs: 3000,
      maxInputBytes: 262144,
      maxOutputBytes: 262144,
      killSwitch: true
    },
    executionAudit: {
      eventSchemaVersion: "phase21.plugin.runtime-audit.v1",
      requiredFields: ["runId", "pluginId", "extensionPoint", "decision", "durationMs", "inputSummary", "outputSummary", "blockedReason"],
      eventTypes: ["runtime-requested", "runtime-blocked", "runtime-timeout", "runtime-output-rejected", "runtime-completed"]
    },
    runtimeAuditReplay: {
      schemaVersion: "phase21.runtime-audit-replay.v1",
      status: "audit-replay-ready-third-party-disabled",
      replayMode: "audit-only-no-code-execution",
      requiredInputs: [
        "executionAudit.eventSchemaVersion",
        "permissionRuntimePolicy.auditEvent",
        "sampleRuntimeDecision",
        "phase20PlatformPlan.pluginReleaseChecklistGate",
        "phase20PlatformPlan.pluginDiffReviewHistory"
      ],
      replayWorkflow: [
        "load-runtime-audit-events",
        "sort-by-run-id-and-event-order",
        "verify-required-fields",
        "recompute-permission-decision",
        "compare-blocked-reason",
        "compare-release-gate-evidence",
        "emit-replay-summary"
      ],
      deterministicChecks: [
        "same-plugin-id",
        "same-extension-point",
        "same-host-api-policy",
        "same-lockfile-decision",
        "same-kill-switch-state",
        "same-blocked-reason"
      ],
      sampleReplay: {
        runId: "phase21-runtime-audit-replay-sample",
        pluginId: "third-party-placeholder",
        eventCount: 3,
        replayResult: "blocked-match",
        runtimeReady: false,
        evidence: ["kill-switch-disabled", "lockfile-entry-blocked", "release-checklist-blocked"]
      },
      blockedWhen: [
        "audit-event-missing",
        "required-field-missing",
        "permission-decision-mismatch",
        "blocked-reason-mismatch",
        "release-gate-evidence-missing",
        "runtime-enabled-without-replay"
      ],
      nextControls: ["runtime-replay-ui", "output-validation-gate", "importer-plugin-runtime-fixtures"]
    },
    outputValidationGate: {
      schemaVersion: "phase21.output-validation-gate.v1",
      status: "output-validation-ready-third-party-disabled",
      validationMode: "post-runtime-contract-model-only",
      requiredInputs: [
        "sandboxRunner.maxOutputBytes",
        "executionAudit.requiredFields",
        "permissionRuntimePolicy.hostApiPermissions",
        "runtimeAuditReplay.sampleReplay",
        "phase20PlatformPlan.extensionContractTests"
      ],
      acceptedOutputTypes: ["memory-draft", "redacted-export", "asset-template-preview", "audit-event"],
      validationWorkflow: [
        "parse-plugin-output-envelope",
        "verify-output-type-allowlist",
        "verify-output-byte-limit",
        "verify-contract-schema",
        "scan-sensitive-fields",
        "compare-audit-evidence",
        "emit-output-validation-event"
      ],
      contractChecks: [
        "output-type-allowed",
        "schema-version-present",
        "source-plugin-id-present",
        "no-secret-fields",
        "no-direct-filesystem-path",
        "audit-event-linked"
      ],
      sampleValidation: {
        pluginId: "third-party-placeholder",
        outputType: "memory-draft",
        result: "rejected",
        reason: "runtime-disabled-and-output-untrusted",
        runtimeReady: false,
        evidence: ["thirdPartyExecution=false", "runtimeAuditReplay:blocked-match", "schema-not-executed"]
      },
      blockedWhen: [
        "output-type-not-allowed",
        "output-too-large",
        "schema-validation-failed",
        "sensitive-field-leak",
        "audit-event-missing",
        "runtime-replay-not-matched",
        "runtime-disabled-and-output-untrusted"
      ],
      nextControls: ["output-validation-ui", "importer-plugin-runtime-fixtures", "runtime-result-quarantine"]
    },
    importerRuntimeFixtures: {
      schemaVersion: "phase21.importer-runtime-fixtures.v1",
      status: "importer-fixtures-ready-third-party-disabled",
      fixtureMode: "deterministic-fixture-only-no-code-execution",
      requiredInputs: [
        "permissionRuntimePolicy.hostApiPermissions",
        "runtimeAuditReplay.sampleReplay",
        "outputValidationGate.acceptedOutputTypes",
        "phase20PlatformPlan.templatePreviewFixtures"
      ],
      fixtureSets: [
        {
          id: "markdown-diary-importer-fixture",
          extensionPoint: "importer",
          inputType: "markdown",
          expectedOutputType: "memory-draft",
          expectedDecision: "blocked-until-runtime-enabled",
          evidence: ["read-redacted-input", "emit-draft-output", "outputValidationGate:memory-draft"]
        },
        {
          id: "csv-memory-importer-fixture",
          extensionPoint: "importer",
          inputType: "csv",
          expectedOutputType: "memory-draft",
          expectedDecision: "blocked-until-runtime-enabled",
          evidence: ["fixture-delimiter-detected", "schema-version-present", "audit-event-linked"]
        },
        {
          id: "oversized-importer-output-fixture",
          extensionPoint: "importer",
          inputType: "json",
          expectedOutputType: "memory-draft",
          expectedDecision: "reject-output",
          evidence: ["output-too-large", "runtime-disabled-and-output-untrusted"]
        }
      ],
      fixtureWorkflow: [
        "load-redacted-fixture-input",
        "attach-reviewed-manifest-snapshot",
        "simulate-host-api-permission-decision",
        "attach-expected-output-envelope",
        "run-output-validation-gate",
        "record-fixture-audit-result"
      ],
      blockedFixtures: [
        { id: "network-importer-fixture", reason: "unknown-host-api", hostApi: "network-request" },
        { id: "secret-field-output-fixture", reason: "sensitive-field-leak", outputType: "memory-draft" }
      ],
      sampleFixtureRun: {
        runId: "phase21-importer-fixture-sample",
        fixtureId: "markdown-diary-importer-fixture",
        result: "blocked-match",
        runtimeReady: false,
        outputValidation: "not-executed-runtime-disabled",
        evidence: ["kill-switch-disabled", "fixture-input-redacted", "audit-event-linked"]
      },
      blockedWhen: [
        "fixture-input-unredacted",
        "manifest-snapshot-missing",
        "expected-output-missing",
        "output-validation-gate-missing",
        "fixture-audit-event-missing",
        "runtime-enabled-for-fixture"
      ],
      nextControls: ["runtime-result-quarantine", "fixture-result-history", "importer-runtime-kill-switch-drill"]
    },
    runtimeResultQuarantine: {
      schemaVersion: "phase21.runtime-result-quarantine.v1",
      status: "quarantine-ready-third-party-disabled",
      quarantineMode: "model-only-block-untrusted-results",
      requiredInputs: [
        "importerRuntimeFixtures.sampleFixtureRun",
        "outputValidationGate.sampleValidation",
        "runtimeAuditReplay.sampleReplay",
        "permissionRuntimePolicy.auditEvent"
      ],
      quarantineStates: ["quarantined", "blocked", "needs-review", "released-fixture-only", "archived"],
      quarantineWorkflow: [
        "capture-runtime-result-envelope",
        "attach-fixture-run",
        "attach-output-validation-result",
        "attach-audit-replay-result",
        "derive-quarantine-state",
        "record-review-disposition",
        "export-quarantine-audit"
      ],
      sampleQuarantineItems: [
        {
          id: "quarantine-markdown-fixture-result",
          pluginId: "third-party-placeholder",
          fixtureId: "markdown-diary-importer-fixture",
          state: "quarantined",
          reason: "runtime-disabled-and-output-untrusted",
          releaseReady: false,
          evidence: ["outputValidationGate:rejected", "runtimeAuditReplay:blocked-match"]
        },
        {
          id: "quarantine-network-importer-fixture",
          pluginId: "unknown-plugin",
          fixtureId: "network-importer-fixture",
          state: "blocked",
          reason: "unknown-host-api",
          releaseReady: false,
          evidence: ["permissionRuntimePolicy:unknown-host-api", "hostApiBoundary:networkAccess=false"]
        }
      ],
      reviewerDispositions: ["keep-quarantined", "archive-with-evidence", "request-fixture-repair", "block-runtime-release"],
      blockedWhen: [
        "quarantine-item-missing",
        "review-disposition-missing",
        "release-ready-while-quarantined",
        "audit-evidence-missing",
        "fixture-result-not-linked",
        "output-validation-not-linked"
      ],
      summary: {
        totalItems: 2,
        quarantined: 1,
        blocked: 1,
        releaseReady: false
      },
      nextControls: ["fixture-result-history", "importer-runtime-kill-switch-drill", "quarantine-review-ui"]
    },
    preflightInputs: [
      "phase20PlatformPlan.pluginReleaseChecklistGate",
      "phase20PlatformPlan.pluginLockfile",
      "phase20PlatformPlan.signedManifestPolicy",
      "phase20PlatformPlan.extensionContractTests",
      "runtimeAuditReplay",
      "outputValidationGate",
      "importerRuntimeFixtures",
      "runtimeResultQuarantine"
    ],
    runtimePreflight: [
      "verify-release-checklist-gate",
      "verify-lockfile-entry",
      "verify-signed-manifest",
      "verify-permission-review",
      "verify-host-api-permission",
      "verify-extension-point-policy",
      "verify-contract-suite",
      "verify-kill-switch",
      "verify-runtime-audit-replay",
      "verify-runtime-output-validation",
      "verify-importer-runtime-fixtures",
      "verify-runtime-result-quarantine",
      "record-runtime-audit"
    ],
    sampleRuntimeDecision: {
      version: "1.1.5",
      buildLabel: "phase21-runtime-result-quarantine",
      pluginId: "third-party-placeholder",
      extensionPoint: "importer",
      decision: "blocked",
      runtimeReady: false,
      blockedReason: "kill-switch-disabled"
    },
    summary: {
      memoryCount: memories.length,
      hostApis: 4,
      permissionRules: 4,
      allowedExtensionPoints: 3,
      blockedExtensionPoints: 2,
      auditReplayReady: true,
      outputValidationReady: true,
      importerFixturesReady: true,
      quarantineReady: true,
      runtimeReady: false,
      thirdPartyExecution: false
    },
    blockedWhen: [
      "release-checklist-not-passed",
      "lockfile-entry-missing",
      "signature-untrusted",
      "permission-review-missing",
      "host-api-permission-denied",
      "unknown-host-api",
      "contract-test-failed",
      "kill-switch-disabled",
      "runtime-audit-missing",
      "audit-replay-mismatch",
      "output-validation-failed",
      "importer-fixture-mismatch",
      "quarantine-review-missing"
    ],
    nextControls: ["fixture-result-history", "importer-runtime-kill-switch-drill", "quarantine-review-ui"]
  };
}

function buildLocalPhase22RuntimeEvidenceReview() {
  const runtimePlan = buildLocalPhase21RuntimeSandboxPlan();
  return {
    phase: 22,
    phaseName: "\u8fd0\u884c\u65f6\u8bc1\u636e\u590d\u6838\u548c\u9694\u79bb\u5904\u7f6e\u7248",
    schemaVersion: "phase22.runtime-evidence-review.v1",
    status: "evidence-review-ready-third-party-disabled",
    version: "1.2.7",
    buildLabel: "phase22-runtime-review-closure",
    runtimeExecution: false,
    thirdPartyExecution: false,
    reviewMode: "human-reviewed-evidence-only",
    evidenceSources: [
      "phase21RuntimeSandboxPlan.runtimeResultQuarantine",
      "phase21RuntimeSandboxPlan.importerRuntimeFixtures",
      "phase21RuntimeSandboxPlan.outputValidationGate",
      "phase21RuntimeSandboxPlan.runtimeAuditReplay",
      "phase20PlatformPlan.pluginReleaseChecklistGate"
    ],
    evidenceBundle: {
      schemaVersion: "phase22.runtime-evidence-bundle.v1",
      requiredSections: [
        "quarantine-items",
        "fixture-runs",
        "output-validation-results",
        "audit-replay-summary",
        "release-gate-decision",
        "reviewer-disposition"
      ],
      sourceSnapshot: {
        quarantineSchema: runtimePlan.runtimeResultQuarantine.schemaVersion,
        fixtureSchema: runtimePlan.importerRuntimeFixtures.schemaVersion,
        outputValidationSchema: runtimePlan.outputValidationGate.schemaVersion,
        auditReplaySchema: runtimePlan.runtimeAuditReplay.schemaVersion
      },
      sampleBundle: {
        bundleId: "phase22-review-signoff-ledger-sample",
        quarantineItemIds: runtimePlan.runtimeResultQuarantine.sampleQuarantineItems.map((item) => item.id),
        fixtureRunId: runtimePlan.importerRuntimeFixtures.sampleFixtureRun.runId,
        replayRunId: runtimePlan.runtimeAuditReplay.sampleReplay.runId,
        validationResult: runtimePlan.outputValidationGate.sampleValidation.result,
        releaseReady: false
      }
    },
    reviewWorkbench: {
      schemaVersion: "phase22.runtime-review-workbench.v1",
      status: "review-workbench-ready-third-party-disabled",
      queueMode: "read-only-disposition-model",
      filters: ["state", "disposition", "blocker", "pluginId", "fixtureId", "evidenceSource"],
      columns: ["itemId", "pluginId", "fixtureId", "state", "suggestedDisposition", "primaryBlocker", "evidenceCount"],
      queueItems: runtimePlan.runtimeResultQuarantine.sampleQuarantineItems.map((item) => ({
        id: `review-${item.id}`,
        quarantineItemId: item.id,
        pluginId: item.pluginId,
        fixtureId: item.fixtureId,
        state: item.state,
        priority: item.state === "blocked" ? "blocker" : "review",
        suggestedDisposition: item.state === "blocked" ? "block-runtime-release" : "keep-quarantined",
        primaryBlocker: item.reason,
        evidenceBundleId: "phase22-review-signoff-ledger-sample",
        evidenceRefs: item.evidence,
        availableActions: ["open-evidence-bundle", "record-review-disposition", "export-review-audit"]
      })),
      detailPanels: [
        "quarantine-summary",
        "fixture-run",
        "output-validation",
        "audit-replay",
        "release-gate",
        "reviewer-disposition"
      ],
      bulkActions: [
        "export-filtered-evidence",
        "mark-needs-review",
        "block-runtime-release"
      ],
      emptyState: "no-runtime-results-ready-for-release",
      summary: {
        queueItems: runtimePlan.runtimeResultQuarantine.summary.totalItems,
        blockerItems: runtimePlan.runtimeResultQuarantine.summary.blocked,
        needsReview: runtimePlan.runtimeResultQuarantine.summary.quarantined,
        releaseReady: false,
        runtimeReady: false
      }
    },
    signoffLedger: {
      schemaVersion: "phase22.review-signoff-ledger.v1",
      status: "signoff-ledger-ready-third-party-disabled",
      ledgerMode: "append-only-model-no-runtime-release",
      requiredFields: [
        "signoffId",
        "reviewId",
        "quarantineItemId",
        "reviewer",
        "disposition",
        "reason",
        "evidenceBundleId",
        "evidenceSnapshot",
        "signedAt",
        "releaseReady"
      ],
      entries: runtimePlan.runtimeResultQuarantine.sampleQuarantineItems.map((item, index) => ({
        signoffId: `phase22-signoff-${index + 1}`,
        reviewId: `review-${item.id}`,
        quarantineItemId: item.id,
        reviewer: "local-owner",
        disposition: item.state === "blocked" ? "block-runtime-release" : "keep-quarantined",
        reason: item.reason,
        evidenceBundleId: "phase22-review-signoff-ledger-sample",
        evidenceSnapshot: {
          quarantineState: item.state,
          evidenceRefs: item.evidence,
          runtimeExecution: false,
          thirdPartyExecution: false
        },
        signedAt: "2026-06-26T00:00:00.000Z",
        releaseReady: false
      })),
      evidenceSnapshotPolicy: {
        snapshotTiming: "before-disposition-write",
        immutableFields: ["quarantineItemId", "pluginId", "fixtureId", "evidenceRefs", "releaseReady"],
        redaction: "plugin-results-redacted-fixture-only"
      },
      chainIntegrity: {
        requiredChecks: [
          "previous-signoff-linked",
          "evidence-bundle-present",
          "reviewer-present",
          "disposition-allowed",
          "release-ready-false-while-blocked"
        ],
        blockedWhen: [
          "signoff-entry-missing",
          "evidence-snapshot-missing",
          "reviewer-missing",
          "disposition-not-allowed",
          "release-ready-true-with-blocker"
        ]
      },
      summary: {
        totalSignoffs: runtimePlan.runtimeResultQuarantine.summary.totalItems,
        blockedSignoffs: runtimePlan.runtimeResultQuarantine.summary.blocked,
        keepQuarantined: runtimePlan.runtimeResultQuarantine.summary.quarantined,
        releaseReady: false,
        runtimeReady: false
      }
    },
    reviewerWorkflow: [
      "load-evidence-bundle",
      "open-review-workbench",
      "verify-source-schemas",
      "compare-quarantine-items-to-fixture-runs",
      "compare-output-validation-to-audit-replay",
      "record-reviewer-disposition",
      "append-signoff-ledger-entry",
      "derive-release-blocker-rules",
      "plan-blocker-clearance",
      "derive-release-readiness-decision",
      "record-clearance-audit-trail",
      "close-phase22-runtime-review",
      "derive-release-blockers",
      "export-review-audit"
    ],
    reviewerDispositions: [
      "keep-quarantined",
      "archive-with-evidence",
      "request-fixture-repair",
      "request-output-contract-repair",
      "block-runtime-release"
    ],
    dispositionPolicy: {
      defaultDisposition: "keep-quarantined",
      releaseReadyRequires: [
        "no-quarantined-items",
        "no-blocked-fixtures",
        "output-validation-accepted",
        "audit-replay-matched",
        "release-checklist-passed",
        "human-review-signed"
      ],
      blockedWhen: [
        "review-disposition-missing",
        "evidence-bundle-incomplete",
        "quarantine-state-unresolved",
        "fixture-result-missing",
        "output-validation-rejected",
        "audit-replay-mismatch",
        "release-gate-blocked",
        "runtime-enabled-during-review"
      ]
    },
    sampleReview: {
      reviewId: "phase22-runtime-review-sample",
      signoffId: "phase22-signoff-1",
      reviewer: "local-owner",
      disposition: "block-runtime-release",
      releaseReady: false,
      runtimeReady: false,
      thirdPartyExecution: false,
      blockers: [
        "runtime-disabled-and-output-untrusted",
        "unknown-host-api",
        "release-gate-blocked"
      ],
      evidence: [
        "phase21.runtime-result-quarantine.v1",
        "phase21.importer-runtime-fixtures.v1",
        "phase21.output-validation-gate.v1",
        "phase21.runtime-audit-replay.v1"
      ]
    },
    auditExport: {
      schemaVersion: "phase22.runtime-review-audit-export.v1",
      exportName: "phase22-review-signoff-ledger.audit.json",
      includes: [
        "evidenceBundle",
        "reviewerWorkflow",
        "signoffLedger",
        "sampleReview",
        "releaseBlockerRules",
        "releaseBlockerClearancePlan",
        "releaseReadinessDecision",
        "releaseClearanceAuditTrail",
        "phase22RuntimeReviewClosure",
        "releaseBlockers",
        "runtimeDisabledProof"
      ],
      redaction: "plugin-results-redacted-fixture-only"
    },
    releaseBlockerRules: {
      schemaVersion: "phase22.release-blocker-rules.v1",
      status: "release-blocker-rules-ready-third-party-disabled",
      ruleMode: "explainable-release-blocking-model",
      severityLevels: ["hold", "block"],
      rules: [
        {
          id: "runtime-disabled-and-output-untrusted",
          severity: "block",
          source: "phase21RuntimeSandboxPlan.outputValidationGate",
          condition: "outputValidationGate.sampleValidation.result=rejected",
          releaseAction: "block-runtime-release",
          clearRequires: ["runtime-remains-disabled", "output-validation-accepted", "human-review-signed"],
          evidenceRefs: ["phase21.output-validation-gate.v1", "phase22.review-signoff-ledger.v1"]
        },
        {
          id: "unknown-host-api",
          severity: "block",
          source: "phase21RuntimeSandboxPlan.permissionRuntimePolicy",
          condition: "quarantine item reason is unknown-host-api",
          releaseAction: "block-runtime-release",
          clearRequires: ["host-api-allowlist-reviewed", "permission-policy-updated", "audit-replay-matched"],
          evidenceRefs: ["phase21.permission.runtime-enforcement.v1"]
        },
        {
          id: "release-gate-blocked",
          severity: "block",
          source: "phase20PlatformPlan.pluginReleaseChecklistGate",
          condition: "release checklist gate result remains block",
          releaseAction: "block-runtime-release",
          clearRequires: ["release-checklist-passed", "ci-badge-not-blocked", "download-integrity-matched"],
          evidenceRefs: ["phase20.plugin.release-checklist-gate.v1"]
        },
        {
          id: "human-review-signed-missing",
          severity: "hold",
          source: "phase22RuntimeEvidenceReview.signoffLedger",
          condition: "required signoff entry missing or unsigned",
          releaseAction: "hold-release-review",
          clearRequires: ["signoff-entry-present", "reviewer-present", "evidence-snapshot-present"],
          evidenceRefs: ["phase22.review-signoff-ledger.v1"]
        }
      ],
      releaseReadiness: {
        releaseReady: false,
        hold: 1,
        block: 3,
        requiredBeforeRelease: [
          "all-block-rules-cleared",
          "all-hold-rules-reviewed",
          "signoff-ledger-complete",
          "runtime-disabled-proof-exported"
        ]
      },
      blockedWhen: [
        "block-rule-active",
        "hold-rule-without-reviewer",
        "clear-requirement-missing",
        "runtime-enabled-while-blocked"
      ]
    },
    releaseBlockerClearancePlan: {
      schemaVersion: "phase22.blocker-clearance-plan.v1",
      status: "clearance-plan-ready-third-party-disabled",
      planMode: "human-owned-clearance-no-runtime-release",
      ownerRoles: ["local-owner", "runtime-reviewer", "release-reviewer"],
      clearanceItems: [
        {
          blockerId: "runtime-disabled-and-output-untrusted",
          owner: "runtime-reviewer",
          requiredEvidence: ["output-validation-accepted", "runtime-disabled-proof", "signoff-ledger-entry"],
          verificationSteps: ["rerun-output-validation-fixture", "compare-audit-replay", "confirm-runtimeExecution-false"],
          clearDecision: "keep-blocked-until-output-accepted"
        },
        {
          blockerId: "unknown-host-api",
          owner: "runtime-reviewer",
          requiredEvidence: ["host-api-allowlist-review", "permission-policy-change-record", "audit-replay-matched"],
          verificationSteps: ["review-host-api-request", "deny-unlisted-host-api", "attach-permission-runtime-decision"],
          clearDecision: "keep-blocked-until-host-api-reviewed"
        },
        {
          blockerId: "release-gate-blocked",
          owner: "release-reviewer",
          requiredEvidence: ["release-checklist-pass", "ci-badge-not-blocked", "download-integrity-match"],
          verificationSteps: ["rerun-phase20-pre-release", "compare-signed-report", "export-release-gate-audit"],
          clearDecision: "keep-blocked-until-release-gate-passes"
        },
        {
          blockerId: "human-review-signed-missing",
          owner: "local-owner",
          requiredEvidence: ["reviewer-present", "evidence-snapshot-present", "signed-signoff-entry"],
          verificationSteps: ["append-signoff-ledger-entry", "verify-chain-integrity", "export-review-audit"],
          clearDecision: "hold-until-signoff-complete"
        }
      ],
      clearanceWorkflow: [
        "assign-clearance-owner",
        "collect-required-evidence",
        "run-verification-steps",
        "append-clearance-note",
        "recompute-release-readiness"
      ],
      blockedWhen: [
        "owner-missing",
        "required-evidence-missing",
        "verification-step-failed",
        "releaseReady-true-before-clearance"
      ],
      summary: {
        totalItems: 4,
        blockItems: 3,
        holdItems: 1,
        releaseReady: false,
        runtimeReady: false
      }
    },
    releaseReadinessDecision: {
      schemaVersion: "phase22.release-readiness-decision.v1",
      status: "release-decision-blocked-third-party-disabled",
      decisionMode: "derived-from-blocker-rules-and-clearance-plan",
      decision: "block-release",
      releaseReady: false,
      runtimeReady: false,
      thirdPartyExecution: false,
      inputs: ["releaseBlockerRules", "releaseBlockerClearancePlan", "signoffLedger", "reviewWorkbench"],
      activeBlockers: [
        {
          id: "runtime-disabled-and-output-untrusted",
          severity: "block",
          clearanceOwner: "runtime-reviewer",
          reason: "output validation remains rejected while runtime output is untrusted",
          missingBeforeRelease: ["output-validation-accepted", "runtime-disabled-proof", "human-review-signed"]
        },
        {
          id: "unknown-host-api",
          severity: "block",
          clearanceOwner: "runtime-reviewer",
          reason: "host API request is not allowlisted or reviewed",
          missingBeforeRelease: ["host-api-allowlist-reviewed", "permission-policy-updated", "audit-replay-matched"]
        },
        {
          id: "release-gate-blocked",
          severity: "block",
          clearanceOwner: "release-reviewer",
          reason: "phase 20 release checklist gate still returns block",
          missingBeforeRelease: ["release-checklist-passed", "ci-badge-not-blocked", "download-integrity-matched"]
        },
        {
          id: "human-review-signed-missing",
          severity: "hold",
          clearanceOwner: "local-owner",
          reason: "review signoff must stay complete before any release decision can change",
          missingBeforeRelease: ["signoff-entry-present", "reviewer-present", "evidence-snapshot-present"]
        }
      ],
      gates: [
        { id: "runtime-execution-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
        { id: "block-rules-cleared", result: "block", evidence: "3 block rules remain active" },
        { id: "hold-rules-reviewed", result: "hold", evidence: "1 hold rule requires reviewer confirmation" },
        { id: "clearance-plan-complete", result: "block", evidence: "clearance verification steps are pending" },
        { id: "signoff-ledger-complete", result: "hold", evidence: "signoff ledger must remain complete before releaseReady can change" }
      ],
      recomputePolicy: {
        triggerOn: [
          "clearance-item-updated",
          "signoff-ledger-appended",
          "release-gate-rerun",
          "output-validation-rerun"
        ],
        requires: [
          "runtimeExecution=false",
          "thirdPartyExecution=false",
          "all-block-rules-cleared",
          "all-clearance-verification-passed",
          "signoff-ledger-complete"
        ],
        blockedWhen: [
          "input-snapshot-missing",
          "clearance-owner-missing",
          "verification-step-failed",
          "releaseReady-true-with-active-blocker"
        ]
      },
      summary: {
        decision: "block-release",
        pass: 1,
        hold: 2,
        block: 2,
        releaseReady: false,
        runtimeReady: false
      }
    },
    releaseClearanceAuditTrail: {
      schemaVersion: "phase22.clearance-audit-trail.v1",
      status: "clearance-audit-ready-third-party-disabled",
      auditMode: "append-only-clearance-attempts",
      requiredFields: [
        "auditId",
        "blockerId",
        "attemptedBy",
        "attemptedAt",
        "evidenceRefs",
        "verificationResults",
        "outcome",
        "decisionRef",
        "releaseReady"
      ],
      entries: [
        {
          auditId: "phase22-clearance-audit-1",
          blockerId: "runtime-disabled-and-output-untrusted",
          attemptedBy: "runtime-reviewer",
          attemptedAt: "2026-06-26T00:00:00.000Z",
          evidenceRefs: ["phase21.output-validation-gate.v1", "phase22.release-readiness-decision.v1"],
          verificationResults: [
            { step: "rerun-output-validation-fixture", result: "failed", note: "sample validation remains rejected" },
            { step: "confirm-runtimeExecution-false", result: "passed", note: "runtime remains disabled" }
          ],
          outcome: "block-kept",
          decisionRef: "phase22.release-readiness-decision.v1",
          releaseReady: false
        },
        {
          auditId: "phase22-clearance-audit-2",
          blockerId: "release-gate-blocked",
          attemptedBy: "release-reviewer",
          attemptedAt: "2026-06-26T00:00:00.000Z",
          evidenceRefs: ["phase20.plugin.release-checklist-gate.v1", "phase22.blocker-clearance-plan.v1"],
          verificationResults: [
            { step: "rerun-phase20-pre-release", result: "failed", note: "release checklist gate remains block" },
            { step: "export-release-gate-audit", result: "passed", note: "release gate evidence exported" }
          ],
          outcome: "block-kept",
          decisionRef: "phase22.release-readiness-decision.v1",
          releaseReady: false
        },
        {
          auditId: "phase22-clearance-audit-3",
          blockerId: "human-review-signed-missing",
          attemptedBy: "local-owner",
          attemptedAt: "2026-06-26T00:00:00.000Z",
          evidenceRefs: ["phase22.review-signoff-ledger.v1"],
          verificationResults: [
            { step: "verify-chain-integrity", result: "passed", note: "existing sample signoff chain remains intact" },
            { step: "append-clearance-note", result: "pending", note: "hold remains until reviewer confirms clearance note" }
          ],
          outcome: "hold-kept",
          decisionRef: "phase22.release-readiness-decision.v1",
          releaseReady: false
        }
      ],
      chainIntegrity: {
        requiredChecks: [
          "audit-entry-linked-to-blocker",
          "decision-ref-present",
          "verification-result-present",
          "release-ready-false-while-blocked"
        ],
        blockedWhen: [
          "audit-entry-missing",
          "decision-ref-missing",
          "verification-result-missing",
          "releaseReady-true-with-failed-clearance"
        ]
      },
      exportPolicy: {
        exportName: "phase22-clearance-audit-trail.json",
        includeInRuntimeReviewAudit: true,
        redaction: "plugin-results-redacted-fixture-only"
      },
      summary: {
        totalEntries: 3,
        blockKept: 2,
        holdKept: 1,
        cleared: 0,
        releaseReady: false,
        runtimeReady: false
      }
    },
    phase22RuntimeReviewClosure: {
      schemaVersion: "phase22.runtime-review-closure.v1",
      status: "phase22-closed-next-phase-ready-third-party-disabled",
      closureMode: "model-and-audit-closure-no-runtime-release",
      phase22Complete: true,
      releaseReady: false,
      runtimeReady: false,
      nextPhaseEntryReady: true,
      runtimeExecution: false,
      thirdPartyExecution: false,
      closedSections: [
        "evidenceBundle",
        "reviewWorkbench",
        "signoffLedger",
        "releaseBlockerRules",
        "releaseBlockerClearancePlan",
        "releaseReadinessDecision",
        "releaseClearanceAuditTrail"
      ],
      blockingSummary: {
        activeBlockers: 4,
        block: 3,
        hold: 1,
        decision: "block-release",
        releaseReady: false
      },
      closureArtifacts: [
        "phase22.runtime-evidence-bundle.v1",
        "phase22.review-signoff-ledger.v1",
        "phase22.release-blocker-rules.v1",
        "phase22.blocker-clearance-plan.v1",
        "phase22.release-readiness-decision.v1",
        "phase22.clearance-audit-trail.v1"
      ],
      nextPhaseEntryConditions: [
        "runtimeExecution=false",
        "thirdPartyExecution=false",
        "releaseReady=false",
        "active-blockers-preserved",
        "clearance-audit-exported",
        "decision-history-preserved"
      ],
      nextPhaseAllowedWork: [
        "release-readiness-review-ui",
        "clearance-audit-search",
        "decision-history-export",
        "runtime-sandbox-ui-planning"
      ],
      blockedWhen: [
        "runtime-enabled-before-next-phase",
        "third-party-execution-enabled",
        "releaseReady-true-with-active-blocker",
        "closure-artifact-missing"
      ],
      summary: {
        phase22Complete: true,
        nextPhaseEntryReady: true,
        releaseReady: false,
        runtimeReady: false,
        thirdPartyExecution: false
      }
    },
    releaseBlockers: [
      "runtime-disabled-and-output-untrusted",
      "unknown-host-api",
      "release-gate-blocked",
      "human-review-signed-missing"
    ],
    summary: {
      memoryCount: memories.length,
      evidenceSources: 5,
      quarantineItems: runtimePlan.runtimeResultQuarantine.summary.totalItems,
      blockedItems: runtimePlan.runtimeResultQuarantine.summary.blocked,
      releaseReady: false,
      runtimeReady: false,
      thirdPartyExecution: false
    },
    nextControls: ["enter-next-phase", "release-readiness-review-ui", "clearance-audit-search"]
  };
}

function buildLocalPhase23ReleaseReadinessReviewUi() {
  const phase22Review = buildLocalPhase22RuntimeEvidenceReview();
  const closure = phase22Review.phase22RuntimeReviewClosure;
  return {
    phase: 23,
    phaseName: "\u53d1\u5e03\u5c31\u7eea\u590d\u6838\u4e0e\u4e0b\u4e00\u9636\u6bb5\u63a7\u5236\u53f0\u7248",
    schemaVersion: "phase23.release-readiness-review-ui.v1",
    status: "review-ui-model-ready-runtime-disabled",
    version: "1.3.9",
    buildLabel: "phase23-closure-handoff-package",
    runtimeExecution: false,
    thirdPartyExecution: false,
    releaseReady: false,
    sourceClosure: {
      schemaVersion: closure.schemaVersion,
      phase22Complete: closure.phase22Complete,
      nextPhaseEntryReady: closure.nextPhaseEntryReady,
      releaseReady: closure.releaseReady
    },
    dashboardCards: [
      { id: "release-decision", label: "release decision", value: phase22Review.releaseReadinessDecision.decision, severity: "block" },
      { id: "active-blockers", label: "active blockers", value: closure.blockingSummary.activeBlockers, severity: "block" },
      { id: "runtime-execution", label: "runtime execution", value: "disabled", severity: "pass" },
      { id: "next-phase-entry", label: "next phase entry", value: "ready-under-guards", severity: "pass" }
    ],
    reviewQueues: [
      {
        id: "blockers",
        title: "active release blockers",
        source: "phase22RuntimeEvidenceReview.releaseReadinessDecision.activeBlockers",
        itemCount: phase22Review.releaseReadinessDecision.activeBlockers.length,
        defaultFilter: "severity:block-or-hold",
        emptyState: "no-active-blockers"
      },
      {
        id: "clearance-audit",
        title: "clearance audit attempts",
        source: "phase22RuntimeEvidenceReview.releaseClearanceAuditTrail.entries",
        itemCount: phase22Review.releaseClearanceAuditTrail.entries.length,
        defaultFilter: "outcome:block-kept-or-hold-kept",
        emptyState: "no-clearance-attempts"
      }
    ],
    clearanceAuditSearch: {
      schemaVersion: "phase23.clearance-audit-search.v1",
      status: "search-model-ready-runtime-disabled",
      source: "phase22RuntimeEvidenceReview.releaseClearanceAuditTrail.entries",
      queryFields: ["auditId", "blockerId", "attemptedBy", "outcome", "decisionRef", "verificationResults.result"],
      indexedFields: ["blockerId", "attemptedBy", "outcome", "decisionRef", "releaseReady"],
      resultColumns: ["auditId", "blockerId", "attemptedBy", "outcome", "failedSteps", "decisionRef", "releaseReady"],
      filterPresets: [
        { id: "failed-clearance", label: "failed clearance", query: { outcome: "block-kept", verificationResult: "failed" } },
        { id: "hold-kept", label: "hold kept", query: { outcome: "hold-kept" } },
        { id: "release-gate", label: "release gate", query: { blockerId: "release-gate-blocked" } },
        { id: "runtime-reviewer", label: "runtime reviewer", query: { attemptedBy: "runtime-reviewer" } }
      ],
      sampleQuery: {
        text: "release-gate failed",
        filters: { blockerId: "release-gate-blocked", outcome: "block-kept" }
      },
      sampleResults: phase22Review.releaseClearanceAuditTrail.entries.map((entry) => ({
        auditId: entry.auditId,
        blockerId: entry.blockerId,
        attemptedBy: entry.attemptedBy,
        outcome: entry.outcome,
        failedSteps: entry.verificationResults.filter((item) => item.result === "failed").map((item) => item.step),
        decisionRef: entry.decisionRef,
        releaseReady: entry.releaseReady
      })),
      exportPolicy: {
        exportName: "phase23-clearance-audit-search-results.json",
        redaction: "plugin-results-redacted-fixture-only",
        includesQuery: true,
        includesResultRows: true
      },
      guardrails: {
        blockedWhen: [
          "search-result-missing-decision-ref",
          "releaseReady-true-in-search-result",
          "runtime-enabled-during-search"
        ]
      },
      summary: {
        searchableEntries: phase22Review.releaseClearanceAuditTrail.entries.length,
        failedClearance: phase22Review.releaseClearanceAuditTrail.entries.filter((entry) => entry.verificationResults.some((item) => item.result === "failed")).length,
        releaseReady: false,
        runtimeReady: false
      }
    },
    decisionHistoryExport: {
      schemaVersion: "phase23.decision-history-export.v1",
      status: "decision-history-export-ready-runtime-disabled",
      exportName: "phase23-release-decision-history.json",
      sourceSections: [
        "phase22RuntimeEvidenceReview.releaseReadinessDecision",
        "phase22RuntimeEvidenceReview.releaseClearanceAuditTrail",
        "phase22RuntimeEvidenceReview.phase22RuntimeReviewClosure",
        "phase23ReleaseReadinessReviewUi.clearanceAuditSearch"
      ],
      includedFields: [
        "decision",
        "activeBlockers",
        "gates",
        "auditEntries",
        "searchQuery",
        "guardrailState",
        "releaseReady"
      ],
      timeline: [
        {
          id: "decision-derived",
          source: "phase22.release-readiness-decision.v1",
          eventType: "release-decision",
          result: phase22Review.releaseReadinessDecision.decision,
          releaseReady: false
        },
        {
          id: "clearance-audit-recorded",
          source: "phase22.clearance-audit-trail.v1",
          eventType: "clearance-audit",
          result: "blockers-preserved",
          releaseReady: false
        },
        {
          id: "phase22-closed",
          source: closure.schemaVersion,
          eventType: "phase-closure",
          result: "next-phase-ready-under-guards",
          releaseReady: false
        }
      ],
      exportGuards: [
        "runtimeExecution=false",
        "thirdPartyExecution=false",
        "releaseReady=false",
        "active-blockers-included",
        "decision-ref-present"
      ],
      blockedWhen: [
        "decision-history-missing",
        "active-blockers-omitted",
        "releaseReady-true-in-export",
        "runtime-enabled-during-export"
      ],
      summary: {
        timelineEvents: 3,
        activeBlockers: closure.blockingSummary.activeBlockers,
        releaseReady: false,
        runtimeReady: false
      }
    },
    blockerDetailPanels: {
      schemaVersion: "phase23.blocker-detail-panels.v1",
      status: "blocker-detail-panels-ready-runtime-disabled",
      source: "phase22RuntimeEvidenceReview.releaseReadinessDecision.activeBlockers",
      panels: phase22Review.releaseReadinessDecision.activeBlockers.map((blocker) => {
        const rule = phase22Review.releaseBlockerRules.rules.find((item) => item.id === blocker.id);
        const audits = phase22Review.releaseClearanceAuditTrail.entries.filter((entry) => entry.blockerId === blocker.id);
        return {
          blockerId: blocker.id,
          severity: blocker.severity,
          clearanceOwner: blocker.clearanceOwner,
          reason: blocker.reason,
          missingBeforeRelease: blocker.missingBeforeRelease,
          clearRequires: rule?.clearRequires || [],
          linkedAuditIds: audits.map((entry) => entry.auditId),
          auditOutcomes: audits.map((entry) => entry.outcome),
          gateRefs: phase22Review.releaseReadinessDecision.gates.filter((gate) => gate.result === blocker.severity || gate.result === "block").map((gate) => gate.id),
          allowedActions: ["open-clearance-audit-entry", "export-decision-history", "copy-blocker-evidence"],
          blockedActions: ["mark-blocker-cleared", "mark-release-ready", "enable-runtime-execution"]
        };
      }),
      panelFields: [
        "blockerId",
        "severity",
        "clearanceOwner",
        "reason",
        "missingBeforeRelease",
        "clearRequires",
        "linkedAuditIds",
        "auditOutcomes",
        "gateRefs"
      ],
      guardrails: {
        blockedWhen: [
          "blocker-panel-missing-clearance-owner",
          "blocker-panel-missing-audit-link",
          "mark-release-ready-from-detail",
          "runtime-enabled-from-detail"
        ]
      },
      summary: {
        panels: phase22Review.releaseReadinessDecision.activeBlockers.length,
        panelsWithAuditLinks: phase22Review.releaseReadinessDecision.activeBlockers.filter((blocker) => phase22Review.releaseClearanceAuditTrail.entries.some((entry) => entry.blockerId === blocker.id)).length,
        releaseReady: false,
        runtimeReady: false
      }
    },
    reviewActionLedger: {
      schemaVersion: "phase23.review-action-ledger.v1",
      status: "review-action-ledger-ready-runtime-disabled",
      ledgerMode: "append-only-review-actions-no-release-mutation",
      sourcePanels: "phase23ReleaseReadinessReviewUi.blockerDetailPanels",
      allowedActionTypes: [
        "filter-release-readiness",
        "open-blocker-detail",
        "open-clearance-audit-entry",
        "export-decision-history",
        "copy-blocker-evidence",
        "enter-next-phase-planning"
      ],
      blockedActionTypes: [
        "mark-blocker-cleared",
        "mark-release-ready",
        "enable-runtime-execution",
        "enable-third-party-execution"
      ],
      requiredFields: [
        "actionId",
        "actionType",
        "actor",
        "targetType",
        "targetId",
        "sourceSchema",
        "recordedAt",
        "releaseReady"
      ],
      entries: [
        {
          actionId: "phase23-action-1",
          actionType: "filter-release-readiness",
          actor: "release-reviewer",
          targetType: "filterPreset",
          targetId: "show-blockers",
          sourceSchema: "phase23.release-readiness-review-ui.v1",
          recordedAt: "2026-06-26T00:00:00.000Z",
          releaseReady: false
        },
        {
          actionId: "phase23-action-2",
          actionType: "open-blocker-detail",
          actor: "runtime-reviewer",
          targetType: "blocker",
          targetId: "release-gate-blocked",
          sourceSchema: "phase23.blocker-detail-panels.v1",
          recordedAt: "2026-06-26T00:00:00.000Z",
          releaseReady: false
        },
        {
          actionId: "phase23-action-3",
          actionType: "export-decision-history",
          actor: "release-reviewer",
          targetType: "export",
          targetId: "phase23-release-decision-history.json",
          sourceSchema: "phase23.decision-history-export.v1",
          recordedAt: "2026-06-26T00:00:00.000Z",
          releaseReady: false
        }
      ],
      chainIntegrity: {
        requiredChecks: [
          "action-type-allowed",
          "target-reference-present",
          "source-schema-present",
          "release-ready-false",
          "runtime-disabled"
        ],
        blockedWhen: [
          "blocked-action-recorded-as-allowed",
          "target-reference-missing",
          "source-schema-missing",
          "releaseReady-true-in-action-ledger",
          "runtime-enabled-during-action-record"
        ]
      },
      exportPolicy: {
        exportName: "phase23-review-action-ledger.json",
        includeInDecisionHistory: true,
        redaction: "reviewer-id-only"
      },
      summary: {
        entries: 3,
        blockedActionTypes: 4,
        releaseReady: false,
        runtimeReady: false
      }
    },
    nextPhaseGuardrailDashboard: {
      schemaVersion: "phase23.next-phase-guardrail-dashboard.v1",
      status: "next-phase-ready-release-blocked-runtime-disabled",
      dashboardMode: "separate-next-phase-entry-from-release-readiness",
      guardrailCards: [
        { id: "phase22-complete", label: "phase 22 complete", result: closure.phase22Complete ? "pass" : "block", evidence: closure.schemaVersion },
        { id: "next-phase-entry", label: "next phase entry", result: closure.nextPhaseEntryReady ? "pass" : "block", evidence: "phase22RuntimeReviewClosure.nextPhaseEntryReady" },
        { id: "release-ready", label: "release ready", result: "block", evidence: "active blockers preserved" },
        { id: "runtime-execution", label: "runtime execution", result: "pass", evidence: "runtimeExecution=false" },
        { id: "third-party-execution", label: "third-party execution", result: "pass", evidence: "thirdPartyExecution=false" }
      ],
      entryConditions: closure.nextPhaseEntryConditions,
      releaseBlockers: phase22Review.releaseReadinessDecision.activeBlockers.map((blocker) => ({
        blockerId: blocker.id,
        severity: blocker.severity,
        owner: blocker.clearanceOwner,
        reason: blocker.reason
      })),
      allowedNextPhaseWork: closure.nextPhaseAllowedWork,
      blockedReleaseActions: [
        "mark-release-ready",
        "enable-runtime-execution",
        "enable-third-party-execution",
        "ship-third-party-plugin-runtime",
        "clear-blockers-without-audit"
      ],
      recommendedNextVersion: {
        label: "phase23-runtime-sandbox-ui-plan",
        reason: "next phase planning can continue while release remains blocked"
      },
      guardrails: {
        blockedWhen: [
          "next-phase-entry-without-phase22-closure",
          "releaseReady-true-with-active-blocker",
          "runtime-enabled-before-release-clearance",
          "third-party-execution-enabled-before-release-clearance"
        ]
      },
      summary: {
        pass: 4,
        block: 1,
        nextPhaseEntryReady: true,
        releaseReady: false,
        runtimeReady: false,
        thirdPartyExecution: false
      }
    },
    runtimeSandboxUiPlan: {
      schemaVersion: "phase23.runtime-sandbox-ui-plan.v1",
      status: "runtime-sandbox-ui-plan-ready-runtime-disabled",
      planMode: "ui-planning-only-no-code-execution",
      sourceDashboard: "phase23ReleaseReadinessReviewUi.nextPhaseGuardrailDashboard",
      uiSections: [
        { id: "sandbox-preflight", label: "sandbox preflight", source: "phase21RuntimeSandboxPlan.runtimePreflight", result: "plan-only" },
        { id: "permission-decisions", label: "permission decisions", source: "phase21RuntimeSandboxPlan.permissionRuntimePolicy", result: "plan-only" },
        { id: "quarantine-results", label: "quarantine results", source: "phase21RuntimeSandboxPlan.runtimeResultQuarantine", result: "plan-only" },
        { id: "release-blockers", label: "release blockers", source: "phase22RuntimeEvidenceReview.releaseReadinessDecision", result: "blocked" }
      ],
      evidenceSources: [
        "phase21RuntimeSandboxPlan",
        "phase22RuntimeEvidenceReview",
        "phase23ReleaseReadinessReviewUi.nextPhaseGuardrailDashboard",
        "phase23ReleaseReadinessReviewUi.reviewActionLedger"
      ],
      preflightCards: [
        { id: "runtime-execution", expected: false, actual: false, result: "pass" },
        { id: "third-party-execution", expected: false, actual: false, result: "pass" },
        { id: "release-ready", expected: false, actual: false, result: "blocked-by-active-release-blockers" },
        { id: "audit-evidence", expected: "present", actual: "present", result: "pass" }
      ],
      blockedRuntimeActions: [
        "run-third-party-plugin",
        "enable-plugin-runtime",
        "bypass-runtime-quarantine",
        "mark-runtime-output-trusted",
        "ship-runtime-sandbox-ui-as-execution"
      ],
      allowedPlanningActions: [
        "design-runtime-sandbox-ui",
        "map-preflight-cards",
        "link-quarantine-evidence",
        "export-runtime-sandbox-ui-plan"
      ],
      implementationOrder: [
        "read-only-preflight-panel",
        "permission-decision-preview",
        "quarantine-result-review-panel",
        "release-blocker-linked-runtime-summary"
      ],
      exportPolicy: {
        exportName: "phase23-runtime-sandbox-ui-plan.json",
        includeInDecisionHistory: true,
        redaction: "no-runtime-output"
      },
      guardrails: {
        blockedWhen: [
          "runtimeSandboxUiPlan-executes-code",
          "third-party-execution-enabled-from-ui-plan",
          "runtime-output-marked-trusted-without-review",
          "releaseReady-true-from-runtime-sandbox-ui-plan"
        ]
      },
      summary: {
        uiSections: 4,
        preflightCards: 4,
        blockedRuntimeActions: 5,
        runtimeExecution: false,
        thirdPartyExecution: false,
        releaseReady: false
      }
    },
    runtimeSandboxReadOnlyPanels: {
      schemaVersion: "phase23.runtime-sandbox-readonly-panels.v1",
      status: "readonly-panels-ready-runtime-disabled",
      panelMode: "read-only-evidence-no-execution",
      sourcePlan: "phase23ReleaseReadinessReviewUi.runtimeSandboxUiPlan",
      panels: [
        {
          id: "preflight-panel",
          title: "runtime preflight",
          source: "phase21RuntimeSandboxPlan.runtimePreflight",
          fields: ["checkId", "expected", "actual", "result", "evidenceRef"],
          primarySignal: "runtimeExecution=false"
        },
        {
          id: "permission-panel",
          title: "permission decisions",
          source: "phase21RuntimeSandboxPlan.permissionRuntimePolicy.samplePermissionDecisions",
          fields: ["pluginId", "hostApi", "decision", "blockedReason", "auditEventType"],
          primarySignal: "defaultDecision=deny"
        },
        {
          id: "quarantine-panel",
          title: "quarantine results",
          source: "phase21RuntimeSandboxPlan.runtimeResultQuarantine.sampleQuarantineItems",
          fields: ["itemId", "pluginId", "state", "reason", "reviewDisposition"],
          primarySignal: "quarantined-results-not-trusted"
        },
        {
          id: "release-blocker-panel",
          title: "release blockers",
          source: "phase22RuntimeEvidenceReview.releaseReadinessDecision.activeBlockers",
          fields: ["blockerId", "severity", "owner", "reason", "missingBeforeRelease"],
          primarySignal: "releaseReady=false"
        }
      ],
      panelActions: {
        allowed: [
          "filter-runtime-panels",
          "open-evidence-reference",
          "export-readonly-panel-state",
          "copy-blocker-reference"
        ],
        blocked: [
          "execute-plugin-from-panel",
          "enable-runtime-from-panel",
          "trust-quarantine-result-from-panel",
          "mark-release-ready-from-panel"
        ]
      },
      displayState: {
        defaultPanel: "preflight-panel",
        filters: ["result", "pluginId", "blockedReason", "severity"],
        emptyState: "runtime evidence unavailable until source plans are present"
      },
      exportPolicy: {
        exportName: "phase23-runtime-sandbox-readonly-panels.json",
        includeInDecisionHistory: true,
        redaction: "no-plugin-output-payload"
      },
      guardrails: {
        blockedWhen: [
          "readOnlyPanel-executes-plugin-code",
          "panel-action-enables-runtime",
          "quarantine-result-trusted-from-panel",
          "releaseReady-true-from-readonly-panel"
        ]
      },
      summary: {
        panels: 4,
        allowedActions: 4,
        blockedActions: 4,
        runtimeExecution: false,
        thirdPartyExecution: false,
        releaseReady: false
      }
    },
    runtimeSandboxPanelViewExport: {
      schemaVersion: "phase23.runtime-sandbox-panel-view-export.v1",
      status: "panel-view-export-ready-runtime-disabled",
      viewMode: "filtered-readonly-export-no-execution",
      sourcePanels: "phase23ReleaseReadinessReviewUi.runtimeSandboxReadOnlyPanels",
      filterModel: {
        fields: ["panelId", "result", "pluginId", "blockedReason", "severity", "releaseReady"],
        presets: ["show-blocked-runtime", "show-permission-denied", "show-quarantined", "show-release-blockers"],
        defaultPreset: "show-blocked-runtime"
      },
      views: [
        { id: "preflight-blocks", panelId: "preflight-panel", filterPreset: "show-blocked-runtime", exportable: true },
        { id: "permission-denials", panelId: "permission-panel", filterPreset: "show-permission-denied", exportable: true },
        { id: "quarantine-review", panelId: "quarantine-panel", filterPreset: "show-quarantined", exportable: true },
        { id: "release-blocker-summary", panelId: "release-blocker-panel", filterPreset: "show-release-blockers", exportable: true }
      ],
      exportViews: [
        "preflight-blocks",
        "permission-denials",
        "quarantine-review",
        "release-blocker-summary"
      ],
      blockedExportActions: [
        "export-plugin-output-payload",
        "export-runtime-execution-result",
        "export-as-release-ready",
        "export-without-active-blockers"
      ],
      exportPolicy: {
        exportName: "phase23-runtime-sandbox-panel-view-export.json",
        includeInDecisionHistory: true,
        redaction: "panel-evidence-only-no-runtime-payload"
      },
      guardrails: {
        blockedWhen: [
          "panel-export-includes-runtime-output",
          "panel-export-marks-releaseReady-true",
          "panel-export-omits-active-blocker",
          "panel-export-enables-third-party-execution"
        ]
      },
      summary: {
        filters: 6,
        views: 4,
        exportViews: 4,
        runtimeExecution: false,
        thirdPartyExecution: false,
        releaseReady: false
      }
    },
    phase23ClosureHandoffPackage: {
      schemaVersion: "phase23.closure-handoff-package.v1",
      status: "phase23-closed-next-phase-ready-release-blocked",
      handoffMode: "close-phase23-with-release-blockers-preserved",
      completedVersionRange: ["1.3.0", "1.3.9"],
      includedArtifacts: [
        "phase23ReleaseReadinessReviewUi",
        "clearanceAuditSearch",
        "decisionHistoryExport",
        "blockerDetailPanels",
        "reviewActionLedger",
        "nextPhaseGuardrailDashboard",
        "runtimeSandboxUiPlan",
        "runtimeSandboxReadOnlyPanels",
        "runtimeSandboxPanelViewExport"
      ],
      nextPhaseEntry: {
        ready: closure.nextPhaseEntryReady,
        source: "phase22RuntimeReviewClosure.nextPhaseEntryReady",
        conditions: closure.nextPhaseEntryConditions,
        allowedWork: closure.nextPhaseAllowedWork
      },
      releaseReadiness: {
        releaseReady: false,
        decision: phase22Review.releaseReadinessDecision.decision,
        activeBlockers: phase22Review.releaseReadinessDecision.activeBlockers.map((blocker) => blocker.id)
      },
      blockedReleaseActions: [
        "mark-release-ready",
        "enable-runtime-execution",
        "enable-third-party-execution",
        "ship-third-party-plugin-runtime",
        "clear-blockers-without-audit"
      ],
      recommendedNextPhase: {
        label: "phase24-runtime-sandbox-ux-implementation-plan",
        entryPolicy: "planning-and-readonly-ux-only-until-release-clearance"
      },
      exportPolicy: {
        exportName: "phase23-closure-handoff-package.json",
        includeInDecisionHistory: true,
        redaction: "handoff-evidence-only"
      },
      guardrails: {
        blockedWhen: [
          "phase23-closed-without-phase22-closure",
          "handoff-marks-releaseReady-true",
          "handoff-enables-runtime-execution",
          "handoff-drops-active-blockers"
        ]
      },
      summary: {
        artifacts: 9,
        nextPhaseEntryReady: true,
        releaseReady: false,
        runtimeExecution: false,
        thirdPartyExecution: false,
        activeBlockers: phase22Review.releaseReadinessDecision.activeBlockers.length
      }
    },
    filterModel: {
      fields: ["severity", "clearanceOwner", "gateResult", "auditOutcome", "releaseReady", "runtimeExecution"],
      presets: ["show-blockers", "show-holds", "show-failed-clearance", "show-next-phase-guards"],
      defaultPreset: "show-blockers"
    },
    detailPanels: [
      "blocker-detail",
      "gate-result-detail",
      "clearance-audit-detail",
      "decision-history",
      "next-phase-entry-guards"
    ],
    allowedActions: [
      "filter-release-readiness",
      "open-blocker-detail",
      "open-clearance-audit-entry",
      "export-decision-history",
      "enter-next-phase-planning"
    ],
    blockedActions: [
      "enable-runtime-execution",
      "enable-third-party-execution",
      "mark-release-ready",
      "clear-blocker-without-evidence"
    ],
    guardrails: {
      releaseReadyRequires: [
        "phase22RuntimeReviewClosure.phase22Complete=true",
        "runtimeExecution=false",
        "thirdPartyExecution=false",
        "no-active-blockers",
        "clearance-audit-passed"
      ],
      blockedWhen: [
        "runtime-enabled-before-clearance",
        "third-party-execution-enabled",
        "releaseReady-true-with-active-blocker",
        "decision-history-missing"
      ]
    },
    summary: {
      memoryCount: memories.length,
      phase22Complete: closure.phase22Complete,
      nextPhaseEntryReady: closure.nextPhaseEntryReady,
      activeBlockers: closure.blockingSummary.activeBlockers,
      releaseReady: false,
      runtimeReady: false,
      thirdPartyExecution: false
    },
    nextControls: ["clearance-audit-search", "decision-history-export", "runtime-sandbox-ui-planning"]
  };
}

function buildLocalPhase24RuntimeSandboxUxEntry() {
  const phase23Review = buildLocalPhase23ReleaseReadinessReviewUi();
  const handoff = phase23Review.phase23ClosureHandoffPackage;
  const runtimePreflightWorkbench = {
    schemaVersion: "phase24.runtime-preflight-workbench.v1",
    version: "1.4.1",
    status: "ready-readonly-runtime-disabled",
    mode: "preflight-review-only-no-code-execution",
    rows: [
      { id: "kill-switch", label: "Runtime kill switch", result: "pass", evidence: "phase21 sandbox runner killSwitch=true", blockedWhen: "kill-switch-disabled" },
      { id: "permission-policy", label: "Permission runtime policy", result: "pass", evidence: "deny-by-default policy is inherited", blockedWhen: "permission-policy-missing" },
      { id: "quarantine", label: "Quarantine queue", result: "block", evidence: "runtime results remain quarantined", blockedWhen: "quarantine-review-missing" },
      { id: "release-gate", label: "Release gate", result: "block", evidence: "phase23 active blockers are preserved", blockedWhen: "releaseReady-true-before-clearance" }
    ],
    detailFields: ["checkId", "result", "evidence", "blockedWhen", "sourceArtifact", "reviewState"],
    allowedActions: ["filter-preflight-checks", "open-readonly-evidence", "export-preflight-review"],
    blockedActions: ["run-preflight-execution", "toggle-kill-switch", "enable-runtime-execution"],
    summary: { total: 4, pass: 2, block: 2, runtimeExecution: false, releaseReady: false }
  };
  const permissionDecisionReviewFlow = {
    schemaVersion: "phase24.permission-decision-review-flow.v1",
    version: "1.4.2",
    status: "ready-readonly-deny-by-default",
    defaultDecision: "deny",
    reviewRows: [
      { id: "host-api-read-redacted-input", pluginId: "markdown-importer", hostApi: "read-redacted-input", requestedPermission: "memory.import.preview", decision: "blocked", reason: "kill-switch-disabled", reviewerAction: "record-review-note" },
      { id: "host-api-network-request", pluginId: "third-party-placeholder", hostApi: "network-request", requestedPermission: "network.request", decision: "blocked", reason: "unknown-host-api", reviewerAction: "keep-blocked" },
      { id: "host-api-sensitive-output", pluginId: "csv-importer", hostApi: "emit-draft-output", requestedPermission: "memory.import.preview", decision: "blocked", reason: "sensitive-field-leak", reviewerAction: "request-redacted-fixture" }
    ],
    filters: ["pluginId", "hostApi", "requestedPermission", "decision", "reason"],
    allowedActions: ["filter-permission-decisions", "record-review-note", "export-permission-review"],
    blockedActions: ["grant-runtime-permission", "execute-host-api", "enable-third-party-execution"],
    summary: { decisions: 3, blocked: 3, approvedRuntimePermissions: 0, runtimeExecution: false }
  };
  const quarantineReviewQueue = {
    schemaVersion: "phase24.quarantine-review-queue.v1",
    version: "1.4.3",
    status: "ready-readonly-quarantine-preserved",
    queueItems: [
      { id: "quarantine-runtime-result-1", pluginId: "markdown-importer", severity: "block", reason: "kill-switch-disabled", disposition: "blocked", missingEvidence: ["runtime-disabled-signoff"], availableActions: ["record-review-disposition", "export-quarantine-item"] },
      { id: "quarantine-runtime-result-2", pluginId: "third-party-placeholder", severity: "block", reason: "lockfile-entry-blocked", disposition: "blocked", missingEvidence: ["trusted-signature", "reviewed-lockfile-entry"], availableActions: ["record-review-disposition", "open-linked-audit"] },
      { id: "quarantine-runtime-result-3", pluginId: "csv-importer", severity: "hold", reason: "output-validation-rejected", disposition: "needs-evidence", missingEvidence: ["redacted-output-fixture"], availableActions: ["request-evidence", "export-quarantine-item"] }
    ],
    allowedActions: ["filter-quarantine-queue", "record-review-disposition", "export-quarantine-review"],
    blockedActions: ["apply-quarantined-result", "mark-result-trusted", "clear-release-blocker"],
    summary: { queueItems: 3, block: 2, hold: 1, cleared: 0, releaseReady: false }
  };
  const runtimeEvidenceExportPack = {
    schemaVersion: "phase24.runtime-evidence-export-pack.v1",
    version: "1.4.4",
    status: "ready-export-guarded",
    exportName: "phase24-runtime-evidence-export-pack.json",
    includes: ["phase23-closure-handoff", "runtime-preflight-workbench", "permission-decision-review-flow", "quarantine-review-queue", "release-blocker-summary", "operations-request-id"],
    redaction: "plugin-results-redacted-fixture-only",
    integrityChecks: ["phase23-handoff-present", "runtime-disabled", "third-party-execution-disabled", "releaseReady-false"],
    allowedActions: ["preview-evidence-export", "download-redacted-evidence-pack"],
    blockedActions: ["export-unredacted-plugin-result", "mutate-release-state-from-export"],
    summary: { exportable: true, redacted: true, integrityChecks: 4, releaseReady: false }
  };
  const releaseClearancePreview = {
    schemaVersion: "phase24.release-clearance-preview.v1",
    version: "1.4.5",
    status: "ready-preview-blocked",
    previewMode: "derived-readonly-no-release-mutation",
    gates: [
      { id: "runtime-disabled", result: "pass", evidence: "runtimeExecution=false" },
      { id: "third-party-disabled", result: "pass", evidence: "thirdPartyExecution=false" },
      { id: "quarantine-cleared", result: "block", evidence: "quarantine queue still has blocked items" },
      { id: "blocker-signoff", result: "block", evidence: "phase23 active blockers remain" },
      { id: "evidence-export-reviewed", result: "hold", evidence: "export pack is previewable but not signed off" }
    ],
    clearRequires: ["quarantine-review-complete", "blocker-signoff-recorded", "evidence-export-reviewed", "release-checklist-pass"],
    allowedActions: ["preview-clearance-result", "export-clearance-preview", "open-blocker-detail"],
    blockedActions: ["mark-release-ready", "clear-blocker-without-evidence", "enable-runtime-execution"],
    summary: { pass: 2, hold: 1, block: 2, releaseReady: false }
  };
  const phase24ClosurePackage = {
    schemaVersion: "phase24.closure-package.v1",
    version: "1.4.6",
    status: "phase24-ux-model-complete-release-blocked",
    completedVersionRange: "1.4.1-1.4.6",
    includedArtifacts: ["runtimePreflightWorkbench", "permissionDecisionReviewFlow", "quarantineReviewQueue", "runtimeEvidenceExportPack", "releaseClearancePreview"],
    closureChecks: [
      { id: "preflight-modeled", result: "pass" },
      { id: "permission-review-modeled", result: "pass" },
      { id: "quarantine-review-modeled", result: "pass" },
      { id: "evidence-export-modeled", result: "pass" },
      { id: "clearance-preview-modeled", result: "pass" },
      { id: "release-ready", result: "block" }
    ],
    nextPhaseEntry: {
      ready: true,
      recommendedFocus: "phase25-runtime-sandbox-ui-surface",
      conditions: ["keep-runtime-disabled", "keep-third-party-execution-disabled", "preserve-release-blockers"]
    },
    summary: { phase24Complete: true, nextPhaseEntryReady: true, releaseReady: false, runtimeExecution: false, thirdPartyExecution: false }
  };
  return {
    phase: 24,
    schemaVersion: "phase24.closure-package.v1",
    status: "phase24-ux-model-complete-release-blocked",
    entryMode: "readonly-ux-implementation-no-code-execution",
    sourceHandoff: "phase23ReleaseReadinessReviewUi.phase23ClosureHandoffPackage",
    inheritedPhase23: {
      handoffStatus: handoff.status,
      completedVersionRange: handoff.completedVersionRange,
      includedArtifacts: handoff.includedArtifacts
    },
    uxScope: [
      "runtime-preflight-workbench",
      "permission-decision-review-flow",
      "quarantine-review-queue",
      "runtime-evidence-export-pack",
      "release-clearance-preview"
    ],
    entryConditions: handoff.nextPhaseEntry.conditions,
    blockedReleaseActions: handoff.blockedReleaseActions,
    allowedActions: [
      "design-readonly-runtime-ux",
      "map-phase23-evidence-to-phase24-panels",
      "export-phase24-entry-plan"
    ],
    blockedActions: [
      "enable-runtime-execution",
      "enable-third-party-execution",
      "execute-third-party-plugin",
      "mark-release-ready"
    ],
    exportPolicy: {
      exportName: "phase24-closure-package.json",
      includeInOperationsExport: true,
      redaction: "handoff-evidence-only"
    },
    guardrails: {
      blockedWhen: [
        "phase24-entry-without-phase23-handoff",
        "runtime-enabled-during-phase24-entry",
        "third-party-execution-enabled-during-phase24-entry",
        "releaseReady-true-before-clearance"
      ]
    },
    runtimePreflightWorkbench,
    permissionDecisionReviewFlow,
    quarantineReviewQueue,
    runtimeEvidenceExportPack,
    releaseClearancePreview,
    phase24ClosurePackage,
    summary: {
      phase24Complete: true,
      phase23Closed: true,
      nextPhaseEntryReady: handoff.nextPhaseEntry.ready,
      activeBlockers: handoff.releaseReadiness.activeBlockers.length,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false
    }
  };
}

function buildLocalPhase25RuntimeSandboxUiSurface() {
  const phase24 = buildLocalPhase24RuntimeSandboxUxEntry();
  const surfacePanels = [
    {
      id: "runtime-preflight-workbench",
      label: "Runtime preflight",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.runtimePreflightWorkbench",
      component: "readonly-check-table",
      routeHash: "#runtime-preflight",
      filters: ["result", "blockedWhen", "sourceArtifact"],
      primaryActions: ["filter-preflight-checks", "open-readonly-evidence", "export-preflight-review"],
      blockedActions: ["run-preflight-execution", "toggle-kill-switch", "enable-runtime-execution"],
      status: "ready"
    },
    {
      id: "permission-decision-review",
      label: "Permission decisions",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.permissionDecisionReviewFlow",
      component: "permission-review-table",
      routeHash: "#permission-decisions",
      filters: ["pluginId", "hostApi", "requestedPermission", "decision", "reason"],
      primaryActions: ["filter-permission-decisions", "record-review-note-preview", "export-permission-review"],
      blockedActions: ["grant-runtime-permission", "execute-host-api", "enable-third-party-execution"],
      status: "ready"
    },
    {
      id: "quarantine-review-queue",
      label: "Quarantine queue",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.quarantineReviewQueue",
      component: "quarantine-review-list",
      routeHash: "#quarantine-queue",
      filters: ["pluginId", "severity", "reason", "disposition"],
      primaryActions: ["filter-quarantine-queue", "open-linked-audit", "export-quarantine-review"],
      blockedActions: ["apply-quarantined-result", "mark-result-trusted", "clear-release-blocker"],
      status: "ready"
    },
    {
      id: "runtime-evidence-export-pack",
      label: "Evidence export",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.runtimeEvidenceExportPack",
      component: "guarded-export-preview",
      routeHash: "#runtime-evidence-export",
      filters: ["include", "integrityCheck", "redaction"],
      primaryActions: ["preview-evidence-export", "download-redacted-evidence-pack"],
      blockedActions: ["export-unredacted-plugin-result", "mutate-release-state-from-export"],
      status: "ready"
    },
    {
      id: "release-clearance-preview",
      label: "Release clearance",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.releaseClearancePreview",
      component: "clearance-gate-preview",
      routeHash: "#release-clearance",
      filters: ["gate", "result", "evidence"],
      primaryActions: ["preview-clearance-result", "export-clearance-preview", "open-blocker-detail"],
      blockedActions: ["mark-release-ready", "clear-blocker-without-evidence", "enable-runtime-execution"],
      status: "ready"
    }
  ];
  const surfaceStateModel = {
    schemaVersion: "phase25.surface-state-model.v1",
    version: "1.5.1",
    mode: "readonly-browser-state-no-runtime-mutation",
    defaultState: {
      activePanel: "runtime-preflight-workbench",
      filterPreset: "show-all-blocked-first",
      detailDrawerOpen: false,
      exportPreviewOpen: false
    },
    routeState: {
      defaultHash: "#runtime-preflight",
      hashParam: "phase25Panel",
      allowedHashes: surfacePanels.map((panel) => panel.routeHash),
      unknownHashFallback: "runtime-preflight-workbench"
    },
    filterPresets: [
      { id: "show-all-blocked-first", label: "All, blockers first", appliesTo: "all-panels", sort: "block-hold-pass" },
      { id: "blocked-only", label: "Blocked only", appliesTo: "runtime-preflight-workbench,permission-decision-review,quarantine-review-queue,release-clearance-preview", filter: "result-or-decision-is-block" },
      { id: "exportable-evidence", label: "Exportable evidence", appliesTo: "runtime-evidence-export-pack", filter: "redacted-exportable-only" }
    ],
    panelState: surfacePanels.map((panel) => ({
      panelId: panel.id,
      selected: panel.id === "runtime-preflight-workbench",
      filterControls: panel.filters.map((filter) => ({ id: filter, mode: "readonly-filter", persisted: false })),
      detailDrawer: {
        enabled: true,
        mode: "readonly",
        fields: ["sourceArtifact", "primaryActions", "blockedActions", "reviewNotePreview"]
      },
      exportPreview: {
        enabled: panel.primaryActions.some((action) => action.includes("export") || action.includes("download")),
        redactedOnly: true
      }
    })),
    blockedStateMutations: [
      "persist-runtime-permission-grant",
      "persist-quarantine-clearance",
      "set-releaseReady-true",
      "enable-runtime-execution-from-ui-state",
      "enable-third-party-execution-from-ui-state"
    ],
    stateIntegrity: {
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistence: "browser-session-or-export-only",
      auditMode: "ui-state-review-preview"
    }
  };
  const panelEvidenceBindings = [
    {
      panelId: "runtime-preflight-workbench",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.runtimePreflightWorkbench",
      sourceSchemaVersion: phase24.runtimePreflightWorkbench.schemaVersion,
      evidenceFields: ["rows", "summary", "blockedActions"],
      sourceCount: phase24.runtimePreflightWorkbench.rows.length,
      redaction: "no-plugin-output",
      integrityChecks: ["source-schema-present", "runtimeExecution-false", "releaseReady-false"]
    },
    {
      panelId: "permission-decision-review",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.permissionDecisionReviewFlow",
      sourceSchemaVersion: phase24.permissionDecisionReviewFlow.schemaVersion,
      evidenceFields: ["reviewRows", "defaultDecision", "blockedActions"],
      sourceCount: phase24.permissionDecisionReviewFlow.reviewRows.length,
      redaction: "host-api-request-only",
      integrityChecks: ["default-deny", "approvedRuntimePermissions-zero", "thirdPartyExecution-false"]
    },
    {
      panelId: "quarantine-review-queue",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.quarantineReviewQueue",
      sourceSchemaVersion: phase24.quarantineReviewQueue.schemaVersion,
      evidenceFields: ["queueItems", "summary", "blockedActions"],
      sourceCount: phase24.quarantineReviewQueue.queueItems.length,
      redaction: "quarantine-metadata-only",
      integrityChecks: ["cleared-zero", "releaseReady-false", "apply-quarantined-result-blocked"]
    },
    {
      panelId: "runtime-evidence-export-pack",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.runtimeEvidenceExportPack",
      sourceSchemaVersion: phase24.runtimeEvidenceExportPack.schemaVersion,
      evidenceFields: ["includes", "integrityChecks", "summary"],
      sourceCount: phase24.runtimeEvidenceExportPack.integrityChecks.length,
      redaction: phase24.runtimeEvidenceExportPack.redaction,
      integrityChecks: ["redacted-export-only", "mutate-release-state-blocked", "operations-request-id-linked"]
    },
    {
      panelId: "release-clearance-preview",
      sourceArtifact: "phase24RuntimeSandboxUxEntry.releaseClearancePreview",
      sourceSchemaVersion: phase24.releaseClearancePreview.schemaVersion,
      evidenceFields: ["gates", "clearRequires", "summary"],
      sourceCount: phase24.releaseClearancePreview.gates.length,
      redaction: "clearance-result-only",
      integrityChecks: ["releaseReady-false", "mark-release-ready-blocked", "blocker-evidence-required"]
    }
  ];
  const reviewActionModel = {
    schemaVersion: "phase25.review-action-model.v1",
    version: "1.5.3",
    actionMode: "readonly-review-preview-no-release-mutation",
    persistence: "session-preview-or-export-only",
    panelActions: surfacePanels.map((panel) => ({
      panelId: panel.id,
      allowedReviewActions: panel.primaryActions.filter((action) => (
        action.includes("filter") || action.includes("open") || action.includes("preview") || action.includes("export") || action.includes("download") || action.includes("record-review-note")
      )),
      auditPreview: {
        eventType: "phase25-review-action-preview",
        sourceArtifact: panel.sourceArtifact,
        writesToRuntime: false,
        writesToReleaseState: false
      },
      disabledMutations: panel.blockedActions
    })),
    globalReviewActions: [
      "switch-panel",
      "apply-readonly-filter",
      "open-evidence-detail",
      "preview-redacted-export",
      "export-ui-state-review"
    ],
    blockedMutationPolicy: {
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      blockedActions: [
        "grant-runtime-permission",
        "apply-quarantined-result",
        "clear-release-blocker",
        "mark-release-ready",
        "execute-third-party-plugin"
      ]
    },
    summary: {
      panels: surfacePanels.length,
      previewOnly: true,
      persistedMutations: 0,
      runtimeExecution: false,
      releaseReady: false
    }
  };
  const reviewAuditPreview = {
    schemaVersion: "phase25.review-audit-preview.v1",
    version: "1.5.4",
    auditMode: "readonly-action-audit-preview-no-persistence",
    eventSource: "phase25.review-action-model.v1",
    exportPackage: {
      exportName: "phase25-review-audit-preview.json",
      includeInOperationsExport: true,
      redaction: "review-actions-and-blocked-mutations-only",
      writesToRuntime: false,
      writesToReleaseState: false
    },
    previewEvents: reviewActionModel.panelActions.map((item, index) => {
      const evidence = panelEvidenceBindings.find((binding) => binding.panelId === item.panelId) || {};
      return {
        eventId: `phase25-review-preview-${String(index + 1).padStart(2, "0")}`,
        panelId: item.panelId,
        eventType: item.auditPreview.eventType,
        action: item.allowedReviewActions[0] || "open-evidence-detail",
        sourceArtifact: item.auditPreview.sourceArtifact,
        sourceSchemaVersion: evidence.sourceSchemaVersion || "unknown",
        redaction: evidence.redaction || "redacted",
        writesToRuntime: false,
        writesToReleaseState: false,
        persisted: false
      };
    }),
    blockedMutationEvents: reviewActionModel.blockedMutationPolicy.blockedActions.map((action) => ({
      action,
      disposition: "blocked-before-runtime",
      writesToRuntime: false,
      writesToReleaseState: false,
      persisted: false
    })),
    integritySummary: {
      previewEvents: reviewActionModel.panelActions.length,
      blockedMutationEvents: reviewActionModel.blockedMutationPolicy.blockedActions.length,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0,
      exportReady: true
    }
  };
  const reviewExportPackage = {
    schemaVersion: "phase25.review-export-package.v1",
    version: "1.5.5",
    packageName: "phase25-review-export-package.json",
    packageMode: "readonly-review-package-no-runtime-execution",
    includedArtifacts: [
      { id: "surface-state", schemaVersion: surfaceStateModel.schemaVersion, exportName: "phase25-surface-state-model.json", source: "surfaceStateModel" },
      { id: "panel-evidence", schemaVersion: "phase25.panel-evidence-bindings.v1", exportName: "phase25-panel-evidence-bindings.json", source: "panelEvidenceBindings", itemCount: panelEvidenceBindings.length },
      { id: "review-actions", schemaVersion: reviewActionModel.schemaVersion, exportName: "phase25-review-action-model.json", source: "reviewActionModel", itemCount: reviewActionModel.panelActions.length },
      { id: "review-audit-preview", schemaVersion: reviewAuditPreview.schemaVersion, exportName: reviewAuditPreview.exportPackage.exportName, source: "reviewAuditPreview", itemCount: reviewAuditPreview.previewEvents.length }
    ],
    integrityManifest: {
      requiresRuntimeExecutionFalse: true,
      requiresThirdPartyExecutionFalse: true,
      requiresReleaseReadyFalse: true,
      redactionPolicy: "ui-state-actions-audit-preview-only",
      sourcePanelCount: surfacePanels.length,
      evidenceBindingCount: panelEvidenceBindings.length,
      previewEventCount: reviewAuditPreview.previewEvents.length,
      blockedMutationCount: reviewAuditPreview.blockedMutationEvents.length,
      persistedMutations: 0
    },
    blockedExportMutations: [
      "export-unredacted-plugin-result",
      "apply-exported-audit-event",
      "mark-release-ready-from-export",
      "enable-runtime-execution-from-export",
      "execute-third-party-plugin-from-export"
    ],
    summary: {
      artifacts: 4,
      exportReady: true,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0
    }
  };
  const reviewPackageValidation = {
    schemaVersion: "phase25.review-package-validation.v1",
    version: "1.5.6",
    validationMode: "readonly-package-validation-no-runtime-execution",
    validatesPackage: reviewExportPackage.packageName,
    artifactChecks: reviewExportPackage.includedArtifacts.map((artifact) => ({
      artifactId: artifact.id,
      exportName: artifact.exportName,
      schemaVersion: artifact.schemaVersion,
      result: "pass",
      checks: [
        "schema-version-present",
        "export-name-present",
        "source-bound",
        "readonly-export"
      ],
      writesToRuntime: false,
      writesToReleaseState: false
    })),
    guardrailChecks: [
      { id: "runtime-disabled", result: "pass", requiredValue: false, actualValue: false },
      { id: "third-party-execution-disabled", result: "pass", requiredValue: false, actualValue: false },
      { id: "release-ready-blocked", result: "pass", requiredValue: false, actualValue: false },
      { id: "persisted-mutations-zero", result: "pass", requiredValue: 0, actualValue: reviewExportPackage.summary.persistedMutations },
      { id: "blocked-export-mutations-present", result: "pass", requiredValue: "non-empty", actualValue: reviewExportPackage.blockedExportMutations.length }
    ],
    blockedValidationMutations: [
      "auto-fix-export-package",
      "apply-validation-result-to-release",
      "enable-runtime-execution-after-validation",
      "clear-release-blocker-after-validation",
      "execute-third-party-plugin-during-validation"
    ],
    summary: {
      artifactsChecked: reviewExportPackage.includedArtifacts.length,
      guardrailsChecked: 5,
      pass: reviewExportPackage.includedArtifacts.length + 5,
      warn: 0,
      block: 0,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0,
      validationReady: true
    }
  };
  return {
    phase: 25,
    schemaVersion: "phase25.runtime-sandbox-ui-surface.v1",
    status: "phase25-ui-surface-entry-ready-runtime-disabled",
    sourceClosure: {
      schemaVersion: phase24.phase24ClosurePackage.schemaVersion,
      completedVersionRange: phase24.phase24ClosurePackage.completedVersionRange,
      phase24Complete: phase24.phase24ClosurePackage.summary.phase24Complete,
      releaseReady: phase24.phase24ClosurePackage.summary.releaseReady
    },
    surfaceMode: "readonly-review-export-no-code-execution",
    navigation: {
      defaultPanel: "runtime-preflight-workbench",
      tabs: surfacePanels.map((panel) => ({ id: panel.id, label: panel.label, routeHash: panel.routeHash, status: panel.status })),
      detailDrawer: { enabled: true, mode: "readonly", fields: ["sourceArtifact", "evidence", "blockedActions", "primaryActions"] }
    },
    surfaceStateModel,
    panelEvidenceBindings,
    reviewActionModel,
    reviewAuditPreview,
    reviewExportPackage,
    reviewPackageValidation,
    surfacePanels,
    sharedControls: {
      filters: ["status", "result", "severity", "pluginId", "reason"],
      exports: ["phase25-runtime-sandbox-ui-surface.json", "phase25-review-audit-preview.json", "phase25-review-export-package.json", "phase25-review-package-validation.json", "phase24-runtime-evidence-export-pack.json"],
      emptyStates: ["no-filter-results", "backend-unavailable", "source-artifact-missing"],
      keyboardModel: "browser-ui-only-no-runtime-shortcuts"
    },
    implementationPlan: [
      "render-phase25-shell-in-platform-panel",
      "wire-phase24-preflight-to-readonly-table",
      "wire-permission-review-to-filterable-table",
      "wire-quarantine-queue-to-detail-drawer",
      "wire-evidence-export-preview",
      "wire-release-clearance-preview"
    ],
    blockedActions: [
      "enable-runtime-execution",
      "enable-third-party-execution",
      "execute-third-party-plugin",
      "grant-runtime-permission",
      "apply-quarantined-result",
      "mark-release-ready"
    ],
    exportPolicy: {
      exportName: "phase25-runtime-sandbox-ui-surface.json",
      includeInOperationsExport: true,
      redaction: "ui-state-and-redacted-evidence-only"
    },
    summary: {
      phase24Complete: true,
      panels: surfacePanels.length,
      readyPanels: surfacePanels.filter((panel) => panel.status === "ready").length,
      stateModelReady: true,
      evidenceBindings: panelEvidenceBindings.length,
      evidenceBindingReady: true,
      reviewActionModelReady: true,
      reviewAuditPreviewReady: true,
      reviewExportPackageReady: true,
      reviewPackageValidationReady: true,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false
    }
  };
}

function buildLocalPhase26RuntimeValidationEntry() {
  const phase25 = buildLocalPhase25RuntimeSandboxUiSurface();
  const packageValidation = phase25.reviewPackageValidation || {};
  const packageExport = phase25.reviewExportPackage || {};
  const artifactChecks = packageValidation.artifactChecks || [];
  const guardrailChecks = packageValidation.guardrailChecks || [];
  const entryReady = packageValidation.summary?.validationReady === true
    && phase25.summary.runtimeExecution === false
    && phase25.summary.thirdPartyExecution === false
    && phase25.summary.releaseReady === false;
  const releaseGateSimulation = {
    schemaVersion: "phase26.release-gate-simulation.v1",
    version: "1.6.1",
    mode: "readonly-release-gate-simulation-no-runtime-execution",
    sourceInputs: [
      { id: "phase25-review-package-validation", schemaVersion: packageValidation.schemaVersion, source: "phase25RuntimeSandboxUiSurface.reviewPackageValidation" },
      { id: "phase25-review-export-package", schemaVersion: packageExport.schemaVersion, source: "phase25RuntimeSandboxUiSurface.reviewExportPackage" },
      { id: "phase26-entry-conditions", schemaVersion: "phase26.runtime-validation-entry.v1", source: "phase26RuntimeValidationEntry.entryConditions" }
    ],
    gateItems: [
      { id: "package-artifacts-present", result: artifactChecks.length >= 4 ? "pass" : "block", evidence: `${artifactChecks.length} artifact checks` },
      { id: "guardrails-pass", result: guardrailChecks.every((item) => item.result === "pass") ? "pass" : "block", evidence: `${guardrailChecks.length} guardrail checks` },
      { id: "runtime-disabled", result: phase25.summary.runtimeExecution === false ? "pass" : "block", evidence: "runtimeExecution=false" },
      { id: "third-party-disabled", result: phase25.summary.thirdPartyExecution === false ? "pass" : "block", evidence: "thirdPartyExecution=false" },
      { id: "release-ready-remains-blocked", result: phase25.summary.releaseReady === false ? "block" : "pass", evidence: "releaseReady=false keeps release simulation blocked" }
    ],
    simulatedDecision: {
      result: "block-release",
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: entryReady ? "release gate simulation completed with releaseReady intentionally blocked" : "release gate simulation input is incomplete"
    },
    blockedActions: [
      "write-releaseReady-true",
      "clear-release-blocker",
      "persist-simulated-decision",
      "execute-runtime-preflight",
      "execute-third-party-plugin"
    ],
    exportPolicy: {
      exportName: "phase26-release-gate-simulation.json",
      includeInOperationsExport: true,
      redaction: "gate-results-and-source-metadata-only"
    },
    summary: {
      totalGateItems: 5,
      pass: 4,
      block: 1,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const blockingGateItems = releaseGateSimulation.gateItems.filter((item) => item.result === "block");
  const blockerClearanceSimulation = {
    schemaVersion: "phase26.blocker-clearance-simulation.v1",
    version: "1.6.2",
    mode: "readonly-blocker-clearance-simulation-no-release-mutation",
    sourceInputs: [
      { id: "release-gate-simulation", schemaVersion: releaseGateSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.releaseGateSimulation" },
      { id: "phase23-closure-handoff", schemaVersion: "phase23.closure-handoff-package.v1", source: "phase23ReleaseReadinessReviewUi.phase23ClosureHandoffPackage" },
      { id: "phase25-package-validation", schemaVersion: packageValidation.schemaVersion, source: "phase25RuntimeSandboxUiSurface.reviewPackageValidation" }
    ],
    clearanceItems: blockingGateItems.map((item) => ({
      id: item.id,
      sourceResult: item.result,
      simulatedDisposition: "not-cleared",
      clearanceOwner: "runtime-reviewer",
      requiredEvidence: ["human-review-note", "runtime-remains-disabled", "releaseReady-remains-false"],
      blockedMutation: "clear-release-blocker"
    })),
    simulationSteps: [
      "collect-blocking-gate-items",
      "map-blockers-to-required-evidence",
      "simulate-clearance-without-state-write",
      "keep-releaseReady-false",
      "emit-clearance-simulation-export"
    ],
    simulatedDecision: {
      result: "blockers-not-cleared",
      releaseReady: false,
      clearedBlockers: 0,
      remainingBlockers: blockingGateItems.length,
      persistedMutations: 0,
      reason: "blocker clearance is simulated only and cannot clear release blockers in Phase 26"
    },
    blockedActions: [
      "clear-release-blocker",
      "mark-blocker-cleared",
      "set-releaseReady-true",
      "write-clearance-result",
      "persist-simulated-clearance"
    ],
    exportPolicy: {
      exportName: "phase26-blocker-clearance-simulation.json",
      includeInOperationsExport: true,
      redaction: "clearance-evidence-requirements-only"
    },
    summary: {
      totalBlockers: blockingGateItems.length,
      clearableInSimulation: blockingGateItems.length,
      clearedBlockers: 0,
      remainingBlockers: blockingGateItems.length,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const runtimeValidationReport = {
    schemaVersion: "phase26.runtime-validation-report.v1",
    version: "1.6.3",
    mode: "readonly-runtime-validation-report-no-runtime-execution",
    sourceInputs: [
      { id: "phase26-entry", schemaVersion: "phase26.runtime-validation-entry.v1", source: "phase26RuntimeValidationEntry" },
      { id: "release-gate-simulation", schemaVersion: releaseGateSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.releaseGateSimulation" },
      { id: "blocker-clearance-simulation", schemaVersion: blockerClearanceSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.blockerClearanceSimulation" },
      { id: "phase25-package-validation", schemaVersion: packageValidation.schemaVersion, source: "phase25RuntimeSandboxUiSurface.reviewPackageValidation" }
    ],
    reportSections: [
      { id: "entry-conditions", status: entryReady ? "pass" : "block", evidence: "5 entry conditions evaluated" },
      { id: "release-gate", status: releaseGateSimulation.simulatedDecision.result, evidence: `${releaseGateSimulation.summary.block} blocked gate item` },
      { id: "blocker-clearance", status: blockerClearanceSimulation.simulatedDecision.result, evidence: `${blockerClearanceSimulation.summary.remainingBlockers} blocker remains` },
      { id: "runtime-guardrails", status: "pass", evidence: "runtimeExecution=false, thirdPartyExecution=false, persistedMutations=0" }
    ],
    findings: [
      { id: "runtime-execution-disabled", severity: "pass", detail: "Runtime execution remains disabled for Phase 26 validation reporting." },
      { id: "third-party-execution-disabled", severity: "pass", detail: "Third-party plugin execution remains disabled." },
      { id: "release-ready-blocked", severity: "block", detail: "releaseReady remains false and cannot be changed by this report." },
      { id: "blocker-clearance-not-persisted", severity: "block", detail: "Clearance simulation records required evidence but does not clear blockers." }
    ],
    reportDecision: {
      result: "report-blocked-for-runtime-release",
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: "runtime validation report is export-only and preserves the blocked release state"
    },
    blockedActions: [
      "execute-runtime-validation",
      "persist-report-as-runtime-result",
      "mark-releaseReady-true",
      "clear-release-blocker-from-report",
      "publish-runtime-release"
    ],
    exportPolicy: {
      exportName: "phase26-runtime-validation-report.json",
      includeInOperationsExport: true,
      redaction: "report-summary-findings-and-guardrail-status-only"
    },
    summary: {
      reportSections: 4,
      findings: 4,
      blockingFindings: 2,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const handoffCriteria = {
    schemaVersion: "phase26.handoff-criteria.v1",
    version: "1.6.4",
    mode: "readonly-handoff-criteria-no-runtime-or-release-mutation",
    sourceInputs: [
      { id: "runtime-validation-report", schemaVersion: runtimeValidationReport.schemaVersion, source: "phase26RuntimeValidationEntry.runtimeValidationReport" },
      { id: "release-gate-simulation", schemaVersion: releaseGateSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.releaseGateSimulation" },
      { id: "blocker-clearance-simulation", schemaVersion: blockerClearanceSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.blockerClearanceSimulation" },
      { id: "phase25-package-validation", schemaVersion: packageValidation.schemaVersion, source: "phase25RuntimeSandboxUiSurface.reviewPackageValidation" }
    ],
    criteria: [
      { id: "runtime-execution-remains-disabled", result: "met", requiredForHandoff: true, evidence: "runtimeExecution=false" },
      { id: "third-party-execution-remains-disabled", result: "met", requiredForHandoff: true, evidence: "thirdPartyExecution=false" },
      { id: "release-ready-remains-blocked", result: "met", requiredForHandoff: true, evidence: "releaseReady=false" },
      { id: "validation-report-exported", result: runtimeValidationReport.exportPolicy?.includeInOperationsExport ? "met" : "blocked", requiredForHandoff: true, evidence: runtimeValidationReport.exportPolicy?.exportName },
      { id: "blockers-carried-forward", result: blockerClearanceSimulation.summary.remainingBlockers > 0 ? "met" : "blocked", requiredForHandoff: true, evidence: `${blockerClearanceSimulation.summary.remainingBlockers} release blocker carried forward` }
    ],
    nextPhaseEntryRequirements: [
      "treat-phase26-report-as-readonly-evidence",
      "carry-release-ready-blocker-forward",
      "require-human-review-before-runtime-discussion",
      "keep-runtime-and-third-party-execution-disabled",
      "do-not-persist-simulated-decisions-as-runtime-results"
    ],
    blockedActions: [
      "enter-runtime-execution",
      "enable-third-party-plugin-execution",
      "clear-carried-forward-blockers",
      "set-releaseReady-true",
      "persist-handoff-as-release-approval"
    ],
    handoffDecision: {
      result: "handoff-ready-with-release-blockers",
      nextPhaseAllowed: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: "Phase 26 can hand off evidence to the next planning stage while release and runtime execution remain blocked"
    },
    exportPolicy: {
      exportName: "phase26-handoff-criteria.json",
      includeInOperationsExport: true,
      redaction: "handoff-criteria-and-carried-blockers-only"
    },
    summary: {
      totalCriteria: 5,
      metCriteria: 5,
      blockedCriteria: 0,
      carriedForwardBlockers: blockerClearanceSimulation.summary.remainingBlockers,
      nextPhaseAllowed: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const closurePackage = {
    schemaVersion: "phase26.closure-package.v1",
    version: "1.6.5",
    mode: "readonly-closure-package-no-runtime-or-release-mutation",
    sourceInputs: [
      { id: "runtime-validation-entry", schemaVersion: "phase26.runtime-validation-entry.v1", source: "phase26RuntimeValidationEntry" },
      { id: "release-gate-simulation", schemaVersion: releaseGateSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.releaseGateSimulation" },
      { id: "blocker-clearance-simulation", schemaVersion: blockerClearanceSimulation.schemaVersion, source: "phase26RuntimeValidationEntry.blockerClearanceSimulation" },
      { id: "runtime-validation-report", schemaVersion: runtimeValidationReport.schemaVersion, source: "phase26RuntimeValidationEntry.runtimeValidationReport" },
      { id: "handoff-criteria", schemaVersion: handoffCriteria.schemaVersion, source: "phase26RuntimeValidationEntry.handoffCriteria" }
    ],
    closureItems: [
      { id: "validation-entry", status: "closed-readonly", artifact: "phase26-runtime-validation-entry.json" },
      { id: "release-gate-simulation", status: "closed-with-blocker", artifact: "phase26-release-gate-simulation.json" },
      { id: "blocker-clearance-simulation", status: "closed-not-cleared", artifact: "phase26-blocker-clearance-simulation.json" },
      { id: "runtime-validation-report", status: "closed-exported", artifact: "phase26-runtime-validation-report.json" },
      { id: "handoff-criteria", status: "closed-for-next-stage-planning", artifact: "phase26-handoff-criteria.json" }
    ],
    carriedForwardBlockers: [
      {
        id: "release-ready-remains-blocked",
        source: "phase26.release-gate-simulation.v1",
        status: "carried-forward",
        requiredBeforeRelease: ["human-review", "runtime-evidence-review", "explicit-release-readiness-decision"]
      }
    ],
    closureDecision: {
      result: "phase26-closed-with-release-blockers",
      phaseClosed: true,
      nextStagePlanningAllowed: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: "Phase 26 is closed as a read-only validation and simulation stage with release blockers carried forward"
    },
    blockedActions: [
      "treat-closure-as-release-approval",
      "clear-carried-forward-blockers",
      "set-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-closure-as-runtime-result"
    ],
    exportPolicy: {
      exportName: "phase26-closure-package.json",
      includeInOperationsExport: true,
      redaction: "closure-summary-artifacts-and-carried-blockers-only"
    },
    summary: {
      closureItems: 5,
      carriedForwardBlockers: 1,
      phaseClosed: true,
      nextStagePlanningAllowed: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  return {
    phase: 26,
    schemaVersion: "phase26.runtime-validation-entry.v1",
    version: "1.6.5",
    status: "phase26-closure-package-ready-runtime-disabled",
    entryMode: "release-gate-simulation-entry-no-runtime-execution",
    sourcePhase25: {
      schemaVersion: phase25.schemaVersion,
      sourceStatus: phase25.status,
      reviewPackageValidation: packageValidation.schemaVersion,
      reviewExportPackage: packageExport.schemaVersion,
      phase25ReleaseReady: phase25.summary.releaseReady,
      phase25RuntimeExecution: phase25.summary.runtimeExecution,
      phase25ThirdPartyExecution: phase25.summary.thirdPartyExecution
    },
    entryConditions: [
      { id: "phase25-surface-present", result: "pass", evidence: "phase25RuntimeSandboxUiSurface.schemaVersion" },
      { id: "package-validation-ready", result: packageValidation.summary?.validationReady ? "pass" : "block", evidence: "reviewPackageValidation.summary.validationReady" },
      { id: "runtime-execution-disabled", result: phase25.summary.runtimeExecution === false ? "pass" : "block", evidence: "phase25RuntimeSandboxUiSurface.summary.runtimeExecution=false" },
      { id: "third-party-execution-disabled", result: phase25.summary.thirdPartyExecution === false ? "pass" : "block", evidence: "phase25RuntimeSandboxUiSurface.summary.thirdPartyExecution=false" },
      { id: "release-ready-blocked", result: phase25.summary.releaseReady === false ? "pass" : "block", evidence: "phase25RuntimeSandboxUiSurface.summary.releaseReady=false" }
    ],
    validationScopes: [
      { id: "release-gate-simulation", status: "simulated", source: "phase25.review-package-validation.v1" },
      { id: "runtime-boundary-validation", status: "planned", source: "phase24RuntimeSandboxUxEntry.phase24ClosurePackage" },
      { id: "blocker-clearance-simulation", status: "simulated", source: "phase23ReleaseReadinessReviewUi.phase23ClosureHandoffPackage" },
      { id: "runtime-validation-report", status: "reported", source: "phase26-runtime-validation-report.json" },
      { id: "handoff-criteria", status: "defined", source: "phase26-handoff-criteria.json" },
      { id: "closure-package", status: "closed", source: "phase26-closure-package.json" }
    ],
    simulationPlan: [
      "derive-release-gate-simulation-inputs",
      "map-package-validation-to-release-gates",
      "simulate-blocker-clearance-without-mutation",
      "emit-runtime-validation-report",
      "define-handoff-criteria-for-future-runtime-discussion"
    ],
    blockedActions: [
      "enable-runtime-execution",
      "enable-third-party-execution",
      "execute-third-party-plugin",
      "auto-clear-release-blocker",
      "set-releaseReady-true",
      "persist-simulation-as-runtime-result"
    ],
    exportPolicy: {
      exportName: "phase26-runtime-validation-entry.json",
      includeInOperationsExport: true,
      redaction: "phase25-validation-summary-and-simulation-plan-only"
    },
    releaseGateSimulation,
    blockerClearanceSimulation,
    runtimeValidationReport,
    handoffCriteria,
    closurePackage,
    summary: {
      entryConditions: 5,
      passedEntryConditions: 5,
      validationScopes: 6,
      simulatedGateItems: 5,
      blockedGateItems: 1,
      simulatedClearanceItems: blockingGateItems.length,
      clearedBlockers: 0,
      runtimeValidationReportReady: true,
      handoffCriteriaReady: true,
      closurePackageReady: true,
      phaseClosed: true,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0,
      readyForSimulationVersions: true
    }
  };
}

function buildLocalPhase27ReleaseBlockerGovernanceEntry() {
  const phase26 = buildLocalPhase26RuntimeValidationEntry();
  const closurePackage = phase26.closurePackage || {};
  const carriedForwardBlockers = closurePackage.carriedForwardBlockers || [];
  const carriedBlockerInventory = carriedForwardBlockers.map((item, index) => ({
    id: item.id,
    inventoryId: `phase27-carried-blocker-${index + 1}`,
    source: item.source,
    sourceArtifact: "phase26-closure-package.json",
    sourceClosureDecision: closurePackage.closureDecision?.result,
    severity: "block",
    status: "governance-open",
    inheritedStatus: item.status,
    governanceOwner: "release-reviewer",
    requiredBeforeRelease: item.requiredBeforeRelease || [],
    requiredEvidence: item.requiredBeforeRelease || [],
    evidenceStatus: "pending-human-governance-review",
    blockedReleaseActions: ["mark-releaseReady-true", "clear-carried-forward-blocker"],
    blockedRuntimeActions: ["enable-runtime-execution", "enable-third-party-execution"],
    allowedActions: ["review-blocker-evidence", "export-carried-blocker-inventory"],
    auditState: "inventory-recorded-no-mutation",
    blockedMutation: "clear-carried-forward-blocker"
  }));
  const entryPackage = {
    schemaVersion: "phase27.entry-package.v1",
    version: "1.7.0",
    mode: "readonly-release-blocker-governance-entry",
    sourceClosurePackage: closurePackage.schemaVersion,
    acceptedArtifacts: [
      "phase26-runtime-validation-entry.json",
      "phase26-release-gate-simulation.json",
      "phase26-blocker-clearance-simulation.json",
      "phase26-runtime-validation-report.json",
      "phase26-handoff-criteria.json",
      "phase26-closure-package.json"
    ],
    entryChecks: [
      { id: "phase26-closed", result: closurePackage.summary?.phaseClosed ? "pass" : "block", evidence: "phase26.closure-package.v1" },
      { id: "carried-blockers-present", result: carriedBlockerInventory.length > 0 ? "pass" : "block", evidence: `${carriedBlockerInventory.length} carried blocker` },
      { id: "runtime-disabled", result: closurePackage.summary?.runtimeExecution === false ? "pass" : "block", evidence: "runtimeExecution=false" },
      { id: "third-party-disabled", result: closurePackage.summary?.thirdPartyExecution === false ? "pass" : "block", evidence: "thirdPartyExecution=false" },
      { id: "release-ready-blocked", result: closurePackage.summary?.releaseReady === false ? "pass" : "block", evidence: "releaseReady=false" }
    ],
    exportPolicy: {
      exportName: "phase27-entry-package.json",
      includeInOperationsExport: true,
      redaction: "phase26-closure-summary-and-carried-blockers-only"
    },
    summary: {
      acceptedArtifacts: 6,
      entryChecks: 5,
      passedEntryChecks: 5,
      carriedBlockers: carriedBlockerInventory.length,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0
    }
  };
  const carriedBlockerInventoryPackage = {
    schemaVersion: "phase27.carried-blocker-inventory.v1",
    version: "1.7.1",
    mode: "readonly-carried-blocker-inventory-no-clearance",
    sourceEntryPackage: entryPackage.schemaVersion,
    sourceClosurePackage: closurePackage.schemaVersion,
    inventoryRows: carriedBlockerInventory,
    inventoryChecks: [
      { id: "source-closure-linked", result: closurePackage.schemaVersion === "phase26.closure-package.v1" ? "pass" : "block", evidence: closurePackage.schemaVersion },
      { id: "carried-blockers-indexed", result: carriedBlockerInventory.length > 0 ? "pass" : "block", evidence: `${carriedBlockerInventory.length} inventory row` },
      { id: "all-blockers-open", result: carriedBlockerInventory.every((item) => item.status === "governance-open") ? "pass" : "block", evidence: "no blocker cleared by inventory" },
      { id: "release-mutation-blocked", result: "pass", evidence: "mark-releaseReady-true remains blocked" },
      { id: "runtime-mutation-blocked", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "clear-carried-forward-blocker",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-inventory-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase27-carried-blocker-inventory.json",
      includeInOperationsExport: true,
      redaction: "blocker-ids-sources-required-evidence-only"
    },
    summary: {
      inventoryReady: true,
      totalBlockers: carriedBlockerInventory.length,
      openBlockers: carriedBlockerInventory.filter((item) => item.status === "governance-open").length,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const signoffQueue = carriedBlockerInventory.map((item) => ({
    signoffId: `phase27-signoff-${item.inventoryId}`,
    blockerId: item.id,
    inventoryId: item.inventoryId,
    reviewerRole: item.governanceOwner,
    requiredDisposition: "review-required-before-release",
    currentDisposition: "pending-review",
    evidenceStatus: item.evidenceStatus,
    requiredEvidence: item.requiredEvidence,
    allowedActions: ["record-review-note", "export-human-review-signoff"],
    blockedActions: [
      "approve-release-from-signoff",
      "clear-blocker-from-signoff",
      "enable-runtime-from-signoff"
    ],
    auditState: "signoff-pending-no-mutation"
  }));
  const humanReviewSignoffPackage = {
    schemaVersion: "phase27.human-review-signoff.v1",
    version: "1.7.2",
    mode: "readonly-human-review-signoff-no-release-approval",
    sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
    sourceClosurePackage: closurePackage.schemaVersion,
    signoffQueue,
    signoffChecks: [
      { id: "inventory-package-linked", result: carriedBlockerInventoryPackage.schemaVersion === "phase27.carried-blocker-inventory.v1" ? "pass" : "block", evidence: carriedBlockerInventoryPackage.schemaVersion },
      { id: "all-carried-blockers-have-signoff", result: signoffQueue.length === carriedBlockerInventory.length ? "pass" : "block", evidence: `${signoffQueue.length} signoff row` },
      { id: "signoff-does-not-clear-blockers", result: "pass", evidence: "currentDisposition=pending-review" },
      { id: "signoff-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "approve-release-from-signoff",
      "clear-blocker-from-signoff",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "persist-signoff-as-release-approval"
    ],
    exportPolicy: {
      exportName: "phase27-human-review-signoff.json",
      includeInOperationsExport: true,
      redaction: "reviewer-role-blocker-id-required-evidence-only"
    },
    summary: {
      signoffReady: true,
      requiredSignoffs: carriedBlockerInventory.length,
      pendingSignoffs: carriedBlockerInventory.length,
      approvedReleaseSignoffs: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const evidenceGapRows = carriedBlockerInventory.flatMap((item) => {
    const requiredEvidence = item.requiredEvidence?.length ? item.requiredEvidence : ["human-review"];
    const signoff = signoffQueue.find((queueItem) => queueItem.inventoryId === item.inventoryId);
    return requiredEvidence.map((evidenceId, index) => {
      const evidenceState = evidenceId === "explicit-release-readiness-decision" ? "missing" : "pending";
      return {
        gapId: `phase27-gap-${item.inventoryId}-${index + 1}`,
        blockerId: item.id,
        inventoryId: item.inventoryId,
        requiredEvidence: evidenceId,
        currentEvidenceState: evidenceState,
        sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
        sourceSignoffPackage: humanReviewSignoffPackage.schemaVersion,
        sourceClosurePackage: closurePackage.schemaVersion,
        currentEvidenceSource: evidenceState === "pending" ? signoff?.signoffId || item.sourceArtifact : "none-recorded",
        gapState: evidenceState === "missing" ? "open-missing-evidence" : "open-pending-review",
        releaseImpact: "releaseReady-remains-blocked",
        allowedActions: ["record-evidence-review-note", "export-evidence-gap-matrix"],
        blockedActions: [
          "resolve-gap-as-clearance",
          "clear-blocker-from-gap",
          "mark-releaseReady-true"
        ],
        auditState: "gap-recorded-no-mutation"
      };
    });
  });
  const representedEvidenceGapBlockers = new Set(evidenceGapRows.map((item) => item.blockerId)).size;
  const pendingEvidenceGaps = evidenceGapRows.filter((item) => item.currentEvidenceState === "pending").length;
  const missingEvidenceGaps = evidenceGapRows.filter((item) => item.currentEvidenceState === "missing").length;
  const evidenceGapMatrixPackage = {
    schemaVersion: "phase27.evidence-gap-matrix.v1",
    version: "1.7.3",
    mode: "readonly-evidence-gap-matrix-no-clearance",
    sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
    sourceSignoffPackage: humanReviewSignoffPackage.schemaVersion,
    sourceClosurePackage: closurePackage.schemaVersion,
    matrixRows: evidenceGapRows,
    matrixChecks: [
      { id: "inventory-package-linked", result: carriedBlockerInventoryPackage.schemaVersion === "phase27.carried-blocker-inventory.v1" ? "pass" : "block", evidence: carriedBlockerInventoryPackage.schemaVersion },
      { id: "signoff-package-linked", result: humanReviewSignoffPackage.schemaVersion === "phase27.human-review-signoff.v1" ? "pass" : "block", evidence: humanReviewSignoffPackage.schemaVersion },
      { id: "all-blockers-represented", result: representedEvidenceGapBlockers === carriedBlockerInventory.length ? "pass" : "block", evidence: `${representedEvidenceGapBlockers} represented blocker` },
      { id: "open-gaps-preserve-blockers", result: evidenceGapRows.length > 0 ? "pass" : "block", evidence: `${evidenceGapRows.length} open evidence gap` },
      { id: "gap-matrix-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "resolve-gap-as-clearance",
      "clear-blocker-from-gap",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "persist-gap-resolution-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase27-evidence-gap-matrix.json",
      includeInOperationsExport: true,
      redaction: "blocker-id-required-evidence-gap-state-only"
    },
    summary: {
      gapMatrixReady: true,
      matrixRows: evidenceGapRows.length,
      representedBlockers: representedEvidenceGapBlockers,
      openGaps: evidenceGapRows.length,
      pendingEvidenceGaps,
      missingEvidenceGaps,
      resolvedGaps: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const blockerResolutionPlanItems = carriedBlockerInventory.map((item) => {
    const relatedGaps = evidenceGapRows.filter((gap) => gap.inventoryId === item.inventoryId);
    return {
      planId: `phase27-resolution-plan-${item.inventoryId}`,
      blockerId: item.id,
      inventoryId: item.inventoryId,
      ownerRole: item.governanceOwner,
      sourceEvidenceGapMatrix: evidenceGapMatrixPackage.schemaVersion,
      sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
      requiredEvidence: item.requiredEvidence,
      openEvidenceGaps: relatedGaps.length,
      missingEvidenceGaps: relatedGaps.filter((gap) => gap.currentEvidenceState === "missing").length,
      pendingEvidenceGaps: relatedGaps.filter((gap) => gap.currentEvidenceState === "pending").length,
      currentPlanState: "planned-not-started",
      blockerDisposition: "governance-open",
      proposedResolutionSteps: [
        "review-human-signoff-note",
        "collect-required-evidence-without-runtime",
        "record-explicit-release-readiness-decision",
        "keep-release-blocked-until-authorized-clearance"
      ],
      requiredBeforeAnyClearance: relatedGaps.map((gap) => gap.requiredEvidence),
      allowedActions: ["record-resolution-note", "export-blocker-resolution-plan"],
      blockedActions: [
        "apply-resolution-as-clearance",
        "clear-blocker-from-plan",
        "mark-releaseReady-true",
        "enable-runtime-from-plan"
      ],
      auditState: "resolution-plan-recorded-no-mutation"
    };
  });
  const blockerResolutionPlanPackage = {
    schemaVersion: "phase27.blocker-resolution-plan.v1",
    version: "1.7.4",
    mode: "readonly-blocker-resolution-plan-no-clearance",
    sourceEvidenceGapMatrixPackage: evidenceGapMatrixPackage.schemaVersion,
    sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
    sourceSignoffPackage: humanReviewSignoffPackage.schemaVersion,
    resolutionPlanItems: blockerResolutionPlanItems,
    planChecks: [
      { id: "evidence-gap-matrix-linked", result: evidenceGapMatrixPackage.schemaVersion === "phase27.evidence-gap-matrix.v1" ? "pass" : "block", evidence: evidenceGapMatrixPackage.schemaVersion },
      { id: "all-open-blockers-have-plan", result: blockerResolutionPlanItems.length === carriedBlockerInventory.length ? "pass" : "block", evidence: `${blockerResolutionPlanItems.length} resolution plan item` },
      { id: "all-plan-items-keep-blockers-open", result: blockerResolutionPlanItems.every((item) => item.blockerDisposition === "governance-open") ? "pass" : "block", evidence: "no blocker cleared by plan" },
      { id: "plan-does-not-resolve-gaps", result: "pass", evidence: "resolvedGaps=0" },
      { id: "plan-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "apply-resolution-as-clearance",
      "clear-blocker-from-plan",
      "resolve-gap-from-plan",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "persist-resolution-plan-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase27-blocker-resolution-plan.json",
      includeInOperationsExport: true,
      redaction: "blocker-id-plan-state-required-evidence-only"
    },
    summary: {
      blockerResolutionPlanReady: true,
      plannedBlockers: blockerResolutionPlanItems.length,
      openPlanItems: blockerResolutionPlanItems.filter((item) => item.currentPlanState === "planned-not-started").length,
      openEvidenceGaps: evidenceGapRows.length,
      resolvedGaps: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const phase27ClosurePackage = {
    schemaVersion: "phase27.closure-package.v1",
    version: "1.7.5",
    mode: "readonly-phase27-closure-no-release-approval",
    sourceEntryPackage: entryPackage.schemaVersion,
    sourceInventoryPackage: carriedBlockerInventoryPackage.schemaVersion,
    sourceSignoffPackage: humanReviewSignoffPackage.schemaVersion,
    sourceEvidenceGapMatrixPackage: evidenceGapMatrixPackage.schemaVersion,
    sourceResolutionPlanPackage: blockerResolutionPlanPackage.schemaVersion,
    closureItems: [
      { id: "entry-package", status: "closed-readonly", artifact: "phase27-entry-package.json" },
      { id: "carried-blocker-inventory", status: "closed-readonly", artifact: "phase27-carried-blocker-inventory.json" },
      { id: "human-review-signoff", status: "closed-readonly", artifact: "phase27-human-review-signoff.json" },
      { id: "evidence-gap-matrix", status: "closed-readonly", artifact: "phase27-evidence-gap-matrix.json" },
      { id: "blocker-resolution-plan", status: "closed-readonly", artifact: "phase27-blocker-resolution-plan.json" }
    ],
    carriedForwardBlockers: carriedBlockerInventory.map((item) => ({
      id: item.id,
      inventoryId: item.inventoryId,
      source: item.source,
      status: "carried-forward-open",
      requiredBeforeRelease: item.requiredEvidence,
      blockerDisposition: "governance-open",
      releaseImpact: "releaseReady-remains-blocked"
    })),
    closureChecks: [
      { id: "entry-package-ready", result: entryPackage.summary?.passedEntryChecks === entryPackage.summary?.entryChecks ? "pass" : "block", evidence: entryPackage.schemaVersion },
      { id: "inventory-ready", result: carriedBlockerInventoryPackage.summary?.inventoryReady ? "pass" : "block", evidence: carriedBlockerInventoryPackage.schemaVersion },
      { id: "signoff-ready-without-approval", result: humanReviewSignoffPackage.summary?.signoffReady && humanReviewSignoffPackage.summary?.approvedReleaseSignoffs === 0 ? "pass" : "block", evidence: humanReviewSignoffPackage.schemaVersion },
      { id: "gap-matrix-ready-with-open-gaps", result: evidenceGapMatrixPackage.summary?.gapMatrixReady && evidenceGapMatrixPackage.summary?.openGaps > 0 ? "pass" : "block", evidence: evidenceGapMatrixPackage.schemaVersion },
      { id: "resolution-plan-ready-without-clearance", result: blockerResolutionPlanPackage.summary?.blockerResolutionPlanReady && blockerResolutionPlanPackage.summary?.clearedBlockers === 0 ? "pass" : "block", evidence: blockerResolutionPlanPackage.schemaVersion },
      { id: "release-remains-blocked", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    closureDecision: {
      result: "phase27-closed-with-open-release-blockers",
      phaseClosed: true,
      nextStagePlanningAllowed: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: "Phase 27 is closed as a read-only release blocker governance stage with blockers carried forward"
    },
    blockedMutations: [
      "treat-closure-as-release-approval",
      "clear-carried-forward-blocker",
      "resolve-gap-from-closure",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "persist-closure-as-release-clearance"
    ],
    exportPolicy: {
      exportName: "phase27-closure-package.json",
      includeInOperationsExport: true,
      redaction: "closure-items-carried-blockers-and-guardrails-only"
    },
    summary: {
      phaseClosed: true,
      nextStagePlanningAllowed: true,
      closureItems: 5,
      carriedForwardBlockers: carriedBlockerInventory.length,
      openEvidenceGaps: evidenceGapRows.length,
      resolvedGaps: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  return {
    phase: 27,
    schemaVersion: "phase27.release-blocker-governance-entry.v1",
    version: "1.7.5",
    status: "phase27-closure-package-ready-runtime-disabled",
    entryMode: "release-blocker-governance-no-runtime-execution",
    sourcePhase26: {
      schemaVersion: phase26.schemaVersion,
      closurePackage: closurePackage.schemaVersion,
      closureDecision: closurePackage.closureDecision?.result,
      phase26Closed: closurePackage.summary?.phaseClosed === true,
      phase26ReleaseReady: phase26.summary?.releaseReady,
      phase26RuntimeExecution: phase26.summary?.runtimeExecution,
      phase26ThirdPartyExecution: phase26.summary?.thirdPartyExecution
    },
    entryPackage,
    carriedBlockerInventory,
    carriedBlockerInventoryPackage,
    humanReviewSignoffPackage,
    evidenceGapMatrixPackage,
    blockerResolutionPlanPackage,
    phase27ClosurePackage,
    governanceScopes: [
      { id: "carried-blocker-inventory", status: "ready", source: "phase27.carried-blocker-inventory.v1" },
      { id: "human-review-signoff", status: "ready", source: "phase27.human-review-signoff.v1" },
      { id: "evidence-gap-matrix", status: "ready", source: "phase27.evidence-gap-matrix.v1" },
      { id: "blocker-resolution-plan", status: "ready", source: "phase27.blocker-resolution-plan.v1" },
      { id: "phase27-closure-package", status: "closed", source: "phase27.closure-package.v1" }
    ],
    blockedActions: [
      "clear-carried-forward-blocker",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "treat-entry-package-as-release-approval",
      "persist-signoff-as-release-approval",
      "persist-gap-resolution-as-clearance",
      "persist-resolution-plan-as-clearance",
      "persist-closure-as-release-clearance",
      "persist-governance-entry-as-runtime-result"
    ],
    entryDecision: {
      result: "phase27-entry-ready-with-carried-blockers",
      governanceReady: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0,
      reason: "Phase 27 can start release blocker governance from the Phase 26 closure package without clearing blockers or enabling runtime"
    },
    exportPolicy: {
      exportName: "phase27-release-blocker-governance-entry.json",
      includeInOperationsExport: true,
      redaction: "entry-package-scopes-and-carried-blockers-only"
    },
    summary: {
      carriedBlockers: carriedBlockerInventory.length,
      inventoryReady: true,
      signoffReady: true,
      gapMatrixReady: true,
      blockerResolutionPlanReady: true,
      closurePackageReady: true,
      phaseClosed: true,
      nextStagePlanningAllowed: true,
      openEvidenceGaps: evidenceGapRows.length,
      missingEvidenceGaps,
      pendingEvidenceGaps,
      governanceScopes: 5,
      governanceReady: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase28ClearanceReviewEntry() {
  const phase27 = buildLocalPhase27ReleaseBlockerGovernanceEntry();
  const closurePackage = phase27.phase27ClosurePackage || {};
  const carriedForwardBlockers = closurePackage.carriedForwardBlockers || [];
  const clearanceReviewQueue = carriedForwardBlockers.map((item, index) => ({
    reviewItemId: `phase28-clearance-review-${index + 1}`,
    blockerId: item.id,
    inventoryId: item.inventoryId,
    source: item.source,
    requiredEvidence: item.requiredBeforeRelease || [],
    reviewState: "pending-clearance-review",
    blockerDisposition: "carried-forward-open",
    allowedActions: ["record-clearance-review-note", "export-clearance-review-entry"],
    blockedActions: [
      "clear-blocker-from-review",
      "mark-releaseReady-true",
      "enable-runtime-from-review"
    ],
    auditState: "clearance-review-open-no-mutation"
  }));
  const entryChecks = [
    { id: "phase27-closure-linked", result: closurePackage.schemaVersion === "phase27.closure-package.v1" ? "pass" : "block", evidence: closurePackage.schemaVersion },
    { id: "phase27-closed", result: closurePackage.summary?.phaseClosed === true ? "pass" : "block", evidence: `phaseClosed=${closurePackage.summary?.phaseClosed === true}` },
    { id: "carried-blockers-present", result: carriedForwardBlockers.length > 0 ? "pass" : "block", evidence: `${carriedForwardBlockers.length} carried blocker` },
    { id: "blockers-remain-open", result: carriedForwardBlockers.every((item) => item.blockerDisposition === "governance-open" || item.status === "carried-forward-open") ? "pass" : "block", evidence: "no carried blocker cleared" },
    { id: "no-clearance-applied", result: "pass", evidence: "clearedBlockers=0" },
    { id: "release-remains-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
  ];
  const evidenceLedgerEntries = clearanceReviewQueue.flatMap((item) => {
    const requirements = item.requiredEvidence?.length ? item.requiredEvidence : ["explicit-human-clearance-evidence"];
    return requirements.map((requirement, index) => ({
      ledgerEntryId: `${item.reviewItemId}-evidence-${index + 1}`,
      reviewItemId: item.reviewItemId,
      blockerId: item.blockerId,
      inventoryId: item.inventoryId,
      evidenceRequirement: requirement,
      intakeState: "pending-evidence-intake",
      evidenceDisposition: "not-submitted",
      sourceReviewState: item.reviewState,
      allowedActions: ["record-evidence-intake-note", "export-evidence-intake-ledger"],
      blockedActions: [
        "accept-evidence-as-clearance",
        "clear-blocker-from-evidence",
        "mark-releaseReady-true",
        "enable-runtime-from-evidence"
      ],
      auditState: "evidence-intake-open-no-mutation"
    }));
  });
  const evidenceIntakeLedger = {
    schemaVersion: "phase28.evidence-intake-ledger.v1",
    version: "1.8.1",
    mode: "readonly-evidence-intake-ledger-no-clearance",
    sourceClearanceReviewEntry: "phase28.clearance-review-entry.v1",
    sourcePhase27ClosurePackage: closurePackage.schemaVersion,
    ledgerEntries: evidenceLedgerEntries,
    ledgerChecks: [
      { id: "clearance-review-linked", result: "pass", evidence: "phase28.clearance-review-entry.v1" },
      { id: "all-review-items-represented", result: new Set(evidenceLedgerEntries.map((item) => item.reviewItemId)).size === clearanceReviewQueue.length ? "pass" : "block", evidence: `${evidenceLedgerEntries.length} evidence intake ledger entry` },
      { id: "evidence-requirements-present", result: evidenceLedgerEntries.length > 0 ? "pass" : "block", evidence: `${evidenceLedgerEntries.length} evidence requirement` },
      { id: "ledger-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "ledger-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "accept-evidence-as-clearance",
      "clear-blocker-from-evidence",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-evidence-intake-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase28-evidence-intake-ledger.json",
      includeInOperationsExport: true,
      redaction: "ledger-entry-review-item-blocker-id-and-evidence-requirement-only"
    },
    summary: {
      evidenceIntakeLedgerReady: true,
      ledgerEntries: evidenceLedgerEntries.length,
      pendingEvidenceIntake: evidenceLedgerEntries.filter((item) => item.intakeState === "pending-evidence-intake").length,
      acceptedEvidenceForClearance: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const reviewerDispositionQueue = evidenceLedgerEntries.map((item, index) => ({
    dispositionId: `phase28-reviewer-disposition-${index + 1}`,
    ledgerEntryId: item.ledgerEntryId,
    reviewItemId: item.reviewItemId,
    blockerId: item.blockerId,
    inventoryId: item.inventoryId,
    reviewerRole: index % 2 === 0 ? "release-governance-reviewer" : "evidence-reviewer",
    sourceEvidenceRequirement: item.evidenceRequirement,
    sourceIntakeState: item.intakeState,
    dispositionState: "pending-reviewer-disposition",
    proposedDisposition: "hold-for-evidence",
    releaseImpact: "releaseReady-remains-blocked",
    allowedActions: ["record-reviewer-disposition-note", "export-reviewer-disposition-model"],
    blockedActions: [
      "apply-disposition-as-clearance",
      "clear-blocker-from-disposition",
      "mark-releaseReady-true",
      "enable-runtime-from-disposition"
    ],
    auditState: "reviewer-disposition-open-no-mutation"
  }));
  const reviewerDispositionModel = {
    schemaVersion: "phase28.reviewer-disposition-model.v1",
    version: "1.8.2",
    mode: "readonly-reviewer-disposition-model-no-clearance",
    sourceEvidenceIntakeLedger: evidenceIntakeLedger.schemaVersion,
    sourceClearanceReviewEntry: "phase28.clearance-review-entry.v1",
    dispositionQueue: reviewerDispositionQueue,
    dispositionChecks: [
      { id: "evidence-ledger-linked", result: evidenceIntakeLedger.schemaVersion === "phase28.evidence-intake-ledger.v1" ? "pass" : "block", evidence: evidenceIntakeLedger.schemaVersion },
      { id: "all-ledger-entries-have-disposition", result: reviewerDispositionQueue.length === evidenceLedgerEntries.length ? "pass" : "block", evidence: `${reviewerDispositionQueue.length} reviewer disposition` },
      { id: "all-dispositions-pending", result: reviewerDispositionQueue.every((item) => item.dispositionState === "pending-reviewer-disposition") ? "pass" : "block", evidence: "no disposition approved" },
      { id: "disposition-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "disposition-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "apply-disposition-as-clearance",
      "clear-blocker-from-disposition",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-reviewer-disposition-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase28-reviewer-disposition-model.json",
      includeInOperationsExport: true,
      redaction: "disposition-id-reviewer-role-blocker-id-and-state-only"
    },
    summary: {
      reviewerDispositionModelReady: true,
      dispositions: reviewerDispositionQueue.length,
      pendingDispositions: reviewerDispositionQueue.filter((item) => item.dispositionState === "pending-reviewer-disposition").length,
      approvedReleaseDispositions: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const clearanceCriteriaItems = reviewerDispositionQueue.map((item, index) => ({
    criteriaId: `phase28-clearance-criteria-${index + 1}`,
    dispositionId: item.dispositionId,
    ledgerEntryId: item.ledgerEntryId,
    reviewItemId: item.reviewItemId,
    blockerId: item.blockerId,
    inventoryId: item.inventoryId,
    reviewerRole: item.reviewerRole,
    sourceDispositionState: item.dispositionState,
    criteriaState: "pending-clearance-criteria",
    criteriaDisposition: "not-satisfied",
    requiredCriteria: [
      "evidence-intake-reviewed",
      "reviewer-disposition-recorded",
      "blocker-clearance-evidence-attached",
      "runtime-and-release-guardrails-verified"
    ],
    allowedActions: ["record-clearance-criteria-note", "export-clearance-criteria-checklist"],
    blockedActions: [
      "mark-criteria-satisfied",
      "apply-criteria-as-clearance",
      "clear-blocker-from-criteria",
      "mark-releaseReady-true",
      "enable-runtime-from-criteria"
    ],
    auditState: "clearance-criteria-open-no-mutation"
  }));
  const clearanceCriteriaChecklist = {
    schemaVersion: "phase28.clearance-criteria-checklist.v1",
    version: "1.8.3",
    mode: "readonly-clearance-criteria-checklist-no-clearance",
    sourceReviewerDispositionModel: reviewerDispositionModel.schemaVersion,
    sourceEvidenceIntakeLedger: evidenceIntakeLedger.schemaVersion,
    sourceClearanceReviewEntry: "phase28.clearance-review-entry.v1",
    checklistItems: clearanceCriteriaItems,
    checklistChecks: [
      { id: "reviewer-disposition-linked", result: reviewerDispositionModel.schemaVersion === "phase28.reviewer-disposition-model.v1" ? "pass" : "block", evidence: reviewerDispositionModel.schemaVersion },
      { id: "all-dispositions-have-criteria", result: clearanceCriteriaItems.length === reviewerDispositionQueue.length ? "pass" : "block", evidence: `${clearanceCriteriaItems.length} clearance criteria item` },
      { id: "all-criteria-pending", result: clearanceCriteriaItems.every((item) => item.criteriaState === "pending-clearance-criteria") ? "pass" : "block", evidence: "no criteria satisfied" },
      { id: "criteria-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "criteria-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "mark-criteria-satisfied",
      "apply-criteria-as-clearance",
      "clear-blocker-from-criteria",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-clearance-criteria-as-clearance"
    ],
    exportPolicy: {
      exportName: "phase28-clearance-criteria-checklist.json",
      includeInOperationsExport: true,
      redaction: "criteria-id-disposition-id-blocker-id-and-state-only"
    },
    summary: {
      clearanceCriteriaChecklistReady: true,
      criteriaItems: clearanceCriteriaItems.length,
      pendingCriteriaItems: clearanceCriteriaItems.filter((item) => item.criteriaState === "pending-clearance-criteria").length,
      satisfiedCriteriaItems: 0,
      approvedReleaseCriteria: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const clearanceDecisionPreviewItems = clearanceCriteriaItems.map((item, index) => ({
    previewId: `phase28-clearance-decision-preview-${index + 1}`,
    criteriaId: item.criteriaId,
    dispositionId: item.dispositionId,
    ledgerEntryId: item.ledgerEntryId,
    reviewItemId: item.reviewItemId,
    blockerId: item.blockerId,
    inventoryId: item.inventoryId,
    sourceCriteriaState: item.criteriaState,
    sourceCriteriaDisposition: item.criteriaDisposition,
    previewState: "decision-preview-open",
    previewDecision: "hold-clearance",
    decisionReason: "criteria-not-satisfied-and-blocker-carried-forward",
    releaseImpact: "releaseReady-remains-blocked",
    allowedActions: ["record-decision-preview-note", "export-clearance-decision-preview"],
    blockedActions: [
      "approve-release-from-preview",
      "apply-decision-preview-as-clearance",
      "clear-blocker-from-preview",
      "mark-releaseReady-true",
      "enable-runtime-from-preview"
    ],
    auditState: "decision-preview-open-no-mutation"
  }));
  const clearanceDecisionPreview = {
    schemaVersion: "phase28.clearance-decision-preview.v1",
    version: "1.8.4",
    mode: "readonly-clearance-decision-preview-no-release",
    sourceClearanceCriteriaChecklist: clearanceCriteriaChecklist.schemaVersion,
    sourceReviewerDispositionModel: reviewerDispositionModel.schemaVersion,
    sourceClearanceReviewEntry: "phase28.clearance-review-entry.v1",
    previewItems: clearanceDecisionPreviewItems,
    previewChecks: [
      { id: "criteria-checklist-linked", result: clearanceCriteriaChecklist.schemaVersion === "phase28.clearance-criteria-checklist.v1" ? "pass" : "block", evidence: clearanceCriteriaChecklist.schemaVersion },
      { id: "all-criteria-have-preview", result: clearanceDecisionPreviewItems.length === clearanceCriteriaItems.length ? "pass" : "block", evidence: `${clearanceDecisionPreviewItems.length} decision preview item` },
      { id: "all-previews-hold-clearance", result: clearanceDecisionPreviewItems.every((item) => item.previewDecision === "hold-clearance") ? "pass" : "block", evidence: "no preview approval" },
      { id: "preview-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "preview-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "approve-release-from-preview",
      "apply-decision-preview-as-clearance",
      "clear-blocker-from-preview",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-decision-preview-as-clearance",
      "apply-decision-preview-as-release-approval"
    ],
    exportPolicy: {
      exportName: "phase28-clearance-decision-preview.json",
      includeInOperationsExport: true,
      redaction: "preview-id-criteria-id-blocker-id-decision-and-state-only"
    },
    summary: {
      clearanceDecisionPreviewReady: true,
      previewItems: clearanceDecisionPreviewItems.length,
      holdClearancePreviews: clearanceDecisionPreviewItems.filter((item) => item.previewDecision === "hold-clearance").length,
      approvedReleasePreviews: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  const phase28ClosurePackage = {
    schemaVersion: "phase28.closure-package.v1",
    version: "1.8.5",
    mode: "readonly-phase28-closure-no-release-approval",
    sourceClearanceReviewEntry: "phase28.clearance-review-entry.v1",
    sourceEvidenceIntakeLedger: evidenceIntakeLedger.schemaVersion,
    sourceReviewerDispositionModel: reviewerDispositionModel.schemaVersion,
    sourceClearanceCriteriaChecklist: clearanceCriteriaChecklist.schemaVersion,
    sourceClearanceDecisionPreview: clearanceDecisionPreview.schemaVersion,
    closureItems: [
      { id: "clearance-review-entry", schemaVersion: "phase28.clearance-review-entry.v1", state: "closed-with-open-blockers-carried-forward", ready: true, releaseReady: false, runtimeExecution: false, persistedMutations: 0 },
      { id: "evidence-intake-ledger", schemaVersion: evidenceIntakeLedger.schemaVersion, state: "closed-readonly-no-clearance", ready: evidenceIntakeLedger.summary.evidenceIntakeLedgerReady, releaseReady: false, runtimeExecution: false, persistedMutations: 0 },
      { id: "reviewer-disposition-model", schemaVersion: reviewerDispositionModel.schemaVersion, state: "closed-readonly-no-release-approval", ready: reviewerDispositionModel.summary.reviewerDispositionModelReady, releaseReady: false, runtimeExecution: false, persistedMutations: 0 },
      { id: "clearance-criteria-checklist", schemaVersion: clearanceCriteriaChecklist.schemaVersion, state: "closed-readonly-criteria-not-satisfied", ready: clearanceCriteriaChecklist.summary.clearanceCriteriaChecklistReady, releaseReady: false, runtimeExecution: false, persistedMutations: 0 },
      { id: "clearance-decision-preview", schemaVersion: clearanceDecisionPreview.schemaVersion, state: "closed-readonly-hold-clearance", ready: clearanceDecisionPreview.summary.clearanceDecisionPreviewReady, releaseReady: false, runtimeExecution: false, persistedMutations: 0 }
    ],
    closureChecks: [
      { id: "clearance-review-entry-linked", result: "pass", evidence: "phase28.clearance-review-entry.v1" },
      { id: "evidence-intake-ledger-linked", result: evidenceIntakeLedger.schemaVersion === "phase28.evidence-intake-ledger.v1" ? "pass" : "block", evidence: evidenceIntakeLedger.schemaVersion },
      { id: "reviewer-disposition-linked", result: reviewerDispositionModel.schemaVersion === "phase28.reviewer-disposition-model.v1" ? "pass" : "block", evidence: reviewerDispositionModel.schemaVersion },
      { id: "criteria-checklist-linked", result: clearanceCriteriaChecklist.schemaVersion === "phase28.clearance-criteria-checklist.v1" ? "pass" : "block", evidence: clearanceCriteriaChecklist.schemaVersion },
      { id: "decision-preview-linked", result: clearanceDecisionPreview.schemaVersion === "phase28.clearance-decision-preview.v1" ? "pass" : "block", evidence: clearanceDecisionPreview.schemaVersion },
      { id: "decision-preview-holds-clearance", result: clearanceDecisionPreview.summary.holdClearancePreviews === clearanceDecisionPreviewItems.length ? "pass" : "block", evidence: `${clearanceDecisionPreview.summary.holdClearancePreviews} hold preview` },
      { id: "blockers-carried-forward", result: "pass", evidence: `carriedForwardBlockers=${clearanceReviewQueue.length}; clearedBlockers=0` },
      { id: "closure-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "persist-phase28-closure-as-release-approval",
      "clear-blocker-from-phase28-closure",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "auto-enter-release-from-closure"
    ],
    exportPolicy: {
      exportName: "phase28-closure-package.json",
      includeInOperationsExport: true,
      redaction: "closure-item-schema-state-and-guardrail-summary-only"
    },
    handoff: {
      phase28Closed: true,
      nextPhaseEntryReady: true,
      carriedForwardBlockers: clearanceReviewQueue.length,
      recommendedNextStage: "phase29-release-governance-planning",
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false
    },
    summary: {
      phase28ClosureReady: true,
      phaseClosed: true,
      closureItems: 5,
      closureChecks: 9,
      carriedForwardBlockers: clearanceReviewQueue.length,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
  return {
    phase: 28,
    schemaVersion: "phase28.clearance-review-entry.v1",
    version: "1.8.5",
    status: "phase28-closure-package-ready-runtime-disabled",
    entryMode: "release-blocker-clearance-review-no-runtime-execution",
    sourcePhase27: {
      schemaVersion: phase27.schemaVersion,
      closurePackage: closurePackage.schemaVersion,
      phase27Closed: closurePackage.summary?.phaseClosed === true,
      phase27ReleaseReady: phase27.summary?.releaseReady,
      phase27RuntimeExecution: phase27.summary?.runtimeExecution,
      phase27ThirdPartyExecution: phase27.summary?.thirdPartyExecution
    },
    clearanceReviewQueue,
    evidenceIntakeLedger,
    reviewerDispositionModel,
    clearanceCriteriaChecklist,
    clearanceDecisionPreview,
    phase28ClosurePackage,
    entryChecks,
    blockedMutations: [
      "clear-blocker-from-review",
      "apply-clearance-review-as-clearance",
      "persist-evidence-intake-as-clearance",
      "persist-reviewer-disposition-as-clearance",
      "persist-clearance-criteria-as-clearance",
      "persist-decision-preview-as-clearance",
      "apply-decision-preview-as-release-approval",
      "persist-phase28-closure-as-release-approval",
      "clear-blocker-from-phase28-closure",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-clearance-review-as-release-approval"
    ],
    exportPolicy: {
      exportName: "phase28-clearance-review-entry.json",
      includeInOperationsExport: true,
      redaction: "review-item-blocker-id-required-evidence-and-guardrails-only"
    },
    summary: {
      clearanceReviewEntryReady: true,
      evidenceIntakeLedgerReady: true,
      reviewerDispositionModelReady: true,
      clearanceCriteriaChecklistReady: true,
      clearanceDecisionPreviewReady: true,
      phase28ClosureReady: true,
      phase28Closed: true,
      reviewItems: clearanceReviewQueue.length,
      pendingReviewItems: clearanceReviewQueue.filter((item) => item.reviewState === "pending-clearance-review").length,
      evidenceLedgerEntries: evidenceLedgerEntries.length,
      pendingEvidenceIntake: evidenceIntakeLedger.summary.pendingEvidenceIntake,
      reviewerDispositions: reviewerDispositionQueue.length,
      pendingReviewerDispositions: reviewerDispositionModel.summary.pendingDispositions,
      clearanceCriteriaItems: clearanceCriteriaItems.length,
      pendingClearanceCriteria: clearanceCriteriaChecklist.summary.pendingCriteriaItems,
      satisfiedClearanceCriteria: 0,
      approvedReleaseCriteria: 0,
      clearanceDecisionPreviews: clearanceDecisionPreviewItems.length,
      holdClearancePreviews: clearanceDecisionPreview.summary.holdClearancePreviews,
      approvedReleasePreviews: 0,
      closureItems: phase28ClosurePackage.summary.closureItems,
      closureChecks: phase28ClosurePackage.summary.closureChecks,
      carriedForwardBlockers: phase28ClosurePackage.summary.carriedForwardBlockers,
      approvedReleaseDispositions: 0,
      acceptedEvidenceForClearance: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29GovernanceDashboardExport(blockerClearanceWorkflow = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}, gateSeparationModel = []) {
  const dashboardSections = [
    {
      id: "blocker-clearance-workflow",
      label: "Blocker clearance workflow",
      sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion,
      state: blockerClearanceWorkflow.summary?.workflowPlanningReady ? "ready-readonly" : "blocked",
      exportName: blockerClearanceWorkflow.exportPolicy?.exportName,
      keyMetrics: {
        pendingWorkflowItems: blockerClearanceWorkflow.summary?.pendingWorkflowItems || 0,
        clearedBlockers: blockerClearanceWorkflow.summary?.clearedBlockers || 0,
        approvedClearanceItems: blockerClearanceWorkflow.summary?.approvedClearanceItems || 0
      }
    },
    {
      id: "release-approval-state-model",
      label: "Release approval state model",
      sourceSchemaVersion: releaseApprovalStateModel.schemaVersion,
      state: releaseApprovalStateModel.summary?.approvalStateModelReady ? "ready-readonly" : "blocked",
      exportName: releaseApprovalStateModel.exportPolicy?.exportName,
      keyMetrics: {
        approvalStates: releaseApprovalStateModel.summary?.approvalStates || 0,
        blockedTransitions: releaseApprovalStateModel.summary?.blockedTransitions || 0,
        approvedReleaseStates: releaseApprovalStateModel.summary?.approvedReleaseStates || 0
      }
    },
    {
      id: "runtime-enable-governance",
      label: "Runtime enable governance",
      sourceSchemaVersion: runtimeEnableGovernance.schemaVersion,
      state: runtimeEnableGovernance.summary?.runtimeEnableGovernanceReady ? "ready-readonly" : "blocked",
      exportName: runtimeEnableGovernance.exportPolicy?.exportName,
      keyMetrics: {
        runtimeGates: runtimeEnableGovernance.summary?.runtimeGates || 0,
        planningOnlyGates: runtimeEnableGovernance.summary?.planningOnlyGates || 0,
        runtimeEnableApprovals: runtimeEnableGovernance.summary?.runtimeEnableApprovals || 0
      }
    },
    {
      id: "gate-separation",
      label: "Gate separation",
      sourceSchemaVersion: "phase29.release-governance-planning.v1",
      state: gateSeparationModel.every((item) => item.mayApproveRelease === false && item.mayEnableRuntime === false) ? "ready-readonly" : "blocked",
      exportName: "phase29-release-governance-planning.json",
      keyMetrics: {
        separatedGates: gateSeparationModel.length,
        releaseApprovingGates: gateSeparationModel.filter((item) => item.mayApproveRelease === true).length,
        runtimeEnablingGates: gateSeparationModel.filter((item) => item.mayEnableRuntime === true).length
      }
    },
    {
      id: "export-policy",
      label: "Export policy",
      sourceSchemaVersion: "phase29.governance-dashboard-export.v1",
      state: "ready-readonly",
      exportName: "phase29-governance-dashboard-export.json",
      keyMetrics: {
        includedExports: 5,
        persistedMutations: 0,
        redactedExports: 5
      }
    }
  ];
  const exportManifest = [
    { id: "phase29-release-governance-planning", exportName: "phase29-release-governance-planning.json", sourceSchemaVersion: "phase29.release-governance-planning.v1", includeInOperationsExport: true },
    { id: "phase29-blocker-clearance-workflow-planning", exportName: "phase29-blocker-clearance-workflow-planning.json", sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion, includeInOperationsExport: blockerClearanceWorkflow.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-release-approval-state-model", exportName: "phase29-release-approval-state-model.json", sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, includeInOperationsExport: releaseApprovalStateModel.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-runtime-enable-governance-planning", exportName: "phase29-runtime-enable-governance-planning.json", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, includeInOperationsExport: runtimeEnableGovernance.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-governance-dashboard-export", exportName: "phase29-governance-dashboard-export.json", sourceSchemaVersion: "phase29.governance-dashboard-export.v1", includeInOperationsExport: true }
  ];
  const dashboardChecks = [
    { id: "blocker-workflow-linked", result: blockerClearanceWorkflow.schemaVersion === "phase29.blocker-clearance-workflow-planning.v1" ? "pass" : "block", evidence: blockerClearanceWorkflow.schemaVersion },
    { id: "approval-state-linked", result: releaseApprovalStateModel.schemaVersion === "phase29.release-approval-state-model.v1" ? "pass" : "block", evidence: releaseApprovalStateModel.schemaVersion },
    { id: "runtime-governance-linked", result: runtimeEnableGovernance.schemaVersion === "phase29.runtime-enable-governance-planning.v1" ? "pass" : "block", evidence: runtimeEnableGovernance.schemaVersion },
    { id: "dashboard-sections-readonly", result: dashboardSections.every((item) => item.state === "ready-readonly") ? "pass" : "block", evidence: `${dashboardSections.length} dashboard section` },
    { id: "release-not-approved", result: "pass", evidence: "releaseReady=false" },
    { id: "runtime-not-enabled", result: "pass", evidence: "runtimeExecution=false" },
    { id: "third-party-not-enabled", result: "pass", evidence: "thirdPartyExecution=false" },
    { id: "dashboard-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.governance-dashboard-export.v1",
    version: "1.9.4",
    mode: "readonly-governance-dashboard-export-no-release-approval",
    sourceModels: {
      blockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      gateSeparation: "phase29.release-governance-planning.v1"
    },
    dashboardSections,
    dashboardChecks,
    exportManifest,
    blockedMutations: [
      "persist-dashboard-as-release-approval",
      "persist-dashboard-as-clearance",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-governance-dashboard-export.json",
      includeInOperationsExport: true,
      redaction: "dashboard-section-status-export-name-and-nonmutating-summary-only"
    },
    summary: {
      dashboardExportReady: true,
      dashboardSections: dashboardSections.length,
      includedExports: exportManifest.filter((item) => item.includeInOperationsExport === true).length,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29DecisionHistoryAuditChain(blockerClearanceWorkflow = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}, governanceDashboardExport = {}) {
  const decisionEvents = [
    {
      id: "phase28-closure-carried-forward",
      label: "Phase 28 closure carried forward",
      sourceSchemaVersion: "phase28.closure-package.v1",
      decisionState: "closed-with-open-blockers-carried-forward",
      releaseReady: false,
      runtimeExecution: false,
      mutatesReleaseState: false
    },
    {
      id: "blocker-clearance-workflow-planned",
      label: "Blocker clearance workflow planned",
      sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion,
      decisionState: "planning-only-no-clearance",
      releaseReady: false,
      runtimeExecution: false,
      mutatesReleaseState: false
    },
    {
      id: "release-approval-state-model-planned",
      label: "Release approval state model planned",
      sourceSchemaVersion: releaseApprovalStateModel.schemaVersion,
      decisionState: "approval-preview-blocked",
      releaseReady: false,
      runtimeExecution: false,
      mutatesReleaseState: false
    },
    {
      id: "runtime-enable-governance-deferred",
      label: "Runtime enable governance deferred",
      sourceSchemaVersion: runtimeEnableGovernance.schemaVersion,
      decisionState: "runtime-enable-planning-only",
      releaseReady: false,
      runtimeExecution: false,
      mutatesReleaseState: false
    },
    {
      id: "governance-dashboard-exported",
      label: "Governance dashboard exported",
      sourceSchemaVersion: governanceDashboardExport.schemaVersion,
      decisionState: "dashboard-export-ready-readonly",
      releaseReady: false,
      runtimeExecution: false,
      mutatesReleaseState: false
    }
  ];
  const chainLinks = decisionEvents.map((event, index) => ({
    id: `phase29-audit-link-${index + 1}`,
    fromEvent: index === 0 ? "phase28-clearance-review-entry" : decisionEvents[index - 1].id,
    toEvent: event.id,
    sourceSchemaVersion: event.sourceSchemaVersion,
    linkState: "linked-readonly",
    integrityCheck: "source-schema-and-nonmutating-state-recorded"
  }));
  const auditChecks = [
    { id: "blocker-workflow-linked", result: blockerClearanceWorkflow.schemaVersion === "phase29.blocker-clearance-workflow-planning.v1" ? "pass" : "block", evidence: blockerClearanceWorkflow.schemaVersion },
    { id: "approval-state-linked", result: releaseApprovalStateModel.schemaVersion === "phase29.release-approval-state-model.v1" ? "pass" : "block", evidence: releaseApprovalStateModel.schemaVersion },
    { id: "runtime-governance-linked", result: runtimeEnableGovernance.schemaVersion === "phase29.runtime-enable-governance-planning.v1" ? "pass" : "block", evidence: runtimeEnableGovernance.schemaVersion },
    { id: "dashboard-export-linked", result: governanceDashboardExport.schemaVersion === "phase29.governance-dashboard-export.v1" ? "pass" : "block", evidence: governanceDashboardExport.schemaVersion },
    { id: "chain-events-nonmutating", result: decisionEvents.every((item) => item.mutatesReleaseState === false) ? "pass" : "block", evidence: `${decisionEvents.length} decision event` },
    { id: "audit-chain-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
    { id: "audit-chain-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "audit-chain-does-not-enable-runtime", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "audit-chain-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.decision-history-audit-chain.v1",
    version: "1.9.5",
    mode: "readonly-decision-history-audit-chain-no-release-approval",
    sourceModels: {
      blockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceDashboardExport: governanceDashboardExport.schemaVersion
    },
    decisionEvents,
    chainLinks,
    auditChecks,
    blockedMutations: [
      "persist-audit-chain-as-release-approval",
      "persist-audit-chain-as-clearance",
      "rewrite-decision-history",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-decision-history-audit-chain.json",
      includeInOperationsExport: true,
      redaction: "decision-event-id-source-schema-state-and-nonmutating-integrity-only"
    },
    summary: {
      decisionHistoryAuditChainReady: true,
      decisionEvents: decisionEvents.length,
      chainLinks: chainLinks.length,
      passedAuditChecks: auditChecks.filter((item) => item.result === "pass").length,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29GovernanceClosureReadiness(blockerClearanceWorkflow = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}, governanceDashboardExport = {}, decisionHistoryAuditChain = {}, carriedForwardBlockers = []) {
  const closureCriteria = [
    {
      id: "release-governance-planning-complete",
      label: "Release governance planning complete",
      sourceSchemaVersion: "phase29.release-governance-planning.v1",
      result: "pass",
      evidence: "planningReady=true",
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "blocker-workflow-readonly-ready",
      label: "Blocker workflow readonly ready",
      sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion,
      result: blockerClearanceWorkflow.summary?.workflowPlanningReady ? "pass" : "block",
      evidence: `pendingWorkflowItems=${blockerClearanceWorkflow.summary?.pendingWorkflowItems || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "approval-state-model-readonly-ready",
      label: "Approval state model readonly ready",
      sourceSchemaVersion: releaseApprovalStateModel.schemaVersion,
      result: releaseApprovalStateModel.summary?.approvalStateModelReady ? "pass" : "block",
      evidence: `approvedReleaseStates=${releaseApprovalStateModel.summary?.approvedReleaseStates || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "runtime-governance-readonly-ready",
      label: "Runtime governance readonly ready",
      sourceSchemaVersion: runtimeEnableGovernance.schemaVersion,
      result: runtimeEnableGovernance.summary?.runtimeEnableGovernanceReady ? "pass" : "block",
      evidence: `runtimeEnableApprovals=${runtimeEnableGovernance.summary?.runtimeEnableApprovals || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "governance-dashboard-export-ready",
      label: "Governance dashboard export ready",
      sourceSchemaVersion: governanceDashboardExport.schemaVersion,
      result: governanceDashboardExport.summary?.dashboardExportReady ? "pass" : "block",
      evidence: governanceDashboardExport.exportPolicy?.exportName,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "decision-history-audit-chain-ready",
      label: "Decision history audit chain ready",
      sourceSchemaVersion: decisionHistoryAuditChain.schemaVersion,
      result: decisionHistoryAuditChain.summary?.decisionHistoryAuditChainReady ? "pass" : "block",
      evidence: `chainLinks=${decisionHistoryAuditChain.summary?.chainLinks || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "phase30-entry-remains-blocked",
      label: "Phase 30 entry remains blocked",
      sourceSchemaVersion: "phase29.governance-closure-readiness.v1",
      result: "pass",
      evidence: "phase30-release-candidate-governance=blocked",
      releaseReady: false,
      runtimeExecution: false
    }
  ];
  const blockedClosureItems = [
    {
      id: "carried-forward-blockers-open",
      label: "Carried forward blockers remain open",
      status: carriedForwardBlockers.length > 0 ? "blocked-carry-forward" : "review-required",
      count: carriedForwardBlockers.length,
      blocks: ["releaseReady", "phase30-entry"],
      requiredBeforeRelease: ["human-owned-clearance-evidence", "reviewer-disposition-recorded", "criteria-satisfaction-reviewed"]
    },
    {
      id: "release-approval-not-signed",
      label: "Release approval not signed",
      status: "blocked-by-design",
      count: releaseApprovalStateModel.summary?.approvedReleaseStates || 0,
      blocks: ["releaseReady", "approval-persistence"],
      requiredBeforeRelease: ["signed-release-decision", "artifact-integrity-verified", "rollback-and-backup-reviewed"]
    },
    {
      id: "runtime-enable-approval-missing",
      label: "Runtime enable approval missing",
      status: "blocked-by-design",
      count: runtimeEnableGovernance.summary?.runtimeEnableApprovals || 0,
      blocks: ["runtimeExecution", "thirdPartyExecution"],
      requiredBeforeRelease: ["runtime-validation-pass", "permission-policy-reviewed", "kill-switch-reviewed"]
    }
  ];
  const exportManifest = [
    { id: "phase29-release-governance-planning", exportName: "phase29-release-governance-planning.json", sourceSchemaVersion: "phase29.release-governance-planning.v1", includeInOperationsExport: true },
    { id: "phase29-blocker-clearance-workflow-planning", exportName: blockerClearanceWorkflow.exportPolicy?.exportName, sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion, includeInOperationsExport: blockerClearanceWorkflow.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-release-approval-state-model", exportName: releaseApprovalStateModel.exportPolicy?.exportName, sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, includeInOperationsExport: releaseApprovalStateModel.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-runtime-enable-governance-planning", exportName: runtimeEnableGovernance.exportPolicy?.exportName, sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, includeInOperationsExport: runtimeEnableGovernance.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-governance-dashboard-export", exportName: governanceDashboardExport.exportPolicy?.exportName, sourceSchemaVersion: governanceDashboardExport.schemaVersion, includeInOperationsExport: governanceDashboardExport.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-decision-history-audit-chain", exportName: decisionHistoryAuditChain.exportPolicy?.exportName, sourceSchemaVersion: decisionHistoryAuditChain.schemaVersion, includeInOperationsExport: decisionHistoryAuditChain.exportPolicy?.includeInOperationsExport === true },
    { id: "phase29-governance-closure-readiness", exportName: "phase29-governance-closure-readiness.json", sourceSchemaVersion: "phase29.governance-closure-readiness.v1", includeInOperationsExport: true }
  ];
  const closureChecks = [
    { id: "closure-criteria-pass", result: closureCriteria.every((item) => item.result === "pass") ? "pass" : "block", evidence: `${closureCriteria.filter((item) => item.result === "pass").length}/${closureCriteria.length} criteria` },
    { id: "closure-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "closure-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "closure-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "closure-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" },
    { id: "phase30-entry-blocked", result: "pass", evidence: "phase30ReleaseCandidateGovernance=blocked" }
  ];
  return {
    schemaVersion: "phase29.governance-closure-readiness.v1",
    version: "1.9.6",
    mode: "readonly-governance-closure-readiness-no-release-approval",
    sourceModels: {
      blockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceDashboardExport: governanceDashboardExport.schemaVersion,
      decisionHistoryAuditChain: decisionHistoryAuditChain.schemaVersion
    },
    closureCriteria,
    blockedClosureItems,
    closureChecks,
    exportManifest,
    blockedMutations: [
      "persist-closure-readiness-as-release-approval",
      "persist-closure-readiness-as-phase30-entry",
      "clear-blocker-from-closure-readiness",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-governance-closure-readiness.json",
      includeInOperationsExport: true,
      redaction: "closure-criteria-blocked-item-and-nonmutating-summary-only"
    },
    summary: {
      governanceClosureReadinessReady: true,
      closureCriteria: closureCriteria.length,
      passedClosureCriteria: closureCriteria.filter((item) => item.result === "pass").length,
      blockedClosureItems: blockedClosureItems.length,
      includedExports: exportManifest.filter((item) => item.includeInOperationsExport === true).length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ClosureHandoffPackage(blockerClearanceWorkflow = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}, governanceDashboardExport = {}, decisionHistoryAuditChain = {}, governanceClosureReadiness = {}) {
  const handoffSections = [
    {
      id: "closure-readiness-summary",
      label: "Closure readiness summary",
      sourceSchemaVersion: governanceClosureReadiness.schemaVersion,
      handoffState: governanceClosureReadiness.summary?.governanceClosureReadinessReady ? "ready-readonly" : "blocked",
      evidence: `closureCriteria=${governanceClosureReadiness.summary?.closureCriteria || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "open-blocker-handoff",
      label: "Open blocker handoff",
      sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion,
      handoffState: "blocked-carry-forward",
      evidence: `pendingWorkflowItems=${blockerClearanceWorkflow.summary?.pendingWorkflowItems || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "release-approval-handoff",
      label: "Release approval handoff",
      sourceSchemaVersion: releaseApprovalStateModel.schemaVersion,
      handoffState: "approval-blocked",
      evidence: `approvedReleaseStates=${releaseApprovalStateModel.summary?.approvedReleaseStates || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "runtime-enable-handoff",
      label: "Runtime enable handoff",
      sourceSchemaVersion: runtimeEnableGovernance.schemaVersion,
      handoffState: "runtime-disabled",
      evidence: `runtimeEnableApprovals=${runtimeEnableGovernance.summary?.runtimeEnableApprovals || 0}`,
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "phase30-entry-guardrail",
      label: "Phase 30 entry guardrail",
      sourceSchemaVersion: governanceClosureReadiness.schemaVersion,
      handoffState: "phase30-blocked",
      evidence: "phase30ReleaseCandidateGovernance=blocked",
      releaseReady: false,
      runtimeExecution: false
    }
  ];
  const handoffArtifacts = [
    { id: "release-governance-planning", exportName: "phase29-release-governance-planning.json", sourceSchemaVersion: "phase29.release-governance-planning.v1", requiredForHandoff: true },
    { id: "blocker-clearance-workflow", exportName: blockerClearanceWorkflow.exportPolicy?.exportName, sourceSchemaVersion: blockerClearanceWorkflow.schemaVersion, requiredForHandoff: true },
    { id: "release-approval-state-model", exportName: releaseApprovalStateModel.exportPolicy?.exportName, sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, requiredForHandoff: true },
    { id: "runtime-enable-governance", exportName: runtimeEnableGovernance.exportPolicy?.exportName, sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, requiredForHandoff: true },
    { id: "governance-dashboard-export", exportName: governanceDashboardExport.exportPolicy?.exportName, sourceSchemaVersion: governanceDashboardExport.schemaVersion, requiredForHandoff: true },
    { id: "decision-history-audit-chain", exportName: decisionHistoryAuditChain.exportPolicy?.exportName, sourceSchemaVersion: decisionHistoryAuditChain.schemaVersion, requiredForHandoff: true },
    { id: "governance-closure-readiness", exportName: governanceClosureReadiness.exportPolicy?.exportName, sourceSchemaVersion: governanceClosureReadiness.schemaVersion, requiredForHandoff: true },
    { id: "closure-handoff-package", exportName: "phase29-closure-handoff-package.json", sourceSchemaVersion: "phase29.closure-handoff-package.v1", requiredForHandoff: true }
  ];
  const handoffChecks = [
    { id: "closure-readiness-linked", result: governanceClosureReadiness.schemaVersion === "phase29.governance-closure-readiness.v1" ? "pass" : "block", evidence: governanceClosureReadiness.schemaVersion },
    { id: "all-handoff-artifacts-required", result: handoffArtifacts.every((item) => item.requiredForHandoff === true) ? "pass" : "block", evidence: `${handoffArtifacts.length} handoff artifact` },
    { id: "handoff-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
    { id: "handoff-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "handoff-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "handoff-keeps-phase30-blocked", result: "pass", evidence: "phase30Blocked=true" },
    { id: "handoff-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.closure-handoff-package.v1",
    version: "1.9.7",
    mode: "readonly-closure-handoff-package-no-release-approval",
    sourceModels: {
      blockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceDashboardExport: governanceDashboardExport.schemaVersion,
      decisionHistoryAuditChain: decisionHistoryAuditChain.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion
    },
    handoffSections,
    handoffArtifacts,
    handoffChecks,
    blockedMutations: [
      "persist-handoff-as-release-approval",
      "persist-handoff-as-phase30-entry",
      "clear-blocker-from-handoff-package",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-closure-handoff-package.json",
      includeInOperationsExport: true,
      redaction: "handoff-section-artifact-check-and-nonmutating-summary-only"
    },
    summary: {
      closureHandoffPackageReady: true,
      handoffSections: handoffSections.length,
      handoffArtifacts: handoffArtifacts.length,
      passedHandoffChecks: handoffChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29HandoffReviewIndex(closureHandoffPackage = {}, governanceClosureReadiness = {}) {
  const handoffSections = closureHandoffPackage.handoffSections || [];
  const handoffArtifacts = closureHandoffPackage.handoffArtifacts || [];
  const handoffChecks = closureHandoffPackage.handoffChecks || [];
  const blockedClosureItems = governanceClosureReadiness.blockedClosureItems || [];
  const reviewIndexEntries = [
    {
      id: "handoff-section-review",
      label: "Handoff section review",
      sourceSchemaVersion: closureHandoffPackage.schemaVersion,
      reviewState: "ready-readonly",
      indexedItems: handoffSections.length,
      requiredBeforeRelease: ["human-review-sections", "confirm-no-release-approval"],
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "handoff-artifact-review",
      label: "Handoff artifact review",
      sourceSchemaVersion: closureHandoffPackage.schemaVersion,
      reviewState: "ready-readonly",
      indexedItems: handoffArtifacts.length,
      requiredBeforeRelease: ["verify-artifact-integrity", "confirm-redaction-policy"],
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "handoff-check-review",
      label: "Handoff check review",
      sourceSchemaVersion: closureHandoffPackage.schemaVersion,
      reviewState: "ready-readonly",
      indexedItems: handoffChecks.length,
      requiredBeforeRelease: ["review-handoff-checks", "confirm-persistedMutations-zero"],
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "blocked-closure-item-review",
      label: "Blocked closure item review",
      sourceSchemaVersion: governanceClosureReadiness.schemaVersion,
      reviewState: "blocked-carry-forward",
      indexedItems: blockedClosureItems.length,
      requiredBeforeRelease: ["resolve-open-blockers", "sign-release-decision"],
      releaseReady: false,
      runtimeExecution: false
    },
    {
      id: "phase30-entry-review",
      label: "Phase 30 entry review",
      sourceSchemaVersion: closureHandoffPackage.schemaVersion,
      reviewState: "phase30-blocked",
      indexedItems: handoffSections.filter((item) => item.handoffState === "phase30-blocked").length,
      requiredBeforeRelease: ["clear-phase29-blockers", "complete-runtime-governance-closure"],
      releaseReady: false,
      runtimeExecution: false
    }
  ];
  const blockedReviewItems = [
    ...blockedClosureItems.map((item) => ({
      id: `closure-${item.id}`,
      label: item.label,
      source: "governanceClosureReadiness.blockedClosureItems",
      reviewState: item.status,
      blocks: item.blocks || [],
      count: item.count || 0
    })),
    {
      id: "handoff-phase30-entry-blocked",
      label: "Phase 30 entry remains blocked by handoff",
      source: "closureHandoffPackage.handoffSections",
      reviewState: "phase30-blocked",
      blocks: ["phase30-entry", "releaseReady"],
      count: handoffSections.filter((item) => item.handoffState === "phase30-blocked").length
    }
  ];
  const reviewChecks = [
    { id: "handoff-package-linked", result: closureHandoffPackage.schemaVersion === "phase29.closure-handoff-package.v1" ? "pass" : "block", evidence: closureHandoffPackage.schemaVersion },
    { id: "closure-readiness-linked", result: governanceClosureReadiness.schemaVersion === "phase29.governance-closure-readiness.v1" ? "pass" : "block", evidence: governanceClosureReadiness.schemaVersion },
    { id: "review-index-covers-handoff-artifacts", result: handoffArtifacts.length >= 1 ? "pass" : "block", evidence: `${handoffArtifacts.length} artifact` },
    { id: "review-index-covers-handoff-checks", result: handoffChecks.length >= 1 ? "pass" : "block", evidence: `${handoffChecks.length} handoff check` },
    { id: "review-index-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "review-index-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "review-index-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.handoff-review-index.v1",
    version: "1.9.8",
    mode: "readonly-handoff-review-index-no-release-approval",
    sourceModels: {
      closureHandoffPackage: closureHandoffPackage.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion
    },
    reviewIndexEntries,
    blockedReviewItems,
    reviewChecks,
    blockedMutations: [
      "persist-review-index-as-release-approval",
      "persist-review-index-as-phase30-entry",
      "clear-blocker-from-review-index",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-handoff-review-index.json",
      includeInOperationsExport: true,
      redaction: "review-index-entry-blocked-item-check-and-nonmutating-summary-only"
    },
    summary: {
      handoffReviewIndexReady: true,
      reviewIndexEntries: reviewIndexEntries.length,
      blockedReviewItems: blockedReviewItems.length,
      indexedArtifacts: handoffArtifacts.length,
      indexedChecks: handoffChecks.length,
      passedReviewChecks: reviewChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29FinalClosureReadinessSnapshot(handoffReviewIndex = {}, closureHandoffPackage = {}, governanceClosureReadiness = {}, decisionHistoryAuditChain = {}, governanceDashboardExport = {}, runtimeEnableGovernance = {}, releaseApprovalStateModel = {}, blockerClearanceWorkflow = {}) {
  const readinessModels = [
    { id: "blocker-clearance-workflow", schemaVersion: blockerClearanceWorkflow.schemaVersion, ready: blockerClearanceWorkflow.summary?.workflowPlanningReady === true, releaseReady: false, persistedMutations: blockerClearanceWorkflow.summary?.persistedMutations || 0 },
    { id: "release-approval-state-model", schemaVersion: releaseApprovalStateModel.schemaVersion, ready: releaseApprovalStateModel.summary?.approvalStateModelReady === true, releaseReady: false, persistedMutations: releaseApprovalStateModel.summary?.persistedMutations || 0 },
    { id: "runtime-enable-governance", schemaVersion: runtimeEnableGovernance.schemaVersion, ready: runtimeEnableGovernance.summary?.runtimeEnableGovernanceReady === true, releaseReady: false, persistedMutations: runtimeEnableGovernance.summary?.persistedMutations || 0 },
    { id: "governance-dashboard-export", schemaVersion: governanceDashboardExport.schemaVersion, ready: governanceDashboardExport.summary?.dashboardExportReady === true, releaseReady: false, persistedMutations: governanceDashboardExport.summary?.persistedMutations || 0 },
    { id: "decision-history-audit-chain", schemaVersion: decisionHistoryAuditChain.schemaVersion, ready: decisionHistoryAuditChain.summary?.decisionHistoryAuditChainReady === true, releaseReady: false, persistedMutations: decisionHistoryAuditChain.summary?.persistedMutations || 0 },
    { id: "governance-closure-readiness", schemaVersion: governanceClosureReadiness.schemaVersion, ready: governanceClosureReadiness.summary?.governanceClosureReadinessReady === true, releaseReady: false, persistedMutations: governanceClosureReadiness.summary?.persistedMutations || 0 },
    { id: "closure-handoff-package", schemaVersion: closureHandoffPackage.schemaVersion, ready: closureHandoffPackage.summary?.closureHandoffPackageReady === true, releaseReady: false, persistedMutations: closureHandoffPackage.summary?.persistedMutations || 0 },
    { id: "handoff-review-index", schemaVersion: handoffReviewIndex.schemaVersion, ready: handoffReviewIndex.summary?.handoffReviewIndexReady === true, releaseReady: false, persistedMutations: handoffReviewIndex.summary?.persistedMutations || 0 }
  ];
  const blockedSnapshotItems = [
    ...(handoffReviewIndex.blockedReviewItems || []).map((item) => ({
      id: `snapshot-${item.id}`,
      label: item.label,
      source: item.source,
      snapshotState: item.reviewState,
      blocks: item.blocks || [],
      count: item.count || 0
    })),
    {
      id: "snapshot-phase30-entry-blocked",
      label: "Phase 30 entry remains blocked in final snapshot",
      source: "phase29.final-closure-readiness-snapshot.v1",
      snapshotState: "phase30-blocked",
      blocks: ["phase30-entry", "releaseReady", "runtimeExecution"],
      count: 1
    }
  ];
  const snapshotChecks = [
    { id: "snapshot-covers-all-phase29-submodels", result: readinessModels.length >= 8 ? "pass" : "block", evidence: `${readinessModels.length} model` },
    { id: "snapshot-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "snapshot-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "snapshot-keeps-phase30-blocked", result: "pass", evidence: "phase30Blocked=true" },
    { id: "snapshot-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "snapshot-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.final-closure-readiness-snapshot.v1",
    version: "1.9.9",
    mode: "readonly-final-closure-readiness-snapshot-no-release-approval",
    sourceModels: {
      handoffReviewIndex: handoffReviewIndex.schemaVersion,
      closureHandoffPackage: closureHandoffPackage.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion
    },
    readinessModels,
    blockedSnapshotItems,
    snapshotChecks,
    blockedMutations: [
      "persist-final-snapshot-as-release-approval",
      "persist-final-snapshot-as-phase30-entry",
      "clear-blocker-from-final-snapshot",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-final-closure-readiness-snapshot.json",
      includeInOperationsExport: true,
      redaction: "snapshot-model-blocker-check-and-nonmutating-summary-only"
    },
    summary: {
      finalClosureReadinessSnapshotReady: true,
      readinessModels: readinessModels.length,
      readyModels: readinessModels.filter((item) => item.ready).length,
      blockedSnapshotItems: blockedSnapshotItems.length,
      passedSnapshotChecks: snapshotChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29GovernanceArchiveFreeze(finalClosureReadinessSnapshot = {}, handoffReviewIndex = {}, closureHandoffPackage = {}, governanceClosureReadiness = {}, decisionHistoryAuditChain = {}, governanceDashboardExport = {}) {
  const archiveManifest = [
    { id: "release-governance-planning", exportName: "phase29-release-governance-planning.json", sourceSchemaVersion: "phase29.release-governance-planning.v1", freezeState: "frozen-readonly" },
    { id: "blocker-clearance-workflow", exportName: "phase29-blocker-clearance-workflow-planning.json", sourceSchemaVersion: "phase29.blocker-clearance-workflow-planning.v1", freezeState: "frozen-readonly" },
    { id: "release-approval-state-model", exportName: "phase29-release-approval-state-model.json", sourceSchemaVersion: "phase29.release-approval-state-model.v1", freezeState: "frozen-readonly" },
    { id: "runtime-enable-governance", exportName: "phase29-runtime-enable-governance-planning.json", sourceSchemaVersion: "phase29.runtime-enable-governance-planning.v1", freezeState: "frozen-readonly" },
    { id: "governance-dashboard-export", exportName: governanceDashboardExport.exportPolicy?.exportName || "phase29-governance-dashboard-export.json", sourceSchemaVersion: governanceDashboardExport.schemaVersion, freezeState: "frozen-readonly" },
    { id: "decision-history-audit-chain", exportName: decisionHistoryAuditChain.exportPolicy?.exportName || "phase29-decision-history-audit-chain.json", sourceSchemaVersion: decisionHistoryAuditChain.schemaVersion, freezeState: "frozen-readonly" },
    { id: "governance-closure-readiness", exportName: governanceClosureReadiness.exportPolicy?.exportName || "phase29-governance-closure-readiness.json", sourceSchemaVersion: governanceClosureReadiness.schemaVersion, freezeState: "frozen-readonly" },
    { id: "closure-handoff-package", exportName: closureHandoffPackage.exportPolicy?.exportName || "phase29-closure-handoff-package.json", sourceSchemaVersion: closureHandoffPackage.schemaVersion, freezeState: "frozen-readonly" },
    { id: "handoff-review-index", exportName: handoffReviewIndex.exportPolicy?.exportName || "phase29-handoff-review-index.json", sourceSchemaVersion: handoffReviewIndex.schemaVersion, freezeState: "frozen-readonly" },
    { id: "final-closure-readiness-snapshot", exportName: finalClosureReadinessSnapshot.exportPolicy?.exportName || "phase29-final-closure-readiness-snapshot.json", sourceSchemaVersion: finalClosureReadinessSnapshot.schemaVersion, freezeState: "frozen-readonly" }
  ];
  const frozenGuards = [
    { id: "release-approval", state: "blocked", evidence: "releaseReady=false", frozen: true },
    { id: "runtime-execution", state: "blocked", evidence: "runtimeExecution=false", frozen: true },
    { id: "third-party-execution", state: "blocked", evidence: "thirdPartyExecution=false", frozen: true },
    { id: "phase30-entry", state: "blocked", evidence: "phase30Blocked=true", frozen: true },
    { id: "blocker-clearance", state: "blocked", evidence: "clearedBlockers=0", frozen: true }
  ];
  const archiveChecks = [
    { id: "archive-covers-final-snapshot", result: finalClosureReadinessSnapshot.schemaVersion === "phase29.final-closure-readiness-snapshot.v1" ? "pass" : "block", evidence: finalClosureReadinessSnapshot.schemaVersion },
    { id: "archive-freezes-all-phase29-exports", result: archiveManifest.length >= 10 ? "pass" : "block", evidence: `${archiveManifest.length} export` },
    { id: "archive-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "archive-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "archive-keeps-phase30-blocked", result: "pass", evidence: "phase30Blocked=true" },
    { id: "archive-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.governance-archive-freeze.v1",
    version: "1.9.10",
    mode: "readonly-governance-archive-freeze-no-release-approval",
    sourceModels: {
      finalClosureReadinessSnapshot: finalClosureReadinessSnapshot.schemaVersion,
      handoffReviewIndex: handoffReviewIndex.schemaVersion,
      closureHandoffPackage: closureHandoffPackage.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion,
      decisionHistoryAuditChain: decisionHistoryAuditChain.schemaVersion,
      governanceDashboardExport: governanceDashboardExport.schemaVersion
    },
    archiveManifest,
    frozenGuards,
    archiveChecks,
    blockedMutations: [
      "persist-archive-freeze-as-release-approval",
      "persist-archive-freeze-as-phase30-entry",
      "unfreeze-phase29-archive-without-review",
      "rewrite-frozen-governance-archive",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-governance-archive-freeze.json",
      includeInOperationsExport: true,
      redaction: "archive-manifest-guard-check-and-nonmutating-summary-only"
    },
    summary: {
      governanceArchiveFreezeReady: true,
      archivedExports: archiveManifest.length,
      frozenGuards: frozenGuards.length,
      passedArchiveChecks: archiveChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29BlockerResolutionMap(governanceArchiveFreeze = {}, finalClosureReadinessSnapshot = {}, handoffReviewIndex = {}, governanceClosureReadiness = {}, blockerClearanceWorkflow = {}) {
  const sourceBlockedItems = finalClosureReadinessSnapshot.blockedSnapshotItems || [];
  const workflowItems = blockerClearanceWorkflow.workflowItems || [];
  const resolutionItems = sourceBlockedItems.map((item, index) => {
    const workflowItem = workflowItems[index % Math.max(workflowItems.length, 1)] || {};
    return {
      id: `resolution-${item.id}`,
      label: item.label,
      source: item.source,
      blockerState: item.snapshotState || "blocked",
      ownerRole: workflowItem.ownerRole || "human-release-governance",
      requiredEvidence: workflowItem.evidenceRequirements || ["human-review-evidence", "release-owner-signoff", "runtime-disabled-verification"],
      requiredBeforePhase30: ["human-clearance-recorded", "release-approval-reviewed", "runtime-governance-closed"],
      blockedActions: ["clear-blocker-from-resolution-map", "mark-releaseReady-true", "enter-phase30"],
      mayClearBlocker: false,
      releaseReady: false,
      runtimeExecution: false
    };
  });
  const resolutionLanes = [
    { id: "human-evidence", label: "Human evidence", state: "required-readonly", itemCount: resolutionItems.length, mayPersistClearance: false },
    { id: "release-approval", label: "Release approval", state: "blocked", itemCount: resolutionItems.filter((item) => item.blockedActions.includes("mark-releaseReady-true")).length, mayPersistClearance: false },
    { id: "runtime-governance", label: "Runtime governance", state: "blocked", itemCount: resolutionItems.filter((item) => item.runtimeExecution === false).length, mayPersistClearance: false },
    { id: "phase30-entry", label: "Phase 30 entry", state: "blocked", itemCount: resolutionItems.filter((item) => item.blockedActions.includes("enter-phase30")).length, mayPersistClearance: false }
  ];
  const resolutionChecks = [
    { id: "resolution-map-linked-to-archive-freeze", result: governanceArchiveFreeze.schemaVersion === "phase29.governance-archive-freeze.v1" ? "pass" : "block", evidence: governanceArchiveFreeze.schemaVersion },
    { id: "resolution-map-covers-blocked-items", result: resolutionItems.length >= 1 ? "pass" : "block", evidence: `${resolutionItems.length} blocker` },
    { id: "resolution-map-keeps-blockers-open", result: "pass", evidence: "clearedBlockers=0" },
    { id: "resolution-map-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "resolution-map-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "resolution-map-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.blocker-resolution-map.v1",
    version: "1.9.11",
    mode: "readonly-blocker-resolution-map-no-clearance",
    sourceModels: {
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      finalClosureReadinessSnapshot: finalClosureReadinessSnapshot.schemaVersion,
      handoffReviewIndex: handoffReviewIndex.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion,
      blockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion
    },
    resolutionItems,
    resolutionLanes,
    resolutionChecks,
    blockedMutations: [
      "persist-resolution-map-as-clearance",
      "persist-resolution-map-as-release-approval",
      "persist-resolution-map-as-phase30-entry",
      "clear-blocker-from-resolution-map",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-blocker-resolution-map.json",
      includeInOperationsExport: true,
      redaction: "resolution-item-lane-check-and-nonmutating-summary-only"
    },
    summary: {
      blockerResolutionMapReady: true,
      resolutionItems: resolutionItems.length,
      resolutionLanes: resolutionLanes.length,
      passedResolutionChecks: resolutionChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryPreflight(blockerResolutionMap = {}, governanceArchiveFreeze = {}, finalClosureReadinessSnapshot = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}) {
  const preflightItems = [
    {
      id: "blocker-resolution-map-reviewed",
      label: "Blocker resolution map reviewed",
      sourceSchemaVersion: blockerResolutionMap.schemaVersion,
      result: blockerResolutionMap.summary?.blockerResolutionMapReady === true ? "pass-readonly" : "block",
      blocksPhase30: true,
      requiredBeforePhase30: ["clear-blockers-with-human-approval", "persist-clearance-outside-phase29-readonly-map"]
    },
    {
      id: "release-approval-still-blocked",
      label: "Release approval still blocked",
      sourceSchemaVersion: releaseApprovalStateModel.schemaVersion,
      result: "block",
      blocksPhase30: true,
      requiredBeforePhase30: ["signed-release-approval", "releaseReady-true-after-review"]
    },
    {
      id: "runtime-governance-still-disabled",
      label: "Runtime governance still disabled",
      sourceSchemaVersion: runtimeEnableGovernance.schemaVersion,
      result: "block",
      blocksPhase30: true,
      requiredBeforePhase30: ["runtime-validation-approved", "runtime-enable-approval-recorded"]
    },
    {
      id: "archive-freeze-present",
      label: "Governance archive freeze present",
      sourceSchemaVersion: governanceArchiveFreeze.schemaVersion,
      result: governanceArchiveFreeze.summary?.governanceArchiveFreezeReady === true ? "pass-readonly" : "block",
      blocksPhase30: true,
      requiredBeforePhase30: ["archive-freeze-reviewed-by-release-owner"]
    },
    {
      id: "final-snapshot-present",
      label: "Final closure readiness snapshot present",
      sourceSchemaVersion: finalClosureReadinessSnapshot.schemaVersion,
      result: finalClosureReadinessSnapshot.summary?.finalClosureReadinessSnapshotReady === true ? "pass-readonly" : "block",
      blocksPhase30: true,
      requiredBeforePhase30: ["snapshot-reviewed-by-release-owner"]
    }
  ];
  const entryGates = [
    { id: "blocker-clearance-gate", state: "blocked", mayEnterPhase30: false, evidence: "clearedBlockers=0" },
    { id: "release-approval-gate", state: "blocked", mayEnterPhase30: false, evidence: "releaseReady=false" },
    { id: "runtime-enable-gate", state: "blocked", mayEnterPhase30: false, evidence: "runtimeExecution=false" },
    { id: "third-party-execution-gate", state: "blocked", mayEnterPhase30: false, evidence: "thirdPartyExecution=false" }
  ];
  const preflightChecks = [
    { id: "preflight-linked-to-resolution-map", result: blockerResolutionMap.schemaVersion === "phase29.blocker-resolution-map.v1" ? "pass" : "block", evidence: blockerResolutionMap.schemaVersion },
    { id: "preflight-keeps-phase30-blocked", result: "pass", evidence: "phase30Blocked=true" },
    { id: "preflight-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "preflight-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "preflight-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "preflight-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-preflight.v1",
    version: "1.9.12",
    mode: "readonly-phase30-entry-preflight-no-entry",
    sourceModels: {
      blockerResolutionMap: blockerResolutionMap.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      finalClosureReadinessSnapshot: finalClosureReadinessSnapshot.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    preflightItems,
    entryGates,
    preflightChecks,
    blockedMutations: [
      "persist-preflight-as-phase30-entry",
      "persist-preflight-as-release-approval",
      "clear-blocker-from-preflight",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-preflight.json",
      includeInOperationsExport: true,
      redaction: "preflight-item-gate-check-and-nonmutating-summary-only"
    },
    summary: {
      phase30EntryPreflightReady: true,
      preflightItems: preflightItems.length,
      blockedPreflightItems: preflightItems.filter((item) => item.blocksPhase30).length,
      entryGates: entryGates.length,
      blockedEntryGates: entryGates.filter((item) => item.mayEnterPhase30 === false).length,
      passedPreflightChecks: preflightChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29FinalReleaseBlockerDossier(phase30EntryPreflight = {}, blockerResolutionMap = {}, governanceClosureReadiness = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}) {
  const unresolvedBlockers = (blockerResolutionMap.resolutionItems || []).map((item) => ({
    id: item.id,
    label: item.label,
    sourceSchemaVersion: blockerResolutionMap.schemaVersion,
    dossierState: "unresolved-release-blocker",
    ownerRole: item.ownerRole,
    requiredEvidence: item.requiredEvidence || [],
    requiredBeforeRelease: item.requiredBeforePhase30 || [],
    mayClearBlocker: false,
    releaseReady: false,
    blocksPhase30: true
  }));
  const releaseGateFindings = [
    { id: "blocker-clearance", sourceSchemaVersion: blockerResolutionMap.schemaVersion, state: "blocked", evidence: "clearedBlockers=0", releaseReady: false, mayApproveRelease: false },
    { id: "release-approval", sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, state: "blocked", evidence: "releaseReady=false", releaseReady: false, mayApproveRelease: false },
    { id: "runtime-enable", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, state: "blocked", evidence: "runtimeExecution=false", releaseReady: false, mayApproveRelease: false },
    { id: "phase30-entry", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, state: "blocked", evidence: "phase30EntryReady=false", releaseReady: false, mayApproveRelease: false }
  ];
  const dossierArtifacts = [
    { id: "blocker-resolution-map", sourceSchemaVersion: blockerResolutionMap.schemaVersion, exportName: blockerResolutionMap.exportPolicy?.exportName, artifactState: "included-readonly" },
    { id: "phase30-entry-preflight", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, exportName: phase30EntryPreflight.exportPolicy?.exportName, artifactState: "included-readonly" },
    { id: "governance-closure-readiness", sourceSchemaVersion: governanceClosureReadiness.schemaVersion, exportName: governanceClosureReadiness.exportPolicy?.exportName, artifactState: "included-readonly" },
    { id: "release-approval-state-model", sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, exportName: releaseApprovalStateModel.exportPolicy?.exportName, artifactState: "included-readonly" },
    { id: "runtime-enable-governance", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, exportName: runtimeEnableGovernance.exportPolicy?.exportName, artifactState: "included-readonly" }
  ];
  const dossierChecks = [
    { id: "dossier-linked-to-preflight", result: phase30EntryPreflight.schemaVersion === "phase29.phase30-entry-preflight.v1" ? "pass" : "block", evidence: phase30EntryPreflight.schemaVersion },
    { id: "dossier-keeps-blockers-open", result: "pass", evidence: "clearedBlockers=0" },
    { id: "dossier-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "dossier-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "dossier-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "dossier-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.final-release-blocker-dossier.v1",
    version: "1.9.13",
    mode: "readonly-final-release-blocker-dossier-no-clearance",
    sourceModels: {
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      blockerResolutionMap: blockerResolutionMap.schemaVersion,
      governanceClosureReadiness: governanceClosureReadiness.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    unresolvedBlockers,
    releaseGateFindings,
    dossierArtifacts,
    dossierChecks,
    blockedMutations: [
      "persist-dossier-as-clearance",
      "persist-dossier-as-release-approval",
      "persist-dossier-as-phase30-entry",
      "clear-blocker-from-dossier",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-final-release-blocker-dossier.json",
      includeInOperationsExport: true,
      redaction: "blocker-id-gate-state-artifact-and-nonmutating-summary-only"
    },
    summary: {
      finalReleaseBlockerDossierReady: true,
      unresolvedBlockers: unresolvedBlockers.length,
      releaseGateFindings: releaseGateFindings.length,
      blockedReleaseGates: releaseGateFindings.filter((item) => item.state === "blocked").length,
      dossierArtifacts: dossierArtifacts.length,
      passedDossierChecks: dossierChecks.filter((item) => item.result === "pass").length,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseExitCriteriaLedger(finalReleaseBlockerDossier = {}, phase30EntryPreflight = {}, governanceArchiveFreeze = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}) {
  const exitCriteria = [
    { id: "all-blockers-cleared", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, state: "unmet", evidence: "unresolvedBlockers>=1", mayExitPhase29: false, requiredBeforeExit: ["human-owned-blocker-clearance"] },
    { id: "release-approval-signed", sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, state: "unmet", evidence: "releaseReady=false", mayExitPhase29: false, requiredBeforeExit: ["signed-release-decision"] },
    { id: "runtime-governance-closed", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, state: "unmet", evidence: "runtimeExecution=false", mayExitPhase29: false, requiredBeforeExit: ["runtime-enable-governance-reviewed"] },
    { id: "phase30-entry-authorized", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, state: "unmet", evidence: "phase30EntryReady=false", mayExitPhase29: false, requiredBeforeExit: ["phase30-entry-approval"] },
    { id: "archive-reviewed-for-exit", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, state: "readonly-ready", evidence: "archive frozen read-only", mayExitPhase29: false, requiredBeforeExit: ["release-owner-archive-review"] }
  ];
  const ledgerEntries = exitCriteria.map((item) => ({
    id: `${item.id}-ledger`,
    criteriaId: item.id,
    sourceSchemaVersion: item.sourceSchemaVersion,
    ledgerState: item.state === "readonly-ready" ? "recorded-readonly" : "recorded-unmet",
    evidence: item.evidence,
    maySatisfyCriteria: false,
    mayExitPhase29: false
  }));
  const exitChecks = [
    { id: "ledger-linked-to-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "ledger-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "ledger-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "ledger-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "ledger-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "ledger-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-criteria-ledger.v1",
    version: "1.9.14",
    mode: "readonly-release-exit-criteria-ledger-no-exit",
    sourceModels: {
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    exitCriteria,
    ledgerEntries,
    exitChecks,
    blockedMutations: [
      "persist-exit-ledger-as-clearance",
      "persist-exit-ledger-as-release-approval",
      "persist-exit-ledger-as-phase30-entry",
      "mark-exit-criteria-met",
      "clear-blocker-from-exit-ledger",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-criteria-ledger.json",
      includeInOperationsExport: true,
      redaction: "criteria-id-ledger-state-evidence-and-nonmutating-summary-only"
    },
    summary: {
      releaseExitCriteriaLedgerReady: true,
      exitCriteria: exitCriteria.length,
      unmetExitCriteria: exitCriteria.filter((item) => item.mayExitPhase29 === false).length,
      ledgerEntries: ledgerEntries.length,
      blockedExitGates: ledgerEntries.filter((item) => item.mayExitPhase29 === false).length,
      passedExitChecks: exitChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseExitDecisionPreview(releaseExitCriteriaLedger = {}, finalReleaseBlockerDossier = {}, phase30EntryPreflight = {}, releaseApprovalStateModel = {}, runtimeEnableGovernance = {}) {
  const decisionPreviewItems = [
    { id: "blocker-clearance-decision", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, previewDecision: "block-exit", evidence: "unresolvedBlockers>=1", mayApproveRelease: false, mayExitPhase29: false },
    { id: "release-approval-decision", sourceSchemaVersion: releaseApprovalStateModel.schemaVersion, previewDecision: "block-exit", evidence: "releaseReady=false", mayApproveRelease: false, mayExitPhase29: false },
    { id: "runtime-governance-decision", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, previewDecision: "block-exit", evidence: "runtimeExecution=false", mayApproveRelease: false, mayExitPhase29: false },
    { id: "phase30-entry-decision", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, previewDecision: "block-exit", evidence: "phase30EntryReady=false", mayApproveRelease: false, mayExitPhase29: false },
    { id: "exit-ledger-decision", sourceSchemaVersion: releaseExitCriteriaLedger.schemaVersion, previewDecision: "block-exit", evidence: "phase29ExitReady=false", mayApproveRelease: false, mayExitPhase29: false }
  ];
  const previewDecision = {
    id: "phase29-exit-preview",
    decision: "block-phase29-exit",
    releaseReady: false,
    phase29ExitReady: false,
    phase30EntryReady: false,
    runtimeExecution: false,
    thirdPartyExecution: false,
    rationale: [
      "carried-blockers-remain-open",
      "release-approval-not-signed",
      "runtime-governance-not-closed",
      "phase30-entry-not-authorized"
    ]
  };
  const decisionChecks = [
    { id: "preview-linked-to-exit-ledger", result: releaseExitCriteriaLedger.schemaVersion === "phase29.release-exit-criteria-ledger.v1" ? "pass" : "block", evidence: releaseExitCriteriaLedger.schemaVersion },
    { id: "preview-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "preview-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "preview-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "preview-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "preview-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-decision-preview.v1",
    version: "1.9.15",
    mode: "readonly-release-exit-decision-preview-no-approval",
    sourceModels: {
      releaseExitCriteriaLedger: releaseExitCriteriaLedger.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      releaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    decisionPreviewItems,
    previewDecision,
    decisionChecks,
    blockedMutations: [
      "apply-exit-preview-as-release-approval",
      "apply-exit-preview-as-phase30-entry",
      "persist-exit-preview-as-clearance",
      "persist-exit-preview-as-release-approval",
      "persist-exit-preview-as-phase30-entry",
      "clear-blocker-from-exit-preview",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-decision-preview.json",
      includeInOperationsExport: true,
      redaction: "decision-id-preview-result-evidence-and-nonmutating-summary-only"
    },
    summary: {
      releaseExitDecisionPreviewReady: true,
      decisionPreviewItems: decisionPreviewItems.length,
      blockedPreviewItems: decisionPreviewItems.filter((item) => item.mayExitPhase29 === false).length,
      decisionChecks: decisionChecks.length,
      passedDecisionChecks: decisionChecks.filter((item) => item.result === "pass").length,
      phase29ExitDecision: previewDecision.decision,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30TransitionRiskRegister(releaseExitDecisionPreview = {}, releaseExitCriteriaLedger = {}, finalReleaseBlockerDossier = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const riskItems = [
    { id: "unresolved-blocker-carryover", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, severity: "critical", transitionState: "blocked", evidence: "unresolvedBlockers>=1", requiredMitigation: ["human-clearance-evidence", "signed-blocker-disposition"], mayEnterPhase30: false },
    { id: "release-approval-gap", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, severity: "critical", transitionState: "blocked", evidence: "releaseReady=false", requiredMitigation: ["release-owner-signoff", "exit-decision-reopened-after-blockers-clear"], mayEnterPhase30: false },
    { id: "runtime-governance-gap", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, severity: "high", transitionState: "blocked", evidence: "runtimeExecution=false", requiredMitigation: ["runtime-validation-review", "permission-policy-review"], mayEnterPhase30: false },
    { id: "phase30-entry-authorization-gap", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, severity: "critical", transitionState: "blocked", evidence: "phase30EntryReady=false", requiredMitigation: ["phase30-entry-approval", "handoff-owner-acceptance"], mayEnterPhase30: false },
    { id: "exit-ledger-unmet-criteria", sourceSchemaVersion: releaseExitCriteriaLedger.schemaVersion, severity: "high", transitionState: "blocked", evidence: "phase29ExitReady=false", requiredMitigation: ["exit-criteria-recheck", "archive-integrity-review"], mayEnterPhase30: false }
  ];
  const mitigationLanes = [
    { id: "blocker-clearance-evidence-lane", owner: "human-release-governance", state: "planning-only", linkedRisks: ["unresolved-blocker-carryover"], blockedActions: ["clear-blocker-from-transition-risk", "persist-risk-register-as-clearance"] },
    { id: "release-approval-lane", owner: "release-owner", state: "blocked-until-clearance", linkedRisks: ["release-approval-gap", "exit-ledger-unmet-criteria"], blockedActions: ["apply-risk-register-as-release-approval", "mark-releaseReady-true"] },
    { id: "runtime-governance-lane", owner: "runtime-reviewer", state: "deferred", linkedRisks: ["runtime-governance-gap"], blockedActions: ["enable-runtime-execution", "enable-third-party-execution"] },
    { id: "phase30-entry-lane", owner: "phase30-owner", state: "blocked", linkedRisks: ["phase30-entry-authorization-gap"], blockedActions: ["persist-risk-register-as-phase30-entry", "open-phase30-entry-from-risk-register"] }
  ];
  const riskChecks = [
    { id: "risk-register-linked-to-exit-preview", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "risk-register-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "risk-register-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "risk-register-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "risk-register-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
    { id: "risk-register-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-transition-risk-register.v1",
    version: "1.9.16",
    mode: "readonly-phase30-transition-risk-register-no-entry",
    sourceModels: {
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      releaseExitCriteriaLedger: releaseExitCriteriaLedger.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    riskItems,
    mitigationLanes,
    riskChecks,
    blockedMutations: [
      "persist-risk-register-as-clearance",
      "persist-risk-register-as-release-approval",
      "persist-risk-register-as-phase30-entry",
      "apply-risk-register-as-release-approval",
      "open-phase30-entry-from-risk-register",
      "clear-blocker-from-transition-risk",
      "mark-transition-risk-mitigated-without-evidence",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-transition-risk-register.json",
      includeInOperationsExport: true,
      redaction: "risk-id-severity-transition-state-evidence-and-readonly-mitigation-only"
    },
    summary: {
      phase30TransitionRiskRegisterReady: true,
      riskItems: riskItems.length,
      blockedTransitionRisks: riskItems.filter((item) => item.mayEnterPhase30 === false).length,
      criticalRisks: riskItems.filter((item) => item.severity === "critical").length,
      mitigationLanes: mitigationLanes.length,
      riskChecks: riskChecks.length,
      passedRiskChecks: riskChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29TransitionEvidenceGapReview(phase30TransitionRiskRegister = {}, releaseExitDecisionPreview = {}, finalReleaseBlockerDossier = {}, phase30EntryPreflight = {}, governanceArchiveFreeze = {}) {
  const gapItems = [
    { id: "blocker-clearance-evidence-gap", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, gapState: "open-blocking", requiredEvidence: ["human-clearance-evidence", "signed-blocker-disposition"], missingEvidence: ["human-clearance-evidence", "signed-blocker-disposition"], linkedRisks: ["unresolved-blocker-carryover"], mayClearBlocker: false, mayApproveRelease: false, mayEnterPhase30: false },
    { id: "release-signoff-evidence-gap", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, gapState: "open-blocking", requiredEvidence: ["release-owner-signoff", "approved-exit-decision"], missingEvidence: ["release-owner-signoff", "approved-exit-decision"], linkedRisks: ["release-approval-gap"], mayClearBlocker: false, mayApproveRelease: false, mayEnterPhase30: false },
    { id: "runtime-validation-evidence-gap", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, gapState: "open-blocking", requiredEvidence: ["runtime-validation-review", "permission-policy-review"], missingEvidence: ["runtime-validation-review", "permission-policy-review"], linkedRisks: ["runtime-governance-gap"], mayClearBlocker: false, mayApproveRelease: false, mayEnterPhase30: false },
    { id: "phase30-entry-approval-evidence-gap", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, gapState: "open-blocking", requiredEvidence: ["phase30-entry-approval", "handoff-owner-acceptance"], missingEvidence: ["phase30-entry-approval", "handoff-owner-acceptance"], linkedRisks: ["phase30-entry-authorization-gap"], mayClearBlocker: false, mayApproveRelease: false, mayEnterPhase30: false },
    { id: "archive-integrity-evidence-gap", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, gapState: "open-blocking", requiredEvidence: ["archive-integrity-review", "frozen-export-manifest-review"], missingEvidence: ["archive-integrity-review"], linkedRisks: ["exit-ledger-unmet-criteria"], mayClearBlocker: false, mayApproveRelease: false, mayEnterPhase30: false }
  ];
  const evidenceReviewLanes = [
    { id: "human-clearance-evidence-review", owner: "human-release-governance", state: "review-needed", linkedGaps: ["blocker-clearance-evidence-gap"], blockedActions: ["clear-blocker-from-evidence-gap-review", "persist-evidence-gap-review-as-clearance"] },
    { id: "release-signoff-review", owner: "release-owner", state: "review-needed", linkedGaps: ["release-signoff-evidence-gap"], blockedActions: ["apply-evidence-gap-review-as-release-approval", "mark-releaseReady-true"] },
    { id: "runtime-evidence-review", owner: "runtime-reviewer", state: "review-needed", linkedGaps: ["runtime-validation-evidence-gap"], blockedActions: ["enable-runtime-execution", "enable-third-party-execution"] },
    { id: "phase30-entry-evidence-review", owner: "phase30-owner", state: "blocked", linkedGaps: ["phase30-entry-approval-evidence-gap", "archive-integrity-evidence-gap"], blockedActions: ["persist-evidence-gap-review-as-phase30-entry", "open-phase30-entry-from-evidence-gap-review"] }
  ];
  const gapChecks = [
    { id: "gap-review-linked-to-risk-register", result: phase30TransitionRiskRegister.schemaVersion === "phase29.phase30-transition-risk-register.v1" ? "pass" : "block", evidence: phase30TransitionRiskRegister.schemaVersion },
    { id: "gap-review-keeps-blockers-open", result: "pass", evidence: "clearedBlockers=0" },
    { id: "gap-review-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "gap-review-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "gap-review-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "gap-review-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.transition-evidence-gap-review.v1",
    version: "1.9.17",
    mode: "readonly-transition-evidence-gap-review-no-clearance",
    sourceModels: {
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    gapItems,
    evidenceReviewLanes,
    gapChecks,
    blockedMutations: [
      "persist-evidence-gap-review-as-clearance",
      "persist-evidence-gap-review-as-release-approval",
      "persist-evidence-gap-review-as-phase30-entry",
      "apply-evidence-gap-review-as-release-approval",
      "open-phase30-entry-from-evidence-gap-review",
      "close-evidence-gap-without-human-signoff",
      "clear-blocker-from-evidence-gap-review",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-transition-evidence-gap-review.json",
      includeInOperationsExport: true,
      redaction: "gap-id-state-required-evidence-missing-evidence-and-readonly-review-lane-only"
    },
    summary: {
      transitionEvidenceGapReviewReady: true,
      gapItems: gapItems.length,
      blockingEvidenceGaps: gapItems.filter((item) => item.gapState === "open-blocking").length,
      evidenceReviewLanes: evidenceReviewLanes.length,
      gapChecks: gapChecks.length,
      passedGapChecks: gapChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29TransitionReadinessRedline(transitionEvidenceGapReview = {}, phase30TransitionRiskRegister = {}, releaseExitDecisionPreview = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const redlineItems = [
    { id: "blocker-clearance-redline", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, redlineState: "active-blocking", evidence: "blockingEvidenceGaps>=1", blockedUntil: ["human-clearance-evidence-reviewed", "signed-blocker-disposition"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "release-approval-redline", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, redlineState: "active-blocking", evidence: "releaseReady=false", blockedUntil: ["release-owner-signoff", "approved-exit-decision"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "phase29-exit-redline", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, redlineState: "active-blocking", evidence: "phase29ExitReady=false", blockedUntil: ["exit-criteria-met", "exit-decision-approved"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "phase30-entry-redline", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, redlineState: "active-blocking", evidence: "phase30EntryReady=false", blockedUntil: ["phase30-entry-approval", "handoff-owner-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "runtime-enable-redline", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, redlineState: "active-blocking", evidence: "runtimeExecution=false and thirdPartyExecution=false", blockedUntil: ["runtime-validation-review", "permission-policy-review"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false }
  ];
  const redlineReviewMatrix = [
    { id: "evidence-gap-redline-review", owner: "human-release-governance", state: "blocked", linkedRedlines: ["blocker-clearance-redline"], blockedActions: ["clear-blocker-from-redline", "persist-redline-as-clearance"] },
    { id: "release-redline-review", owner: "release-owner", state: "blocked", linkedRedlines: ["release-approval-redline", "phase29-exit-redline"], blockedActions: ["approve-release-from-redline", "persist-redline-as-release-approval"] },
    { id: "phase30-entry-redline-review", owner: "phase30-owner", state: "blocked", linkedRedlines: ["phase30-entry-redline"], blockedActions: ["persist-redline-as-phase30-entry", "open-phase30-entry-from-redline"] },
    { id: "runtime-redline-review", owner: "runtime-reviewer", state: "blocked", linkedRedlines: ["runtime-enable-redline"], blockedActions: ["enable-runtime-execution", "enable-third-party-execution"] }
  ];
  const redlineChecks = [
    { id: "redline-linked-to-evidence-gap-review", result: transitionEvidenceGapReview.schemaVersion === "phase29.transition-evidence-gap-review.v1" ? "pass" : "block", evidence: transitionEvidenceGapReview.schemaVersion },
    { id: "redline-keeps-blockers-open", result: "pass", evidence: "clearedBlockers=0" },
    { id: "redline-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "redline-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "redline-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "redline-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "redline-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.transition-readiness-redline.v1",
    version: "1.9.18",
    mode: "readonly-transition-readiness-redline-no-override",
    sourceModels: {
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    redlineItems,
    redlineReviewMatrix,
    redlineChecks,
    blockedMutations: [
      "persist-redline-as-clearance",
      "persist-redline-as-release-approval",
      "persist-redline-as-phase30-entry",
      "approve-release-from-redline",
      "open-phase30-entry-from-redline",
      "override-transition-redline",
      "clear-blocker-from-redline",
      "mark-redline-resolved-without-evidence",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-transition-readiness-redline.json",
      includeInOperationsExport: true,
      redaction: "redline-id-state-evidence-blocked-until-and-readonly-review-matrix-only"
    },
    summary: {
      transitionReadinessRedlineReady: true,
      redlineItems: redlineItems.length,
      activeRedlines: redlineItems.filter((item) => item.redlineState === "active-blocking").length,
      redlineReviewItems: redlineReviewMatrix.length,
      redlineChecks: redlineChecks.length,
      passedRedlineChecks: redlineChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29FinalTransitionHandoffPreview(transitionReadinessRedline = {}, transitionEvidenceGapReview = {}, phase30TransitionRiskRegister = {}, releaseExitDecisionPreview = {}, phase30EntryPreflight = {}, governanceArchiveFreeze = {}) {
  const handoffPreviewSections = [
    { id: "transition-readiness-redline-summary", label: "Transition readiness redline summary", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, sectionState: "readonly-blocked", evidence: "activeRedlines>=1", requiredBeforeHandoff: ["redline-review-complete", "human-release-governance-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "transition-evidence-gap-summary", label: "Transition evidence gap summary", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, sectionState: "readonly-blocked", evidence: "blockingEvidenceGaps>=1", requiredBeforeHandoff: ["blocking-evidence-gaps-closed", "signed-evidence-review"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "phase30-transition-risk-summary", label: "Phase 30 transition risk summary", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, sectionState: "readonly-blocked", evidence: "blockedTransitionRisks>=1", requiredBeforeHandoff: ["transition-risk-mitigation-reviewed", "phase30-owner-acceptance"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "release-exit-decision-summary", label: "Release exit decision summary", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, sectionState: "readonly-blocked", evidence: "phase29ExitDecision=block-exit", requiredBeforeHandoff: ["release-owner-decision", "exit-criteria-met"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "phase30-entry-guardrail-summary", label: "Phase 30 entry guardrail summary", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, sectionState: "phase30-blocked", evidence: "phase30EntryReady=false", requiredBeforeHandoff: ["phase30-entry-approval", "handoff-owner-acceptance"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "runtime-guardrail-summary", label: "Runtime guardrail summary", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, sectionState: "runtime-disabled", evidence: "runtimeExecution=false and thirdPartyExecution=false", requiredBeforeHandoff: ["runtime-validation-review", "permission-policy-review"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false }
  ];
  const handoffPreviewChecks = [
    { id: "handoff-preview-linked-to-redline", result: transitionReadinessRedline.schemaVersion === "phase29.transition-readiness-redline.v1" ? "pass" : "block", evidence: transitionReadinessRedline.schemaVersion },
    { id: "handoff-preview-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "handoff-preview-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "handoff-preview-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "handoff-preview-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "handoff-preview-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.final-transition-handoff-preview.v1",
    version: "1.9.19",
    mode: "readonly-final-transition-handoff-preview-no-entry",
    sourceModels: {
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    handoffPreviewSections,
    handoffPreviewChecks,
    blockedMutations: [
      "persist-handoff-preview-as-clearance",
      "persist-handoff-preview-as-release-approval",
      "persist-handoff-preview-as-phase30-entry",
      "approve-release-from-handoff-preview",
      "open-phase30-entry-from-handoff-preview",
      "clear-blocker-from-handoff-preview",
      "override-redline-from-handoff-preview",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-final-transition-handoff-preview.json",
      includeInOperationsExport: true,
      redaction: "handoff-section-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      finalTransitionHandoffPreviewReady: true,
      handoffPreviewSections: handoffPreviewSections.length,
      blockedHandoffPreviewSections: handoffPreviewSections.filter((item) => item.sectionState.includes("blocked")).length,
      handoffPreviewChecks: handoffPreviewChecks.length,
      passedHandoffPreviewChecks: handoffPreviewChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29TransitionClosurePackagePreview(finalTransitionHandoffPreview = {}, transitionReadinessRedline = {}, transitionEvidenceGapReview = {}, phase30TransitionRiskRegister = {}, releaseExitDecisionPreview = {}, governanceArchiveFreeze = {}, phase30EntryPreflight = {}) {
  const closurePreviewItems = [
    { id: "final-handoff-preview-lock", label: "Final handoff preview lock", sourceSchemaVersion: finalTransitionHandoffPreview.schemaVersion, previewState: "readonly-blocked", evidence: "finalTransitionHandoffPreviewReady=true", requiredBeforeClosure: ["handoff-preview-reviewed", "handoff-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "transition-redline-lock", label: "Transition redline lock", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, previewState: "readonly-blocked", evidence: "activeRedlines>=1", requiredBeforeClosure: ["redline-resolution-reviewed", "redline-override-not-requested"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "evidence-gap-lock", label: "Evidence gap lock", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, previewState: "readonly-blocked", evidence: "blockingEvidenceGaps>=1", requiredBeforeClosure: ["missing-evidence-attached", "human-evidence-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "transition-risk-lock", label: "Transition risk lock", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, previewState: "readonly-blocked", evidence: "blockedTransitionRisks>=1", requiredBeforeClosure: ["transition-risk-mitigation-reviewed", "phase30-owner-risk-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "exit-decision-lock", label: "Exit decision lock", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, previewState: "readonly-blocked", evidence: "phase29ExitDecision=block-exit", requiredBeforeClosure: ["exit-decision-approved", "release-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "archive-freeze-lock", label: "Archive freeze lock", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, previewState: "frozen-readonly", evidence: "archive remains frozen-readonly", requiredBeforeClosure: ["archive-integrity-review", "no-archive-rewrite-request"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "phase30-entry-lock", label: "Phase 30 entry lock", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, previewState: "phase30-blocked", evidence: "phase30EntryReady=false", requiredBeforeClosure: ["phase30-entry-approval", "handoff-owner-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false },
    { id: "runtime-execution-lock", label: "Runtime execution lock", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, previewState: "runtime-disabled", evidence: "runtimeExecution=false and thirdPartyExecution=false", requiredBeforeClosure: ["runtime-validation-review", "permission-policy-review"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false }
  ];
  const closurePreviewChecks = [
    { id: "closure-preview-linked-to-handoff", result: finalTransitionHandoffPreview.schemaVersion === "phase29.final-transition-handoff-preview.v1" ? "pass" : "block", evidence: finalTransitionHandoffPreview.schemaVersion },
    { id: "closure-preview-keeps-blockers-open", result: "pass", evidence: "clearedBlockers=0" },
    { id: "closure-preview-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "closure-preview-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "closure-preview-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "closure-preview-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "closure-preview-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.transition-closure-package-preview.v1",
    version: "1.9.20",
    mode: "readonly-transition-closure-package-preview-no-exit",
    sourceModels: {
      finalTransitionHandoffPreview: finalTransitionHandoffPreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion
    },
    closurePreviewItems,
    closurePreviewChecks,
    blockedMutations: [
      "persist-closure-preview-as-clearance",
      "persist-closure-preview-as-release-approval",
      "persist-closure-preview-as-phase29-exit",
      "persist-closure-preview-as-phase30-entry",
      "approve-release-from-closure-preview",
      "exit-phase29-from-closure-preview",
      "open-phase30-entry-from-closure-preview",
      "clear-blocker-from-closure-preview",
      "override-handoff-preview-from-closure-preview",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-transition-closure-package-preview.json",
      includeInOperationsExport: true,
      redaction: "closure-preview-item-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      transitionClosurePackagePreviewReady: true,
      closurePreviewItems: closurePreviewItems.length,
      blockedClosurePreviewItems: closurePreviewItems.filter((item) => item.previewState.includes("blocked")).length,
      closurePreviewChecks: closurePreviewChecks.length,
      passedClosurePreviewChecks: closurePreviewChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseCandidateFreezePreview(transitionClosurePackagePreview = {}, finalTransitionHandoffPreview = {}, transitionReadinessRedline = {}, releaseExitDecisionPreview = {}, governanceArchiveFreeze = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const freezePreviewItems = [
    { id: "closure-package-freeze", label: "Closure package freeze", sourceSchemaVersion: transitionClosurePackagePreview.schemaVersion, freezeState: "readonly-freeze-blocked", evidence: "transitionClosurePackagePreviewReady=true", requiredBeforeReleaseCandidate: ["closure-preview-reviewed", "human-release-governance-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "handoff-freeze", label: "Handoff freeze", sourceSchemaVersion: finalTransitionHandoffPreview.schemaVersion, freezeState: "readonly-freeze-blocked", evidence: "finalTransitionHandoffPreviewReady=true", requiredBeforeReleaseCandidate: ["handoff-preview-reviewed", "handoff-owner-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "redline-freeze", label: "Redline freeze", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, freezeState: "readonly-freeze-blocked", evidence: "activeRedlines>=1", requiredBeforeReleaseCandidate: ["redlines-resolved", "override-not-requested"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "exit-decision-freeze", label: "Exit decision freeze", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, freezeState: "exit-blocked", evidence: "phase29ExitDecision=block-exit", requiredBeforeReleaseCandidate: ["exit-decision-approved", "release-owner-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "archive-freeze-integrity", label: "Archive freeze integrity", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, freezeState: "frozen-readonly", evidence: "archive remains frozen-readonly", requiredBeforeReleaseCandidate: ["archive-integrity-reviewed", "no-archive-rewrite-request"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "phase30-entry-freeze", label: "Phase 30 entry freeze", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, freezeState: "phase30-blocked", evidence: "phase30EntryReady=false", requiredBeforeReleaseCandidate: ["phase30-entry-approval", "handoff-owner-acceptance"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "runtime-freeze", label: "Runtime freeze", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, freezeState: "runtime-disabled", evidence: "runtimeExecution=false and thirdPartyExecution=false", requiredBeforeReleaseCandidate: ["runtime-validation-review", "permission-policy-review"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false }
  ];
  const freezePreviewChecks = [
    { id: "freeze-preview-linked-to-closure-preview", result: transitionClosurePackagePreview.schemaVersion === "phase29.transition-closure-package-preview.v1" ? "pass" : "block", evidence: transitionClosurePackagePreview.schemaVersion },
    { id: "freeze-preview-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "freeze-preview-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "freeze-preview-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "freeze-preview-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "freeze-preview-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-candidate-freeze-preview.v1",
    version: "1.9.21",
    mode: "readonly-release-candidate-freeze-preview-no-approval",
    sourceModels: {
      transitionClosurePackagePreview: transitionClosurePackagePreview.schemaVersion,
      finalTransitionHandoffPreview: finalTransitionHandoffPreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    freezePreviewItems,
    freezePreviewChecks,
    blockedMutations: [
      "persist-freeze-preview-as-release-approval",
      "persist-freeze-preview-as-phase29-exit",
      "persist-freeze-preview-as-phase30-entry",
      "approve-release-from-freeze-preview",
      "exit-phase29-from-freeze-preview",
      "open-phase30-entry-from-freeze-preview",
      "clear-blocker-from-freeze-preview",
      "unfreeze-release-candidate-without-review",
      "override-closure-preview-from-freeze-preview",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-candidate-freeze-preview.json",
      includeInOperationsExport: true,
      redaction: "freeze-preview-item-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      releaseCandidateFreezePreviewReady: true,
      freezePreviewItems: freezePreviewItems.length,
      blockedFreezePreviewItems: freezePreviewItems.filter((item) => item.freezeState.includes("blocked")).length,
      freezePreviewChecks: freezePreviewChecks.length,
      passedFreezePreviewChecks: freezePreviewChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29FinalReadinessBoardPreview(releaseCandidateFreezePreview = {}, transitionClosurePackagePreview = {}, finalTransitionHandoffPreview = {}, transitionReadinessRedline = {}, releaseExitDecisionPreview = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const readinessBoardItems = [
    { id: "release-candidate-freeze-board", label: "Release candidate freeze board", sourceSchemaVersion: releaseCandidateFreezePreview.schemaVersion, boardState: "release-blocked-readonly", evidence: "releaseCandidateFreezePreviewReady=true", requiredBeforeFinalReadiness: ["release-candidate-freeze-reviewed", "human-release-governance-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "transition-closure-board", label: "Transition closure board", sourceSchemaVersion: transitionClosurePackagePreview.schemaVersion, boardState: "exit-blocked-readonly", evidence: "transitionClosurePackagePreviewReady=true", requiredBeforeFinalReadiness: ["transition-closure-reviewed", "phase29-exit-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "handoff-preview-board", label: "Handoff preview board", sourceSchemaVersion: finalTransitionHandoffPreview.schemaVersion, boardState: "handoff-blocked-readonly", evidence: "blockedHandoffPreviewSections>=1", requiredBeforeFinalReadiness: ["handoff-preview-reviewed", "handoff-owner-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "transition-redline-board", label: "Transition redline board", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, boardState: "redline-blocked-readonly", evidence: "activeRedlines>=1", requiredBeforeFinalReadiness: ["redlines-resolved", "override-not-requested"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "release-exit-decision-board", label: "Release exit decision board", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, boardState: "exit-decision-blocked", evidence: "phase29ExitDecision=block-exit", requiredBeforeFinalReadiness: ["exit-decision-approved", "release-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "phase30-entry-board", label: "Phase 30 entry board", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, boardState: "phase30-blocked", evidence: "phase30EntryReady=false", requiredBeforeFinalReadiness: ["phase30-entry-approval", "handoff-owner-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "runtime-readiness-board", label: "Runtime readiness board", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, boardState: "runtime-disabled", evidence: "runtimeExecution=false and thirdPartyExecution=false", requiredBeforeFinalReadiness: ["runtime-validation-review", "permission-policy-review"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false }
  ];
  const readinessBoardChecks = [
    { id: "board-linked-to-freeze-preview", result: releaseCandidateFreezePreview.schemaVersion === "phase29.release-candidate-freeze-preview.v1" ? "pass" : "block", evidence: releaseCandidateFreezePreview.schemaVersion },
    { id: "board-linked-to-transition-closure", result: transitionClosurePackagePreview.schemaVersion === "phase29.transition-closure-package-preview.v1" ? "pass" : "block", evidence: transitionClosurePackagePreview.schemaVersion },
    { id: "board-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "board-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "board-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "board-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "board-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.final-readiness-board-preview.v1",
    version: "1.9.22",
    mode: "readonly-final-readiness-board-preview-no-approval",
    sourceModels: {
      releaseCandidateFreezePreview: releaseCandidateFreezePreview.schemaVersion,
      transitionClosurePackagePreview: transitionClosurePackagePreview.schemaVersion,
      finalTransitionHandoffPreview: finalTransitionHandoffPreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    readinessBoardItems,
    readinessBoardChecks,
    blockedMutations: [
      "persist-readiness-board-as-release-approval",
      "persist-readiness-board-as-phase29-exit",
      "persist-readiness-board-as-phase30-entry",
      "approve-release-from-readiness-board",
      "exit-phase29-from-readiness-board",
      "open-phase30-entry-from-readiness-board",
      "clear-blocker-from-readiness-board",
      "override-freeze-preview-from-readiness-board",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-final-readiness-board-preview.json",
      includeInOperationsExport: true,
      redaction: "readiness-board-item-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      finalReadinessBoardPreviewReady: true,
      readinessBoardItems: readinessBoardItems.length,
      blockedReadinessBoardItems: readinessBoardItems.filter((item) => item.boardState.includes("blocked")).length,
      readinessBoardChecks: readinessBoardChecks.length,
      passedReadinessBoardChecks: readinessBoardChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseFinalReviewEnvelopePreview(finalReadinessBoardPreview = {}, releaseCandidateFreezePreview = {}, transitionClosurePackagePreview = {}, transitionReadinessRedline = {}, releaseExitDecisionPreview = {}, phase30EntryPreflight = {}, governanceArchiveFreeze = {}, runtimeEnableGovernance = {}) {
  const envelopeSections = [
    { id: "final-readiness-board-envelope", label: "Final readiness board envelope", sourceSchemaVersion: finalReadinessBoardPreview.schemaVersion, envelopeState: "review-open-readonly", evidence: "finalReadinessBoardPreviewReady=true", requiredBeforeFinalReview: ["final-board-reviewed", "release-governance-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "release-candidate-freeze-envelope", label: "Release candidate freeze envelope", sourceSchemaVersion: releaseCandidateFreezePreview.schemaVersion, envelopeState: "freeze-review-blocked", evidence: "releaseCandidateFreezePreviewReady=true", requiredBeforeFinalReview: ["freeze-preview-reviewed", "no-unfreeze-request"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "transition-closure-envelope", label: "Transition closure envelope", sourceSchemaVersion: transitionClosurePackagePreview.schemaVersion, envelopeState: "phase29-exit-blocked", evidence: "transitionClosurePackagePreviewReady=true", requiredBeforeFinalReview: ["transition-closure-reviewed", "phase29-exit-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "redline-envelope", label: "Redline envelope", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, envelopeState: "redline-blocked", evidence: "activeRedlines>=1", requiredBeforeFinalReview: ["redlines-resolved", "override-not-requested"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "exit-decision-envelope", label: "Exit decision envelope", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, envelopeState: "exit-decision-blocked", evidence: "phase29ExitDecision=block-exit", requiredBeforeFinalReview: ["exit-decision-approved", "release-owner-signoff"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "phase30-entry-envelope", label: "Phase 30 entry envelope", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, envelopeState: "phase30-entry-blocked", evidence: "phase30EntryReady=false", requiredBeforeFinalReview: ["phase30-entry-approval", "handoff-owner-acceptance"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "archive-and-runtime-envelope", label: "Archive and runtime envelope", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, runtimeSourceSchemaVersion: runtimeEnableGovernance.schemaVersion, envelopeState: "runtime-disabled-readonly", evidence: "archive frozen and runtimeExecution=false", requiredBeforeFinalReview: ["archive-integrity-review", "runtime-validation-review"], mayClearBlocker: false, mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false }
  ];
  const envelopeChecks = [
    { id: "envelope-linked-to-readiness-board", result: finalReadinessBoardPreview.schemaVersion === "phase29.final-readiness-board-preview.v1" ? "pass" : "block", evidence: finalReadinessBoardPreview.schemaVersion },
    { id: "envelope-linked-to-freeze-preview", result: releaseCandidateFreezePreview.schemaVersion === "phase29.release-candidate-freeze-preview.v1" ? "pass" : "block", evidence: releaseCandidateFreezePreview.schemaVersion },
    { id: "envelope-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "envelope-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "envelope-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "envelope-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "envelope-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-final-review-envelope-preview.v1",
    version: "1.9.23",
    mode: "readonly-release-final-review-envelope-preview-no-approval",
    sourceModels: {
      finalReadinessBoardPreview: finalReadinessBoardPreview.schemaVersion,
      releaseCandidateFreezePreview: releaseCandidateFreezePreview.schemaVersion,
      transitionClosurePackagePreview: transitionClosurePackagePreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    envelopeSections,
    envelopeChecks,
    blockedMutations: [
      "persist-final-review-envelope-as-release-approval",
      "persist-final-review-envelope-as-phase29-exit",
      "persist-final-review-envelope-as-phase30-entry",
      "approve-release-from-final-review-envelope",
      "exit-phase29-from-final-review-envelope",
      "open-phase30-entry-from-final-review-envelope",
      "clear-blocker-from-final-review-envelope",
      "override-readiness-board-from-final-review-envelope",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-final-review-envelope-preview.json",
      includeInOperationsExport: true,
      redaction: "final-review-envelope-section-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      releaseFinalReviewEnvelopePreviewReady: true,
      envelopeSections: envelopeSections.length,
      blockedEnvelopeSections: envelopeSections.filter((item) => item.envelopeState.includes("blocked")).length,
      envelopeChecks: envelopeChecks.length,
      passedEnvelopeChecks: envelopeChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ExitReadinessAttestationPreview(releaseFinalReviewEnvelopePreview = {}, finalReadinessBoardPreview = {}, releaseExitDecisionPreview = {}, phase30EntryPreflight = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const attestationItems = [
    { id: "final-review-envelope-attestation", label: "Final review envelope attestation", sourceSchemaVersion: releaseFinalReviewEnvelopePreview.schemaVersion, attestationState: "reviewed-readonly-blocked", evidence: "releaseFinalReviewEnvelopePreviewReady=true", missingBeforeExit: ["final-human-governance-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "final-readiness-board-attestation", label: "Final readiness board attestation", sourceSchemaVersion: finalReadinessBoardPreview.schemaVersion, attestationState: "board-ready-readonly-blocked", evidence: "finalReadinessBoardPreviewReady=true", missingBeforeExit: ["board-owner-attestation"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "release-exit-decision-attestation", label: "Release exit decision attestation", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, attestationState: "phase29-exit-blocked", evidence: "phase29ExitDecision=block-exit", missingBeforeExit: ["exit-decision-approved", "release-owner-signoff"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "phase30-entry-attestation", label: "Phase 30 entry attestation", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, attestationState: "phase30-entry-blocked", evidence: "phase30EntryReady=false", missingBeforeExit: ["phase30-entry-approval", "handoff-owner-acceptance"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "redline-attestation", label: "Redline attestation", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, attestationState: "redline-active-blocked", evidence: "activeRedlines>=1", missingBeforeExit: ["redlines-resolved", "override-not-requested"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "blocker-dossier-attestation", label: "Blocker dossier attestation", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, attestationState: "blockers-carried-forward", evidence: "unresolvedBlockers>=1", missingBeforeExit: ["blocker-clearance-evidence", "release-owner-review"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false },
    { id: "runtime-and-archive-attestation", label: "Runtime and archive attestation", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, attestationState: "runtime-disabled-archive-frozen", evidence: "runtimeExecution=false and archive frozen-readonly", missingBeforeExit: ["runtime-validation-review", "archive-integrity-review"], mayApproveRelease: false, mayExitPhase29: false, mayEnterPhase30: false, mayEnableRuntime: false }
  ];
  const attestationChecks = [
    { id: "attestation-linked-to-final-review-envelope", result: releaseFinalReviewEnvelopePreview.schemaVersion === "phase29.release-final-review-envelope-preview.v1" ? "pass" : "block", evidence: releaseFinalReviewEnvelopePreview.schemaVersion },
    { id: "attestation-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "attestation-keeps-exit-blocked", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "attestation-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "attestation-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "attestation-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.exit-readiness-attestation-preview.v1",
    version: "1.9.24",
    mode: "readonly-exit-readiness-attestation-preview-no-exit",
    sourceModels: {
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      finalReadinessBoardPreview: finalReadinessBoardPreview.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    attestationItems,
    attestationChecks,
    blockedMutations: [
      "persist-attestation-as-release-approval",
      "persist-attestation-as-phase29-exit",
      "persist-attestation-as-phase30-entry",
      "approve-release-from-attestation",
      "exit-phase29-from-attestation",
      "open-phase30-entry-from-attestation",
      "clear-blocker-from-attestation",
      "override-final-review-envelope-from-attestation",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-exit-readiness-attestation-preview.json",
      includeInOperationsExport: true,
      redaction: "attestation-item-state-source-evidence-missing-condition-and-readonly-guardrail-summary-only"
    },
    summary: {
      exitReadinessAttestationPreviewReady: true,
      attestationItems: attestationItems.length,
      blockedAttestationItems: attestationItems.filter((item) => item.attestationState.includes("blocked")).length,
      attestationChecks: attestationChecks.length,
      passedAttestationChecks: attestationChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30PlanningBriefPreview(exitReadinessAttestationPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, releaseFinalReviewEnvelopePreview = {}, finalReadinessBoardPreview = {}, governanceArchiveFreeze = {}) {
  const briefSections = [
    { id: "phase30-scope-boundary", label: "Phase 30 scope boundary", sourceSchemaVersion: exitReadinessAttestationPreview.schemaVersion, briefState: "planning-brief-readonly", evidence: "phase29 remains active and Phase 30 entry remains blocked", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "entry-preflight-blockers", label: "Entry preflight blockers", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, briefState: "phase30-entry-blocked", evidence: "blockedEntryGates>=1", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "transition-risk-summary", label: "Transition risk summary", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, briefState: "transition-risk-open-blocked", evidence: "blockedTransitionRisks>=1", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "evidence-gap-summary", label: "Evidence gap summary", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, briefState: "evidence-gaps-open-blocked", evidence: "blockingEvidenceGaps>=1", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "redline-and-review-envelope", label: "Redline and review envelope", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, envelopeSourceSchemaVersion: releaseFinalReviewEnvelopePreview.schemaVersion, briefState: "redline-active-review-open", evidence: "activeTransitionRedlines>=1 and final review remains readonly", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "archive-and-readiness-board", label: "Archive and readiness board", sourceSchemaVersion: governanceArchiveFreeze.schemaVersion, boardSourceSchemaVersion: finalReadinessBoardPreview.schemaVersion, briefState: "archive-frozen-readiness-board-blocked", evidence: "archive frozen and readiness board does not approve release", phase30PlanningOnly: true, mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const briefChecks = [
    { id: "planning-brief-linked-to-attestation", result: exitReadinessAttestationPreview.schemaVersion === "phase29.exit-readiness-attestation-preview.v1" ? "pass" : "block", evidence: exitReadinessAttestationPreview.schemaVersion },
    { id: "planning-brief-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "planning-brief-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "planning-brief-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "planning-brief-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "planning-brief-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-planning-brief-preview.v1",
    version: "1.9.25",
    mode: "readonly-phase30-planning-brief-preview-no-entry",
    sourceModels: {
      exitReadinessAttestationPreview: exitReadinessAttestationPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      finalReadinessBoardPreview: finalReadinessBoardPreview.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    briefSections,
    briefChecks,
    blockedMutations: [
      "persist-phase30-brief-as-release-approval",
      "persist-phase30-brief-as-phase29-exit",
      "persist-phase30-brief-as-phase30-entry",
      "approve-release-from-phase30-brief",
      "exit-phase29-from-phase30-brief",
      "open-phase30-entry-from-phase30-brief",
      "clear-blocker-from-phase30-brief",
      "resolve-redline-from-phase30-brief",
      "override-attestation-from-phase30-brief",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-planning-brief-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-planning-brief-section-state-source-evidence-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30PlanningBriefPreviewReady: true,
      briefSections: briefSections.length,
      blockedBriefSections: briefSections.filter((item) => item.briefState.includes("blocked")).length,
      briefChecks: briefChecks.length,
      passedBriefChecks: briefChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30ScopeLockPreview(phase30PlanningBriefPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const scopeLockItems = [
    { id: "phase30-entry-boundary-lock", label: "Phase 30 entry boundary lock", sourceSchemaVersion: phase30PlanningBriefPreview.schemaVersion, lockState: "phase30-entry-locked-readonly", evidence: "phase30PlanningBriefPreviewReady=true and phase30EntryReady=false", lockedScope: ["planning-inputs-only", "no-phase-transition"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "preflight-gate-lock", label: "Preflight gate lock", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, lockState: "preflight-blockers-locked", evidence: "blockedEntryGates>=1", lockedScope: ["entry-gates", "preflight-blockers"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "transition-risk-lock", label: "Transition risk lock", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, lockState: "transition-risks-locked", evidence: "blockedTransitionRisks>=1", lockedScope: ["risk-register", "mitigation-lanes"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "evidence-gap-lock", label: "Evidence gap lock", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, lockState: "evidence-gaps-locked", evidence: "blockingEvidenceGaps>=1", lockedScope: ["evidence-gaps", "human-signoff-required"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "redline-lock", label: "Transition redline lock", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, lockState: "redlines-locked-no-override", evidence: "activeRedlines>=1", lockedScope: ["redline-review", "override-blocked"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "release-and-runtime-lock", label: "Release and runtime lock", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, runtimeSourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, lockState: "release-runtime-archive-locked", evidence: "releaseReady=false, runtimeExecution=false, archive frozen", lockedScope: ["release-approval", "runtime-enable", "archive-freeze"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const scopeLockChecks = [
    { id: "scope-lock-linked-to-planning-brief", result: phase30PlanningBriefPreview.schemaVersion === "phase29.phase30-planning-brief-preview.v1" ? "pass" : "block", evidence: phase30PlanningBriefPreview.schemaVersion },
    { id: "scope-lock-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "scope-lock-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "scope-lock-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "scope-lock-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "scope-lock-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-scope-lock-preview.v1",
    version: "1.9.26",
    mode: "readonly-phase30-scope-lock-preview-no-entry",
    sourceModels: {
      phase30PlanningBriefPreview: phase30PlanningBriefPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    scopeLockItems,
    scopeLockChecks,
    blockedMutations: [
      "persist-scope-lock-as-release-approval",
      "persist-scope-lock-as-phase29-exit",
      "persist-scope-lock-as-phase30-entry",
      "approve-release-from-scope-lock",
      "exit-phase29-from-scope-lock",
      "open-phase30-entry-from-scope-lock",
      "clear-blocker-from-scope-lock",
      "unlock-phase30-scope-without-review",
      "override-planning-brief-from-scope-lock",
      "resolve-redline-from-scope-lock",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-scope-lock-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-scope-lock-item-state-source-evidence-locked-scope-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30ScopeLockPreviewReady: true,
      scopeLockItems: scopeLockItems.length,
      blockedScopeLockItems: scopeLockItems.filter((item) => item.lockState.includes("locked")).length,
      scopeLockChecks: scopeLockChecks.length,
      passedScopeLockChecks: scopeLockChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30ReadinessMapPreview(phase30ScopeLockPreview = {}, phase30PlanningBriefPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const readinessMapItems = [
    { id: "scope-lock-readiness-map", label: "Scope lock readiness map", sourceSchemaVersion: phase30ScopeLockPreview.schemaVersion, readinessState: "scope-locked-readonly", evidence: "phase30ScopeLockPreviewReady=true and phase30EntryReady=false", requiredBeforePhase30: ["scope-lock-reviewed", "entry-boundary-remains-locked"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "planning-brief-readiness-map", label: "Planning brief readiness map", sourceSchemaVersion: phase30PlanningBriefPreview.schemaVersion, readinessState: "planning-brief-mapped-blocked", evidence: "phase30PlanningBriefPreviewReady=true and planningOnly=true", requiredBeforePhase30: ["planning-brief-human-reviewed", "planning-brief-not-persisted-as-entry"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "entry-preflight-readiness-map", label: "Entry preflight readiness map", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, readinessState: "preflight-blocked-readonly", evidence: "blockedEntryGates>=1", requiredBeforePhase30: ["entry-gates-resolved-with-evidence", "human-entry-signoff"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "transition-risk-readiness-map", label: "Transition risk readiness map", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, readinessState: "transition-risk-mapped-blocked", evidence: "blockedTransitionRisks>=1", requiredBeforePhase30: ["risk-mitigation-evidence", "transition-risk-review"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "evidence-gap-readiness-map", label: "Evidence gap readiness map", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, readinessState: "evidence-gap-mapped-blocked", evidence: "blockingEvidenceGaps>=1", requiredBeforePhase30: ["evidence-gap-human-signoff", "gap-closure-proof"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "redline-readiness-map", label: "Redline readiness map", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, readinessState: "redline-active-readonly", evidence: "activeRedlines>=1", requiredBeforePhase30: ["redline-resolution-evidence", "no-override-without-review"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "blocker-dossier-readiness-map", label: "Blocker dossier readiness map", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, readinessState: "blocker-dossier-mapped-blocked", evidence: "unresolvedBlockers>=1 and blockedReleaseGates>=1", requiredBeforePhase30: ["blocker-disposition-recorded", "release-gates-reviewed"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "release-exit-readiness-map", label: "Release exit readiness map", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, readinessState: "release-exit-blocked-readonly", evidence: "phase29ExitReady=false and releaseReady=false", requiredBeforePhase30: ["signed-exit-decision", "release-approval-separated"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "runtime-archive-readiness-map", label: "Runtime and archive readiness map", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, readinessState: "runtime-archive-locked-readonly", evidence: "runtimeExecution=false, thirdPartyExecution=false, archive frozen", requiredBeforePhase30: ["runtime-enable-review", "archive-freeze-retained"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const readinessMapChecks = [
    { id: "readiness-map-linked-to-scope-lock", result: phase30ScopeLockPreview.schemaVersion === "phase29.phase30-scope-lock-preview.v1" ? "pass" : "block", evidence: phase30ScopeLockPreview.schemaVersion },
    { id: "readiness-map-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "readiness-map-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "readiness-map-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "readiness-map-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "readiness-map-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-readiness-map-preview.v1",
    version: "1.9.27",
    mode: "readonly-phase30-readiness-map-preview-no-entry",
    sourceModels: {
      phase30ScopeLockPreview: phase30ScopeLockPreview.schemaVersion,
      phase30PlanningBriefPreview: phase30PlanningBriefPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    readinessMapItems,
    readinessMapChecks,
    blockedMutations: [
      "persist-readiness-map-as-release-approval",
      "persist-readiness-map-as-phase29-exit",
      "persist-readiness-map-as-phase30-entry",
      "approve-release-from-readiness-map",
      "exit-phase29-from-readiness-map",
      "open-phase30-entry-from-readiness-map",
      "clear-blocker-from-readiness-map",
      "resolve-redline-from-readiness-map",
      "override-scope-lock-from-readiness-map",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-readiness-map-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-readiness-map-item-state-source-evidence-required-before-phase30-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30ReadinessMapPreviewReady: true,
      readinessMapItems: readinessMapItems.length,
      blockedReadinessMapItems: readinessMapItems.filter((item) => item.readinessState.includes("blocked") || item.readinessState.includes("locked") || item.readinessState.includes("active")).length,
      readinessMapChecks: readinessMapChecks.length,
      passedReadinessMapChecks: readinessMapChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30ReadinessGapBriefPreview(phase30ReadinessMapPreview = {}, phase30ScopeLockPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const gapBriefItems = [
    { id: "scope-lock-gap-brief", label: "Scope lock gap brief", sourceSchemaVersion: phase30ScopeLockPreview.schemaVersion, gapState: "scope-gap-blocked-readonly", evidence: "scope lock remains read-only and phase30EntryReady=false", missingBeforePhase30: ["scope-lock-human-review", "entry-boundary-release-decision"], actionOwner: "release-governance", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "entry-preflight-gap-brief", label: "Entry preflight gap brief", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, gapState: "entry-preflight-gap-blocked", evidence: "blockedEntryGates>=1", missingBeforePhase30: ["entry-gate-evidence", "human-entry-signoff"], actionOwner: "phase30-entry-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "transition-risk-gap-brief", label: "Transition risk gap brief", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, gapState: "transition-risk-gap-blocked", evidence: "blockedTransitionRisks>=1", missingBeforePhase30: ["risk-mitigation-proof", "risk-owner-disposition"], actionOwner: "transition-risk-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "evidence-gap-brief", label: "Evidence gap brief", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, gapState: "evidence-gap-blocked", evidence: "blockingEvidenceGaps>=1", missingBeforePhase30: ["gap-closure-evidence", "human-evidence-signoff"], actionOwner: "evidence-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "redline-gap-brief", label: "Redline gap brief", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, gapState: "redline-gap-blocked", evidence: "activeRedlines>=1", missingBeforePhase30: ["redline-resolution", "no-override-review"], actionOwner: "release-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "blocker-dossier-gap-brief", label: "Blocker dossier gap brief", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, gapState: "blocker-dossier-gap-blocked", evidence: "unresolvedBlockers>=1 and blockedReleaseGates>=1", missingBeforePhase30: ["blocker-clearance-evidence", "release-gate-disposition"], actionOwner: "blocker-clearance-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "release-exit-gap-brief", label: "Release exit gap brief", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, gapState: "release-exit-gap-blocked", evidence: "phase29ExitReady=false and releaseReady=false", missingBeforePhase30: ["signed-release-exit-decision", "release-approval-separated"], actionOwner: "release-approver", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "runtime-archive-gap-brief", label: "Runtime and archive gap brief", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, gapState: "runtime-archive-gap-blocked", evidence: "runtimeExecution=false, thirdPartyExecution=false, archive frozen", missingBeforePhase30: ["runtime-enable-review", "archive-freeze-retention-review"], actionOwner: "runtime-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const gapBriefChecks = [
    { id: "gap-brief-linked-to-readiness-map", result: phase30ReadinessMapPreview.schemaVersion === "phase29.phase30-readiness-map-preview.v1" ? "pass" : "block", evidence: phase30ReadinessMapPreview.schemaVersion },
    { id: "gap-brief-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "gap-brief-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "gap-brief-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "gap-brief-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "gap-brief-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-readiness-gap-brief-preview.v1",
    version: "1.9.28",
    mode: "readonly-phase30-readiness-gap-brief-preview-no-entry",
    sourceModels: {
      phase30ReadinessMapPreview: phase30ReadinessMapPreview.schemaVersion,
      phase30ScopeLockPreview: phase30ScopeLockPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    gapBriefItems,
    gapBriefChecks,
    blockedMutations: [
      "persist-gap-brief-as-release-approval",
      "persist-gap-brief-as-phase29-exit",
      "persist-gap-brief-as-phase30-entry",
      "approve-release-from-gap-brief",
      "exit-phase29-from-gap-brief",
      "open-phase30-entry-from-gap-brief",
      "clear-blocker-from-gap-brief",
      "resolve-redline-from-gap-brief",
      "override-readiness-map-from-gap-brief",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-readiness-gap-brief-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-readiness-gap-brief-item-state-source-evidence-missing-before-phase30-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30ReadinessGapBriefPreviewReady: true,
      gapBriefItems: gapBriefItems.length,
      blockedGapBriefItems: gapBriefItems.filter((item) => item.gapState.includes("blocked")).length,
      gapBriefChecks: gapBriefChecks.length,
      passedGapBriefChecks: gapBriefChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30GapResolutionSequencePreview(phase30ReadinessGapBriefPreview = {}, phase30ReadinessMapPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const resolutionSequenceItems = [
    { id: "sequence-scope-lock-review", label: "Scope lock review sequence", sequenceOrder: 1, sourceSchemaVersion: phase30ReadinessGapBriefPreview.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "scope-lock-gap-brief remains blocked", prerequisiteGapIds: [], requiredEvidence: ["scope-lock-human-review", "entry-boundary-release-decision"], owner: "release-governance", blockedBy: ["phase30EntryReady=false"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-entry-preflight-evidence", label: "Entry preflight evidence sequence", sequenceOrder: 2, sourceSchemaVersion: phase30EntryPreflight.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "blockedEntryGates>=1", prerequisiteGapIds: ["scope-lock-gap-brief"], requiredEvidence: ["entry-gate-evidence", "human-entry-signoff"], owner: "phase30-entry-reviewer", blockedBy: ["preflight-blockers-open"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-transition-risk-mitigation", label: "Transition risk mitigation sequence", sequenceOrder: 3, sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "blockedTransitionRisks>=1", prerequisiteGapIds: ["entry-preflight-gap-brief"], requiredEvidence: ["risk-mitigation-proof", "risk-owner-disposition"], owner: "transition-risk-owner", blockedBy: ["transition-risk-open"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-evidence-gap-closure", label: "Evidence gap closure sequence", sequenceOrder: 4, sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "blockingEvidenceGaps>=1", prerequisiteGapIds: ["transition-risk-gap-brief"], requiredEvidence: ["gap-closure-evidence", "human-evidence-signoff"], owner: "evidence-reviewer", blockedBy: ["evidence-gap-open"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-redline-resolution", label: "Redline resolution sequence", sequenceOrder: 5, sourceSchemaVersion: transitionReadinessRedline.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "activeRedlines>=1", prerequisiteGapIds: ["evidence-gap-brief"], requiredEvidence: ["redline-resolution", "no-override-review"], owner: "release-owner", blockedBy: ["active-redlines"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-blocker-dossier-disposition", label: "Blocker dossier disposition sequence", sequenceOrder: 6, sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "unresolvedBlockers>=1 and blockedReleaseGates>=1", prerequisiteGapIds: ["redline-gap-brief"], requiredEvidence: ["blocker-clearance-evidence", "release-gate-disposition"], owner: "blocker-clearance-owner", blockedBy: ["unresolved-blockers"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-release-exit-decision", label: "Release exit decision sequence", sequenceOrder: 7, sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "phase29ExitReady=false and releaseReady=false", prerequisiteGapIds: ["blocker-dossier-gap-brief"], requiredEvidence: ["signed-release-exit-decision", "release-approval-separated"], owner: "release-approver", blockedBy: ["releaseReady=false", "phase29ExitReady=false"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "sequence-runtime-archive-review", label: "Runtime and archive review sequence", sequenceOrder: 8, sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, resolutionState: "blocked-readonly-sequenced", evidence: "runtimeExecution=false, thirdPartyExecution=false, archive frozen", prerequisiteGapIds: ["release-exit-gap-brief"], requiredEvidence: ["runtime-enable-review", "archive-freeze-retention-review"], owner: "runtime-reviewer", blockedBy: ["runtime-disabled", "archive-frozen"], mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const sequenceChecks = [
    { id: "sequence-linked-to-gap-brief", result: phase30ReadinessGapBriefPreview.schemaVersion === "phase29.phase30-readiness-gap-brief-preview.v1" ? "pass" : "block", evidence: phase30ReadinessGapBriefPreview.schemaVersion },
    { id: "sequence-linked-to-readiness-map", result: phase30ReadinessMapPreview.schemaVersion === "phase29.phase30-readiness-map-preview.v1" ? "pass" : "block", evidence: phase30ReadinessMapPreview.schemaVersion },
    { id: "sequence-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "sequence-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "sequence-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "sequence-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "sequence-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-gap-resolution-sequence-preview.v1",
    version: "1.9.29",
    mode: "readonly-phase30-gap-resolution-sequence-preview-no-entry",
    sourceModels: {
      phase30ReadinessGapBriefPreview: phase30ReadinessGapBriefPreview.schemaVersion,
      phase30ReadinessMapPreview: phase30ReadinessMapPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    resolutionSequenceItems,
    sequenceChecks,
    blockedMutations: [
      "persist-resolution-sequence-as-release-approval",
      "persist-resolution-sequence-as-phase29-exit",
      "persist-resolution-sequence-as-phase30-entry",
      "approve-release-from-resolution-sequence",
      "exit-phase29-from-resolution-sequence",
      "open-phase30-entry-from-resolution-sequence",
      "clear-blocker-from-resolution-sequence",
      "resolve-redline-from-resolution-sequence",
      "override-gap-brief-from-resolution-sequence",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-gap-resolution-sequence-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-gap-resolution-sequence-item-order-state-source-evidence-owner-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30GapResolutionSequencePreviewReady: true,
      resolutionSequenceItems: resolutionSequenceItems.length,
      blockedResolutionSequenceItems: resolutionSequenceItems.filter((item) => item.resolutionState.includes("blocked")).length,
      sequenceChecks: sequenceChecks.length,
      passedSequenceChecks: sequenceChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidencePackPreview(phase30GapResolutionSequencePreview = {}, phase30ReadinessGapBriefPreview = {}, phase30ReadinessMapPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const evidencePackItems = [
    { id: "entry-boundary-evidence-pack", label: "Entry boundary evidence pack", sourceSchemaVersion: phase30ReadinessMapPreview.schemaVersion, packState: "blocked-readonly-pack", evidence: "phase30EntryReady=false and readiness map remains blocked", requiredEvidence: ["phase30-entry-boundary-review", "scope-lock-human-disposition"], sourceSequenceIds: ["sequence-scope-lock-review"], owner: "release-governance", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "preflight-evidence-pack", label: "Preflight evidence pack", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, packState: "blocked-readonly-pack", evidence: "blockedEntryGates>=1", requiredEvidence: ["entry-gate-evidence", "human-entry-signoff"], sourceSequenceIds: ["sequence-entry-preflight-evidence"], owner: "phase30-entry-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "transition-risk-evidence-pack", label: "Transition risk evidence pack", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, packState: "blocked-readonly-pack", evidence: "blockedTransitionRisks>=1", requiredEvidence: ["risk-mitigation-proof", "risk-owner-disposition"], sourceSequenceIds: ["sequence-transition-risk-mitigation"], owner: "transition-risk-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "evidence-gap-evidence-pack", label: "Evidence gap evidence pack", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, packState: "blocked-readonly-pack", evidence: "blockingEvidenceGaps>=1", requiredEvidence: ["gap-closure-evidence", "human-evidence-signoff"], sourceSequenceIds: ["sequence-evidence-gap-closure"], owner: "evidence-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "redline-evidence-pack", label: "Redline evidence pack", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, packState: "blocked-readonly-pack", evidence: "activeRedlines>=1", requiredEvidence: ["redline-resolution", "no-override-review"], sourceSequenceIds: ["sequence-redline-resolution"], owner: "release-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "blocker-dossier-evidence-pack", label: "Blocker dossier evidence pack", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, packState: "blocked-readonly-pack", evidence: "unresolvedBlockers>=1 and blockedReleaseGates>=1", requiredEvidence: ["blocker-clearance-evidence", "release-gate-disposition"], sourceSequenceIds: ["sequence-blocker-dossier-disposition"], owner: "blocker-clearance-owner", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "release-exit-evidence-pack", label: "Release exit evidence pack", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, packState: "blocked-readonly-pack", evidence: "phase29ExitReady=false and releaseReady=false", requiredEvidence: ["signed-release-exit-decision", "release-approval-separated"], sourceSequenceIds: ["sequence-release-exit-decision"], owner: "release-approver", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "runtime-archive-evidence-pack", label: "Runtime and archive evidence pack", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, packState: "blocked-readonly-pack", evidence: "runtimeExecution=false, thirdPartyExecution=false, archive frozen", requiredEvidence: ["runtime-enable-review", "archive-freeze-retention-review"], sourceSequenceIds: ["sequence-runtime-archive-review"], owner: "runtime-reviewer", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const evidencePackChecks = [
    { id: "entry-evidence-pack-linked-to-resolution-sequence", result: phase30GapResolutionSequencePreview.schemaVersion === "phase29.phase30-gap-resolution-sequence-preview.v1" ? "pass" : "block", evidence: phase30GapResolutionSequencePreview.schemaVersion },
    { id: "entry-evidence-pack-linked-to-gap-brief", result: phase30ReadinessGapBriefPreview.schemaVersion === "phase29.phase30-readiness-gap-brief-preview.v1" ? "pass" : "block", evidence: phase30ReadinessGapBriefPreview.schemaVersion },
    { id: "entry-evidence-pack-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "entry-evidence-pack-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "entry-evidence-pack-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "entry-evidence-pack-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "entry-evidence-pack-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-pack-preview.v1",
    version: "1.9.30",
    mode: "readonly-phase30-entry-evidence-pack-preview-no-entry",
    sourceModels: {
      phase30GapResolutionSequencePreview: phase30GapResolutionSequencePreview.schemaVersion,
      phase30ReadinessGapBriefPreview: phase30ReadinessGapBriefPreview.schemaVersion,
      phase30ReadinessMapPreview: phase30ReadinessMapPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    evidencePackItems,
    evidencePackChecks,
    blockedMutations: [
      "persist-entry-evidence-pack-as-release-approval",
      "persist-entry-evidence-pack-as-phase29-exit",
      "persist-entry-evidence-pack-as-phase30-entry",
      "approve-release-from-entry-evidence-pack",
      "exit-phase29-from-entry-evidence-pack",
      "open-phase30-entry-from-entry-evidence-pack",
      "clear-blocker-from-entry-evidence-pack",
      "resolve-redline-from-entry-evidence-pack",
      "override-resolution-sequence-from-entry-evidence-pack",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-pack-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-pack-item-state-source-evidence-sequence-owner-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidencePackPreviewReady: true,
      evidencePackItems: evidencePackItems.length,
      blockedEvidencePackItems: evidencePackItems.filter((item) => item.packState.includes("blocked")).length,
      evidencePackChecks: evidencePackChecks.length,
      passedEvidencePackChecks: evidencePackChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidenceIndexPreview(phase30EntryEvidencePackPreview = {}, phase30GapResolutionSequencePreview = {}, phase30ReadinessGapBriefPreview = {}, phase30ReadinessMapPreview = {}, phase30EntryPreflight = {}, phase30TransitionRiskRegister = {}, transitionEvidenceGapReview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}, governanceArchiveFreeze = {}) {
  const evidenceIndexItems = [
    { id: "index-entry-boundary-evidence", label: "Entry boundary evidence index", evidenceType: "entry-boundary", sourceSchemaVersion: phase30ReadinessMapPreview.schemaVersion, sourcePackIds: ["entry-boundary-evidence-pack"], sourceSequenceIds: ["sequence-scope-lock-review"], blockerCategory: "phase30-entry-boundary", owner: "release-governance", indexState: "blocked-readonly-index", evidence: "phase30EntryReady=false and readiness map remains blocked", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-preflight-evidence", label: "Preflight evidence index", evidenceType: "entry-preflight", sourceSchemaVersion: phase30EntryPreflight.schemaVersion, sourcePackIds: ["preflight-evidence-pack"], sourceSequenceIds: ["sequence-entry-preflight-evidence"], blockerCategory: "preflight-blockers-open", owner: "phase30-entry-reviewer", indexState: "blocked-readonly-index", evidence: "blockedEntryGates>=1", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-transition-risk-evidence", label: "Transition risk evidence index", evidenceType: "transition-risk", sourceSchemaVersion: phase30TransitionRiskRegister.schemaVersion, sourcePackIds: ["transition-risk-evidence-pack"], sourceSequenceIds: ["sequence-transition-risk-mitigation"], blockerCategory: "transition-risk-open", owner: "transition-risk-owner", indexState: "blocked-readonly-index", evidence: "blockedTransitionRisks>=1", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-evidence-gap-evidence", label: "Evidence gap evidence index", evidenceType: "evidence-gap", sourceSchemaVersion: transitionEvidenceGapReview.schemaVersion, sourcePackIds: ["evidence-gap-evidence-pack"], sourceSequenceIds: ["sequence-evidence-gap-closure"], blockerCategory: "evidence-gap-open", owner: "evidence-reviewer", indexState: "blocked-readonly-index", evidence: "blockingEvidenceGaps>=1", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-redline-evidence", label: "Redline evidence index", evidenceType: "transition-redline", sourceSchemaVersion: transitionReadinessRedline.schemaVersion, sourcePackIds: ["redline-evidence-pack"], sourceSequenceIds: ["sequence-redline-resolution"], blockerCategory: "active-redlines", owner: "release-owner", indexState: "blocked-readonly-index", evidence: "activeRedlines>=1", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-blocker-dossier-evidence", label: "Blocker dossier evidence index", evidenceType: "blocker-dossier", sourceSchemaVersion: finalReleaseBlockerDossier.schemaVersion, sourcePackIds: ["blocker-dossier-evidence-pack"], sourceSequenceIds: ["sequence-blocker-dossier-disposition"], blockerCategory: "unresolved-blockers", owner: "blocker-clearance-owner", indexState: "blocked-readonly-index", evidence: "unresolvedBlockers>=1 and blockedReleaseGates>=1", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-release-exit-evidence", label: "Release exit evidence index", evidenceType: "release-exit", sourceSchemaVersion: releaseExitDecisionPreview.schemaVersion, sourcePackIds: ["release-exit-evidence-pack"], sourceSequenceIds: ["sequence-release-exit-decision"], blockerCategory: "release-and-phase29-exit-blocked", owner: "release-approver", indexState: "blocked-readonly-index", evidence: "phase29ExitReady=false and releaseReady=false", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "index-runtime-archive-evidence", label: "Runtime and archive evidence index", evidenceType: "runtime-archive", sourceSchemaVersion: runtimeEnableGovernance.schemaVersion, archiveSourceSchemaVersion: governanceArchiveFreeze.schemaVersion, sourcePackIds: ["runtime-archive-evidence-pack"], sourceSequenceIds: ["sequence-runtime-archive-review"], blockerCategory: "runtime-disabled-and-archive-frozen", owner: "runtime-reviewer", indexState: "blocked-readonly-index", evidence: "runtimeExecution=false, thirdPartyExecution=false, archive frozen", mayExitPhase29: false, mayEnterPhase30: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const evidenceIndexChecks = [
    { id: "entry-evidence-index-linked-to-pack", result: phase30EntryEvidencePackPreview.schemaVersion === "phase29.phase30-entry-evidence-pack-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidencePackPreview.schemaVersion },
    { id: "entry-evidence-index-linked-to-resolution-sequence", result: phase30GapResolutionSequencePreview.schemaVersion === "phase29.phase30-gap-resolution-sequence-preview.v1" ? "pass" : "block", evidence: phase30GapResolutionSequencePreview.schemaVersion },
    { id: "entry-evidence-index-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "entry-evidence-index-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "entry-evidence-index-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "entry-evidence-index-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" },
    { id: "entry-evidence-index-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-index-preview.v1",
    version: "1.9.31",
    mode: "readonly-phase30-entry-evidence-index-preview-no-entry",
    sourceModels: {
      phase30EntryEvidencePackPreview: phase30EntryEvidencePackPreview.schemaVersion,
      phase30GapResolutionSequencePreview: phase30GapResolutionSequencePreview.schemaVersion,
      phase30ReadinessGapBriefPreview: phase30ReadinessGapBriefPreview.schemaVersion,
      phase30ReadinessMapPreview: phase30ReadinessMapPreview.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      phase30TransitionRiskRegister: phase30TransitionRiskRegister.schemaVersion,
      transitionEvidenceGapReview: transitionEvidenceGapReview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion
    },
    evidenceIndexItems,
    evidenceIndexChecks,
    blockedMutations: [
      "persist-entry-evidence-index-as-release-approval",
      "persist-entry-evidence-index-as-phase29-exit",
      "persist-entry-evidence-index-as-phase30-entry",
      "approve-release-from-entry-evidence-index",
      "exit-phase29-from-entry-evidence-index",
      "open-phase30-entry-from-entry-evidence-index",
      "clear-blocker-from-entry-evidence-index",
      "resolve-redline-from-entry-evidence-index",
      "override-entry-evidence-pack-from-entry-evidence-index",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-index-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-index-type-source-pack-sequence-owner-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidenceIndexPreviewReady: true,
      evidenceIndexItems: evidenceIndexItems.length,
      blockedEvidenceIndexItems: evidenceIndexItems.filter((item) => item.indexState.includes("blocked")).length,
      evidenceIndexChecks: evidenceIndexChecks.length,
      passedEvidenceIndexChecks: evidenceIndexChecks.filter((item) => item.result === "pass").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidenceReviewQueuePreview(phase30EntryEvidenceIndexPreview = {}, phase30EntryEvidencePackPreview = {}, phase30GapResolutionSequencePreview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const evidenceReviewQueueItems = (phase30EntryEvidenceIndexPreview.evidenceIndexItems || []).map((item, index) => ({
    id: `review-${item.id.replace(/^index-/, "")}`,
    label: item.label.replace("index", "review queue"),
    sourceIndexItemId: item.id,
    evidenceType: item.evidenceType,
    sourceSchemaVersion: item.sourceSchemaVersion,
    sourcePackIds: item.sourcePackIds || [],
    sourceSequenceIds: item.sourceSequenceIds || [],
    blockerCategory: item.blockerCategory,
    owner: item.owner,
    reviewerRole: item.owner,
    queueLane: item.blockerCategory,
    queueOrder: index + 1,
    priority: ["active-redlines", "unresolved-blockers", "release-and-phase29-exit-blocked"].includes(item.blockerCategory) ? "high" : "normal",
    queueState: "blocked-readonly-review-pending",
    requiredReviewActions: ["inspect-index-evidence", "verify-source-pack-link", "record-non-persistent-review-note"],
    blockedBy: [item.blockerCategory, "phase29ExitReady=false", "phase30EntryReady=false", "releaseReady=false"],
    mayRecordPersistentDisposition: false,
    mayClearBlocker: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayApproveRelease: false,
    mayEnableRuntime: false
  }));
  const evidenceReviewQueueChecks = [
    { id: "entry-evidence-review-queue-linked-to-index", result: phase30EntryEvidenceIndexPreview.schemaVersion === "phase29.phase30-entry-evidence-index-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceIndexPreview.schemaVersion },
    { id: "entry-evidence-review-queue-linked-to-pack", result: phase30EntryEvidencePackPreview.schemaVersion === "phase29.phase30-entry-evidence-pack-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidencePackPreview.schemaVersion },
    { id: "entry-evidence-review-queue-linked-to-resolution-sequence", result: phase30GapResolutionSequencePreview.schemaVersion === "phase29.phase30-gap-resolution-sequence-preview.v1" ? "pass" : "block", evidence: phase30GapResolutionSequencePreview.schemaVersion },
    { id: "entry-evidence-review-queue-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: `activeRedlines=${transitionReadinessRedline.summary?.activeRedlines ?? 0}` },
    { id: "entry-evidence-review-queue-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "entry-evidence-review-queue-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "entry-evidence-review-queue-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "entry-evidence-review-queue-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "entry-evidence-review-queue-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "entry-evidence-review-queue-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-review-queue-preview.v1",
    version: "1.9.32",
    mode: "readonly-phase30-entry-evidence-review-queue-preview-no-disposition",
    sourceModels: {
      phase30EntryEvidenceIndexPreview: phase30EntryEvidenceIndexPreview.schemaVersion,
      phase30EntryEvidencePackPreview: phase30EntryEvidencePackPreview.schemaVersion,
      phase30GapResolutionSequencePreview: phase30GapResolutionSequencePreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    evidenceReviewQueueItems,
    evidenceReviewQueueChecks,
    blockedMutations: [
      "persist-entry-evidence-review-queue-as-release-approval",
      "persist-entry-evidence-review-queue-as-phase29-exit",
      "persist-entry-evidence-review-queue-as-phase30-entry",
      "persist-review-disposition-from-entry-evidence-review-queue",
      "approve-release-from-entry-evidence-review-queue",
      "exit-phase29-from-entry-evidence-review-queue",
      "open-phase30-entry-from-entry-evidence-review-queue",
      "clear-blocker-from-entry-evidence-review-queue",
      "resolve-redline-from-entry-evidence-review-queue",
      "mark-entry-evidence-reviewed",
      "override-entry-evidence-index-from-review-queue",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-review-queue-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-review-queue-source-index-lane-owner-priority-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidenceReviewQueuePreviewReady: true,
      evidenceReviewQueueItems: evidenceReviewQueueItems.length,
      blockedEvidenceReviewQueueItems: evidenceReviewQueueItems.filter((item) => item.queueState.includes("blocked")).length,
      evidenceReviewQueueChecks: evidenceReviewQueueChecks.length,
      passedEvidenceReviewQueueChecks: evidenceReviewQueueChecks.filter((item) => item.result === "pass").length,
      reviewLanes: new Set(evidenceReviewQueueItems.map((item) => item.queueLane)).size,
      highPriorityReviewItems: evidenceReviewQueueItems.filter((item) => item.priority === "high").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidenceReviewDispositionPreview(phase30EntryEvidenceReviewQueuePreview = {}, phase30EntryEvidenceIndexPreview = {}, phase30EntryEvidencePackPreview = {}, transitionReadinessRedline = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const dispositionPreviewItems = (phase30EntryEvidenceReviewQueuePreview.evidenceReviewQueueItems || []).map((item) => ({
    id: `disposition-${item.sourceIndexItemId?.replace(/^index-/, "") || item.id.replace(/^review-/, "")}`,
    label: item.label.replace("review queue", "review disposition"),
    sourceQueueItemId: item.id,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    queueLane: item.queueLane,
    priority: item.priority,
    owner: item.owner,
    reviewerRole: item.reviewerRole,
    dispositionPreviewState: "blocked-readonly-disposition-preview",
    candidateDisposition: item.priority === "high" ? "hold-for-human-evidence" : "needs-evidence-review",
    dispositionReason: `${item.blockerCategory} remains open; Phase 30 entry, Phase 29 exit, release approval, and runtime stay blocked`,
    requiredEvidence: ["human-review-note", "source-evidence-verified", "blocker-state-unchanged"],
    unresolvedGuardrails: item.blockedBy || [],
    mayPersistDisposition: false,
    mayClearBlocker: false,
    mayResolveRedline: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayApproveRelease: false,
    mayEnableRuntime: false
  }));
  const dispositionPreviewChecks = [
    { id: "entry-evidence-review-disposition-linked-to-queue", result: phase30EntryEvidenceReviewQueuePreview.schemaVersion === "phase29.phase30-entry-evidence-review-queue-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewQueuePreview.schemaVersion },
    { id: "entry-evidence-review-disposition-linked-to-index", result: phase30EntryEvidenceIndexPreview.schemaVersion === "phase29.phase30-entry-evidence-index-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceIndexPreview.schemaVersion },
    { id: "entry-evidence-review-disposition-linked-to-pack", result: phase30EntryEvidencePackPreview.schemaVersion === "phase29.phase30-entry-evidence-pack-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidencePackPreview.schemaVersion },
    { id: "entry-evidence-review-disposition-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: `activeRedlines=${transitionReadinessRedline.summary?.activeRedlines ?? 0}` },
    { id: "entry-evidence-review-disposition-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "entry-evidence-review-disposition-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "entry-evidence-review-disposition-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "entry-evidence-review-disposition-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "entry-evidence-review-disposition-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "entry-evidence-review-disposition-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-review-disposition-preview.v1",
    version: "1.9.33",
    mode: "readonly-phase30-entry-evidence-review-disposition-preview-no-disposition",
    sourceModels: {
      phase30EntryEvidenceReviewQueuePreview: phase30EntryEvidenceReviewQueuePreview.schemaVersion,
      phase30EntryEvidenceIndexPreview: phase30EntryEvidenceIndexPreview.schemaVersion,
      phase30EntryEvidencePackPreview: phase30EntryEvidencePackPreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    dispositionPreviewItems,
    dispositionPreviewChecks,
    blockedMutations: [
      "persist-entry-evidence-review-disposition-as-release-approval",
      "persist-entry-evidence-review-disposition-as-phase29-exit",
      "persist-entry-evidence-review-disposition-as-phase30-entry",
      "persist-review-disposition-preview",
      "approve-release-from-entry-evidence-review-disposition",
      "exit-phase29-from-entry-evidence-review-disposition",
      "open-phase30-entry-from-entry-evidence-review-disposition",
      "clear-blocker-from-entry-evidence-review-disposition",
      "resolve-redline-from-entry-evidence-review-disposition",
      "mark-entry-evidence-dispositioned",
      "override-entry-evidence-review-queue-from-disposition",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-review-disposition-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-review-disposition-source-queue-candidate-reason-owner-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidenceReviewDispositionPreviewReady: true,
      dispositionPreviewItems: dispositionPreviewItems.length,
      blockedDispositionPreviewItems: dispositionPreviewItems.filter((item) => item.dispositionPreviewState.includes("blocked")).length,
      dispositionPreviewChecks: dispositionPreviewChecks.length,
      passedDispositionPreviewChecks: dispositionPreviewChecks.filter((item) => item.result === "pass").length,
      holdForEvidenceItems: dispositionPreviewItems.filter((item) => item.candidateDisposition === "hold-for-human-evidence").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidenceDispositionLedgerPreview(phase30EntryEvidenceReviewDispositionPreview = {}, phase30EntryEvidenceReviewQueuePreview = {}, phase30EntryEvidenceIndexPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const dispositionLedgerEntries = (phase30EntryEvidenceReviewDispositionPreview.dispositionPreviewItems || []).map((item, index) => ({
    id: `ledger-${item.id.replace(/^disposition-/, "")}`,
    label: item.label.replace("review disposition", "disposition ledger"),
    ledgerOrder: index + 1,
    sourceDispositionItemId: item.id,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    ledgerState: "blocked-readonly-ledger-entry",
    ledgerReason: item.dispositionReason,
    requiredEvidence: item.requiredEvidence || [],
    unresolvedGuardrails: item.unresolvedGuardrails || [],
    auditPreview: {
      eventType: "entry-evidence-disposition-previewed",
      persisted: false,
      releaseReady: false,
      phase29ExitReady: false,
      phase30EntryReady: false
    },
    mayPersistLedgerEntry: false,
    mayPersistDisposition: false,
    mayClearBlocker: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayApproveRelease: false,
    mayEnableRuntime: false
  }));
  const dispositionLedgerChecks = [
    { id: "entry-evidence-disposition-ledger-linked-to-disposition-preview", result: phase30EntryEvidenceReviewDispositionPreview.schemaVersion === "phase29.phase30-entry-evidence-review-disposition-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewDispositionPreview.schemaVersion },
    { id: "entry-evidence-disposition-ledger-linked-to-review-queue", result: phase30EntryEvidenceReviewQueuePreview.schemaVersion === "phase29.phase30-entry-evidence-review-queue-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewQueuePreview.schemaVersion },
    { id: "entry-evidence-disposition-ledger-linked-to-index", result: phase30EntryEvidenceIndexPreview.schemaVersion === "phase29.phase30-entry-evidence-index-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceIndexPreview.schemaVersion },
    { id: "entry-evidence-disposition-ledger-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "entry-evidence-disposition-ledger-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "entry-evidence-disposition-ledger-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "entry-evidence-disposition-ledger-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "entry-evidence-disposition-ledger-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "entry-evidence-disposition-ledger-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-disposition-ledger-preview.v1",
    version: "1.9.34",
    mode: "readonly-phase30-entry-evidence-disposition-ledger-preview-no-ledger-persist",
    sourceModels: {
      phase30EntryEvidenceReviewDispositionPreview: phase30EntryEvidenceReviewDispositionPreview.schemaVersion,
      phase30EntryEvidenceReviewQueuePreview: phase30EntryEvidenceReviewQueuePreview.schemaVersion,
      phase30EntryEvidenceIndexPreview: phase30EntryEvidenceIndexPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    dispositionLedgerEntries,
    dispositionLedgerChecks,
    blockedMutations: [
      "persist-entry-evidence-disposition-ledger-as-release-approval",
      "persist-entry-evidence-disposition-ledger-as-phase29-exit",
      "persist-entry-evidence-disposition-ledger-as-phase30-entry",
      "persist-entry-evidence-disposition-ledger-entry",
      "persist-review-disposition-from-ledger",
      "approve-release-from-entry-evidence-disposition-ledger",
      "exit-phase29-from-entry-evidence-disposition-ledger",
      "open-phase30-entry-from-entry-evidence-disposition-ledger",
      "clear-blocker-from-entry-evidence-disposition-ledger",
      "resolve-redline-from-entry-evidence-disposition-ledger",
      "mark-entry-evidence-ledger-final",
      "override-entry-evidence-review-disposition-from-ledger",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-disposition-ledger-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-disposition-ledger-source-disposition-candidate-owner-audit-preview-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidenceDispositionLedgerPreviewReady: true,
      dispositionLedgerEntries: dispositionLedgerEntries.length,
      blockedDispositionLedgerEntries: dispositionLedgerEntries.filter((item) => item.ledgerState.includes("blocked")).length,
      dispositionLedgerChecks: dispositionLedgerChecks.length,
      passedDispositionLedgerChecks: dispositionLedgerChecks.filter((item) => item.result === "pass").length,
      holdForEvidenceLedgerEntries: dispositionLedgerEntries.filter((item) => item.candidateDisposition === "hold-for-human-evidence").length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryEvidenceFinalLedgerExportPreview(phase30EntryEvidenceDispositionLedgerPreview = {}, phase30EntryEvidenceReviewDispositionPreview = {}, phase30EntryEvidenceReviewQueuePreview = {}, phase30EntryEvidenceIndexPreview = {}, phase30EntryEvidencePackPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const finalLedgerExportItems = (phase30EntryEvidenceDispositionLedgerPreview.dispositionLedgerEntries || []).map((item, index) => ({
    id: `final-export-${item.id.replace(/^ledger-/, "")}`,
    label: item.label.replace("disposition ledger", "final ledger export"),
    exportOrder: index + 1,
    sourceLedgerEntryId: item.id,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    exportState: "blocked-readonly-final-ledger-export",
    exportReason: item.ledgerReason,
    includedInPreview: true,
    persisted: false,
    requiredEvidence: item.requiredEvidence || [],
    unresolvedGuardrails: item.unresolvedGuardrails || [],
    auditPreview: {
      sourceEventType: item.auditPreview?.eventType,
      finalExportPreviewed: true,
      persisted: false,
      releaseReady: false,
      phase29ExitReady: false,
      phase30EntryReady: false
    },
    mayPersistFinalExport: false,
    mayPersistLedgerEntry: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false
  }));
  const finalLedgerExportChecks = [
    { id: "final-ledger-export-linked-to-disposition-ledger", result: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion === "phase29.phase30-entry-evidence-disposition-ledger-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion },
    { id: "final-ledger-export-linked-to-review-disposition", result: phase30EntryEvidenceReviewDispositionPreview.schemaVersion === "phase29.phase30-entry-evidence-review-disposition-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewDispositionPreview.schemaVersion },
    { id: "final-ledger-export-linked-to-review-queue", result: phase30EntryEvidenceReviewQueuePreview.schemaVersion === "phase29.phase30-entry-evidence-review-queue-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewQueuePreview.schemaVersion },
    { id: "final-ledger-export-linked-to-index", result: phase30EntryEvidenceIndexPreview.schemaVersion === "phase29.phase30-entry-evidence-index-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceIndexPreview.schemaVersion },
    { id: "final-ledger-export-linked-to-evidence-pack", result: phase30EntryEvidencePackPreview.schemaVersion === "phase29.phase30-entry-evidence-pack-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidencePackPreview.schemaVersion },
    { id: "final-ledger-export-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "final-ledger-export-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "final-ledger-export-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "final-ledger-export-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "final-ledger-export-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "final-ledger-export-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-evidence-final-ledger-export-preview.v1",
    version: "1.9.35",
    mode: "readonly-phase30-entry-evidence-final-ledger-export-preview-no-export-persist",
    sourceModels: {
      phase30EntryEvidenceDispositionLedgerPreview: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion,
      phase30EntryEvidenceReviewDispositionPreview: phase30EntryEvidenceReviewDispositionPreview.schemaVersion,
      phase30EntryEvidenceReviewQueuePreview: phase30EntryEvidenceReviewQueuePreview.schemaVersion,
      phase30EntryEvidenceIndexPreview: phase30EntryEvidenceIndexPreview.schemaVersion,
      phase30EntryEvidencePackPreview: phase30EntryEvidencePackPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    finalLedgerExportItems,
    finalLedgerExportChecks,
    blockedMutations: [
      "persist-entry-evidence-final-ledger-export-as-release-approval",
      "persist-entry-evidence-final-ledger-export-as-phase29-exit",
      "persist-entry-evidence-final-ledger-export-as-phase30-entry",
      "persist-entry-evidence-final-ledger-export-artifact",
      "persist-entry-evidence-final-ledger-export-item",
      "approve-release-from-entry-evidence-final-ledger-export",
      "exit-phase29-from-entry-evidence-final-ledger-export",
      "open-phase30-entry-from-entry-evidence-final-ledger-export",
      "clear-blocker-from-entry-evidence-final-ledger-export",
      "resolve-redline-from-entry-evidence-final-ledger-export",
      "mark-entry-evidence-final-ledger-export-ready",
      "override-entry-evidence-disposition-ledger-from-final-export",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-evidence-final-ledger-export-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-evidence-final-ledger-source-ledger-disposition-owner-audit-preview-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryEvidenceFinalLedgerExportPreviewReady: true,
      finalLedgerExportItems: finalLedgerExportItems.length,
      blockedFinalLedgerExportItems: finalLedgerExportItems.filter((item) => item.exportState.includes("blocked")).length,
      finalLedgerExportChecks: finalLedgerExportChecks.length,
      passedFinalLedgerExportChecks: finalLedgerExportChecks.filter((item) => item.result === "pass").length,
      includedFinalLedgerExportItems: finalLedgerExportItems.filter((item) => item.includedInPreview).length,
      persistedFinalLedgerExportItems: finalLedgerExportItems.filter((item) => item.persisted).length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryFinalExportReconciliationPreview(phase30EntryEvidenceFinalLedgerExportPreview = {}, phase30EntryEvidenceDispositionLedgerPreview = {}, phase30EntryEvidenceReviewDispositionPreview = {}, phase30EntryEvidenceReviewQueuePreview = {}, phase30EntryEvidenceIndexPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const ledgerEntries = phase30EntryEvidenceDispositionLedgerPreview.dispositionLedgerEntries || [];
  const finalExportItems = phase30EntryEvidenceFinalLedgerExportPreview.finalLedgerExportItems || [];
  const finalExportLedgerIds = new Set(finalExportItems.map((item) => item.sourceLedgerEntryId));
  const reconciliationItems = finalExportItems.map((item, index) => {
    const sourceLedgerEntry = ledgerEntries.find((entry) => entry.id === item.sourceLedgerEntryId) || {};
    return {
      id: `reconcile-${item.id.replace(/^final-export-/, "")}`,
      label: item.label.replace("final ledger export", "final export reconciliation"),
      reconciliationOrder: index + 1,
      sourceFinalExportItemId: item.id,
      sourceLedgerEntryId: item.sourceLedgerEntryId,
      sourceDispositionItemId: item.sourceDispositionItemId,
      sourceQueueItemId: item.sourceQueueItemId,
      sourceIndexItemId: item.sourceIndexItemId,
      evidenceType: item.evidenceType,
      owner: item.owner,
      candidateDisposition: item.candidateDisposition,
      reconciliationState: "blocked-readonly-final-export-reconciliation",
      coverageState: sourceLedgerEntry.id ? "covered-by-final-export-preview" : "missing-source-ledger-entry",
      persistedState: item.persisted === false ? "not-persisted" : "unexpected-persisted",
      blockerState: "carried-forward",
      sourceLedgerState: sourceLedgerEntry.ledgerState || "missing",
      finalExportState: item.exportState,
      mayPersistReconciliation: false,
      mayPersistFinalExport: false,
      mayApproveRelease: false,
      mayExitPhase29: false,
      mayEnterPhase30: false,
      mayEnableRuntime: false
    };
  });
  const uncoveredLedgerEntries = ledgerEntries.filter((item) => !finalExportLedgerIds.has(item.id));
  const reconciliationChecks = [
    { id: "final-export-reconciliation-linked-to-final-export", result: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion === "phase29.phase30-entry-evidence-final-ledger-export-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion },
    { id: "final-export-reconciliation-linked-to-disposition-ledger", result: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion === "phase29.phase30-entry-evidence-disposition-ledger-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion },
    { id: "final-export-reconciliation-linked-to-review-disposition", result: phase30EntryEvidenceReviewDispositionPreview.schemaVersion === "phase29.phase30-entry-evidence-review-disposition-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewDispositionPreview.schemaVersion },
    { id: "final-export-reconciliation-linked-to-review-queue", result: phase30EntryEvidenceReviewQueuePreview.schemaVersion === "phase29.phase30-entry-evidence-review-queue-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceReviewQueuePreview.schemaVersion },
    { id: "final-export-reconciliation-linked-to-index", result: phase30EntryEvidenceIndexPreview.schemaVersion === "phase29.phase30-entry-evidence-index-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceIndexPreview.schemaVersion },
    { id: "final-export-reconciliation-covers-all-ledger-entries", result: uncoveredLedgerEntries.length === 0 && finalExportItems.length === ledgerEntries.length ? "pass" : "block", evidence: `finalExportItems=${finalExportItems.length}; ledgerEntries=${ledgerEntries.length}; uncovered=${uncoveredLedgerEntries.length}` },
    { id: "final-export-reconciliation-keeps-final-export-unpersisted", result: finalExportItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedFinalExportItems=${finalExportItems.filter((item) => item.persisted !== false).length}` },
    { id: "final-export-reconciliation-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "final-export-reconciliation-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "final-export-reconciliation-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "final-export-reconciliation-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "final-export-reconciliation-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "final-export-reconciliation-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-final-export-reconciliation-preview.v1",
    version: "1.9.36",
    mode: "readonly-phase30-entry-final-export-reconciliation-preview-no-reconciliation-persist",
    sourceModels: {
      phase30EntryEvidenceFinalLedgerExportPreview: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion,
      phase30EntryEvidenceDispositionLedgerPreview: phase30EntryEvidenceDispositionLedgerPreview.schemaVersion,
      phase30EntryEvidenceReviewDispositionPreview: phase30EntryEvidenceReviewDispositionPreview.schemaVersion,
      phase30EntryEvidenceReviewQueuePreview: phase30EntryEvidenceReviewQueuePreview.schemaVersion,
      phase30EntryEvidenceIndexPreview: phase30EntryEvidenceIndexPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    reconciliationItems,
    uncoveredLedgerEntries: uncoveredLedgerEntries.map((item) => item.id),
    reconciliationChecks,
    blockedMutations: [
      "persist-entry-final-export-reconciliation-as-release-approval",
      "persist-entry-final-export-reconciliation-as-phase29-exit",
      "persist-entry-final-export-reconciliation-as-phase30-entry",
      "persist-entry-final-export-reconciliation-artifact",
      "persist-entry-final-export-reconciliation-item",
      "persist-final-ledger-export-from-reconciliation",
      "approve-release-from-entry-final-export-reconciliation",
      "exit-phase29-from-entry-final-export-reconciliation",
      "open-phase30-entry-from-entry-final-export-reconciliation",
      "clear-blocker-from-entry-final-export-reconciliation",
      "resolve-redline-from-entry-final-export-reconciliation",
      "mark-entry-final-export-reconciliation-complete",
      "override-entry-final-ledger-export-from-reconciliation",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-final-export-reconciliation-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-final-export-reconciliation-source-ledger-final-export-coverage-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryFinalExportReconciliationPreviewReady: true,
      reconciliationItems: reconciliationItems.length,
      blockedReconciliationItems: reconciliationItems.filter((item) => item.reconciliationState.includes("blocked")).length,
      coveredLedgerEntries: reconciliationItems.filter((item) => item.coverageState === "covered-by-final-export-preview").length,
      uncoveredLedgerEntries: uncoveredLedgerEntries.length,
      reconciliationChecks: reconciliationChecks.length,
      passedReconciliationChecks: reconciliationChecks.filter((item) => item.result === "pass").length,
      persistedFinalExportItems: finalExportItems.filter((item) => item.persisted !== false).length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryReconciliationSignoffPreview(phase30EntryFinalExportReconciliationPreview = {}, phase30EntryEvidenceFinalLedgerExportPreview = {}, releaseFinalReviewEnvelopePreview = {}, exitReadinessAttestationPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const reconciliationItems = phase30EntryFinalExportReconciliationPreview.reconciliationItems || [];
  const requiredSignoffRoles = ["release-owner", "phase29-exit-reviewer", "phase30-entry-reviewer", "runtime-governance-reviewer"];
  const signoffItems = reconciliationItems.map((item, index) => ({
    id: `signoff-${item.id.replace(/^reconcile-/, "")}`,
    label: item.label.replace("final export reconciliation", "reconciliation signoff"),
    signoffOrder: index + 1,
    sourceReconciliationItemId: item.id,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles,
    signoffState: "blocked-readonly-reconciliation-signoff-preview",
    evidenceCoverageState: item.coverageState,
    reconciliationPersistedState: item.persistedState,
    releaseBlockerState: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "blockers-open" : "blockers-missing",
    signoffPersistenceState: "not-persisted",
    mayRecordSignoff: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const signoffChecks = [
    { id: "reconciliation-signoff-linked-to-reconciliation-preview", result: phase30EntryFinalExportReconciliationPreview.schemaVersion === "phase29.phase30-entry-final-export-reconciliation-preview.v1" ? "pass" : "block", evidence: phase30EntryFinalExportReconciliationPreview.schemaVersion },
    { id: "reconciliation-signoff-linked-to-final-export", result: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion === "phase29.phase30-entry-evidence-final-ledger-export-preview.v1" ? "pass" : "block", evidence: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion },
    { id: "reconciliation-signoff-linked-to-final-review-envelope", result: releaseFinalReviewEnvelopePreview.schemaVersion === "phase29.release-final-review-envelope-preview.v1" ? "pass" : "block", evidence: releaseFinalReviewEnvelopePreview.schemaVersion },
    { id: "reconciliation-signoff-linked-to-exit-attestation", result: exitReadinessAttestationPreview.schemaVersion === "phase29.exit-readiness-attestation-preview.v1" ? "pass" : "block", evidence: exitReadinessAttestationPreview.schemaVersion },
    { id: "reconciliation-signoff-covers-all-reconciliation-items", result: signoffItems.length === reconciliationItems.length && (phase30EntryFinalExportReconciliationPreview.summary?.uncoveredLedgerEntries || 0) === 0 ? "pass" : "block", evidence: `signoffItems=${signoffItems.length}; reconciliationItems=${reconciliationItems.length}; uncovered=${phase30EntryFinalExportReconciliationPreview.summary?.uncoveredLedgerEntries || 0}` },
    { id: "reconciliation-signoff-keeps-signoffs-unpersisted", result: signoffItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedSignoffs=${signoffItems.filter((item) => item.persisted !== false).length}` },
    { id: "reconciliation-signoff-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "reconciliation-signoff-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "reconciliation-signoff-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "reconciliation-signoff-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "reconciliation-signoff-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "reconciliation-signoff-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-reconciliation-signoff-preview.v1",
    version: "1.9.37",
    mode: "readonly-phase30-entry-reconciliation-signoff-preview-no-signoff-persist",
    sourceModels: {
      phase30EntryFinalExportReconciliationPreview: phase30EntryFinalExportReconciliationPreview.schemaVersion,
      phase30EntryEvidenceFinalLedgerExportPreview: phase30EntryEvidenceFinalLedgerExportPreview.schemaVersion,
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      exitReadinessAttestationPreview: exitReadinessAttestationPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    signoffItems,
    signoffChecks,
    blockedMutations: [
      "persist-entry-reconciliation-signoff-as-release-approval",
      "persist-entry-reconciliation-signoff-as-phase29-exit",
      "persist-entry-reconciliation-signoff-as-phase30-entry",
      "persist-entry-reconciliation-signoff-artifact",
      "persist-entry-reconciliation-signoff-item",
      "persist-final-export-reconciliation-from-signoff",
      "record-reconciliation-signoff",
      "approve-release-from-entry-reconciliation-signoff",
      "exit-phase29-from-entry-reconciliation-signoff",
      "open-phase30-entry-from-entry-reconciliation-signoff",
      "clear-blocker-from-entry-reconciliation-signoff",
      "resolve-redline-from-entry-reconciliation-signoff",
      "mark-entry-reconciliation-signoff-complete",
      "override-entry-final-export-reconciliation-from-signoff",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-reconciliation-signoff-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-reconciliation-signoff-role-source-coverage-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryReconciliationSignoffPreviewReady: true,
      signoffItems: signoffItems.length,
      blockedSignoffItems: signoffItems.filter((item) => item.signoffState.includes("blocked")).length,
      signoffChecks: signoffChecks.length,
      passedSignoffChecks: signoffChecks.filter((item) => item.result === "pass").length,
      requiredSignoffRoles: requiredSignoffRoles.length,
      signedOffItems: 0,
      persistedSignoffs: signoffItems.filter((item) => item.persisted !== false).length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntrySignoffAuditPreview(phase30EntryReconciliationSignoffPreview = {}, phase30EntryFinalExportReconciliationPreview = {}, releaseFinalReviewEnvelopePreview = {}, exitReadinessAttestationPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const signoffItems = phase30EntryReconciliationSignoffPreview.signoffItems || [];
  const auditItems = signoffItems.map((item, index) => ({
    id: `audit-${item.id.replace(/^signoff-/, "")}`,
    label: item.label.replace("reconciliation signoff", "signoff audit"),
    auditOrder: index + 1,
    sourceSignoffItemId: item.id,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    auditState: "blocked-readonly-signoff-audit-preview",
    signoffState: item.signoffState,
    evidenceCoverageState: item.evidenceCoverageState,
    signoffPersistenceState: item.signoffPersistenceState,
    auditPersistenceState: "not-persisted",
    releaseBlockerState: item.releaseBlockerState,
    mayPersistAudit: false,
    mayRecordSignoff: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const auditChecks = [
    { id: "signoff-audit-linked-to-signoff-preview", result: phase30EntryReconciliationSignoffPreview.schemaVersion === "phase29.phase30-entry-reconciliation-signoff-preview.v1" ? "pass" : "block", evidence: phase30EntryReconciliationSignoffPreview.schemaVersion },
    { id: "signoff-audit-linked-to-reconciliation-preview", result: phase30EntryFinalExportReconciliationPreview.schemaVersion === "phase29.phase30-entry-final-export-reconciliation-preview.v1" ? "pass" : "block", evidence: phase30EntryFinalExportReconciliationPreview.schemaVersion },
    { id: "signoff-audit-linked-to-final-review-envelope", result: releaseFinalReviewEnvelopePreview.schemaVersion === "phase29.release-final-review-envelope-preview.v1" ? "pass" : "block", evidence: releaseFinalReviewEnvelopePreview.schemaVersion },
    { id: "signoff-audit-linked-to-exit-attestation", result: exitReadinessAttestationPreview.schemaVersion === "phase29.exit-readiness-attestation-preview.v1" ? "pass" : "block", evidence: exitReadinessAttestationPreview.schemaVersion },
    { id: "signoff-audit-covers-all-signoff-items", result: auditItems.length === signoffItems.length ? "pass" : "block", evidence: `auditItems=${auditItems.length}; signoffItems=${signoffItems.length}` },
    { id: "signoff-audit-keeps-signoffs-unpersisted", result: signoffItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedSignoffs=${signoffItems.filter((item) => item.persisted !== false).length}` },
    { id: "signoff-audit-keeps-audit-unpersisted", result: auditItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedAuditItems=${auditItems.filter((item) => item.persisted !== false).length}` },
    { id: "signoff-audit-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "signoff-audit-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "signoff-audit-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "signoff-audit-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "signoff-audit-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "signoff-audit-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-signoff-audit-preview.v1",
    version: "1.9.38",
    mode: "readonly-phase30-entry-signoff-audit-preview-no-audit-persist",
    sourceModels: {
      phase30EntryReconciliationSignoffPreview: phase30EntryReconciliationSignoffPreview.schemaVersion,
      phase30EntryFinalExportReconciliationPreview: phase30EntryFinalExportReconciliationPreview.schemaVersion,
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      exitReadinessAttestationPreview: exitReadinessAttestationPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    auditItems,
    auditChecks,
    blockedMutations: [
      "persist-entry-signoff-audit-as-release-approval",
      "persist-entry-signoff-audit-as-phase29-exit",
      "persist-entry-signoff-audit-as-phase30-entry",
      "persist-entry-signoff-audit-artifact",
      "persist-entry-signoff-audit-item",
      "persist-reconciliation-signoff-from-audit",
      "record-signoff-audit",
      "record-reconciliation-signoff-from-audit",
      "approve-release-from-entry-signoff-audit",
      "exit-phase29-from-entry-signoff-audit",
      "open-phase30-entry-from-entry-signoff-audit",
      "clear-blocker-from-entry-signoff-audit",
      "resolve-redline-from-entry-signoff-audit",
      "mark-entry-signoff-audit-complete",
      "override-entry-reconciliation-signoff-from-audit",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-signoff-audit-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-signoff-audit-source-role-persistence-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntrySignoffAuditPreviewReady: true,
      auditItems: auditItems.length,
      blockedAuditItems: auditItems.filter((item) => item.auditState.includes("blocked")).length,
      auditChecks: auditChecks.length,
      passedAuditChecks: auditChecks.filter((item) => item.result === "pass").length,
      auditedSignoffItems: 0,
      persistedAuditItems: auditItems.filter((item) => item.persisted !== false).length,
      persistedSignoffs: signoffItems.filter((item) => item.persisted !== false).length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29Phase30EntryAuditClosurePreview(phase30EntrySignoffAuditPreview = {}, phase30EntryReconciliationSignoffPreview = {}, phase30EntryFinalExportReconciliationPreview = {}, releaseFinalReviewEnvelopePreview = {}, exitReadinessAttestationPreview = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const auditItems = phase30EntrySignoffAuditPreview.auditItems || [];
  const closureItems = auditItems.map((item, index) => ({
    id: `closure-${item.id.replace(/^audit-/, "")}`,
    label: item.label.replace("signoff audit", "audit closure"),
    closureOrder: index + 1,
    sourceAuditItemId: item.id,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    closureState: "blocked-readonly-audit-closure-preview",
    auditState: item.auditState,
    signoffState: item.signoffState,
    evidenceCoverageState: item.evidenceCoverageState,
    auditPersistenceState: item.auditPersistenceState,
    signoffPersistenceState: item.signoffPersistenceState,
    closurePersistenceState: "not-persisted",
    releaseBlockerState: item.releaseBlockerState,
    mayPersistClosure: false,
    mayPersistAudit: false,
    mayRecordSignoff: false,
    mayRecordClosure: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const closureChecks = [
    { id: "audit-closure-linked-to-signoff-audit-preview", result: phase30EntrySignoffAuditPreview.schemaVersion === "phase29.phase30-entry-signoff-audit-preview.v1" ? "pass" : "block", evidence: phase30EntrySignoffAuditPreview.schemaVersion },
    { id: "audit-closure-linked-to-reconciliation-signoff-preview", result: phase30EntryReconciliationSignoffPreview.schemaVersion === "phase29.phase30-entry-reconciliation-signoff-preview.v1" ? "pass" : "block", evidence: phase30EntryReconciliationSignoffPreview.schemaVersion },
    { id: "audit-closure-linked-to-final-export-reconciliation-preview", result: phase30EntryFinalExportReconciliationPreview.schemaVersion === "phase29.phase30-entry-final-export-reconciliation-preview.v1" ? "pass" : "block", evidence: phase30EntryFinalExportReconciliationPreview.schemaVersion },
    { id: "audit-closure-linked-to-final-review-envelope", result: releaseFinalReviewEnvelopePreview.schemaVersion === "phase29.release-final-review-envelope-preview.v1" ? "pass" : "block", evidence: releaseFinalReviewEnvelopePreview.schemaVersion },
    { id: "audit-closure-linked-to-exit-attestation", result: exitReadinessAttestationPreview.schemaVersion === "phase29.exit-readiness-attestation-preview.v1" ? "pass" : "block", evidence: exitReadinessAttestationPreview.schemaVersion },
    { id: "audit-closure-covers-all-audit-items", result: closureItems.length === auditItems.length ? "pass" : "block", evidence: `closureItems=${closureItems.length}; auditItems=${auditItems.length}` },
    { id: "audit-closure-keeps-audit-unpersisted", result: auditItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedAuditItems=${auditItems.filter((item) => item.persisted !== false).length}` },
    { id: "audit-closure-keeps-closures-unpersisted", result: closureItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedClosureItems=${closureItems.filter((item) => item.persisted !== false).length}` },
    { id: "audit-closure-keeps-signoffs-unpersisted", result: (phase30EntryReconciliationSignoffPreview.signoffItems || []).every((item) => item.persisted === false) ? "pass" : "block", evidence: `persistedSignoffs=${(phase30EntryReconciliationSignoffPreview.signoffItems || []).filter((item) => item.persisted !== false).length}` },
    { id: "audit-closure-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: `unresolvedBlockers=${finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0}` },
    { id: "audit-closure-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "audit-closure-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "audit-closure-keeps-release-blocked", result: "pass", evidence: `releaseReady=${releaseExitDecisionPreview.summary?.releaseReady === true}` },
    { id: "audit-closure-keeps-runtime-disabled", result: "pass", evidence: `runtimeExecution=${runtimeEnableGovernance.summary?.runtimeExecution === true}` },
    { id: "audit-closure-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.phase30-entry-audit-closure-preview.v1",
    version: "1.9.39",
    mode: "readonly-phase30-entry-audit-closure-preview-no-closure-persist",
    sourceModels: {
      phase30EntrySignoffAuditPreview: phase30EntrySignoffAuditPreview.schemaVersion,
      phase30EntryReconciliationSignoffPreview: phase30EntryReconciliationSignoffPreview.schemaVersion,
      phase30EntryFinalExportReconciliationPreview: phase30EntryFinalExportReconciliationPreview.schemaVersion,
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      exitReadinessAttestationPreview: exitReadinessAttestationPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    closureItems,
    closureChecks,
    blockedMutations: [
      "persist-entry-audit-closure-as-release-approval",
      "persist-entry-audit-closure-as-phase29-exit",
      "persist-entry-audit-closure-as-phase30-entry",
      "persist-entry-audit-closure-artifact",
      "persist-entry-audit-closure-item",
      "persist-signoff-audit-from-closure",
      "record-audit-closure",
      "record-signoff-audit-from-closure",
      "record-reconciliation-signoff-from-closure",
      "approve-release-from-entry-audit-closure",
      "exit-phase29-from-entry-audit-closure",
      "open-phase30-entry-from-entry-audit-closure",
      "clear-blocker-from-entry-audit-closure",
      "resolve-redline-from-entry-audit-closure",
      "mark-entry-audit-closure-complete",
      "override-entry-signoff-audit-from-closure",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-phase30-entry-audit-closure-preview.json",
      includeInOperationsExport: true,
      redaction: "phase30-entry-audit-closure-source-audit-signoff-persistence-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase30EntryAuditClosurePreviewReady: true,
      closureItems: closureItems.length,
      blockedClosureItems: closureItems.filter((item) => item.closureState.includes("blocked")).length,
      closureChecks: closureChecks.length,
      passedClosureChecks: closureChecks.filter((item) => item.result === "pass").length,
      closedAuditItems: 0,
      persistedClosureItems: closureItems.filter((item) => item.persisted !== false).length,
      persistedAuditItems: auditItems.filter((item) => item.persisted !== false).length,
      persistedSignoffs: (phase30EntryReconciliationSignoffPreview.signoffItems || []).filter((item) => item.persisted !== false).length,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29AuditClosureReadinessHardening(phase30EntryAuditClosurePreview = {}, phase30EntrySignoffAuditPreview = {}, phase30EntryReconciliationSignoffPreview = {}, phase30EntryFinalExportReconciliationPreview = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, finalReleaseBlockerDossier = {}, releaseExitDecisionPreview = {}, runtimeEnableGovernance = {}) {
  const closureItems = phase30EntryAuditClosurePreview.closureItems || [];
  const auditItems = phase30EntrySignoffAuditPreview.auditItems || [];
  const signoffItems = phase30EntryReconciliationSignoffPreview.signoffItems || [];
  const hardeningItems = closureItems.map((item, index) => ({
    id: "hardening-" + item.id.replace(/^closure-/, ""),
    label: item.label.replace("audit closure", "audit closure hardening"),
    hardeningOrder: index + 1,
    sourceClosureItemId: item.id,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    hardeningState: "blocked-readonly-audit-closure-readiness-hardening",
    closureState: item.closureState,
    auditState: item.auditState,
    signoffState: item.signoffState,
    evidenceCoverageState: item.evidenceCoverageState,
    closurePersistenceState: item.closurePersistenceState,
    auditPersistenceState: item.auditPersistenceState,
    signoffPersistenceState: item.signoffPersistenceState,
    hardeningPersistenceState: "not-persisted",
    releaseBlockerState: item.releaseBlockerState,
    mayPersistHardening: false,
    mayPersistClosure: false,
    mayPersistAudit: false,
    mayRecordClosure: false,
    mayRecordSignoff: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const hardeningChecks = [
    { id: "hardening-linked-to-audit-closure-preview", result: phase30EntryAuditClosurePreview.schemaVersion === "phase29.phase30-entry-audit-closure-preview.v1" ? "pass" : "block", evidence: phase30EntryAuditClosurePreview.schemaVersion },
    { id: "hardening-linked-to-signoff-audit-preview", result: phase30EntrySignoffAuditPreview.schemaVersion === "phase29.phase30-entry-signoff-audit-preview.v1" ? "pass" : "block", evidence: phase30EntrySignoffAuditPreview.schemaVersion },
    { id: "hardening-linked-to-reconciliation-signoff-preview", result: phase30EntryReconciliationSignoffPreview.schemaVersion === "phase29.phase30-entry-reconciliation-signoff-preview.v1" ? "pass" : "block", evidence: phase30EntryReconciliationSignoffPreview.schemaVersion },
    { id: "hardening-linked-to-final-export-reconciliation-preview", result: phase30EntryFinalExportReconciliationPreview.schemaVersion === "phase29.phase30-entry-final-export-reconciliation-preview.v1" ? "pass" : "block", evidence: phase30EntryFinalExportReconciliationPreview.schemaVersion },
    { id: "hardening-covers-all-closure-items", result: hardeningItems.length === closureItems.length ? "pass" : "block", evidence: "hardeningItems=" + hardeningItems.length + "; closureItems=" + closureItems.length },
    { id: "hardening-confirms-closure-checks-pass", result: (phase30EntryAuditClosurePreview.closureChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "closureChecks=" + (phase30EntryAuditClosurePreview.summary?.closureChecks ?? 0) + "; passed=" + (phase30EntryAuditClosurePreview.summary?.passedClosureChecks ?? 0) },
    { id: "hardening-keeps-closure-unpersisted", result: closureItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedClosureItems=" + closureItems.filter((item) => item.persisted !== false).length },
    { id: "hardening-keeps-audit-unpersisted", result: auditItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedAuditItems=" + auditItems.filter((item) => item.persisted !== false).length },
    { id: "hardening-keeps-signoffs-unpersisted", result: signoffItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedSignoffs=" + signoffItems.filter((item) => item.persisted !== false).length },
    { id: "hardening-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "hardening-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "hardening-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "hardening-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "hardening-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "hardening-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "hardening-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "hardening-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.audit-closure-readiness-hardening.v1",
    version: "1.9.40",
    mode: "readonly-audit-closure-readiness-hardening-no-hardening-persist",
    sourceModels: {
      phase30EntryAuditClosurePreview: phase30EntryAuditClosurePreview.schemaVersion,
      phase30EntrySignoffAuditPreview: phase30EntrySignoffAuditPreview.schemaVersion,
      phase30EntryReconciliationSignoffPreview: phase30EntryReconciliationSignoffPreview.schemaVersion,
      phase30EntryFinalExportReconciliationPreview: phase30EntryFinalExportReconciliationPreview.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    hardeningItems,
    hardeningChecks,
    blockedMutations: [
      "persist-audit-closure-hardening-as-release-approval",
      "persist-audit-closure-hardening-as-phase29-exit",
      "persist-audit-closure-hardening-as-phase30-entry",
      "persist-audit-closure-hardening-artifact",
      "persist-audit-closure-hardening-item",
      "persist-entry-audit-closure-from-hardening",
      "record-audit-closure-hardening",
      "record-audit-closure-from-hardening",
      "record-signoff-audit-from-hardening",
      "approve-release-from-audit-closure-hardening",
      "exit-phase29-from-audit-closure-hardening",
      "open-phase30-entry-from-audit-closure-hardening",
      "clear-blocker-from-audit-closure-hardening",
      "resolve-redline-from-audit-closure-hardening",
      "mark-audit-closure-hardened",
      "override-entry-audit-closure-from-hardening",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-audit-closure-readiness-hardening.json",
      includeInOperationsExport: true,
      redaction: "phase29-audit-closure-hardening-source-coverage-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29AuditClosureReadinessHardeningReady: true,
      hardeningItems: hardeningItems.length,
      blockedHardeningItems: hardeningItems.filter((item) => item.hardeningState.includes("blocked")).length,
      hardeningChecks: hardeningChecks.length,
      passedHardeningChecks: hardeningChecks.filter((item) => item.result === "pass").length,
      hardenedClosureItems: 0,
      persistedHardeningItems: hardeningItems.filter((item) => item.persisted !== false).length,
      persistedClosureItems: closureItems.filter((item) => item.persisted !== false).length,
      persistedAuditItems: auditItems.filter((item) => item.persisted !== false).length,
      persistedSignoffs: signoffItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}
function buildLocalPhase29FinalReadonlyExitPackagePreview(phase29AuditClosureReadinessHardening = {}, releaseExitDecisionPreview = {}, exitReadinessAttestationPreview = {}, releaseFinalReviewEnvelopePreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const hardeningItems = phase29AuditClosureReadinessHardening.hardeningItems || [];
  const exitPackageItems = hardeningItems.map((item, index) => ({
    id: "exit-package-" + item.id.replace(/^hardening-/, ""),
    label: item.label.replace("audit closure hardening", "final readonly exit package"),
    packageOrder: index + 1,
    sourceHardeningItemId: item.id,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    packageState: "blocked-readonly-final-exit-package-preview",
    hardeningState: item.hardeningState,
    closureState: item.closureState,
    auditState: item.auditState,
    signoffState: item.signoffState,
    releaseBlockerState: item.releaseBlockerState,
    hardeningPersistenceState: item.hardeningPersistenceState,
    packagePersistenceState: "not-persisted",
    mayPersistPackage: false,
    mayPersistHardening: false,
    mayPersistClosure: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const exitPackageChecks = [
    { id: "exit-package-linked-to-audit-closure-hardening", result: phase29AuditClosureReadinessHardening.schemaVersion === "phase29.audit-closure-readiness-hardening.v1" ? "pass" : "block", evidence: phase29AuditClosureReadinessHardening.schemaVersion },
    { id: "exit-package-linked-to-release-exit-decision", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "exit-package-linked-to-exit-attestation", result: exitReadinessAttestationPreview.schemaVersion === "phase29.exit-readiness-attestation-preview.v1" ? "pass" : "block", evidence: exitReadinessAttestationPreview.schemaVersion },
    { id: "exit-package-linked-to-final-review-envelope", result: releaseFinalReviewEnvelopePreview.schemaVersion === "phase29.release-final-review-envelope-preview.v1" ? "pass" : "block", evidence: releaseFinalReviewEnvelopePreview.schemaVersion },
    { id: "exit-package-covers-all-hardening-items", result: exitPackageItems.length === hardeningItems.length ? "pass" : "block", evidence: "exitPackageItems=" + exitPackageItems.length + "; hardeningItems=" + hardeningItems.length },
    { id: "exit-package-confirms-hardening-checks-pass", result: (phase29AuditClosureReadinessHardening.hardeningChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "hardeningChecks=" + (phase29AuditClosureReadinessHardening.summary?.hardeningChecks ?? 0) + "; passed=" + (phase29AuditClosureReadinessHardening.summary?.passedHardeningChecks ?? 0) },
    { id: "exit-package-keeps-hardening-unpersisted", result: hardeningItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedHardeningItems=" + hardeningItems.filter((item) => item.persisted !== false).length },
    { id: "exit-package-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "exit-package-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "exit-package-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "exit-package-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "exit-package-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "exit-package-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "exit-package-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "exit-package-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.final-readonly-exit-package-preview.v1",
    version: "1.9.41",
    mode: "readonly-final-exit-package-preview-no-exit-persist",
    sourceModels: {
      phase29AuditClosureReadinessHardening: phase29AuditClosureReadinessHardening.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      exitReadinessAttestationPreview: exitReadinessAttestationPreview.schemaVersion,
      releaseFinalReviewEnvelopePreview: releaseFinalReviewEnvelopePreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    exitPackageItems,
    exitPackageChecks,
    blockedMutations: [
      "persist-final-readonly-exit-package-as-release-approval",
      "persist-final-readonly-exit-package-as-phase29-exit",
      "persist-final-readonly-exit-package-as-phase30-entry",
      "persist-final-readonly-exit-package-artifact",
      "persist-final-readonly-exit-package-item",
      "record-final-readonly-exit-package",
      "record-exit-package-from-hardening",
      "approve-release-from-final-readonly-exit-package",
      "exit-phase29-from-final-readonly-exit-package",
      "open-phase30-entry-from-final-readonly-exit-package",
      "clear-blocker-from-final-readonly-exit-package",
      "resolve-redline-from-final-readonly-exit-package",
      "mark-final-exit-package-approved",
      "override-exit-decision-from-final-readonly-package",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-final-readonly-exit-package-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-final-readonly-exit-package-source-coverage-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29FinalReadonlyExitPackagePreviewReady: true,
      exitPackageItems: exitPackageItems.length,
      blockedExitPackageItems: exitPackageItems.filter((item) => item.packageState.includes("blocked")).length,
      exitPackageChecks: exitPackageChecks.length,
      passedExitPackageChecks: exitPackageChecks.filter((item) => item.result === "pass").length,
      packagedExitItems: 0,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      persistedHardeningItems: hardeningItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}
function buildLocalPhase29ReleaseExitReadinessDossierPreview(phase29FinalReadonlyExitPackagePreview = {}, phase29AuditClosureReadinessHardening = {}, releaseExitDecisionPreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const dossierItems = exitPackageItems.map((item, index) => ({
    id: "dossier-" + item.id.replace(/^exit-package-/, ""),
    label: item.label.replace("final readonly exit package", "release exit readiness dossier"),
    dossierOrder: index + 1,
    sourceExitPackageItemId: item.id,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    dossierState: "blocked-readonly-release-exit-readiness-dossier-preview",
    packageState: item.packageState,
    hardeningState: item.hardeningState,
    closureState: item.closureState,
    auditState: item.auditState,
    signoffState: item.signoffState,
    releaseBlockerState: item.releaseBlockerState,
    packagePersistenceState: item.packagePersistenceState,
    dossierPersistenceState: "not-persisted",
    mayPersistDossier: false,
    mayPersistPackage: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const dossierChecks = [
    { id: "dossier-linked-to-final-exit-package-preview", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "dossier-linked-to-audit-closure-hardening", result: phase29AuditClosureReadinessHardening.schemaVersion === "phase29.audit-closure-readiness-hardening.v1" ? "pass" : "block", evidence: phase29AuditClosureReadinessHardening.schemaVersion },
    { id: "dossier-linked-to-release-exit-decision", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "dossier-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "dossier-covers-all-exit-package-items", result: dossierItems.length === exitPackageItems.length ? "pass" : "block", evidence: "dossierItems=" + dossierItems.length + "; exitPackageItems=" + exitPackageItems.length },
    { id: "dossier-confirms-exit-package-checks-pass", result: (phase29FinalReadonlyExitPackagePreview.exitPackageChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "exitPackageChecks=" + (phase29FinalReadonlyExitPackagePreview.summary?.exitPackageChecks ?? 0) + "; passed=" + (phase29FinalReadonlyExitPackagePreview.summary?.passedExitPackageChecks ?? 0) },
    { id: "dossier-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "dossier-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "dossier-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "dossier-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "dossier-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "dossier-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "dossier-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "dossier-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "dossier-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-readiness-dossier-preview.v1",
    version: "1.9.42",
    mode: "readonly-release-exit-readiness-dossier-preview-no-dossier-persist",
    sourceModels: {
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      phase29AuditClosureReadinessHardening: phase29AuditClosureReadinessHardening.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    dossierItems,
    dossierChecks,
    blockedMutations: [
      "persist-release-exit-readiness-dossier-as-release-approval",
      "persist-release-exit-readiness-dossier-as-phase29-exit",
      "persist-release-exit-readiness-dossier-as-phase30-entry",
      "persist-release-exit-readiness-dossier-artifact",
      "persist-release-exit-readiness-dossier-item",
      "record-release-exit-readiness-dossier",
      "record-final-exit-package-from-dossier",
      "approve-release-from-release-exit-readiness-dossier",
      "exit-phase29-from-release-exit-readiness-dossier",
      "open-phase30-entry-from-release-exit-readiness-dossier",
      "clear-blocker-from-release-exit-readiness-dossier",
      "resolve-redline-from-release-exit-readiness-dossier",
      "mark-release-exit-dossier-approved",
      "override-final-exit-package-from-dossier",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-readiness-dossier-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-readiness-dossier-source-coverage-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitReadinessDossierPreviewReady: true,
      dossierItems: dossierItems.length,
      blockedDossierItems: dossierItems.filter((item) => item.dossierState.includes("blocked")).length,
      dossierChecks: dossierChecks.length,
      passedDossierChecks: dossierChecks.filter((item) => item.result === "pass").length,
      acceptedDossierItems: 0,
      persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseExitGovernanceFreezePreview(phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, governanceArchiveFreeze = {}, releaseExitDecisionPreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const freezeItems = dossierItems.map((item, index) => ({
    id: "freeze-" + item.id.replace(/^dossier-/, ""),
    label: item.label.replace("release exit readiness dossier", "release exit governance freeze"),
    freezeOrder: index + 1,
    sourceDossierItemId: item.id,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    freezeState: "blocked-readonly-release-exit-governance-freeze-preview",
    dossierState: item.dossierState,
    packageState: item.packageState,
    hardeningState: item.hardeningState,
    closureState: item.closureState,
    auditState: item.auditState,
    signoffState: item.signoffState,
    releaseBlockerState: item.releaseBlockerState,
    dossierPersistenceState: item.dossierPersistenceState,
    packagePersistenceState: item.packagePersistenceState,
    freezePersistenceState: "not-persisted",
    mayPersistFreeze: false,
    mayPersistDossier: false,
    mayPersistPackage: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const freezeChecks = [
    { id: "freeze-linked-to-release-exit-dossier-preview", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "freeze-linked-to-final-exit-package-preview", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "freeze-linked-to-governance-archive-freeze", result: governanceArchiveFreeze.schemaVersion === "phase29.governance-archive-freeze.v1" ? "pass" : "block", evidence: governanceArchiveFreeze.schemaVersion },
    { id: "freeze-linked-to-release-exit-decision", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "freeze-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "freeze-covers-all-dossier-items", result: freezeItems.length === dossierItems.length ? "pass" : "block", evidence: "freezeItems=" + freezeItems.length + "; dossierItems=" + dossierItems.length },
    { id: "freeze-confirms-dossier-checks-pass", result: (phase29ReleaseExitReadinessDossierPreview.dossierChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "dossierChecks=" + (phase29ReleaseExitReadinessDossierPreview.summary?.dossierChecks ?? 0) + "; passed=" + (phase29ReleaseExitReadinessDossierPreview.summary?.passedDossierChecks ?? 0) },
    { id: "freeze-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "freeze-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "freeze-keeps-governance-archive-readonly", result: governanceArchiveFreeze.summary?.persistedMutations === 0 ? "pass" : "block", evidence: "archivePersistedMutations=" + (governanceArchiveFreeze.summary?.persistedMutations ?? 0) },
    { id: "freeze-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "freeze-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "freeze-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "freeze-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "freeze-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "freeze-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "freeze-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "freeze-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-governance-freeze-preview.v1",
    version: "1.9.43",
    mode: "readonly-release-exit-governance-freeze-preview-no-freeze-persist",
    sourceModels: {
      phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion,
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      governanceArchiveFreeze: governanceArchiveFreeze.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    freezeItems,
    freezeChecks,
    blockedMutations: [
      "persist-release-exit-governance-freeze-as-release-approval",
      "persist-release-exit-governance-freeze-as-phase29-exit",
      "persist-release-exit-governance-freeze-as-phase30-entry",
      "persist-release-exit-governance-freeze-artifact",
      "persist-release-exit-governance-freeze-item",
      "record-release-exit-governance-freeze",
      "record-release-exit-dossier-from-freeze",
      "approve-release-from-release-exit-governance-freeze",
      "exit-phase29-from-release-exit-governance-freeze",
      "open-phase30-entry-from-release-exit-governance-freeze",
      "clear-blocker-from-release-exit-governance-freeze",
      "resolve-redline-from-release-exit-governance-freeze",
      "unfreeze-release-exit-governance-without-review",
      "mark-release-exit-governance-frozen-approved",
      "override-release-exit-dossier-from-freeze",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-governance-freeze-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-governance-freeze-source-coverage-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitGovernanceFreezePreviewReady: true,
      freezeItems: freezeItems.length,
      blockedFreezeItems: freezeItems.filter((item) => item.freezeState.includes("blocked")).length,
      freezeChecks: freezeChecks.length,
      passedFreezeChecks: freezeChecks.filter((item) => item.result === "pass").length,
      frozenGovernanceItems: 0,
      persistedFreezeItems: freezeItems.filter((item) => item.persisted !== false).length,
      persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseExitFinalSignoffReviewPreview(phase29ReleaseExitGovernanceFreezePreview = {}, phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, releaseExitDecisionPreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const freezeItems = phase29ReleaseExitGovernanceFreezePreview.freezeItems || [];
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const signoffReviewItems = freezeItems.map((item, index) => ({
    id: "signoff-review-" + item.id.replace(/^freeze-/, ""),
    label: item.label.replace("release exit governance freeze", "release exit final signoff review"),
    reviewOrder: index + 1,
    sourceFreezeItemId: item.id,
    sourceDossierItemId: item.sourceDossierItemId,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    signoffReviewState: "blocked-readonly-release-exit-final-signoff-review-preview",
    freezeState: item.freezeState,
    dossierState: item.dossierState,
    packageState: item.packageState,
    releaseBlockerState: item.releaseBlockerState,
    freezePersistenceState: item.freezePersistenceState,
    dossierPersistenceState: item.dossierPersistenceState,
    packagePersistenceState: item.packagePersistenceState,
    signoffReviewPersistenceState: "not-persisted",
    mayPersistSignoffReview: false,
    mayPersistFreeze: false,
    mayPersistDossier: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const signoffReviewChecks = [
    { id: "signoff-review-linked-to-governance-freeze-preview", result: phase29ReleaseExitGovernanceFreezePreview.schemaVersion === "phase29.release-exit-governance-freeze-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitGovernanceFreezePreview.schemaVersion },
    { id: "signoff-review-linked-to-release-exit-dossier-preview", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "signoff-review-linked-to-final-exit-package-preview", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "signoff-review-linked-to-release-exit-decision", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "signoff-review-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "signoff-review-covers-all-freeze-items", result: signoffReviewItems.length === freezeItems.length ? "pass" : "block", evidence: "signoffReviewItems=" + signoffReviewItems.length + "; freezeItems=" + freezeItems.length },
    { id: "signoff-review-confirms-freeze-checks-pass", result: (phase29ReleaseExitGovernanceFreezePreview.freezeChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "freezeChecks=" + (phase29ReleaseExitGovernanceFreezePreview.summary?.freezeChecks ?? 0) + "; passed=" + (phase29ReleaseExitGovernanceFreezePreview.summary?.passedFreezeChecks ?? 0) },
    { id: "signoff-review-keeps-freeze-unpersisted", result: freezeItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedFreezeItems=" + freezeItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-review-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-review-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-review-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "signoff-review-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "signoff-review-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "signoff-review-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "signoff-review-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "signoff-review-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "signoff-review-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "signoff-review-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-final-signoff-review-preview.v1",
    version: "1.9.44",
    mode: "readonly-release-exit-final-signoff-review-preview-no-signoff-persist",
    sourceModels: { phase29ReleaseExitGovernanceFreezePreview: phase29ReleaseExitGovernanceFreezePreview.schemaVersion, phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion, phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion, releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion, finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion, transitionReadinessRedline: transitionReadinessRedline.schemaVersion, phase30EntryPreflight: phase30EntryPreflight.schemaVersion, runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion },
    signoffReviewItems,
    signoffReviewChecks,
    blockedMutations: ["persist-release-exit-final-signoff-review-as-release-approval", "persist-release-exit-final-signoff-review-as-phase29-exit", "persist-release-exit-final-signoff-review-as-phase30-entry", "persist-release-exit-final-signoff-review-artifact", "persist-release-exit-final-signoff-review-item", "record-release-exit-final-signoff-review", "record-release-exit-governance-freeze-from-signoff-review", "approve-release-from-release-exit-final-signoff-review", "exit-phase29-from-release-exit-final-signoff-review", "open-phase30-entry-from-release-exit-final-signoff-review", "clear-blocker-from-release-exit-final-signoff-review", "resolve-redline-from-release-exit-final-signoff-review", "mark-release-exit-final-signoff-approved", "override-release-exit-governance-freeze-from-signoff-review", "override-release-exit-dossier-from-signoff-review", "mark-releaseReady-true", "enable-runtime-execution", "enable-third-party-execution"],
    exportPolicy: { exportName: "phase29-release-exit-final-signoff-review-preview.json", includeInOperationsExport: true, redaction: "phase29-release-exit-final-signoff-review-source-freeze-dossier-redline-blocker-and-readonly-guardrail-summary-only" },
    summary: { phase29ReleaseExitFinalSignoffReviewPreviewReady: true, signoffReviewItems: signoffReviewItems.length, blockedSignoffReviewItems: signoffReviewItems.filter((item) => item.signoffReviewState.includes("blocked")).length, signoffReviewChecks: signoffReviewChecks.length, passedSignoffReviewChecks: signoffReviewChecks.filter((item) => item.result === "pass").length, approvedSignoffItems: 0, persistedSignoffReviewItems: signoffReviewItems.filter((item) => item.persisted !== false).length, persistedFreezeItems: freezeItems.filter((item) => item.persisted !== false).length, persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length, persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length, activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0, unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0, phase29ExitReady: false, phase30Blocked: true, phase30EntryReady: false, releaseReady: false, runtimeExecution: false, thirdPartyExecution: false, clearedBlockers: 0, persistedMutations: 0 }
  };
}

function buildLocalPhase29ReleaseExitSignoffEvidenceReconciliationPreview(phase29ReleaseExitFinalSignoffReviewPreview = {}, phase29ReleaseExitGovernanceFreezePreview = {}, phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, releaseExitDecisionPreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const signoffReviewItems = phase29ReleaseExitFinalSignoffReviewPreview.signoffReviewItems || [];
  const freezeItems = phase29ReleaseExitGovernanceFreezePreview.freezeItems || [];
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const evidenceReconciliationItems = signoffReviewItems.map((item, index) => ({
    id: "signoff-evidence-reconciliation-" + item.id.replace(/^signoff-review-/, ""),
    label: item.label.replace("release exit final signoff review", "release exit signoff evidence reconciliation"),
    reconciliationOrder: index + 1,
    sourceSignoffReviewItemId: item.id,
    sourceFreezeItemId: item.sourceFreezeItemId,
    sourceDossierItemId: item.sourceDossierItemId,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    reconciliationState: "blocked-readonly-release-exit-signoff-evidence-reconciliation-preview",
    evidenceMatchState: "readonly-match-preview",
    signoffReviewState: item.signoffReviewState,
    freezeState: item.freezeState,
    dossierState: item.dossierState,
    packageState: item.packageState,
    releaseBlockerState: item.releaseBlockerState,
    signoffReviewPersistenceState: item.signoffReviewPersistenceState,
    reconciliationPersistenceState: "not-persisted",
    mayPersistReconciliation: false,
    mayPersistSignoffReview: false,
    mayPersistFreeze: false,
    mayPersistDossier: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const evidenceReconciliationChecks = [
    { id: "signoff-evidence-reconciliation-linked-to-final-signoff-review", result: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion === "phase29.release-exit-final-signoff-review-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion },
    { id: "signoff-evidence-reconciliation-linked-to-governance-freeze", result: phase29ReleaseExitGovernanceFreezePreview.schemaVersion === "phase29.release-exit-governance-freeze-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitGovernanceFreezePreview.schemaVersion },
    { id: "signoff-evidence-reconciliation-linked-to-release-exit-dossier", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "signoff-evidence-reconciliation-linked-to-final-exit-package", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "signoff-evidence-reconciliation-linked-to-release-exit-decision", result: releaseExitDecisionPreview.schemaVersion === "phase29.release-exit-decision-preview.v1" ? "pass" : "block", evidence: releaseExitDecisionPreview.schemaVersion },
    { id: "signoff-evidence-reconciliation-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "signoff-evidence-reconciliation-covers-all-signoff-review-items", result: evidenceReconciliationItems.length === signoffReviewItems.length ? "pass" : "block", evidence: "evidenceReconciliationItems=" + evidenceReconciliationItems.length + "; signoffReviewItems=" + signoffReviewItems.length },
    { id: "signoff-evidence-reconciliation-confirms-signoff-review-checks-pass", result: (phase29ReleaseExitFinalSignoffReviewPreview.signoffReviewChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "signoffReviewChecks=" + (phase29ReleaseExitFinalSignoffReviewPreview.summary?.signoffReviewChecks ?? 0) + "; passed=" + (phase29ReleaseExitFinalSignoffReviewPreview.summary?.passedSignoffReviewChecks ?? 0) },
    { id: "signoff-evidence-reconciliation-matches-freeze-dossier-and-package-counts", result: signoffReviewItems.length === freezeItems.length && freezeItems.length === dossierItems.length && dossierItems.length === exitPackageItems.length ? "pass" : "block", evidence: "signoffReviewItems=" + signoffReviewItems.length + "; freezeItems=" + freezeItems.length + "; dossierItems=" + dossierItems.length + "; exitPackageItems=" + exitPackageItems.length },
    { id: "signoff-evidence-reconciliation-keeps-signoff-review-unpersisted", result: signoffReviewItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedSignoffReviewItems=" + signoffReviewItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-evidence-reconciliation-keeps-freeze-unpersisted", result: freezeItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedFreezeItems=" + freezeItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-evidence-reconciliation-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-evidence-reconciliation-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "signoff-evidence-reconciliation-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "signoff-evidence-reconciliation-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "signoff-evidence-reconciliation-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "signoff-evidence-reconciliation-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "signoff-evidence-reconciliation-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "signoff-evidence-reconciliation-keeps-release-blocked", result: "pass", evidence: "releaseReady=" + (releaseExitDecisionPreview.summary?.releaseReady === true) },
    { id: "signoff-evidence-reconciliation-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=" + (runtimeEnableGovernance.summary?.runtimeExecution === true) },
    { id: "signoff-evidence-reconciliation-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-signoff-evidence-reconciliation-preview.v1",
    version: "1.9.45",
    mode: "readonly-release-exit-signoff-evidence-reconciliation-preview-no-reconciliation-persist",
    sourceModels: {
      phase29ReleaseExitFinalSignoffReviewPreview: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion,
      phase29ReleaseExitGovernanceFreezePreview: phase29ReleaseExitGovernanceFreezePreview.schemaVersion,
      phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion,
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      releaseExitDecisionPreview: releaseExitDecisionPreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    evidenceReconciliationItems,
    evidenceReconciliationChecks,
    blockedMutations: [
      "persist-release-exit-signoff-evidence-reconciliation-as-release-approval",
      "persist-release-exit-signoff-evidence-reconciliation-as-phase29-exit",
      "persist-release-exit-signoff-evidence-reconciliation-as-phase30-entry",
      "persist-release-exit-signoff-evidence-reconciliation-artifact",
      "persist-release-exit-signoff-evidence-reconciliation-item",
      "record-release-exit-signoff-evidence-reconciliation",
      "record-release-exit-final-signoff-review-from-evidence-reconciliation",
      "record-release-exit-governance-freeze-from-evidence-reconciliation",
      "approve-release-from-release-exit-signoff-evidence-reconciliation",
      "exit-phase29-from-release-exit-signoff-evidence-reconciliation",
      "open-phase30-entry-from-release-exit-signoff-evidence-reconciliation",
      "clear-blocker-from-release-exit-signoff-evidence-reconciliation",
      "resolve-redline-from-release-exit-signoff-evidence-reconciliation",
      "mark-release-exit-signoff-evidence-reconciled",
      "override-release-exit-final-signoff-review-from-evidence-reconciliation",
      "override-release-exit-governance-freeze-from-evidence-reconciliation",
      "override-release-exit-dossier-from-evidence-reconciliation",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-signoff-evidence-reconciliation-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-signoff-evidence-reconciliation-source-signoff-freeze-dossier-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitSignoffEvidenceReconciliationPreviewReady: true,
      evidenceReconciliationItems: evidenceReconciliationItems.length,
      blockedEvidenceReconciliationItems: evidenceReconciliationItems.filter((item) => item.reconciliationState.includes("blocked")).length,
      evidenceReconciliationChecks: evidenceReconciliationChecks.length,
      passedEvidenceReconciliationChecks: evidenceReconciliationChecks.filter((item) => item.result === "pass").length,
      reconciledEvidenceItems: 0,
      persistedEvidenceReconciliationItems: evidenceReconciliationItems.filter((item) => item.persisted !== false).length,
      persistedSignoffReviewItems: signoffReviewItems.filter((item) => item.persisted !== false).length,
      persistedFreezeItems: freezeItems.filter((item) => item.persisted !== false).length,
      persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}


function buildLocalPhase29ReleaseExitFinalArchiveIndexPreview(phase29ReleaseExitSignoffEvidenceReconciliationPreview = {}, phase29ReleaseExitFinalSignoffReviewPreview = {}, phase29ReleaseExitGovernanceFreezePreview = {}, phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const evidenceReconciliationItems = phase29ReleaseExitSignoffEvidenceReconciliationPreview.evidenceReconciliationItems || [];
  const signoffReviewItems = phase29ReleaseExitFinalSignoffReviewPreview.signoffReviewItems || [];
  const freezeItems = phase29ReleaseExitGovernanceFreezePreview.freezeItems || [];
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const archiveIndexItems = evidenceReconciliationItems.map((item, index) => ({
    id: "final-archive-index-" + item.id.replace(/^signoff-evidence-reconciliation-/, ""),
    label: item.label.replace("release exit signoff evidence reconciliation", "release exit final archive index"),
    archiveOrder: index + 1,
    sourceEvidenceReconciliationItemId: item.id,
    sourceSignoffReviewItemId: item.sourceSignoffReviewItemId,
    sourceFreezeItemId: item.sourceFreezeItemId,
    sourceDossierItemId: item.sourceDossierItemId,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    archiveIndexState: "blocked-readonly-release-exit-final-archive-index-preview",
    archivePersistenceState: "not-persisted",
    evidenceMatchState: item.evidenceMatchState,
    reconciliationState: item.reconciliationState,
    signoffReviewState: item.signoffReviewState,
    freezeState: item.freezeState,
    dossierState: item.dossierState,
    packageState: item.packageState,
    releaseBlockerState: item.releaseBlockerState,
    signoffReviewPersistenceState: item.signoffReviewPersistenceState,
    reconciliationPersistenceState: item.reconciliationPersistenceState,
    mayPersistArchiveIndex: false,
    mayPersistReconciliation: false,
    mayPersistSignoffReview: false,
    mayPersistFreeze: false,
    mayPersistDossier: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const archiveIndexChecks = [
    { id: "archive-index-linked-to-signoff-evidence-reconciliation", result: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion === "phase29.release-exit-signoff-evidence-reconciliation-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion },
    { id: "archive-index-linked-to-final-signoff-review", result: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion === "phase29.release-exit-final-signoff-review-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion },
    { id: "archive-index-linked-to-governance-freeze", result: phase29ReleaseExitGovernanceFreezePreview.schemaVersion === "phase29.release-exit-governance-freeze-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitGovernanceFreezePreview.schemaVersion },
    { id: "archive-index-linked-to-release-exit-dossier", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "archive-index-linked-to-final-exit-package", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "archive-index-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "archive-index-covers-all-reconciliation-items", result: archiveIndexItems.length === evidenceReconciliationItems.length ? "pass" : "block", evidence: "archiveIndexItems=" + archiveIndexItems.length + "; evidenceReconciliationItems=" + evidenceReconciliationItems.length },
    { id: "archive-index-confirms-reconciliation-checks-pass", result: (phase29ReleaseExitSignoffEvidenceReconciliationPreview.evidenceReconciliationChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "evidenceReconciliationChecks=" + (phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary?.evidenceReconciliationChecks ?? 0) + "; passed=" + (phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary?.passedEvidenceReconciliationChecks ?? 0) },
    { id: "archive-index-keeps-reconciliation-unpersisted", result: evidenceReconciliationItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedEvidenceReconciliationItems=" + evidenceReconciliationItems.filter((item) => item.persisted !== false).length },
    { id: "archive-index-keeps-signoff-review-unpersisted", result: signoffReviewItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedSignoffReviewItems=" + signoffReviewItems.filter((item) => item.persisted !== false).length },
    { id: "archive-index-keeps-freeze-unpersisted", result: freezeItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedFreezeItems=" + freezeItems.filter((item) => item.persisted !== false).length },
    { id: "archive-index-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "archive-index-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "archive-index-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "archive-index-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "archive-index-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "archive-index-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "archive-index-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "archive-index-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "archive-index-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false" },
    { id: "archive-index-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-final-archive-index-preview.v1",
    version: "1.9.46",
    mode: "readonly-release-exit-final-archive-index-preview-no-archive-persist",
    sourceModels: {
      phase29ReleaseExitSignoffEvidenceReconciliationPreview: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion,
      phase29ReleaseExitFinalSignoffReviewPreview: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion,
      phase29ReleaseExitGovernanceFreezePreview: phase29ReleaseExitGovernanceFreezePreview.schemaVersion,
      phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion,
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    archiveIndexItems,
    archiveIndexChecks,
    blockedMutations: [
      "persist-release-exit-final-archive-index-as-release-approval",
      "persist-release-exit-final-archive-index-as-phase29-exit",
      "persist-release-exit-final-archive-index-as-phase30-entry",
      "persist-release-exit-final-archive-index-artifact",
      "persist-release-exit-final-archive-index-item",
      "record-release-exit-final-archive-index",
      "record-release-exit-signoff-evidence-reconciliation-from-archive-index",
      "approve-release-from-release-exit-final-archive-index",
      "exit-phase29-from-release-exit-final-archive-index",
      "open-phase30-entry-from-release-exit-final-archive-index",
      "clear-blocker-from-release-exit-final-archive-index",
      "resolve-redline-from-release-exit-final-archive-index",
      "mark-release-exit-final-archive-indexed",
      "override-release-exit-signoff-evidence-reconciliation-from-archive-index",
      "override-release-exit-final-signoff-review-from-archive-index",
      "override-release-exit-governance-freeze-from-archive-index",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-final-archive-index-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-final-archive-index-source-reconciliation-signoff-freeze-dossier-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitFinalArchiveIndexPreviewReady: true,
      archiveIndexItems: archiveIndexItems.length,
      blockedArchiveIndexItems: archiveIndexItems.filter((item) => item.archiveIndexState.includes("blocked")).length,
      archiveIndexChecks: archiveIndexChecks.length,
      passedArchiveIndexChecks: archiveIndexChecks.filter((item) => item.result === "pass").length,
      indexedArchiveItems: 0,
      persistedArchiveIndexItems: archiveIndexItems.filter((item) => item.persisted !== false).length,
      persistedEvidenceReconciliationItems: evidenceReconciliationItems.filter((item) => item.persisted !== false).length,
      persistedSignoffReviewItems: signoffReviewItems.filter((item) => item.persisted !== false).length,
      persistedFreezeItems: freezeItems.filter((item) => item.persisted !== false).length,
      persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseExitFinalArchiveIntegrityCheckPreview(phase29ReleaseExitFinalArchiveIndexPreview = {}, phase29ReleaseExitSignoffEvidenceReconciliationPreview = {}, phase29ReleaseExitFinalSignoffReviewPreview = {}, phase29ReleaseExitGovernanceFreezePreview = {}, phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const archiveIndexItems = phase29ReleaseExitFinalArchiveIndexPreview.archiveIndexItems || [];
  const evidenceReconciliationItems = phase29ReleaseExitSignoffEvidenceReconciliationPreview.evidenceReconciliationItems || [];
  const signoffReviewItems = phase29ReleaseExitFinalSignoffReviewPreview.signoffReviewItems || [];
  const freezeItems = phase29ReleaseExitGovernanceFreezePreview.freezeItems || [];
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const integrityCheckItems = archiveIndexItems.map((item, index) => ({
    id: "final-archive-integrity-check-" + item.id.replace(/^final-archive-index-/, ""),
    label: item.label.replace("release exit final archive index", "release exit final archive integrity check"),
    integrityCheckOrder: index + 1,
    sourceArchiveIndexItemId: item.id,
    sourceEvidenceReconciliationItemId: item.sourceEvidenceReconciliationItemId,
    sourceSignoffReviewItemId: item.sourceSignoffReviewItemId,
    sourceFreezeItemId: item.sourceFreezeItemId,
    sourceDossierItemId: item.sourceDossierItemId,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    integrityCheckState: "blocked-readonly-release-exit-final-archive-integrity-check-preview",
    integrityPersistenceState: "not-persisted",
    archiveIndexState: item.archiveIndexState,
    archivePersistenceState: item.archivePersistenceState,
    evidenceMatchState: item.evidenceMatchState,
    reconciliationState: item.reconciliationState,
    signoffReviewState: item.signoffReviewState,
    freezeState: item.freezeState,
    dossierState: item.dossierState,
    packageState: item.packageState,
    releaseBlockerState: item.releaseBlockerState,
    referenceCoverageState: item.sourceEvidenceReconciliationItemId && item.sourceSignoffReviewItemId && item.sourceFreezeItemId && item.sourceDossierItemId && item.sourceExitPackageItemId ? "complete-readonly-reference-coverage" : "missing-reference",
    mayPersistIntegrityCheck: false,
    mayPersistArchiveIndex: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const integrityChecks = [
    { id: "archive-integrity-linked-to-final-archive-index", result: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion === "phase29.release-exit-final-archive-index-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion },
    { id: "archive-integrity-linked-to-signoff-evidence-reconciliation", result: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion === "phase29.release-exit-signoff-evidence-reconciliation-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion },
    { id: "archive-integrity-linked-to-final-signoff-review", result: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion === "phase29.release-exit-final-signoff-review-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion },
    { id: "archive-integrity-linked-to-governance-freeze", result: phase29ReleaseExitGovernanceFreezePreview.schemaVersion === "phase29.release-exit-governance-freeze-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitGovernanceFreezePreview.schemaVersion },
    { id: "archive-integrity-linked-to-release-exit-dossier", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "archive-integrity-linked-to-final-exit-package", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "archive-integrity-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "archive-integrity-covers-all-archive-index-items", result: integrityCheckItems.length === archiveIndexItems.length ? "pass" : "block", evidence: "integrityCheckItems=" + integrityCheckItems.length + "; archiveIndexItems=" + archiveIndexItems.length },
    { id: "archive-integrity-confirms-archive-index-checks-pass", result: (phase29ReleaseExitFinalArchiveIndexPreview.archiveIndexChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "archiveIndexChecks=" + (phase29ReleaseExitFinalArchiveIndexPreview.summary?.archiveIndexChecks ?? 0) + "; passed=" + (phase29ReleaseExitFinalArchiveIndexPreview.summary?.passedArchiveIndexChecks ?? 0) },
    { id: "archive-integrity-confirms-no-missing-references", result: integrityCheckItems.every((item) => item.referenceCoverageState === "complete-readonly-reference-coverage") ? "pass" : "block", evidence: "missingReferences=" + integrityCheckItems.filter((item) => item.referenceCoverageState !== "complete-readonly-reference-coverage").length },
    { id: "archive-integrity-keeps-archive-index-unpersisted", result: archiveIndexItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedArchiveIndexItems=" + archiveIndexItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-reconciliation-unpersisted", result: evidenceReconciliationItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedEvidenceReconciliationItems=" + evidenceReconciliationItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-signoff-review-unpersisted", result: signoffReviewItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedSignoffReviewItems=" + signoffReviewItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-freeze-unpersisted", result: freezeItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedFreezeItems=" + freezeItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "archive-integrity-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "archive-integrity-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "archive-integrity-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "archive-integrity-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "archive-integrity-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "archive-integrity-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "archive-integrity-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false" },
    { id: "archive-integrity-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-final-archive-integrity-check-preview.v1",
    version: "1.9.47",
    mode: "readonly-release-exit-final-archive-integrity-check-preview-no-integrity-persist",
    sourceModels: {
      phase29ReleaseExitFinalArchiveIndexPreview: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion,
      phase29ReleaseExitSignoffEvidenceReconciliationPreview: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion,
      phase29ReleaseExitFinalSignoffReviewPreview: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion,
      phase29ReleaseExitGovernanceFreezePreview: phase29ReleaseExitGovernanceFreezePreview.schemaVersion,
      phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion,
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    integrityCheckItems,
    integrityChecks,
    blockedMutations: [
      "persist-release-exit-final-archive-integrity-check-as-release-approval",
      "persist-release-exit-final-archive-integrity-check-as-phase29-exit",
      "persist-release-exit-final-archive-integrity-check-as-phase30-entry",
      "persist-release-exit-final-archive-integrity-check-artifact",
      "persist-release-exit-final-archive-integrity-check-item",
      "record-release-exit-final-archive-integrity-check",
      "record-release-exit-final-archive-index-from-integrity-check",
      "approve-release-from-release-exit-final-archive-integrity-check",
      "exit-phase29-from-release-exit-final-archive-integrity-check",
      "open-phase30-entry-from-release-exit-final-archive-integrity-check",
      "clear-blocker-from-release-exit-final-archive-integrity-check",
      "resolve-redline-from-release-exit-final-archive-integrity-check",
      "mark-release-exit-final-archive-integrity-checked",
      "override-release-exit-final-archive-index-from-integrity-check",
      "override-release-exit-signoff-evidence-reconciliation-from-integrity-check",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-final-archive-integrity-check-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-final-archive-integrity-check-source-index-reconciliation-signoff-freeze-dossier-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitFinalArchiveIntegrityCheckPreviewReady: true,
      integrityCheckItems: integrityCheckItems.length,
      blockedIntegrityCheckItems: integrityCheckItems.filter((item) => item.integrityCheckState.includes("blocked")).length,
      integrityChecks: integrityChecks.length,
      passedIntegrityChecks: integrityChecks.filter((item) => item.result === "pass").length,
      verifiedArchiveItems: 0,
      missingArchiveReferences: integrityCheckItems.filter((item) => item.referenceCoverageState !== "complete-readonly-reference-coverage").length,
      persistedIntegrityCheckItems: integrityCheckItems.filter((item) => item.persisted !== false).length,
      persistedArchiveIndexItems: archiveIndexItems.filter((item) => item.persisted !== false).length,
      persistedEvidenceReconciliationItems: evidenceReconciliationItems.filter((item) => item.persisted !== false).length,
      persistedSignoffReviewItems: signoffReviewItems.filter((item) => item.persisted !== false).length,
      persistedFreezeItems: freezeItems.filter((item) => item.persisted !== false).length,
      persistedDossierItems: dossierItems.filter((item) => item.persisted !== false).length,
      persistedExitPackageItems: exitPackageItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}


function buildLocalPhase29ReleaseExitFinalArchiveManifestPreview(phase29ReleaseExitFinalArchiveIntegrityCheckPreview = {}, phase29ReleaseExitFinalArchiveIndexPreview = {}, phase29ReleaseExitSignoffEvidenceReconciliationPreview = {}, phase29ReleaseExitFinalSignoffReviewPreview = {}, phase29ReleaseExitGovernanceFreezePreview = {}, phase29ReleaseExitReadinessDossierPreview = {}, phase29FinalReadonlyExitPackagePreview = {}, finalReleaseBlockerDossier = {}, transitionReadinessRedline = {}, phase30EntryPreflight = {}, runtimeEnableGovernance = {}) {
  const integrityCheckItems = phase29ReleaseExitFinalArchiveIntegrityCheckPreview.integrityCheckItems || [];
  const archiveIndexItems = phase29ReleaseExitFinalArchiveIndexPreview.archiveIndexItems || [];
  const evidenceReconciliationItems = phase29ReleaseExitSignoffEvidenceReconciliationPreview.evidenceReconciliationItems || [];
  const signoffReviewItems = phase29ReleaseExitFinalSignoffReviewPreview.signoffReviewItems || [];
  const freezeItems = phase29ReleaseExitGovernanceFreezePreview.freezeItems || [];
  const dossierItems = phase29ReleaseExitReadinessDossierPreview.dossierItems || [];
  const exitPackageItems = phase29FinalReadonlyExitPackagePreview.exitPackageItems || [];
  const manifestItems = integrityCheckItems.map((item, index) => ({
    id: "final-archive-manifest-" + item.id.replace(/^final-archive-integrity-check-/, ""),
    label: item.label.replace("release exit final archive integrity check", "release exit final archive manifest"),
    manifestOrder: index + 1,
    sourceIntegrityCheckItemId: item.id,
    sourceArchiveIndexItemId: item.sourceArchiveIndexItemId,
    sourceEvidenceReconciliationItemId: item.sourceEvidenceReconciliationItemId,
    sourceSignoffReviewItemId: item.sourceSignoffReviewItemId,
    sourceFreezeItemId: item.sourceFreezeItemId,
    sourceDossierItemId: item.sourceDossierItemId,
    sourceExitPackageItemId: item.sourceExitPackageItemId,
    sourceHardeningItemId: item.sourceHardeningItemId,
    sourceClosureItemId: item.sourceClosureItemId,
    sourceAuditItemId: item.sourceAuditItemId,
    sourceSignoffItemId: item.sourceSignoffItemId,
    sourceReconciliationItemId: item.sourceReconciliationItemId,
    sourceFinalExportItemId: item.sourceFinalExportItemId,
    sourceLedgerEntryId: item.sourceLedgerEntryId,
    sourceDispositionItemId: item.sourceDispositionItemId,
    sourceQueueItemId: item.sourceQueueItemId,
    sourceIndexItemId: item.sourceIndexItemId,
    evidenceType: item.evidenceType,
    owner: item.owner,
    candidateDisposition: item.candidateDisposition,
    requiredSignoffRoles: item.requiredSignoffRoles || [],
    manifestState: "blocked-readonly-release-exit-final-archive-manifest-preview",
    manifestPersistenceState: "not-persisted",
    integrityCheckState: item.integrityCheckState,
    integrityPersistenceState: item.integrityPersistenceState,
    archiveIndexState: item.archiveIndexState,
    archivePersistenceState: item.archivePersistenceState,
    referenceCoverageState: item.referenceCoverageState,
    manifestCoverageState: item.sourceArchiveIndexItemId && item.sourceEvidenceReconciliationItemId && item.sourceSignoffReviewItemId && item.referenceCoverageState === "complete-readonly-reference-coverage" ? "complete-readonly-manifest-coverage" : "missing-manifest-reference",
    mayPersistManifest: false,
    mayPersistIntegrityCheck: false,
    mayPersistArchiveIndex: false,
    mayApproveRelease: false,
    mayExitPhase29: false,
    mayEnterPhase30: false,
    mayEnableRuntime: false,
    persisted: false
  }));
  const manifestChecks = [
    { id: "archive-manifest-linked-to-integrity-check", result: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.schemaVersion === "phase29.release-exit-final-archive-integrity-check-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.schemaVersion },
    { id: "archive-manifest-linked-to-final-archive-index", result: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion === "phase29.release-exit-final-archive-index-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion },
    { id: "archive-manifest-linked-to-signoff-evidence-reconciliation", result: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion === "phase29.release-exit-signoff-evidence-reconciliation-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion },
    { id: "archive-manifest-linked-to-final-signoff-review", result: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion === "phase29.release-exit-final-signoff-review-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion },
    { id: "archive-manifest-linked-to-governance-freeze", result: phase29ReleaseExitGovernanceFreezePreview.schemaVersion === "phase29.release-exit-governance-freeze-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitGovernanceFreezePreview.schemaVersion },
    { id: "archive-manifest-linked-to-release-exit-dossier", result: phase29ReleaseExitReadinessDossierPreview.schemaVersion === "phase29.release-exit-readiness-dossier-preview.v1" ? "pass" : "block", evidence: phase29ReleaseExitReadinessDossierPreview.schemaVersion },
    { id: "archive-manifest-linked-to-final-exit-package", result: phase29FinalReadonlyExitPackagePreview.schemaVersion === "phase29.final-readonly-exit-package-preview.v1" ? "pass" : "block", evidence: phase29FinalReadonlyExitPackagePreview.schemaVersion },
    { id: "archive-manifest-linked-to-final-blocker-dossier", result: finalReleaseBlockerDossier.schemaVersion === "phase29.final-release-blocker-dossier.v1" ? "pass" : "block", evidence: finalReleaseBlockerDossier.schemaVersion },
    { id: "archive-manifest-covers-all-integrity-check-items", result: manifestItems.length === integrityCheckItems.length ? "pass" : "block", evidence: "manifestItems=" + manifestItems.length + "; integrityCheckItems=" + integrityCheckItems.length },
    { id: "archive-manifest-confirms-integrity-checks-pass", result: (phase29ReleaseExitFinalArchiveIntegrityCheckPreview.integrityChecks || []).every((item) => item.result === "pass") ? "pass" : "block", evidence: "integrityChecks=" + (phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary?.integrityChecks ?? 0) + "; passed=" + (phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary?.passedIntegrityChecks ?? 0) },
    { id: "archive-manifest-confirms-no-missing-references", result: manifestItems.every((item) => item.manifestCoverageState === "complete-readonly-manifest-coverage") ? "pass" : "block", evidence: "missingManifestReferences=" + manifestItems.filter((item) => item.manifestCoverageState !== "complete-readonly-manifest-coverage").length },
    { id: "archive-manifest-keeps-integrity-check-unpersisted", result: integrityCheckItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedIntegrityCheckItems=" + integrityCheckItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-archive-index-unpersisted", result: archiveIndexItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedArchiveIndexItems=" + archiveIndexItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-reconciliation-unpersisted", result: evidenceReconciliationItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedEvidenceReconciliationItems=" + evidenceReconciliationItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-signoff-review-unpersisted", result: signoffReviewItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedSignoffReviewItems=" + signoffReviewItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-freeze-unpersisted", result: freezeItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedFreezeItems=" + freezeItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-dossier-unpersisted", result: dossierItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedDossierItems=" + dossierItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-exit-package-unpersisted", result: exitPackageItems.every((item) => item.persisted === false) ? "pass" : "block", evidence: "persistedExitPackageItems=" + exitPackageItems.filter((item) => item.persisted !== false).length },
    { id: "archive-manifest-keeps-redlines-active", result: transitionReadinessRedline.summary?.activeRedlines >= 1 ? "pass" : "block", evidence: "activeRedlines=" + (transitionReadinessRedline.summary?.activeRedlines ?? 0) },
    { id: "archive-manifest-keeps-blockers-open", result: finalReleaseBlockerDossier.summary?.unresolvedBlockers >= 1 ? "pass" : "block", evidence: "unresolvedBlockers=" + (finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0) },
    { id: "archive-manifest-keeps-entry-preflight-blocked", result: phase30EntryPreflight.summary?.phase30EntryReady === false || phase30EntryPreflight.summary?.blockedEntryGates >= 1 ? "pass" : "block", evidence: "blockedEntryGates=" + (phase30EntryPreflight.summary?.blockedEntryGates ?? 0) + "; phase30EntryReady=" + (phase30EntryPreflight.summary?.phase30EntryReady === true) },
    { id: "archive-manifest-keeps-phase29-active", result: "pass", evidence: "phase29ExitReady=false" },
    { id: "archive-manifest-keeps-phase30-blocked", result: "pass", evidence: "phase30EntryReady=false" },
    { id: "archive-manifest-keeps-release-blocked", result: "pass", evidence: "releaseReady=false" },
    { id: "archive-manifest-keeps-runtime-disabled", result: "pass", evidence: "runtimeExecution=false" },
    { id: "archive-manifest-does-not-persist-mutations", result: "pass", evidence: "persistedMutations=0" }
  ];
  return {
    schemaVersion: "phase29.release-exit-final-archive-manifest-preview.v1",
    version: "1.9.48",
    mode: "readonly-release-exit-final-archive-manifest-preview-no-manifest-persist",
    sourceModels: {
      phase29ReleaseExitFinalArchiveIntegrityCheckPreview: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.schemaVersion,
      phase29ReleaseExitFinalArchiveIndexPreview: phase29ReleaseExitFinalArchiveIndexPreview.schemaVersion,
      phase29ReleaseExitSignoffEvidenceReconciliationPreview: phase29ReleaseExitSignoffEvidenceReconciliationPreview.schemaVersion,
      phase29ReleaseExitFinalSignoffReviewPreview: phase29ReleaseExitFinalSignoffReviewPreview.schemaVersion,
      phase29ReleaseExitGovernanceFreezePreview: phase29ReleaseExitGovernanceFreezePreview.schemaVersion,
      phase29ReleaseExitReadinessDossierPreview: phase29ReleaseExitReadinessDossierPreview.schemaVersion,
      phase29FinalReadonlyExitPackagePreview: phase29FinalReadonlyExitPackagePreview.schemaVersion,
      finalReleaseBlockerDossier: finalReleaseBlockerDossier.schemaVersion,
      transitionReadinessRedline: transitionReadinessRedline.schemaVersion,
      phase30EntryPreflight: phase30EntryPreflight.schemaVersion,
      runtimeEnableGovernance: runtimeEnableGovernance.schemaVersion
    },
    manifestItems,
    manifestChecks,
    blockedMutations: [
      "persist-release-exit-final-archive-manifest-as-release-approval",
      "persist-release-exit-final-archive-manifest-as-phase29-exit",
      "persist-release-exit-final-archive-manifest-as-phase30-entry",
      "persist-release-exit-final-archive-manifest-artifact",
      "persist-release-exit-final-archive-manifest-item",
      "record-release-exit-final-archive-manifest",
      "record-release-exit-final-archive-integrity-check-from-manifest",
      "approve-release-from-release-exit-final-archive-manifest",
      "exit-phase29-from-release-exit-final-archive-manifest",
      "open-phase30-entry-from-release-exit-final-archive-manifest",
      "clear-blocker-from-release-exit-final-archive-manifest",
      "resolve-redline-from-release-exit-final-archive-manifest",
      "mark-release-exit-final-archive-manifested",
      "override-release-exit-final-archive-integrity-check-from-manifest",
      "override-release-exit-final-archive-index-from-manifest",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-exit-final-archive-manifest-preview.json",
      includeInOperationsExport: true,
      redaction: "phase29-release-exit-final-archive-manifest-source-integrity-index-reconciliation-signoff-freeze-dossier-redline-blocker-and-readonly-guardrail-summary-only"
    },
    summary: {
      phase29ReleaseExitFinalArchiveManifestPreviewReady: true,
      manifestItems: manifestItems.length,
      blockedManifestItems: manifestItems.filter((item) => item.manifestState.includes("blocked")).length,
      manifestChecks: manifestChecks.length,
      passedManifestChecks: manifestChecks.filter((item) => item.result === "pass").length,
      manifestedArchiveItems: 0,
      missingManifestReferences: manifestItems.filter((item) => item.manifestCoverageState !== "complete-readonly-manifest-coverage").length,
      persistedManifestItems: manifestItems.filter((item) => item.persisted !== false).length,
      persistedIntegrityCheckItems: integrityCheckItems.filter((item) => item.persisted !== false).length,
      persistedArchiveIndexItems: archiveIndexItems.filter((item) => item.persisted !== false).length,
      activeRedlines: transitionReadinessRedline.summary?.activeRedlines ?? 0,
      unresolvedBlockers: finalReleaseBlockerDossier.summary?.unresolvedBlockers ?? 0,
      phase29ExitReady: false,
      phase30Blocked: true,
      phase30EntryReady: false,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      clearedBlockers: 0,
      persistedMutations: 0
    }
  };
}
function buildLocalPhase29ReleaseGovernancePlanning() {
  const phase28 = buildLocalPhase28ClearanceReviewEntry();
  const closurePackage = phase28.phase28ClosurePackage || {};
  const carriedForwardBlockers = phase28.clearanceReviewQueue || [];
  const blockerClearanceWorkflow = buildLocalPhase29BlockerClearanceWorkflow(phase28, closurePackage, carriedForwardBlockers);
  const releaseApprovalStateModel = buildLocalPhase29ReleaseApprovalStateModel(blockerClearanceWorkflow);
  const runtimeEnableGovernance = buildLocalPhase29RuntimeEnableGovernance(releaseApprovalStateModel);
  const governanceTracks = [
    {
      id: "blocker-clearance-governance",
      label: "Blocker clearance governance",
      source: closurePackage.schemaVersion,
      state: "planning",
      owner: "human-release-governance",
      requiredBeforeRelease: [
        "human-owned-clearance-evidence",
        "reviewer-disposition-recorded",
        "criteria-satisfaction-reviewed",
        "release-approval-separated-from-clearance"
      ],
      blockedActions: ["auto-clear-blocker", "clear-blocker-without-human-evidence"]
    },
    {
      id: "release-approval-state-model",
      label: "Release approval state model",
      source: "phase28.closure-package.v1",
      state: "planning",
      owner: "release-owner",
      requiredBeforeRelease: [
        "no-open-blockers",
        "signed-release-decision",
        "artifact-integrity-verified",
        "rollback-and-backup-reviewed"
      ],
      blockedActions: ["mark-releaseReady-true", "approve-release-from-planning"]
    },
    {
      id: "runtime-enable-governance",
      label: "Runtime enable governance",
      source: "phase26.runtime-validation-entry.v1",
      state: "deferred",
      owner: "runtime-reviewer",
      requiredBeforeRelease: [
        "runtime-validation-pass",
        "quarantine-cleared",
        "permission-policy-reviewed",
        "kill-switch-reviewed"
      ],
      blockedActions: ["enable-runtime-execution", "execute-third-party-plugin"]
    }
  ];
  const gateSeparationModel = [
    { id: "review-complete", label: "Review complete", mayClearBlockers: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "blocker-clearance", label: "Blocker clearance", mayClearBlockers: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "release-approval", label: "Release approval", mayClearBlockers: false, mayApproveRelease: false, mayEnableRuntime: false },
    { id: "runtime-approval", label: "Runtime approval", mayClearBlockers: false, mayApproveRelease: false, mayEnableRuntime: false }
  ];
  const governanceDashboardExport = buildLocalPhase29GovernanceDashboardExport(blockerClearanceWorkflow, releaseApprovalStateModel, runtimeEnableGovernance, gateSeparationModel);
  const decisionHistoryAuditChain = buildLocalPhase29DecisionHistoryAuditChain(blockerClearanceWorkflow, releaseApprovalStateModel, runtimeEnableGovernance, governanceDashboardExport);
  const governanceClosureReadiness = buildLocalPhase29GovernanceClosureReadiness(blockerClearanceWorkflow, releaseApprovalStateModel, runtimeEnableGovernance, governanceDashboardExport, decisionHistoryAuditChain, carriedForwardBlockers);
  const closureHandoffPackage = buildLocalPhase29ClosureHandoffPackage(blockerClearanceWorkflow, releaseApprovalStateModel, runtimeEnableGovernance, governanceDashboardExport, decisionHistoryAuditChain, governanceClosureReadiness);
  const handoffReviewIndex = buildLocalPhase29HandoffReviewIndex(closureHandoffPackage, governanceClosureReadiness);
  const finalClosureReadinessSnapshot = buildLocalPhase29FinalClosureReadinessSnapshot(handoffReviewIndex, closureHandoffPackage, governanceClosureReadiness, decisionHistoryAuditChain, governanceDashboardExport, runtimeEnableGovernance, releaseApprovalStateModel, blockerClearanceWorkflow);
  const governanceArchiveFreeze = buildLocalPhase29GovernanceArchiveFreeze(finalClosureReadinessSnapshot, handoffReviewIndex, closureHandoffPackage, governanceClosureReadiness, decisionHistoryAuditChain, governanceDashboardExport);
  const blockerResolutionMap = buildLocalPhase29BlockerResolutionMap(governanceArchiveFreeze, finalClosureReadinessSnapshot, handoffReviewIndex, governanceClosureReadiness, blockerClearanceWorkflow);
  const phase30EntryPreflight = buildLocalPhase29Phase30EntryPreflight(blockerResolutionMap, governanceArchiveFreeze, finalClosureReadinessSnapshot, releaseApprovalStateModel, runtimeEnableGovernance);
  const finalReleaseBlockerDossier = buildLocalPhase29FinalReleaseBlockerDossier(phase30EntryPreflight, blockerResolutionMap, governanceClosureReadiness, releaseApprovalStateModel, runtimeEnableGovernance);
  const releaseExitCriteriaLedger = buildLocalPhase29ReleaseExitCriteriaLedger(finalReleaseBlockerDossier, phase30EntryPreflight, governanceArchiveFreeze, releaseApprovalStateModel, runtimeEnableGovernance);
  const releaseExitDecisionPreview = buildLocalPhase29ReleaseExitDecisionPreview(releaseExitCriteriaLedger, finalReleaseBlockerDossier, phase30EntryPreflight, releaseApprovalStateModel, runtimeEnableGovernance);
  const phase30TransitionRiskRegister = buildLocalPhase29Phase30TransitionRiskRegister(releaseExitDecisionPreview, releaseExitCriteriaLedger, finalReleaseBlockerDossier, phase30EntryPreflight, runtimeEnableGovernance);
  const transitionEvidenceGapReview = buildLocalPhase29TransitionEvidenceGapReview(phase30TransitionRiskRegister, releaseExitDecisionPreview, finalReleaseBlockerDossier, phase30EntryPreflight, governanceArchiveFreeze);
  const transitionReadinessRedline = buildLocalPhase29TransitionReadinessRedline(transitionEvidenceGapReview, phase30TransitionRiskRegister, releaseExitDecisionPreview, phase30EntryPreflight, runtimeEnableGovernance);
  const finalTransitionHandoffPreview = buildLocalPhase29FinalTransitionHandoffPreview(transitionReadinessRedline, transitionEvidenceGapReview, phase30TransitionRiskRegister, releaseExitDecisionPreview, phase30EntryPreflight, governanceArchiveFreeze);
  const transitionClosurePackagePreview = buildLocalPhase29TransitionClosurePackagePreview(finalTransitionHandoffPreview, transitionReadinessRedline, transitionEvidenceGapReview, phase30TransitionRiskRegister, releaseExitDecisionPreview, governanceArchiveFreeze, phase30EntryPreflight);
  const releaseCandidateFreezePreview = buildLocalPhase29ReleaseCandidateFreezePreview(transitionClosurePackagePreview, finalTransitionHandoffPreview, transitionReadinessRedline, releaseExitDecisionPreview, governanceArchiveFreeze, phase30EntryPreflight, runtimeEnableGovernance);
  const finalReadinessBoardPreview = buildLocalPhase29FinalReadinessBoardPreview(releaseCandidateFreezePreview, transitionClosurePackagePreview, finalTransitionHandoffPreview, transitionReadinessRedline, releaseExitDecisionPreview, phase30EntryPreflight, runtimeEnableGovernance);
  const releaseFinalReviewEnvelopePreview = buildLocalPhase29ReleaseFinalReviewEnvelopePreview(finalReadinessBoardPreview, releaseCandidateFreezePreview, transitionClosurePackagePreview, transitionReadinessRedline, releaseExitDecisionPreview, phase30EntryPreflight, governanceArchiveFreeze, runtimeEnableGovernance);
  const exitReadinessAttestationPreview = buildLocalPhase29ExitReadinessAttestationPreview(releaseFinalReviewEnvelopePreview, finalReadinessBoardPreview, releaseExitDecisionPreview, phase30EntryPreflight, transitionReadinessRedline, finalReleaseBlockerDossier, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30PlanningBriefPreview = buildLocalPhase29Phase30PlanningBriefPreview(exitReadinessAttestationPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, releaseFinalReviewEnvelopePreview, finalReadinessBoardPreview, governanceArchiveFreeze);
  const phase30ScopeLockPreview = buildLocalPhase29Phase30ScopeLockPreview(phase30PlanningBriefPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30ReadinessMapPreview = buildLocalPhase29Phase30ReadinessMapPreview(phase30ScopeLockPreview, phase30PlanningBriefPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30ReadinessGapBriefPreview = buildLocalPhase29Phase30ReadinessGapBriefPreview(phase30ReadinessMapPreview, phase30ScopeLockPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30GapResolutionSequencePreview = buildLocalPhase29Phase30GapResolutionSequencePreview(phase30ReadinessGapBriefPreview, phase30ReadinessMapPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30EntryEvidencePackPreview = buildLocalPhase29Phase30EntryEvidencePackPreview(phase30GapResolutionSequencePreview, phase30ReadinessGapBriefPreview, phase30ReadinessMapPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30EntryEvidenceIndexPreview = buildLocalPhase29Phase30EntryEvidenceIndexPreview(phase30EntryEvidencePackPreview, phase30GapResolutionSequencePreview, phase30ReadinessGapBriefPreview, phase30ReadinessMapPreview, phase30EntryPreflight, phase30TransitionRiskRegister, transitionEvidenceGapReview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance, governanceArchiveFreeze);
  const phase30EntryEvidenceReviewQueuePreview = buildLocalPhase29Phase30EntryEvidenceReviewQueuePreview(phase30EntryEvidenceIndexPreview, phase30EntryEvidencePackPreview, phase30GapResolutionSequencePreview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryEvidenceReviewDispositionPreview = buildLocalPhase29Phase30EntryEvidenceReviewDispositionPreview(phase30EntryEvidenceReviewQueuePreview, phase30EntryEvidenceIndexPreview, phase30EntryEvidencePackPreview, transitionReadinessRedline, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryEvidenceDispositionLedgerPreview = buildLocalPhase29Phase30EntryEvidenceDispositionLedgerPreview(phase30EntryEvidenceReviewDispositionPreview, phase30EntryEvidenceReviewQueuePreview, phase30EntryEvidenceIndexPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryEvidenceFinalLedgerExportPreview = buildLocalPhase29Phase30EntryEvidenceFinalLedgerExportPreview(phase30EntryEvidenceDispositionLedgerPreview, phase30EntryEvidenceReviewDispositionPreview, phase30EntryEvidenceReviewQueuePreview, phase30EntryEvidenceIndexPreview, phase30EntryEvidencePackPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryFinalExportReconciliationPreview = buildLocalPhase29Phase30EntryFinalExportReconciliationPreview(phase30EntryEvidenceFinalLedgerExportPreview, phase30EntryEvidenceDispositionLedgerPreview, phase30EntryEvidenceReviewDispositionPreview, phase30EntryEvidenceReviewQueuePreview, phase30EntryEvidenceIndexPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryReconciliationSignoffPreview = buildLocalPhase29Phase30EntryReconciliationSignoffPreview(phase30EntryFinalExportReconciliationPreview, phase30EntryEvidenceFinalLedgerExportPreview, releaseFinalReviewEnvelopePreview, exitReadinessAttestationPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntrySignoffAuditPreview = buildLocalPhase29Phase30EntrySignoffAuditPreview(phase30EntryReconciliationSignoffPreview, phase30EntryFinalExportReconciliationPreview, releaseFinalReviewEnvelopePreview, exitReadinessAttestationPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase30EntryAuditClosurePreview = buildLocalPhase29Phase30EntryAuditClosurePreview(phase30EntrySignoffAuditPreview, phase30EntryReconciliationSignoffPreview, phase30EntryFinalExportReconciliationPreview, releaseFinalReviewEnvelopePreview, exitReadinessAttestationPreview, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase29AuditClosureReadinessHardening = buildLocalPhase29AuditClosureReadinessHardening(phase30EntryAuditClosurePreview, phase30EntrySignoffAuditPreview, phase30EntryReconciliationSignoffPreview, phase30EntryFinalExportReconciliationPreview, transitionReadinessRedline, phase30EntryPreflight, finalReleaseBlockerDossier, releaseExitDecisionPreview, runtimeEnableGovernance);
  const phase29FinalReadonlyExitPackagePreview = buildLocalPhase29FinalReadonlyExitPackagePreview(phase29AuditClosureReadinessHardening, releaseExitDecisionPreview, exitReadinessAttestationPreview, releaseFinalReviewEnvelopePreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitReadinessDossierPreview = buildLocalPhase29ReleaseExitReadinessDossierPreview(phase29FinalReadonlyExitPackagePreview, phase29AuditClosureReadinessHardening, releaseExitDecisionPreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitGovernanceFreezePreview = buildLocalPhase29ReleaseExitGovernanceFreezePreview(phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, governanceArchiveFreeze, releaseExitDecisionPreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitFinalSignoffReviewPreview = buildLocalPhase29ReleaseExitFinalSignoffReviewPreview(phase29ReleaseExitGovernanceFreezePreview, phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, releaseExitDecisionPreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitSignoffEvidenceReconciliationPreview = buildLocalPhase29ReleaseExitSignoffEvidenceReconciliationPreview(phase29ReleaseExitFinalSignoffReviewPreview, phase29ReleaseExitGovernanceFreezePreview, phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, releaseExitDecisionPreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitFinalArchiveIndexPreview = buildLocalPhase29ReleaseExitFinalArchiveIndexPreview(phase29ReleaseExitSignoffEvidenceReconciliationPreview, phase29ReleaseExitFinalSignoffReviewPreview, phase29ReleaseExitGovernanceFreezePreview, phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitFinalArchiveIntegrityCheckPreview = buildLocalPhase29ReleaseExitFinalArchiveIntegrityCheckPreview(phase29ReleaseExitFinalArchiveIndexPreview, phase29ReleaseExitSignoffEvidenceReconciliationPreview, phase29ReleaseExitFinalSignoffReviewPreview, phase29ReleaseExitGovernanceFreezePreview, phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  const phase29ReleaseExitFinalArchiveManifestPreview = buildLocalPhase29ReleaseExitFinalArchiveManifestPreview(phase29ReleaseExitFinalArchiveIntegrityCheckPreview, phase29ReleaseExitFinalArchiveIndexPreview, phase29ReleaseExitSignoffEvidenceReconciliationPreview, phase29ReleaseExitFinalSignoffReviewPreview, phase29ReleaseExitGovernanceFreezePreview, phase29ReleaseExitReadinessDossierPreview, phase29FinalReadonlyExitPackagePreview, finalReleaseBlockerDossier, transitionReadinessRedline, phase30EntryPreflight, runtimeEnableGovernance);
  return {
    phase: 29,
    schemaVersion: "phase29.release-governance-planning.v1",
    version: "1.9.48",
    status: "phase29-release-exit-final-archive-manifest-preview-ready-runtime-disabled",
    planningMode: "readonly-release-governance-planning-no-release-approval",
    sourcePhase28: {
      schemaVersion: phase28.schemaVersion,
      closurePackage: closurePackage.schemaVersion,
      phase28Closed: closurePackage.summary?.phaseClosed === true,
      phase28ReleaseReady: phase28.summary?.releaseReady,
      phase28RuntimeExecution: phase28.summary?.runtimeExecution,
      carriedForwardBlockers: carriedForwardBlockers.length
    },
    governanceTracks,
    blockerClearanceWorkflow,
    releaseApprovalStateModel,
    runtimeEnableGovernance,
    governanceDashboardExport,
    decisionHistoryAuditChain,
    governanceClosureReadiness,
    closureHandoffPackage,
    handoffReviewIndex,
    finalClosureReadinessSnapshot,
    governanceArchiveFreeze,
    blockerResolutionMap,
    phase30EntryPreflight,
    finalReleaseBlockerDossier,
    releaseExitCriteriaLedger,
    releaseExitDecisionPreview,
    phase30TransitionRiskRegister,
    transitionEvidenceGapReview,
    transitionReadinessRedline,
    finalTransitionHandoffPreview,
    transitionClosurePackagePreview,
    releaseCandidateFreezePreview,
    finalReadinessBoardPreview,
    releaseFinalReviewEnvelopePreview,
    exitReadinessAttestationPreview,
    phase30PlanningBriefPreview,
    phase30ScopeLockPreview,
    phase30ReadinessMapPreview,
    phase30ReadinessGapBriefPreview,
    phase30GapResolutionSequencePreview,
    phase30EntryEvidencePackPreview,
    phase30EntryEvidenceIndexPreview,
    phase30EntryEvidenceReviewQueuePreview,
    phase30EntryEvidenceReviewDispositionPreview,
    phase30EntryEvidenceDispositionLedgerPreview,
    phase30EntryEvidenceFinalLedgerExportPreview,
    phase30EntryFinalExportReconciliationPreview,
    phase30EntryReconciliationSignoffPreview,
    phase30EntrySignoffAuditPreview,
    phase30EntryAuditClosurePreview,
    phase29AuditClosureReadinessHardening,
    phase29FinalReadonlyExitPackagePreview,
      phase29ReleaseExitReadinessDossierPreview,
      phase29ReleaseExitGovernanceFreezePreview,
      phase29ReleaseExitFinalSignoffReviewPreview,
      phase29ReleaseExitSignoffEvidenceReconciliationPreview,
      phase29ReleaseExitFinalArchiveIndexPreview,
      phase29ReleaseExitFinalArchiveIntegrityCheckPreview,
      phase29ReleaseExitFinalArchiveManifestPreview,
      gateSeparationModel,
    entryChecks: [
      { id: "phase28-closure-linked", result: closurePackage.schemaVersion === "phase28.closure-package.v1" ? "pass" : "block", evidence: closurePackage.schemaVersion },
      { id: "phase28-closed", result: closurePackage.summary?.phaseClosed === true ? "pass" : "block", evidence: `phaseClosed=${closurePackage.summary?.phaseClosed === true}` },
      { id: "open-blockers-carried", result: carriedForwardBlockers.length > 0 ? "pass" : "block", evidence: `${carriedForwardBlockers.length} carried blocker` },
      { id: "planning-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "planning-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "clear-blocker-from-phase29-planning",
      "apply-planning-as-clearance",
      "apply-workflow-as-clearance",
      "persist-clearance-as-approved",
      "approve-release-from-planning",
      "approve-release-from-state-model",
      "persist-approval-state-as-release-ready",
      "mark-releaseReady-true",
      "enable-runtime-from-phase29-governance",
      "persist-runtime-governance-as-enable-approval",
      "persist-dashboard-as-release-approval",
      "persist-dashboard-as-clearance",
      "persist-audit-chain-as-release-approval",
      "persist-audit-chain-as-clearance",
      "rewrite-decision-history",
      "persist-closure-readiness-as-release-approval",
      "persist-closure-readiness-as-phase30-entry",
      "clear-blocker-from-closure-readiness",
      "persist-handoff-as-release-approval",
      "persist-handoff-as-phase30-entry",
      "clear-blocker-from-handoff-package",
      "persist-review-index-as-release-approval",
      "persist-review-index-as-phase30-entry",
      "clear-blocker-from-review-index",
      "persist-final-snapshot-as-release-approval",
      "persist-final-snapshot-as-phase30-entry",
      "clear-blocker-from-final-snapshot",
      "persist-archive-freeze-as-release-approval",
      "persist-archive-freeze-as-phase30-entry",
      "unfreeze-phase29-archive-without-review",
      "rewrite-frozen-governance-archive",
      "persist-resolution-map-as-clearance",
      "persist-resolution-map-as-release-approval",
      "persist-resolution-map-as-phase30-entry",
      "clear-blocker-from-resolution-map",
      "persist-preflight-as-phase30-entry",
      "persist-preflight-as-release-approval",
      "clear-blocker-from-preflight",
      "persist-dossier-as-clearance",
      "persist-dossier-as-release-approval",
      "persist-dossier-as-phase30-entry",
      "clear-blocker-from-dossier",
      "persist-exit-ledger-as-clearance",
      "persist-exit-ledger-as-release-approval",
      "persist-exit-ledger-as-phase30-entry",
      "mark-exit-criteria-met",
      "clear-blocker-from-exit-ledger",
      "apply-exit-preview-as-release-approval",
      "apply-exit-preview-as-phase30-entry",
      "persist-exit-preview-as-clearance",
      "persist-exit-preview-as-release-approval",
      "persist-exit-preview-as-phase30-entry",
      "clear-blocker-from-exit-preview",
      "persist-risk-register-as-clearance",
      "persist-risk-register-as-release-approval",
      "persist-risk-register-as-phase30-entry",
      "apply-risk-register-as-release-approval",
      "open-phase30-entry-from-risk-register",
      "clear-blocker-from-transition-risk",
      "mark-transition-risk-mitigated-without-evidence",
      "persist-evidence-gap-review-as-clearance",
      "persist-evidence-gap-review-as-release-approval",
      "persist-evidence-gap-review-as-phase30-entry",
      "apply-evidence-gap-review-as-release-approval",
      "open-phase30-entry-from-evidence-gap-review",
      "close-evidence-gap-without-human-signoff",
      "clear-blocker-from-evidence-gap-review",
      "persist-redline-as-clearance",
      "persist-redline-as-release-approval",
      "persist-redline-as-phase30-entry",
      "approve-release-from-redline",
      "open-phase30-entry-from-redline",
      "override-transition-redline",
      "clear-blocker-from-redline",
      "mark-redline-resolved-without-evidence",
      "persist-handoff-preview-as-clearance",
      "persist-handoff-preview-as-release-approval",
      "persist-handoff-preview-as-phase30-entry",
      "approve-release-from-handoff-preview",
      "open-phase30-entry-from-handoff-preview",
      "clear-blocker-from-handoff-preview",
      "override-redline-from-handoff-preview",
      "persist-closure-preview-as-clearance",
      "persist-closure-preview-as-release-approval",
      "persist-closure-preview-as-phase29-exit",
      "persist-closure-preview-as-phase30-entry",
      "approve-release-from-closure-preview",
      "exit-phase29-from-closure-preview",
      "open-phase30-entry-from-closure-preview",
      "clear-blocker-from-closure-preview",
      "override-handoff-preview-from-closure-preview",
      "persist-freeze-preview-as-release-approval",
      "persist-freeze-preview-as-phase29-exit",
      "persist-freeze-preview-as-phase30-entry",
      "approve-release-from-freeze-preview",
      "exit-phase29-from-freeze-preview",
      "open-phase30-entry-from-freeze-preview",
      "clear-blocker-from-freeze-preview",
      "unfreeze-release-candidate-without-review",
      "override-closure-preview-from-freeze-preview",
      "persist-readiness-board-as-release-approval",
      "persist-readiness-board-as-phase29-exit",
      "persist-readiness-board-as-phase30-entry",
      "approve-release-from-readiness-board",
      "exit-phase29-from-readiness-board",
      "open-phase30-entry-from-readiness-board",
      "clear-blocker-from-readiness-board",
      "override-freeze-preview-from-readiness-board",
      "persist-final-review-envelope-as-release-approval",
      "persist-final-review-envelope-as-phase29-exit",
      "persist-final-review-envelope-as-phase30-entry",
      "approve-release-from-final-review-envelope",
      "exit-phase29-from-final-review-envelope",
      "open-phase30-entry-from-final-review-envelope",
      "clear-blocker-from-final-review-envelope",
      "override-readiness-board-from-final-review-envelope",
      "persist-attestation-as-release-approval",
      "persist-attestation-as-phase29-exit",
      "persist-attestation-as-phase30-entry",
      "approve-release-from-attestation",
      "exit-phase29-from-attestation",
      "open-phase30-entry-from-attestation",
      "clear-blocker-from-attestation",
      "override-final-review-envelope-from-attestation",
      "persist-phase30-brief-as-release-approval",
      "persist-phase30-brief-as-phase29-exit",
      "persist-phase30-brief-as-phase30-entry",
      "approve-release-from-phase30-brief",
      "exit-phase29-from-phase30-brief",
      "open-phase30-entry-from-phase30-brief",
      "clear-blocker-from-phase30-brief",
      "resolve-redline-from-phase30-brief",
      "override-attestation-from-phase30-brief",
      "persist-scope-lock-as-release-approval",
      "persist-scope-lock-as-phase29-exit",
      "persist-scope-lock-as-phase30-entry",
      "approve-release-from-scope-lock",
      "exit-phase29-from-scope-lock",
      "open-phase30-entry-from-scope-lock",
      "clear-blocker-from-scope-lock",
      "unlock-phase30-scope-without-review",
      "override-planning-brief-from-scope-lock",
      "resolve-redline-from-scope-lock",
      "persist-readiness-map-as-release-approval",
      "persist-readiness-map-as-phase29-exit",
      "persist-readiness-map-as-phase30-entry",
      "approve-release-from-readiness-map",
      "exit-phase29-from-readiness-map",
      "open-phase30-entry-from-readiness-map",
      "clear-blocker-from-readiness-map",
      "resolve-redline-from-readiness-map",
      "override-scope-lock-from-readiness-map",
      "persist-gap-brief-as-release-approval",
      "persist-gap-brief-as-phase29-exit",
      "persist-gap-brief-as-phase30-entry",
      "approve-release-from-gap-brief",
      "exit-phase29-from-gap-brief",
      "open-phase30-entry-from-gap-brief",
      "clear-blocker-from-gap-brief",
      "resolve-redline-from-gap-brief",
      "override-readiness-map-from-gap-brief",
      "persist-resolution-sequence-as-release-approval",
      "persist-resolution-sequence-as-phase29-exit",
      "persist-resolution-sequence-as-phase30-entry",
      "approve-release-from-resolution-sequence",
      "exit-phase29-from-resolution-sequence",
      "open-phase30-entry-from-resolution-sequence",
      "clear-blocker-from-resolution-sequence",
      "resolve-redline-from-resolution-sequence",
      "override-gap-brief-from-resolution-sequence",
      "persist-entry-evidence-pack-as-release-approval",
      "persist-entry-evidence-pack-as-phase29-exit",
      "persist-entry-evidence-pack-as-phase30-entry",
      "approve-release-from-entry-evidence-pack",
      "exit-phase29-from-entry-evidence-pack",
      "open-phase30-entry-from-entry-evidence-pack",
      "clear-blocker-from-entry-evidence-pack",
      "resolve-redline-from-entry-evidence-pack",
      "override-resolution-sequence-from-entry-evidence-pack",
      "persist-entry-evidence-index-as-release-approval",
      "persist-entry-evidence-index-as-phase29-exit",
      "persist-entry-evidence-index-as-phase30-entry",
      "approve-release-from-entry-evidence-index",
      "exit-phase29-from-entry-evidence-index",
      "open-phase30-entry-from-entry-evidence-index",
      "clear-blocker-from-entry-evidence-index",
      "resolve-redline-from-entry-evidence-index",
      "override-entry-evidence-pack-from-entry-evidence-index",
      "persist-entry-evidence-review-queue-as-release-approval",
      "persist-entry-evidence-review-queue-as-phase29-exit",
      "persist-entry-evidence-review-queue-as-phase30-entry",
      "persist-review-disposition-from-entry-evidence-review-queue",
      "approve-release-from-entry-evidence-review-queue",
      "exit-phase29-from-entry-evidence-review-queue",
      "open-phase30-entry-from-entry-evidence-review-queue",
      "clear-blocker-from-entry-evidence-review-queue",
      "resolve-redline-from-entry-evidence-review-queue",
      "mark-entry-evidence-reviewed",
      "override-entry-evidence-index-from-review-queue",
      "persist-entry-evidence-review-disposition-as-release-approval",
      "persist-entry-evidence-review-disposition-as-phase29-exit",
      "persist-entry-evidence-review-disposition-as-phase30-entry",
      "persist-review-disposition-preview",
      "approve-release-from-entry-evidence-review-disposition",
      "exit-phase29-from-entry-evidence-review-disposition",
      "open-phase30-entry-from-entry-evidence-review-disposition",
      "clear-blocker-from-entry-evidence-review-disposition",
      "resolve-redline-from-entry-evidence-review-disposition",
      "mark-entry-evidence-dispositioned",
      "override-entry-evidence-review-queue-from-disposition",
      "persist-entry-evidence-disposition-ledger-as-release-approval",
      "persist-entry-evidence-disposition-ledger-as-phase29-exit",
      "persist-entry-evidence-disposition-ledger-as-phase30-entry",
      "persist-entry-evidence-disposition-ledger-entry",
      "persist-review-disposition-from-ledger",
      "approve-release-from-entry-evidence-disposition-ledger",
      "exit-phase29-from-entry-evidence-disposition-ledger",
      "open-phase30-entry-from-entry-evidence-disposition-ledger",
      "clear-blocker-from-entry-evidence-disposition-ledger",
      "resolve-redline-from-entry-evidence-disposition-ledger",
      "mark-entry-evidence-ledger-final",
      "override-entry-evidence-review-disposition-from-ledger",
      "persist-entry-evidence-final-ledger-export-as-release-approval",
      "persist-entry-evidence-final-ledger-export-as-phase29-exit",
      "persist-entry-evidence-final-ledger-export-as-phase30-entry",
      "persist-entry-evidence-final-ledger-export-artifact",
      "persist-entry-evidence-final-ledger-export-item",
      "approve-release-from-entry-evidence-final-ledger-export",
      "exit-phase29-from-entry-evidence-final-ledger-export",
      "open-phase30-entry-from-entry-evidence-final-ledger-export",
      "clear-blocker-from-entry-evidence-final-ledger-export",
      "resolve-redline-from-entry-evidence-final-ledger-export",
      "mark-entry-evidence-final-ledger-export-ready",
      "override-entry-evidence-disposition-ledger-from-final-export",
      "persist-entry-final-export-reconciliation-as-release-approval",
      "persist-entry-final-export-reconciliation-as-phase29-exit",
      "persist-entry-final-export-reconciliation-as-phase30-entry",
      "persist-entry-final-export-reconciliation-artifact",
      "persist-entry-final-export-reconciliation-item",
      "persist-final-ledger-export-from-reconciliation",
      "approve-release-from-entry-final-export-reconciliation",
      "exit-phase29-from-entry-final-export-reconciliation",
      "open-phase30-entry-from-entry-final-export-reconciliation",
      "clear-blocker-from-entry-final-export-reconciliation",
      "resolve-redline-from-entry-final-export-reconciliation",
      "mark-entry-final-export-reconciliation-complete",
      "override-entry-final-ledger-export-from-reconciliation",
      "persist-entry-reconciliation-signoff-as-release-approval",
      "persist-entry-reconciliation-signoff-as-phase29-exit",
      "persist-entry-reconciliation-signoff-as-phase30-entry",
      "persist-entry-reconciliation-signoff-artifact",
      "persist-entry-reconciliation-signoff-item",
      "persist-final-export-reconciliation-from-signoff",
      "record-reconciliation-signoff",
      "approve-release-from-entry-reconciliation-signoff",
      "exit-phase29-from-entry-reconciliation-signoff",
      "open-phase30-entry-from-entry-reconciliation-signoff",
      "clear-blocker-from-entry-reconciliation-signoff",
      "resolve-redline-from-entry-reconciliation-signoff",
      "mark-entry-reconciliation-signoff-complete",
      "override-entry-final-export-reconciliation-from-signoff",
      "persist-entry-signoff-audit-as-release-approval",
      "persist-entry-signoff-audit-as-phase29-exit",
      "persist-entry-signoff-audit-as-phase30-entry",
      "persist-entry-signoff-audit-artifact",
      "persist-entry-signoff-audit-item",
      "persist-reconciliation-signoff-from-audit",
      "record-signoff-audit",
      "record-reconciliation-signoff-from-audit",
      "approve-release-from-entry-signoff-audit",
      "exit-phase29-from-entry-signoff-audit",
      "open-phase30-entry-from-entry-signoff-audit",
      "clear-blocker-from-entry-signoff-audit",
      "resolve-redline-from-entry-signoff-audit",
      "mark-entry-signoff-audit-complete",
      "override-entry-reconciliation-signoff-from-audit",
      "persist-entry-audit-closure-as-release-approval",
      "persist-entry-audit-closure-as-phase29-exit",
      "persist-entry-audit-closure-as-phase30-entry",
      "persist-entry-audit-closure-artifact",
      "persist-entry-audit-closure-item",
      "persist-signoff-audit-from-closure",
      "record-audit-closure",
      "record-signoff-audit-from-closure",
      "record-reconciliation-signoff-from-closure",
      "approve-release-from-entry-audit-closure",
      "exit-phase29-from-entry-audit-closure",
      "open-phase30-entry-from-entry-audit-closure",
      "clear-blocker-from-entry-audit-closure",
      "resolve-redline-from-entry-audit-closure",
      "mark-entry-audit-closure-complete",
      "override-entry-signoff-audit-from-closure",
      "persist-audit-closure-hardening-as-release-approval",
      "persist-audit-closure-hardening-as-phase29-exit",
      "persist-audit-closure-hardening-as-phase30-entry",
      "persist-audit-closure-hardening-artifact",
      "persist-audit-closure-hardening-item",
      "persist-entry-audit-closure-from-hardening",
      "record-audit-closure-hardening",
      "record-audit-closure-from-hardening",
      "record-signoff-audit-from-hardening",
      "approve-release-from-audit-closure-hardening",
      "exit-phase29-from-audit-closure-hardening",
      "open-phase30-entry-from-audit-closure-hardening",
      "clear-blocker-from-audit-closure-hardening",
      "resolve-redline-from-audit-closure-hardening",
      "mark-audit-closure-hardened",
      "override-entry-audit-closure-from-hardening",
      "persist-final-readonly-exit-package-as-release-approval",
      "persist-final-readonly-exit-package-as-phase29-exit",
      "persist-final-readonly-exit-package-as-phase30-entry",
      "persist-final-readonly-exit-package-artifact",
      "persist-final-readonly-exit-package-item",
      "record-final-readonly-exit-package",
      "record-exit-package-from-hardening",
      "approve-release-from-final-readonly-exit-package",
      "exit-phase29-from-final-readonly-exit-package",
      "open-phase30-entry-from-final-readonly-exit-package",
      "clear-blocker-from-final-readonly-exit-package",
      "resolve-redline-from-final-readonly-exit-package",
      "mark-final-exit-package-approved",
      "override-exit-decision-from-final-readonly-package",
      "persist-release-exit-readiness-dossier-as-release-approval",
      "persist-release-exit-readiness-dossier-as-phase29-exit",
      "persist-release-exit-readiness-dossier-as-phase30-entry",
      "persist-release-exit-readiness-dossier-artifact",
      "persist-release-exit-readiness-dossier-item",
      "record-release-exit-readiness-dossier",
      "record-final-exit-package-from-dossier",
      "approve-release-from-release-exit-readiness-dossier",
      "exit-phase29-from-release-exit-readiness-dossier",
      "open-phase30-entry-from-release-exit-readiness-dossier",
      "clear-blocker-from-release-exit-readiness-dossier",
      "resolve-redline-from-release-exit-readiness-dossier",
      "mark-release-exit-dossier-approved",
      "override-final-exit-package-from-dossier",
      "persist-release-exit-governance-freeze-as-release-approval",
      "persist-release-exit-governance-freeze-as-phase29-exit",
      "persist-release-exit-governance-freeze-as-phase30-entry",
      "persist-release-exit-governance-freeze-artifact",
      "persist-release-exit-governance-freeze-item",
      "record-release-exit-governance-freeze",
      "record-release-exit-dossier-from-freeze",
      "approve-release-from-release-exit-governance-freeze",
      "exit-phase29-from-release-exit-governance-freeze",
      "open-phase30-entry-from-release-exit-governance-freeze",
      "clear-blocker-from-release-exit-governance-freeze",
      "resolve-redline-from-release-exit-governance-freeze",
      "unfreeze-release-exit-governance-without-review",
      "mark-release-exit-governance-frozen-approved",
      "override-release-exit-dossier-from-freeze",
      "persist-release-exit-final-signoff-review-as-release-approval",
      "persist-release-exit-final-signoff-review-as-phase29-exit",
      "persist-release-exit-final-signoff-review-as-phase30-entry",
      "persist-release-exit-final-signoff-review-artifact",
      "persist-release-exit-final-signoff-review-item",
      "record-release-exit-final-signoff-review",
      "record-release-exit-governance-freeze-from-signoff-review",
      "approve-release-from-release-exit-final-signoff-review",
      "exit-phase29-from-release-exit-final-signoff-review",
      "open-phase30-entry-from-release-exit-final-signoff-review",
      "clear-blocker-from-release-exit-final-signoff-review",
      "resolve-redline-from-release-exit-final-signoff-review",
      "mark-release-exit-final-signoff-approved",
      "override-release-exit-governance-freeze-from-signoff-review",
      "override-release-exit-dossier-from-signoff-review",
      "persist-release-exit-signoff-evidence-reconciliation-as-release-approval",
      "persist-release-exit-signoff-evidence-reconciliation-as-phase29-exit",
      "persist-release-exit-signoff-evidence-reconciliation-as-phase30-entry",
      "persist-release-exit-signoff-evidence-reconciliation-artifact",
      "persist-release-exit-signoff-evidence-reconciliation-item",
      "record-release-exit-signoff-evidence-reconciliation",
      "record-release-exit-final-signoff-review-from-evidence-reconciliation",
      "approve-release-from-release-exit-signoff-evidence-reconciliation",
      "exit-phase29-from-release-exit-signoff-evidence-reconciliation",
      "open-phase30-entry-from-release-exit-signoff-evidence-reconciliation",
      "clear-blocker-from-release-exit-signoff-evidence-reconciliation",
      "resolve-redline-from-release-exit-signoff-evidence-reconciliation",
      "mark-release-exit-signoff-evidence-reconciled",
      "persist-release-exit-final-archive-index-as-release-approval",
      "persist-release-exit-final-archive-index-as-phase29-exit",
      "persist-release-exit-final-archive-index-as-phase30-entry",
      "persist-release-exit-final-archive-index-artifact",
      "persist-release-exit-final-archive-index-item",
      "record-release-exit-final-archive-index",
      "record-release-exit-signoff-evidence-reconciliation-from-archive-index",
      "approve-release-from-release-exit-final-archive-index",
      "exit-phase29-from-release-exit-final-archive-index",
      "open-phase30-entry-from-release-exit-final-archive-index",
      "clear-blocker-from-release-exit-final-archive-index",
      "resolve-redline-from-release-exit-final-archive-index",
      "mark-release-exit-final-archive-indexed",
      "persist-release-exit-final-archive-integrity-check-as-release-approval",
      "persist-release-exit-final-archive-integrity-check-as-phase29-exit",
      "persist-release-exit-final-archive-integrity-check-as-phase30-entry",
      "persist-release-exit-final-archive-integrity-check-artifact",
      "persist-release-exit-final-archive-integrity-check-item",
      "record-release-exit-final-archive-integrity-check",
      "record-release-exit-final-archive-index-from-integrity-check",
      "approve-release-from-release-exit-final-archive-integrity-check",
      "exit-phase29-from-release-exit-final-archive-integrity-check",
      "open-phase30-entry-from-release-exit-final-archive-integrity-check",
      "clear-blocker-from-release-exit-final-archive-integrity-check",
      "resolve-redline-from-release-exit-final-archive-integrity-check",
      "mark-release-exit-final-archive-integrity-checked",
      "persist-release-exit-final-archive-manifest-as-release-approval",
      "persist-release-exit-final-archive-manifest-as-phase29-exit",
      "persist-release-exit-final-archive-manifest-as-phase30-entry",
      "persist-release-exit-final-archive-manifest-artifact",
      "persist-release-exit-final-archive-manifest-item",
      "record-release-exit-final-archive-manifest",
      "record-release-exit-final-archive-integrity-check-from-manifest",
      "approve-release-from-release-exit-final-archive-manifest",
      "exit-phase29-from-release-exit-final-archive-manifest",
      "open-phase30-entry-from-release-exit-final-archive-manifest",
      "clear-blocker-from-release-exit-final-archive-manifest",
      "resolve-redline-from-release-exit-final-archive-manifest",
      "mark-release-exit-final-archive-manifested",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-phase29-planning-as-release-approval"
    ],
    exportPolicy: {
      exportName: "phase29-release-governance-planning.json",
      includeInOperationsExport: true,
      redaction: "track-id-gate-state-and-guardrail-summary-only"
    },
    nextStageOptions: [
      { id: "phase29-release-exit-final-governance-lock-preview", status: "superseded-by-final-archive-integrity-check-preview", releaseReady: false, runtimeExecution: false },
      { id: "phase30-release-candidate-governance", status: "blocked-until-release-exit-final-signoff-human-review-and-all-blockers-cleared", releaseReady: false, runtimeExecution: false }
    ],
    summary: {
      planningReady: true,
      blockerClearanceWorkflowReady: blockerClearanceWorkflow.summary.workflowPlanningReady,
      releaseApprovalStateModelReady: releaseApprovalStateModel.summary.approvalStateModelReady,
      runtimeEnableGovernanceReady: runtimeEnableGovernance.summary.runtimeEnableGovernanceReady,
      governanceDashboardExportReady: governanceDashboardExport.summary.dashboardExportReady,
      decisionHistoryAuditChainReady: decisionHistoryAuditChain.summary.decisionHistoryAuditChainReady,
      governanceClosureReadinessReady: governanceClosureReadiness.summary.governanceClosureReadinessReady,
      closureHandoffPackageReady: closureHandoffPackage.summary.closureHandoffPackageReady,
      handoffReviewIndexReady: handoffReviewIndex.summary.handoffReviewIndexReady,
      finalClosureReadinessSnapshotReady: finalClosureReadinessSnapshot.summary.finalClosureReadinessSnapshotReady,
      governanceArchiveFreezeReady: governanceArchiveFreeze.summary.governanceArchiveFreezeReady,
      blockerResolutionMapReady: blockerResolutionMap.summary.blockerResolutionMapReady,
      phase30EntryPreflightReady: phase30EntryPreflight.summary.phase30EntryPreflightReady,
      finalReleaseBlockerDossierReady: finalReleaseBlockerDossier.summary.finalReleaseBlockerDossierReady,
      releaseExitCriteriaLedgerReady: releaseExitCriteriaLedger.summary.releaseExitCriteriaLedgerReady,
      releaseExitDecisionPreviewReady: releaseExitDecisionPreview.summary.releaseExitDecisionPreviewReady,
      phase30TransitionRiskRegisterReady: phase30TransitionRiskRegister.summary.phase30TransitionRiskRegisterReady,
      transitionEvidenceGapReviewReady: transitionEvidenceGapReview.summary.transitionEvidenceGapReviewReady,
      transitionReadinessRedlineReady: transitionReadinessRedline.summary.transitionReadinessRedlineReady,
      finalTransitionHandoffPreviewReady: finalTransitionHandoffPreview.summary.finalTransitionHandoffPreviewReady,
      transitionClosurePackagePreviewReady: transitionClosurePackagePreview.summary.transitionClosurePackagePreviewReady,
      releaseCandidateFreezePreviewReady: releaseCandidateFreezePreview.summary.releaseCandidateFreezePreviewReady,
      finalReadinessBoardPreviewReady: finalReadinessBoardPreview.summary.finalReadinessBoardPreviewReady,
      releaseFinalReviewEnvelopePreviewReady: releaseFinalReviewEnvelopePreview.summary.releaseFinalReviewEnvelopePreviewReady,
      exitReadinessAttestationPreviewReady: exitReadinessAttestationPreview.summary.exitReadinessAttestationPreviewReady,
      phase30PlanningBriefPreviewReady: phase30PlanningBriefPreview.summary.phase30PlanningBriefPreviewReady,
      phase30ScopeLockPreviewReady: phase30ScopeLockPreview.summary.phase30ScopeLockPreviewReady,
      phase30ReadinessMapPreviewReady: phase30ReadinessMapPreview.summary.phase30ReadinessMapPreviewReady,
      phase30ReadinessGapBriefPreviewReady: phase30ReadinessGapBriefPreview.summary.phase30ReadinessGapBriefPreviewReady,
      phase30GapResolutionSequencePreviewReady: phase30GapResolutionSequencePreview.summary.phase30GapResolutionSequencePreviewReady,
      phase30EntryEvidencePackPreviewReady: phase30EntryEvidencePackPreview.summary.phase30EntryEvidencePackPreviewReady,
      phase30EntryEvidenceIndexPreviewReady: phase30EntryEvidenceIndexPreview.summary.phase30EntryEvidenceIndexPreviewReady,
      phase30EntryEvidenceReviewQueuePreviewReady: phase30EntryEvidenceReviewQueuePreview.summary.phase30EntryEvidenceReviewQueuePreviewReady,
      phase30EntryEvidenceReviewDispositionPreviewReady: phase30EntryEvidenceReviewDispositionPreview.summary.phase30EntryEvidenceReviewDispositionPreviewReady,
      phase30EntryEvidenceDispositionLedgerPreviewReady: phase30EntryEvidenceDispositionLedgerPreview.summary.phase30EntryEvidenceDispositionLedgerPreviewReady,
      phase30EntryEvidenceFinalLedgerExportPreviewReady: phase30EntryEvidenceFinalLedgerExportPreview.summary.phase30EntryEvidenceFinalLedgerExportPreviewReady,
      phase30EntryFinalExportReconciliationPreviewReady: phase30EntryFinalExportReconciliationPreview.summary.phase30EntryFinalExportReconciliationPreviewReady,
      phase30EntryReconciliationSignoffPreviewReady: phase30EntryReconciliationSignoffPreview.summary.phase30EntryReconciliationSignoffPreviewReady,
      phase30EntrySignoffAuditPreviewReady: phase30EntrySignoffAuditPreview.summary.phase30EntrySignoffAuditPreviewReady,
      phase30EntryAuditClosurePreviewReady: phase30EntryAuditClosurePreview.summary.phase30EntryAuditClosurePreviewReady,
      phase29AuditClosureReadinessHardeningReady: phase29AuditClosureReadinessHardening.summary.phase29AuditClosureReadinessHardeningReady,
      phase29FinalReadonlyExitPackagePreviewReady: phase29FinalReadonlyExitPackagePreview.summary.phase29FinalReadonlyExitPackagePreviewReady,
      phase29ReleaseExitReadinessDossierPreviewReady: phase29ReleaseExitReadinessDossierPreview.summary.phase29ReleaseExitReadinessDossierPreviewReady,
      phase29ReleaseExitGovernanceFreezePreviewReady: phase29ReleaseExitGovernanceFreezePreview.summary.phase29ReleaseExitGovernanceFreezePreviewReady,
      phase29ReleaseExitFinalSignoffReviewPreviewReady: phase29ReleaseExitFinalSignoffReviewPreview.summary.phase29ReleaseExitFinalSignoffReviewPreviewReady,
      phase29ReleaseExitSignoffEvidenceReconciliationPreviewReady: phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary.phase29ReleaseExitSignoffEvidenceReconciliationPreviewReady,
      phase29ReleaseExitFinalArchiveIndexPreviewReady: phase29ReleaseExitFinalArchiveIndexPreview.summary.phase29ReleaseExitFinalArchiveIndexPreviewReady,
      phase29ReleaseExitFinalArchiveIntegrityCheckPreviewReady: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary.phase29ReleaseExitFinalArchiveIntegrityCheckPreviewReady,
      phase29ReleaseExitFinalArchiveManifestPreviewReady: phase29ReleaseExitFinalArchiveManifestPreview.summary.phase29ReleaseExitFinalArchiveManifestPreviewReady,
      governanceTracks: governanceTracks.length,
      dashboardSections: governanceDashboardExport.summary.dashboardSections,
      includedExports: governanceDashboardExport.summary.includedExports,
      decisionEvents: decisionHistoryAuditChain.summary.decisionEvents,
      auditChainLinks: decisionHistoryAuditChain.summary.chainLinks,
      closureCriteria: governanceClosureReadiness.summary.closureCriteria,
      blockedClosureItems: governanceClosureReadiness.summary.blockedClosureItems,
      handoffSections: closureHandoffPackage.summary.handoffSections,
      handoffArtifacts: closureHandoffPackage.summary.handoffArtifacts,
      reviewIndexEntries: handoffReviewIndex.summary.reviewIndexEntries,
      blockedReviewItems: handoffReviewIndex.summary.blockedReviewItems,
      finalSnapshotModels: finalClosureReadinessSnapshot.summary.readinessModels,
      finalSnapshotBlockedItems: finalClosureReadinessSnapshot.summary.blockedSnapshotItems,
      archivedExports: governanceArchiveFreeze.summary.archivedExports,
      frozenGuards: governanceArchiveFreeze.summary.frozenGuards,
      resolutionItems: blockerResolutionMap.summary.resolutionItems,
      resolutionLanes: blockerResolutionMap.summary.resolutionLanes,
      preflightItems: phase30EntryPreflight.summary.preflightItems,
      blockedEntryGates: phase30EntryPreflight.summary.blockedEntryGates,
      finalDossierUnresolvedBlockers: finalReleaseBlockerDossier.summary.unresolvedBlockers,
      finalDossierBlockedReleaseGates: finalReleaseBlockerDossier.summary.blockedReleaseGates,
      exitCriteria: releaseExitCriteriaLedger.summary.exitCriteria,
      unmetExitCriteria: releaseExitCriteriaLedger.summary.unmetExitCriteria,
      blockedExitGates: releaseExitCriteriaLedger.summary.blockedExitGates,
      exitDecisionPreviewItems: releaseExitDecisionPreview.summary.decisionPreviewItems,
      blockedExitDecisionItems: releaseExitDecisionPreview.summary.blockedPreviewItems,
      phase29ExitDecision: releaseExitDecisionPreview.summary.phase29ExitDecision,
      transitionRiskItems: phase30TransitionRiskRegister.summary.riskItems,
      blockedTransitionRisks: phase30TransitionRiskRegister.summary.blockedTransitionRisks,
      transitionMitigationLanes: phase30TransitionRiskRegister.summary.mitigationLanes,
      transitionEvidenceGaps: transitionEvidenceGapReview.summary.gapItems,
      blockingEvidenceGaps: transitionEvidenceGapReview.summary.blockingEvidenceGaps,
      evidenceReviewLanes: transitionEvidenceGapReview.summary.evidenceReviewLanes,
      transitionRedlines: transitionReadinessRedline.summary.redlineItems,
      activeTransitionRedlines: transitionReadinessRedline.summary.activeRedlines,
      redlineReviewItems: transitionReadinessRedline.summary.redlineReviewItems,
      handoffPreviewSections: finalTransitionHandoffPreview.summary.handoffPreviewSections,
      blockedHandoffPreviewSections: finalTransitionHandoffPreview.summary.blockedHandoffPreviewSections,
      handoffPreviewChecks: finalTransitionHandoffPreview.summary.handoffPreviewChecks,
      closurePreviewItems: transitionClosurePackagePreview.summary.closurePreviewItems,
      blockedClosurePreviewItems: transitionClosurePackagePreview.summary.blockedClosurePreviewItems,
      closurePreviewChecks: transitionClosurePackagePreview.summary.closurePreviewChecks,
      freezePreviewItems: releaseCandidateFreezePreview.summary.freezePreviewItems,
      blockedFreezePreviewItems: releaseCandidateFreezePreview.summary.blockedFreezePreviewItems,
      freezePreviewChecks: releaseCandidateFreezePreview.summary.freezePreviewChecks,
      readinessBoardItems: finalReadinessBoardPreview.summary.readinessBoardItems,
      blockedReadinessBoardItems: finalReadinessBoardPreview.summary.blockedReadinessBoardItems,
      readinessBoardChecks: finalReadinessBoardPreview.summary.readinessBoardChecks,
      finalReviewEnvelopeSections: releaseFinalReviewEnvelopePreview.summary.envelopeSections,
      blockedFinalReviewEnvelopeSections: releaseFinalReviewEnvelopePreview.summary.blockedEnvelopeSections,
      finalReviewEnvelopeChecks: releaseFinalReviewEnvelopePreview.summary.envelopeChecks,
      exitReadinessAttestationItems: exitReadinessAttestationPreview.summary.attestationItems,
      blockedExitReadinessAttestationItems: exitReadinessAttestationPreview.summary.blockedAttestationItems,
      exitReadinessAttestationChecks: exitReadinessAttestationPreview.summary.attestationChecks,
      phase30PlanningBriefSections: phase30PlanningBriefPreview.summary.briefSections,
      blockedPhase30PlanningBriefSections: phase30PlanningBriefPreview.summary.blockedBriefSections,
      phase30PlanningBriefChecks: phase30PlanningBriefPreview.summary.briefChecks,
      phase30ScopeLockItems: phase30ScopeLockPreview.summary.scopeLockItems,
      blockedPhase30ScopeLockItems: phase30ScopeLockPreview.summary.blockedScopeLockItems,
      phase30ScopeLockChecks: phase30ScopeLockPreview.summary.scopeLockChecks,
      phase30ReadinessMapItems: phase30ReadinessMapPreview.summary.readinessMapItems,
      blockedPhase30ReadinessMapItems: phase30ReadinessMapPreview.summary.blockedReadinessMapItems,
      phase30ReadinessMapChecks: phase30ReadinessMapPreview.summary.readinessMapChecks,
      phase30ReadinessGapBriefItems: phase30ReadinessGapBriefPreview.summary.gapBriefItems,
      blockedPhase30ReadinessGapBriefItems: phase30ReadinessGapBriefPreview.summary.blockedGapBriefItems,
      phase30ReadinessGapBriefChecks: phase30ReadinessGapBriefPreview.summary.gapBriefChecks,
      phase30GapResolutionSequenceItems: phase30GapResolutionSequencePreview.summary.resolutionSequenceItems,
      blockedPhase30GapResolutionSequenceItems: phase30GapResolutionSequencePreview.summary.blockedResolutionSequenceItems,
      phase30GapResolutionSequenceChecks: phase30GapResolutionSequencePreview.summary.sequenceChecks,
      phase30EntryEvidencePackItems: phase30EntryEvidencePackPreview.summary.evidencePackItems,
      blockedPhase30EntryEvidencePackItems: phase30EntryEvidencePackPreview.summary.blockedEvidencePackItems,
      phase30EntryEvidencePackChecks: phase30EntryEvidencePackPreview.summary.evidencePackChecks,
      phase30EntryEvidenceIndexItems: phase30EntryEvidenceIndexPreview.summary.evidenceIndexItems,
      blockedPhase30EntryEvidenceIndexItems: phase30EntryEvidenceIndexPreview.summary.blockedEvidenceIndexItems,
      phase30EntryEvidenceIndexChecks: phase30EntryEvidenceIndexPreview.summary.evidenceIndexChecks,
      phase30EntryEvidenceReviewQueueItems: phase30EntryEvidenceReviewQueuePreview.summary.evidenceReviewQueueItems,
      blockedPhase30EntryEvidenceReviewQueueItems: phase30EntryEvidenceReviewQueuePreview.summary.blockedEvidenceReviewQueueItems,
      phase30EntryEvidenceReviewQueueChecks: phase30EntryEvidenceReviewQueuePreview.summary.evidenceReviewQueueChecks,
      phase30EntryEvidenceReviewDispositionItems: phase30EntryEvidenceReviewDispositionPreview.summary.dispositionPreviewItems,
      blockedPhase30EntryEvidenceReviewDispositionItems: phase30EntryEvidenceReviewDispositionPreview.summary.blockedDispositionPreviewItems,
      phase30EntryEvidenceReviewDispositionChecks: phase30EntryEvidenceReviewDispositionPreview.summary.dispositionPreviewChecks,
      phase30EntryEvidenceDispositionLedgerEntries: phase30EntryEvidenceDispositionLedgerPreview.summary.dispositionLedgerEntries,
      blockedPhase30EntryEvidenceDispositionLedgerEntries: phase30EntryEvidenceDispositionLedgerPreview.summary.blockedDispositionLedgerEntries,
      phase30EntryEvidenceDispositionLedgerChecks: phase30EntryEvidenceDispositionLedgerPreview.summary.dispositionLedgerChecks,
      phase30EntryEvidenceFinalLedgerExportItems: phase30EntryEvidenceFinalLedgerExportPreview.summary.finalLedgerExportItems,
      blockedPhase30EntryEvidenceFinalLedgerExportItems: phase30EntryEvidenceFinalLedgerExportPreview.summary.blockedFinalLedgerExportItems,
      phase30EntryEvidenceFinalLedgerExportChecks: phase30EntryEvidenceFinalLedgerExportPreview.summary.finalLedgerExportChecks,
      phase30EntryFinalExportReconciliationItems: phase30EntryFinalExportReconciliationPreview.summary.reconciliationItems,
      blockedPhase30EntryFinalExportReconciliationItems: phase30EntryFinalExportReconciliationPreview.summary.blockedReconciliationItems,
      phase30EntryFinalExportReconciliationChecks: phase30EntryFinalExportReconciliationPreview.summary.reconciliationChecks,
      phase30EntryReconciliationSignoffItems: phase30EntryReconciliationSignoffPreview.summary.signoffItems,
      blockedPhase30EntryReconciliationSignoffItems: phase30EntryReconciliationSignoffPreview.summary.blockedSignoffItems,
      phase30EntryReconciliationSignoffChecks: phase30EntryReconciliationSignoffPreview.summary.signoffChecks,
      phase30EntrySignoffAuditItems: phase30EntrySignoffAuditPreview.summary.auditItems,
      blockedPhase30EntrySignoffAuditItems: phase30EntrySignoffAuditPreview.summary.blockedAuditItems,
      phase30EntrySignoffAuditChecks: phase30EntrySignoffAuditPreview.summary.auditChecks,
      phase30EntryAuditClosureItems: phase30EntryAuditClosurePreview.summary.closureItems,
      blockedPhase30EntryAuditClosureItems: phase30EntryAuditClosurePreview.summary.blockedClosureItems,
      phase30EntryAuditClosureChecks: phase30EntryAuditClosurePreview.summary.closureChecks,
      phase29AuditClosureHardeningItems: phase29AuditClosureReadinessHardening.summary.hardeningItems,
      blockedPhase29AuditClosureHardeningItems: phase29AuditClosureReadinessHardening.summary.blockedHardeningItems,
      phase29AuditClosureHardeningChecks: phase29AuditClosureReadinessHardening.summary.hardeningChecks,
      phase29FinalExitPackageItems: phase29FinalReadonlyExitPackagePreview.summary.exitPackageItems,
      blockedPhase29FinalExitPackageItems: phase29FinalReadonlyExitPackagePreview.summary.blockedExitPackageItems,
      phase29FinalExitPackageChecks: phase29FinalReadonlyExitPackagePreview.summary.exitPackageChecks,
      phase29ReleaseExitDossierItems: phase29ReleaseExitReadinessDossierPreview.summary.dossierItems,
      blockedPhase29ReleaseExitDossierItems: phase29ReleaseExitReadinessDossierPreview.summary.blockedDossierItems,
      phase29ReleaseExitDossierChecks: phase29ReleaseExitReadinessDossierPreview.summary.dossierChecks,
      phase29ReleaseExitFreezeItems: phase29ReleaseExitGovernanceFreezePreview.summary.freezeItems,
      blockedPhase29ReleaseExitFreezeItems: phase29ReleaseExitGovernanceFreezePreview.summary.blockedFreezeItems,
      phase29ReleaseExitFreezeChecks: phase29ReleaseExitGovernanceFreezePreview.summary.freezeChecks,
      phase29ReleaseExitSignoffReviewItems: phase29ReleaseExitFinalSignoffReviewPreview.summary.signoffReviewItems,
      blockedPhase29ReleaseExitSignoffReviewItems: phase29ReleaseExitFinalSignoffReviewPreview.summary.blockedSignoffReviewItems,
      phase29ReleaseExitSignoffReviewChecks: phase29ReleaseExitFinalSignoffReviewPreview.summary.signoffReviewChecks,
      phase29ReleaseExitSignoffEvidenceReconciliationItems: phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary.evidenceReconciliationItems,
      blockedPhase29ReleaseExitSignoffEvidenceReconciliationItems: phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary.blockedEvidenceReconciliationItems,
      phase29ReleaseExitSignoffEvidenceReconciliationChecks: phase29ReleaseExitSignoffEvidenceReconciliationPreview.summary.evidenceReconciliationChecks,
      phase29ReleaseExitFinalArchiveIndexItems: phase29ReleaseExitFinalArchiveIndexPreview.summary.archiveIndexItems,
      blockedPhase29ReleaseExitFinalArchiveIndexItems: phase29ReleaseExitFinalArchiveIndexPreview.summary.blockedArchiveIndexItems,
      phase29ReleaseExitFinalArchiveIndexChecks: phase29ReleaseExitFinalArchiveIndexPreview.summary.archiveIndexChecks,
      phase29ReleaseExitFinalArchiveIntegrityCheckItems: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary.integrityCheckItems,
      blockedPhase29ReleaseExitFinalArchiveIntegrityCheckItems: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary.blockedIntegrityCheckItems,
      phase29ReleaseExitFinalArchiveIntegrityChecks: phase29ReleaseExitFinalArchiveIntegrityCheckPreview.summary.integrityChecks,

      phase29ReleaseExitFinalArchiveManifestItems: phase29ReleaseExitFinalArchiveManifestPreview.summary.manifestItems,

      blockedPhase29ReleaseExitFinalArchiveManifestItems: phase29ReleaseExitFinalArchiveManifestPreview.summary.blockedManifestItems,

      phase29ReleaseExitFinalArchiveManifestChecks: phase29ReleaseExitFinalArchiveManifestPreview.summary.manifestChecks,

      phase29ExitReady: false,
      phase30Blocked: governanceClosureReadiness.summary.phase30Blocked,
      phase30EntryReady: false,
      separatedGates: gateSeparationModel.length,
      carriedForwardBlockers: carriedForwardBlockers.length,
      pendingWorkflowItems: blockerClearanceWorkflow.summary.pendingWorkflowItems,
      approvalStates: releaseApprovalStateModel.summary.approvalStates,
      approvedReleaseStates: 0,
      runtimeGates: runtimeEnableGovernance.summary.runtimeGates,
      runtimeEnableApprovals: 0,
      clearedBlockers: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29RuntimeEnableGovernance(releaseApprovalStateModel = {}) {
  const runtimeGates = [
    {
      id: "runtime-validation-evidence",
      label: "Runtime validation evidence",
      source: "phase26.runtime-validation-report.v1",
      state: "planning-only",
      requiredEvidence: ["runtime-validation-report-reviewed", "runtime-result-quarantine-reviewed"],
      blockedActions: ["accept-runtime-validation-as-enable-approval", "enable-runtime-execution"]
    },
    {
      id: "permission-policy-review",
      label: "Permission policy review",
      source: "phase21.permission.runtime-enforcement.v1",
      state: "planning-only",
      requiredEvidence: ["host-api-boundary-reviewed", "permission-default-deny-confirmed"],
      blockedActions: ["grant-runtime-permission-from-phase29", "execute-third-party-plugin"]
    },
    {
      id: "kill-switch-and-quarantine",
      label: "Kill switch and quarantine",
      source: "phase25.runtime-sandbox-ui-surface.v1",
      state: "planning-only",
      requiredEvidence: ["kill-switch-reviewed", "quarantine-release-block-reviewed"],
      blockedActions: ["disable-kill-switch", "release-quarantined-runtime-result"]
    },
    {
      id: "third-party-execution-boundary",
      label: "Third-party execution boundary",
      source: "phase20.plugin.release-checklist-gate.v1",
      state: "planning-only",
      requiredEvidence: ["plugin-release-gate-block-reviewed", "third-party-runtime-remains-disabled"],
      blockedActions: ["enable-third-party-execution", "install-plugin-as-runtime-enabled"]
    }
  ];
  return {
    schemaVersion: "phase29.runtime-enable-governance-planning.v1",
    version: "1.9.3",
    mode: "readonly-runtime-enable-governance-planning-no-runtime-enable",
    sourceReleaseApprovalStateModel: releaseApprovalStateModel.schemaVersion,
    runtimeGates,
    enablementChecks: [
      { id: "release-approval-state-linked", result: releaseApprovalStateModel.schemaVersion === "phase29.release-approval-state-model.v1" ? "pass" : "block", evidence: releaseApprovalStateModel.schemaVersion },
      { id: "release-not-approved", result: releaseApprovalStateModel.summary?.approvedReleaseStates === 0 ? "pass" : "block", evidence: `approvedReleaseStates=${releaseApprovalStateModel.summary?.approvedReleaseStates || 0}` },
      { id: "runtime-gates-planning-only", result: runtimeGates.every((item) => item.state === "planning-only") ? "pass" : "block", evidence: `${runtimeGates.length} runtime gate` },
      { id: "runtime-enable-not-applied", result: "pass", evidence: "runtimeExecution=false" },
      { id: "third-party-execution-not-applied", result: "pass", evidence: "thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "enable-runtime-from-phase29-governance",
      "persist-runtime-governance-as-enable-approval",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "execute-third-party-plugin",
      "grant-runtime-permission-from-phase29",
      "disable-runtime-kill-switch"
    ],
    exportPolicy: {
      exportName: "phase29-runtime-enable-governance-planning.json",
      includeInOperationsExport: true,
      redaction: "runtime-gate-id-state-required-evidence-and-blocked-action-summary-only"
    },
    summary: {
      runtimeEnableGovernanceReady: true,
      runtimeGates: runtimeGates.length,
      planningOnlyGates: runtimeGates.filter((item) => item.state === "planning-only").length,
      runtimeEnableApprovals: 0,
      runtimeExecution: false,
      thirdPartyExecution: false,
      releaseReady: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29ReleaseApprovalStateModel(blockerClearanceWorkflow = {}) {
  const workflowItems = blockerClearanceWorkflow.workflowItems || [];
  const approvalStates = [
    {
      id: "draft",
      label: "Draft",
      state: "current",
      releaseReady: false,
      allowedActions: ["record-approval-planning-note", "export-release-approval-state-model"],
      blockedActions: ["approve-release-from-draft", "mark-releaseReady-true"]
    },
    {
      id: "clearance-blocked",
      label: "Clearance blocked",
      state: workflowItems.length > 0 ? "current" : "inactive",
      releaseReady: false,
      allowedActions: ["link-clearance-workflow-evidence", "export-release-approval-state-model"],
      blockedActions: ["approve-release-before-clearance", "persist-clearance-as-release-approval"]
    },
    {
      id: "approval-ready-preview",
      label: "Approval ready preview",
      state: "blocked-until-clearance",
      releaseReady: false,
      allowedActions: ["preview-required-release-approval-fields"],
      blockedActions: ["apply-preview-as-release-approval", "mark-releaseReady-true"]
    },
    {
      id: "release-hold",
      label: "Release hold",
      state: "current",
      releaseReady: false,
      allowedActions: ["record-release-hold-note", "export-release-hold-summary"],
      blockedActions: ["resolve-hold-without-human-signoff", "enable-runtime-from-hold"]
    }
  ];
  const transitionRules = [
    { from: "draft", to: "clearance-blocked", condition: "carried-blockers-present", allowed: true, mutatesReleaseReady: false },
    { from: "clearance-blocked", to: "approval-ready-preview", condition: "all-clearance-workflow-items-approved", allowed: false, mutatesReleaseReady: false },
    { from: "approval-ready-preview", to: "release-approved", condition: "human-release-owner-signoff", allowed: false, mutatesReleaseReady: false },
    { from: "release-hold", to: "runtime-approval", condition: "separate-runtime-governance-complete", allowed: false, mutatesReleaseReady: false }
  ];
  return {
    schemaVersion: "phase29.release-approval-state-model.v1",
    version: "1.9.2",
    mode: "readonly-release-approval-state-model-no-approval",
    sourceBlockerClearanceWorkflow: blockerClearanceWorkflow.schemaVersion,
    approvalStates,
    transitionRules,
    approvalChecks: [
      { id: "blocker-workflow-linked", result: blockerClearanceWorkflow.schemaVersion === "phase29.blocker-clearance-workflow-planning.v1" ? "pass" : "block", evidence: blockerClearanceWorkflow.schemaVersion },
      { id: "open-workflow-items-block-approval", result: workflowItems.length > 0 ? "pass" : "block", evidence: `${workflowItems.length} workflow item remains planning-only` },
      { id: "state-model-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "approval-preview-is-not-release-approval", result: "pass", evidence: "approval-ready-preview remains blocked" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "approve-release-from-state-model",
      "apply-preview-as-release-approval",
      "persist-approval-state-as-release-ready",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution"
    ],
    exportPolicy: {
      exportName: "phase29-release-approval-state-model.json",
      includeInOperationsExport: true,
      redaction: "state-id-transition-rule-and-blocked-action-summary-only"
    },
    summary: {
      approvalStateModelReady: true,
      approvalStates: approvalStates.length,
      transitionRules: transitionRules.length,
      blockedTransitions: transitionRules.filter((item) => item.allowed === false).length,
      approvedReleaseStates: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase29BlockerClearanceWorkflow(phase28 = {}, closurePackage = {}, carriedForwardBlockers = []) {
  const workflowItems = carriedForwardBlockers.map((item, index) => ({
    workflowItemId: `phase29-blocker-clearance-workflow-${index + 1}`,
    reviewItemId: item.reviewItemId,
    blockerId: item.blockerId,
    inventoryId: item.inventoryId,
    source: item.source,
    sourceReviewState: item.reviewState,
    sourceDisposition: item.blockerDisposition,
    workflowState: "pending-clearance-planning",
    ownerRole: index % 2 === 0 ? "release-governance-reviewer" : "evidence-owner",
    evidenceRequirements: [
      "human-owned-clearance-evidence",
      "reviewer-disposition-recorded",
      "clearance-criteria-satisfaction-reviewed",
      "release-approval-kept-separate"
    ],
    gateChecks: [
      "evidence-intake-linked",
      "reviewer-disposition-linked",
      "criteria-checklist-linked",
      "runtime-and-release-guardrails-linked"
    ],
    allowedActions: ["record-clearance-planning-note", "export-blocker-clearance-workflow"],
    blockedActions: [
      "clear-blocker-from-phase29-workflow",
      "persist-clearance-as-approved",
      "mark-releaseReady-true",
      "enable-runtime-execution"
    ],
    auditState: "clearance-workflow-planned-no-mutation"
  }));
  return {
    schemaVersion: "phase29.blocker-clearance-workflow-planning.v1",
    version: "1.9.1",
    mode: "readonly-blocker-clearance-workflow-planning-no-clearance",
    sourcePhase28ClosurePackage: closurePackage.schemaVersion,
    sourceClearanceReviewEntry: phase28.schemaVersion,
    workflowItems,
    workflowChecks: [
      { id: "phase28-closure-linked", result: closurePackage.schemaVersion === "phase28.closure-package.v1" ? "pass" : "block", evidence: closurePackage.schemaVersion },
      { id: "all-carried-blockers-represented", result: workflowItems.length === carriedForwardBlockers.length ? "pass" : "block", evidence: `${workflowItems.length} workflow item` },
      { id: "all-items-pending-planning", result: workflowItems.every((item) => item.workflowState === "pending-clearance-planning") ? "pass" : "block", evidence: "no workflow item clears a blocker" },
      { id: "workflow-does-not-clear-blockers", result: "pass", evidence: "clearedBlockers=0" },
      { id: "workflow-does-not-approve-release", result: "pass", evidence: "releaseReady=false" },
      { id: "runtime-remains-disabled", result: "pass", evidence: "runtimeExecution=false and thirdPartyExecution=false" }
    ],
    blockedMutations: [
      "clear-blocker-from-phase29-workflow",
      "apply-workflow-as-clearance",
      "persist-clearance-as-approved",
      "mark-releaseReady-true",
      "enable-runtime-execution",
      "enable-third-party-execution",
      "persist-phase29-workflow-as-release-approval"
    ],
    exportPolicy: {
      exportName: "phase29-blocker-clearance-workflow-planning.json",
      includeInOperationsExport: true,
      redaction: "workflow-item-blocker-id-state-and-required-evidence-only"
    },
    summary: {
      workflowPlanningReady: true,
      workflowItems: workflowItems.length,
      pendingWorkflowItems: workflowItems.filter((item) => item.workflowState === "pending-clearance-planning").length,
      clearedBlockers: 0,
      approvedClearanceItems: 0,
      releaseReady: false,
      runtimeExecution: false,
      thirdPartyExecution: false,
      persistedMutations: 0
    }
  };
}

function buildLocalPhase15AssetPlan() {
  return [
    { id: "saved-exhibitions", label: "可保存专题展", status: "ready-to-design", detail: "基于现有主题洞察、展品引用和导览词设计专题展字段。" },
    { id: "report-drafts", label: "回忆报告草稿", status: "ready-to-design", detail: "复用报告章节和引用来源，增加草稿、复核、发布、归档状态。" },
    { id: "citation-lock", label: "引用来源锁定", status: "ready", detail: "现有报告和讲解 citations 已保留展品引用，可作为资产证据来源。" },
    { id: "asset-export", label: "专题资产导出", status: "ready-to-design", detail: "在现有 JSON 导出包中增加 savedExhibitions 和 reportDrafts。" },
    { id: "sample-route", label: "样本路线", status: memories.length > 0 ? "ready" : "optional", detail: "可从当前展品生成第一条专题展；无样本时仍可先完成模型和接口。" }
  ];
}

function buildLocalPhase15Readiness() {
  const withSignals = memories.filter((memory) => (
    memory.date || memory.exhibitText || memory.tags?.length || memory.people?.length || memory.location || memory.emotions?.length
  )).length;
  const checks = [
    { id: "api-guard", label: "API 保护网", status: "ready", detail: "Phase 14 readiness 和 API smoke 已覆盖核心接口。" },
    { id: "route-boundary", label: "路由边界", status: "ready", detail: "health 与 operations routes 已从 server.js 抽离。" },
    { id: "insight-source", label: "洞察来源", status: "ready", detail: "时间线、主题候选、报告章节和引用来源已存在。" },
    { id: "workflow-source", label: "工作流来源", status: "ready", detail: "工作流蓝图已声明报告工作流与人工复核点。" },
    { id: "export-boundary", label: "导出边界", status: "ready", detail: "现有导出包可扩展专题资产字段。" },
    { id: "sample-quality", label: "样本质量", status: memories.length > 0 && withSignals > 0 ? "ready" : "optional", detail: "有样本可直接生成首个专题展；无样本时仍可先做资产模型。" }
  ];
  const readyCount = checks.filter((item) => item.status === "ready").length;
  return {
    targetPhase: 15,
    targetName: "专题资产、报告编辑和可保存展览",
    ready: readyCount >= 5,
    score: Math.round((readyCount / checks.length) * 100),
    checks,
    assetPlan: buildLocalPhase15AssetPlan(),
    recommendedFirstBuild: [
      "新增 savedExhibitions/reportDrafts 数据模型",
      "从主题候选一键生成专题展草稿",
      "让导出包包含专题资产与报告草稿",
      "前端增加可保存专题展编辑面板"
    ],
    recommendation: readyCount >= 5
      ? "可以进入阶段 15。建议先做可保存专题展和报告草稿模型，再做编辑与导出。"
      : "继续补齐专题资产模型、导出边界和报告引用来源后再进入阶段 15。"
  };
}

function buildLocalReleaseHistory() {
  return [
    { version: "1.9.48", label: "phase29-release-exit-final-archive-manifest-preview", phase: 29, date: "2026-07-02", summary: "Phase 29 forty-ninth edition adds a read-only release exit final archive manifest preview over the final archive integrity check while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.47", label: "phase29-release-exit-final-archive-integrity-check-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-eighth edition adds a read-only release exit final archive integrity check preview over the final archive index while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.46", label: "phase29-release-exit-final-archive-index-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-seventh edition adds a read-only release exit final archive index preview over signoff evidence reconciliation and the final release-exit materials while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.45", label: "phase29-release-exit-signoff-evidence-reconciliation-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-sixth edition adds a read-only release exit signoff evidence reconciliation preview over the final signoff review while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.44", label: "phase29-release-exit-final-signoff-review-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-fifth edition adds a read-only release exit final signoff review preview over the governance freeze and release exit dossier while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.43", label: "phase29-release-exit-governance-freeze-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-fourth edition adds a read-only release exit governance freeze preview over the release exit readiness dossier while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.42", label: "phase29-release-exit-readiness-dossier-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-third edition adds a read-only release exit readiness dossier preview over the final readonly exit package while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.41", label: "phase29-final-readonly-exit-package-preview", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-second edition adds a read-only final exit package preview over audit closure hardening while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.40", label: "phase29-audit-closure-readiness-hardening", phase: 29, date: "2026-07-01", summary: "Phase 29 forty-first edition adds a read-only audit closure readiness hardening layer over the Phase 30 entry audit closure preview while blockers, redlines, release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.39", label: "phase29-phase30-entry-audit-closure-preview", phase: 29, date: "2026-06-30", summary: "Phase 29 fortieth edition adds a read-only Phase 30 entry audit closure preview over signoff audit, reconciliation signoff, final export reconciliation, release review, exit attestation, blocker dossier, release exit decision, and runtime guardrails while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.38", label: "phase29-phase30-entry-signoff-audit-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-ninth edition adds a read-only Phase 30 entry signoff audit preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.37", label: "phase29-phase30-entry-reconciliation-signoff-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-eighth edition adds a read-only Phase 30 entry reconciliation signoff preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.36", label: "phase29-phase30-entry-final-export-reconciliation-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-seventh edition adds a read-only Phase 30 entry final export reconciliation preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.35", label: "phase29-phase30-entry-evidence-final-ledger-export-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-sixth edition adds a read-only Phase 30 entry evidence final ledger export preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.34", label: "phase29-phase30-entry-evidence-disposition-ledger-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-fifth edition adds a read-only Phase 30 entry evidence disposition ledger preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.33", label: "phase29-phase30-entry-evidence-review-disposition-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-fourth edition adds a read-only Phase 30 entry evidence review disposition preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.32", label: "phase29-phase30-entry-evidence-review-queue-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-third edition adds a read-only Phase 30 entry evidence review queue preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.31", label: "phase29-phase30-entry-evidence-index-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-second edition adds a read-only Phase 30 entry evidence index preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.30", label: "phase29-phase30-entry-evidence-pack-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirty-first edition adds a read-only Phase 30 entry evidence pack preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.29", label: "phase29-phase30-gap-resolution-sequence-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 thirtieth edition adds a read-only Phase 30 gap resolution sequence preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.28", label: "phase29-phase30-readiness-gap-brief-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-ninth edition adds a read-only Phase 30 readiness gap brief preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.27", label: "phase29-phase30-readiness-map-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-eighth edition adds a read-only Phase 30 readiness map preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.26", label: "phase29-phase30-scope-lock-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-seventh edition adds a read-only Phase 30 scope lock preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.25", label: "phase29-phase30-planning-brief-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-sixth edition adds a read-only Phase 30 planning brief preview while release approval, Phase 29 exit, Phase 30 entry, runtime, and third-party execution remain blocked." },
    { version: "1.9.24", label: "phase29-exit-readiness-attestation-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-fifth edition adds a read-only exit readiness attestation preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.23", label: "phase29-release-final-review-envelope-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-fourth edition adds a read-only release final review envelope preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.22", label: "phase29-final-readiness-board-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-third edition adds a read-only final readiness board preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.21", label: "phase29-release-candidate-freeze-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-second edition adds a read-only release candidate freeze preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.20", label: "phase29-transition-closure-package-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twenty-first edition adds a read-only transition closure package preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.19", label: "phase29-final-transition-handoff-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 twentieth edition adds a read-only final transition handoff preview while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.18", label: "phase29-transition-readiness-redline", phase: 29, date: "2026-06-29", summary: "Phase 29 nineteenth edition adds a read-only transition readiness redline while blocker clearance, release approval, runtime, Phase 29 exit, and Phase 30 entry remain blocked." },
    { version: "1.9.17", label: "phase29-transition-evidence-gap-review", phase: 29, date: "2026-06-29", summary: "Phase 29 eighteenth edition adds a read-only transition evidence gap review while blocker clearance, release approval, runtime, and Phase 30 entry remain blocked." },
    { version: "1.9.16", label: "phase29-phase30-transition-risk-register", phase: 29, date: "2026-06-29", summary: "Phase 29 seventeenth edition adds a read-only Phase 30 transition risk register while Phase 29 exit, release approval, runtime, and Phase 30 entry remain blocked." },
    { version: "1.9.15", label: "phase29-release-exit-decision-preview", phase: 29, date: "2026-06-29", summary: "Phase 29 sixteenth edition adds a read-only release exit decision preview that keeps Phase 29 exit, release approval, runtime, and Phase 30 entry blocked." },
    { version: "1.9.14", label: "phase29-release-exit-criteria-ledger", phase: 29, date: "2026-06-29", summary: "Phase 29 fifteenth edition adds a read-only release exit criteria ledger over blocker, release, runtime, archive, and Phase 30 entry gates while Phase 30 entry remains blocked." },
    { version: "1.9.13", label: "phase29-final-release-blocker-dossier", phase: 29, date: "2026-06-29", summary: "Phase 29 fourteenth edition adds a read-only final release blocker dossier across blockers, evidence artifacts, and release gates while Phase 30 entry remains blocked." },
    { version: "1.9.12", label: "phase29-phase30-entry-preflight", phase: 29, date: "2026-06-29", summary: "Phase 29 thirteenth edition adds a read-only Phase 30 entry preflight over blockers, release approval, and runtime gates while Phase 30 entry remains blocked." },
    { version: "1.9.11", label: "phase29-blocker-resolution-map", phase: 29, date: "2026-06-29", summary: "Phase 29 twelfth edition adds a read-only blocker resolution map with evidence, ownership, and Phase 30 preconditions while blocker clearance, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.10", label: "phase29-governance-archive-freeze", phase: 29, date: "2026-06-29", summary: "Phase 29 eleventh edition freezes the read-only governance archive across Phase 29 exports and guards while release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.9", label: "phase29-final-closure-readiness-snapshot", phase: 29, date: "2026-06-29", summary: "Phase 29 tenth edition adds a read-only final closure readiness snapshot across all Phase 29 governance submodels while release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.8", label: "phase29-handoff-review-index", phase: 29, date: "2026-06-29", summary: "Phase 29 ninth edition adds a read-only handoff review index across Phase 29 handoff artifacts, checks, and blockers while release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.7", label: "phase29-closure-handoff-package", phase: 29, date: "2026-06-29", summary: "Phase 29 eighth edition adds a read-only closure handoff package across Phase 29 governance artifacts while blockers, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.6", label: "phase29-governance-closure-readiness", phase: 29, date: "2026-06-29", summary: "Phase 29 seventh edition adds read-only governance closure readiness across Phase 29 models while blockers, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.5", label: "phase29-decision-history-audit-chain", phase: 29, date: "2026-06-29", summary: "Phase 29 sixth edition adds a read-only decision history audit chain across Phase 29 governance models while blockers, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.4", label: "phase29-governance-dashboard-export", phase: 29, date: "2026-06-28", summary: "Phase 29 fifth edition adds a read-only governance dashboard export over Phase 29 planning models while blockers, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.3", label: "phase29-runtime-enable-governance-planning", phase: 29, date: "2026-06-28", summary: "Phase 29 fourth edition adds read-only runtime enable governance planning while blockers, release readiness, runtime, and third-party execution remain blocked." },
    { version: "1.9.2", label: "phase29-release-approval-state-model-planning", phase: 29, date: "2026-06-28", summary: "Phase 29 third edition adds a read-only release approval state model while blockers, release readiness, and runtime remain blocked." },
    { version: "1.9.1", label: "phase29-blocker-clearance-workflow-planning", phase: 29, date: "2026-06-28", summary: "Phase 29 second edition adds read-only blocker clearance workflow planning while blockers, release readiness, and runtime remain blocked." },
    { version: "1.9.0", label: "phase29-release-governance-planning", phase: 29, date: "2026-06-28", summary: "Phase 29 first edition opens release governance planning from the Phase 28 closure package while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.5", label: "phase28-closure-package", phase: 28, date: "2026-06-28", summary: "Phase 28 sixth edition closes the clearance review stage with a read-only closure package while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.4", label: "phase28-clearance-decision-preview", phase: 28, date: "2026-06-28", summary: "Phase 28 fifth edition adds a read-only clearance decision preview over criteria while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.3", label: "phase28-clearance-criteria-checklist", phase: 28, date: "2026-06-28", summary: "Phase 28 fourth edition adds a read-only clearance criteria checklist over reviewer dispositions while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.2", label: "phase28-reviewer-disposition-model", phase: 28, date: "2026-06-28", summary: "Phase 28 third edition adds a read-only reviewer disposition model over evidence intake while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.1", label: "phase28-evidence-intake-ledger", phase: 28, date: "2026-06-28", summary: "Phase 28 second edition adds a read-only evidence intake ledger to the clearance review entry while blockers, release readiness, and runtime remain blocked." },
    { version: "1.8.0", label: "phase28-clearance-review-entry", phase: 28, date: "2026-06-28", summary: "Phase 28 first edition opens a read-only release blocker clearance review entry from the Phase 27 closure package while blockers, release readiness, and runtime remain blocked." },
    { version: "1.7.5", label: "phase27-closure-package", phase: 27, date: "2026-06-28", summary: "Phase 27 sixth edition closes release blocker governance with a read-only closure package while open blockers, evidence gaps, runtime-disabled guardrails, and releaseReady=false are carried forward." },
    { version: "1.7.4", label: "phase27-blocker-resolution-plan", phase: 27, date: "2026-06-28", summary: "Phase 27 fifth edition adds a read-only blocker resolution plan over the evidence gap matrix while runtime, third-party execution, release readiness, and blocker clearance remain disabled." },
    { version: "1.7.3", label: "phase27-evidence-gap-matrix", phase: 27, date: "2026-06-28", summary: "Phase 27 fourth edition adds a read-only evidence gap matrix over carried blockers and human review signoff while runtime, third-party execution, release readiness, and blocker clearance remain disabled." },
    { version: "1.7.2", label: "phase27-human-review-signoff", phase: 27, date: "2026-06-28", summary: "Phase 27 third edition adds a read-only human review signoff package over carried blockers while runtime, third-party execution, release readiness, and blocker clearance remain disabled." },
    { version: "1.7.1", label: "phase27-carried-blocker-inventory", phase: 27, date: "2026-06-28", summary: "Phase 27 second edition promotes carried release blockers into a read-only governance inventory package while runtime, third-party execution, and release readiness remain disabled." },
    { version: "1.7.0", label: "phase27-entry-package", phase: 27, date: "2026-06-28", summary: "Phase 27 first edition opens release blocker governance with a read-only entry package sourced from the Phase 26 closure package while runtime, third-party execution, and release readiness remain disabled." },
    { version: "1.6.5", label: "phase26-closure-package", phase: 26, date: "2026-06-27", summary: "Phase 26 sixth edition closes the validation and simulation stage with a read-only closure package while release blockers and runtime guardrails are carried forward." },
    { version: "1.6.4", label: "phase26-handoff-criteria", phase: 26, date: "2026-06-27", summary: "Phase 26 fifth edition defines read-only handoff criteria for the next planning stage while runtime execution, third-party execution, release readiness, and blocker clearance stay disabled." },
    { version: "1.6.3", label: "phase26-runtime-validation-report", phase: 26, date: "2026-06-27", summary: "Phase 26 fourth edition adds a read-only runtime validation report over entry, release gate, and blocker clearance simulations while all runtime and release mutations stay disabled." },
    { version: "1.6.2", label: "phase26-blocker-clearance-simulation", phase: 26, date: "2026-06-27", summary: "Phase 26 third edition adds a read-only blocker clearance simulation model while runtime, third-party execution, release readiness, and blocker mutations stay disabled." },
    { version: "1.6.1", label: "phase26-release-gate-simulation", phase: 26, date: "2026-06-27", summary: "Phase 26 second edition adds a read-only release gate simulation model while runtime, third-party execution, and release readiness stay disabled." },
    { version: "1.6.0", label: "phase26-validation-entry", phase: 26, date: "2026-06-27", summary: "Phase 26 first edition opens the runtime sandbox validation entry for release gate simulation while runtime, third-party execution, and release readiness stay disabled." },
    { version: "1.5.6", label: "phase25-review-package-validation", phase: 25, date: "2026-06-27", summary: "Phase 25 seventh edition adds a read-only review package validation model for artifact checks, guardrail checks, and blocked validation mutations without runtime execution." },
    { version: "1.5.5", label: "phase25-review-export-package", phase: 25, date: "2026-06-27", summary: "Phase 25 sixth edition adds a read-only review export package manifest that bundles UI state, evidence bindings, review actions, and audit preview integrity without runtime execution." },
    { version: "1.5.4", label: "phase25-review-audit-preview", phase: 25, date: "2026-06-27", summary: "Phase 25 fifth edition adds a read-only review audit preview package for action events, blocked mutation evidence, and export integrity while runtime execution stays disabled." },
    { version: "1.5.3", label: "phase25-review-action-model", phase: 25, date: "2026-06-27", summary: "Phase 25 fourth edition adds a read-only review action model for panel switching, filtering, evidence detail, redacted export preview, and blocked mutation policy." },
    { version: "1.5.2", label: "phase25-panel-evidence-bindings", phase: 25, date: "2026-06-27", summary: "Phase 25 third edition binds every runtime sandbox UI panel to its Phase 24 source evidence, redaction rule, source count, and integrity checks while execution remains disabled." },
    { version: "1.5.1", label: "phase25-surface-state-model", phase: 25, date: "2026-06-27", summary: "Phase 25 second edition adds a read-only surface state model for active panels, filters, detail drawers, and export previews while runtime and third-party execution stay disabled." },
    { version: "1.5.0", label: "phase25-runtime-sandbox-ui-surface", phase: 25, date: "2026-06-27", summary: "Phase 25 first edition renders the runtime sandbox UX closure package as a read-only UI surface while runtime and third-party execution stay disabled." },
    { version: "1.4.6", label: "phase24-closure-package", phase: 24, date: "2026-06-27", summary: "Phase 24 seventh edition closes the runtime sandbox UX model package while runtime and third-party execution stay disabled and release stays blocked." },
    { version: "1.4.5", label: "phase24-release-clearance-preview", phase: 24, date: "2026-06-27", summary: "Phase 24 sixth edition adds a read-only release clearance preview that keeps releaseReady blocked until quarantine and blocker evidence are cleared." },
    { version: "1.4.4", label: "phase24-runtime-evidence-export-pack", phase: 24, date: "2026-06-27", summary: "Phase 24 fifth edition adds a guarded runtime evidence export pack with redacted handoff, preflight, permission, quarantine, and blocker evidence." },
    { version: "1.4.3", label: "phase24-quarantine-review-queue", phase: 24, date: "2026-06-27", summary: "Phase 24 fourth edition adds a read-only quarantine review queue for blocked and held runtime results without applying quarantined output." },
    { version: "1.4.2", label: "phase24-permission-decision-review-flow", phase: 24, date: "2026-06-27", summary: "Phase 24 third edition adds a permission decision review flow for denied Host API decisions while runtime permissions remain ungrantable." },
    { version: "1.4.1", label: "phase24-runtime-preflight-workbench", phase: 24, date: "2026-06-27", summary: "Phase 24 second edition adds a read-only runtime preflight workbench for kill switch, permission policy, quarantine, and release gate checks." },
    { version: "1.4.0", label: "phase24-runtime-sandbox-ux-entry", phase: 24, date: "2026-06-27", summary: "Phase 24 first edition establishes the runtime sandbox UX entry from the Phase 23 handoff while runtime and third-party execution stay disabled and release stays blocked." },
    { version: "1.3.9", label: "phase23-closure-handoff-package", phase: 23, date: "2026-06-27", summary: "Phase 23 tenth edition closes Phase 23 with a handoff package that preserves release blockers, next-phase entry conditions, and no-runtime guardrails." },
    { version: "1.3.8", label: "phase23-runtime-sandbox-panel-view-export", phase: 23, date: "2026-06-27", summary: "Phase 23 ninth edition adds filtered runtime sandbox read-only panel views and guarded exports while runtime and third-party execution stay disabled." },
    { version: "1.3.7", label: "phase23-runtime-sandbox-readonly-panels", phase: 23, date: "2026-06-26", summary: "Phase 23 eighth edition adds runtime sandbox read-only panel models for preflight, permission decisions, quarantine results, and release blockers while runtime and third-party execution stay disabled." },
    { version: "1.3.6", label: "phase23-runtime-sandbox-ui-plan", phase: 23, date: "2026-06-26", summary: "Phase 23 seventh edition adds a runtime sandbox UI planning model for read-only preflight, permission, quarantine, and blocker panels while runtime and third-party execution stay disabled." },
    { version: "1.3.5", label: "phase23-next-phase-guardrail-dashboard", phase: 23, date: "2026-06-26", summary: "Phase 23 sixth edition adds a next-phase guardrail dashboard that separates next-phase entry readiness from blocked release readiness while runtime and third-party execution stay disabled." },
    { version: "1.3.4", label: "phase23-review-action-ledger", phase: 23, date: "2026-06-26", summary: "Phase 23 fifth edition adds an append-only review action ledger for filter, detail, audit, export, and next-phase planning actions with no-release-mutation safeguards." },
    { version: "1.3.3", label: "phase23-blocker-detail-panels", phase: 23, date: "2026-06-26", summary: "Phase 23 fourth edition adds blocker detail panel models with owner, reason, missing evidence, clear requirements, linked audit entries, gate references, and no-runtime action guards." },
    { version: "1.3.2", label: "phase23-decision-history-export", phase: 23, date: "2026-06-26", summary: "Phase 23 third edition adds a decision history export model for release decisions, blocker states, clearance audit evidence, guardrails, and no-runtime export safeguards." },
    { version: "1.3.1", label: "phase23-clearance-audit-search", phase: 23, date: "2026-06-26", summary: "Phase 23 second edition adds a clearance audit search model with query fields, indexes, filter presets, result rows, and guarded exports while runtime and third-party plugin execution stay disabled." },
    { version: "1.3.0", label: "phase23-release-readiness-review-ui", phase: 23, date: "2026-06-26", summary: "Phase 23 first edition adds a release readiness review UI model for blockers, clearance audit entries, decision history, and next-phase guardrails while runtime and third-party plugin execution stay disabled." },
    { version: "1.2.7", label: "phase22-runtime-review-closure", phase: 22, date: "2026-06-26", summary: "Phase 22 eighth edition closes runtime review with a phase completion artifact, preserved blockers, next-phase entry conditions, and explicit no-runtime-release safeguards while third-party plugin execution stays disabled." },
    { version: "1.2.6", label: "phase22-clearance-audit-trail", phase: 22, date: "2026-06-26", summary: "Phase 22 seventh edition adds an append-only clearance audit trail for blocker clearance attempts, verification results, outcomes, decision references, and export coverage while third-party plugin execution stays disabled." },
    { version: "1.2.5", label: "phase22-release-readiness-decision", phase: 22, date: "2026-06-26", summary: "Phase 22 sixth edition adds a derived release readiness decision with gate results, active blocker reasons, recompute policy, and releaseReady safeguards while third-party plugin execution stays disabled." },
    { version: "1.2.4", label: "phase22-blocker-clearance-plan", phase: 22, date: "2026-06-26", summary: "Phase 22 fifth edition adds a human-owned release blocker clearance plan with owner roles, required evidence, verification steps, clearance decisions, and readiness recompute safeguards while third-party plugin execution stays disabled." },
    { version: "1.2.3", label: "phase22-release-blocker-rules", phase: 22, date: "2026-06-26", summary: "Phase 22 fourth edition adds explainable release blocker rules with severity levels, clearing requirements, release readiness summaries, and blocker audit coverage while third-party plugin execution stays disabled." },
    { version: "1.2.2", label: "phase22-review-signoff-ledger", phase: 22, date: "2026-06-26", summary: "Phase 22 third edition adds a reviewer signoff ledger with append-only disposition entries, evidence snapshots, chain integrity checks, and release-blocking signoff summaries while third-party plugin execution stays disabled." },
    { version: "1.2.1", label: "phase22-runtime-review-workbench", phase: 22, date: "2026-06-26", summary: "Phase 22 second edition adds a runtime review workbench with review queues, filters, detail panels, bulk actions, and disposition-ready evidence rows while third-party plugin execution stays disabled." },
    { version: "1.2.0", label: "phase22-runtime-evidence-review", phase: 22, date: "2026-06-26", summary: "Phase 22 first edition adds runtime evidence review bundles, reviewer dispositions, release blockers, and audit exports for quarantined plugin runtime results while third-party plugin execution stays disabled." },
    { version: "1.1.5", label: "phase21-runtime-result-quarantine", phase: 21, date: "2026-06-26", summary: "Phase 21 sixth edition adds runtime result quarantine for untrusted fixture and output results, reviewer disposition states, release blockers, and quarantine audit exports while third-party plugin execution stays disabled." },
    { version: "1.1.4", label: "phase21-importer-runtime-fixtures", phase: 21, date: "2026-06-26", summary: "Phase 21 fifth edition adds importer runtime fixtures for deterministic importer inputs, expected drafts, blocked fixture cases, audit replay links, and output validation coverage while third-party plugin execution stays disabled." },
    { version: "1.1.3", label: "phase21-output-validation-gate", phase: 21, date: "2026-06-26", summary: "Phase 21 fourth edition adds an output validation gate for plugin output schema checks, sensitivity blocking, byte limits, audit evidence, and deterministic rejection summaries while third-party plugin execution stays disabled." },
    { version: "1.1.2", label: "phase21-runtime-audit-replay", phase: 21, date: "2026-06-26", summary: "Phase 21 third edition adds runtime audit replay modeling for deterministic event replay, evidence snapshots, blocker checks, and replay summaries while third-party plugin execution stays disabled." },
    { version: "1.1.1", label: "phase21-permission-runtime-enforcement", phase: 21, date: "2026-06-26", summary: "Phase 21 second edition adds permission runtime enforcement modeling for manifest permissions, Host API allowlists, lockfile/signature/release-gate evidence, and deny-by-default runtime decisions while third-party plugin execution stays disabled." },
    { version: "1.1.0", label: "phase21-runtime-sandbox-foundation", phase: 21, date: "2026-06-26", summary: "Phase 21 first edition adds runtime sandbox foundation modeling for host API boundaries, kill-switch defaults, execution audit schema, and runtime preflight while third-party plugin execution stays disabled." },
    { version: "1.0.25", label: "phase20-release-checklist-gate", phase: 20, date: "2026-06-26", summary: "Phase 20 twenty-sixth edition adds release checklist gate modeling for final releaseReady decisions, pass/hold/block aggregation, runtime-disabled checks, and release gate artifacts while plugin runtime stays disabled." },
    { version: "1.0.24", label: "phase20-download-integrity-preview", phase: 20, date: "2026-06-26", summary: "Phase 20 twenty-fifth edition adds download integrity preview modeling for checksum policy, source artifact drift, badge-state linkage, and release-blocking mismatch signals while plugin runtime stays disabled." },
    { version: "1.0.23", label: "phase20-ci-badge-summary", phase: 20, date: "2026-06-26", summary: "Phase 20 twenty-fourth edition adds CI badge summary modeling for validation result badges, release visibility, badge artifacts, and audit coverage while plugin runtime stays disabled." },
    { version: "1.0.22", label: "phase20-signed-report-download", phase: 20, date: "2026-06-26", summary: "Phase 20 twenty-third edition adds signed report download modeling for redacted download envelopes, CI-gated artifacts, audit metadata, and download integrity checks while plugin runtime stays disabled." },
    { version: "1.0.21", label: "phase20-validation-command-ci-wrapper", phase: 20, date: "2026-06-25", summary: "Phase 20 twenty-second edition adds validation command CI wrapper modeling for exit code policy, JSON summaries, release artifacts, and release gate automation while plugin runtime stays disabled." },
    { version: "1.0.20", label: "phase20-review-history-ui", phase: 20, date: "2026-06-25", summary: "Phase 20 twenty-first edition adds review history UI modeling for blocker rows, hold review actions, reviewer evidence, filters, and signed export links while plugin runtime stays disabled." },
    { version: "1.0.19", label: "phase20-signed-report-export", phase: 20, date: "2026-06-25", summary: "Phase 20 twentieth edition adds signed release report export modeling for validation summaries, release artifacts, review history, signature envelopes, and archive readiness while plugin runtime stays disabled." },
    { version: "1.0.18", label: "phase20-release-report-validation-command", phase: 20, date: "2026-06-25", summary: "Phase 20 nineteenth edition adds release report validation command modeling for artifact, diff review history, releaseReady, and blocker consistency checks while plugin runtime stays disabled." },
    { version: "1.0.17", label: "phase20-diff-review-history", phase: 20, date: "2026-06-25", summary: "Phase 20 eighteenth edition adds diff review history for release report findings, reviewer decisions, evidence links, and readiness/smoke coverage while plugin runtime stays disabled." },
    { version: "1.0.16", label: "phase20-release-report-artifact", phase: 20, date: "2026-06-25", summary: "Phase 20 seventeenth edition adds a release report artifact with bundled evidence sections, release decision summary, export fields, and readiness/smoke coverage while plugin runtime stays disabled." },
    { version: "1.0.15", label: "phase20-signature-diff-report", phase: 20, date: "2026-06-25", summary: "Phase 20 sixteenth edition adds a signature diff report with severity grouping, release actions, and readiness/smoke coverage while plugin runtime stays disabled." },
    { version: "1.0.14", label: "phase20-pre-release-check-command", phase: 20, date: "2026-06-25", summary: "Phase 20 fifteenth edition adds a pre-release plugin check command, release assertions, command sample results, and readiness/smoke coverage while runtime execution stays disabled." },
    { version: "1.0.13", label: "phase20-lockfile-import-preview", phase: 20, date: "2026-06-25", summary: "Phase 20 fourteenth edition adds lockfile import preview, diff categories, permission drift warnings, signature drift blocking, and preview audit fields while runtime execution stays disabled." },
    { version: "1.0.12", label: "phase20-lockfile-export-release-gate", phase: 20, date: "2026-06-25", summary: "Phase 20 thirteenth edition adds lockfile export, install queue persistence model, release signature gate, and pre-release blocking checks while runtime execution stays disabled." },
    { version: "1.0.11", label: "phase20-plugin-review-lockfile", phase: 20, date: "2026-06-25", summary: "Phase 20 twelfth edition adds plugin review workbench, lockfile model, reviewed manifest freeze checks, and lockfile export coverage while runtime execution stays disabled." },
    { version: "1.0.10", label: "phase20-template-preview-fixtures", phase: 20, date: "2026-06-25", summary: "Phase 20 eleventh edition adds template preview fixtures, negative fixture blocking, preview workflows, and fixture export coverage while runtime execution stays disabled." },
    { version: "1.0.9", label: "phase20-plugin-installation-workflow", phase: 20, date: "2026-06-25", summary: "Phase 20 tenth edition adds a plugin installation workflow with manifest import, signature verification, permission review, contract tests, sandbox checks, and audit decisions while runtime execution stays disabled." },
    { version: "1.0.8", label: "phase20-signed-plugin-manifest", phase: 20, date: "2026-06-25", summary: "Phase 20 ninth edition adds signed manifest policy, digest fields, trust checks, and blocked unsigned plugin samples while runtime execution stays disabled." },
    { version: "1.0.7", label: "phase20-no-code-template-pack", phase: 20, date: "2026-06-25", summary: "Phase 20 eighth edition adds no-code template packs for importer, exporter, agent-tool, asset-template, and sync-adapter extension points while runtime execution stays disabled." },
    { version: "1.0.6", label: "phase20-plugin-sandbox-boundary", phase: 20, date: "2026-06-25", summary: "Phase 20 seventh edition defines plugin sandbox boundaries, blocked capabilities, data access limits, and runtime handoff gates while execution stays disabled." },
    { version: "1.0.5", label: "phase20-extension-contract-tests", phase: 20, date: "2026-06-25", summary: "Phase 20 sixth edition adds extension contract test suites, fixture expectations, failure policy, and readiness coverage while plugin runtime stays disabled." },
    { version: "1.0.4", label: "phase20-built-in-plugin-registry", phase: 20, date: "2026-06-25", summary: "阶段 20 第五版补充内置插件注册表、能力目录、输入输出契约和注册表检查项。" },
    { version: "1.0.3", label: "phase20-plugin-audit-log", phase: 20, date: "2026-06-25", summary: "阶段 20 第四版补充插件审计日志模型、审计事件 schema、运行阻断样例和导出字段。" },
    { version: "1.0.2", label: "phase20-plugin-permission-review", phase: 20, date: "2026-06-25", summary: "阶段 20 第三版补充插件权限复核策略、默认拒绝、人工确认、内置插件决策和审计事件类型。" },
    { version: "1.0.1", label: "phase20-plugin-manifest-schema", phase: 20, date: "2026-06-25", summary: "阶段 20 第二版补充插件 manifest schema、权限标签、扩展点契约和内置插件 manifest 摘要，真实插件运行时继续关闭。" },
    { version: "1.0.0", label: "phase20-platform-plugin-first-edition", phase: 20, date: "2026-06-25", summary: "阶段 20 第一版启动可扩展产品平台和插件生态边界，新增插件清单、扩展点、安全策略和 readiness 检查。" },
    { version: "0.9.8", label: "phase19-import-audit-ninth-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第九版补充复核状态流转、字段别名规则、导入报告视图和批次审计检索。" },
    { version: "0.9.7", label: "phase19-import-review-eighth-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第八版补充模板规则默认值、导入后整理队列、冲突复核台和批次审计导出。" },
    { version: "0.9.6", label: "phase19-import-conflict-seventh-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第七版补充自定义映射模板、重复项导入决策、导入前冲突预览和批次筛选对比。" },
    { version: "0.9.5", label: "phase19-import-template-sixth-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第六版补充字段映射模板、批次命名、重复项预判和跨批次对比。" },
    { version: "0.9.4", label: "phase19-import-recovery-fifth-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第五版补充失败项保留、批次内单项撤销、补全任务状态回看和质量趋势。" },
    { version: "0.9.3", label: "phase19-import-quality-fourth-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第四版补充导入完整度评分、批次详情和第十八阶段补全任务。" },
    { version: "0.9.2", label: "phase19-import-batch-third-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第三版补充草稿字段编辑、导入批次记录和批次撤销。" },
    { version: "0.9.1", label: "phase19-import-cleanup-second-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第二版补充导入清洗策略、CSV 分隔符容错、聊天聚合、草稿选择和来源追踪。" },
    { version: "0.9.0", label: "phase19-external-import-first-edition", phase: 19, date: "2026-06-24", summary: "阶段 19 第一版启动外部资料导入预览，支持文本、Markdown、CSV、JSON 和聊天片段生成展品草稿。" },
    { version: "0.8.12", label: "phase18-agent-digest-thirteenth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第十三版补充长期助理摘要、今日/本周焦点和摘要任务入口。" },
    { version: "0.8.11", label: "phase18-graph-asset-navigation-twelfth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第十二版补充关系图谱与专题展/报告的双向跳转。" },
    { version: "0.8.10", label: "phase18-sync-asset-link-eleventh-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第十一版补充资产同步状态、第十七阶段同步提示联动和导出结构。" },
    { version: "0.8.9", label: "phase18-batch-task-tenth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第十版补充复盘任务批量选择、批量状态流转和批量生成修复草案。" },
    { version: "0.8.8", label: "phase18-noise-rule-ninth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第九版补充可配置降噪规则、规则预览和被收起建议恢复提示。" },
    { version: "0.8.7", label: "phase18-sync-bridge-eighth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第八版补充第十七阶段同步维护桥接、复盘任务生成和图谱节点打开展品。" },
    { version: "0.8.6", label: "phase18-review-dashboard-seventh-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第七版补充长期助理复盘面板、建议自动降噪、关系图筛选和资产/报告回看入口。" },
    { version: "0.8.5", label: "phase18-review-report-sixth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第六版补充周期回顾报告草稿、建议质量分层和关系图谱。" },
    { version: "0.8.4", label: "phase18-review-assets-fifth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第五版补充周期回顾生成专题资产、Agent 质量统计和跨展品关系证据增强。" },
    { version: "0.8.3", label: "phase18-audit-batch-fourth-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第四版补充长期任务审计、批量应用前复核和修复草案批量处理。" },
    { version: "0.8.2", label: "phase18-task-queue-third-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第三版补充长期任务队列持久化、筛选、状态流转和清理能力。" },
    { version: "0.8.1", label: "phase18-repair-draft-second-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第二版补充建议采纳后的半自动修复草案、预览和逐条应用。" },
    { version: "0.8.0", label: "phase18-long-term-agent-first-edition", phase: 18, date: "2026-06-24", summary: "阶段 18 第一版启动长期记忆助理、主动整理建议、跨展品关系、周期回顾和反馈闭环。" },
    { version: "0.7.5", label: "phase17-sync-health-sixth-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第六版补充同步健康度，汇总设备信任、队列状态、局域网握手和私有云边界。" },
    { version: "0.7.4", label: "phase17-private-cloud-boundary-fifth-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第五版补充私有云适配配置边界，默认关闭且不保存密钥。" },
    { version: "0.7.3", label: "phase17-device-trust-fourth-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第四版补充设备信任策略、待复核设备和阻止设备状态。" },
    { version: "0.7.2", label: "phase17-queue-state-third-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第三版补充同步队列状态机和任务推进操作。" },
    { version: "0.7.1", label: "phase17-lan-handshake-second-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第二版补充局域网只读握手模拟和本机同步指纹。" },
    { version: "0.7.0", label: "phase17-sync-adapter-first-edition", phase: 17, date: "2026-06-24", summary: "阶段 17 第一版启动真实多设备同步适配层、设备注册表、同步队列和通道占位。" },
    { version: "0.6.4", label: "phase16-asset-merge-fifth-edition", phase: 16, date: "2026-06-23", summary: "阶段 16 第五版补充旧包兼容说明和专题资产逐项合并预览。" },
    { version: "0.6.3", label: "phase16-risk-audit-fourth-edition", phase: 16, date: "2026-06-23", summary: "阶段 16 第四版补充导入前风险确认和同步审计筛选。" },
    { version: "0.6.2", label: "phase16-quality-drill-third-edition", phase: 16, date: "2026-06-23", summary: "阶段 16 第三版补充同步包质量检查、恢复演练报告和专题资产同步边界。" },
    { version: "0.6.1", label: "phase16-conflict-audit-second-edition", phase: 16, date: "2026-06-23", summary: "阶段 16 第二版补充逐项冲突决策、同步审计记录和同步面板信息层级。" },
    { version: "0.6.0", label: "phase16-sync-preview-first-edition", phase: 16, date: "2026-06-23", summary: "阶段 16 第一版启动手动同步包、导入冲突预览和本地优先写入策略。" },
    { version: "0.5.16", label: "phase15-experience-polish-seventeenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十七版优化展品墙主路径、阶段文案、资产工作台降噪和恢复说明。" },
    { version: "0.5.15", label: "phase15-asset-recovery-search-sixteenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十六版补充快照选择对比、审计记录搜索和签名异常修复建议。" },
    { version: "0.5.14", label: "phase15-asset-restore-fifteenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十五版补充签名异常处理、快照导出恢复入口和审计筛选摘要。" },
    { version: "0.5.13", label: "phase15-asset-signature-fourteenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十四版补充快照对比恢复、审计记录筛选和发布包签名校验。" },
    { version: "0.5.12", label: "phase15-asset-recovery-thirteenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十三版补充审计日志导入恢复、资产版本快照和发布包校验摘要。" },
    { version: "0.5.11", label: "phase15-asset-audit-twelfth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十二版补充发布记录时间线、批量操作审计和资产包内容对比。" },
    { version: "0.5.10", label: "phase15-asset-batch-eleventh-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十一版补充批量状态操作、发布前差异预览和资产包明细清单。" },
    { version: "0.5.9", label: "phase15-asset-workbench-tenth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第十版补充资产发布状态流转、修复入口和导出清单过滤。" },
    { version: "0.5.8", label: "phase15-asset-review-ninth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第九版补充资产引用关系图、草稿修复建议和导出前质量确认。" },
    { version: "0.5.7", label: "phase15-asset-quality-eighth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第八版补充导出包质量预览、发布前字段校验和资产完整度提示。" },
    { version: "0.5.6", label: "phase15-asset-guided-editing-seventh-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第七版支持从已保存展品插入排序/引用、排序上移下移和完整导出包预览摘要。" },
    { version: "0.5.5", label: "phase15-asset-export-preview-sixth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第六版支持专题展展品排序、报告引用编辑和单个资产导出预览。" },
    { version: "0.5.4", label: "phase15-asset-editing-fifth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第五版支持在详情弹窗内编辑专题展和报告草稿核心字段。" },
    { version: "0.5.3", label: "phase15-asset-management-fourth-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第四版补充专题资产详情、刷新和删除管理体验。" },
    { version: "0.5.2", label: "phase15-frontend-assets-third-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第三版把专题资产闭环接入前端，支持保存主题展、生成报告草稿并展示已保存资产。" },
    { version: "0.5.1", label: "phase15-insight-assets-second-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第二版新增从主题洞察生成专题展草稿、从 insights 生成报告草稿，并修复资产创建状态码判断。" },
    { version: "0.5.0", label: "phase15-assets-first-edition", phase: 15, date: "2026-06-23", summary: "阶段 15 第一版启动专题资产建设，新增可保存专题展和回忆报告草稿的数据模型、API 与导出边界。" },
    { version: "0.4.2", label: "phase14-health-route-phase15-readiness-third-edition", phase: 14, date: "2026-06-23", summary: "阶段 14 第三版新增 src/routes/health.js，/api/health 从 server.js 抽离，并补充第十五阶段 readiness 和专题资产计划。" },
    { version: "0.4.1", label: "phase14-operations-route-split-second-edition", phase: 14, date: "2026-06-23", summary: "阶段 14 第二版新增 src/routes/operations.js，/api/version、/api/operations 和 /api/operations/export 的路由分发从 server.js 抽离。" },
    { version: "0.4.0", label: "phase14-operations-service-split-first-edition", phase: 14, date: "2026-06-23", summary: "阶段 14 第一版抽离 operations 服务边界，版本、运维摘要、发布记录、演示包和阶段 14 readiness 改由 src/services/operations.js 提供。" },
    { version: "0.3.4", label: "phase13-phase14-readiness-edition", phase: 13, date: "2026-06-23", summary: "第五版补充第十四阶段 readiness、模块边界计划、迁移顺序和页面进入阶段 14 判断。" },
    { version: "0.3.3", label: "phase13-operations-fourth-edition", phase: 13, date: "2026-06-23", summary: "第四版补充持久化 JSONL 运维日志、/api/operations/export、演示包摘要和页面日志/演示面板。" },
    { version: "0.3.2", label: "phase13-operations-third-edition", phase: 13, date: "2026-06-22", summary: "第三版补充 X-Request-Id、/api/operations、最近运行事件、发布记录和页面运行事件面板。" },
    { version: "0.3.1", label: "phase13-operations-second-edition", phase: 13, date: "2026-06-22", summary: "第二版补充发布清单、运行手册、部署模式、备份策略和风险登记。" },
    { version: "0.3.0", label: "phase13-operations-first-edition", phase: 13, date: "2026-06-22", summary: "第一版补充 /api/version、operations 健康摘要和部署与运维面板。" }
  ];
}

function getVersionInfo() {
  if (versionInfo?.phase >= 15) {
    operationsSource = "server";
    return versionInfo;
  }
  operationsSource = "local";
  return buildLocalVersionInfo();
}

async function syncVersionInfo({ quiet = false } = {}) {
  if (!databaseAvailable && !quiet) {
    versionInfo = null;
    operationsSource = "local";
    renderOperationsPanel();
    renderPhase20PlatformPanel();
    return null;
  }
  try {
    const response = await fetch(API_VERSION);
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    const payload = await response.json();
    if ((payload?.phase || 0) < 15) throw new Error("version info is invalid");
    versionInfo = payload;
    operationsSource = "server";
    renderOperationsPanel();
    renderPhase20PlatformPanel();
    return payload;
  } catch {
    versionInfo = null;
    operationsSource = "local";
    if (!quiet) {
      renderOperationsPanel();
      renderPhase20PlatformPanel();
    }
    return null;
  }
}

function buildPhase10Insights(items = [], filters = {}) {
  const structuredAll = items.map(buildStructuredMemory);
  const structured = filterInsightMemories(structuredAll, filters);
  return {
    filters: normalizeInsightFilters(filters),
    sourceTotal: structuredAll.length,
    filteredTotal: structured.length,
    handoff: buildPhase10Handoff(structured),
    timeline: buildTimelineInsights(structured),
    themes: buildThemeInsights(structured),
    report: buildMemoryReport(structured)
  };
}

function normalizeInsightFilters(filters = {}) {
  return {
    hall: limitText(filters.hall, 40),
    year: /^\d{4}$/.test(String(filters.year || "")) ? String(filters.year) : "",
    theme: limitText(filters.theme, 80)
  };
}

function filterInsightMemories(items = [], filters = {}) {
  const normalized = normalizeInsightFilters(filters);
  const theme = normalized.theme.toLowerCase();
  return items.filter((memory) => {
    const hallId = memory.hall?.id || memory.hall;
    if (normalized.hall && normalized.hall !== "all" && hallId !== normalized.hall) return false;
    if (normalized.year) {
      const date = memory.date || memory.createdAt || "";
      if (!String(date).startsWith(normalized.year)) return false;
    }
    if (theme) {
      const haystack = [
        memory.title,
        memory.exhibitText,
        memory.rawContent,
        memory.location,
        memory.sourceType,
        memory.coverImage,
        memory.mediaNote,
        memory.hall?.name,
        ...(memory.people || []),
        ...(memory.tags || []),
        ...(memory.emotions || []),
        ...(memory.attachments || []).flatMap((item) => [item.name, item.type, item.note])
      ].join(" ").toLowerCase();
      if (!haystack.includes(theme)) return false;
    }
    return true;
  });
}

function buildTimelineInsights(items = []) {
  const groups = items.reduce((map, memory) => {
    const period = getMemoryPeriod(memory);
    if (!map.has(period.key)) {
      map.set(period.key, { ...period, count: 0, mediaCount: 0, emotions: {}, memories: [] });
    }
    const group = map.get(period.key);
    group.count += 1;
    if (hasMultimodalMetadata(memory)) group.mediaCount += 1;
    group.memories.push(buildMemoryReference(memory));
    (memory.emotions || []).forEach((emotion) => {
      group.emotions[emotion] = (group.emotions[emotion] || 0) + 1;
    });
    return map;
  }, new Map());

  return [...groups.values()]
    .sort((a, b) => b.sortValue.localeCompare(a.sortValue))
    .map((group) => ({
      key: group.key,
      label: group.label,
      count: group.count,
      mediaCount: group.mediaCount,
      topEmotions: toTopEntries(group.emotions, 3),
      memories: group.memories.slice(0, 5)
    }));
}

function buildThemeInsights(items = []) {
  const themeMap = new Map();
  items.forEach((memory) => {
    addThemeValue(themeMap, "展厅", memory.hall?.name || getHallName(memory.hall?.id), memory);
    addThemeValue(themeMap, "来源", memory.sourceType, memory);
    (memory.people || []).forEach((value) => addThemeValue(themeMap, "人物", value, memory));
    (memory.tags || []).forEach((value) => addThemeValue(themeMap, "标签", value, memory));
    (memory.emotions || []).forEach((value) => addThemeValue(themeMap, "情绪", value, memory));
    if (memory.location) addThemeValue(themeMap, "地点", memory.location, memory);
  });

  return [...themeMap.values()]
    .sort((a, b) => b.count - a.count || b.mediaCount - a.mediaCount || a.title.localeCompare(b.title, "zh-CN"))
    .slice(0, 8)
    .map((theme) => ({
      type: theme.type,
      title: theme.title,
      count: theme.count,
      mediaCount: theme.mediaCount,
      description: buildThemeDescription(theme),
      coverMemory: theme.memories[0] || null,
      topEmotions: toTopEntries(theme.emotions, 3),
      memories: theme.memories.slice(0, 4)
    }));
}

function buildThemeDescription(theme) {
  const emotion = toTopEntries(theme.emotions, 1)[0]?.label || "平静";
  const mediaText = theme.mediaCount ? `其中 ${theme.mediaCount} 件带有多模态线索` : "目前主要由文字线索构成";
  return `${theme.type}“${theme.title}”串联了 ${theme.count} 件展品，主导情绪偏向“${emotion}”，${mediaText}。`;
}

function addThemeValue(themeMap, type, value, memory) {
  const title = limitText(value, 40);
  if (!title) return;
  const key = `${type}:${title}`;
  if (!themeMap.has(key)) {
    themeMap.set(key, { type, title, count: 0, mediaCount: 0, emotions: {}, memories: [] });
  }
  const theme = themeMap.get(key);
  theme.count += 1;
  if (hasMultimodalMetadata(memory)) theme.mediaCount += 1;
  theme.memories.push(buildMemoryReference(memory));
  (memory.emotions || []).forEach((emotion) => {
    theme.emotions[emotion] = (theme.emotions[emotion] || 0) + 1;
  });
}

function buildMemoryReport(items = []) {
  const total = items.length;
  const favoriteCount = items.filter((memory) => memory.favorite).length;
  const multimodalCount = items.filter(hasMultimodalMetadata).length;
  const topEmotions = toTopEntries(countValues(items.flatMap((memory) => memory.emotions || [])), 5);
  const topTags = toTopEntries(countValues(items.flatMap((memory) => memory.tags || [])), 5);
  const topPeople = toTopEntries(countValues(items.flatMap((memory) => memory.people || [])), 5);
  const topLocations = toTopEntries(countValues(items.map((memory) => memory.location).filter(Boolean)), 5);
  const halls = toTopEntries(countValues(items.map((memory) => memory.hall?.name || getHallName(memory.hall?.id))), 6);
  const dateRange = buildDateRange(items);
  const highlights = [...items]
    .sort((a, b) => Number(b.favorite) - Number(a.favorite) || (b.importance || 1) - (a.importance || 1))
    .slice(0, 3)
    .map(buildMemoryReference);
  const references = highlights.map((memory, index) => ({
    ...memory,
    role: index === 0 ? "开篇展品" : "支撑展品"
  }));
  const dominantEmotion = topEmotions[0]?.label || "平静";
  const dominantTag = topTags[0]?.label || halls[0]?.label || "日常";
  const sections = buildReportSections({
    total,
    dominantTag,
    dominantEmotion,
    favoriteCount,
    multimodalCount,
    dateRange,
    topPeople,
    topLocations,
    highlights
  });

  return {
    total,
    favoriteCount,
    multimodalCount,
    topEmotions,
    topTags,
    topPeople,
    topLocations,
    halls,
    dateRange,
    highlights,
    references,
    sections,
    summary: total
      ? `这批记忆共 ${total} 件展品，主要围绕“${dominantTag}”展开，最明显的情绪是“${dominantEmotion}”。其中 ${favoriteCount} 件被标记为重点展品，${multimodalCount} 件带有照片、OCR、语音或附件线索。`
      : "当前还没有展品，保存几段记忆后可以生成时间线、主题展和回忆报告。",
    nextQuestions: [
      "按月份看，这些记忆在哪些阶段最密集？",
      "哪些人物、地点或标签适合组成主题展？",
      "哪些重点展品适合作为年度回忆报告开头？"
    ]
  };
}

function buildDateRange(items = []) {
  const dates = items
    .map((memory) => memory.date)
    .filter((date) => /^\d{4}-\d{2}-\d{2}$/.test(date))
    .sort();
  return dates.length ? { start: dates[0], end: dates[dates.length - 1] } : { start: "", end: "" };
}

function buildReportSections({ total, dominantTag, dominantEmotion, favoriteCount, multimodalCount, dateRange, topPeople, topLocations, highlights }) {
  if (!total) return [];
  const rangeText = dateRange.start
    ? dateRange.start === dateRange.end ? dateRange.start : `${dateRange.start} 至 ${dateRange.end}`
    : "尚未形成明确时间范围";
  const peopleText = topPeople.length ? topPeople.slice(0, 3).map((item) => item.label).join("、") : "暂未形成高频人物";
  const locationText = topLocations.length ? topLocations.slice(0, 3).map((item) => item.label).join("、") : "暂未形成高频地点";
  const highlightText = highlights.length ? highlights.map((memory) => `《${memory.title}》`).join("、") : "暂未选出重点展品";
  return [
    { title: "时间范围", text: `这批记忆覆盖 ${rangeText}，可以作为时间线回看的起点。` },
    { title: "主题主线", text: `当前最明显的主题是“${dominantTag}”，主导情绪是“${dominantEmotion}”。` },
    { title: "人物与地点", text: `人物线索集中在 ${peopleText}；地点线索集中在 ${locationText}。` },
    { title: "报告开头", text: `建议从 ${highlightText} 开始讲述，其中 ${favoriteCount} 件是重点展品，${multimodalCount} 件带多模态线索。` }
  ];
}

function getMemoryPeriod(memory) {
  const rawDate = memory.date || memory.createdAt || "";
  const match = String(rawDate).match(/^(\d{4})(?:-(\d{2}))?/);
  if (!match) return { key: "undated", label: "未标注时间", sortValue: "0000-00" };
  const year = match[1];
  const month = match[2] || "00";
  return {
    key: `${year}-${month}`,
    label: month === "00" ? `${year} 年` : `${year} 年 ${month} 月`,
    sortValue: `${year}-${month}`
  };
}

function buildMemoryReference(memory) {
  return {
    id: memory.id,
    title: memory.title,
    hall: memory.hall?.name || getHallName(memory.hall?.id),
    date: memory.date || "",
    importance: memory.importance,
    favorite: memory.favorite,
    media: hasMultimodalMetadata(memory)
  };
}

function countValues(values = []) {
  return values.filter(Boolean).reduce((counts, value) => {
    counts[value] = (counts[value] || 0) + 1;
    return counts;
  }, {});
}

function toTopEntries(counts = {}, maxItems = 5) {
  return Object.entries(counts)
    .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0], "zh-CN"))
    .slice(0, maxItems)
    .map(([label, count]) => ({ label, count }));
}

function renderHallSelect() {
  elements.hallSelect.innerHTML = halls
    .filter((hall) => hall.id !== "all")
    .map((hall) => `<option value="${hall.id}">${escapeHtml(hall.name)}</option>`)
    .join("");
}

function renderEmotionOptions() {
  elements.emotionOptions.innerHTML = emotionOptions
    .map((emotion) => `<label><input type="checkbox" name="emotion" value="${escapeHtml(emotion)}" />${escapeHtml(emotion)}</label>`)
    .join("");
}

function renderEmotionFilter() {
  const usedEmotions = [...new Set(memories.flatMap((memory) => memory.emotions || []))].sort((a, b) => a.localeCompare(b, "zh-CN"));
  const options = [
    `<option value="all">全部情绪</option>`,
    `<option value="favorite">重点展品</option>`,
    `<option value="media">多模态线索</option>`,
    ...usedEmotions.map((emotion) => `<option value="${escapeHtml(emotion)}">${escapeHtml(emotion)}</option>`)
  ];
  elements.emotionFilterSelect.innerHTML = options.join("");
  elements.emotionFilterSelect.value = usedEmotions.includes(emotionFilter) || ["favorite", "media"].includes(emotionFilter) ? emotionFilter : "all";
  emotionFilter = elements.emotionFilterSelect.value;
}

function renderHallNav() {
  elements.hallNav.innerHTML = halls.map((hall) => {
    const count = hall.id === "all" ? memories.length : memories.filter((memory) => memory.hall === hall.id).length;
    const activeClass = activeHall === hall.id ? "active" : "";
    return `<button class="hall-button ${activeClass}" type="button" data-hall="${hall.id}"><span>${escapeHtml(hall.name)}</span><small>${count}</small></button>`;
  }).join("");

  elements.hallNav.querySelectorAll("button").forEach((button) => {
    button.addEventListener("click", () => {
      activeHall = button.dataset.hall;
      render();
    });
  });
}

function filteredMemories() {
  const keyword = searchKeyword.toLowerCase();
  const list = memories.filter((memory) => {
    const hallMatch = activeHall === "all" || memory.hall === activeHall;
    const emotionMatch = emotionFilter === "all"
      || (emotionFilter === "favorite" && memory.favorite)
      || (emotionFilter === "media" && hasMultimodalMetadata(memory))
      || (memory.emotions || []).includes(emotionFilter);
    const haystack = [
      memory.title,
      memory.rawContent,
      memory.exhibitText,
      memory.location,
      memory.date,
      memory.createdAt,
      memory.coverImage,
      memory.mediaNote,
      memory.sourceType,
      getImportanceLabel(memory.importance),
      ...(memory.people || []),
      ...(memory.tags || []),
      ...(memory.emotions || []),
      ...(memory.attachments || []).flatMap((item) => typeof item === "string" ? [item] : [item.name, item.type, item.note])
    ].join(" ").toLowerCase();
    const keywordMatch = !keyword || haystack.includes(keyword);
    return hallMatch && emotionMatch && keywordMatch;
  });

  return [...list].sort((a, b) => {
    if (sortMode === "oldest") return new Date(a.createdAt) - new Date(b.createdAt);
    if (sortMode === "title") return a.title.localeCompare(b.title, "zh-CN");
    if (sortMode === "importance") return (b.importance || 1) - (a.importance || 1) || new Date(b.createdAt) - new Date(a.createdAt);
    if (sortMode === "intensity") return (b.emotionIntensity || 1) - (a.emotionIntensity || 1) || new Date(b.createdAt) - new Date(a.createdAt);
    return new Date(b.createdAt) - new Date(a.createdAt);
  });
}

function renderStats() {
  const uniqueTags = new Set(memories.flatMap((memory) => memory.tags || []));
  const uniqueEmotions = new Set(memories.flatMap((memory) => memory.emotions || []));
  const usedHalls = new Set(memories.map((memory) => memory.hall));
  const linkedAgentRuns = new Set(memories.map((memory) => memory.agentRunId).filter(Boolean));
  const localMultimodal = memories.filter(hasMultimodalMetadata).length;
  elements.memoryCount.textContent = memories.length;
  elements.hallCount.textContent = usedHalls.size;
  elements.tagCount.textContent = uniqueTags.size;
  elements.emotionCount.textContent = uniqueEmotions.size;
  const backendMultimodal = Number(backendStats?.multimodal);
  elements.mediaCount.textContent = Number.isFinite(backendMultimodal)
    ? Math.max(backendMultimodal, localMultimodal)
    : localMultimodal;
  const backendAgentRuns = Number(backendStats?.agentRuns);
  elements.agentRunCount.textContent = Number.isFinite(backendAgentRuns)
    ? Math.max(backendAgentRuns, linkedAgentRuns.size)
    : linkedAgentRuns.size;
}

function hasMultimodalMetadata(memory = {}) {
  return Boolean(
    memory.coverImage
    || memory.mediaNote
    || normalizeAttachments(memory.attachments).length
  );
}

function renderFormMode() {
  const editingMemory = memories.find((memory) => memory.id === editingId);
  elements.saveButton.textContent = editingMemory ? "保存修改" : "保存为展品";
  elements.cancelEditButton.classList.toggle("is-hidden", !editingMemory);
  elements.formModeHint.textContent = editingMemory
    ? `正在编辑《${editingMemory.title}》。保存后会更新原展品，不会创建副本。`
    : "写下原始记忆后，可以让后端 AI 生成展品草稿。";
  renderDraftPreview();
}

function setAiStatus(message, type = "neutral") {
  elements.aiStatus.textContent = message;
  elements.aiStatus.dataset.status = type;
}

function setAnalyzePending(isPending) {
  isAnalyzing = isPending;
  elements.mockAiButton.disabled = isPending;
  elements.mockAiButton.textContent = isPending ? AI_BUTTON_PENDING_TEXT : AI_BUTTON_TEXT;
  elements.sampleButton.disabled = isPending;
  elements.saveButton.disabled = isPending || isPersisting;
  elements.resetButton.disabled = isPending;
  elements.cancelEditButton.disabled = isPending;
  updateAgentActionState();
}

function setGuidePending(isPending) {
  isGuideAsking = isPending;
  updateGuideAskState();
  elements.guideAskButton.textContent = isPending ? "检索中..." : "提问";
  document.querySelectorAll("[data-question]").forEach((button) => {
    button.disabled = isPending;
  });
}

function updateGuideAskState() {
  const hasQuestion = elements.guideQuestionInput.value.trim().length > 0;
  elements.guideAskButton.disabled = isGuideAsking || !hasQuestion;
}

function renderDraftPreview() {
  const raw = elements.rawContent.value.trim();
  const title = elements.titleInput.value.trim() || (raw ? mockAnalyzeMemory(raw).title : "等待一段记忆");
  const hallName = getHallName(elements.hallSelect.value || "daily");
  const emotions = selectedEmotions();
  const tags = splitList(elements.tagsInput.value).slice(0, 4);
  const people = splitList(elements.peopleInput.value).slice(0, 3);
  const exhibitText = elements.exhibitText.value.trim();
  const coverImage = elements.coverImageInput.value.trim();
  const mediaNote = elements.mediaNoteInput.value.trim();
  const attachments = normalizeAttachments(elements.attachmentsInput.value);
  const workflowStatus = latestAgentWorkflow?.summary?.nextAction || (raw ? "可运行 Agent 整理" : "先写下原始记忆");
  const attachmentPreview = attachments.slice(0, 3).map((item) => `${item.name}${item.type ? ` / ${item.type}` : ""}`).join("；");
  const attachmentTypeSummary = formatAttachmentTypeSummary(attachments);
  const chips = [
    ...emotions.slice(0, 3),
    ...tags.slice(0, Math.max(0, 4 - Math.min(emotions.length, 3)))
  ];
  const previewText = exhibitText || mediaNote || (raw ? limitText(raw, 90) : "这里会显示即将入馆的展品草稿。");

  elements.draftPreview.innerHTML = `
    <div class="draft-preview-card">
      <span class="preview-stamp">草稿</span>
      <p class="eyebrow">Exhibit Draft</p>
      <h4>${escapeHtml(title)}</h4>
      <p>${escapeHtml(previewText)}</p>
      <div class="preview-chip-row">
        <span>${escapeHtml(hallName)}</span>
        <span>${escapeHtml(getImportanceLabel(Number(elements.importanceSelect.value) || 3))}</span>
        ${coverImage ? "<span>封面线索</span>" : ""}
        ${attachments.length ? `<span>${attachments.length} 个附件</span>` : ""}
        ${elements.favoriteInput.checked ? "<span>重点展品</span>" : ""}
      </div>
      <div class="preview-chip-row">
        ${chips.length ? chips.map((item) => `<span>${escapeHtml(item)}</span>`).join("") : "<span>等待情绪和标签</span>"}
      </div>
      <div class="preview-route">
        <strong>入馆线索</strong>
        <small>${escapeHtml(people.length ? `相关人物：${people.join("、")}` : "人物线索可稍后补充")}</small>
        <small>${escapeHtml(mediaNote ? `多模态线索：${limitText(mediaNote, 42)}` : "第十阶段会把图片、OCR、语音线索纳入时间线和主题展")}</small>
        ${attachmentPreview ? `<small class="preview-attachment-list">${escapeHtml(`附件预览：${attachmentPreview}${attachments.length > 3 ? "；..." : ""}`)}</small>` : ""}
        ${attachmentTypeSummary ? `<small class="preview-attachment-types">${escapeHtml(`附件类型：${attachmentTypeSummary}`)}</small>` : ""}
        <small>${escapeHtml(attachments.length ? "附件状态：已记录清单，原文件待后续上传能力接入" : "附件状态：暂无附件清单")}</small>
        <small>${escapeHtml(workflowStatus)}</small>
      </div>
    </div>
  `;
}

async function readApiPayload(response) {
  const text = await response.text();
  if (!text) return {};

  try {
    return JSON.parse(text);
  } catch {
    return { error: text.slice(0, 180) };
  }
}

function setStorageStatus(message, type = "neutral") {
  elements.storageStatus.textContent = message;
  elements.storageStatus.dataset.status = type;
}

function setPersistencePending(isPending) {
  isPersisting = isPending;
  elements.saveButton.disabled = isPending || isAnalyzing;
  elements.exportButton.disabled = isPending;
  elements.syncDatabaseButton.disabled = isPending;
  elements.migrateLocalButton.disabled = isPending;
  elements.saveProfileButton.disabled = isPending;
  elements.purgeDatabaseButton.disabled = isPending;
  elements.syncDatabaseButton.textContent = isPending ? "处理中..." : "同步数据库";
  elements.migrateLocalButton.textContent = isPending ? "处理中..." : "迁移本地";
}

async function requestJson(url, options = {}) {
  const response = await fetch(url, {
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...(options.headers || {})
    }
  });
  const payload = await readApiPayload(response);
  if (!response.ok) throw new Error(payload.error || `请求失败：${response.status}`);
  return payload;
}

function buildLocalAssetCollection() {
  return {
    schemaVersion: SCHEMA_VERSION,
    phase: 18,
    source: "browser-fallback",
    savedExhibitions: [],
    reportDrafts: [],
    phase15Readiness: buildLocalPhase15Readiness(),
    phase15AssetPlan: buildLocalPhase15AssetPlan()
  };
}

function getAssetCollection() {
  if (assetCollection?.phase >= 15) {
    assetSource = "server";
    return assetCollection;
  }
  assetSource = "local";
  return buildLocalAssetCollection();
}

async function syncAssetCollection({ quiet = false } = {}) {
  if (!databaseAvailable && !quiet) {
    assetCollection = null;
    assetSource = "local";
    renderAssetCollectionPanel();
    return null;
  }
  try {
    const payload = await requestJson(API_ASSETS);
    if ((payload?.phase || 0) < 15) throw new Error("asset collection is invalid");
    assetCollection = payload;
    assetSource = "server";
    renderAssetCollectionPanel();
    return payload;
  } catch {
    assetCollection = null;
    assetSource = "local";
    if (!quiet) renderAssetCollectionPanel();
    return null;
  }
}

async function saveThemeAsExhibition(themeTitle) {
  const theme = limitText(themeTitle, 80);
  if (!theme) return;
  if (!databaseAvailable) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能把主题候选保存为长期专题展资产。", "warning");
    renderAssetCollectionPanel();
    return;
  }
  setStorageStatus(`正在把“${theme}”保存为专题展草稿...`, "loading");
  try {
    const payload = await requestJson(`${API_EXHIBITIONS}/from-theme`, {
      method: "POST",
      body: JSON.stringify({
        theme,
        hall: insightScope === "hall" && activeHall !== "all" ? activeHall : "",
        title: `${theme}专题展草稿`
      })
    });
    const saved = payload.savedExhibition;
    setStorageStatus(`已保存专题展草稿：${saved?.title || theme}`, "success");
    await syncAssetCollection({ quiet: true });
  } catch (error) {
    setStorageStatus(`专题展保存失败：${error.message}`, "warning");
  }
}

async function saveReportDraftFromInsights() {
  if (!databaseAvailable) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能把当前洞察保存为报告草稿。", "warning");
    renderAssetCollectionPanel();
    return;
  }
  const scope = {
    hall: insightScope === "hall" && activeHall !== "all" ? activeHall : "",
    source: insightScope
  };
  setStorageStatus("正在把当前洞察生成回忆报告草稿...", "loading");
  try {
    const payload = await requestJson(`${API_REPORT_DRAFTS}/from-insights`, {
      method: "POST",
      body: JSON.stringify({
        scope,
        title: "回忆报告草稿"
      })
    });
    const saved = payload.reportDraft;
    setStorageStatus(`已生成报告草稿：${saved?.title || "回忆报告草稿"}`, "success");
    await syncAssetCollection({ quiet: true });
  } catch (error) {
    setStorageStatus(`报告草稿生成失败：${error.message}`, "warning");
  }
}

async function refreshAssetCollection() {
  if (!databaseAvailable) {
    setStorageStatus("后端未连接：当前只能显示浏览器回退的空资产面板。", "warning");
    renderAssetCollectionPanel();
    return;
  }
  setStorageStatus("正在刷新专题资产库...", "loading");
  const payload = await syncAssetCollection({ quiet: true });
  setStorageStatus(payload ? "专题资产库已刷新。" : "专题资产库刷新失败，已保留本地回退视图。", payload ? "success" : "warning");
}

async function fetchSavedAsset(type, id) {
  const endpoint = type === "report" ? API_REPORT_DRAFTS : API_EXHIBITIONS;
  const payload = await requestJson(`${endpoint}/${encodeURIComponent(id)}`);
  return type === "report" ? payload.reportDraft : payload.savedExhibition;
}

async function openSavedAsset(type, id) {
  if (!id) return;
  const collection = getAssetCollection();
  const localItems = type === "report" ? collection.reportDrafts : collection.savedExhibitions;
  let asset = Array.isArray(localItems) ? localItems.find((item) => item.id === id) : null;
  if (databaseAvailable) {
    try {
      asset = await fetchSavedAsset(type, id);
    } catch (error) {
      setStorageStatus(`资产详情读取失败：${error.message}`, "warning");
    }
  }
  if (asset) renderSavedAssetDialog(type, asset);
}

async function deleteSavedAsset(type, id, title = "") {
  if (!databaseAvailable || !id) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能删除专题资产。", "warning");
    return;
  }
  const label = title || (type === "report" ? "这份报告草稿" : "这个专题展");
  if (!confirm(`确定删除“${label}”吗？这个操作只删除专题资产，不会删除原始展品。`)) return;
  const endpoint = type === "report" ? API_REPORT_DRAFTS : API_EXHIBITIONS;
  setStorageStatus(`正在删除“${label}”...`, "loading");
  try {
    await requestJson(`${endpoint}/${encodeURIComponent(id)}`, { method: "DELETE" });
    setStorageStatus(`已删除专题资产：${label}`, "success");
    await syncAssetCollection({ quiet: true });
  } catch (error) {
    setStorageStatus(`专题资产删除失败：${error.message}`, "warning");
  }
}

async function updateSavedAssetStatus(type, id, nextStatus) {
  if (!databaseAvailable || !id) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能推进专题资产状态。", "warning");
    return;
  }
  try {
    const asset = await fetchSavedAsset(type, id);
    const validation = validateAssetPayload(type, { ...asset, status: nextStatus });
    if (validation.blocking.length) {
      setStorageStatus(`暂时不能进入 ${nextStatus}：${validation.blocking.join(" ")}`, "warning");
      if (asset) renderSavedAssetDialog(type, asset);
      return;
    }
    const endpoint = type === "report" ? API_REPORT_DRAFTS : API_EXHIBITIONS;
    const response = await requestJson(endpoint, {
      method: "POST",
      body: JSON.stringify({ ...asset, status: nextStatus })
    });
    const saved = type === "report" ? response.reportDraft : response.savedExhibition;
    recordAssetAuditEvent({
      action: "status-update",
      label: `${type === "report" ? "报告" : "专题展"}《${saved?.title || asset.title || id}》状态更新`,
      detail: `${asset.status || "draft"} -> ${nextStatus}`
    });
    captureAssetSnapshot(`单资产状态更新：${saved?.title || asset.title || id}`);
    setStorageStatus(`专题资产状态已更新：${saved?.title || asset.title || id} / ${nextStatus}`, "success");
    await syncAssetCollection({ quiet: true });
  } catch (error) {
    setStorageStatus(`专题资产状态更新失败：${error.message}`, "warning");
  }
}

async function batchUpdateAssetStatus(fromStatus, nextStatus) {
  if (!databaseAvailable) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能批量推进专题资产状态。", "warning");
    return;
  }
  const collection = getAssetCollection();
  const candidates = [
    ...(collection.savedExhibitions || []).map((asset) => ({ type: "exhibition", asset })),
    ...(collection.reportDrafts || []).map((asset) => ({ type: "report", asset }))
  ].filter(({ asset }) => (asset.status || "draft") === fromStatus);
  if (!candidates.length) {
    setStorageStatus("当前没有符合条件的专题资产可批量推进。", "warning");
    return;
  }
  const ready = candidates.filter(({ type, asset }) => !validateAssetPayload(type, { ...asset, status: nextStatus }).blocking.length);
  const blocked = candidates.length - ready.length;
  if (!ready.length) {
    setStorageStatus(`批量推进已暂停：${blocked} 个资产需要先修复关键内容。`, "warning");
    return;
  }
  if (!confirm(`准备把 ${ready.length} 个资产从 ${fromStatus} 推进到 ${nextStatus}${blocked ? `；另有 ${blocked} 个需要先修复` : ""}。继续吗？`)) return;
  let savedCount = 0;
  setStorageStatus(`正在批量推进专题资产状态：${fromStatus} -> ${nextStatus}`, "loading");
  for (const { type, asset } of ready) {
    const endpoint = type === "report" ? API_REPORT_DRAFTS : API_EXHIBITIONS;
    try {
      await requestJson(endpoint, {
        method: "POST",
        body: JSON.stringify({ ...asset, status: nextStatus })
      });
      savedCount += 1;
    } catch (error) {
      console.warn("Batch asset status update failed", error);
    }
  }
  await syncAssetCollection({ quiet: true });
  if (savedCount) {
    recordAssetAuditEvent({
      action: "batch-status-update",
      label: `批量状态推进 ${fromStatus} -> ${nextStatus}`,
      detail: `已更新 ${savedCount} 个资产${blocked ? `，${blocked} 个仍需修复` : ""}`
    });
    captureAssetSnapshot(`批量状态推进：${fromStatus} -> ${nextStatus}`);
  }
  setStorageStatus(`批量状态推进完成：已更新 ${savedCount} 个资产${blocked ? `，${blocked} 个仍需修复` : ""}。`, savedCount ? "success" : "warning");
}

function exportFilteredAssetPackage(filterId) {
  const collection = getAssetCollection();
  const manifest = buildAssetExportManifest(collection);
  const filter = manifest.filters.find((item) => item.id === filterId);
  if (!filter) return;
  const statusSet = new Set(filter.statuses);
  const savedExhibitions = (collection.savedExhibitions || []).filter((item) => statusSet.has(item.status || "draft"));
  const reportDrafts = (collection.reportDrafts || []).filter((item) => statusSet.has(item.status || "draft"));
  const filteredCollection = { savedExhibitions, reportDrafts };
  recordAssetAuditEvent({
    action: "filtered-export",
    label: `导出专题资产筛选包：${filter.label}`,
    detail: `${savedExhibitions.length} 个专题展，${reportDrafts.length} 份报告草稿`
  });
  captureAssetSnapshot(`筛选导出：${filter.label}`);
  const payload = {
    app: "AI 记忆博物馆",
    schemaVersion: SCHEMA_VERSION,
    phase: 15,
    exportMode: "phase15-assets-filtered",
    filter: filter.id,
    filterLabel: filter.label,
    exportedAt: new Date().toISOString(),
    savedExhibitions,
    reportDrafts,
    packageItems: buildAssetPackageItems(filteredCollection),
    ...buildPhase15AssetExportReview(filteredCollection)
  };
  downloadJsonPayload(payload, `memory-museum-assets-${filter.id}-${new Date().toISOString().slice(0, 10)}.json`);
  setStorageStatus(`已导出专题资产筛选包：${filter.label} ${savedExhibitions.length + reportDrafts.length} 个。`, "success");
}

function exportSelectedAssetSnapshot(snapshotId = selectedAssetSnapshotId) {
  const snapshot = snapshotId ? findAssetSnapshotById(snapshotId) : getSelectedAssetSnapshot();
  if (!snapshot) {
    setStorageStatus("当前还没有可导出的资产快照。", "warning");
    return;
  }
  selectedAssetSnapshotId = snapshot.id;
  const payload = {
    app: "AI 记忆博物馆",
    schemaVersion: SCHEMA_VERSION,
    phase: 15,
    exportMode: "phase15-asset-snapshot",
    exportedAt: new Date().toISOString(),
    snapshot,
    comparison: buildAssetSnapshotComparison(getAssetCollection(), snapshot),
    signature: buildAssetPackageSignature()
  };
  downloadJsonPayload(payload, `memory-museum-asset-snapshot-${new Date().toISOString().slice(0, 10)}.json`);
  recordAssetAuditEvent({
    action: "snapshot-export",
    label: "导出选中资产快照",
    detail: snapshot.reason || snapshot.id
  });
  setStorageStatus("已导出选中资产快照。", "success");
}

function exportLatestAssetSnapshot() {
  exportSelectedAssetSnapshot();
}

function formatReportSectionsInput(sections = []) {
  return (Array.isArray(sections) ? sections : [])
    .map((section) => `${section.title || "未命名章节"}：${section.text || ""}`.trim())
    .join("\n");
}

function parseReportSectionsInput(value = "") {
  return String(value || "")
    .split(/\n+/)
    .map((line) => line.trim())
    .filter(Boolean)
    .slice(0, 30)
    .map((line, index) => {
      const separator = line.includes("：") ? "：" : line.includes(":") ? ":" : "";
      if (!separator) return { title: `章节 ${index + 1}`, text: limitText(line, 800) };
      const [title, ...rest] = line.split(separator);
      return {
        title: limitText(title || `章节 ${index + 1}`, 80),
        text: limitText(rest.join(separator) || "", 800)
      };
    });
}

function formatAssetMemoryIdsInput(memoryIds = []) {
  return (Array.isArray(memoryIds) ? memoryIds : []).join("\n");
}

function getMemoryOptionLabel(id) {
  const memory = memories.find((item) => item.id === id);
  if (!memory) return id;
  return `${memory.title} / ${getHallName(memory.hall)}`;
}

function renderMemoryOptionItems() {
  return memories.map((memory) => `<option value="${escapeHtml(memory.id)}">${escapeHtml(`${memory.title} / ${getHallName(memory.hall)}`)}</option>`).join("");
}

function renderAssetSortList(memoryIds = []) {
  const ids = Array.isArray(memoryIds) ? memoryIds : [];
  if (!ids.length) return `<div class="asset-sort-empty">暂无展品排序。可从下拉框插入已保存展品。</div>`;
  return ids.map((id, index) => `
    <div class="asset-sort-row">
      <span>${index + 1}. ${escapeHtml(getMemoryOptionLabel(id))}</span>
      <button type="button" data-memory-sort-index="${index}" data-memory-sort-direction="up">上移</button>
      <button type="button" data-memory-sort-index="${index}" data-memory-sort-direction="down">下移</button>
    </div>
  `).join("");
}

function parseAssetMemoryIdsInput(value = "") {
  return [...new Set(String(value || "")
    .split(/[\n,，、\s]+/)
    .map((item) => limitText(item, 80))
    .filter(Boolean))]
    .slice(0, 80);
}

function formatReportReferencesInput(references = []) {
  return (Array.isArray(references) ? references : [])
    .map((item) => {
      const role = item.role || "引用";
      const title = item.title || item.id || "未命名展品";
      return `${role}：${title}${item.id ? `｜${item.id}` : ""}`;
    })
    .join("\n");
}

function parseReportReferencesInput(value = "") {
  return String(value || "")
    .split(/\n+/)
    .map((line) => line.trim())
    .filter(Boolean)
    .slice(0, 120)
    .map((line) => {
      const roleSeparator = line.includes("：") ? "：" : line.includes(":") ? ":" : "";
      const [rolePart, detailPart = ""] = roleSeparator ? line.split(roleSeparator) : ["引用", line];
      const detail = detailPart || rolePart;
      const assetSeparator = detail.includes("｜") ? "｜" : detail.includes("|") ? "|" : "";
      const [title, id = ""] = assetSeparator ? detail.split(assetSeparator) : [detail, ""];
      return {
        role: limitText(roleSeparator ? rolePart : "引用", 40),
        title: limitText(title, 120),
        id: limitText(id, 80)
      };
    });
}

function syncAssetSortList(form) {
  const textarea = form.querySelector("[name='memoryIds']");
  const target = form.querySelector("[data-asset-sort-list]");
  if (!textarea || !target) return;
  target.innerHTML = renderAssetSortList(parseAssetMemoryIdsInput(textarea.value));
}

function insertAssetMemoryFromPicker(form) {
  const select = form.querySelector("[data-memory-picker]");
  const textarea = form.querySelector("[name='memoryIds']");
  if (!select || !textarea || !select.value) return;
  const ids = parseAssetMemoryIdsInput(textarea.value);
  if (!ids.includes(select.value)) ids.push(select.value);
  textarea.value = formatAssetMemoryIdsInput(ids);
  syncAssetSortList(form);
  setAssetFormStatus(form, "已加入展品排序，保存后生效。", "editing");
}

function moveAssetMemoryLine(form, index, direction) {
  const textarea = form.querySelector("[name='memoryIds']");
  if (!textarea) return;
  const ids = parseAssetMemoryIdsInput(textarea.value);
  const nextIndex = direction === "up" ? index - 1 : index + 1;
  if (nextIndex < 0 || nextIndex >= ids.length) return;
  [ids[index], ids[nextIndex]] = [ids[nextIndex], ids[index]];
  textarea.value = formatAssetMemoryIdsInput(ids);
  syncAssetSortList(form);
  setAssetFormStatus(form, "展品顺序已调整，保存后生效。", "editing");
}

function insertReportReferenceFromPicker(form) {
  const select = form.querySelector("[data-reference-picker]");
  const textarea = form.querySelector("[name='references']");
  const memory = memories.find((item) => item.id === select?.value);
  if (!select || !textarea || !memory) return;
  const line = `引用：${memory.title}｜${memory.id}`;
  const current = textarea.value.trim();
  if (!current.includes(memory.id)) {
    textarea.value = current ? `${current}\n${line}` : line;
  }
  setAssetFormStatus(form, "已加入报告引用，保存后生效。", "editing");
}

function setAssetFormStatus(form, message, status = "neutral") {
  const target = form?.querySelector("[data-asset-save-status]");
  if (!target) return;
  target.textContent = message;
  target.dataset.status = status;
}

function buildAssetQualitySummary(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const knownMemoryIds = new Set(memories.map((memory) => memory.id));
  const missingCovers = exhibitions.filter((item) => !item.coverMemoryId || !knownMemoryIds.has(item.coverMemoryId)).length;
  const emptyExhibitions = exhibitions.filter((item) => !Array.isArray(item.memoryIds) || !item.memoryIds.length).length;
  const thinExhibitions = exhibitions.filter((item) => !String(item.intro || item.guideText || "").trim()).length;
  const reportsWithoutSections = reportDrafts.filter((item) => !Array.isArray(item.sections) || !item.sections.length).length;
  const reportsWithoutReferences = reportDrafts.filter((item) => !Array.isArray(item.references) || !item.references.length).length;
  const reviewAssets = [...exhibitions, ...reportDrafts].filter((item) => ["review", "published"].includes(item.status)).length;
  const draftAssets = [...exhibitions, ...reportDrafts].filter((item) => !item.status || item.status === "draft").length;
  const totalIssues = missingCovers + emptyExhibitions + thinExhibitions + reportsWithoutSections + reportsWithoutReferences;
  return {
    totalMemories: memories.length,
    exhibitionCount: exhibitions.length,
    reportCount: reportDrafts.length,
    reviewAssets,
    draftAssets,
    totalIssues,
    items: [
      { id: "cover", label: "封面可追溯", value: missingCovers, ok: missingCovers === 0, detail: missingCovers ? `${missingCovers} 个专题展缺少有效封面` : "专题展封面已关联到展品" },
      { id: "order", label: "展品排序", value: emptyExhibitions, ok: emptyExhibitions === 0, detail: emptyExhibitions ? `${emptyExhibitions} 个专题展还没有展品排序` : "专题展已具备展品顺序" },
      { id: "copy", label: "导览文本", value: thinExhibitions, ok: thinExhibitions === 0, detail: thinExhibitions ? `${thinExhibitions} 个专题展需要补充说明` : "专题说明和导览文案已就绪" },
      { id: "sections", label: "报告章节", value: reportsWithoutSections, ok: reportsWithoutSections === 0, detail: reportsWithoutSections ? `${reportsWithoutSections} 份报告缺少章节` : "报告章节结构已就绪" },
      { id: "references", label: "引用来源", value: reportsWithoutReferences, ok: reportsWithoutReferences === 0, detail: reportsWithoutReferences ? `${reportsWithoutReferences} 份报告缺少引用` : "报告引用来源已就绪" }
    ]
  };
}

function renderAssetQualitySummary(summary) {
  const readinessLabel = summary.totalIssues ? "需要整理" : "可以导出";
  return `
    <div class="asset-quality-panel" data-quality-ready="${summary.totalIssues ? "false" : "true"}">
      <div class="asset-quality-heading">
        <strong>${escapeHtml(readinessLabel)}</strong>
        <small>${summary.reviewAssets} 个复核/发布资产，${summary.draftAssets} 个草稿资产</small>
      </div>
      <div class="asset-quality-grid">
        ${summary.items.map((item) => `
          <span data-quality-status="${item.ok ? "ok" : "needs-work"}">
            <b>${escapeHtml(item.label)}</b>
            <small>${escapeHtml(item.detail)}</small>
          </span>
        `).join("")}
      </div>
    </div>
  `;
}

function buildAssetReferenceGraph(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const memoryLookup = new Map(memories.map((memory) => [memory.id, memory]));
  const rows = [];
  exhibitions.forEach((item) => {
    const ids = Array.isArray(item.memoryIds) ? item.memoryIds : [];
    rows.push({
      id: item.id,
      type: "专题展",
      title: item.title || "未命名专题展",
      targetCount: ids.length,
      targets: ids.slice(0, 5).map((id) => {
        const memory = memoryLookup.get(id);
        return memory ? `《${memory.title}》` : id;
      })
    });
  });
  reportDrafts.forEach((item) => {
    const refs = Array.isArray(item.references) ? item.references : [];
    rows.push({
      id: item.id,
      type: "报告",
      title: item.title || "未命名报告草稿",
      targetCount: refs.length,
      targets: refs.slice(0, 5).map((ref) => ref.title || ref.id || "未命名引用")
    });
  });
  return {
    totalLinks: rows.reduce((sum, row) => sum + row.targetCount, 0),
    rows: rows.slice(0, 8)
  };
}

function renderAssetReferenceGraph(graph) {
  if (!graph.rows.length) {
    return `<div class="asset-reference-map"><small>还没有专题展展品或报告引用关系。</small></div>`;
  }
  return `
    <div class="asset-reference-map">
      <div class="asset-reference-heading">
        <strong>引用关系图</strong>
        <small>${graph.totalLinks} 条展品/引用连接</small>
      </div>
      ${graph.rows.map((row) => `
        <div class="asset-reference-row">
          <span>${escapeHtml(row.type)}</span>
          <strong>${escapeHtml(row.title)}</strong>
          <small>${row.targets.length ? escapeHtml(row.targets.join("、")) : "等待补充引用对象"}</small>
        </div>
      `).join("")}
    </div>
  `;
}

function buildAssetRepairSuggestions(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const knownMemoryIds = new Set(memories.map((memory) => memory.id));
  const suggestions = [];
  exhibitions.forEach((item) => {
    const title = item.title || "未命名专题展";
    if (!item.coverMemoryId || !knownMemoryIds.has(item.coverMemoryId)) {
      suggestions.push({ id: item.id, assetType: "exhibition", type: "专题展", title, detail: "补一个有效封面展品 ID，让导出包更像完整展览。" });
    }
    if (!Array.isArray(item.memoryIds) || !item.memoryIds.length) {
      suggestions.push({ id: item.id, assetType: "exhibition", type: "专题展", title, detail: "加入至少一件展品，并用上移/下移整理观看顺序。" });
    }
    if (!String(item.intro || item.guideText || "").trim()) {
      suggestions.push({ id: item.id, assetType: "exhibition", type: "专题展", title, detail: "补一段专题说明或导览词，让回看时有温度和上下文。" });
    }
  });
  reportDrafts.forEach((item) => {
    const title = item.title || "未命名报告草稿";
    if (!Array.isArray(item.sections) || !item.sections.length) {
      suggestions.push({ id: item.id, assetType: "report", type: "报告", title, detail: "补充至少一个章节，形成可继续扩写的报告骨架。" });
    }
    if (!Array.isArray(item.references) || !item.references.length) {
      suggestions.push({ id: item.id, assetType: "report", type: "报告", title, detail: "插入引用展品，让报告能追溯到具体记忆。" });
    }
  });
  return suggestions.slice(0, 6);
}

function renderAssetRepairSuggestions(suggestions = []) {
  return `
    <div class="asset-repair-list">
      <div class="asset-reference-heading">
        <strong>草稿修复建议</strong>
        <small>${suggestions.length ? "按发布前优先级整理" : "暂无明显缺口"}</small>
      </div>
      ${suggestions.length ? suggestions.map((item) => `
        <div class="asset-repair-item">
          <span>${escapeHtml(item.type)}</span>
          <strong>${escapeHtml(item.title)}</strong>
          <small>${escapeHtml(item.detail)}</small>
          <button type="button" data-asset-open="${escapeHtml(item.id || "")}" data-asset-type="${escapeHtml(item.assetType || "exhibition")}">打开修复</button>
        </div>
      `).join("") : "<small>当前专题资产已经具备较完整的导出基础。</small>"}
    </div>
  `;
}

function getAssetStatusCounts(collection = getAssetCollection()) {
  const assets = [
    ...(Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : []),
    ...(Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [])
  ];
  return assets.reduce((counts, item) => {
    const status = item.status || "draft";
    counts[status] = (counts[status] || 0) + 1;
    return counts;
  }, { draft: 0, review: 0, published: 0, archived: 0 });
}

function getAssetIssueList(type, item) {
  const status = item.status || "draft";
  const validation = validateAssetPayload(type, { ...item, status });
  return [...validation.blocking, ...validation.warnings];
}

function buildAssetPackageItems(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  return [
    ...exhibitions.map((item) => ({
      id: item.id,
      type: "exhibition",
      typeLabel: "专题展",
      title: item.title || "未命名专题展",
      status: item.status || "draft",
      links: Array.isArray(item.memoryIds) ? item.memoryIds.length : 0,
      issues: getAssetIssueList("exhibition", item),
      updatedAt: item.updatedAt || item.createdAt || ""
    })),
    ...reportDrafts.map((item) => ({
      id: item.id,
      type: "report",
      typeLabel: "报告",
      title: item.title || "未命名报告草稿",
      status: item.status || "draft",
      links: Array.isArray(item.references) ? item.references.length : 0,
      issues: getAssetIssueList("report", item),
      updatedAt: item.updatedAt || item.createdAt || ""
    }))
  ];
}

function getBatchTransitionPreview(collection = getAssetCollection()) {
  const items = buildAssetPackageItems(collection);
  const transitions = [
    { from: "draft", to: "review", label: "草稿送复核" },
    { from: "review", to: "published", label: "复核发布" },
    { from: "published", to: "archived", label: "发布归档" }
  ];
  return transitions.map((transition) => {
    const candidates = items.filter((item) => item.status === transition.from);
    const ready = candidates.filter((item) => {
      const source = findAssetById(item.type, item.id, collection);
      return source && !validateAssetPayload(item.type, { ...source, status: transition.to }).blocking.length;
    }).length;
    return {
      ...transition,
      candidates: candidates.length,
      ready,
      blocked: candidates.length - ready
    };
  });
}

function findAssetById(type, id, collection = getAssetCollection()) {
  const list = type === "report" ? collection.reportDrafts : collection.savedExhibitions;
  return Array.isArray(list) ? list.find((item) => item.id === id) : null;
}

function loadAssetAuditLog() {
  try {
    const parsed = JSON.parse(localStorage.getItem(ASSET_AUDIT_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 80) : [];
  } catch (error) {
    return [];
  }
}

function persistAssetAuditLog(entries) {
  try {
    localStorage.setItem(ASSET_AUDIT_KEY, JSON.stringify(entries.slice(0, 80)));
  } catch (error) {
    console.warn("Asset audit log persist failed", error);
  }
}

function recordAssetAuditEvent(event) {
  const entries = loadAssetAuditLog();
  const next = [{
    id: `asset-audit-${Date.now()}-${Math.random().toString(16).slice(2, 8)}`,
    at: new Date().toISOString(),
    ...event
  }, ...entries];
  persistAssetAuditLog(next);
  return next[0];
}

function mergeImportedAssetAuditLog(entries = []) {
  const imported = Array.isArray(entries) ? entries.filter((item) => item && item.id && item.at) : [];
  if (!imported.length) return 0;
  const current = loadAssetAuditLog();
  const byId = new Map([...imported, ...current].map((item) => [item.id, item]));
  const merged = [...byId.values()].sort((a, b) => String(b.at || "").localeCompare(String(a.at || "")));
  persistAssetAuditLog(merged);
  return Math.min(imported.length, merged.length);
}

function loadAssetSnapshots() {
  try {
    const parsed = JSON.parse(localStorage.getItem(ASSET_SNAPSHOT_KEY) || "[]");
    return Array.isArray(parsed) ? parsed.slice(0, 30) : [];
  } catch (error) {
    return [];
  }
}

function persistAssetSnapshots(snapshots) {
  try {
    localStorage.setItem(ASSET_SNAPSHOT_KEY, JSON.stringify(snapshots.slice(0, 30)));
  } catch (error) {
    console.warn("Asset snapshots persist failed", error);
  }
}

function captureAssetSnapshot(reason, collection = getAssetCollection()) {
  const snapshot = {
    id: `asset-snapshot-${Date.now()}-${Math.random().toString(16).slice(2, 8)}`,
    at: new Date().toISOString(),
    reason,
    counts: getAssetStatusCounts(collection),
    quality: buildAssetQualitySummary(collection),
    packageComparison: buildAssetPackageComparison(collection),
    packageItems: buildAssetPackageItems(collection).slice(0, 20)
  };
  persistAssetSnapshots([snapshot, ...loadAssetSnapshots()]);
  return snapshot;
}

function findAssetSnapshotById(id) {
  return loadAssetSnapshots().find((snapshot) => snapshot.id === id) || null;
}

function getSelectedAssetSnapshot() {
  const snapshots = loadAssetSnapshots();
  if (!snapshots.length) {
    selectedAssetSnapshotId = "";
    return null;
  }
  const selected = snapshots.find((snapshot) => snapshot.id === selectedAssetSnapshotId);
  if (selected) return selected;
  selectedAssetSnapshotId = snapshots[0].id;
  return snapshots[0];
}

function mergeImportedAssetSnapshots(snapshots = []) {
  const imported = Array.isArray(snapshots) ? snapshots.filter((item) => item && item.id && item.at) : [];
  if (!imported.length) return 0;
  const current = loadAssetSnapshots();
  const byId = new Map([...imported, ...current].map((item) => [item.id, item]));
  const merged = [...byId.values()].sort((a, b) => String(b.at || "").localeCompare(String(a.at || "")));
  persistAssetSnapshots(merged);
  return Math.min(imported.length, merged.length);
}

function restorePhase15AssetMetadata(payload = {}) {
  const auditCount = mergeImportedAssetAuditLog(payload.phase15AssetAuditLog || payload.assetAuditLog || []);
  const snapshotCount = mergeImportedAssetSnapshots(payload.phase15AssetSnapshots || payload.assetSnapshots || []);
  const signature = verifyImportedAssetPackageSignature(payload);
  if (signature.checked) {
    recordAssetAuditEvent({
      action: "signature-verify",
      label: signature.ok ? "导入包签名校验通过" : "导入包签名校验异常",
      detail: `${signature.detail}${signature.repairSuggestions?.suggestions?.length ? `；建议：${signature.repairSuggestions.suggestions.join(" / ")}` : ""}`,
      severity: signature.severity || (signature.ok ? "ok" : "medium")
    });
  }
  return { auditCount, snapshotCount, signature };
}

function stableStringify(value) {
  if (Array.isArray(value)) return `[${value.map(stableStringify).join(",")}]`;
  if (value && typeof value === "object") {
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${stableStringify(value[key])}`).join(",")}}`;
  }
  return JSON.stringify(value);
}

function simpleChecksum(text = "") {
  let hash = 2166136261;
  for (let index = 0; index < text.length; index += 1) {
    hash ^= text.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0");
}

function buildAssetPackageSignature(collection = getAssetCollection()) {
  const payload = {
    schemaVersion: SCHEMA_VERSION,
    phase: 15,
    items: buildAssetPackageItems(collection).map((item) => ({
      id: item.id,
      type: item.type,
      title: item.title,
      status: item.status,
      links: item.links,
      issues: item.issues.length
    })),
    quality: buildAssetQualitySummary(collection).items.map((item) => ({ id: item.id, value: item.value })),
    comparison: buildAssetPackageComparison(collection)
  };
  return {
    algorithm: "fnv1a-lightweight",
    checksum: simpleChecksum(stableStringify(payload)),
    generatedAt: new Date().toISOString(),
    itemCount: payload.items.length
  };
}

function buildAssetSignatureAnomalyPlan(signature = {}) {
  const suggestions = [];
  if (!signature.checked) {
    suggestions.push("导入包未触发签名检查，先确认它是否为第十五阶段完整资产包。");
  } else if (signature.ok) {
    suggestions.push("签名一致，可继续按正常资产包归档。");
  } else if (!signature.expected) {
    suggestions.push("缺少 phase15PackageSignature，建议重新从第十五阶段完整导出入口生成资产包。");
    suggestions.push("如果这是旧版本包，先导入到测试环境，再导出新版带签名包。");
  } else {
    suggestions.push("对照 expected 与 actual 校验码，确认导入包没有被手动改写。");
    suggestions.push("优先使用最近快照导出包和审计搜索定位异常来源。");
  }
  return {
    severity: !signature.checked ? "info" : signature.ok ? "ok" : signature.expected ? "high" : "medium",
    expected: signature.expected || "",
    actual: signature.actual || "",
    suggestions
  };
}

function verifyImportedAssetPackageSignature(payload = {}) {
  const expected = payload.phase15PackageSignature?.checksum;
  const hasPhase15Assets = Array.isArray(payload.savedExhibitions) || Array.isArray(payload.reportDrafts) || Array.isArray(payload.phase15AssetSnapshots);
  if (!expected) {
    const signature = {
      checked: hasPhase15Assets,
      ok: false,
      expected: "",
      actual: "",
      severity: hasPhase15Assets ? "medium" : "info",
      detail: hasPhase15Assets ? "导入包没有发布包签名，建议重新导出新版资产包" : "导入包没有发布包签名"
    };
    return { ...signature, repairSuggestions: buildAssetSignatureAnomalyPlan(signature) };
  }
  const collection = {
    savedExhibitions: Array.isArray(payload.savedExhibitions) ? payload.savedExhibitions : [],
    reportDrafts: Array.isArray(payload.reportDrafts) ? payload.reportDrafts : []
  };
  const actual = buildAssetPackageSignature(collection).checksum;
  const signature = {
    checked: true,
    ok: actual === expected,
    expected,
    actual,
    severity: actual === expected ? "ok" : "high",
    detail: actual === expected ? "发布包签名一致" : `发布包签名不一致，expected=${expected}，actual=${actual}，建议人工复核`
  };
  return { ...signature, repairSuggestions: buildAssetSignatureAnomalyPlan(signature) };
}

function buildAssetAuditFilters(entries = loadAssetAuditLog()) {
  const groups = [
    { id: "status", label: "状态推进", actions: ["status-update", "batch-status-update"] },
    { id: "export", label: "导出记录", actions: ["full-export", "filtered-export"] },
    { id: "recovery", label: "恢复记录", actions: ["metadata-restore", "signature-verify"] }
  ];
  return groups.map((group) => {
    const items = entries.filter((entry) => group.actions.includes(entry.action));
    return {
      ...group,
      count: items.length,
      latestAt: items[0]?.at || "",
      latestLabel: items[0]?.label || ""
    };
  });
}

function buildAssetAuditSearch(query = assetAuditSearchTerm, entries = loadAssetAuditLog()) {
  const term = String(query || "").trim().toLowerCase();
  const matches = term
    ? entries.filter((entry) => [entry.action, entry.label, entry.detail, entry.id]
      .some((value) => String(value || "").toLowerCase().includes(term)))
    : entries.slice(0, 6);
  return {
    query: String(query || "").trim(),
    total: entries.length,
    count: matches.length,
    items: matches.slice(0, 8)
  };
}

function buildAssetSnapshotComparison(collection = getAssetCollection(), snapshot = getSelectedAssetSnapshot()) {
  const current = {
    counts: getAssetStatusCounts(collection),
    quality: buildAssetQualitySummary(collection),
    packageItems: buildAssetPackageItems(collection)
  };
  if (!snapshot) {
    return {
      hasSnapshot: false,
      label: "暂无快照可对比",
      countDelta: {},
      issueDelta: current.quality.totalIssues,
      itemDelta: current.packageItems.length
    };
  }
  const statusKeys = ["draft", "review", "published", "archived"];
  return {
    hasSnapshot: true,
    snapshotId: snapshot.id,
    label: snapshot.reason || "选中快照",
    at: snapshot.at,
    countDelta: statusKeys.reduce((result, key) => {
      result[key] = (current.counts[key] || 0) - (snapshot.counts?.[key] || 0);
      return result;
    }, {}),
    issueDelta: current.quality.totalIssues - (snapshot.quality?.totalIssues || 0),
    itemDelta: current.packageItems.length - (snapshot.packageItems?.length || 0)
  };
}

function buildAssetReleaseTimeline(collection = getAssetCollection()) {
  const packageItems = buildAssetPackageItems(collection);
  const statusEvents = packageItems
    .filter((item) => item.updatedAt)
    .map((item) => ({
      id: `asset-status-${item.id}`,
      at: item.updatedAt,
      action: "status-snapshot",
      label: `${item.typeLabel}《${item.title}》处于 ${item.status}`,
      detail: item.issues.length ? `${item.issues.length} 项仍需整理` : "当前可进入导出包"
    }));
  const auditEvents = loadAssetAuditLog().map((item) => ({
    id: item.id,
    at: item.at,
    action: item.action,
    label: item.label || "专题资产操作",
    detail: item.detail || ""
  }));
  return [...auditEvents, ...statusEvents]
    .sort((a, b) => String(b.at || "").localeCompare(String(a.at || "")))
    .slice(0, 8);
}

function buildAssetPackageComparison(collection = getAssetCollection()) {
  const items = buildAssetPackageItems(collection);
  const groups = [
    { id: "review-ready", label: "复核/发布", statuses: ["review", "published"] },
    { id: "drafts", label: "草稿", statuses: ["draft"] },
    { id: "archive", label: "归档", statuses: ["archived"] }
  ];
  return groups.map((group) => {
    const groupItems = items.filter((item) => group.statuses.includes(item.status));
    return {
      id: group.id,
      label: group.label,
      count: groupItems.length,
      links: groupItems.reduce((sum, item) => sum + item.links, 0),
      issues: groupItems.reduce((sum, item) => sum + item.issues.length, 0),
      ready: groupItems.filter((item) => !item.issues.length).length,
      sample: groupItems.slice(0, 3).map((item) => item.title)
    };
  });
}

function buildAssetPackageValidationSummary(collection = getAssetCollection()) {
  const quality = buildAssetQualitySummary(collection);
  const items = buildAssetPackageItems(collection);
  const auditCount = loadAssetAuditLog().length;
  const snapshotCount = loadAssetSnapshots().length;
  const publishedReady = items.filter((item) => ["review", "published"].includes(item.status) && !item.issues.length).length;
  const status = quality.totalIssues ? "needs-review" : "ready";
  return {
    status,
    totalAssets: items.length,
    publishedReady,
    issueCount: quality.totalIssues,
    auditCount,
    snapshotCount,
    generatedAt: new Date().toISOString(),
    checks: [
      { id: "quality", label: "质量缺口", ok: quality.totalIssues === 0, detail: quality.totalIssues ? `${quality.totalIssues} 个缺口` : "无明显缺口" },
      { id: "audit", label: "审计记录", ok: auditCount > 0, detail: auditCount ? `${auditCount} 条记录` : "尚无审计记录" },
      { id: "snapshot", label: "版本快照", ok: snapshotCount > 0, detail: snapshotCount ? `${snapshotCount} 个快照` : "尚无快照" },
      { id: "package", label: "资产明细", ok: items.length > 0, detail: `${items.length} 个专题资产` }
    ]
  };
}

function renderAssetPackageValidation(summary) {
  return `
    <div class="asset-package-validation" data-validation-status="${escapeHtml(summary.status)}">
      <div class="asset-reference-heading">
        <strong>发布包校验摘要</strong>
        <small>${summary.status === "ready" ? "可以归档" : "建议复核"}</small>
      </div>
      <div class="asset-validation-grid">
        ${summary.checks.map((check) => `
          <span data-validation-ok="${check.ok ? "true" : "false"}">
            <b>${escapeHtml(check.label)}</b>
            <small>${escapeHtml(check.detail)}</small>
          </span>
        `).join("")}
      </div>
    </div>
  `;
}

function renderAssetSnapshotList(snapshots = []) {
  const selected = getSelectedAssetSnapshot();
  return `
    <div class="asset-snapshot-list">
      <div class="asset-reference-heading">
        <strong>资产版本快照</strong>
        <small>${snapshots.length ? "可选择快照对比或导出" : "等待生成快照"}</small>
      </div>
      ${snapshots.length ? snapshots.slice(0, 4).map((snapshot) => `
        <div class="asset-snapshot-row" data-snapshot-selected="${selected?.id === snapshot.id ? "true" : "false"}">
          <span>${escapeHtml(formatDate(snapshot.at) || "刚刚")}</span>
          <strong>${escapeHtml(snapshot.reason || "资产快照")}</strong>
          <small>草稿 ${snapshot.counts?.draft || 0} / 复核 ${snapshot.counts?.review || 0} / 发布 ${snapshot.counts?.published || 0} / 归档 ${snapshot.counts?.archived || 0}</small>
          <div class="asset-row-actions">
            <button type="button" data-asset-snapshot-select="${escapeHtml(snapshot.id)}">${selected?.id === snapshot.id ? "已选中" : "对比"}</button>
            <button type="button" data-asset-snapshot-export="${escapeHtml(snapshot.id)}">导出</button>
          </div>
        </div>
      `).join("") : "<small>状态推进或导出后会留下快照，便于回看发布包变化。</small>"}
    </div>
  `;
}

function renderAssetSnapshotComparison(comparison) {
  const deltaText = comparison.hasSnapshot
    ? `草稿 ${comparison.countDelta.draft || 0} / 复核 ${comparison.countDelta.review || 0} / 发布 ${comparison.countDelta.published || 0} / 归档 ${comparison.countDelta.archived || 0}`
    : "还没有可对比快照";
  return `
    <div class="asset-snapshot-compare">
      <div class="asset-reference-heading">
        <strong>快照对比恢复</strong>
        <small>${comparison.hasSnapshot ? `当前状态对比：${escapeHtml(comparison.label)}` : "等待快照"}</small>
      </div>
      <div class="asset-snapshot-compare-grid">
        <span><b>状态变化</b><small>${escapeHtml(deltaText)}</small></span>
        <span><b>缺口变化</b><small>${comparison.issueDelta > 0 ? "+" : ""}${comparison.issueDelta}</small></span>
        <span><b>资产变化</b><small>${comparison.itemDelta > 0 ? "+" : ""}${comparison.itemDelta}</small></span>
      </div>
      <small class="asset-recovery-note">快照对比不会直接覆盖当前资产；它用于导出恢复参考、人工核对差异和定位异常来源。</small>
      <div class="asset-export-actions">
        <button type="button" data-asset-snapshot-export="${escapeHtml(comparison.snapshotId || "")}" ${comparison.hasSnapshot ? "" : "disabled"}>导出选中快照</button>
      </div>
    </div>
  `;
}

function renderAssetAuditSearch(search = buildAssetAuditSearch()) {
  return `
    <div class="asset-audit-search">
      <div class="asset-reference-heading">
        <strong>审计记录搜索</strong>
        <small>${search.query ? `${search.count}/${search.total} 条命中` : `最近 ${Math.min(search.items.length, search.total)} 条`}</small>
      </div>
      <div class="asset-audit-search-form">
        <input type="search" data-asset-audit-query value="${escapeHtml(search.query)}" placeholder="搜索动作、标题、详情或校验码">
        <button type="button" data-asset-audit-search>搜索</button>
        <button type="button" data-asset-audit-clear ${search.query ? "" : "disabled"}>清空</button>
      </div>
      <div class="asset-audit-search-list">
        ${search.items.length ? search.items.map((item) => `
          <div class="asset-audit-row" data-audit-severity="${escapeHtml(item.severity || "info")}">
            <span>${escapeHtml(formatDate(item.at) || "刚刚")}</span>
            <strong>${escapeHtml(item.label || item.action || "专题资产操作")}</strong>
            <small>${escapeHtml(item.detail || item.id || "")}</small>
          </div>
        `).join("") : "<small>没有找到匹配的审计记录。</small>"}
      </div>
    </div>
  `;
}

function renderAssetAuditFilters(filters = []) {
  return `
    <div class="asset-audit-filter">
      <div class="asset-reference-heading">
        <strong>审计记录筛选</strong>
        <small>按操作类型查看最近留痕</small>
      </div>
      <div class="asset-audit-filter-grid">
        ${filters.map((filter) => `
          <span>
            <b>${escapeHtml(filter.label)}</b>
            <small>${filter.count} 条记录</small>
            <small>${filter.latestLabel ? escapeHtml(filter.latestLabel) : "暂无记录"}</small>
          </span>
        `).join("")}
      </div>
    </div>
  `;
}

function renderAssetPackageSignature(signature, recovery = buildAssetSignatureAnomalyPlan({ checked: true, ok: true })) {
  return `
    <div class="asset-package-signature">
      <div class="asset-reference-heading">
        <strong>发布包签名校验</strong>
        <small>${escapeHtml(signature.algorithm)}</small>
      </div>
      <div class="asset-signature-line">
        <span>${escapeHtml(signature.checksum)}</span>
        <small>${signature.itemCount} 个资产 / ${escapeHtml(formatDate(signature.generatedAt) || "刚刚")}</small>
      </div>
      <div class="asset-signature-recovery" data-signature-severity="${escapeHtml(recovery.severity)}">
        ${recovery.suggestions.map((suggestion) => `<small>${escapeHtml(suggestion)}</small>`).join("")}
      </div>
    </div>
  `;
}

function getNextAssetStatus(status = "draft") {
  if (status === "draft") return { value: "review", label: "送复核" };
  if (status === "review") return { value: "published", label: "发布" };
  if (status === "published") return { value: "archived", label: "归档" };
  return { value: "draft", label: "转草稿" };
}

function renderAssetStatusAction(type, item) {
  const next = getNextAssetStatus(item.status || "draft");
  return `<button type="button" data-asset-status="${escapeHtml(item.id)}" data-asset-type="${escapeHtml(type)}" data-next-status="${escapeHtml(next.value)}">${escapeHtml(next.label)}</button>`;
}

function buildAssetExportManifest(collection = getAssetCollection()) {
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const counts = getAssetStatusCounts(collection);
  const items = buildAssetPackageItems(collection);
  const transitionPreview = getBatchTransitionPreview(collection);
  const releaseTimeline = buildAssetReleaseTimeline(collection);
  const packageComparison = buildAssetPackageComparison(collection);
  const packageValidation = buildAssetPackageValidationSummary(collection);
  const snapshots = loadAssetSnapshots();
  const snapshotComparison = buildAssetSnapshotComparison(collection);
  const auditFilters = buildAssetAuditFilters();
  const auditSearch = buildAssetAuditSearch();
  const packageSignature = buildAssetPackageSignature(collection);
  const signatureRecovery = buildAssetSignatureAnomalyPlan({ checked: true, ok: true });
  return {
    counts,
    items,
    transitionPreview,
    releaseTimeline,
    packageComparison,
    packageValidation,
    snapshots,
    snapshotComparison,
    auditFilters,
    auditSearch,
    packageSignature,
    signatureRecovery,
    filters: [
      { id: "review-ready", label: "复核/发布资产", statuses: ["review", "published"], count: counts.review + counts.published },
      { id: "drafts", label: "草稿资产", statuses: ["draft"], count: counts.draft },
      { id: "archive", label: "归档资产", statuses: ["archived"], count: counts.archived }
    ],
    total: exhibitions.length + reportDrafts.length
  };
}

function renderAssetReleaseTimeline(timeline = []) {
  return `
    <div class="asset-release-timeline">
      <div class="asset-reference-heading">
        <strong>发布记录时间线</strong>
        <small>${timeline.length ? "最近资产状态与批量操作" : "等待发布操作"}</small>
      </div>
      ${timeline.length ? timeline.map((item) => `
        <div class="asset-audit-row">
          <span>${escapeHtml(formatDate(item.at) || "刚刚")}</span>
          <strong>${escapeHtml(item.label || "专题资产操作")}</strong>
          <small>${escapeHtml(item.detail || item.action || "")}</small>
        </div>
      `).join("") : "<small>完成状态推进或筛选导出后，这里会留下轻量记录。</small>"}
    </div>
  `;
}

function renderAssetPackageComparison(comparison = []) {
  return `
    <div class="asset-package-compare">
      <div class="asset-reference-heading">
        <strong>资产包内容对比</strong>
        <small>按状态查看数量、连接和缺口</small>
      </div>
      <div class="asset-compare-grid">
        ${comparison.map((item) => `
          <span>
            <b>${escapeHtml(item.label)}</b>
            <small>${item.count} 个资产 / ${item.links} 条连接 / ${item.issues} 个缺口</small>
            <small>${item.sample.length ? escapeHtml(item.sample.join("、")) : "暂无资产"}</small>
          </span>
        `).join("")}
      </div>
    </div>
  `;
}

function renderAssetExportManifest(manifest) {
  return `
    <div class="asset-export-filter">
      <div class="asset-reference-heading">
        <strong>导出清单过滤</strong>
        <small>${manifest.total} 个专题资产</small>
      </div>
      <div class="asset-export-statuses">
        <span>草稿 ${manifest.counts.draft || 0}</span>
        <span>复核 ${manifest.counts.review || 0}</span>
        <span>发布 ${manifest.counts.published || 0}</span>
        <span>归档 ${manifest.counts.archived || 0}</span>
      </div>
      <div class="asset-export-actions">
        ${manifest.filters.map((filter) => `
          <button type="button" data-asset-export-filter="${escapeHtml(filter.id)}" ${filter.count ? "" : "disabled"}>
            ${escapeHtml(filter.label)} ${filter.count}
          </button>
        `).join("")}
      </div>
      <div class="asset-batch-actions">
        ${manifest.transitionPreview.map((item) => `
          <button type="button" data-asset-batch-from="${escapeHtml(item.from)}" data-asset-batch-to="${escapeHtml(item.to)}" ${item.ready ? "" : "disabled"}>
            ${escapeHtml(item.label)} ${item.ready}/${item.candidates}
          </button>
        `).join("")}
      </div>
      <div class="asset-diff-preview">
        ${manifest.transitionPreview.map((item) => `
          <span>${escapeHtml(item.label)}：可推进 ${item.ready}，需修复 ${item.blocked}</span>
        `).join("")}
      </div>
      <div class="asset-package-list">
        ${manifest.items.slice(0, 8).map((item) => `
          <div class="asset-package-row">
            <span>${escapeHtml(item.typeLabel)}</span>
            <strong>${escapeHtml(item.title)}</strong>
            <small>${escapeHtml(item.status)} / 连接 ${item.links} / ${item.issues.length ? `${item.issues.length} 项待整理` : "可进入导出包"}</small>
            <button type="button" data-asset-open="${escapeHtml(item.id)}" data-asset-type="${escapeHtml(item.type)}">打开</button>
          </div>
        `).join("")}
      </div>
      ${renderAssetPackageComparison(manifest.packageComparison)}
      ${renderAssetPackageValidation(manifest.packageValidation)}
      <details class="asset-advanced-tools">
        <summary>
          <span>高级审计、快照与签名</span>
          <small>出现导入异常或发布复核时再展开</small>
        </summary>
        ${renderAssetReleaseTimeline(manifest.releaseTimeline)}
        ${renderAssetAuditFilters(manifest.auditFilters)}
        ${renderAssetAuditSearch(manifest.auditSearch)}
        ${renderAssetSnapshotList(manifest.snapshots)}
        ${renderAssetSnapshotComparison(manifest.snapshotComparison)}
        ${renderAssetPackageSignature(manifest.packageSignature, manifest.signatureRecovery)}
      </details>
    </div>
  `;
}

function validateAssetPayload(type, payload) {
  const blocking = [];
  const warnings = [];
  const status = payload.status || "draft";
  const needsReviewQuality = ["review", "published"].includes(status);
  if (!String(payload.title || "").trim()) blocking.push("请先填写标题。");
  if (type === "report") {
    if (!Array.isArray(payload.sections) || !payload.sections.length) {
      (needsReviewQuality ? blocking : warnings).push("报告还没有章节。");
    }
    if (!Array.isArray(payload.references) || !payload.references.length) {
      (needsReviewQuality ? blocking : warnings).push("报告还没有引用来源。");
    }
  } else {
    if (!Array.isArray(payload.memoryIds) || !payload.memoryIds.length) {
      (needsReviewQuality ? blocking : warnings).push("专题展还没有展品排序。");
    }
    if (!payload.coverMemoryId) warnings.push("建议补充封面展品 ID，导出包会更完整。");
    if (!String(payload.intro || payload.guideText || "").trim()) warnings.push("建议补充专题说明或导览词。");
  }
  return { blocking, warnings };
}

function exportActiveAsset() {
  if (!activeAssetDialog?.asset?.id) return;
  const payload = {
    schemaVersion: SCHEMA_VERSION,
    phase: 15,
    assetType: activeAssetDialog.type,
    exportedAt: new Date().toISOString(),
    asset: activeAssetDialog.asset
  };
  downloadJsonPayload(payload, `memory-museum-${activeAssetDialog.type}-${activeAssetDialog.asset.id}.json`);
  setStorageStatus(`已导出专题资产预览：${activeAssetDialog.asset.title || activeAssetDialog.asset.id}`, "success");
}

async function saveSavedAssetEdits(form) {
  if (!databaseAvailable || !activeAssetDialog?.asset?.id) {
    setStorageStatus("需要先连接 Node 后端和 SQLite，才能保存专题资产编辑。", "warning");
    return;
  }
  const type = activeAssetDialog.type;
  const asset = activeAssetDialog.asset;
  const formData = new FormData(form);
  const endpoint = type === "report" ? API_REPORT_DRAFTS : API_EXHIBITIONS;
  const title = limitText(String(formData.get("title") || "").trim(), 120);
  const payload = type === "report"
    ? {
      ...asset,
      title,
      status: formData.get("status"),
      sections: parseReportSectionsInput(formData.get("sections")),
      references: parseReportReferencesInput(formData.get("references"))
    }
    : {
      ...asset,
      title,
      status: formData.get("status"),
      intro: limitText(formData.get("intro"), 800),
      guideText: limitText(formData.get("guideText"), 2000),
      tags: normalizeList(formData.get("tags")),
      coverMemoryId: limitText(formData.get("coverMemoryId"), 80),
      memoryIds: parseAssetMemoryIdsInput(formData.get("memoryIds")),
      sort: parseAssetMemoryIdsInput(formData.get("memoryIds"))
    };
  const validation = validateAssetPayload(type, payload);
  if (validation.blocking.length) {
    setAssetFormStatus(form, validation.blocking.join(" "), "error");
    return;
  }
  if (validation.warnings.length) {
    setAssetFormStatus(form, validation.warnings.join(" "), "editing");
  }
  const saveMessage = validation.warnings.length
    ? `正在保存到 SQLite... 另有 ${validation.warnings.length} 项草稿提示。`
    : "正在保存到 SQLite...";
  setAssetFormStatus(form, saveMessage, "saving");
  setStorageStatus(`正在保存专题资产编辑：${payload.title || asset.id}`, "loading");
  try {
    const response = await requestJson(endpoint, {
      method: "POST",
      body: JSON.stringify(payload)
    });
    const saved = type === "report" ? response.reportDraft : response.savedExhibition;
    activeAssetDialog = { type, asset: saved };
    await syncAssetCollection({ quiet: true });
    setStorageStatus(`专题资产编辑已保存：${saved?.title || payload.title}`, "success");
    setAssetFormStatus(form, validation.warnings.length ? `已保存，仍建议处理：${validation.warnings.join(" ")}` : "已保存。", validation.warnings.length ? "editing" : "saved");
    renderSavedAssetDialog(type, saved);
  } catch (error) {
    setStorageStatus(`专题资产编辑保存失败：${error.message}`, "warning");
    setAssetFormStatus(form, `保存失败：${error.message}`, "error");
  }
}

async function loadMemoriesFromDatabase({ silent = false } = {}) {
  if (!silent) setStorageStatus("正在从 SQLite 数据库读取展品...", "loading");
  const payload = await requestJson(API_MEMORIES);
  const remoteMemories = Array.isArray(payload.memories) ? payload.memories.map(normalizeMemory) : [];
  databaseAvailable = true;
  const remoteIds = new Set(remoteMemories.map((memory) => memory.id));
  const localOnlyMemories = memories.filter((memory) => !remoteIds.has(memory.id));

  if (remoteMemories.length === 0 && memories.length > 0) {
    databaseNeedsMigration = true;
    setStorageStatus(`数据库已连接但暂为空：当前显示 ${memories.length} 件本地备份，可点击“迁移本地”写入 SQLite。`, "warning");
    render();
    return remoteMemories;
  }

  if (remoteMemories.length > 0 && localOnlyMemories.length > 0) {
    databaseNeedsMigration = true;
    memories = [...localOnlyMemories, ...remoteMemories];
    persistMemories(memories);
    setStorageStatus(`数据库已连接：发现 ${localOnlyMemories.length} 件只在本地备份中的展品，当前已合并显示，可点击“迁移本地”补写入 SQLite。`, "warning");
    render();
    return remoteMemories;
  }

  databaseNeedsMigration = false;
  memories = remoteMemories;
  persistMemories(remoteMemories);
  setStorageStatus(`数据库已连接：SQLite 中有 ${remoteMemories.length} 件展品，本地保留备份。`, "success");
  render();
  return remoteMemories;
}

async function initializeStorage() {
  try {
    await loadMemoriesFromDatabase();
    await syncWorkflowBlueprint({ quiet: true });
    await syncPrivacyPolicy({ quiet: true });
    await syncVersionInfo({ quiet: true });
    await syncAssetCollection({ quiet: true });
    renderWorkflowOrchestration();
    renderPrivacyPanel();
    renderOperationsPanel();
    renderPhase20PlatformPanel();
    renderAssetCollectionPanel();
    renderPhase19ImportLab();
  } catch (error) {
    databaseAvailable = false;
    databaseNeedsMigration = false;
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = "local";
    privacyPolicy = null;
    privacyPolicySource = "local";
    versionInfo = null;
    operationsSource = "local";
    assetCollection = null;
    assetSource = "local";
    setStorageStatus(`数据库未连接：正在使用浏览器本地备份。${error.message ? `原因：${error.message}` : ""}`, "warning");
    render();
  }
}

async function saveMemoryToDatabase(memory, existingId = "") {
  const method = existingId ? "PUT" : "POST";
  const url = existingId ? `${API_MEMORIES}/${encodeURIComponent(existingId)}` : API_MEMORIES;
  const agentWorkflow = latestAgentWorkflow?.run?.id === memory.agentRunId ? latestAgentWorkflow : null;
  const payload = await requestJson(url, {
    method,
    body: JSON.stringify(agentWorkflow ? { ...memory, agentWorkflow } : memory)
  });
  return normalizeMemory(payload.memory);
}

async function fetchAgentRunForMemory(memoryId) {
  const payload = await requestJson(`${API_MEMORIES}/${encodeURIComponent(memoryId)}/agent-run`);
  return payload.run || null;
}

async function deleteMemoryFromDatabase(id) {
  await requestJson(`${API_MEMORIES}/${encodeURIComponent(id)}`, { method: "DELETE" });
}

async function purgeDatabaseMemories() {
  return requestJson(`${API_MEMORIES}/purge`, {
    method: "DELETE",
    body: JSON.stringify({ confirm: "DELETE" })
  });
}

async function importMemoriesToDatabase(items) {
  const payload = await requestJson(`${API_MEMORIES}/import`, {
    method: "POST",
    body: JSON.stringify({ memories: items })
  });
  return {
    imported: payload.imported || 0,
    importedIds: Array.isArray(payload.importedIds) ? payload.importedIds : items.map((item) => item.id).filter(Boolean),
    memories: Array.isArray(payload.memories) ? payload.memories.map(normalizeMemory) : []
  };
}

async function migrateCurrentLocalBackup({ quiet = false } = {}) {
  const localMemories = memories.map(normalizeMemory);
  if (localMemories.length === 0) {
    databaseNeedsMigration = false;
    return { imported: 0, memories: [] };
  }

  if (!quiet) setStorageStatus(`正在把 ${localMemories.length} 件本地备份写入 SQLite...`, "loading");
  const result = await importMemoriesToDatabase(localMemories);
  databaseAvailable = true;
  databaseNeedsMigration = false;
  memories = result.memories;
  persistMemories(memories);
  return result;
}

async function checkAiBackend() {
  try {
    const response = await fetch("/api/health");
    if (!response.ok) throw new Error("health check failed");
    const payload = await response.json();
    backendStats = payload.database?.stats || null;
    backendAiConfigured = Boolean(payload.aiConfigured);
    renderStats();
    if (payload.aiConfigured) {
      setAiStatus(`后端已连接：Agent 工作流可用，AI 模型 ${payload.model} 已配置。`, "success");
    } else {
      setAiStatus("后端已连接：未配置 AI_API_KEY，分析时会使用后端 Mock Agent 工作流回退。", "warning");
    }
  } catch {
    backendAiConfigured = false;
    setAiStatus("未连接 Node 后端：当前会使用前端本地 Agent 工作流。运行 npm start 可启用 AI 与数据库接口。", "warning");
  }
}

function updateEmotionIntensityOutput() {
  elements.emotionIntensityOutput.textContent = `${elements.emotionIntensity.value} / 5`;
  renderDraftPreview();
}

function updateRawCount() {
  elements.rawCount.textContent = `${elements.rawContent.value.length} / ${fieldLimits.rawContent}`;
  renderDraftPreview();
}

function renderMemories() {
  const list = filteredMemories();

  if (list.length === 0) {
    const hint = searchKeyword ? "没有搜到匹配展品。可以换个关键词，或清空搜索后再看。" : "先放入第一件记忆展品。它会像一张展签一样留在这里，之后还能进入主题展、报告和发布包。";
    const actionText = searchKeyword ? "清空筛选后再看" : "去添加记忆";
    elements.memoryGrid.innerHTML = `
      <div class="empty-state museum-empty-state">
        <span class="empty-state-mark" aria-hidden="true"></span>
        <strong>${escapeHtml(searchKeyword ? "暂时没有匹配展品" : "展品墙等待第一束灯光")}</strong>
        <small>${escapeHtml(hint)}</small>
        <button type="button" data-empty-action>${escapeHtml(actionText)}</button>
      </div>
    `;
    elements.memoryGrid.querySelector("[data-empty-action]")?.addEventListener("click", () => {
      if (searchKeyword) {
        clearCollectionFilters();
        return;
      }
      elements.rawContent?.focus();
      elements.memoryForm?.scrollIntoView({ behavior: "smooth", block: "start" });
    });
    return;
  }

  elements.memoryGrid.innerHTML = list.map((memory) => {
    const tags = [...(memory.emotions || []).slice(0, 3), ...(memory.tags || []).slice(0, 3)]
      .map((tag) => pillHtml(tag))
      .join("");
    const dateOrCreated = memory.date || formatDate(memory.createdAt);
    const archiveNumber = getArchiveNumber(memory);
    const attachmentCount = normalizeAttachments(memory.attachments).length;
    const attachmentTypeSummary = formatAttachmentTypeSummary(memory.attachments);

    return `
      <article class="memory-card ${editingId === memory.id ? "is-editing" : ""}">
        <span class="card-stamp">${escapeHtml(archiveNumber)}</span>
        <div class="card-meta">
          ${pillHtml(getHallName(memory.hall), "hall-pill")}
          ${memory.favorite ? pillHtml("重点", "favorite-pill") : ""}
          ${memory.agentRunId ? pillHtml("有整理历史", "history-pill") : ""}
          ${memory.coverImage ? pillHtml("封面线索", "media-pill") : ""}
          ${attachmentCount ? pillHtml(`${attachmentCount} 个附件`, "media-pill") : ""}
          ${memory.location ? pillHtml(memory.location) : ""}
          ${dateOrCreated ? pillHtml(dateOrCreated) : ""}
        </div>
        <h4>${escapeHtml(memory.title)}</h4>
        <p>${escapeHtml(memory.exhibitText)}</p>
        ${memory.mediaNote ? `<p class="media-note-preview">${escapeHtml(limitText(memory.mediaNote, 86))}</p>` : ""}
        ${attachmentTypeSummary ? `<div class="media-type-strip"><span>附件类型</span><strong>${escapeHtml(attachmentTypeSummary)}</strong></div>` : ""}
        <div class="card-facts">
          <span>${escapeHtml(memory.sourceType)}</span>
          <span>${escapeHtml(getImportanceLabel(memory.importance))}</span>
          <span>情绪强度 ${escapeHtml(memory.emotionIntensity)} / 5</span>
        </div>
        <div class="tag-row">${tags}</div>
        <div class="card-actions">
          <button type="button" data-view="${memory.id}">查看详情</button>
          <button type="button" data-edit="${memory.id}">编辑</button>
          <button type="button" class="delete-button" data-delete="${memory.id}">删除</button>
        </div>
      </article>
    `;
  }).join("");

  elements.memoryGrid.querySelectorAll("[data-view]").forEach((button) => {
    button.addEventListener("click", () => openMemory(button.dataset.view));
  });

  elements.memoryGrid.querySelectorAll("[data-edit]").forEach((button) => {
    button.addEventListener("click", () => startEdit(button.dataset.edit));
  });

  elements.memoryGrid.querySelectorAll("[data-delete]").forEach((button) => {
    button.addEventListener("click", () => deleteMemory(button.dataset.delete));
  });
}

function getArchiveNumber(memory) {
  const index = memories.findIndex((item) => item.id === memory.id);
  return `MM-${String(index + 1).padStart(4, "0")}`;
}

function renderGuide(text) {
  if (text) {
    elements.guideCopy.textContent = text;
    return;
  }

  const currentHall = halls.find((hall) => hall.id === activeHall) || halls[0];
  const currentMemories = filteredMemories();
  const featured = currentMemories[0] ? `\n\n当前推荐先看《${currentMemories[0].title}》。` : "";
  const favoriteCount = currentMemories.filter((memory) => memory.favorite).length;
  const averageIntensity = currentMemories.length
    ? (currentMemories.reduce((sum, memory) => sum + (memory.emotionIntensity || 1), 0) / currentMemories.length).toFixed(1)
    : "0";
  const storageName = databaseAvailable ? "SQLite 数据库" : "浏览器本地备份";
  const runCount = currentMemories.filter((memory) => memory.agentRunId).length;
  const phase10 = buildPhase10Handoff(currentMemories);
  const phase10Text = `第十阶段交接线索：${phase10.timelineReady} 件可进入时间线，${phase10.themeReady} 件具备主题展线索，${phase10.multimodalEvidence} 件带多模态证据。`;
  elements.guideCopy.textContent = `欢迎来到${currentHall.name}。\n\n这里现在有 ${currentMemories.length} 件展品，其中 ${favoriteCount} 件被标记为重点展品，平均情绪强度 ${averageIntensity} / 5。当前使用${storageName}，其中 ${runCount} 件展品已经带有 Agent 整理历史。\n\n第十阶段会把已保存展品整理为时间线、主题展和回忆报告草稿，并继续参考封面图、附件清单、OCR 和语音转写描述。\n\n${phase10Text}${featured}`;
}

function renderInsights() {
  const source = insightScope === "filtered"
    ? filteredMemories()
    : insightScope === "hall" && activeHall !== "all"
      ? memories.filter((memory) => memory.hall === activeHall)
      : memories;
  const insights = buildPhase10Insights(source);
  const longTermAgent = buildPhase18LongTermAgent(source);
  const scopeLabel = insightScope === "filtered"
    ? `基于当前筛选结果：${source.length} / ${memories.length} 件`
    : insightScope === "hall" && activeHall !== "all"
      ? `基于${getHallName(activeHall)}：${source.length} / ${memories.length} 件`
      : `基于全馆展品：${source.length} 件`;
  elements.insightScopeSelect.value = insightScope;
  elements.insightScopeMeta.textContent = scopeLabel;
  elements.timelineCount.textContent = `${insights.timeline.length} 个阶段`;
  elements.themeCount.textContent = `${insights.themes.length} 个主题`;
  elements.reportMeta.textContent = insights.report.total ? `${insights.report.total} 件展品` : "等待展品";

  elements.timelineList.innerHTML = insights.timeline.length
    ? insights.timeline.slice(0, 5).map((group) => `
      <div class="timeline-item">
        <span class="timeline-dot"></span>
        <div>
          <strong>${escapeHtml(group.label)}</strong>
          <p>${group.count} 件展品${group.mediaCount ? `，${group.mediaCount} 件带多模态线索` : ""}</p>
          <small>${escapeHtml(group.memories.map((memory) => `《${memory.title}》`).join("、"))}</small>
          <button type="button" data-timeline-search="${escapeHtml(group.memories[0]?.date || group.label)}">查看这一段</button>
        </div>
      </div>
    `).join("")
    : `<div class="insight-empty">保存带时间的展品后，这里会生成记忆时间线。</div>`;

  elements.themeList.innerHTML = insights.themes.length
    ? insights.themes.slice(0, 6).map((theme) => {
      const emotions = theme.topEmotions.map((item) => `${item.label} ${item.count}`).join(" / ");
      return `
        <div class="theme-item">
          <span>${escapeHtml(theme.type)}</span>
          <strong>${escapeHtml(theme.title)}</strong>
          <p>${escapeHtml(theme.description)}</p>
          <small>${theme.count} 件展品${theme.mediaCount ? ` / 多模态 ${theme.mediaCount}` : ""}${emotions ? ` / ${escapeHtml(emotions)}` : ""}</small>
          ${theme.coverMemory ? `<small>代表展品：${escapeHtml(`《${theme.coverMemory.title}》`)}</small>` : ""}
          <div class="theme-action-row">
            <button type="button" data-theme-search="${escapeHtml(theme.title)}">查看相关展品</button>
            <button type="button" data-theme-save="${escapeHtml(theme.title)}">保存为专题展</button>
          </div>
        </div>
      `;
    }).join("")
    : `<div class="insight-empty">补充人物、地点、标签或情绪后，这里会生成主题展候选。</div>`;

  const report = insights.report;
  const topTags = report.topTags.map((item) => `${item.label} ${item.count}`).join(" / ") || "暂无标签";
  const topPeople = report.topPeople.map((item) => `${item.label} ${item.count}`).join(" / ") || "暂无人物";
  const highlights = report.highlights.map((memory) => `《${memory.title}》`).join("、") || "暂无重点展品";
  const references = (report.references || []).map((memory) => `${memory.role}：《${memory.title}》`).join("；") || "暂无引用展品";
  const dateRange = report.dateRange?.start
    ? report.dateRange.start === report.dateRange.end ? report.dateRange.start : `${report.dateRange.start} 至 ${report.dateRange.end}`
    : "未形成时间范围";
  const coverage = `${insights.handoff.timelineReady}/${insights.handoff.total || 0} 时间线，${insights.handoff.themeReady}/${insights.handoff.total || 0} 主题，${insights.handoff.reportReady}/${insights.handoff.total || 0} 报告`;
  elements.reportPanel.innerHTML = `
    <p>${escapeHtml(report.summary)}</p>
    <div class="report-facts">
      <span>重点展品 ${report.favoriteCount}</span>
      <span>多模态 ${report.multimodalCount}</span>
      <span>时间 ${escapeHtml(dateRange)}</span>
      <span>覆盖 ${escapeHtml(coverage)}</span>
      <span>标签 ${escapeHtml(topTags)}</span>
      <span>人物 ${escapeHtml(topPeople)}</span>
    </div>
    <div class="report-sections">
      ${(report.sections || []).map((section) => `
        <section>
          <strong>${escapeHtml(section.title)}</strong>
          <small>${escapeHtml(section.text)}</small>
        </section>
      `).join("")}
    </div>
    <strong>报告开头候选</strong>
    <small>${escapeHtml(highlights)}</small>
    <strong>引用展品</strong>
    <small>${escapeHtml(references)}</small>
    <button type="button" class="asset-inline-action" data-report-draft>生成报告草稿</button>
  `;
  renderPhase18AgentPanel(longTermAgent);
  renderAssetCollectionPanel();
}

function renderPhase18AgentPanel(agent = buildPhase18LongTermAgent()) {
  if (!elements.phase18AgentPanel) return;
  const visibleSuggestions = agent.visibleSuggestions?.length ? agent.visibleSuggestions : agent.suggestions;
  const suggestionItems = visibleSuggestions.length
    ? visibleSuggestions.slice(0, 5).map((item) => `
      <div class="phase18-suggestion" data-priority="${escapeHtml(item.priority)}">
        <div>
          <strong>${escapeHtml(item.title)}</strong>
          <small>${escapeHtml(item.detail)}</small>
        </div>
        <small>${escapeHtml(item.type)} / ${escapeHtml(item.count)} 件相关展品 / ${escapeHtml(item.quality?.tier || "C")} 级建议 ${escapeHtml(item.quality?.score || 0)} 分</small>
        <small>${escapeHtml(item.quality?.reason || "")}</small>
        <div class="phase18-memory-links">${item.examples.map((memory) => `<button type="button" data-phase18-open="${escapeHtml(memory.id)}">${escapeHtml(memory.title)}</button>`).join("")}</div>
        <div class="phase18-actions">
          <button type="button" data-phase18-repair-draft="${escapeHtml(item.id)}" data-phase18-label="${escapeHtml(item.title)}">生成修复草案</button>
          <button type="button" data-phase18-feedback="accepted" data-phase18-target="${escapeHtml(item.id)}" data-phase18-label="${escapeHtml(item.title)}">采纳</button>
          <button type="button" data-phase18-feedback="dismissed" data-phase18-target="${escapeHtml(item.id)}" data-phase18-label="${escapeHtml(item.title)}">暂不处理</button>
        </div>
      </div>
    `).join("")
    : `<small>当前没有主动整理建议；继续补充展品后，长期助理会重新评估。</small>`;
  const relationItems = agent.relationships.clusters.length
    ? agent.relationships.clusters.slice(0, 5).map((item) => `
      <div class="phase18-relation">
        <strong>${escapeHtml(item.type)}：${escapeHtml(item.value)}</strong>
        <small>${escapeHtml(item.count)} 件展品 / ${escapeHtml(item.strength || "light")} 证据</small>
        <small>${escapeHtml(item.evidence || "")}</small>
        <small>${escapeHtml(item.memories.map((memory) => `《${memory.title}》`).join("、"))}</small>
        ${(item.assetLinks?.length || item.reportLinks?.length) ? `
          <div class="phase18-relation-assets">
            ${(item.assetLinks || []).map((asset) => `<button type="button" data-phase18-open-asset="${escapeHtml(asset.id)}">专题展：${escapeHtml(asset.title)}</button>`).join("")}
            ${(item.reportLinks || []).map((report) => `<button type="button" data-phase18-open-report="${escapeHtml(report.id)}">报告：${escapeHtml(report.title)}</button>`).join("")}
          </div>
        ` : `<small>${escapeHtml(agent.relationships.assetNavigation?.recommendation || "")}</small>`}
      </div>
    `).join("")
    : `<small>还没有稳定的跨展品关系。</small>`;
  const graph = filterPhase18RelationshipGraph(agent.relationships.graph || buildPhase18RelationshipGraph(agent.relationships.clusters || []));
  const graphNodes = graph.visibleNodes || graph.nodes || [];
  const graphEdges = graph.visibleEdges || graph.edges || [];
  const graphItems = graphNodes.length
    ? `
      <div class="phase18-graph">
        <div>
          <strong>关系图谱</strong>
          <small>${escapeHtml(graph.summary)}</small>
        </div>
        <div class="phase18-graph-filters">
          ${["all", "cluster", "memory", "asset", "report"].map((filter) => `<button type="button" data-phase18-graph-filter="${escapeHtml(filter)}" ${graph.filter === filter ? "aria-pressed=\"true\"" : ""}>${escapeHtml(filter === "all" ? "全部" : filter === "cluster" ? "关系" : filter === "memory" ? "展品" : filter === "asset" ? "专题展" : "报告")}</button>`).join("")}
        </div>
        <div class="phase18-graph-nodes">
          ${graphNodes.slice(0, 12).map((node) => {
            const memoryId = node.type === "memory" ? String(node.id || "").replace(/^memory-/, "") : "";
            const assetId = node.type === "asset" ? String(node.id || "").replace(/^asset-/, "") : "";
            const reportId = node.type === "report" ? String(node.id || "").replace(/^report-/, "") : "";
            return `<span data-node-type="${escapeHtml(node.type)}" ${memoryId ? `data-phase18-open="${escapeHtml(memoryId)}"` : ""} ${assetId ? `data-phase18-open-asset="${escapeHtml(assetId)}"` : ""} ${reportId ? `data-phase18-open-report="${escapeHtml(reportId)}"` : ""}>${escapeHtml(node.label)}</span>`;
          }).join("")}
        </div>
        <small>${escapeHtml(graphEdges.slice(0, 5).map((edge) => `${edge.label}:${edge.strength}`).join(" / ") || "等待关系边")}</small>
      </div>
    `
    : `<div class="phase18-graph"><strong>关系图谱</strong><small>等待更多人物、地点、标签或情绪线索。</small></div>`;
  const reviewItems = agent.periodicReviews.length
    ? agent.periodicReviews.slice(0, 4).map((item) => `
      <div class="phase18-review">
        <strong>${escapeHtml(item.label)}</strong>
        <small>${escapeHtml(item.count)} 件展品 / ${escapeHtml(item.topEmotions.map((emotion) => `${emotion.label} ${emotion.count}`).join(" / ") || "情绪待补充")}</small>
        <div class="phase18-review-actions">
          <button type="button" data-phase18-save-review="${escapeHtml(item.id)}">保存为专题展</button>
          <button type="button" data-phase18-save-review-report="${escapeHtml(item.id)}">生成报告草稿</button>
          ${item.assetLink?.exists ? `<button type="button" data-phase18-open-asset="${escapeHtml(item.assetLink.id)}">打开专题展</button>` : ""}
          ${item.reportLink?.exists ? `<button type="button" data-phase18-open-report="${escapeHtml(item.reportLink.id)}">打开报告</button>` : ""}
        </div>
      </div>
    `).join("")
    : `<small>带日期的展品还不足以形成周期回顾。</small>`;
  const repairItems = agent.repairDrafts.recent.length
    ? agent.repairDrafts.recent.map((draft) => `
      <div class="phase18-repair-row" data-repair-status="${escapeHtml(draft.status)}">
        <div>
          <strong>${escapeHtml(draft.memoryTitle || draft.memoryId)}</strong>
          <small>${escapeHtml(draft.reason || "")}</small>
        </div>
        <small>${Object.entries(draft.patch || {}).map(([key, value]) => `${key}: ${Array.isArray(value) ? value.join("、") : value}`).join(" / ")}</small>
        ${draft.status === "preview" ? `<button type="button" data-phase18-apply-repair="${escapeHtml(draft.id)}">应用草案</button>` : `<small>已应用</small>`}
      </div>
    `).join("")
    : `<small>还没有修复草案。点击建议里的“生成修复草案”后会在这里预览。</small>`;
  const batchReview = agent.repairDrafts.batchReview || buildPhase18RepairBatchReview();
  const batchReviewItems = batchReview.ready
    ? `
      <div class="phase18-batch-review">
        <div>
          <strong>批量应用前复核</strong>
          <small>${escapeHtml(batchReview.previewCount)} 条草案 / ${escapeHtml(batchReview.targetCount)} 件展品 / ${escapeHtml(batchReview.highRiskCount)} 条占位字段</small>
        </div>
        <small>字段：${escapeHtml(batchReview.fields.map((item) => `${item.label} ${item.count}`).join(" / ") || "待统计")}</small>
        <small>${escapeHtml(batchReview.policy)}</small>
        <div class="phase18-batch-examples">
          ${batchReview.recent.map((item) => `<span>${escapeHtml(item.memoryTitle)}：${escapeHtml(item.fields.join("、") || "字段待复核")}</span>`).join("")}
        </div>
        <button type="button" data-phase18-apply-repair-batch>批量应用预览草案</button>
      </div>
    `
    : `<div class="phase18-batch-review"><strong>批量应用前复核</strong><small>当前没有待应用的预览草案。</small></div>`;
  const auditItems = agent.taskAudit?.recent?.length
    ? agent.taskAudit.recent.slice(0, 5).map((item) => `
      <div class="phase18-audit-row">
        <strong>${escapeHtml(item.action)}</strong>
        <small>${escapeHtml(item.detail || item.targetId || "")}</small>
        <small>${escapeHtml(formatDateTime(item.at))}</small>
      </div>
    `).join("")
    : `<small>任务状态变更、草案生成和批量应用会在这里留下审计记录。</small>`;
  const selectedTaskIds = new Set(agent.taskQueue.selectedIds || []);
  const taskBatch = agent.taskQueue.batchReview || buildPhase18TaskBatchReview();
  const taskBatchPanel = `
    <div class="phase18-task-batch-panel">
      <div>
        <strong>批量任务处理</strong>
        <small>${escapeHtml(taskBatch.recommendation || "")}</small>
      </div>
      <div class="phase18-task-batch-summary">
        <span><b>${escapeHtml(taskBatch.selectedCount || 0)}</b><small>已选择</small></span>
        <span><b>${escapeHtml(taskBatch.activeCount || 0)}</b><small>活跃</small></span>
        <span><b>${escapeHtml(taskBatch.memoryCount || 0)}</b><small>关联展品</small></span>
        <span><b>${escapeHtml(taskBatch.draftableCount || 0)}</b><small>可生成草案</small></span>
      </div>
      <small>状态分布：等待 ${escapeHtml(taskBatch.statusCounts?.queued || 0)} / 复核 ${escapeHtml(taskBatch.statusCounts?.reviewing || 0)} / 完成 ${escapeHtml(taskBatch.statusCounts?.applied || 0)} / 忽略 ${escapeHtml(taskBatch.statusCounts?.dismissed || 0)} / 失败 ${escapeHtml(taskBatch.statusCounts?.failed || 0)}</small>
      <div class="phase18-task-batch-actions">
        <button type="button" data-phase18-task-select-visible>选择当前列表</button>
        <button type="button" data-phase18-task-selection-clear>清空选择</button>
        <button type="button" data-phase18-task-batch-status="reviewing" ${taskBatch.canBatch ? "" : "disabled"}>批量复核</button>
        <button type="button" data-phase18-task-batch-status="applied" ${taskBatch.canBatch ? "" : "disabled"}>批量完成</button>
        <button type="button" data-phase18-task-batch-status="dismissed" ${taskBatch.canBatch ? "" : "disabled"}>批量忽略</button>
        <button type="button" data-phase18-task-batch-status="failed" ${taskBatch.canBatch ? "" : "disabled"}>批量失败</button>
        <button type="button" data-phase18-task-batch-repair ${taskBatch.draftableCount ? "" : "disabled"}>批量生成草案</button>
      </div>
    </div>
  `;
  const taskItems = agent.taskQueue.visible.length
    ? agent.taskQueue.visible.map((task) => `
      <div class="phase18-task-row" data-task-status="${escapeHtml(task.status)}">
        <label class="phase18-task-select">
          <input type="checkbox" data-phase18-task-select="${escapeHtml(task.id)}" ${selectedTaskIds.has(task.id) ? "checked" : ""}>
          <span>选择</span>
        </label>
        <div>
          <strong>${escapeHtml(task.label)}</strong>
          <small>${escapeHtml(getPhase18TaskStatusLabel(task.status))} / ${escapeHtml(task.source)} / ${escapeHtml(task.memoryIds.length)} 件相关展品</small>
        </div>
        <small>${escapeHtml(task.detail || "")}</small>
        <div class="phase18-task-actions">
          ${getPhase18TaskNextActions(task.status).map((action) => `
            <button type="button" data-phase18-task-status="${escapeHtml(action.status)}" data-phase18-task-id="${escapeHtml(task.id)}">${escapeHtml(action.label)}</button>
          `).join("")}
        </div>
      </div>
    `).join("")
    : `<small>当前筛选下没有长期任务。</small>`;
  const quality = agent.agentQuality || buildPhase18AgentQuality();
  const dashboard = agent.reviewDashboard || {};
  const digest = agent.agentDigest || buildPhase18AgentDigest();
  const digestActions = digest.topActions?.length
    ? digest.topActions.map((item) => `
      <span>
        <b>${escapeHtml(item.label)}</b>
        <small>${escapeHtml(item.detail || "")}</small>
      </span>
    `).join("")
    : `<small>暂无明确动作，先继续补充展品线索。</small>`;
  const digestPanel = `
    <article class="phase18-agent-digest" data-readiness="${escapeHtml(digest.readiness || "warming-up")}">
      <div class="phase18-digest-heading">
        <strong>长期助理摘要</strong>
        <small>${escapeHtml(digest.readiness === "needs-attention" ? "需要先处理风险" : digest.readiness === "ready" ? "适合推进" : "继续升温")}</small>
      </div>
      <div class="phase18-digest-focus">
        <span><b>今日焦点</b><small>${escapeHtml(digest.todayFocus || "")}</small></span>
        <span><b>本周焦点</b><small>${escapeHtml(digest.weeklyFocus || "")}</small></span>
      </div>
      <div class="phase18-digest-strip">
        <span><b>${escapeHtml(digest.signals?.activeTasks || 0)}</b><small>活跃任务</small></span>
        <span><b>${escapeHtml(digest.signals?.relationClusters || 0)}</b><small>关系簇</small></span>
        <span><b>${escapeHtml(digest.signals?.reviewCandidates || 0)}</b><small>回顾候选</small></span>
        <span><b>${escapeHtml(digest.signals?.previewDrafts || 0)}</b><small>待审草案</small></span>
        <span><b>${escapeHtml(digest.signals?.assetSyncRisk || 0)}</b><small>同步风险</small></span>
      </div>
      <div class="phase18-digest-actions">${digestActions}</div>
      <small>${escapeHtml(digest.recommendation || "")}</small>
      <small>节奏：${escapeHtml(digest.cadence?.daily || "")} / ${escapeHtml(digest.cadence?.weekly || "")}</small>
      <button type="button" data-phase18-create-digest-task>生成摘要任务</button>
    </article>
  `;
  const reviewPanel = `
    <article class="phase18-review-dashboard">
      <strong>长期助理复盘</strong>
      <div class="phase18-dashboard-strip">
        <span><b>${escapeHtml(dashboard.qualityScore || 0)}</b><small>综合质量</small></span>
        <span><b>${escapeHtml(dashboard.openTasks || 0)}</b><small>活跃任务</small></span>
        <span><b>${escapeHtml(dashboard.quietSuggestions || 0)}</b><small>已降噪</small></span>
        <span><b>${escapeHtml(dashboard.reviewCoverage || 0)}%</b><small>回顾沉淀</small></span>
      </div>
      <small>${escapeHtml(dashboard.focus || "")}</small>
      <small>${escapeHtml(dashboard.recommendation || "")}</small>
      <small>最近审计：${escapeHtml(dashboard.latestAudit || "暂无审计记录")}</small>
      <button type="button" data-phase18-create-review-task>生成复盘任务</button>
    </article>
  `;
  const noiseConfig = agent.suggestionNoise?.config || getDefaultPhase18NoiseRuleConfig();
  const noisePreview = agent.suggestionNoise?.preview || {};
  const quietItems = agent.suggestionNoise?.quiet?.length
    ? agent.suggestionNoise.quiet.slice(0, 4).map((item) => `
      <span>
        <b>${escapeHtml(item.title)}</b>
        <small>${escapeHtml(item.tier)} / ${escapeHtml(item.score)} / ${escapeHtml(item.reason)}</small>
      </span>
    `).join("")
    : `<small>当前没有被规则收起的建议。</small>`;
  const recoverItems = noisePreview.recoverable?.length
    ? noisePreview.recoverable.map((item) => `<span>${escapeHtml(item.title)} ${escapeHtml(item.tier)} ${escapeHtml(item.score)}</span>`).join("")
    : `<small>暂无需要恢复复核的建议。</small>`;
  const noiseRulePanel = `
    <article class="phase18-noise-rule-panel">
      <div class="phase18-noise-rule-heading">
        <strong>降噪规则配置</strong>
        <small>${escapeHtml(noisePreview.recommendation || "")}</small>
      </div>
      <div class="phase18-noise-rule-controls">
        <button type="button" data-phase18-noise-toggle="hideLowTier" aria-pressed="${noiseConfig.hideLowTier ? "true" : "false"}">隐藏 C 级</button>
        <button type="button" data-phase18-noise-toggle="hideDismissed" aria-pressed="${noiseConfig.hideDismissed ? "true" : "false"}">隐藏已忽略</button>
        <button type="button" data-phase18-noise-toggle="hideWeakEvidence" aria-pressed="${noiseConfig.hideWeakEvidence ? "true" : "false"}">隐藏弱证据</button>
        <button type="button" data-phase18-noise-toggle="keepHighPriority" aria-pressed="${noiseConfig.keepHighPriority ? "true" : "false"}">保留高优先级</button>
        <button type="button" data-phase18-noise-score="-8">降低阈值</button>
        <button type="button" data-phase18-noise-score="8">提高阈值</button>
        <button type="button" data-phase18-noise-reset>恢复默认</button>
      </div>
      <div class="phase18-noise-preview">
        <span><b>${escapeHtml(noiseConfig.minVisibleScore)}</b><small>显示阈值</small></span>
        <span><b>${escapeHtml(noisePreview.visibleCount ?? 0)}</b><small>当前显示</small></span>
        <span><b>${escapeHtml(noisePreview.quietCount ?? 0)}</b><small>已收起</small></span>
        <span><b>${escapeHtml(noisePreview.recoverableCount ?? 0)}</b><small>建议复核</small></span>
      </div>
      <div class="phase18-noise-breakdown">
        <small>分布：分层 ${escapeHtml(noisePreview.hiddenByTier || 0)} / 忽略 ${escapeHtml(noisePreview.hiddenByFeedback || 0)} / 弱证据 ${escapeHtml(noisePreview.hiddenByWeakEvidence || 0)} / 分数 ${escapeHtml(noisePreview.hiddenByScore || 0)}</small>
      </div>
      <div class="phase18-noise-quiet">${quietItems}</div>
      <div class="phase18-noise-recover"><strong>可恢复复核</strong>${recoverItems}</div>
    </article>
  `;
  elements.phase18AgentPanel.innerHTML = `
    <div class="phase18-agent-heading">
      <div>
        <span>Phase 18 Agent</span>
        <strong>长期记忆助理</strong>
      </div>
      <small>${escapeHtml(agent.sourceCount)} 件展品 / ${escapeHtml(agent.suggestions.length)} 条建议 / ${escapeHtml(agent.relationships.clusterCount)} 组关系</small>
    </div>
    <div class="phase18-grid">
      ${digestPanel}
      ${reviewPanel}
      ${noiseRulePanel}
      <article>
        <strong>主动整理建议</strong>
        <small>${escapeHtml(agent.suggestionNoise?.recommendation || "")}</small>
        ${suggestionItems}
      </article>
      <article>
        <strong>跨展品关系</strong>
        ${relationItems}
        ${graphItems}
      </article>
      <article>
        <strong>周期回顾</strong>
        ${reviewItems}
      </article>
      <article class="phase18-task-panel">
        <div class="phase18-task-heading">
          <strong>长期任务队列</strong>
          <small>活跃 ${escapeHtml(agent.taskQueue.active)} / 已完成 ${escapeHtml(agent.taskQueue.applied)} / 已忽略 ${escapeHtml(agent.taskQueue.dismissed)}</small>
        </div>
        <div class="phase18-task-filters">
          ${["active", "queued", "reviewing", "failed", "applied", "dismissed", "all"].map((filter) => `
            <button type="button" data-phase18-task-filter="${escapeHtml(filter)}" ${agent.taskQueue.filter === filter ? "aria-pressed=\"true\"" : ""}>${escapeHtml(filter === "active" ? "活跃" : getPhase18TaskStatusLabel(filter))}</button>
          `).join("")}
          <button type="button" data-phase18-clear-resolved>清理已完成</button>
        </div>
        ${taskBatchPanel}
        ${taskItems}
      </article>
      <article>
        <strong>反馈闭环</strong>
        <small>采纳 ${escapeHtml(agent.feedbackLoop.accepted)} / 忽略 ${escapeHtml(agent.feedbackLoop.dismissed)}</small>
        <small>${escapeHtml(agent.nextStep)}</small>
        <div class="phase18-quality-strip">
          <span><b>${escapeHtml(quality.acceptanceRate)}%</b><small>采纳率</small></span>
          <span><b>${escapeHtml(quality.taskResolvedRate)}%</b><small>任务闭环</small></span>
          <span><b>${escapeHtml(quality.repairApplyRate)}%</b><small>草案应用</small></span>
        </div>
        <small>建议分层：A ${escapeHtml(agent.suggestionQuality?.high || 0)} / B ${escapeHtml(agent.suggestionQuality?.medium || 0)} / C ${escapeHtml(agent.suggestionQuality?.low || 0)}，均分 ${escapeHtml(agent.suggestionQuality?.averageScore || 0)}</small>
        <small>${escapeHtml(quality.recommendation || "")}</small>
        <div class="phase18-readiness">
          ${agent.readinessChecks.map((item) => `<span data-status="${escapeHtml(item.status)}"><b>${escapeHtml(item.label)}</b><small>${escapeHtml(item.detail)}</small></span>`).join("")}
        </div>
      </article>
      <article class="phase18-repair-panel">
        <strong>半自动修复草案</strong>
        <small>预览 ${escapeHtml(agent.repairDrafts.preview)} / 已应用 ${escapeHtml(agent.repairDrafts.applied)}</small>
        ${batchReviewItems}
        ${repairItems}
        ${agent.repairDrafts.total ? `<button type="button" class="phase18-clear-repairs" data-phase18-clear-repairs>清空草案</button>` : ""}
      </article>
      <article class="phase18-audit-panel">
        <strong>长期助理审计</strong>
        <small>共 ${escapeHtml(agent.taskAudit?.total || 0)} 条本地记录，用于回看任务状态和批量修复。</small>
        ${auditItems}
      </article>
    </div>
  `;
}

function renderAssetCollectionPanel(collection = getAssetCollection()) {
  if (!elements.savedAssetsPanel) return;
  const exhibitions = Array.isArray(collection.savedExhibitions) ? collection.savedExhibitions : [];
  const reportDrafts = Array.isArray(collection.reportDrafts) ? collection.reportDrafts : [];
  const qualitySummary = buildAssetQualitySummary(collection);
  const referenceGraph = buildAssetReferenceGraph(collection);
  const repairSuggestions = buildAssetRepairSuggestions(collection);
  const exportManifest = buildAssetExportManifest(collection);
  const assetSyncState = buildPhase18AssetSyncState(collection);
  const assetSyncMap = new Map(assetSyncState.items.map((item) => [`${item.type}:${item.id}`, item]));
  const relationshipNavigation = buildPhase18RelationshipMap(memories.map(normalizeMemory)).assetNavigation || {};
  const sourceLabel = assetSource === "server" ? "SQLite 资产库" : "等待后端连接";
  if (elements.assetSummaryMeta) {
    elements.assetSummaryMeta.textContent = `${sourceLabel} / ${exhibitions.length} 个专题展 / ${reportDrafts.length} 份报告`;
  }
  if (!exhibitions.length && !reportDrafts.length) {
    elements.savedAssetsPanel.innerHTML = `
      <div class="asset-empty">
        <strong>还没有长期资产</strong>
        <small>可以从主题展候选保存专题展，也可以把当前洞察生成回忆报告草稿。</small>
        <div class="asset-action-row">
          <button type="button" data-report-draft>生成报告草稿</button>
          <button type="button" data-assets-refresh>刷新资产库</button>
        </div>
      </div>
    `;
    return;
  }
  const exhibitionItems = exhibitions.slice(0, 4).map((item) => {
    const count = Array.isArray(item.memoryIds) ? item.memoryIds.length : 0;
    const tags = Array.isArray(item.tags) ? item.tags.slice(0, 3).join(" / ") : "";
    const sync = assetSyncMap.get(`exhibition:${item.id}`) || {};
    const relations = relationshipNavigation.assetLookup?.[item.id] || [];
    return `
      <article class="asset-item" data-sync-status="${escapeHtml(sync.syncStatus || "review")}">
        <span>${escapeHtml(item.status || "draft")}</span>
        <span class="asset-sync-badge">${escapeHtml(sync.syncStatus || "review")}</span>
        <strong>${escapeHtml(item.title || "未命名专题展")}</strong>
        <small>${count} 件展品${tags ? ` / ${escapeHtml(tags)}` : ""}</small>
        <small>${escapeHtml(sync.recommendation || "")}</small>
        ${relations.length ? `<div class="asset-relation-links">${relations.map((relation) => `<span>${escapeHtml(relation.label)} · ${escapeHtml(relation.overlapCount)} 件</span>`).join("")}</div>` : ""}
        <p>${escapeHtml(limitText(item.intro || item.guideText || "等待继续编辑专题展说明。", 92))}</p>
        <div class="asset-action-row">
          <button type="button" data-asset-open="${escapeHtml(item.id)}" data-asset-type="exhibition">打开</button>
          ${renderAssetStatusAction("exhibition", item)}
          <button type="button" data-asset-delete="${escapeHtml(item.id)}" data-asset-title="${escapeHtml(item.title || "未命名专题展")}" data-asset-type="exhibition">删除</button>
        </div>
      </article>
    `;
  }).join("");
  const reportItems = reportDrafts.slice(0, 4).map((item) => {
    const sectionCount = Array.isArray(item.sections) ? item.sections.length : 0;
    const referenceCount = Array.isArray(item.references) ? item.references.length : 0;
    const sync = assetSyncMap.get(`report:${item.id}`) || {};
    const relations = relationshipNavigation.reportLookup?.[item.id] || [];
    return `
      <article class="asset-item" data-sync-status="${escapeHtml(sync.syncStatus || "review")}">
        <span>${escapeHtml(item.status || "draft")}</span>
        <span class="asset-sync-badge">${escapeHtml(sync.syncStatus || "review")}</span>
        <strong>${escapeHtml(item.title || "未命名报告草稿")}</strong>
        <small>${sectionCount} 个章节 / ${referenceCount} 条引用</small>
        <small>${escapeHtml(sync.recommendation || "")}</small>
        ${relations.length ? `<div class="asset-relation-links">${relations.map((relation) => `<span>${escapeHtml(relation.label)} · ${escapeHtml(relation.overlapCount)} 件</span>`).join("")}</div>` : ""}
        <p>${escapeHtml(limitText(item.summary || item.notes || "等待继续编辑报告章节和引用来源。", 92))}</p>
        <div class="asset-action-row">
          <button type="button" data-asset-open="${escapeHtml(item.id)}" data-asset-type="report">打开</button>
          ${renderAssetStatusAction("report", item)}
          <button type="button" data-asset-delete="${escapeHtml(item.id)}" data-asset-title="${escapeHtml(item.title || "未命名报告草稿")}" data-asset-type="report">删除</button>
        </div>
      </article>
    `;
  }).join("");
  elements.savedAssetsPanel.innerHTML = `
    <div class="asset-summary-strip">
      <span><b>${exhibitions.length}</b>专题展</span>
      <span><b>${reportDrafts.length}</b>报告草稿</span>
      <span><b>${escapeHtml(sourceLabel)}</b>来源</span>
    </div>
    <div class="phase18-asset-sync-state" data-sync-readiness="${escapeHtml(assetSyncState.readiness)}">
      <div>
        <strong>第十八阶段资产同步状态</strong>
        <small>${escapeHtml(assetSyncState.recommendation)}</small>
      </div>
      <div class="phase18-asset-sync-grid">
        <span><b>${escapeHtml(assetSyncState.packageCandidates)}</b><small>可入同步包</small></span>
        <span><b>${escapeHtml(assetSyncState.ready)}</b><small>已发布</small></span>
        <span><b>${escapeHtml(assetSyncState.review)}</b><small>待复核</small></span>
        <span><b>${escapeHtml(assetSyncState.risk)}</b><small>有风险</small></span>
      </div>
    </div>
    <div class="asset-toolbar">
      <button type="button" data-assets-refresh>刷新资产库</button>
      <button type="button" data-report-draft>生成报告草稿</button>
    </div>
    <details class="asset-export-preview asset-package-preview">
      <summary>完整导出包预览</summary>
      <div class="asset-package-grid">
        <span><b>${qualitySummary.totalMemories}</b>展品</span>
        <span><b>${exhibitions.length}</b>专题展</span>
        <span><b>${reportDrafts.length}</b>报告草稿</span>
      </div>
      ${renderAssetQualitySummary(qualitySummary)}
      ${renderAssetReferenceGraph(referenceGraph)}
      ${renderAssetRepairSuggestions(repairSuggestions)}
      ${renderAssetExportManifest(exportManifest)}
      <small>完整导出仍使用页面底部“导出”按钮；这里用于发布前快速确认数量、引用来源和关键内容缺口。</small>
    </details>
    <div class="asset-list">
      ${exhibitionItems}
      ${reportItems}
    </div>
  `;
}

function renderSavedAssetDialog(type, asset = {}) {
  const isReport = type === "report";
  const title = asset.title || (isReport ? "未命名报告草稿" : "未命名专题展");
  const status = asset.status || "draft";
  const tags = Array.isArray(asset.tags) ? asset.tags.map((tag) => pillHtml(tag)).join("") : "";
  const tagText = Array.isArray(asset.tags) ? asset.tags.join("、") : "";
  const memoryIds = Array.isArray(asset.memoryIds) ? asset.memoryIds : [];
  const sections = Array.isArray(asset.sections) ? asset.sections : [];
  const references = Array.isArray(asset.references) ? asset.references : [];
  const scope = asset.scope ? JSON.stringify(asset.scope, null, 2) : "";
  activeAssetDialog = { type, asset };
  elements.dialogContent.innerHTML = `
    <div class="dialog-body asset-dialog-body">
      <p class="eyebrow">Phase 15 / ${isReport ? "回忆报告草稿" : "专题展资产"} / ${escapeHtml(status)}</p>
      <h3>${escapeHtml(title)}</h3>
      ${tags ? `<div class="tag-row">${tags}</div>` : ""}
      <form class="asset-edit-form" data-asset-edit-form data-asset-type="${escapeHtml(type)}">
        <div class="asset-edit-grid">
          <label>标题<input name="title" type="text" maxlength="120" value="${escapeHtml(title)}" /></label>
          <label>状态
            <select name="status">
              ${["draft", "review", "published", "archived"].map((value) => `<option value="${value}" ${status === value ? "selected" : ""}>${value}</option>`).join("")}
            </select>
          </label>
        </div>
        ${isReport ? `
          <label>报告章节<textarea name="sections" rows="6" maxlength="6000">${escapeHtml(formatReportSectionsInput(sections))}</textarea></label>
          <label>引用展品<textarea name="references" rows="4" maxlength="6000">${escapeHtml(formatReportReferencesInput(references))}</textarea></label>
          <div class="asset-picker-row">
            <select data-reference-picker aria-label="选择引用展品">
              <option value="">选择已保存展品作为引用</option>
              ${renderMemoryOptionItems()}
            </select>
            <button type="button" data-reference-insert>插入引用</button>
          </div>
          <small>章节格式为“标题：内容”；引用格式为“角色：标题｜展品ID”。洞察来源会保留。</small>
        ` : `
          <label>专题标签<input name="tags" type="text" maxlength="260" value="${escapeHtml(tagText)}" /></label>
          <div class="asset-edit-grid">
            <label>封面展品 ID<input name="coverMemoryId" type="text" maxlength="80" value="${escapeHtml(asset.coverMemoryId || "")}" /></label>
            <label>展品排序<textarea name="memoryIds" rows="4" maxlength="6400">${escapeHtml(formatAssetMemoryIdsInput(memoryIds))}</textarea></label>
          </div>
          <div class="asset-picker-row">
            <select data-memory-picker aria-label="选择专题展品">
              <option value="">选择已保存展品加入专题展</option>
              ${renderMemoryOptionItems()}
            </select>
            <button type="button" data-memory-insert>加入展品</button>
          </div>
          <div class="asset-sort-list" data-asset-sort-list>${renderAssetSortList(memoryIds)}</div>
          <label>专题说明<textarea name="intro" rows="4" maxlength="800">${escapeHtml(asset.intro || "")}</textarea></label>
          <label>导览词<textarea name="guideText" rows="5" maxlength="2000">${escapeHtml(asset.guideText || "")}</textarea></label>
        `}
        <small data-asset-save-status data-status="neutral">编辑后点击保存，写入 SQLite 后会刷新资产卡片。</small>
        <div class="asset-dialog-actions">
          <button type="button" data-asset-export>导出预览</button>
          <button type="submit">保存编辑</button>
        </div>
      </form>
      <details class="asset-export-preview">
        <summary>JSON 导出预览</summary>
        <pre>${escapeHtml(JSON.stringify({ assetType: type, asset }, null, 2))}</pre>
      </details>
      ${isReport ? `
        <p><strong>报告摘要：</strong>${escapeHtml(asset.summary || asset.notes || "等待继续编辑报告摘要。")}</p>
        <p><strong>洞察范围：</strong>${escapeHtml(scope || "未记录")}</p>
        <div class="asset-dialog-grid">
          <div><strong>章节</strong>${sections.length ? sections.map((section) => `<small>${escapeHtml(section.title || "未命名章节")}：${escapeHtml(limitText(section.text || "", 120))}</small>`).join("") : "<small>暂无章节</small>"}</div>
          <div><strong>引用</strong>${references.length ? references.map((item) => `<small>${escapeHtml(item.role || "引用")}：${escapeHtml(item.title || item.id || "未命名展品")}</small>`).join("") : "<small>暂无引用</small>"}</div>
        </div>
      ` : `
        <p><strong>专题说明：</strong>${escapeHtml(asset.intro || "等待继续编辑专题说明。")}</p>
        <p><strong>导览词：</strong>${escapeHtml(asset.guideText || "等待继续编辑导览词。")}</p>
        <p><strong>展品数量：</strong>${memoryIds.length} 件${asset.coverMemoryId ? `；封面展品：${escapeHtml(asset.coverMemoryId)}` : ""}</p>
        <div class="asset-dialog-grid">
          <div><strong>展品 ID</strong>${memoryIds.length ? memoryIds.map((id) => `<small>${escapeHtml(id)}</small>`).join("") : "<small>暂无展品</small>"}</div>
          <div><strong>后续编辑建议</strong><small>补充专题开场、展品排序、封面说明和导览结尾。</small></div>
        </div>
      `}
      <p><strong>创建时间：</strong>${escapeHtml(formatDate(asset.createdAt) || "未记录")}；<strong>更新时间：</strong>${escapeHtml(formatDate(asset.updatedAt) || "未记录")}</p>
    </div>
  `;
  elements.memoryDialog.showModal();
}

function renderWorkflowOrchestration() {
  if (!elements.workflowOrchestration) return;
  const blueprint = getWorkflowBlueprint();
  const handoff = blueprint.handoff;
  const activeRun = latestAgentWorkflow?.run?.id ? "当前有可回看的整理运行" : "等待新的整理运行";
  const sourceLabel = workflowBlueprintSource === "server" ? "后端蓝图" : workflowBlueprintSource === "server-fallback" ? "本地回退" : "本地蓝图";
  const selectedTemplate = blueprint.templates.find((template) => template.id === activeWorkflowTemplateId) || blueprint.templates[0];
  if (selectedTemplate) activeWorkflowTemplateId = selectedTemplate.id;
  elements.workflowSummaryMeta.textContent = `${sourceLabel} / ${handoff.templates} 个模板 / 复核覆盖 ${handoff.reviewCoverage}% / 回放覆盖 ${handoff.runCoverage}% / 导览覆盖 ${handoff.guideCoverage}%`;
  elements.workflowReadinessGrid.innerHTML = `
    <div><strong>${handoff.total}</strong><small>展品样本</small></div>
    <div><strong>${handoff.withAgentRun}</strong><small>可回放运行</small></div>
    <div><strong>${handoff.withReviewEvidence}</strong><small>复核依据</small></div>
    <div><strong>${handoff.withGuideEvidence}</strong><small>导览证据</small></div>
  `;
  elements.workflowGapList.innerHTML = `
    <div class="workflow-quality-gates">
      ${blueprint.qualityGates.map((gate) => `<span data-gate-status="${escapeHtml(gate.status)}">${escapeHtml(gate.label)}</span>`).join("")}
    </div>
    <div class="workflow-gap-items">
      ${handoff.gaps.map((gap) => `<span data-gap-severity="${escapeHtml(gap.severity)}">${escapeHtml(gap.label)}</span>`).join("")}
    </div>
    <div class="workflow-next-actions">
      ${handoff.recommendedNextActions.map((action) => `<small>${escapeHtml(action)}</small>`).join("")}
    </div>
  `;
  elements.phase12ReadinessPanel.innerHTML = renderPhase12Readiness(blueprint.phase12Readiness);
  elements.workflowDetailPanel.innerHTML = selectedTemplate ? renderWorkflowTemplateDetail(selectedTemplate) : "";
  elements.workflowTemplateList.innerHTML = blueprint.templates.map((template) => `
    <article class="workflow-template-card" data-template="${escapeHtml(template.id)}" ${template.id === activeWorkflowTemplateId ? "data-active-template=\"true\"" : ""}>
      <div class="workflow-template-heading">
        <span>${escapeHtml(template.name)}</span>
        <small>${escapeHtml(`${template.statusLabel} · ${template.entry}`)}</small>
      </div>
      <p>${escapeHtml(template.purpose)}</p>
      <div class="workflow-node-strip">
        ${template.nodes.map((node) => `<span>${escapeHtml(formatWorkflowNodeLabel(node))}</span>`).join("")}
      </div>
      <div class="workflow-meta-row">
        <small>控制：${escapeHtml(formatWorkflowList(template.controls))}</small>
        <small>暂停点：${escapeHtml(formatWorkflowList(template.pausePoints))}</small>
        <small>风险：${escapeHtml(formatWorkflowList(template.riskSignals, 2))}</small>
        <small>保存：${escapeHtml(formatWorkflowList(template.persistence))}</small>
      </div>
      <div class="workflow-action-row">
        ${(template.nextActions || []).slice(0, 3).map((action) => `<span>${escapeHtml(action)}</span>`).join("")}
      </div>
      <strong>${escapeHtml(template.warmCue)}</strong>
    </article>
  `).join("");
  elements.workflowCapabilityList.innerHTML = `
    <span>人工复核 ${blueprint.capabilities.humanReview ? "已接入" : "待接入"}</span>
    <span>重试/驳回 ${blueprint.capabilities.retryAndReject ? "已接入" : "待接入"}</span>
    <span>运行回放 ${blueprint.capabilities.replay ? activeRun : "待接入"}</span>
    <span>引用依据 ${blueprint.capabilities.citations ? "已接入" : "待接入"}</span>
    <span>范围洞察 ${blueprint.capabilities.scopedInsights ? "已接入" : "待接入"}</span>
  `;
}

function renderPrivacyPanel() {
  if (!elements.privacySection) return;
  const policy = getPrivacyPolicy();
  const summary = policy.summary || {};
  const sourceLabel = privacyPolicySource === "server" ? "后端策略" : "本地策略";
  elements.profileNameInput.value = localProfile.displayName;
  elements.profileDeviceInput.value = localProfile.deviceLabel;
  elements.profileSyncSelect.value = localProfile.syncPreference;
  elements.profileAiConsentInput.checked = localProfile.aiConsent;
  elements.privacySummary.textContent = `${sourceLabel} / ${summary.accountMode || "local-single-user"} / ${summary.memoryCount || 0} 件展品`;
  elements.aiScopePanel.innerHTML = `
    <p>${escapeHtml(policy.aiDataScope?.note || "当前未记录 AI 调用范围。")}</p>
    <div class="privacy-chip-row">
      <span>AI ${escapeHtml(summary.aiMode || "unknown")}</span>
      <span>模型 ${escapeHtml(policy.aiDataScope?.model || "not-configured")}</span>
      <span>${localProfile.aiConsent ? "本地已允许" : "本地未允许"}</span>
    </div>
    <small>发送字段：${escapeHtml(formatPrivacyList(policy.aiDataScope?.sentFields))}</small>
    <small>不会发送：${escapeHtml(formatPrivacyList(policy.aiDataScope?.notSentByCurrentApp))}</small>
  `;
  const sensitive = policy.sensitiveData || {};
  elements.sensitivityPanel.innerHTML = `
    <div class="sensitivity-heading">
      <strong>敏感线索</strong>
      <span data-risk-level="${escapeHtml(sensitive.riskLevel || "none")}">${escapeHtml(sensitive.riskLevel || "none")}</span>
    </div>
    <p>${escapeHtml(sensitive.recommendation || "暂无敏感线索摘要。")}</p>
    <div class="privacy-chip-row">
      ${(sensitive.categories || []).map((item) => `<span>${escapeHtml(item.label)} ${escapeHtml(item.count || 0)}</span>`).join("")}
    </div>
  `;
  elements.dataLocationList.innerHTML = (policy.dataLocations || []).map((item) => `
    <div class="privacy-line-item">
      <span>${escapeHtml(item.label)}</span>
      <small>${escapeHtml(item.location)}</small>
      <small>${escapeHtml(formatPrivacyList(item.contains, 5))}</small>
    </div>
  `).join("");
  elements.userControlList.innerHTML = (policy.userControls || []).map((item) => `
    <span data-control-status="${escapeHtml(item.status)}">${escapeHtml(item.label)}</span>
  `).join("");
  renderPhase16SyncPanel();
  elements.phase13ReadinessPanel.innerHTML = renderPhase13Readiness(policy.productizationReadiness || policy.sovereignty?.phase13Readiness);
}

function formatPrivacyList(items, limit = 6) {
  if (!Array.isArray(items) || !items.length) return "未记录";
  return items.slice(0, limit).join(" / ");
}

function renderPhase13Readiness(readiness) {
  if (!readiness) return "";
  return `
    <div class="phase13-readiness-heading">
      <div>
        <span>Phase ${escapeHtml(readiness.targetPhase)}</span>
        <strong>${escapeHtml(readiness.targetName)}</strong>
      </div>
      <small>${escapeHtml(`准备度 ${readiness.score}%`)}</small>
    </div>
    <div class="phase13-checks">
      ${(readiness.checks || []).map((item) => `<span data-readiness-status="${escapeHtml(item.status)}">${escapeHtml(item.label)}</span>`).join("")}
    </div>
    <p>${escapeHtml(readiness.recommendation || "")}</p>
  `;
}

function renderOperationsPanel() {
  if (!elements.operationsSection) return;
  const info = getVersionInfo();
  const ops = info.operations || {};
  const sourceLabel = operationsSource === "server" ? "后端版本" : "本地回退";
  elements.operationsSummaryGrid.innerHTML = `
    <div><strong>${escapeHtml(String(info.phase))}</strong><small>当前阶段</small></div>
    <div><strong>${escapeHtml(info.version || "1.0.0")}</strong><small>版本</small></div>
    <div><strong>${escapeHtml(ops.status || "unknown")}</strong><small>${escapeHtml(sourceLabel)}</small></div>
    <div><strong>${escapeHtml(`${ops.release?.checklistReady || 0}/${ops.release?.checklistTotal || 0}`)}</strong><small>发布清单</small></div>
  `;
  elements.runtimePanel.innerHTML = renderOperationLines([
    ["Node", info.runtime?.node],
    ["平台", info.runtime?.platform],
    ["端口", info.runtime?.port],
    ["运行秒数", info.runtime?.uptimeSeconds],
    ["数据库", info.runtime?.databasePath],
    ["AI 模型", info.runtime?.aiModel]
  ]);
  elements.deploymentPanel.innerHTML = renderOperationLines([
    ["模式", info.deployment?.mode],
    ["静态托管", info.deployment?.staticHosting],
    ["数据库", info.deployment?.database],
    ["启动命令", info.deployment?.startCommand],
    ["健康检查", info.deployment?.healthCheck],
    ["版本检查", info.deployment?.versionCheck],
    ["说明", info.deployment?.notes]
  ]) + renderOperationItems(info.deploymentModes || [], { showStatus: true, compact: true });
  elements.checksPanel.innerHTML = renderOperationLines([
    ["完整检查", info.checks?.full],
    ["Smoke", info.checks?.smoke],
    ["语法", info.checks?.syntax],
    ["备份", ops.backup?.fullExport],
    ["脱敏备份", ops.backup?.redactedExport],
    ["清空", ops.backup?.purge],
    ["观测接口", ops.observability?.operations],
    ["请求 ID", ops.observability?.requestId]
  ]);
  elements.engineeringPanel.innerHTML = `
    <div class="operation-line-list">
      ${(info.nextEngineeringSteps || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
    </div>
    <p>${escapeHtml(ops.readiness?.reason || "")}</p>
  `;
  if (elements.releaseChecklistPanel) {
    elements.releaseChecklistPanel.innerHTML = renderOperationItems(info.releaseChecklist || [], { showStatus: true });
  }
  if (elements.runbookPanel) {
    elements.runbookPanel.innerHTML = renderOperationItems(info.runbook || []);
  }
  if (elements.backupPolicyPanel) {
    elements.backupPolicyPanel.innerHTML = renderBackupPolicy(info.backupPolicy || ops.backupPolicy || {});
  }
  if (elements.riskPanel) {
    elements.riskPanel.innerHTML = renderOperationItems(info.riskRegister || [], { showLevel: true });
  }
  if (elements.operationEventsPanel) {
    elements.operationEventsPanel.innerHTML = renderOperationEvents(info.operationsConsole?.recentEvents || info.recentEvents || []);
  }
  if (elements.releaseHistoryPanel) {
    elements.releaseHistoryPanel.innerHTML = renderReleaseHistory(info.releaseHistory || []);
  }
  if (elements.logArchivePanel) {
    elements.logArchivePanel.innerHTML = renderLogArchive(info.operationsConsole?.logArchive || info.logArchive || {});
  }
  if (elements.demoKitPanel) {
    elements.demoKitPanel.innerHTML = renderDemoKit(info.operationsConsole?.demoKit || info.demoKit || {});
  }
  if (elements.phase14Panel) {
    elements.phase14Panel.innerHTML = renderPhase14Readiness(info.operationsConsole?.phase14Readiness || info.phase14Readiness || {});
  }
  if (elements.moduleBoundaryPanel) {
    elements.moduleBoundaryPanel.innerHTML = renderModuleBoundaryPlan(info.operationsConsole?.moduleBoundaryPlan || info.moduleBoundaryPlan || []);
  }
  if (elements.phase15Panel) {
    elements.phase15Panel.innerHTML = renderPhase14Readiness(info.operationsConsole?.phase15Readiness || info.phase15Readiness || {});
  }
  if (elements.phase15AssetPanel) {
    elements.phase15AssetPanel.innerHTML = renderPhase15AssetPlan(info.operationsConsole?.phase15AssetPlan || info.phase15AssetPlan || []);
  }
}

function renderPhase20PlatformPanel() {
  if (!elements.phase20PlatformSection) return;
  const info = getVersionInfo();
  const plan = info.operationsConsole?.phase20PlatformPlan || info.phase20PlatformPlan || buildLocalPhase20PlatformPlan();
  const readiness = plan.readiness || {};
  const security = plan.securityModel || {};
  const builtInPluginRegistry = plan.builtInPluginRegistry || {};
  const manifestSchema = plan.manifestSchema || {};
  const manifestValidation = plan.manifestValidation || {};
  const permissionReview = plan.permissionReview || {};
  const pluginAuditLog = plan.pluginAuditLog || {};
  const extensionContractTests = plan.extensionContractTests || {};
  const sandboxBoundary = plan.sandboxBoundary || {};
  const noCodeTemplatePack = plan.noCodeTemplatePack || {};
  const templatePreviewFixtures = plan.templatePreviewFixtures || {};
  const signedManifestPolicy = plan.signedManifestPolicy || {};
  const pluginInstallationWorkflow = plan.pluginInstallationWorkflow || {};
  const pluginReviewWorkbench = plan.pluginReviewWorkbench || {};
  const pluginLockfile = plan.pluginLockfile || {};
  const pluginLockfileExport = plan.pluginLockfileExport || {};
  const pluginInstallQueuePersistence = plan.pluginInstallQueuePersistence || {};
  const pluginReleaseSignatureGate = plan.pluginReleaseSignatureGate || {};
  const pluginLockfileImportPreview = plan.pluginLockfileImportPreview || {};
  const pluginPreReleaseCheckCommand = plan.pluginPreReleaseCheckCommand || {};
  const pluginSignatureDiffReport = plan.pluginSignatureDiffReport || {};
  const pluginReleaseReportArtifact = plan.pluginReleaseReportArtifact || {};
  const pluginDiffReviewHistory = plan.pluginDiffReviewHistory || {};
  const pluginReleaseReportValidationCommand = plan.pluginReleaseReportValidationCommand || {};
  const pluginSignedReleaseReportExport = plan.pluginSignedReleaseReportExport || {};
  const pluginReviewHistoryUi = plan.pluginReviewHistoryUi || {};
  const pluginValidationCommandCiWrapper = plan.pluginValidationCommandCiWrapper || {};
  const pluginSignedReportDownload = plan.pluginSignedReportDownload || {};
  const pluginCiBadgeSummary = plan.pluginCiBadgeSummary || {};
  const pluginDownloadIntegrityPreview = plan.pluginDownloadIntegrityPreview || {};
  const pluginReleaseChecklistGate = plan.pluginReleaseChecklistGate || {};
  const phase25Surface = info.operationsConsole?.phase25RuntimeSandboxUiSurface || info.phase25RuntimeSandboxUiSurface || buildLocalPhase25RuntimeSandboxUiSurface();
  const phase26Entry = info.operationsConsole?.phase26RuntimeValidationEntry || info.phase26RuntimeValidationEntry || buildLocalPhase26RuntimeValidationEntry();
  const phase27Entry = info.operationsConsole?.phase27ReleaseBlockerGovernanceEntry || info.phase27ReleaseBlockerGovernanceEntry || buildLocalPhase27ReleaseBlockerGovernanceEntry();
  const phase28Entry = info.operationsConsole?.phase28ClearanceReviewEntry || info.phase28ClearanceReviewEntry || buildLocalPhase28ClearanceReviewEntry();
  const phase29Plan = info.operationsConsole?.phase29ReleaseGovernancePlanning || info.phase29ReleaseGovernancePlanning || buildLocalPhase29ReleaseGovernancePlanning();
  const builtInCount = (plan.builtInPlugins || []).filter((item) => item.enabled !== false).length;
  elements.phase20SummaryGrid.innerHTML = `
    <div><strong>${escapeHtml(String(plan.phase || 20))}</strong><small>阶段</small></div>
    <div><strong>${escapeHtml(String((plan.extensionPoints || []).length))}</strong><small>扩展点</small></div>
    <div><strong>${escapeHtml(String(builtInCount))}</strong><small>内置插件</small></div>
    <div><strong>${escapeHtml(security.thirdPartyExecution ? "开放" : "关闭")}</strong><small>第三方执行</small></div>
  `;
  elements.phase20ExtensionPanel.innerHTML = renderOperationItems(plan.extensionPoints || [], { showStatus: true });
  if (elements.phase25SurfacePanel) {
    elements.phase25SurfacePanel.innerHTML = renderPhase25RuntimeSandboxUiSurface(phase25Surface);
  }
  if (elements.phase26ValidationPanel) {
    elements.phase26ValidationPanel.innerHTML = renderPhase26RuntimeValidationEntry(phase26Entry);
  }
  if (elements.phase27GovernancePanel) {
    elements.phase27GovernancePanel.innerHTML = renderPhase27ReleaseBlockerGovernanceEntry(phase27Entry);
  }
  if (elements.phase28ClearanceReviewPanel) {
    elements.phase28ClearanceReviewPanel.innerHTML = renderPhase28ClearanceReviewEntry(phase28Entry);
  }
  if (elements.phase29ReleaseGovernancePanel) {
    elements.phase29ReleaseGovernancePanel.innerHTML = renderPhase29ReleaseGovernancePlanning(phase29Plan);
  }
  elements.phase20PluginPanel.innerHTML = renderOperationItems((plan.builtInPlugins || []).map((item) => ({
    ...item,
    label: `${item.id} / ${item.type}`,
    detail: `来源：${item.source || "local"}；状态：${item.enabled === false ? "disabled" : "enabled"}`
  })), { showStatus: true });
  if (elements.phase20RegistryPanel) {
    elements.phase20RegistryPanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", builtInPluginRegistry.schemaVersion],
        ["Status", builtInPluginRegistry.status],
        ["Owner", builtInPluginRegistry.owner],
        ["Total", builtInPluginRegistry.total],
        ["Enabled", builtInPluginRegistry.enabled],
        ["Categories", formatWorkflowList(builtInPluginRegistry.categories || [])],
        ["Checks", formatWorkflowList(builtInPluginRegistry.registryChecks || [])],
        ["Runtime", builtInPluginRegistry.runtimeExecution ? "enabled" : "disabled"]
      ])}
      ${renderOperationItems((builtInPluginRegistry.entries || []).map((item) => ({
        ...item,
        label: `${item.id} / ${item.type}`,
        detail: `${item.capability} / ${item.input} -> ${item.output} / ${item.contract}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20ManifestPanel) {
    elements.phase20ManifestPanel.innerHTML = renderOperationLines([
      ["Schema", manifestSchema.schemaVersion],
      ["Status", manifestSchema.status],
      ["Required", formatWorkflowList(manifestSchema.requiredFields || [])],
      ["Permissions", formatWorkflowList(manifestSchema.permissionLabels || [])],
      ["Contracts", formatWorkflowList(manifestSchema.extensionContracts || [])],
      ["Built-in manifests", manifestValidation.builtInManifestCount],
      ["Runtime", manifestValidation.runtimeExecution ? "enabled" : "disabled"]
    ]);
  }
  if (elements.phase20PermissionPanel) {
    elements.phase20PermissionPanel.innerHTML = `
      ${renderOperationLines([
        ["Status", permissionReview.status],
        ["Default", permissionReview.defaultDecision],
        ["Human approval", permissionReview.humanApprovalRequired ? "required" : "optional"],
        ["Review scope", formatWorkflowList(permissionReview.reviewScope || [])],
        ["Audit events", formatWorkflowList(permissionReview.auditEventTypes || [])],
        ["Blocked until", formatWorkflowList(permissionReview.blockedUntil || [])]
      ])}
      ${renderOperationItems((permissionReview.builtInDecisions || []).map((item) => ({
        ...item,
        status: item.decision,
        label: item.id,
        detail: `${formatWorkflowList(item.permissions || [])} / confirmation: ${item.confirmationRequired ? "required" : "not required"}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20AuditPanel) {
    elements.phase20AuditPanel.innerHTML = `
      ${renderOperationLines([
        ["Status", pluginAuditLog.status],
        ["Schema", pluginAuditLog.eventSchemaVersion],
        ["Storage", pluginAuditLog.storageMode],
        ["Runtime", pluginAuditLog.runtimeExecution ? "enabled" : "disabled"],
        ["Required", formatWorkflowList(pluginAuditLog.requiredFields || [])],
        ["Events", formatWorkflowList(pluginAuditLog.eventTypes || [])],
        ["Next controls", formatWorkflowList(pluginAuditLog.nextControls || [])]
      ])}
      ${renderOperationItems((pluginAuditLog.sampleEvents || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.eventType}`,
        detail: `${item.actor || "system"} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20ContractPanel) {
    elements.phase20ContractPanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", extensionContractTests.schemaVersion],
        ["Status", extensionContractTests.status],
        ["Coverage", formatWorkflowList(extensionContractTests.coverage || [])],
        ["Assertions", formatWorkflowList(extensionContractTests.requiredAssertions || [])],
        ["Failure policy", extensionContractTests.failurePolicy],
        ["Runtime", extensionContractTests.runtimeExecution ? "enabled" : "disabled"],
        ["Next controls", formatWorkflowList(extensionContractTests.nextControls || [])]
      ])}
      ${renderOperationItems((extensionContractTests.contractSuites || []).map((item) => ({
        ...item,
        label: `${item.id} / ${item.extensionPoint}`,
        detail: `${item.samplePlugin} / ${item.inputFixture} -> ${item.expectedOutput} / ${item.blockingFailure}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20SandboxPanel) {
    elements.phase20SandboxPanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", sandboxBoundary.schemaVersion],
        ["Status", sandboxBoundary.status],
        ["Isolation", sandboxBoundary.isolationMode],
        ["Trust zone", sandboxBoundary.trustZone],
        ["Blocked", formatWorkflowList(sandboxBoundary.blockedCapabilities || [])],
        ["Allowed", formatWorkflowList(sandboxBoundary.allowedCapabilities || [])],
        ["Runtime", sandboxBoundary.runtimeExecution ? "enabled" : "disabled"],
        ["Handoff", formatWorkflowList(sandboxBoundary.handoffToRuntime || [])],
        ["Next controls", formatWorkflowList(sandboxBoundary.nextControls || [])]
      ])}
      ${renderOperationLines([
        ["Memory", sandboxBoundary.dataBoundary?.memoryAccess],
        ["Assets", sandboxBoundary.dataBoundary?.assetAccess],
        ["Export", sandboxBoundary.dataBoundary?.exportAccess],
        ["Sync", sandboxBoundary.dataBoundary?.syncAccess]
      ])}
      ${renderOperationItems((sandboxBoundary.enforcementChecks || []).map((item) => ({
        ...item,
        label: item.id,
        detail: item.rule
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20TemplatePanel) {
    elements.phase20TemplatePanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", noCodeTemplatePack.schemaVersion],
        ["Status", noCodeTemplatePack.status],
        ["Owner", noCodeTemplatePack.owner],
        ["Templates", noCodeTemplatePack.templateCount],
        ["Categories", formatWorkflowList(noCodeTemplatePack.categories || [])],
        ["Guardrails", formatWorkflowList(noCodeTemplatePack.guardrails || [])],
        ["Workflow", formatWorkflowList(noCodeTemplatePack.authoringWorkflow || [])],
        ["Runtime", noCodeTemplatePack.runtimeExecution ? "enabled" : "disabled"],
        ["Next controls", formatWorkflowList(noCodeTemplatePack.nextControls || [])]
      ])}
      ${renderOperationItems((noCodeTemplatePack.templates || []).map((item) => ({
        ...item,
        label: `${item.id} / ${item.extensionPoint}`,
        detail: `${item.input} -> ${item.output} / ${formatWorkflowList(item.controls || [])}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20FixturePanel) {
    elements.phase20FixturePanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", templatePreviewFixtures.schemaVersion],
        ["Status", templatePreviewFixtures.status],
        ["Fixtures", templatePreviewFixtures.fixtureCount],
        ["Coverage", formatWorkflowList(templatePreviewFixtures.coverage || [])],
        ["Workflow", formatWorkflowList(templatePreviewFixtures.previewWorkflow || [])],
        ["Blocked", formatWorkflowList(templatePreviewFixtures.blockedWhen || [])],
        ["Runtime", templatePreviewFixtures.runtimeExecution ? "enabled" : "disabled"],
        ["Next controls", formatWorkflowList(templatePreviewFixtures.nextControls || [])]
      ])}
      ${renderOperationItems((templatePreviewFixtures.fixtures || []).map((item) => ({
        ...item,
        label: `${item.id} / ${item.templateId}`,
        detail: `${item.inputFixture} -> ${item.expectedPreview} / ${formatWorkflowList(item.requiredAssertions || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((templatePreviewFixtures.negativeFixtures || []).map((item) => ({
        ...item,
        label: `${item.id} / ${item.templateId}`,
        detail: `${item.reason} -> ${item.expectedDecision}`
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20SignaturePanel) {
    elements.phase20SignaturePanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", signedManifestPolicy.schemaVersion],
        ["Status", signedManifestPolicy.status],
        ["Required", signedManifestPolicy.signatureRequired ? "yes" : "no"],
        ["Algorithm", signedManifestPolicy.algorithm],
        ["Trust", signedManifestPolicy.signerTrust],
        ["Signed fields", formatWorkflowList(signedManifestPolicy.signedFields || [])],
        ["Checksum fields", formatWorkflowList(signedManifestPolicy.checksumFields || [])],
        ["Verify", formatWorkflowList(signedManifestPolicy.verificationSteps || [])],
        ["Blocked", formatWorkflowList(signedManifestPolicy.blockedWhen || [])],
        ["Runtime", signedManifestPolicy.runtimeExecution ? "enabled" : "disabled"],
        ["Next controls", formatWorkflowList(signedManifestPolicy.nextControls || [])]
      ])}
      ${renderOperationItems((signedManifestPolicy.sampleSignatures || []).map((item) => ({
        ...item,
        label: item.pluginId,
        detail: item.digest
      })), { showStatus: true, compact: true })}
    `;
  }
  if (elements.phase20InstallPanel) {
    elements.phase20InstallPanel.innerHTML = `
      ${renderOperationLines([
        ["Schema", pluginInstallationWorkflow.schemaVersion],
        ["Status", pluginInstallationWorkflow.status],
        ["Default", pluginInstallationWorkflow.defaultDecision],
        ["States", formatWorkflowList(pluginInstallationWorkflow.installStates || [])],
        ["Gates", formatWorkflowList(pluginInstallationWorkflow.requiredGates || [])],
        ["Blocked", formatWorkflowList(pluginInstallationWorkflow.blockedWhen || [])],
        ["Runtime", pluginInstallationWorkflow.runtimeExecution ? "enabled" : "disabled"],
        ["Next controls", formatWorkflowList(pluginInstallationWorkflow.nextControls || [])]
      ])}
      ${renderOperationItems((pluginInstallationWorkflow.workflowSteps || []).map((item) => ({
        ...item,
        label: item.id,
        detail: `${item.input} -> ${item.output} / ${item.blockingFailure}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginInstallationWorkflow.sampleDecisions || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.state}`,
        detail: formatWorkflowList(item.evidence || [])
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Review schema", pluginReviewWorkbench.schemaVersion],
        ["Review status", pluginReviewWorkbench.status],
        ["Queue states", formatWorkflowList(pluginReviewWorkbench.queueStates || [])],
        ["Review actions", formatWorkflowList(pluginReviewWorkbench.reviewActions || [])],
        ["Reviewer fields", formatWorkflowList(pluginReviewWorkbench.reviewerRecord?.requiredFields || [])],
        ["Review next", formatWorkflowList(pluginReviewWorkbench.nextControls || [])],
        ["Lock schema", pluginLockfile.schemaVersion],
        ["Lock status", pluginLockfile.status],
        ["Lockfile", pluginLockfile.lockfileName],
        ["Integrity", formatWorkflowList(pluginLockfile.integrityChecks || [])],
        ["Lock blocked", formatWorkflowList(pluginLockfile.blockedWhen || [])],
        ["Lock next", formatWorkflowList(pluginLockfile.nextControls || [])],
        ["Export schema", pluginLockfileExport.schemaVersion],
        ["Export status", pluginLockfileExport.status],
        ["Export name", pluginLockfileExport.exportName],
        ["Export modes", formatWorkflowList(pluginLockfileExport.exportModes || [])],
        ["Export workflow", formatWorkflowList(pluginLockfileExport.exportWorkflow || [])],
        ["Export blocked", formatWorkflowList(pluginLockfileExport.blockedWhen || [])],
        ["Queue schema", pluginInstallQueuePersistence.schemaVersion],
        ["Queue status", pluginInstallQueuePersistence.status],
        ["Queue states", formatWorkflowList(pluginInstallQueuePersistence.queueStates || [])],
        ["Queue checks", formatWorkflowList(pluginInstallQueuePersistence.persistenceChecks || [])],
        ["Release gate", pluginReleaseSignatureGate.schemaVersion],
        ["Gate status", pluginReleaseSignatureGate.status],
        ["Gate policy", pluginReleaseSignatureGate.releaseGatePolicy],
        ["Gate required", formatWorkflowList(pluginReleaseSignatureGate.requiredBeforeRelease || [])],
        ["Gate blocked", formatWorkflowList(pluginReleaseSignatureGate.blockedWhen || [])],
        ["Import schema", pluginLockfileImportPreview.schemaVersion],
        ["Import status", pluginLockfileImportPreview.status],
        ["Import inputs", formatWorkflowList(pluginLockfileImportPreview.acceptedInputs || [])],
        ["Import workflow", formatWorkflowList(pluginLockfileImportPreview.previewWorkflow || [])],
        ["Import diffs", formatWorkflowList(pluginLockfileImportPreview.diffCategories || [])],
        ["Import blocked", formatWorkflowList(pluginLockfileImportPreview.blockedWhen || [])],
        ["Import next", formatWorkflowList(pluginLockfileImportPreview.nextControls || [])],
        ["Pre-release schema", pluginPreReleaseCheckCommand.schemaVersion],
        ["Pre-release status", pluginPreReleaseCheckCommand.status],
        ["Pre-release command", pluginPreReleaseCheckCommand.command],
        ["Pre-release inputs", formatWorkflowList(pluginPreReleaseCheckCommand.requiredInputs || [])],
        ["Pre-release workflow", formatWorkflowList(pluginPreReleaseCheckCommand.checkWorkflow || [])],
        ["Pre-release assertions", formatWorkflowList(pluginPreReleaseCheckCommand.requiredAssertions || [])],
        ["Pre-release blocked", formatWorkflowList(pluginPreReleaseCheckCommand.blockedWhen || [])],
        ["Pre-release next", formatWorkflowList(pluginPreReleaseCheckCommand.nextControls || [])],
        ["Diff report schema", pluginSignatureDiffReport.schemaVersion],
        ["Diff report status", pluginSignatureDiffReport.status],
        ["Diff report name", pluginSignatureDiffReport.reportName],
        ["Diff report inputs", formatWorkflowList(pluginSignatureDiffReport.reportInputs || [])],
        ["Diff axes", formatWorkflowList(pluginSignatureDiffReport.diffAxes || [])],
        ["Diff workflow", formatWorkflowList(pluginSignatureDiffReport.reportWorkflow || [])],
        ["Diff severity", formatWorkflowList(pluginSignatureDiffReport.severityLevels || [])],
        ["Diff blocked", formatWorkflowList(pluginSignatureDiffReport.blockedWhen || [])],
        ["Diff next", formatWorkflowList(pluginSignatureDiffReport.nextControls || [])],
        ["Release report schema", pluginReleaseReportArtifact.schemaVersion],
        ["Release report status", pluginReleaseReportArtifact.status],
        ["Release report name", pluginReleaseReportArtifact.artifactName],
        ["Release report type", pluginReleaseReportArtifact.artifactType],
        ["Release report inputs", formatWorkflowList(pluginReleaseReportArtifact.reportInputs || [])],
        ["Release report sections", formatWorkflowList(pluginReleaseReportArtifact.includedSections || [])],
        ["Release report workflow", formatWorkflowList(pluginReleaseReportArtifact.generationWorkflow || [])],
        ["Release report blocked", formatWorkflowList(pluginReleaseReportArtifact.blockedWhen || [])],
        ["Release report next", formatWorkflowList(pluginReleaseReportArtifact.nextControls || [])],
        ["Review history schema", pluginDiffReviewHistory.schemaVersion],
        ["Review history status", pluginDiffReviewHistory.status],
        ["Review history name", pluginDiffReviewHistory.historyName],
        ["Review history inputs", formatWorkflowList(pluginDiffReviewHistory.historyInputs || [])],
        ["Review tracked fields", formatWorkflowList(pluginDiffReviewHistory.trackedFields || [])],
        ["Review states", formatWorkflowList(pluginDiffReviewHistory.reviewStates || [])],
        ["Review workflow", formatWorkflowList(pluginDiffReviewHistory.decisionWorkflow || [])],
        ["Review blocked", formatWorkflowList(pluginDiffReviewHistory.blockedWhen || [])],
        ["Review next", formatWorkflowList(pluginDiffReviewHistory.nextControls || [])],
        ["Validation schema", pluginReleaseReportValidationCommand.schemaVersion],
        ["Validation status", pluginReleaseReportValidationCommand.status],
        ["Validation command", pluginReleaseReportValidationCommand.command],
        ["Validation target", pluginReleaseReportValidationCommand.validationTarget],
        ["Validation inputs", formatWorkflowList(pluginReleaseReportValidationCommand.requiredInputs || [])],
        ["Validation workflow", formatWorkflowList(pluginReleaseReportValidationCommand.validationWorkflow || [])],
        ["Validation assertions", formatWorkflowList(pluginReleaseReportValidationCommand.requiredAssertions || [])],
        ["Validation blocked", formatWorkflowList(pluginReleaseReportValidationCommand.blockedWhen || [])],
        ["Validation next", formatWorkflowList(pluginReleaseReportValidationCommand.nextControls || [])],
        ["Signed export schema", pluginSignedReleaseReportExport.schemaVersion],
        ["Signed export status", pluginSignedReleaseReportExport.status],
        ["Signed export name", pluginSignedReleaseReportExport.exportName],
        ["Signed export type", pluginSignedReleaseReportExport.exportType],
        ["Signed export inputs", formatWorkflowList(pluginSignedReleaseReportExport.exportInputs || [])],
        ["Signed export workflow", formatWorkflowList(pluginSignedReleaseReportExport.exportWorkflow || [])],
        ["Signed export assertions", formatWorkflowList(pluginSignedReleaseReportExport.requiredAssertions || [])],
        ["Signed export blocked", formatWorkflowList(pluginSignedReleaseReportExport.blockedWhen || [])],
        ["Signed export next", formatWorkflowList(pluginSignedReleaseReportExport.nextControls || [])],
        ["Review UI schema", pluginReviewHistoryUi.schemaVersion],
        ["Review UI status", pluginReviewHistoryUi.status],
        ["Review UI panel", pluginReviewHistoryUi.panelId],
        ["Review UI source", pluginReviewHistoryUi.sourceHistory],
        ["Review UI inputs", formatWorkflowList(pluginReviewHistoryUi.requiredInputs || [])],
        ["Review UI columns", formatWorkflowList(pluginReviewHistoryUi.displayColumns || [])],
        ["Review UI filters", formatWorkflowList(pluginReviewHistoryUi.filterControls || [])],
        ["Review UI actions", formatWorkflowList(pluginReviewHistoryUi.reviewActions || [])],
        ["Review UI blocked", formatWorkflowList(pluginReviewHistoryUi.blockedWhen || [])],
        ["Review UI next", formatWorkflowList(pluginReviewHistoryUi.nextControls || [])],
        ["CI wrapper schema", pluginValidationCommandCiWrapper.schemaVersion],
        ["CI wrapper status", pluginValidationCommandCiWrapper.status],
        ["CI wrapper command", pluginValidationCommandCiWrapper.command],
        ["CI command", pluginValidationCommandCiWrapper.ciCommand],
        ["CI inputs", formatWorkflowList(pluginValidationCommandCiWrapper.requiredInputs || [])],
        ["CI workflow", formatWorkflowList(pluginValidationCommandCiWrapper.ciWorkflow || [])],
        ["CI artifacts", formatWorkflowList(pluginValidationCommandCiWrapper.requiredArtifacts || [])],
        ["CI blocked", formatWorkflowList(pluginValidationCommandCiWrapper.blockedWhen || [])],
        ["CI next", formatWorkflowList(pluginValidationCommandCiWrapper.nextControls || [])],
        ["Download schema", pluginSignedReportDownload.schemaVersion],
        ["Download status", pluginSignedReportDownload.status],
        ["Download name", pluginSignedReportDownload.downloadName],
        ["Download source", pluginSignedReportDownload.sourceArtifact],
        ["Download inputs", formatWorkflowList(pluginSignedReportDownload.requiredInputs || [])],
        ["Download workflow", formatWorkflowList(pluginSignedReportDownload.downloadWorkflow || [])],
        ["Download blocked", formatWorkflowList(pluginSignedReportDownload.blockedWhen || [])],
        ["Download next", formatWorkflowList(pluginSignedReportDownload.nextControls || [])],
        ["Badge schema", pluginCiBadgeSummary.schemaVersion],
        ["Badge status", pluginCiBadgeSummary.status],
        ["Badge artifact", pluginCiBadgeSummary.badgeName],
        ["Badge inputs", formatWorkflowList(pluginCiBadgeSummary.requiredInputs || [])],
        ["Badge workflow", formatWorkflowList(pluginCiBadgeSummary.badgeWorkflow || [])],
        ["Badge blocked", formatWorkflowList(pluginCiBadgeSummary.blockedWhen || [])],
        ["Badge next", formatWorkflowList(pluginCiBadgeSummary.nextControls || [])],
        ["Integrity schema", pluginDownloadIntegrityPreview.schemaVersion],
        ["Integrity status", pluginDownloadIntegrityPreview.status],
        ["Integrity preview", pluginDownloadIntegrityPreview.previewName],
        ["Integrity source", pluginDownloadIntegrityPreview.sourceDownload],
        ["Integrity inputs", formatWorkflowList(pluginDownloadIntegrityPreview.requiredInputs || [])],
        ["Integrity workflow", formatWorkflowList(pluginDownloadIntegrityPreview.integrityWorkflow || [])],
        ["Integrity blocked", formatWorkflowList(pluginDownloadIntegrityPreview.blockedWhen || [])],
        ["Integrity next", formatWorkflowList(pluginDownloadIntegrityPreview.nextControls || [])],
        ["Gate schema", pluginReleaseChecklistGate.schemaVersion],
        ["Gate status", pluginReleaseChecklistGate.status],
        ["Gate artifact", pluginReleaseChecklistGate.gateName],
        ["Gate inputs", formatWorkflowList(pluginReleaseChecklistGate.requiredInputs || [])],
        ["Gate workflow", formatWorkflowList(pluginReleaseChecklistGate.gateWorkflow || [])],
        ["Gate blocked", formatWorkflowList(pluginReleaseChecklistGate.blockedWhen || [])],
        ["Gate next", formatWorkflowList(pluginReleaseChecklistGate.nextControls || [])]
      ])}
      ${renderOperationItems((pluginReviewWorkbench.reviewQueue || []).map((item) => ({
        ...item,
        status: item.state,
        label: `${item.pluginId} / ${item.risk}`,
        detail: `${item.requiredAction} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginLockfile.lockedPlugins || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.version}`,
        detail: `${item.digest} / runtime: ${item.runtimeEnabled ? "enabled" : "disabled"}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginInstallQueuePersistence.queueItems || []).map((item) => ({
        ...item,
        status: item.state,
        label: `${item.id} / ${item.pluginId}`,
        detail: `${item.nextAction} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginReleaseSignatureGate.sampleGateResults || []).map((item) => ({
        ...item,
        status: item.result,
        label: item.pluginId,
        detail: formatWorkflowList(item.evidence || [])
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginLockfileImportPreview.sampleDiffs || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.category}`,
        detail: `${item.severity} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginPreReleaseCheckCommand.sampleResults || []).map((item) => ({
        ...item,
        status: item.result,
        label: item.id,
        detail: formatWorkflowList(item.evidence || [])
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Diff total", pluginSignatureDiffReport.summary?.totalFindings],
        ["Diff info", pluginSignatureDiffReport.summary?.info],
        ["Diff hold", pluginSignatureDiffReport.summary?.hold],
        ["Diff block", pluginSignatureDiffReport.summary?.block],
        ["Diff release ready", pluginSignatureDiffReport.summary?.releaseReady ? "yes" : "no"]
      ])}
      ${renderOperationItems((pluginSignatureDiffReport.sampleFindings || []).map((item) => ({
        ...item,
        status: item.severity,
        label: `${item.pluginId} / ${item.axis}`,
        detail: `${item.result} / ${item.releaseAction} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Release decision", pluginReleaseReportArtifact.releaseDecision?.status],
        ["Release ready", pluginReleaseReportArtifact.releaseDecision?.releaseReady ? "yes" : "no"],
        ["Release info", pluginReleaseReportArtifact.releaseDecision?.infoFindings],
        ["Release hold", pluginReleaseReportArtifact.releaseDecision?.holdFindings],
        ["Release block", pluginReleaseReportArtifact.releaseDecision?.blockFindings],
        ["Required action", pluginReleaseReportArtifact.releaseDecision?.requiredAction],
        ["Decision evidence", formatWorkflowList(pluginReleaseReportArtifact.releaseDecision?.evidence || [])],
        ["Artifact version", pluginReleaseReportArtifact.sampleArtifact?.version],
        ["Artifact build", pluginReleaseReportArtifact.sampleArtifact?.buildLabel],
        ["Generated by", pluginReleaseReportArtifact.sampleArtifact?.generatedBy],
        ["Blocking findings", formatWorkflowList(pluginReleaseReportArtifact.sampleArtifact?.blockingFindingIds || [])],
        ["Attached reports", formatWorkflowList(pluginReleaseReportArtifact.sampleArtifact?.attachedReports || [])]
      ])}
      ${renderOperationLines([
        ["Review total", pluginDiffReviewHistory.summary?.totalReviews],
        ["Review approved", pluginDiffReviewHistory.summary?.approved],
        ["Review held", pluginDiffReviewHistory.summary?.held],
        ["Review blocked", pluginDiffReviewHistory.summary?.blocked],
        ["Review waived", pluginDiffReviewHistory.summary?.waived],
        ["Review unresolved", pluginDiffReviewHistory.summary?.unresolved],
        ["Review release ready", pluginDiffReviewHistory.summary?.releaseReady ? "yes" : "no"]
      ])}
      ${renderOperationItems((pluginDiffReviewHistory.reviewEntries || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.findingId}`,
        detail: `${item.severity} / ${item.releaseAction} / ${item.reviewer} / ${formatWorkflowList(item.evidence || [])}`
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Review UI rows", pluginReviewHistoryUi.summary?.totalRows],
        ["Visible blockers", pluginReviewHistoryUi.summary?.visibleBlockers],
        ["Unresolved holds", pluginReviewHistoryUi.summary?.unresolvedHolds],
        ["Signed export linked", pluginReviewHistoryUi.summary?.signedExportLinked ? "yes" : "no"],
        ["Review UI release ready", pluginReviewHistoryUi.summary?.releaseReady ? "yes" : "no"]
      ])}
      ${renderOperationItems((pluginReviewHistoryUi.sampleRows || []).map((item) => ({
        ...item,
        status: item.decision,
        label: `${item.pluginId} / ${item.findingId}`,
        detail: `${item.severity} / ${item.primaryAction} / visible: ${item.visible ? "yes" : "no"}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginReviewHistoryUi.stateBadges || []).map((item) => ({
        ...item,
        status: item.tone,
        label: item.state,
        detail: item.label
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["CI total steps", pluginValidationCommandCiWrapper.summary?.totalSteps],
        ["CI pass", pluginValidationCommandCiWrapper.summary?.pass],
        ["CI hold", pluginValidationCommandCiWrapper.summary?.hold],
        ["CI block", pluginValidationCommandCiWrapper.summary?.block],
        ["CI release ready", pluginValidationCommandCiWrapper.summary?.releaseReady ? "yes" : "no"],
        ["CI run version", pluginValidationCommandCiWrapper.sampleCiRun?.version],
        ["CI run build", pluginValidationCommandCiWrapper.sampleCiRun?.buildLabel],
        ["CI run result", pluginValidationCommandCiWrapper.sampleCiRun?.result],
        ["CI run exit code", pluginValidationCommandCiWrapper.sampleCiRun?.exitCode],
        ["CI published artifacts", formatWorkflowList(pluginValidationCommandCiWrapper.sampleCiRun?.artifactsPublished || [])]
      ])}
      ${renderOperationItems((pluginValidationCommandCiWrapper.exitCodePolicy || []).map((item) => ({
        ...item,
        status: item.result,
        label: `${item.result} -> ${item.exitCode}`,
        detail: `releaseReady: ${item.releaseReady ? "yes" : "no"}`
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Download files", pluginSignedReportDownload.summary?.totalFiles],
        ["Downloadable", pluginSignedReportDownload.summary?.downloadable],
        ["Download redacted", pluginSignedReportDownload.summary?.redacted],
        ["Download blocked", pluginSignedReportDownload.summary?.blocked],
        ["Download release ready", pluginSignedReportDownload.summary?.releaseReady ? "yes" : "no"],
        ["Envelope format", pluginSignedReportDownload.downloadEnvelope?.format],
        ["Envelope redacted", pluginSignedReportDownload.downloadEnvelope?.redacted ? "yes" : "no"],
        ["Secrets included", pluginSignedReportDownload.downloadEnvelope?.includesRuntimeSecrets ? "yes" : "no"],
        ["Checksum", pluginSignedReportDownload.downloadEnvelope?.checksumAlgorithm],
        ["Disposition", pluginSignedReportDownload.downloadEnvelope?.contentDisposition],
        ["Download version", pluginSignedReportDownload.sampleDownload?.version],
        ["Download build", pluginSignedReportDownload.sampleDownload?.buildLabel],
        ["Download file", pluginSignedReportDownload.sampleDownload?.fileName],
        ["Download ready", pluginSignedReportDownload.sampleDownload?.ready ? "yes" : "no"],
        ["Download source artifact", pluginSignedReportDownload.sampleDownload?.sourceArtifact],
        ["Download included reports", formatWorkflowList(pluginSignedReportDownload.sampleDownload?.includedReports || [])],
        ["Badge total", pluginCiBadgeSummary.summary?.totalBadges],
        ["Badge published", pluginCiBadgeSummary.summary?.published],
        ["Badge blocked", pluginCiBadgeSummary.summary?.blocked],
        ["Badge release ready", pluginCiBadgeSummary.summary?.releaseReady ? "yes" : "no"],
        ["Badge version", pluginCiBadgeSummary.sampleBadge?.version],
        ["Badge build", pluginCiBadgeSummary.sampleBadge?.buildLabel],
        ["Badge result", pluginCiBadgeSummary.sampleBadge?.result],
        ["Badge label", pluginCiBadgeSummary.sampleBadge?.label],
        ["Badge exit code", pluginCiBadgeSummary.sampleBadge?.exitCode],
        ["Badge download ready", pluginCiBadgeSummary.sampleBadge?.downloadReady ? "yes" : "no"],
        ["Badge sample artifact", pluginCiBadgeSummary.sampleBadge?.artifact],
        ["Integrity checks", pluginDownloadIntegrityPreview.summary?.totalChecks],
        ["Integrity pass", pluginDownloadIntegrityPreview.summary?.pass],
        ["Integrity warning", pluginDownloadIntegrityPreview.summary?.warning],
        ["Integrity block", pluginDownloadIntegrityPreview.summary?.block],
        ["Integrity release ready", pluginDownloadIntegrityPreview.summary?.releaseReady ? "yes" : "no"],
        ["Checksum algorithm", pluginDownloadIntegrityPreview.checksumPolicy?.algorithm],
        ["Checksum required", pluginDownloadIntegrityPreview.checksumPolicy?.required ? "yes" : "no"],
        ["Redacted envelope only", pluginDownloadIntegrityPreview.checksumPolicy?.redactedEnvelopeOnly ? "yes" : "no"],
        ["Mismatch blocks", pluginDownloadIntegrityPreview.checksumPolicy?.mismatchBlocksRelease ? "yes" : "no"],
        ["Integrity version", pluginDownloadIntegrityPreview.samplePreview?.version],
        ["Integrity build", pluginDownloadIntegrityPreview.samplePreview?.buildLabel],
        ["Integrity file", pluginDownloadIntegrityPreview.samplePreview?.fileName],
        ["Integrity checksum", pluginDownloadIntegrityPreview.samplePreview?.checksum],
        ["Integrity match", pluginDownloadIntegrityPreview.samplePreview?.match ? "yes" : "no"],
        ["Integrity badge result", pluginDownloadIntegrityPreview.samplePreview?.badgeResult],
        ["Gate total", pluginReleaseChecklistGate.summary?.totalItems],
        ["Gate pass", pluginReleaseChecklistGate.summary?.pass],
        ["Gate hold", pluginReleaseChecklistGate.summary?.hold],
        ["Gate block", pluginReleaseChecklistGate.summary?.block],
        ["Gate release ready", pluginReleaseChecklistGate.summary?.releaseReady ? "yes" : "no"],
        ["Gate version", pluginReleaseChecklistGate.sampleGate?.version],
        ["Gate build", pluginReleaseChecklistGate.sampleGate?.buildLabel],
        ["Gate result", pluginReleaseChecklistGate.sampleGate?.result],
        ["Gate blocking items", formatWorkflowList(pluginReleaseChecklistGate.sampleGate?.blockingItems || [])],
        ["Gate hold items", formatWorkflowList(pluginReleaseChecklistGate.sampleGate?.holdItems || [])],
        ["Gate sample artifact", pluginReleaseChecklistGate.sampleGate?.artifact]
      ])}
      ${renderOperationItems((pluginCiBadgeSummary.badgeStates || []).map((item) => ({
        ...item,
        status: item.tone,
        label: item.result,
        detail: `${item.label} / release ready: ${item.releaseReady ? "yes" : "no"}`
      })), { showStatus: true, compact: true })}
      ${renderOperationItems((pluginReleaseChecklistGate.gateItems || []).map((item) => ({
        ...item,
        status: item.result,
        label: `${item.id} / ${item.source}`,
        detail: `release ready: ${item.releaseReady ? "yes" : "no"}`
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Validation total", pluginReleaseReportValidationCommand.summary?.totalAssertions],
        ["Validation pass", pluginReleaseReportValidationCommand.summary?.pass],
        ["Validation hold", pluginReleaseReportValidationCommand.summary?.hold],
        ["Validation block", pluginReleaseReportValidationCommand.summary?.block],
        ["Validation release ready", pluginReleaseReportValidationCommand.summary?.releaseReady ? "yes" : "no"]
      ])}
      ${renderOperationItems((pluginReleaseReportValidationCommand.sampleValidationResults || []).map((item) => ({
        ...item,
        status: item.result,
        label: item.id,
        detail: formatWorkflowList(item.evidence || [])
      })), { showStatus: true, compact: true })}
      ${renderOperationLines([
        ["Signed export total", pluginSignedReleaseReportExport.summary?.totalExports],
        ["Signed export signed", pluginSignedReleaseReportExport.summary?.signed],
        ["Signed export pending", pluginSignedReleaseReportExport.summary?.pendingSignature],
        ["Signed export blocked", pluginSignedReleaseReportExport.summary?.blocked],
        ["Signed export release ready", pluginSignedReleaseReportExport.summary?.releaseReady ? "yes" : "no"],
        ["Signature algorithm", pluginSignedReleaseReportExport.signatureEnvelope?.algorithm],
        ["Signature signer", pluginSignedReleaseReportExport.signatureEnvelope?.signer],
        ["Signature digest source", pluginSignedReleaseReportExport.signatureEnvelope?.digestSource],
        ["Signature required", pluginSignedReleaseReportExport.signatureEnvelope?.signatureRequired ? "yes" : "no"],
        ["Runtime secrets used", pluginSignedReleaseReportExport.signatureEnvelope?.runtimeSecretsUsed ? "yes" : "no"],
        ["Export version", pluginSignedReleaseReportExport.sampleExport?.version],
        ["Export build", pluginSignedReleaseReportExport.sampleExport?.buildLabel],
        ["Export ready", pluginSignedReleaseReportExport.sampleExport?.exportReady ? "yes" : "no"],
        ["Export signed", pluginSignedReleaseReportExport.sampleExport?.signed ? "yes" : "no"],
        ["Signature state", pluginSignedReleaseReportExport.sampleExport?.signatureState],
        ["Export attached reports", formatWorkflowList(pluginSignedReleaseReportExport.sampleExport?.attachedReports || [])]
      ])}
    `;
  }
  elements.phase20SecurityPanel.innerHTML = renderOperationLines([
    ["默认信任", security.defaultTrust],
    ["运行策略", plan.runtimePolicy],
    ["第三方代码", security.thirdPartyExecution ? "允许" : "不允许"],
    ["插件网络访问", security.networkAccessForPlugins ? "允许" : "不允许"],
    ["密钥存储", security.secretStorage ? "允许" : "不允许"],
    ["数据访问", security.dataAccess],
    ["必需控制", formatWorkflowList(security.requiredControls || [])],
    ["建议", readiness.recommendation]
  ]);
  elements.phase20MilestonePanel.innerHTML = `
    <div class="operation-line-list">
      ${(plan.nextMilestones || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
    </div>
    <p>${escapeHtml(readiness.pluginRuntimeReady ? "插件运行时已可用。" : "当前仅启用清单和边界规划，真实插件运行时仍保持关闭。")}</p>
  `;
}

function renderPhase25RuntimeSandboxUiSurface(surface = {}) {
  const panels = surface.surfacePanels || [];
  const tabs = surface.navigation?.tabs || [];
  const stateModel = surface.surfaceStateModel || {};
  const bindings = surface.panelEvidenceBindings || [];
  const reviewActionModel = surface.reviewActionModel || {};
  const reviewAuditPreview = surface.reviewAuditPreview || {};
  const reviewExportPackage = surface.reviewExportPackage || {};
  const reviewPackageValidation = surface.reviewPackageValidation || {};
  return `
    ${renderOperationLines([
      ["Schema", surface.schemaVersion],
      ["Status", surface.status],
      ["Mode", surface.surfaceMode],
      ["Source", surface.sourceClosure?.schemaVersion],
      ["Phase 24 complete", surface.sourceClosure?.phase24Complete ? "yes" : "no"],
      ["Panels", surface.summary?.panels],
      ["State model", stateModel.schemaVersion],
      ["Active panel", stateModel.defaultState?.activePanel],
      ["Evidence bindings", surface.summary?.evidenceBindings],
      ["Review actions", reviewActionModel.schemaVersion],
      ["Audit preview", reviewAuditPreview.schemaVersion],
      ["Export package", reviewExportPackage.schemaVersion],
      ["Package validation", reviewPackageValidation.schemaVersion],
      ["Runtime", surface.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Release", surface.summary?.releaseReady ? "ready" : "blocked"]
    ])}
    <div class="phase25-tab-strip">
      ${tabs.map((tab) => `<span>${escapeHtml(`${tab.label} · ${tab.routeHash}`)}</span>`).join("")}
    </div>
    ${renderOperationItems(panels.map((panel) => ({
      id: panel.id,
      label: `${panel.label} / ${panel.component}`,
      status: panel.status,
      detail: `${panel.sourceArtifact}；filters: ${formatWorkflowList(panel.filters || [])}；blocked: ${formatWorkflowList(panel.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems(bindings.map((binding) => ({
      id: binding.panelId,
      label: `${binding.panelId} evidence`,
      status: binding.redaction,
      detail: `${binding.sourceSchemaVersion} / fields: ${formatWorkflowList(binding.evidenceFields || [])} / checks: ${formatWorkflowList(binding.integrityChecks || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewActionModel.panelActions || []).map((item) => ({
      id: item.panelId,
      label: `${item.panelId} review actions`,
      status: item.auditPreview?.writesToReleaseState ? "mutating" : "preview-only",
      detail: `allowed: ${formatWorkflowList(item.allowedReviewActions || [])} / disabled: ${formatWorkflowList(item.disabledMutations || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewAuditPreview.previewEvents || []).map((item) => ({
      id: item.eventId,
      label: `${item.panelId} audit preview`,
      status: item.persisted ? "persisted" : "preview-only",
      detail: `${item.action} / ${item.sourceSchemaVersion} / redaction: ${item.redaction}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewExportPackage.includedArtifacts || []).map((item) => ({
      id: item.id,
      label: `${item.id} export artifact`,
      status: item.schemaVersion,
      detail: `${item.exportName} / source: ${item.source}${item.itemCount !== undefined ? ` / items: ${item.itemCount}` : ""}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewPackageValidation.artifactChecks || []).map((item) => ({
      id: item.artifactId,
      label: `${item.artifactId} validation`,
      status: item.result,
      detail: `${item.exportName} / checks: ${formatWorkflowList(item.checks || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewPackageValidation.guardrailChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} guardrail`,
      status: item.result,
      detail: `required: ${item.requiredValue} / actual: ${item.actualValue}`
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Shared filters", formatWorkflowList(surface.sharedControls?.filters || [])],
      ["Filter presets", formatWorkflowList((stateModel.filterPresets || []).map((item) => item.id))],
      ["Route hashes", formatWorkflowList(stateModel.routeState?.allowedHashes || [])],
      ["Blocked state mutations", formatWorkflowList(stateModel.blockedStateMutations || [])],
      ["Global review actions", formatWorkflowList(reviewActionModel.globalReviewActions || [])],
      ["Blocked review mutations", formatWorkflowList(reviewActionModel.blockedMutationPolicy?.blockedActions || [])],
      ["Audit export", reviewAuditPreview.exportPackage?.exportName],
      ["Audit blocked events", formatWorkflowList((reviewAuditPreview.blockedMutationEvents || []).map((item) => item.action))],
      ["Review package", reviewExportPackage.packageName],
      ["Package redaction", reviewExportPackage.integrityManifest?.redactionPolicy],
      ["Blocked export mutations", formatWorkflowList(reviewExportPackage.blockedExportMutations || [])],
      ["Validation mode", reviewPackageValidation.validationMode],
      ["Validation blocked mutations", formatWorkflowList(reviewPackageValidation.blockedValidationMutations || [])],
      ["Exports", formatWorkflowList(surface.sharedControls?.exports || [])],
      ["Empty states", formatWorkflowList(surface.sharedControls?.emptyStates || [])],
      ["Implementation", formatWorkflowList(surface.implementationPlan || [])]
    ])}
  `;
}

function renderPhase26RuntimeValidationEntry(entry = {}) {
  const gateSimulation = entry.releaseGateSimulation || {};
  const clearanceSimulation = entry.blockerClearanceSimulation || {};
  const validationReport = entry.runtimeValidationReport || {};
  const handoffCriteria = entry.handoffCriteria || {};
  const closurePackage = entry.closurePackage || {};
  return `
    ${renderOperationLines([
      ["Schema", entry.schemaVersion],
      ["Status", entry.status],
      ["Mode", entry.entryMode],
      ["Source", entry.sourcePhase25?.schemaVersion],
      ["Package validation", entry.sourcePhase25?.reviewPackageValidation],
      ["Export package", entry.sourcePhase25?.reviewExportPackage],
      ["Runtime", entry.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Release", entry.summary?.releaseReady ? "ready" : "blocked"]
    ])}
    ${renderOperationLines([
      ["Gate schema", gateSimulation.schemaVersion],
      ["Gate mode", gateSimulation.mode],
      ["Gate decision", gateSimulation.simulatedDecision?.result],
      ["Gate export", gateSimulation.exportPolicy?.exportName],
      ["Gate pass", gateSimulation.summary?.pass],
      ["Gate block", gateSimulation.summary?.block],
      ["Gate mutations", gateSimulation.summary?.persistedMutations]
    ])}
    ${renderOperationLines([
      ["Clearance schema", clearanceSimulation.schemaVersion],
      ["Clearance mode", clearanceSimulation.mode],
      ["Clearance decision", clearanceSimulation.simulatedDecision?.result],
      ["Clearance export", clearanceSimulation.exportPolicy?.exportName],
      ["Clearance total blockers", clearanceSimulation.summary?.totalBlockers],
      ["Clearance cleared", clearanceSimulation.summary?.clearedBlockers],
      ["Clearance remaining", clearanceSimulation.summary?.remainingBlockers]
    ])}
    ${renderOperationLines([
      ["Report schema", validationReport.schemaVersion],
      ["Report mode", validationReport.mode],
      ["Report decision", validationReport.reportDecision?.result],
      ["Report export", validationReport.exportPolicy?.exportName],
      ["Report sections", validationReport.summary?.reportSections],
      ["Report findings", validationReport.summary?.findings],
      ["Report blockers", validationReport.summary?.blockingFindings]
    ])}
    ${renderOperationLines([
      ["Handoff schema", handoffCriteria.schemaVersion],
      ["Handoff mode", handoffCriteria.mode],
      ["Handoff decision", handoffCriteria.handoffDecision?.result],
      ["Handoff export", handoffCriteria.exportPolicy?.exportName],
      ["Handoff criteria", handoffCriteria.summary?.totalCriteria],
      ["Handoff met", handoffCriteria.summary?.metCriteria],
      ["Carried blockers", handoffCriteria.summary?.carriedForwardBlockers]
    ])}
    ${renderOperationLines([
      ["Closure schema", closurePackage.schemaVersion],
      ["Closure mode", closurePackage.mode],
      ["Closure decision", closurePackage.closureDecision?.result],
      ["Closure export", closurePackage.exportPolicy?.exportName],
      ["Closure items", closurePackage.summary?.closureItems],
      ["Closure blockers", closurePackage.summary?.carriedForwardBlockers],
      ["Phase closed", closurePackage.summary?.phaseClosed ? "yes" : undefined]
    ])}
    ${renderOperationItems((entry.entryConditions || []).map((item) => ({
      id: item.id,
      label: `${item.id} entry condition`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((entry.validationScopes || []).map((item) => ({
      id: item.id,
      label: `${item.id} scope`,
      status: item.status,
      detail: item.source
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((gateSimulation.gateItems || []).map((item) => ({
      id: item.id,
      label: `${item.id} gate`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((clearanceSimulation.clearanceItems || []).map((item) => ({
      id: item.id,
      label: `${item.id} clearance`,
      status: item.simulatedDisposition,
      detail: `${formatWorkflowList(item.requiredEvidence || [])} / blocked: ${item.blockedMutation}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((validationReport.reportSections || []).map((item) => ({
      id: item.id,
      label: `${item.id} report section`,
      status: item.status,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((validationReport.findings || []).map((item) => ({
      id: item.id,
      label: `${item.id} finding`,
      status: item.severity,
      detail: item.detail
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((handoffCriteria.criteria || []).map((item) => ({
      id: item.id,
      label: `${item.id} handoff criterion`,
      status: item.result,
      detail: `${item.evidence} / required: ${item.requiredForHandoff ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.closureItems || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure item`,
      status: item.status,
      detail: item.artifact
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.carriedForwardBlockers || []).map((item) => ({
      id: item.id,
      label: `${item.id} carried blocker`,
      status: item.status,
      detail: `${item.source} / required: ${formatWorkflowList(item.requiredBeforeRelease || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Simulation plan", formatWorkflowList(entry.simulationPlan || [])],
      ["Blocked actions", formatWorkflowList(entry.blockedActions || [])],
      ["Gate blocked actions", formatWorkflowList(gateSimulation.blockedActions || [])],
      ["Gate reason", gateSimulation.simulatedDecision?.reason],
      ["Clearance steps", formatWorkflowList(clearanceSimulation.simulationSteps || [])],
      ["Clearance blocked actions", formatWorkflowList(clearanceSimulation.blockedActions || [])],
      ["Clearance reason", clearanceSimulation.simulatedDecision?.reason],
      ["Report blocked actions", formatWorkflowList(validationReport.blockedActions || [])],
      ["Report reason", validationReport.reportDecision?.reason],
      ["Handoff requirements", formatWorkflowList(handoffCriteria.nextPhaseEntryRequirements || [])],
      ["Handoff blocked actions", formatWorkflowList(handoffCriteria.blockedActions || [])],
      ["Handoff reason", handoffCriteria.handoffDecision?.reason],
      ["Closure blocked actions", formatWorkflowList(closurePackage.blockedActions || [])],
      ["Closure reason", closurePackage.closureDecision?.reason],
      ["Export", entry.exportPolicy?.exportName],
      ["Redaction", entry.exportPolicy?.redaction],
      ["Persisted mutations", entry.summary?.persistedMutations]
    ])}
  `;
}

function renderPhase27ReleaseBlockerGovernanceEntry(entry = {}) {
  const entryPackage = entry.entryPackage || {};
  const inventoryPackage = entry.carriedBlockerInventoryPackage || {};
  const signoffPackage = entry.humanReviewSignoffPackage || {};
  const gapMatrixPackage = entry.evidenceGapMatrixPackage || {};
  const resolutionPlanPackage = entry.blockerResolutionPlanPackage || {};
  const closurePackage = entry.phase27ClosurePackage || {};
  return `
    ${renderOperationLines([
      ["Schema", entry.schemaVersion],
      ["Status", entry.status],
      ["Mode", entry.entryMode],
      ["Source closure", entry.sourcePhase26?.closurePackage],
      ["Closure decision", entry.sourcePhase26?.closureDecision],
      ["Governance decision", entry.entryDecision?.result],
      ["Runtime", entry.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Release", entry.summary?.releaseReady ? "ready" : "blocked"]
    ])}
    ${renderOperationLines([
      ["Entry schema", entryPackage.schemaVersion],
      ["Entry mode", entryPackage.mode],
      ["Entry export", entryPackage.exportPolicy?.exportName],
      ["Accepted artifacts", entryPackage.summary?.acceptedArtifacts],
      ["Entry checks", entryPackage.summary?.entryChecks],
      ["Carried blockers", entryPackage.summary?.carriedBlockers],
      ["Persisted mutations", entryPackage.summary?.persistedMutations]
    ])}
    ${renderOperationLines([
      ["Inventory schema", inventoryPackage.schemaVersion],
      ["Inventory mode", inventoryPackage.mode],
      ["Inventory export", inventoryPackage.exportPolicy?.exportName],
      ["Open blockers", inventoryPackage.summary?.openBlockers],
      ["Cleared blockers", inventoryPackage.summary?.clearedBlockers],
      ["Inventory ready", inventoryPackage.summary?.inventoryReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Signoff schema", signoffPackage.schemaVersion],
      ["Signoff mode", signoffPackage.mode],
      ["Signoff export", signoffPackage.exportPolicy?.exportName],
      ["Required signoffs", signoffPackage.summary?.requiredSignoffs],
      ["Pending signoffs", signoffPackage.summary?.pendingSignoffs],
      ["Release approvals", signoffPackage.summary?.approvedReleaseSignoffs],
      ["Signoff ready", signoffPackage.summary?.signoffReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Gap matrix schema", gapMatrixPackage.schemaVersion],
      ["Gap matrix mode", gapMatrixPackage.mode],
      ["Gap matrix export", gapMatrixPackage.exportPolicy?.exportName],
      ["Matrix rows", gapMatrixPackage.summary?.matrixRows],
      ["Open gaps", gapMatrixPackage.summary?.openGaps],
      ["Pending evidence", gapMatrixPackage.summary?.pendingEvidenceGaps],
      ["Missing evidence", gapMatrixPackage.summary?.missingEvidenceGaps],
      ["Resolved gaps", gapMatrixPackage.summary?.resolvedGaps],
      ["Gap matrix ready", gapMatrixPackage.summary?.gapMatrixReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Resolution plan schema", resolutionPlanPackage.schemaVersion],
      ["Resolution plan mode", resolutionPlanPackage.mode],
      ["Resolution plan export", resolutionPlanPackage.exportPolicy?.exportName],
      ["Planned blockers", resolutionPlanPackage.summary?.plannedBlockers],
      ["Open plan items", resolutionPlanPackage.summary?.openPlanItems],
      ["Open evidence gaps", resolutionPlanPackage.summary?.openEvidenceGaps],
      ["Resolved gaps", resolutionPlanPackage.summary?.resolvedGaps],
      ["Cleared blockers", resolutionPlanPackage.summary?.clearedBlockers],
      ["Resolution plan ready", resolutionPlanPackage.summary?.blockerResolutionPlanReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Closure schema", closurePackage.schemaVersion],
      ["Closure mode", closurePackage.mode],
      ["Closure export", closurePackage.exportPolicy?.exportName],
      ["Closure decision", closurePackage.closureDecision?.result],
      ["Closure items", closurePackage.summary?.closureItems],
      ["Carried forward blockers", closurePackage.summary?.carriedForwardBlockers],
      ["Phase closed", closurePackage.summary?.phaseClosed ? "yes" : "no"],
      ["Next stage planning", closurePackage.summary?.nextStagePlanningAllowed ? "allowed" : "blocked"],
      ["Release", closurePackage.summary?.releaseReady ? "ready" : "blocked"]
    ])}
    ${renderOperationItems((entryPackage.entryChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} entry check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((inventoryPackage.inventoryChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} inventory check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((signoffPackage.signoffChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} signoff check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((gapMatrixPackage.matrixChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} gap matrix check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((resolutionPlanPackage.planChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} resolution plan check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.closureChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((signoffPackage.signoffQueue || []).map((item) => ({
      id: item.signoffId,
      label: `${item.blockerId} signoff`,
      status: item.currentDisposition,
      detail: `${item.reviewerRole} / evidence: ${item.evidenceStatus} / required: ${formatWorkflowList(item.requiredEvidence || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((gapMatrixPackage.matrixRows || []).map((item) => ({
      id: item.gapId,
      label: `${item.blockerId} evidence gap`,
      status: item.gapState,
      detail: `${item.requiredEvidence} / state: ${item.currentEvidenceState} / source: ${item.currentEvidenceSource} / impact: ${item.releaseImpact}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((resolutionPlanPackage.resolutionPlanItems || []).map((item) => ({
      id: item.planId,
      label: `${item.blockerId} resolution plan`,
      status: item.currentPlanState,
      detail: `${item.ownerRole} / disposition: ${item.blockerDisposition} / gaps: ${item.openEvidenceGaps} / steps: ${formatWorkflowList(item.proposedResolutionSteps || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.closureItems || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure item`,
      status: item.status,
      detail: item.artifact
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.carriedForwardBlockers || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure carried blocker`,
      status: item.status,
      detail: `${item.source} / disposition: ${item.blockerDisposition} / required: ${formatWorkflowList(item.requiredBeforeRelease || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((entry.carriedBlockerInventory || []).map((item) => ({
      id: item.id,
      label: `${item.id} carried blocker`,
      status: item.status,
      detail: `${item.source} / ${item.sourceArtifact || "source"} / owner: ${item.governanceOwner} / required: ${formatWorkflowList(item.requiredEvidence || item.requiredBeforeRelease || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((entry.governanceScopes || []).map((item) => ({
      id: item.id,
      label: `${item.id} governance scope`,
      status: item.status,
      detail: item.source
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Blocked actions", formatWorkflowList(entry.blockedActions || [])],
      ["Inventory blocked mutations", formatWorkflowList(inventoryPackage.blockedMutations || [])],
      ["Signoff blocked mutations", formatWorkflowList(signoffPackage.blockedMutations || [])],
      ["Gap matrix blocked mutations", formatWorkflowList(gapMatrixPackage.blockedMutations || [])],
      ["Resolution plan blocked mutations", formatWorkflowList(resolutionPlanPackage.blockedMutations || [])],
      ["Closure blocked mutations", formatWorkflowList(closurePackage.blockedMutations || [])],
      ["Decision reason", entry.entryDecision?.reason],
      ["Closure reason", closurePackage.closureDecision?.reason],
      ["Export", entry.exportPolicy?.exportName],
      ["Redaction", entry.exportPolicy?.redaction],
      ["Governance ready", entry.summary?.governanceReady ? "yes" : "no"]
    ])}
  `;
}

function renderPhase28ClearanceReviewEntry(entry = {}) {
  const evidenceLedger = entry.evidenceIntakeLedger || {};
  const reviewerDisposition = entry.reviewerDispositionModel || {};
  const criteriaChecklist = entry.clearanceCriteriaChecklist || {};
  const decisionPreview = entry.clearanceDecisionPreview || {};
  const closurePackage = entry.phase28ClosurePackage || {};
  return `
    ${renderOperationLines([
      ["Schema", entry.schemaVersion],
      ["Status", entry.status],
      ["Mode", entry.entryMode],
      ["Source phase 27", entry.sourcePhase27?.schemaVersion],
      ["Source closure", entry.sourcePhase27?.closurePackage],
      ["Phase 27 closed", entry.sourcePhase27?.phase27Closed ? "yes" : "no"],
      ["Runtime", entry.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Release", entry.summary?.releaseReady ? "ready" : "blocked"]
    ])}
    ${renderOperationLines([
      ["Review items", entry.summary?.reviewItems],
      ["Pending reviews", entry.summary?.pendingReviewItems],
      ["Evidence ledger entries", entry.summary?.evidenceLedgerEntries],
      ["Pending evidence intake", entry.summary?.pendingEvidenceIntake],
      ["Reviewer dispositions", entry.summary?.reviewerDispositions],
      ["Pending reviewer dispositions", entry.summary?.pendingReviewerDispositions],
      ["Approved release dispositions", entry.summary?.approvedReleaseDispositions],
      ["Clearance criteria items", entry.summary?.clearanceCriteriaItems],
      ["Pending clearance criteria", entry.summary?.pendingClearanceCriteria],
      ["Satisfied clearance criteria", entry.summary?.satisfiedClearanceCriteria],
      ["Approved release criteria", entry.summary?.approvedReleaseCriteria],
      ["Decision previews", entry.summary?.clearanceDecisionPreviews],
      ["Hold clearance previews", entry.summary?.holdClearancePreviews],
      ["Approved release previews", entry.summary?.approvedReleasePreviews],
      ["Phase 28 closed", entry.summary?.phase28Closed ? "yes" : "no"],
      ["Closure package ready", entry.summary?.phase28ClosureReady ? "yes" : "no"],
      ["Closure items", entry.summary?.closureItems],
      ["Closure checks", entry.summary?.closureChecks],
      ["Carried forward blockers", entry.summary?.carriedForwardBlockers],
      ["Accepted evidence for clearance", entry.summary?.acceptedEvidenceForClearance],
      ["Cleared blockers", entry.summary?.clearedBlockers],
      ["Persisted mutations", entry.summary?.persistedMutations],
      ["Export", entry.exportPolicy?.exportName],
      ["Redaction", entry.exportPolicy?.redaction]
    ])}
    ${renderOperationLines([
      ["Evidence ledger schema", evidenceLedger.schemaVersion],
      ["Evidence ledger mode", evidenceLedger.mode],
      ["Evidence ledger export", evidenceLedger.exportPolicy?.exportName],
      ["Ledger entries", evidenceLedger.summary?.ledgerEntries],
      ["Pending intake", evidenceLedger.summary?.pendingEvidenceIntake],
      ["Accepted for clearance", evidenceLedger.summary?.acceptedEvidenceForClearance],
      ["Ledger ready", evidenceLedger.summary?.evidenceIntakeLedgerReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Reviewer disposition schema", reviewerDisposition.schemaVersion],
      ["Reviewer disposition mode", reviewerDisposition.mode],
      ["Reviewer disposition export", reviewerDisposition.exportPolicy?.exportName],
      ["Dispositions", reviewerDisposition.summary?.dispositions],
      ["Pending dispositions", reviewerDisposition.summary?.pendingDispositions],
      ["Approved release dispositions", reviewerDisposition.summary?.approvedReleaseDispositions],
      ["Disposition ready", reviewerDisposition.summary?.reviewerDispositionModelReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Criteria checklist schema", criteriaChecklist.schemaVersion],
      ["Criteria checklist mode", criteriaChecklist.mode],
      ["Criteria checklist export", criteriaChecklist.exportPolicy?.exportName],
      ["Criteria items", criteriaChecklist.summary?.criteriaItems],
      ["Pending criteria", criteriaChecklist.summary?.pendingCriteriaItems],
      ["Satisfied criteria", criteriaChecklist.summary?.satisfiedCriteriaItems],
      ["Criteria ready", criteriaChecklist.summary?.clearanceCriteriaChecklistReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Decision preview schema", decisionPreview.schemaVersion],
      ["Decision preview mode", decisionPreview.mode],
      ["Decision preview export", decisionPreview.exportPolicy?.exportName],
      ["Preview items", decisionPreview.summary?.previewItems],
      ["Hold clearance previews", decisionPreview.summary?.holdClearancePreviews],
      ["Approved release previews", decisionPreview.summary?.approvedReleasePreviews],
      ["Decision preview ready", decisionPreview.summary?.clearanceDecisionPreviewReady ? "yes" : "no"]
    ])}
    ${renderOperationLines([
      ["Closure schema", closurePackage.schemaVersion],
      ["Closure mode", closurePackage.mode],
      ["Closure export", closurePackage.exportPolicy?.exportName],
      ["Closure items", closurePackage.summary?.closureItems],
      ["Closure checks", closurePackage.summary?.closureChecks],
      ["Phase closed", closurePackage.summary?.phaseClosed ? "yes" : "no"],
      ["Next phase entry ready", closurePackage.handoff?.nextPhaseEntryReady ? "yes" : "no"],
      ["Recommended next stage", closurePackage.handoff?.recommendedNextStage],
      ["Carried forward blockers", closurePackage.summary?.carriedForwardBlockers],
      ["Release", closurePackage.summary?.releaseReady ? "ready" : "blocked"],
      ["Runtime", closurePackage.summary?.runtimeExecution ? "enabled" : "disabled"]
    ])}
    ${renderOperationItems((entry.entryChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} clearance entry check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((evidenceLedger.ledgerChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} evidence ledger check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewerDisposition.dispositionChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} reviewer disposition check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((criteriaChecklist.checklistChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} clearance criteria check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((decisionPreview.previewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} decision preview check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.closureChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((entry.clearanceReviewQueue || []).map((item) => ({
      id: item.reviewItemId,
      label: `${item.blockerId} clearance review`,
      status: item.reviewState,
      detail: `${item.source} / disposition: ${item.blockerDisposition} / required: ${formatWorkflowList(item.requiredEvidence || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((evidenceLedger.ledgerEntries || []).map((item) => ({
      id: item.ledgerEntryId,
      label: `${item.blockerId} evidence intake`,
      status: item.intakeState,
      detail: `${item.evidenceRequirement} / disposition: ${item.evidenceDisposition} / audit: ${item.auditState}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((reviewerDisposition.dispositionQueue || []).map((item) => ({
      id: item.dispositionId,
      label: `${item.blockerId} reviewer disposition`,
      status: item.dispositionState,
      detail: `${item.reviewerRole} / proposed: ${item.proposedDisposition} / impact: ${item.releaseImpact}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((criteriaChecklist.checklistItems || []).map((item) => ({
      id: item.criteriaId,
      label: `${item.blockerId} clearance criteria`,
      status: item.criteriaState,
      detail: `${item.criteriaDisposition} / required: ${formatWorkflowList(item.requiredCriteria || [])} / audit: ${item.auditState}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((decisionPreview.previewItems || []).map((item) => ({
      id: item.previewId,
      label: `${item.blockerId} decision preview`,
      status: item.previewState,
      detail: `${item.previewDecision} / reason: ${item.decisionReason} / impact: ${item.releaseImpact}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((closurePackage.closureItems || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure item`,
      status: item.state,
      detail: `${item.schemaVersion} / ready: ${item.ready ? "yes" : "no"} / releaseReady: ${item.releaseReady ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((entry.clearanceReviewQueue || []).map((item) => ({
      id: `${item.reviewItemId}-blocked-actions`,
      label: `${item.reviewItemId} blocked actions`,
      status: item.auditState,
      detail: formatWorkflowList(item.blockedActions || [])
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Blocked mutations", formatWorkflowList(entry.blockedMutations || [])],
      ["Evidence ledger blocked mutations", formatWorkflowList(evidenceLedger.blockedMutations || [])],
      ["Reviewer disposition blocked mutations", formatWorkflowList(reviewerDisposition.blockedMutations || [])],
      ["Criteria checklist blocked mutations", formatWorkflowList(criteriaChecklist.blockedMutations || [])],
      ["Decision preview blocked mutations", formatWorkflowList(decisionPreview.blockedMutations || [])],
      ["Closure blocked mutations", formatWorkflowList(closurePackage.blockedMutations || [])],
      ["Allowed review actions", formatWorkflowList([...(new Set((entry.clearanceReviewQueue || []).flatMap((item) => item.allowedActions || [])))] )],
      ["Allowed evidence actions", formatWorkflowList([...(new Set((evidenceLedger.ledgerEntries || []).flatMap((item) => item.allowedActions || [])))] )],
      ["Allowed disposition actions", formatWorkflowList([...(new Set((reviewerDisposition.dispositionQueue || []).flatMap((item) => item.allowedActions || [])))] )],
      ["Allowed criteria actions", formatWorkflowList([...(new Set((criteriaChecklist.checklistItems || []).flatMap((item) => item.allowedActions || [])))] )],
      ["Allowed preview actions", formatWorkflowList([...(new Set((decisionPreview.previewItems || []).flatMap((item) => item.allowedActions || [])))] )],
      ["Clearance review ready", entry.summary?.clearanceReviewEntryReady ? "yes" : "no"]
    ])}
  `;
}

function renderPhase29ReleaseGovernancePlanning(plan = {}) {
  return `
    ${renderOperationLines([
      ["Schema", plan.schemaVersion],
      ["Status", plan.status],
      ["Mode", plan.planningMode],
      ["Source phase 28", plan.sourcePhase28?.schemaVersion],
      ["Source closure", plan.sourcePhase28?.closurePackage],
      ["Phase 28 closed", plan.sourcePhase28?.phase28Closed ? "yes" : "no"],
      ["Planning ready", plan.summary?.planningReady ? "yes" : "no"],
      ["Blocker workflow ready", plan.summary?.blockerClearanceWorkflowReady ? "yes" : "no"],
      ["Approval state model ready", plan.summary?.releaseApprovalStateModelReady ? "yes" : "no"],
      ["Runtime governance ready", plan.summary?.runtimeEnableGovernanceReady ? "yes" : "no"],
      ["Dashboard export ready", plan.summary?.governanceDashboardExportReady ? "yes" : "no"],
      ["Decision audit ready", plan.summary?.decisionHistoryAuditChainReady ? "yes" : "no"],
      ["Closure readiness ready", plan.summary?.governanceClosureReadinessReady ? "yes" : "no"],
      ["Closure handoff ready", plan.summary?.closureHandoffPackageReady ? "yes" : "no"],
      ["Handoff review index ready", plan.summary?.handoffReviewIndexReady ? "yes" : "no"],
      ["Final snapshot ready", plan.summary?.finalClosureReadinessSnapshotReady ? "yes" : "no"],
      ["Archive freeze ready", plan.summary?.governanceArchiveFreezeReady ? "yes" : "no"],
      ["Blocker resolution map ready", plan.summary?.blockerResolutionMapReady ? "yes" : "no"],
      ["Phase 30 preflight ready", plan.summary?.phase30EntryPreflightReady ? "yes" : "no"],
      ["Final blocker dossier ready", plan.summary?.finalReleaseBlockerDossierReady ? "yes" : "no"],
      ["Release exit ledger ready", plan.summary?.releaseExitCriteriaLedgerReady ? "yes" : "no"],
      ["Release exit decision preview ready", plan.summary?.releaseExitDecisionPreviewReady ? "yes" : "no"],
      ["Phase 30 transition risk register ready", plan.summary?.phase30TransitionRiskRegisterReady ? "yes" : "no"],
      ["Transition evidence gap review ready", plan.summary?.transitionEvidenceGapReviewReady ? "yes" : "no"],
      ["Transition readiness redline ready", plan.summary?.transitionReadinessRedlineReady ? "yes" : "no"],
      ["Final transition handoff preview ready", plan.summary?.finalTransitionHandoffPreviewReady ? "yes" : "no"],
      ["Transition closure package preview ready", plan.summary?.transitionClosurePackagePreviewReady ? "yes" : "no"],
      ["Release candidate freeze preview ready", plan.summary?.releaseCandidateFreezePreviewReady ? "yes" : "no"],
      ["Final readiness board preview ready", plan.summary?.finalReadinessBoardPreviewReady ? "yes" : "no"],
      ["Release final review envelope preview ready", plan.summary?.releaseFinalReviewEnvelopePreviewReady ? "yes" : "no"],
      ["Exit readiness attestation preview ready", plan.summary?.exitReadinessAttestationPreviewReady ? "yes" : "no"],
      ["Phase 30 planning brief preview ready", plan.summary?.phase30PlanningBriefPreviewReady ? "yes" : "no"],
      ["Phase 30 scope lock preview ready", plan.summary?.phase30ScopeLockPreviewReady ? "yes" : "no"],
      ["Phase 30 readiness map preview ready", plan.summary?.phase30ReadinessMapPreviewReady ? "yes" : "no"],
      ["Phase 30 readiness gap brief preview ready", plan.summary?.phase30ReadinessGapBriefPreviewReady ? "yes" : "no"],
      ["Phase 30 gap resolution sequence preview ready", plan.summary?.phase30GapResolutionSequencePreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence pack preview ready", plan.summary?.phase30EntryEvidencePackPreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence index preview ready", plan.summary?.phase30EntryEvidenceIndexPreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence review queue preview ready", plan.summary?.phase30EntryEvidenceReviewQueuePreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence review disposition preview ready", plan.summary?.phase30EntryEvidenceReviewDispositionPreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence disposition ledger preview ready", plan.summary?.phase30EntryEvidenceDispositionLedgerPreviewReady ? "yes" : "no"],
      ["Phase 30 entry evidence final ledger export preview ready", plan.summary?.phase30EntryEvidenceFinalLedgerExportPreviewReady ? "yes" : "no"],
      ["Phase 30 entry final export reconciliation preview ready", plan.summary?.phase30EntryFinalExportReconciliationPreviewReady ? "yes" : "no"],
      ["Phase 30 entry reconciliation signoff preview ready", plan.summary?.phase30EntryReconciliationSignoffPreviewReady ? "yes" : "no"],
      ["Phase 30 entry signoff audit preview ready", plan.summary?.phase30EntrySignoffAuditPreviewReady ? "yes" : "no"],
      ["Governance tracks", plan.summary?.governanceTracks],
      ["Dashboard sections", plan.summary?.dashboardSections],
      ["Included exports", plan.summary?.includedExports],
      ["Decision events", plan.summary?.decisionEvents],
      ["Audit chain links", plan.summary?.auditChainLinks],
      ["Closure criteria", plan.summary?.closureCriteria],
      ["Blocked closure items", plan.summary?.blockedClosureItems],
      ["Handoff sections", plan.summary?.handoffSections],
      ["Handoff artifacts", plan.summary?.handoffArtifacts],
      ["Review index entries", plan.summary?.reviewIndexEntries],
      ["Blocked review items", plan.summary?.blockedReviewItems],
      ["Final snapshot models", plan.summary?.finalSnapshotModels],
      ["Final snapshot blocked items", plan.summary?.finalSnapshotBlockedItems],
      ["Archived exports", plan.summary?.archivedExports],
      ["Frozen guards", plan.summary?.frozenGuards],
      ["Resolution items", plan.summary?.resolutionItems],
      ["Resolution lanes", plan.summary?.resolutionLanes],
      ["Preflight items", plan.summary?.preflightItems],
      ["Blocked entry gates", plan.summary?.blockedEntryGates],
      ["Final dossier unresolved blockers", plan.summary?.finalDossierUnresolvedBlockers],
      ["Final dossier blocked gates", plan.summary?.finalDossierBlockedReleaseGates],
      ["Exit criteria", plan.summary?.exitCriteria],
      ["Unmet exit criteria", plan.summary?.unmetExitCriteria],
      ["Blocked exit gates", plan.summary?.blockedExitGates],
      ["Exit decision preview items", plan.summary?.exitDecisionPreviewItems],
      ["Blocked exit decision items", plan.summary?.blockedExitDecisionItems],
      ["Phase 29 exit decision", plan.summary?.phase29ExitDecision],
      ["Transition risk items", plan.summary?.transitionRiskItems],
      ["Blocked transition risks", plan.summary?.blockedTransitionRisks],
      ["Transition mitigation lanes", plan.summary?.transitionMitigationLanes],
      ["Transition evidence gaps", plan.summary?.transitionEvidenceGaps],
      ["Blocking evidence gaps", plan.summary?.blockingEvidenceGaps],
      ["Evidence review lanes", plan.summary?.evidenceReviewLanes],
      ["Transition redlines", plan.summary?.transitionRedlines],
      ["Active transition redlines", plan.summary?.activeTransitionRedlines],
      ["Redline review items", plan.summary?.redlineReviewItems],
      ["Handoff preview sections", plan.summary?.handoffPreviewSections],
      ["Blocked handoff preview sections", plan.summary?.blockedHandoffPreviewSections],
      ["Handoff preview checks", plan.summary?.handoffPreviewChecks],
      ["Closure preview items", plan.summary?.closurePreviewItems],
      ["Blocked closure preview items", plan.summary?.blockedClosurePreviewItems],
      ["Closure preview checks", plan.summary?.closurePreviewChecks],
      ["Freeze preview items", plan.summary?.freezePreviewItems],
      ["Blocked freeze preview items", plan.summary?.blockedFreezePreviewItems],
      ["Freeze preview checks", plan.summary?.freezePreviewChecks],
      ["Readiness board items", plan.summary?.readinessBoardItems],
      ["Blocked readiness board items", plan.summary?.blockedReadinessBoardItems],
      ["Readiness board checks", plan.summary?.readinessBoardChecks],
      ["Final review envelope sections", plan.summary?.finalReviewEnvelopeSections],
      ["Blocked final review envelope sections", plan.summary?.blockedFinalReviewEnvelopeSections],
      ["Final review envelope checks", plan.summary?.finalReviewEnvelopeChecks],
      ["Exit readiness attestation items", plan.summary?.exitReadinessAttestationItems],
      ["Blocked exit readiness attestation items", plan.summary?.blockedExitReadinessAttestationItems],
      ["Exit readiness attestation checks", plan.summary?.exitReadinessAttestationChecks],
      ["Phase 30 planning brief sections", plan.summary?.phase30PlanningBriefSections],
      ["Blocked Phase 30 planning brief sections", plan.summary?.blockedPhase30PlanningBriefSections],
      ["Phase 30 planning brief checks", plan.summary?.phase30PlanningBriefChecks],
      ["Phase 30 scope lock items", plan.summary?.phase30ScopeLockItems],
      ["Blocked Phase 30 scope lock items", plan.summary?.blockedPhase30ScopeLockItems],
      ["Phase 30 scope lock checks", plan.summary?.phase30ScopeLockChecks],
      ["Phase 30 readiness map items", plan.summary?.phase30ReadinessMapItems],
      ["Blocked Phase 30 readiness map items", plan.summary?.blockedPhase30ReadinessMapItems],
      ["Phase 30 readiness map checks", plan.summary?.phase30ReadinessMapChecks],
      ["Phase 30 readiness gap brief items", plan.summary?.phase30ReadinessGapBriefItems],
      ["Blocked Phase 30 readiness gap brief items", plan.summary?.blockedPhase30ReadinessGapBriefItems],
      ["Phase 30 readiness gap brief checks", plan.summary?.phase30ReadinessGapBriefChecks],
      ["Phase 30 gap resolution sequence items", plan.summary?.phase30GapResolutionSequenceItems],
      ["Blocked Phase 30 gap resolution sequence items", plan.summary?.blockedPhase30GapResolutionSequenceItems],
      ["Phase 30 gap resolution sequence checks", plan.summary?.phase30GapResolutionSequenceChecks],
      ["Phase 29 exit", plan.summary?.phase29ExitReady ? "ready" : "blocked"],
      ["Phase 30 blocked", plan.summary?.phase30Blocked ? "yes" : "no"],
      ["Phase 30 entry", plan.summary?.phase30EntryReady ? "ready" : "blocked"],
      ["Separated gates", plan.summary?.separatedGates],
      ["Carried forward blockers", plan.summary?.carriedForwardBlockers],
      ["Pending workflow items", plan.summary?.pendingWorkflowItems],
      ["Approval states", plan.summary?.approvalStates],
      ["Approved release states", plan.summary?.approvedReleaseStates],
      ["Runtime gates", plan.summary?.runtimeGates],
      ["Runtime enable approvals", plan.summary?.runtimeEnableApprovals],
      ["Cleared blockers", plan.summary?.clearedBlockers],
      ["Release", plan.summary?.releaseReady ? "ready" : "blocked"],
      ["Runtime", plan.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Persisted mutations", plan.summary?.persistedMutations],
      ["Export", plan.exportPolicy?.exportName],
      ["Redaction", plan.exportPolicy?.redaction]
    ])}
    ${renderOperationItems((plan.governanceTracks || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.state,
      detail: `${item.owner} / required: ${formatWorkflowList(item.requiredBeforeRelease || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Workflow schema", plan.blockerClearanceWorkflow?.schemaVersion],
      ["Workflow mode", plan.blockerClearanceWorkflow?.mode],
      ["Workflow export", plan.blockerClearanceWorkflow?.exportPolicy?.exportName],
      ["Pending workflow", plan.blockerClearanceWorkflow?.summary?.pendingWorkflowItems],
      ["Approved clearance items", plan.blockerClearanceWorkflow?.summary?.approvedClearanceItems]
    ])}
    ${renderOperationItems((plan.blockerClearanceWorkflow?.workflowItems || []).map((item) => ({
      id: item.workflowItemId,
      label: item.blockerId || item.workflowItemId,
      status: item.workflowState,
      detail: `${item.ownerRole} / required: ${formatWorkflowList(item.evidenceRequirements || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.blockerClearanceWorkflow?.workflowChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} workflow check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Approval schema", plan.releaseApprovalStateModel?.schemaVersion],
      ["Approval mode", plan.releaseApprovalStateModel?.mode],
      ["Approval export", plan.releaseApprovalStateModel?.exportPolicy?.exportName],
      ["Blocked transitions", plan.releaseApprovalStateModel?.summary?.blockedTransitions],
      ["Approved states", plan.releaseApprovalStateModel?.summary?.approvedReleaseStates]
    ])}
    ${renderOperationItems((plan.releaseApprovalStateModel?.approvalStates || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.state,
      detail: `releaseReady: ${item.releaseReady ? "true" : "false"} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseApprovalStateModel?.transitionRules || []).map((item) => ({
      id: `${item.from}-${item.to}`,
      label: `${item.from} -> ${item.to}`,
      status: item.allowed ? "allowed-readonly" : "blocked",
      detail: `${item.condition} / mutatesReleaseReady: ${item.mutatesReleaseReady ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseApprovalStateModel?.approvalChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} approval check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Runtime governance schema", plan.runtimeEnableGovernance?.schemaVersion],
      ["Runtime governance mode", plan.runtimeEnableGovernance?.mode],
      ["Runtime governance export", plan.runtimeEnableGovernance?.exportPolicy?.exportName],
      ["Planning-only gates", plan.runtimeEnableGovernance?.summary?.planningOnlyGates],
      ["Runtime approvals", plan.runtimeEnableGovernance?.summary?.runtimeEnableApprovals]
    ])}
    ${renderOperationItems((plan.runtimeEnableGovernance?.runtimeGates || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.state,
      detail: `${item.source} / required: ${formatWorkflowList(item.requiredEvidence || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.runtimeEnableGovernance?.enablementChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} runtime check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Dashboard schema", plan.governanceDashboardExport?.schemaVersion],
      ["Dashboard mode", plan.governanceDashboardExport?.mode],
      ["Dashboard export", plan.governanceDashboardExport?.exportPolicy?.exportName],
      ["Dashboard sections", plan.governanceDashboardExport?.summary?.dashboardSections],
      ["Included exports", plan.governanceDashboardExport?.summary?.includedExports],
      ["Dashboard persisted mutations", plan.governanceDashboardExport?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.governanceDashboardExport?.dashboardSections || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.state,
      detail: `${item.sourceSchemaVersion} / export: ${item.exportName} / metrics: ${formatWorkflowList(Object.entries(item.keyMetrics || {}).map(([key, value]) => `${key}=${value}`))}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceDashboardExport?.dashboardChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} dashboard check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceDashboardExport?.exportManifest || []).map((item) => ({
      id: item.id,
      label: item.exportName,
      status: item.includeInOperationsExport ? "included" : "excluded",
      detail: item.sourceSchemaVersion
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Decision audit schema", plan.decisionHistoryAuditChain?.schemaVersion],
      ["Decision audit mode", plan.decisionHistoryAuditChain?.mode],
      ["Decision audit export", plan.decisionHistoryAuditChain?.exportPolicy?.exportName],
      ["Decision events", plan.decisionHistoryAuditChain?.summary?.decisionEvents],
      ["Audit links", plan.decisionHistoryAuditChain?.summary?.chainLinks],
      ["Passed audit checks", plan.decisionHistoryAuditChain?.summary?.passedAuditChecks],
      ["Decision audit persisted mutations", plan.decisionHistoryAuditChain?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.decisionHistoryAuditChain?.decisionEvents || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.decisionState,
      detail: `${item.sourceSchemaVersion} / releaseReady: ${item.releaseReady ? "true" : "false"} / runtimeExecution: ${item.runtimeExecution ? "true" : "false"} / mutatesReleaseState: ${item.mutatesReleaseState ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.decisionHistoryAuditChain?.chainLinks || []).map((item) => ({
      id: item.id,
      label: `${item.fromEvent} -> ${item.toEvent}`,
      status: item.linkState,
      detail: `${item.sourceSchemaVersion} / ${item.integrityCheck}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.decisionHistoryAuditChain?.auditChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} audit check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Closure readiness schema", plan.governanceClosureReadiness?.schemaVersion],
      ["Closure readiness mode", plan.governanceClosureReadiness?.mode],
      ["Closure readiness export", plan.governanceClosureReadiness?.exportPolicy?.exportName],
      ["Closure criteria", plan.governanceClosureReadiness?.summary?.closureCriteria],
      ["Passed closure criteria", plan.governanceClosureReadiness?.summary?.passedClosureCriteria],
      ["Blocked closure items", plan.governanceClosureReadiness?.summary?.blockedClosureItems],
      ["Phase 30 blocked", plan.governanceClosureReadiness?.summary?.phase30Blocked ? "yes" : "no"],
      ["Closure persisted mutations", plan.governanceClosureReadiness?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.governanceClosureReadiness?.closureCriteria || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.result,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / releaseReady: ${item.releaseReady ? "true" : "false"} / runtimeExecution: ${item.runtimeExecution ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceClosureReadiness?.blockedClosureItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.status,
      detail: `count: ${item.count} / blocks: ${formatWorkflowList(item.blocks || [])} / required: ${formatWorkflowList(item.requiredBeforeRelease || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceClosureReadiness?.closureChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceClosureReadiness?.exportManifest || []).map((item) => ({
      id: item.id,
      label: item.exportName,
      status: item.includeInOperationsExport ? "included" : "excluded",
      detail: item.sourceSchemaVersion
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Closure handoff schema", plan.closureHandoffPackage?.schemaVersion],
      ["Closure handoff mode", plan.closureHandoffPackage?.mode],
      ["Closure handoff export", plan.closureHandoffPackage?.exportPolicy?.exportName],
      ["Handoff sections", plan.closureHandoffPackage?.summary?.handoffSections],
      ["Handoff artifacts", plan.closureHandoffPackage?.summary?.handoffArtifacts],
      ["Passed handoff checks", plan.closureHandoffPackage?.summary?.passedHandoffChecks],
      ["Phase 30 blocked by handoff", plan.closureHandoffPackage?.summary?.phase30Blocked ? "yes" : "no"],
      ["Handoff persisted mutations", plan.closureHandoffPackage?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.closureHandoffPackage?.handoffSections || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.handoffState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / releaseReady: ${item.releaseReady ? "true" : "false"} / runtimeExecution: ${item.runtimeExecution ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.closureHandoffPackage?.handoffArtifacts || []).map((item) => ({
      id: item.id,
      label: item.exportName,
      status: item.requiredForHandoff ? "required" : "optional",
      detail: item.sourceSchemaVersion
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.closureHandoffPackage?.handoffChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} handoff check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Handoff review index schema", plan.handoffReviewIndex?.schemaVersion],
      ["Handoff review index mode", plan.handoffReviewIndex?.mode],
      ["Handoff review index export", plan.handoffReviewIndex?.exportPolicy?.exportName],
      ["Review index entries", plan.handoffReviewIndex?.summary?.reviewIndexEntries],
      ["Blocked review items", plan.handoffReviewIndex?.summary?.blockedReviewItems],
      ["Indexed artifacts", plan.handoffReviewIndex?.summary?.indexedArtifacts],
      ["Indexed checks", plan.handoffReviewIndex?.summary?.indexedChecks],
      ["Review index persisted mutations", plan.handoffReviewIndex?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.handoffReviewIndex?.reviewIndexEntries || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.reviewState,
      detail: `${item.sourceSchemaVersion} / indexed: ${item.indexedItems} / required: ${formatWorkflowList(item.requiredBeforeRelease || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.handoffReviewIndex?.blockedReviewItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.reviewState,
      detail: `${item.source} / count: ${item.count} / blocks: ${formatWorkflowList(item.blocks || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.handoffReviewIndex?.reviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} review index check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Final snapshot schema", plan.finalClosureReadinessSnapshot?.schemaVersion],
      ["Final snapshot mode", plan.finalClosureReadinessSnapshot?.mode],
      ["Final snapshot export", plan.finalClosureReadinessSnapshot?.exportPolicy?.exportName],
      ["Final snapshot models", plan.finalClosureReadinessSnapshot?.summary?.readinessModels],
      ["Ready snapshot models", plan.finalClosureReadinessSnapshot?.summary?.readyModels],
      ["Blocked snapshot items", plan.finalClosureReadinessSnapshot?.summary?.blockedSnapshotItems],
      ["Passed snapshot checks", plan.finalClosureReadinessSnapshot?.summary?.passedSnapshotChecks],
      ["Final snapshot persisted mutations", plan.finalClosureReadinessSnapshot?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.finalClosureReadinessSnapshot?.readinessModels || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.ready ? "ready-readonly" : "blocked",
      detail: `${item.schemaVersion} / releaseReady: ${item.releaseReady ? "true" : "false"} / persistedMutations: ${item.persistedMutations}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalClosureReadinessSnapshot?.blockedSnapshotItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.snapshotState,
      detail: `${item.source} / count: ${item.count} / blocks: ${formatWorkflowList(item.blocks || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalClosureReadinessSnapshot?.snapshotChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} final snapshot check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Archive freeze schema", plan.governanceArchiveFreeze?.schemaVersion],
      ["Archive freeze mode", plan.governanceArchiveFreeze?.mode],
      ["Archive freeze export", plan.governanceArchiveFreeze?.exportPolicy?.exportName],
      ["Archived exports", plan.governanceArchiveFreeze?.summary?.archivedExports],
      ["Frozen guards", plan.governanceArchiveFreeze?.summary?.frozenGuards],
      ["Passed archive checks", plan.governanceArchiveFreeze?.summary?.passedArchiveChecks],
      ["Archive persisted mutations", plan.governanceArchiveFreeze?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.governanceArchiveFreeze?.archiveManifest || []).map((item) => ({
      id: item.id,
      label: item.exportName,
      status: item.freezeState,
      detail: item.sourceSchemaVersion
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceArchiveFreeze?.frozenGuards || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.evidence} / frozen: ${item.frozen ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.governanceArchiveFreeze?.archiveChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} archive check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Resolution map schema", plan.blockerResolutionMap?.schemaVersion],
      ["Resolution map mode", plan.blockerResolutionMap?.mode],
      ["Resolution map export", plan.blockerResolutionMap?.exportPolicy?.exportName],
      ["Resolution items", plan.blockerResolutionMap?.summary?.resolutionItems],
      ["Resolution lanes", plan.blockerResolutionMap?.summary?.resolutionLanes],
      ["Passed resolution checks", plan.blockerResolutionMap?.summary?.passedResolutionChecks],
      ["Resolution map persisted mutations", plan.blockerResolutionMap?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.blockerResolutionMap?.resolutionItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.blockerState,
      detail: `${item.ownerRole} / evidence: ${formatWorkflowList(item.requiredEvidence || [])} / before phase30: ${formatWorkflowList(item.requiredBeforePhase30 || [])} / mayClear: ${item.mayClearBlocker ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.blockerResolutionMap?.resolutionLanes || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.state,
      detail: `items: ${item.itemCount} / persistClearance: ${item.mayPersistClearance ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.blockerResolutionMap?.resolutionChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} resolution check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 preflight schema", plan.phase30EntryPreflight?.schemaVersion],
      ["Phase 30 preflight mode", plan.phase30EntryPreflight?.mode],
      ["Phase 30 preflight export", plan.phase30EntryPreflight?.exportPolicy?.exportName],
      ["Preflight items", plan.phase30EntryPreflight?.summary?.preflightItems],
      ["Blocked preflight items", plan.phase30EntryPreflight?.summary?.blockedPreflightItems],
      ["Entry gates", plan.phase30EntryPreflight?.summary?.entryGates],
      ["Blocked entry gates", plan.phase30EntryPreflight?.summary?.blockedEntryGates],
      ["Phase 30 entry ready", plan.phase30EntryPreflight?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Preflight persisted mutations", plan.phase30EntryPreflight?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryPreflight?.preflightItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.result,
      detail: `${item.sourceSchemaVersion} / blocksPhase30: ${item.blocksPhase30 ? "yes" : "no"} / required: ${formatWorkflowList(item.requiredBeforePhase30 || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryPreflight?.entryGates || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.evidence} / mayEnterPhase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryPreflight?.preflightChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} preflight check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Final blocker dossier schema", plan.finalReleaseBlockerDossier?.schemaVersion],
      ["Final blocker dossier mode", plan.finalReleaseBlockerDossier?.mode],
      ["Final blocker dossier export", plan.finalReleaseBlockerDossier?.exportPolicy?.exportName],
      ["Unresolved blockers", plan.finalReleaseBlockerDossier?.summary?.unresolvedBlockers],
      ["Release gate findings", plan.finalReleaseBlockerDossier?.summary?.releaseGateFindings],
      ["Blocked release gates", plan.finalReleaseBlockerDossier?.summary?.blockedReleaseGates],
      ["Dossier artifacts", plan.finalReleaseBlockerDossier?.summary?.dossierArtifacts],
      ["Dossier persisted mutations", plan.finalReleaseBlockerDossier?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.finalReleaseBlockerDossier?.unresolvedBlockers || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.dossierState,
      detail: `${item.ownerRole} / evidence: ${formatWorkflowList(item.requiredEvidence || [])} / before release: ${formatWorkflowList(item.requiredBeforeRelease || [])} / mayClear: ${item.mayClearBlocker ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalReleaseBlockerDossier?.releaseGateFindings || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / mayApproveRelease: ${item.mayApproveRelease ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalReleaseBlockerDossier?.dossierArtifacts || []).map((item) => ({
      id: item.id,
      label: item.exportName || item.id,
      status: item.artifactState,
      detail: item.sourceSchemaVersion
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalReleaseBlockerDossier?.dossierChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} dossier check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Release exit ledger schema", plan.releaseExitCriteriaLedger?.schemaVersion],
      ["Release exit ledger mode", plan.releaseExitCriteriaLedger?.mode],
      ["Release exit ledger export", plan.releaseExitCriteriaLedger?.exportPolicy?.exportName],
      ["Exit criteria", plan.releaseExitCriteriaLedger?.summary?.exitCriteria],
      ["Unmet exit criteria", plan.releaseExitCriteriaLedger?.summary?.unmetExitCriteria],
      ["Ledger entries", plan.releaseExitCriteriaLedger?.summary?.ledgerEntries],
      ["Blocked exit gates", plan.releaseExitCriteriaLedger?.summary?.blockedExitGates],
      ["Phase 29 exit ready", plan.releaseExitCriteriaLedger?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Exit ledger persisted mutations", plan.releaseExitCriteriaLedger?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.releaseExitCriteriaLedger?.exitCriteria || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeExit || [])} / mayExitPhase29: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseExitCriteriaLedger?.ledgerEntries || []).map((item) => ({
      id: item.id,
      label: item.criteriaId,
      status: item.ledgerState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / maySatisfyCriteria: ${item.maySatisfyCriteria ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseExitCriteriaLedger?.exitChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} exit check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Release exit decision schema", plan.releaseExitDecisionPreview?.schemaVersion],
      ["Release exit decision mode", plan.releaseExitDecisionPreview?.mode],
      ["Release exit decision export", plan.releaseExitDecisionPreview?.exportPolicy?.exportName],
      ["Decision preview items", plan.releaseExitDecisionPreview?.summary?.decisionPreviewItems],
      ["Blocked preview items", plan.releaseExitDecisionPreview?.summary?.blockedPreviewItems],
      ["Decision", plan.releaseExitDecisionPreview?.summary?.phase29ExitDecision],
      ["Decision checks", plan.releaseExitDecisionPreview?.summary?.decisionChecks],
      ["Phase 29 exit ready", plan.releaseExitDecisionPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Decision preview persisted mutations", plan.releaseExitDecisionPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.releaseExitDecisionPreview?.decisionPreviewItems || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.previewDecision,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / mayApproveRelease: ${item.mayApproveRelease ? "yes" : "no"} / mayExitPhase29: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseExitDecisionPreview?.decisionChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} decision check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 transition risk schema", plan.phase30TransitionRiskRegister?.schemaVersion],
      ["Phase 30 transition risk mode", plan.phase30TransitionRiskRegister?.mode],
      ["Phase 30 transition risk export", plan.phase30TransitionRiskRegister?.exportPolicy?.exportName],
      ["Transition risks", plan.phase30TransitionRiskRegister?.summary?.riskItems],
      ["Blocked transition risks", plan.phase30TransitionRiskRegister?.summary?.blockedTransitionRisks],
      ["Critical transition risks", plan.phase30TransitionRiskRegister?.summary?.criticalRisks],
      ["Mitigation lanes", plan.phase30TransitionRiskRegister?.summary?.mitigationLanes],
      ["Risk checks", plan.phase30TransitionRiskRegister?.summary?.riskChecks],
      ["Phase 30 entry ready", plan.phase30TransitionRiskRegister?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Risk register persisted mutations", plan.phase30TransitionRiskRegister?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30TransitionRiskRegister?.riskItems || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.transitionState,
      detail: `${item.sourceSchemaVersion} / ${item.severity} / ${item.evidence} / mayEnterPhase30: ${item.mayEnterPhase30 ? "yes" : "no"} / mitigation: ${formatWorkflowList(item.requiredMitigation || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30TransitionRiskRegister?.mitigationLanes || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.owner} / risks: ${formatWorkflowList(item.linkedRisks || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30TransitionRiskRegister?.riskChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} risk check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Transition evidence gap schema", plan.transitionEvidenceGapReview?.schemaVersion],
      ["Transition evidence gap mode", plan.transitionEvidenceGapReview?.mode],
      ["Transition evidence gap export", plan.transitionEvidenceGapReview?.exportPolicy?.exportName],
      ["Evidence gaps", plan.transitionEvidenceGapReview?.summary?.gapItems],
      ["Blocking evidence gaps", plan.transitionEvidenceGapReview?.summary?.blockingEvidenceGaps],
      ["Evidence review lanes", plan.transitionEvidenceGapReview?.summary?.evidenceReviewLanes],
      ["Gap checks", plan.transitionEvidenceGapReview?.summary?.gapChecks],
      ["Release ready", plan.transitionEvidenceGapReview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.transitionEvidenceGapReview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Evidence gap persisted mutations", plan.transitionEvidenceGapReview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.transitionEvidenceGapReview?.gapItems || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.gapState,
      detail: `${item.sourceSchemaVersion} / required: ${formatWorkflowList(item.requiredEvidence || [])} / missing: ${formatWorkflowList(item.missingEvidence || [])} / clear: ${item.mayClearBlocker ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.transitionEvidenceGapReview?.evidenceReviewLanes || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.owner} / gaps: ${formatWorkflowList(item.linkedGaps || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.transitionEvidenceGapReview?.gapChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} gap check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Transition redline schema", plan.transitionReadinessRedline?.schemaVersion],
      ["Transition redline mode", plan.transitionReadinessRedline?.mode],
      ["Transition redline export", plan.transitionReadinessRedline?.exportPolicy?.exportName],
      ["Redline items", plan.transitionReadinessRedline?.summary?.redlineItems],
      ["Active redlines", plan.transitionReadinessRedline?.summary?.activeRedlines],
      ["Redline review items", plan.transitionReadinessRedline?.summary?.redlineReviewItems],
      ["Redline checks", plan.transitionReadinessRedline?.summary?.redlineChecks],
      ["Phase 29 exit ready", plan.transitionReadinessRedline?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.transitionReadinessRedline?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.transitionReadinessRedline?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Redline persisted mutations", plan.transitionReadinessRedline?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.transitionReadinessRedline?.redlineItems || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.redlineState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / blocked until: ${formatWorkflowList(item.blockedUntil || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.transitionReadinessRedline?.redlineReviewMatrix || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.state,
      detail: `${item.owner} / redlines: ${formatWorkflowList(item.linkedRedlines || [])} / blocked: ${formatWorkflowList(item.blockedActions || [])}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.transitionReadinessRedline?.redlineChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} redline check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Final handoff preview schema", plan.finalTransitionHandoffPreview?.schemaVersion],
      ["Final handoff preview mode", plan.finalTransitionHandoffPreview?.mode],
      ["Final handoff preview export", plan.finalTransitionHandoffPreview?.exportPolicy?.exportName],
      ["Handoff preview sections", plan.finalTransitionHandoffPreview?.summary?.handoffPreviewSections],
      ["Blocked handoff preview sections", plan.finalTransitionHandoffPreview?.summary?.blockedHandoffPreviewSections],
      ["Handoff preview checks", plan.finalTransitionHandoffPreview?.summary?.handoffPreviewChecks],
      ["Phase 29 exit ready", plan.finalTransitionHandoffPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.finalTransitionHandoffPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.finalTransitionHandoffPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Handoff preview persisted mutations", plan.finalTransitionHandoffPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.finalTransitionHandoffPreview?.handoffPreviewSections || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.sectionState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeHandoff || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalTransitionHandoffPreview?.handoffPreviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} handoff preview check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Transition closure preview schema", plan.transitionClosurePackagePreview?.schemaVersion],
      ["Transition closure preview mode", plan.transitionClosurePackagePreview?.mode],
      ["Transition closure preview export", plan.transitionClosurePackagePreview?.exportPolicy?.exportName],
      ["Closure preview items", plan.transitionClosurePackagePreview?.summary?.closurePreviewItems],
      ["Blocked closure preview items", plan.transitionClosurePackagePreview?.summary?.blockedClosurePreviewItems],
      ["Closure preview checks", plan.transitionClosurePackagePreview?.summary?.closurePreviewChecks],
      ["Phase 29 exit ready", plan.transitionClosurePackagePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.transitionClosurePackagePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.transitionClosurePackagePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Closure preview persisted mutations", plan.transitionClosurePackagePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.transitionClosurePackagePreview?.closurePreviewItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.previewState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeClosure || [])} / clear: ${item.mayClearBlocker ? "yes" : "no"} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.transitionClosurePackagePreview?.closurePreviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} closure preview check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Release candidate freeze schema", plan.releaseCandidateFreezePreview?.schemaVersion],
      ["Release candidate freeze mode", plan.releaseCandidateFreezePreview?.mode],
      ["Release candidate freeze export", plan.releaseCandidateFreezePreview?.exportPolicy?.exportName],
      ["Freeze preview items", plan.releaseCandidateFreezePreview?.summary?.freezePreviewItems],
      ["Blocked freeze preview items", plan.releaseCandidateFreezePreview?.summary?.blockedFreezePreviewItems],
      ["Freeze preview checks", plan.releaseCandidateFreezePreview?.summary?.freezePreviewChecks],
      ["Phase 29 exit ready", plan.releaseCandidateFreezePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.releaseCandidateFreezePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.releaseCandidateFreezePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Freeze preview persisted mutations", plan.releaseCandidateFreezePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.releaseCandidateFreezePreview?.freezePreviewItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.freezeState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeReleaseCandidate || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseCandidateFreezePreview?.freezePreviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} freeze preview check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Final readiness board schema", plan.finalReadinessBoardPreview?.schemaVersion],
      ["Final readiness board mode", plan.finalReadinessBoardPreview?.mode],
      ["Final readiness board export", plan.finalReadinessBoardPreview?.exportPolicy?.exportName],
      ["Readiness board items", plan.finalReadinessBoardPreview?.summary?.readinessBoardItems],
      ["Blocked readiness board items", plan.finalReadinessBoardPreview?.summary?.blockedReadinessBoardItems],
      ["Readiness board checks", plan.finalReadinessBoardPreview?.summary?.readinessBoardChecks],
      ["Phase 29 exit ready", plan.finalReadinessBoardPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.finalReadinessBoardPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.finalReadinessBoardPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Readiness board persisted mutations", plan.finalReadinessBoardPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.finalReadinessBoardPreview?.readinessBoardItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.boardState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeFinalReadiness || [])} / clear: ${item.mayClearBlocker ? "yes" : "no"} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.finalReadinessBoardPreview?.readinessBoardChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} readiness board check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Release final review envelope schema", plan.releaseFinalReviewEnvelopePreview?.schemaVersion],
      ["Release final review envelope mode", plan.releaseFinalReviewEnvelopePreview?.mode],
      ["Release final review envelope export", plan.releaseFinalReviewEnvelopePreview?.exportPolicy?.exportName],
      ["Final review envelope sections", plan.releaseFinalReviewEnvelopePreview?.summary?.envelopeSections],
      ["Blocked final review envelope sections", plan.releaseFinalReviewEnvelopePreview?.summary?.blockedEnvelopeSections],
      ["Final review envelope checks", plan.releaseFinalReviewEnvelopePreview?.summary?.envelopeChecks],
      ["Phase 29 exit ready", plan.releaseFinalReviewEnvelopePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.releaseFinalReviewEnvelopePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.releaseFinalReviewEnvelopePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Final review envelope persisted mutations", plan.releaseFinalReviewEnvelopePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.releaseFinalReviewEnvelopePreview?.envelopeSections || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.envelopeState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforeFinalReview || [])} / clear: ${item.mayClearBlocker ? "yes" : "no"} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.releaseFinalReviewEnvelopePreview?.envelopeChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} final review envelope check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Exit readiness attestation schema", plan.exitReadinessAttestationPreview?.schemaVersion],
      ["Exit readiness attestation mode", plan.exitReadinessAttestationPreview?.mode],
      ["Exit readiness attestation export", plan.exitReadinessAttestationPreview?.exportPolicy?.exportName],
      ["Exit readiness attestation items", plan.exitReadinessAttestationPreview?.summary?.attestationItems],
      ["Blocked exit readiness attestation items", plan.exitReadinessAttestationPreview?.summary?.blockedAttestationItems],
      ["Exit readiness attestation checks", plan.exitReadinessAttestationPreview?.summary?.attestationChecks],
      ["Phase 29 exit ready", plan.exitReadinessAttestationPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.exitReadinessAttestationPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.exitReadinessAttestationPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Attestation persisted mutations", plan.exitReadinessAttestationPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.exitReadinessAttestationPreview?.attestationItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.attestationState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / missing: ${formatWorkflowList(item.missingBeforeExit || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.exitReadinessAttestationPreview?.attestationChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} exit readiness attestation check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 planning brief schema", plan.phase30PlanningBriefPreview?.schemaVersion],
      ["Phase 30 planning brief mode", plan.phase30PlanningBriefPreview?.mode],
      ["Phase 30 planning brief export", plan.phase30PlanningBriefPreview?.exportPolicy?.exportName],
      ["Phase 30 planning brief sections", plan.phase30PlanningBriefPreview?.summary?.briefSections],
      ["Blocked Phase 30 planning brief sections", plan.phase30PlanningBriefPreview?.summary?.blockedBriefSections],
      ["Phase 30 planning brief checks", plan.phase30PlanningBriefPreview?.summary?.briefChecks],
      ["Phase 29 exit ready", plan.phase30PlanningBriefPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30PlanningBriefPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30PlanningBriefPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Phase 30 brief persisted mutations", plan.phase30PlanningBriefPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30PlanningBriefPreview?.briefSections || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.briefState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / planningOnly: ${item.phase30PlanningOnly ? "yes" : "no"} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30PlanningBriefPreview?.briefChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 planning brief check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 scope lock schema", plan.phase30ScopeLockPreview?.schemaVersion],
      ["Phase 30 scope lock mode", plan.phase30ScopeLockPreview?.mode],
      ["Phase 30 scope lock export", plan.phase30ScopeLockPreview?.exportPolicy?.exportName],
      ["Phase 30 scope lock items", plan.phase30ScopeLockPreview?.summary?.scopeLockItems],
      ["Blocked Phase 30 scope lock items", plan.phase30ScopeLockPreview?.summary?.blockedScopeLockItems],
      ["Phase 30 scope lock checks", plan.phase30ScopeLockPreview?.summary?.scopeLockChecks],
      ["Phase 29 exit ready", plan.phase30ScopeLockPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30ScopeLockPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30ScopeLockPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Phase 30 scope lock persisted mutations", plan.phase30ScopeLockPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30ScopeLockPreview?.scopeLockItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.lockState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / locked: ${formatWorkflowList(item.lockedScope || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30ScopeLockPreview?.scopeLockChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 scope lock check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 readiness map schema", plan.phase30ReadinessMapPreview?.schemaVersion],
      ["Phase 30 readiness map mode", plan.phase30ReadinessMapPreview?.mode],
      ["Phase 30 readiness map export", plan.phase30ReadinessMapPreview?.exportPolicy?.exportName],
      ["Phase 30 readiness map items", plan.phase30ReadinessMapPreview?.summary?.readinessMapItems],
      ["Blocked Phase 30 readiness map items", plan.phase30ReadinessMapPreview?.summary?.blockedReadinessMapItems],
      ["Phase 30 readiness map checks", plan.phase30ReadinessMapPreview?.summary?.readinessMapChecks],
      ["Phase 29 exit ready", plan.phase30ReadinessMapPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30ReadinessMapPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30ReadinessMapPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30ReadinessMapPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30ReadinessMapPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 readiness map persisted mutations", plan.phase30ReadinessMapPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30ReadinessMapPreview?.readinessMapItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.readinessState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / required: ${formatWorkflowList(item.requiredBeforePhase30 || [])} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30ReadinessMapPreview?.readinessMapChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 readiness map check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 readiness gap brief schema", plan.phase30ReadinessGapBriefPreview?.schemaVersion],
      ["Phase 30 readiness gap brief mode", plan.phase30ReadinessGapBriefPreview?.mode],
      ["Phase 30 readiness gap brief export", plan.phase30ReadinessGapBriefPreview?.exportPolicy?.exportName],
      ["Phase 30 readiness gap brief items", plan.phase30ReadinessGapBriefPreview?.summary?.gapBriefItems],
      ["Blocked Phase 30 readiness gap brief items", plan.phase30ReadinessGapBriefPreview?.summary?.blockedGapBriefItems],
      ["Phase 30 readiness gap brief checks", plan.phase30ReadinessGapBriefPreview?.summary?.gapBriefChecks],
      ["Phase 29 exit ready", plan.phase30ReadinessGapBriefPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30ReadinessGapBriefPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30ReadinessGapBriefPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30ReadinessGapBriefPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30ReadinessGapBriefPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 readiness gap brief persisted mutations", plan.phase30ReadinessGapBriefPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30ReadinessGapBriefPreview?.gapBriefItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.gapState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / missing: ${formatWorkflowList(item.missingBeforePhase30 || [])} / owner: ${item.actionOwner} / exit: ${item.mayExitPhase29 ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30ReadinessGapBriefPreview?.gapBriefChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 readiness gap brief check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 gap resolution sequence schema", plan.phase30GapResolutionSequencePreview?.schemaVersion],
      ["Phase 30 gap resolution sequence mode", plan.phase30GapResolutionSequencePreview?.mode],
      ["Phase 30 gap resolution sequence export", plan.phase30GapResolutionSequencePreview?.exportPolicy?.exportName],
      ["Phase 30 gap resolution sequence items", plan.phase30GapResolutionSequencePreview?.summary?.resolutionSequenceItems],
      ["Blocked Phase 30 gap resolution sequence items", plan.phase30GapResolutionSequencePreview?.summary?.blockedResolutionSequenceItems],
      ["Phase 30 gap resolution sequence checks", plan.phase30GapResolutionSequencePreview?.summary?.sequenceChecks],
      ["Phase 29 exit ready", plan.phase30GapResolutionSequencePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30GapResolutionSequencePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30GapResolutionSequencePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30GapResolutionSequencePreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30GapResolutionSequencePreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 gap resolution sequence persisted mutations", plan.phase30GapResolutionSequencePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30GapResolutionSequencePreview?.resolutionSequenceItems || []).map((item) => ({
      id: item.id,
      label: `${item.sequenceOrder}. ${item.label}`,
      status: item.resolutionState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / prereq: ${formatWorkflowList(item.prerequisiteGapIds || [])} / required: ${formatWorkflowList(item.requiredEvidence || [])} / owner: ${item.owner} / blocked: ${formatWorkflowList(item.blockedBy || [])} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30GapResolutionSequencePreview?.sequenceChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 gap resolution sequence check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence pack schema", plan.phase30EntryEvidencePackPreview?.schemaVersion],
      ["Phase 30 entry evidence pack mode", plan.phase30EntryEvidencePackPreview?.mode],
      ["Phase 30 entry evidence pack export", plan.phase30EntryEvidencePackPreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence pack items", plan.phase30EntryEvidencePackPreview?.summary?.evidencePackItems],
      ["Blocked Phase 30 entry evidence pack items", plan.phase30EntryEvidencePackPreview?.summary?.blockedEvidencePackItems],
      ["Phase 30 entry evidence pack checks", plan.phase30EntryEvidencePackPreview?.summary?.evidencePackChecks],
      ["Phase 29 exit ready", plan.phase30EntryEvidencePackPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidencePackPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidencePackPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidencePackPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidencePackPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence pack persisted mutations", plan.phase30EntryEvidencePackPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidencePackPreview?.evidencePackItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.packState,
      detail: `${item.sourceSchemaVersion} / ${item.evidence} / sequence: ${formatWorkflowList(item.sourceSequenceIds || [])} / required: ${formatWorkflowList(item.requiredEvidence || [])} / owner: ${item.owner} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidencePackPreview?.evidencePackChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence pack check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence index schema", plan.phase30EntryEvidenceIndexPreview?.schemaVersion],
      ["Phase 30 entry evidence index mode", plan.phase30EntryEvidenceIndexPreview?.mode],
      ["Phase 30 entry evidence index export", plan.phase30EntryEvidenceIndexPreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence index items", plan.phase30EntryEvidenceIndexPreview?.summary?.evidenceIndexItems],
      ["Blocked Phase 30 entry evidence index items", plan.phase30EntryEvidenceIndexPreview?.summary?.blockedEvidenceIndexItems],
      ["Phase 30 entry evidence index checks", plan.phase30EntryEvidenceIndexPreview?.summary?.evidenceIndexChecks],
      ["Phase 29 exit ready", plan.phase30EntryEvidenceIndexPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidenceIndexPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidenceIndexPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidenceIndexPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidenceIndexPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence index persisted mutations", plan.phase30EntryEvidenceIndexPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidenceIndexPreview?.evidenceIndexItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.indexState,
      detail: `${item.evidenceType} / ${item.sourceSchemaVersion} / pack: ${formatWorkflowList(item.sourcePackIds || [])} / sequence: ${formatWorkflowList(item.sourceSequenceIds || [])} / blocker: ${item.blockerCategory} / owner: ${item.owner} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidenceIndexPreview?.evidenceIndexChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence index check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence review queue schema", plan.phase30EntryEvidenceReviewQueuePreview?.schemaVersion],
      ["Phase 30 entry evidence review queue mode", plan.phase30EntryEvidenceReviewQueuePreview?.mode],
      ["Phase 30 entry evidence review queue export", plan.phase30EntryEvidenceReviewQueuePreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence review queue items", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.evidenceReviewQueueItems],
      ["Blocked Phase 30 entry evidence review queue items", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.blockedEvidenceReviewQueueItems],
      ["Phase 30 entry evidence review queue checks", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.evidenceReviewQueueChecks],
      ["High priority Phase 30 entry evidence review queue items", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.highPriorityReviewItems],
      ["Phase 29 exit ready", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence review queue persisted mutations", plan.phase30EntryEvidenceReviewQueuePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidenceReviewQueuePreview?.evidenceReviewQueueItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.queueState,
      detail: `${item.evidenceType} / index: ${item.sourceIndexItemId} / lane: ${item.queueLane} / priority: ${item.priority} / owner: ${item.owner} / actions: ${formatWorkflowList(item.requiredReviewActions || [])} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidenceReviewQueuePreview?.evidenceReviewQueueChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence review queue check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence review disposition schema", plan.phase30EntryEvidenceReviewDispositionPreview?.schemaVersion],
      ["Phase 30 entry evidence review disposition mode", plan.phase30EntryEvidenceReviewDispositionPreview?.mode],
      ["Phase 30 entry evidence review disposition export", plan.phase30EntryEvidenceReviewDispositionPreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence review disposition items", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.dispositionPreviewItems],
      ["Blocked Phase 30 entry evidence review disposition items", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.blockedDispositionPreviewItems],
      ["Phase 30 entry evidence review disposition checks", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.dispositionPreviewChecks],
      ["Hold-for-evidence disposition items", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.holdForEvidenceItems],
      ["Phase 29 exit ready", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence review disposition persisted mutations", plan.phase30EntryEvidenceReviewDispositionPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidenceReviewDispositionPreview?.dispositionPreviewItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.dispositionPreviewState,
      detail: `${item.evidenceType} / queue: ${item.sourceQueueItemId} / index: ${item.sourceIndexItemId} / disposition: ${item.candidateDisposition} / owner: ${item.owner} / required: ${formatWorkflowList(item.requiredEvidence || [])} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidenceReviewDispositionPreview?.dispositionPreviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence review disposition check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence disposition ledger schema", plan.phase30EntryEvidenceDispositionLedgerPreview?.schemaVersion],
      ["Phase 30 entry evidence disposition ledger mode", plan.phase30EntryEvidenceDispositionLedgerPreview?.mode],
      ["Phase 30 entry evidence disposition ledger export", plan.phase30EntryEvidenceDispositionLedgerPreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence disposition ledger entries", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.dispositionLedgerEntries],
      ["Blocked Phase 30 entry evidence disposition ledger entries", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.blockedDispositionLedgerEntries],
      ["Phase 30 entry evidence disposition ledger checks", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.dispositionLedgerChecks],
      ["Hold-for-evidence ledger entries", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.holdForEvidenceLedgerEntries],
      ["Phase 29 exit ready", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence disposition ledger persisted mutations", plan.phase30EntryEvidenceDispositionLedgerPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidenceDispositionLedgerPreview?.dispositionLedgerEntries || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.ledgerState,
      detail: `${item.evidenceType} / disposition: ${item.sourceDispositionItemId} / queue: ${item.sourceQueueItemId} / candidate: ${item.candidateDisposition} / owner: ${item.owner} / audit persisted: ${item.auditPreview?.persisted ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidenceDispositionLedgerPreview?.dispositionLedgerChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence disposition ledger check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry evidence final ledger export schema", plan.phase30EntryEvidenceFinalLedgerExportPreview?.schemaVersion],
      ["Phase 30 entry evidence final ledger export mode", plan.phase30EntryEvidenceFinalLedgerExportPreview?.mode],
      ["Phase 30 entry evidence final ledger export name", plan.phase30EntryEvidenceFinalLedgerExportPreview?.exportPolicy?.exportName],
      ["Phase 30 entry evidence final ledger export items", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.finalLedgerExportItems],
      ["Blocked Phase 30 entry evidence final ledger export items", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.blockedFinalLedgerExportItems],
      ["Phase 30 entry evidence final ledger export checks", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.finalLedgerExportChecks],
      ["Included final ledger export items", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.includedFinalLedgerExportItems],
      ["Persisted final ledger export items", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.persistedFinalLedgerExportItems],
      ["Phase 29 exit ready", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry evidence final ledger export persisted mutations", plan.phase30EntryEvidenceFinalLedgerExportPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryEvidenceFinalLedgerExportPreview?.finalLedgerExportItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.exportState,
      detail: `${item.evidenceType} / ledger: ${item.sourceLedgerEntryId} / disposition: ${item.sourceDispositionItemId} / candidate: ${item.candidateDisposition} / owner: ${item.owner} / included: ${item.includedInPreview ? "yes" : "no"} / persisted: ${item.persisted ? "yes" : "no"} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryEvidenceFinalLedgerExportPreview?.finalLedgerExportChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry evidence final ledger export check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry final export reconciliation schema", plan.phase30EntryFinalExportReconciliationPreview?.schemaVersion],
      ["Phase 30 entry final export reconciliation mode", plan.phase30EntryFinalExportReconciliationPreview?.mode],
      ["Phase 30 entry final export reconciliation export", plan.phase30EntryFinalExportReconciliationPreview?.exportPolicy?.exportName],
      ["Phase 30 entry final export reconciliation items", plan.phase30EntryFinalExportReconciliationPreview?.summary?.reconciliationItems],
      ["Blocked Phase 30 entry final export reconciliation items", plan.phase30EntryFinalExportReconciliationPreview?.summary?.blockedReconciliationItems],
      ["Covered ledger entries", plan.phase30EntryFinalExportReconciliationPreview?.summary?.coveredLedgerEntries],
      ["Uncovered ledger entries", plan.phase30EntryFinalExportReconciliationPreview?.summary?.uncoveredLedgerEntries],
      ["Phase 30 entry final export reconciliation checks", plan.phase30EntryFinalExportReconciliationPreview?.summary?.reconciliationChecks],
      ["Persisted final export items", plan.phase30EntryFinalExportReconciliationPreview?.summary?.persistedFinalExportItems],
      ["Phase 29 exit ready", plan.phase30EntryFinalExportReconciliationPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryFinalExportReconciliationPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryFinalExportReconciliationPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryFinalExportReconciliationPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryFinalExportReconciliationPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry final export reconciliation persisted mutations", plan.phase30EntryFinalExportReconciliationPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryFinalExportReconciliationPreview?.reconciliationItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.reconciliationState,
      detail: `${item.evidenceType} / final export: ${item.sourceFinalExportItemId} / ledger: ${item.sourceLedgerEntryId} / coverage: ${item.coverageState} / persisted: ${item.persistedState} / blocker: ${item.blockerState} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryFinalExportReconciliationPreview?.reconciliationChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry final export reconciliation check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry reconciliation signoff schema", plan.phase30EntryReconciliationSignoffPreview?.schemaVersion],
      ["Phase 30 entry reconciliation signoff mode", plan.phase30EntryReconciliationSignoffPreview?.mode],
      ["Phase 30 entry reconciliation signoff export", plan.phase30EntryReconciliationSignoffPreview?.exportPolicy?.exportName],
      ["Phase 30 entry reconciliation signoff items", plan.phase30EntryReconciliationSignoffPreview?.summary?.signoffItems],
      ["Blocked Phase 30 entry reconciliation signoff items", plan.phase30EntryReconciliationSignoffPreview?.summary?.blockedSignoffItems],
      ["Phase 30 entry reconciliation signoff checks", plan.phase30EntryReconciliationSignoffPreview?.summary?.signoffChecks],
      ["Required signoff roles", plan.phase30EntryReconciliationSignoffPreview?.summary?.requiredSignoffRoles],
      ["Signed off items", plan.phase30EntryReconciliationSignoffPreview?.summary?.signedOffItems],
      ["Persisted signoffs", plan.phase30EntryReconciliationSignoffPreview?.summary?.persistedSignoffs],
      ["Phase 29 exit ready", plan.phase30EntryReconciliationSignoffPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryReconciliationSignoffPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryReconciliationSignoffPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryReconciliationSignoffPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryReconciliationSignoffPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry reconciliation signoff persisted mutations", plan.phase30EntryReconciliationSignoffPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryReconciliationSignoffPreview?.signoffItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.signoffState,
      detail: `${item.evidenceType} / reconciliation: ${item.sourceReconciliationItemId} / final export: ${item.sourceFinalExportItemId} / coverage: ${item.evidenceCoverageState} / roles: ${formatWorkflowList(item.requiredSignoffRoles || [])} / persisted: ${item.signoffPersistenceState} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryReconciliationSignoffPreview?.signoffChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry reconciliation signoff check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry signoff audit schema", plan.phase30EntrySignoffAuditPreview?.schemaVersion],
      ["Phase 30 entry signoff audit mode", plan.phase30EntrySignoffAuditPreview?.mode],
      ["Phase 30 entry signoff audit export", plan.phase30EntrySignoffAuditPreview?.exportPolicy?.exportName],
      ["Phase 30 entry signoff audit items", plan.phase30EntrySignoffAuditPreview?.summary?.auditItems],
      ["Blocked Phase 30 entry signoff audit items", plan.phase30EntrySignoffAuditPreview?.summary?.blockedAuditItems],
      ["Phase 30 entry signoff audit checks", plan.phase30EntrySignoffAuditPreview?.summary?.auditChecks],
      ["Audited signoff items", plan.phase30EntrySignoffAuditPreview?.summary?.auditedSignoffItems],
      ["Persisted audit items", plan.phase30EntrySignoffAuditPreview?.summary?.persistedAuditItems],
      ["Persisted signoffs", plan.phase30EntrySignoffAuditPreview?.summary?.persistedSignoffs],
      ["Phase 29 exit ready", plan.phase30EntrySignoffAuditPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntrySignoffAuditPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntrySignoffAuditPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntrySignoffAuditPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntrySignoffAuditPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry signoff audit persisted mutations", plan.phase30EntrySignoffAuditPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntrySignoffAuditPreview?.auditItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.auditState,
      detail: `${item.evidenceType} / signoff: ${item.sourceSignoffItemId} / reconciliation: ${item.sourceReconciliationItemId} / coverage: ${item.evidenceCoverageState} / audit persisted: ${item.auditPersistenceState} / signoff persisted: ${item.signoffPersistenceState} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntrySignoffAuditPreview?.auditChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry signoff audit check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 30 entry audit closure schema", plan.phase30EntryAuditClosurePreview?.schemaVersion],
      ["Phase 30 entry audit closure mode", plan.phase30EntryAuditClosurePreview?.mode],
      ["Phase 30 entry audit closure export", plan.phase30EntryAuditClosurePreview?.exportPolicy?.exportName],
      ["Phase 30 entry audit closure items", plan.phase30EntryAuditClosurePreview?.summary?.closureItems],
      ["Blocked Phase 30 entry audit closure items", plan.phase30EntryAuditClosurePreview?.summary?.blockedClosureItems],
      ["Phase 30 entry audit closure checks", plan.phase30EntryAuditClosurePreview?.summary?.closureChecks],
      ["Closed audit items", plan.phase30EntryAuditClosurePreview?.summary?.closedAuditItems],
      ["Persisted closure items", plan.phase30EntryAuditClosurePreview?.summary?.persistedClosureItems],
      ["Persisted audit items", plan.phase30EntryAuditClosurePreview?.summary?.persistedAuditItems],
      ["Phase 29 exit ready", plan.phase30EntryAuditClosurePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase30EntryAuditClosurePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase30EntryAuditClosurePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase30EntryAuditClosurePreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase30EntryAuditClosurePreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 30 entry audit closure persisted mutations", plan.phase30EntryAuditClosurePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase30EntryAuditClosurePreview?.closureItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.closureState,
      detail: `${item.evidenceType} / audit: ${item.sourceAuditItemId} / signoff: ${item.sourceSignoffItemId} / closure persisted: ${item.closurePersistenceState} / audit persisted: ${item.auditPersistenceState} / blocker: ${item.releaseBlockerState} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase30EntryAuditClosurePreview?.closureChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 30 entry audit closure check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 audit closure hardening schema", plan.phase29AuditClosureReadinessHardening?.schemaVersion],
      ["Phase 29 audit closure hardening mode", plan.phase29AuditClosureReadinessHardening?.mode],
      ["Phase 29 audit closure hardening export", plan.phase29AuditClosureReadinessHardening?.exportPolicy?.exportName],
      ["Phase 29 audit closure hardening items", plan.phase29AuditClosureReadinessHardening?.summary?.hardeningItems],
      ["Blocked Phase 29 audit closure hardening items", plan.phase29AuditClosureReadinessHardening?.summary?.blockedHardeningItems],
      ["Phase 29 audit closure hardening checks", plan.phase29AuditClosureReadinessHardening?.summary?.hardeningChecks],
      ["Hardened closure items", plan.phase29AuditClosureReadinessHardening?.summary?.hardenedClosureItems],
      ["Persisted hardening items", plan.phase29AuditClosureReadinessHardening?.summary?.persistedHardeningItems],
      ["Active redlines", plan.phase29AuditClosureReadinessHardening?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29AuditClosureReadinessHardening?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29AuditClosureReadinessHardening?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29AuditClosureReadinessHardening?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29AuditClosureReadinessHardening?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29AuditClosureReadinessHardening?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29AuditClosureReadinessHardening?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 audit closure hardening persisted mutations", plan.phase29AuditClosureReadinessHardening?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29AuditClosureReadinessHardening?.hardeningItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.hardeningState,
      detail: `${item.evidenceType} / closure: ${item.sourceClosureItemId} / audit: ${item.sourceAuditItemId} / hardening persisted: ${item.hardeningPersistenceState} / closure persisted: ${item.closurePersistenceState} / blocker: ${item.releaseBlockerState} / phase30: ${item.mayEnterPhase30 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29AuditClosureReadinessHardening?.hardeningChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 audit closure hardening check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 final exit package schema", plan.phase29FinalReadonlyExitPackagePreview?.schemaVersion],
      ["Phase 29 final exit package mode", plan.phase29FinalReadonlyExitPackagePreview?.mode],
      ["Phase 29 final exit package export", plan.phase29FinalReadonlyExitPackagePreview?.exportPolicy?.exportName],
      ["Phase 29 final exit package items", plan.phase29FinalReadonlyExitPackagePreview?.summary?.exitPackageItems],
      ["Blocked Phase 29 final exit package items", plan.phase29FinalReadonlyExitPackagePreview?.summary?.blockedExitPackageItems],
      ["Phase 29 final exit package checks", plan.phase29FinalReadonlyExitPackagePreview?.summary?.exitPackageChecks],
      ["Packaged exit items", plan.phase29FinalReadonlyExitPackagePreview?.summary?.packagedExitItems],
      ["Persisted exit package items", plan.phase29FinalReadonlyExitPackagePreview?.summary?.persistedExitPackageItems],
      ["Active redlines", plan.phase29FinalReadonlyExitPackagePreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29FinalReadonlyExitPackagePreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29FinalReadonlyExitPackagePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29FinalReadonlyExitPackagePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29FinalReadonlyExitPackagePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29FinalReadonlyExitPackagePreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29FinalReadonlyExitPackagePreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 final exit package persisted mutations", plan.phase29FinalReadonlyExitPackagePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29FinalReadonlyExitPackagePreview?.exitPackageItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.packageState,
      detail: `${item.evidenceType} / hardening: ${item.sourceHardeningItemId} / closure: ${item.sourceClosureItemId} / package persisted: ${item.packagePersistenceState} / blocker: ${item.releaseBlockerState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29FinalReadonlyExitPackagePreview?.exitPackageChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 final exit package check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit dossier schema", plan.phase29ReleaseExitReadinessDossierPreview?.schemaVersion],
      ["Phase 29 release exit dossier mode", plan.phase29ReleaseExitReadinessDossierPreview?.mode],
      ["Phase 29 release exit dossier export", plan.phase29ReleaseExitReadinessDossierPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit dossier items", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.dossierItems],
      ["Blocked Phase 29 release exit dossier items", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.blockedDossierItems],
      ["Phase 29 release exit dossier checks", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.dossierChecks],
      ["Accepted dossier items", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.acceptedDossierItems],
      ["Persisted dossier items", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.persistedDossierItems],
      ["Active redlines", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit dossier persisted mutations", plan.phase29ReleaseExitReadinessDossierPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitReadinessDossierPreview?.dossierItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.dossierState,
      detail: `${item.evidenceType} / final package: ${item.sourceExitPackageItemId} / hardening: ${item.sourceHardeningItemId} / dossier persisted: ${item.dossierPersistenceState} / blocker: ${item.releaseBlockerState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitReadinessDossierPreview?.dossierChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit dossier check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit governance freeze schema", plan.phase29ReleaseExitGovernanceFreezePreview?.schemaVersion],
      ["Phase 29 release exit governance freeze mode", plan.phase29ReleaseExitGovernanceFreezePreview?.mode],
      ["Phase 29 release exit governance freeze export", plan.phase29ReleaseExitGovernanceFreezePreview?.exportPolicy?.exportName],
      ["Phase 29 release exit governance freeze items", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.freezeItems],
      ["Blocked Phase 29 release exit governance freeze items", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.blockedFreezeItems],
      ["Phase 29 release exit governance freeze checks", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.freezeChecks],
      ["Frozen governance items", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.frozenGovernanceItems],
      ["Persisted freeze items", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.persistedFreezeItems],
      ["Active redlines", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit governance freeze persisted mutations", plan.phase29ReleaseExitGovernanceFreezePreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitGovernanceFreezePreview?.freezeItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.freezeState,
      detail: `${item.evidenceType} / dossier: ${item.sourceDossierItemId} / final package: ${item.sourceExitPackageItemId} / freeze persisted: ${item.freezePersistenceState} / blocker: ${item.releaseBlockerState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitGovernanceFreezePreview?.freezeChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit governance freeze check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit final signoff review schema", plan.phase29ReleaseExitFinalSignoffReviewPreview?.schemaVersion],
      ["Phase 29 release exit final signoff review mode", plan.phase29ReleaseExitFinalSignoffReviewPreview?.mode],
      ["Phase 29 release exit final signoff review export", plan.phase29ReleaseExitFinalSignoffReviewPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit final signoff review items", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.signoffReviewItems],
      ["Blocked Phase 29 release exit final signoff review items", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.blockedSignoffReviewItems],
      ["Phase 29 release exit final signoff review checks", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.signoffReviewChecks],
      ["Approved signoff items", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.approvedSignoffItems],
      ["Persisted signoff review items", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.persistedSignoffReviewItems],
      ["Active redlines", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit final signoff review persisted mutations", plan.phase29ReleaseExitFinalSignoffReviewPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitFinalSignoffReviewPreview?.signoffReviewItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.signoffReviewState,
      detail: `${item.evidenceType} / freeze: ${item.sourceFreezeItemId} / dossier: ${item.sourceDossierItemId} / signoff review persisted: ${item.signoffReviewPersistenceState} / blocker: ${item.releaseBlockerState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitFinalSignoffReviewPreview?.signoffReviewChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit final signoff review check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit signoff evidence reconciliation schema", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.schemaVersion],
      ["Phase 29 release exit signoff evidence reconciliation mode", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.mode],
      ["Phase 29 release exit signoff evidence reconciliation export", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit signoff evidence reconciliation items", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.evidenceReconciliationItems],
      ["Blocked Phase 29 release exit signoff evidence reconciliation items", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.blockedEvidenceReconciliationItems],
      ["Phase 29 release exit signoff evidence reconciliation checks", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.evidenceReconciliationChecks],
      ["Reconciled evidence items", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.reconciledEvidenceItems],
      ["Persisted evidence reconciliation items", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.persistedEvidenceReconciliationItems],
      ["Persisted signoff review items", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.persistedSignoffReviewItems],
      ["Active redlines", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit signoff evidence reconciliation persisted mutations", plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.evidenceReconciliationItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.reconciliationState,
      detail: `${item.evidenceType} / signoff review: ${item.sourceSignoffReviewItemId} / freeze: ${item.sourceFreezeItemId} / evidence match: ${item.evidenceMatchState} / reconciliation persisted: ${item.reconciliationPersistenceState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.evidenceReconciliationChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit signoff evidence reconciliation check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit final archive index schema", plan.phase29ReleaseExitFinalArchiveIndexPreview?.schemaVersion],
      ["Phase 29 release exit final archive index mode", plan.phase29ReleaseExitFinalArchiveIndexPreview?.mode],
      ["Phase 29 release exit final archive index export", plan.phase29ReleaseExitFinalArchiveIndexPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit final archive index items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.archiveIndexItems],
      ["Blocked Phase 29 release exit final archive index items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.blockedArchiveIndexItems],
      ["Phase 29 release exit final archive index checks", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.archiveIndexChecks],
      ["Indexed archive items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.indexedArchiveItems],
      ["Persisted archive index items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.persistedArchiveIndexItems],
      ["Persisted evidence reconciliation items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.persistedEvidenceReconciliationItems],
      ["Persisted signoff review items", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.persistedSignoffReviewItems],
      ["Active redlines", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.activeRedlines],
      ["Unresolved blockers", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.unresolvedBlockers],
      ["Phase 29 exit ready", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit final archive index persisted mutations", plan.phase29ReleaseExitFinalArchiveIndexPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveIndexPreview?.archiveIndexItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.archiveIndexState,
      detail: `${item.evidenceType} / reconciliation: ${item.sourceEvidenceReconciliationItemId} / signoff review: ${item.sourceSignoffReviewItemId} / archive persisted: ${item.archivePersistenceState} / blocker: ${item.releaseBlockerState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveIndexPreview?.archiveIndexChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit final archive index check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit final archive integrity check schema", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.schemaVersion],
      ["Phase 29 release exit final archive integrity check mode", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.mode],
      ["Phase 29 release exit final archive integrity check export", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit final archive integrity check items", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.integrityCheckItems],
      ["Blocked Phase 29 release exit final archive integrity check items", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.blockedIntegrityCheckItems],
      ["Phase 29 release exit final archive integrity checks", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.integrityChecks],
      ["Verified archive items", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.verifiedArchiveItems],
      ["Missing archive references", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.missingArchiveReferences],
      ["Persisted integrity check items", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.persistedIntegrityCheckItems],
      ["Persisted archive index items", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.persistedArchiveIndexItems],
      ["Phase 29 exit ready", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit final archive integrity check persisted mutations", plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.integrityCheckItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.integrityCheckState,
      detail: `${item.evidenceType} / archive index: ${item.sourceArchiveIndexItemId} / coverage: ${item.referenceCoverageState} / integrity persisted: ${item.integrityPersistenceState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.integrityChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit final archive integrity check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Phase 29 release exit final archive manifest schema", plan.phase29ReleaseExitFinalArchiveManifestPreview?.schemaVersion],
      ["Phase 29 release exit final archive manifest mode", plan.phase29ReleaseExitFinalArchiveManifestPreview?.mode],
      ["Phase 29 release exit final archive manifest export", plan.phase29ReleaseExitFinalArchiveManifestPreview?.exportPolicy?.exportName],
      ["Phase 29 release exit final archive manifest items", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.manifestItems],
      ["Blocked Phase 29 release exit final archive manifest items", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.blockedManifestItems],
      ["Phase 29 release exit final archive manifest checks", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.manifestChecks],
      ["Manifested archive items", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.manifestedArchiveItems],
      ["Missing manifest references", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.missingManifestReferences],
      ["Persisted manifest items", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.persistedManifestItems],
      ["Persisted integrity check items", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.persistedIntegrityCheckItems],
      ["Phase 29 exit ready", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.phase29ExitReady ? "yes" : "no"],
      ["Release ready", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.releaseReady ? "yes" : "no"],
      ["Phase 30 entry ready", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.phase30EntryReady ? "yes" : "no"],
      ["Runtime execution", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.runtimeExecution ? "enabled" : "disabled"],
      ["Third-party execution", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.thirdPartyExecution ? "enabled" : "disabled"],
      ["Phase 29 release exit final archive manifest persisted mutations", plan.phase29ReleaseExitFinalArchiveManifestPreview?.summary?.persistedMutations]
    ])}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveManifestPreview?.manifestItems || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.manifestState,
      detail: `${item.evidenceType} / integrity check: ${item.sourceIntegrityCheckItemId} / coverage: ${item.manifestCoverageState} / manifest persisted: ${item.manifestPersistenceState} / phase29 exit: ${item.mayExitPhase29 ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.phase29ReleaseExitFinalArchiveManifestPreview?.manifestChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} Phase 29 release exit final archive manifest check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.gateSeparationModel || []).map((item) => ({
      id: item.id,
      label: item.label,
      status: item.mayApproveRelease ? "mutating" : "read-only",
      detail: `clear: ${item.mayClearBlockers ? "yes" : "no"} / release: ${item.mayApproveRelease ? "yes" : "no"} / runtime: ${item.mayEnableRuntime ? "yes" : "no"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.entryChecks || []).map((item) => ({
      id: item.id,
      label: `${item.id} planning check`,
      status: item.result,
      detail: item.evidence
    })), { showStatus: true, compact: true })}
    ${renderOperationItems((plan.nextStageOptions || []).map((item) => ({
      id: item.id,
      label: item.id,
      status: item.status,
      detail: `releaseReady: ${item.releaseReady ? "true" : "false"} / runtimeExecution: ${item.runtimeExecution ? "true" : "false"}`
    })), { showStatus: true, compact: true })}
    ${renderOperationLines([
      ["Blocked mutations", formatWorkflowList(plan.blockedMutations || [])],
      ["Dashboard blocked mutations", formatWorkflowList(plan.governanceDashboardExport?.blockedMutations || [])],
      ["Decision audit blocked mutations", formatWorkflowList(plan.decisionHistoryAuditChain?.blockedMutations || [])],
      ["Closure blocked mutations", formatWorkflowList(plan.governanceClosureReadiness?.blockedMutations || [])],
      ["Handoff blocked mutations", formatWorkflowList(plan.closureHandoffPackage?.blockedMutations || [])],
      ["Review index blocked mutations", formatWorkflowList(plan.handoffReviewIndex?.blockedMutations || [])],
      ["Final snapshot blocked mutations", formatWorkflowList(plan.finalClosureReadinessSnapshot?.blockedMutations || [])],
      ["Archive freeze blocked mutations", formatWorkflowList(plan.governanceArchiveFreeze?.blockedMutations || [])],
      ["Resolution map blocked mutations", formatWorkflowList(plan.blockerResolutionMap?.blockedMutations || [])],
      ["Phase 30 preflight blocked mutations", formatWorkflowList(plan.phase30EntryPreflight?.blockedMutations || [])],
      ["Final dossier blocked mutations", formatWorkflowList(plan.finalReleaseBlockerDossier?.blockedMutations || [])],
      ["Release exit ledger blocked mutations", formatWorkflowList(plan.releaseExitCriteriaLedger?.blockedMutations || [])],
      ["Release exit decision blocked mutations", formatWorkflowList(plan.releaseExitDecisionPreview?.blockedMutations || [])],
      ["Phase 30 transition risk blocked mutations", formatWorkflowList(plan.phase30TransitionRiskRegister?.blockedMutations || [])],
      ["Transition evidence gap blocked mutations", formatWorkflowList(plan.transitionEvidenceGapReview?.blockedMutations || [])],
      ["Transition redline blocked mutations", formatWorkflowList(plan.transitionReadinessRedline?.blockedMutations || [])],
      ["Final transition handoff preview blocked mutations", formatWorkflowList(plan.finalTransitionHandoffPreview?.blockedMutations || [])],
      ["Transition closure package preview blocked mutations", formatWorkflowList(plan.transitionClosurePackagePreview?.blockedMutations || [])],
      ["Release candidate freeze preview blocked mutations", formatWorkflowList(plan.releaseCandidateFreezePreview?.blockedMutations || [])],
      ["Final readiness board preview blocked mutations", formatWorkflowList(plan.finalReadinessBoardPreview?.blockedMutations || [])],
      ["Release final review envelope preview blocked mutations", formatWorkflowList(plan.releaseFinalReviewEnvelopePreview?.blockedMutations || [])],
      ["Exit readiness attestation preview blocked mutations", formatWorkflowList(plan.exitReadinessAttestationPreview?.blockedMutations || [])],
      ["Phase 30 planning brief preview blocked mutations", formatWorkflowList(plan.phase30PlanningBriefPreview?.blockedMutations || [])],
      ["Phase 30 scope lock preview blocked mutations", formatWorkflowList(plan.phase30ScopeLockPreview?.blockedMutations || [])],
      ["Phase 30 readiness map preview blocked mutations", formatWorkflowList(plan.phase30ReadinessMapPreview?.blockedMutations || [])],
      ["Phase 30 readiness gap brief preview blocked mutations", formatWorkflowList(plan.phase30ReadinessGapBriefPreview?.blockedMutations || [])],
      ["Phase 30 gap resolution sequence preview blocked mutations", formatWorkflowList(plan.phase30GapResolutionSequencePreview?.blockedMutations || [])],
      ["Phase 30 entry evidence pack preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidencePackPreview?.blockedMutations || [])],
      ["Phase 30 entry evidence index preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidenceIndexPreview?.blockedMutations || [])],
      ["Phase 30 entry evidence review queue preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidenceReviewQueuePreview?.blockedMutations || [])],
      ["Phase 30 entry evidence review disposition preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidenceReviewDispositionPreview?.blockedMutations || [])],
      ["Phase 30 entry evidence disposition ledger preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidenceDispositionLedgerPreview?.blockedMutations || [])],
      ["Phase 30 entry evidence final ledger export preview blocked mutations", formatWorkflowList(plan.phase30EntryEvidenceFinalLedgerExportPreview?.blockedMutations || [])],
      ["Phase 30 entry final export reconciliation preview blocked mutations", formatWorkflowList(plan.phase30EntryFinalExportReconciliationPreview?.blockedMutations || [])],
      ["Phase 30 entry reconciliation signoff preview blocked mutations", formatWorkflowList(plan.phase30EntryReconciliationSignoffPreview?.blockedMutations || [])],
      ["Phase 30 entry signoff audit preview blocked mutations", formatWorkflowList(plan.phase30EntrySignoffAuditPreview?.blockedMutations || [])],
      ["Phase 30 entry audit closure preview blocked mutations", formatWorkflowList(plan.phase30EntryAuditClosurePreview?.blockedMutations || [])],
      ["Phase 29 audit closure hardening blocked mutations", formatWorkflowList(plan.phase29AuditClosureReadinessHardening?.blockedMutations || [])],
      ["Phase 29 final exit package blocked mutations", formatWorkflowList(plan.phase29FinalReadonlyExitPackagePreview?.blockedMutations || [])],
      ["Phase 29 release exit dossier blocked mutations", formatWorkflowList(plan.phase29ReleaseExitReadinessDossierPreview?.blockedMutations || [])],
      ["Phase 29 release exit governance freeze blocked mutations", formatWorkflowList(plan.phase29ReleaseExitGovernanceFreezePreview?.blockedMutations || [])],
      ["Phase 29 release exit final signoff review blocked mutations", formatWorkflowList(plan.phase29ReleaseExitFinalSignoffReviewPreview?.blockedMutations || [])],
      ["Phase 29 release exit signoff evidence reconciliation blocked mutations", formatWorkflowList(plan.phase29ReleaseExitSignoffEvidenceReconciliationPreview?.blockedMutations || [])],
      ["Phase 29 release exit final archive index blocked mutations", formatWorkflowList(plan.phase29ReleaseExitFinalArchiveIndexPreview?.blockedMutations || [])],
      ["Phase 29 release exit final archive integrity check blocked mutations", formatWorkflowList(plan.phase29ReleaseExitFinalArchiveIntegrityCheckPreview?.blockedMutations || [])],
      ["Phase 29 release exit final archive manifest blocked mutations", formatWorkflowList(plan.phase29ReleaseExitFinalArchiveManifestPreview?.blockedMutations || [])],
      ["Planning source", plan.sourcePhase28?.closurePackage],
      ["Release approval separated", "yes"],
      ["Runtime approval separated", "yes"]
    ])}
  `;
}

function renderOperationLines(lines = []) {
  return `
    <div class="operation-line-list">
      ${lines.filter(([, value]) => value !== undefined && value !== "").map(([label, value]) => `
        <span><b>${escapeHtml(label)}</b>${escapeHtml(String(value))}</span>
      `).join("")}
    </div>
  `;
}

function renderOperationItems(items = [], options = {}) {
  if (!items.length) return `<p>暂无记录。</p>`;
  return `
    <div class="operation-line-list ${options.compact ? "operation-line-list-compact" : ""}">
      ${items.map((item) => {
        const meta = [
          options.showStatus && item.status ? `状态：${item.status}` : "",
          options.showLevel && item.level ? `级别：${item.level}` : "",
          item.command ? `命令：${item.command}` : "",
          item.database ? `数据：${item.database}` : ""
        ].filter(Boolean).join(" / ");
        const detail = item.detail || item.note || item.mitigation || "";
        return `
          <span data-operation-status="${escapeHtml(item.status || item.level || "info")}">
            <b>${escapeHtml(item.label || item.id || "item")}</b>
            ${meta ? `<em>${escapeHtml(meta)}</em>` : ""}
            ${detail ? `<small>${escapeHtml(detail)}</small>` : ""}
          </span>
        `;
      }).join("")}
    </div>
  `;
}

function renderBackupPolicy(policy = {}) {
  return renderOperationLines([
    ["完整备份", policy.full],
    ["脱敏备份", policy.redacted],
    ["恢复", policy.restore],
    ["清空", policy.purge],
    ["节奏", policy.recommendedCadence],
    ["保存建议", policy.storageAdvice]
  ]);
}

function renderOperationEvents(events = []) {
  if (!events.length) return `<p>暂无运行事件。打开页面或调用接口后会逐步出现最近请求。</p>`;
  return `
    <div class="operation-line-list operation-event-list">
      ${events.slice(0, 8).map((event) => `
        <span data-operation-status="${escapeHtml(event.level || "info")}">
          <b>${escapeHtml(`${event.method || event.type || "event"} ${event.path || ""}`.trim())}</b>
          <em>${escapeHtml(`状态：${event.statusCode || "-"} / 耗时：${event.durationMs || 0}ms`)}</em>
          <small>${escapeHtml(event.id || "")}</small>
          <small>${escapeHtml(event.error || event.at || "")}</small>
        </span>
      `).join("")}
    </div>
  `;
}

function renderReleaseHistory(history = []) {
  if (!history.length) return `<p>暂无发布记录。</p>`;
  return `
    <div class="operation-line-list">
      ${history.map((item) => `
        <span>
          <b>${escapeHtml(`${item.version || ""} ${item.label || ""}`.trim())}</b>
          <em>${escapeHtml(`Phase ${item.phase || 13} / ${item.date || ""}`)}</em>
          <small>${escapeHtml(item.summary || "")}</small>
        </span>
      `).join("")}
    </div>
  `;
}

function renderLogArchive(logArchive = {}) {
  return renderOperationLines([
    ["格式", logArchive.format],
    ["路径", logArchive.path],
    ["大小", logArchive.sizeBytes === undefined ? "" : `${logArchive.sizeBytes} bytes`],
    ["内存事件", logArchive.inMemoryEvents],
    ["导出端点", logArchive.exportEndpoint],
    ["保留策略", logArchive.retention]
  ]);
}

function renderDemoKit(demoKit = {}) {
  const checks = Array.isArray(demoKit.checks) ? demoKit.checks : [];
  return `
    <div class="operation-line-list">
      <span data-operation-status="${demoKit.ready ? "ready" : "needs-sample"}">
        <b>准备度 ${escapeHtml(demoKit.score ?? 0)}%</b>
        <em>${escapeHtml(`样本：${demoKit.sampleCount ?? 0} 件`)}</em>
        <small>${escapeHtml(demoKit.privacyNote || "")}</small>
      </span>
    </div>
    ${renderOperationItems(checks, { showStatus: true, compact: true })}
    <div class="operation-line-list operation-line-list-compact">
      ${(demoKit.storyline || []).slice(0, 5).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
    </div>
  `;
}

function renderPhase14Readiness(readiness = {}) {
  const checks = Array.isArray(readiness.checks) ? readiness.checks : [];
  return `
    <div class="operation-line-list">
      <span data-operation-status="${readiness.ready ? "ready" : "needs-sample"}">
        <b>Phase ${escapeHtml(readiness.targetPhase || 14)} / ${escapeHtml(readiness.score ?? 0)}%</b>
        <em>${escapeHtml(readiness.targetName || "工程模块化和服务边界重构")}</em>
        <small>${escapeHtml(readiness.recommendation || "")}</small>
      </span>
    </div>
    ${renderOperationItems(checks, { showStatus: true, compact: true })}
    <div class="operation-line-list operation-line-list-compact">
      ${(readiness.recommendedOrder || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("")}
    </div>
  `;
}

function renderModuleBoundaryPlan(plan = []) {
  if (!plan.length) return `<p>暂无模块边界计划。</p>`;
  return `
    <div class="operation-line-list">
      ${plan.map((item) => `
        <span data-operation-status="${escapeHtml(item.status || "info")}">
          <b>${escapeHtml(item.label || item.id)}</b>
          <em>${escapeHtml(item.status || "")}</em>
          <small>${escapeHtml(item.rule || "")}</small>
        </span>
      `).join("")}
    </div>
  `;
}

function renderPhase15AssetPlan(plan = []) {
  if (!plan.length) return `<p>暂无专题资产计划。</p>`;
  return `
    ${renderOperationItems(plan, { showStatus: true, compact: true })}
  `;
}

function confirmAiDataScope(rawContent) {
  if (!backendAiConfigured || localProfile.aiConsent) return true;
  const sensitivity = buildLocalSensitivitySummary([{ rawContent, people: [], emotions: [], attachments: [] }]);
  const message = [
    "当前后端已配置 AI。继续整理会把原始记忆文本发送到配置的 OpenAI-compatible 接口。",
    `本段敏感线索风险：${sensitivity.riskLevel}`,
    "确认继续后，会把“允许配置 AI 后发送原始记忆给模型”保存到本地馆主配置。"
  ].join("\n\n");
  if (!confirm(message)) {
    setAiStatus("已取消 Agent 整理：尚未确认 AI 调用范围。", "warning");
    return false;
  }
  persistLocalProfile({ ...localProfile, aiConsent: true });
  renderPrivacyPanel();
  return true;
}

function renderPhase12Readiness(readiness) {
  if (!readiness) return "";
  return `
    <div class="phase12-readiness-heading">
      <div>
        <span>Phase ${escapeHtml(readiness.targetPhase)}</span>
        <strong>${escapeHtml(readiness.targetName)}</strong>
      </div>
      <small>${escapeHtml(`准备度 ${readiness.score}%`)}</small>
    </div>
    <div class="phase12-checks">
      ${(readiness.checks || []).map((item) => `
        <span data-readiness-status="${escapeHtml(item.status)}" title="${escapeHtml(item.detail || "")}">
          ${escapeHtml(item.label)}
        </span>
      `).join("")}
    </div>
    <p>${escapeHtml(readiness.recommendation || "")}</p>
  `;
}

function renderWorkflowTemplateDetail(template) {
  const nodes = Array.isArray(template.nodes) ? template.nodes : [];
  return `
    <div class="workflow-detail-heading">
      <div>
        <span>${escapeHtml(template.statusLabel || "可查看")}</span>
        <strong>${escapeHtml(template.name)}</strong>
      </div>
      <small>${escapeHtml(template.entry || "")}</small>
    </div>
    <div class="workflow-detail-grid">
      <div>
        <strong>节点依据</strong>
        ${nodes.map((node) => `
          <small>${escapeHtml(formatWorkflowNodeLabel(node))}</small>
          <span>${escapeHtml(formatWorkflowList(node.evidence || [], 5))}</span>
        `).join("")}
      </div>
      <div>
        <strong>暂停点</strong>
        ${(template.pausePoints || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("") || "<span>未记录</span>"}
      </div>
      <div>
        <strong>风险信号</strong>
        ${(template.riskSignals || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("") || "<span>未记录</span>"}
      </div>
      <div>
        <strong>下一步动作</strong>
        ${(template.nextActions || []).map((item) => `<span>${escapeHtml(item)}</span>`).join("") || "<span>未记录</span>"}
      </div>
    </div>
  `;
}

function formatWorkflowNodeLabel(node) {
  if (typeof node === "string") return node;
  return [node.agent || node.id, node.stage].filter(Boolean).join(" · ");
}

function formatWorkflowList(items, limit = 6) {
  if (!Array.isArray(items)) return String(items || "未记录");
  return items.slice(0, limit).map((item) => typeof item === "string" ? item : item.label || item.id || String(item)).join(" / ") || "未记录";
}

function render() {
  renderHallNav();
  renderEmotionFilter();
  renderStats();
  renderFormMode();
  renderInsights();
  renderPhase19ImportLab();
  renderWorkflowOrchestration();
  renderPrivacyPanel();
  renderOperationsPanel();
  renderPhase20PlatformPanel();
  renderAssetCollectionPanel();
  renderMemories();
  renderGuide();
}

function mockAnalyzeMemory(rawContent) {
  const text = rawContent.trim();
  const rules = [
    { hall: "youth", keywords: ["毕业", "学校", "操场", "高三", "同学", "考试", "教室", "校园"], title: "被留在校园里的片段", emotions: ["怀念", "遗憾", "迷茫"], tags: ["校园", "青春", "成长"], sourceType: "日记", importance: 4, emotionIntensity: 4 },
    { hall: "friends", keywords: ["朋友", "室友", "兄弟", "姐妹", "群聊", "聚会", "一起"], title: "朋友之间的小型纪念碑", emotions: ["快乐", "怀念", "温暖"], tags: ["朋友", "陪伴", "关系"], sourceType: "聊天片段", importance: 3, emotionIntensity: 3 },
    { hall: "family", keywords: ["妈妈", "爸爸", "家", "爷爷", "奶奶", "外婆", "饭", "春节"], title: "家里传来的回声", emotions: ["怀念", "平静", "温暖"], tags: ["家庭", "生活", "牵挂"], sourceType: "日记", importance: 4, emotionIntensity: 4 },
    { hall: "low", keywords: ["难过", "失眠", "崩溃", "失败", "累", "焦虑", "哭", "撑不住"], title: "低谷里的小灯", emotions: ["委屈", "迷茫", "释然"], tags: ["低谷", "恢复", "自我"], sourceType: "日记", importance: 4, emotionIntensity: 5 },
    { hall: "strange", keywords: ["离谱", "尴尬", "奇怪", "抽象", "荒唐", "笑死", "社死"], title: "一件很难解释的展品", emotions: ["荒诞", "快乐", "紧张"], tags: ["离谱", "尴尬", "趣事"], sourceType: "聊天片段", importance: 2, emotionIntensity: 3 },
    { hall: "daily", keywords: ["照片", "拍照", "相册", "合影", "镜头"], title: "照片背面的一小段注释", emotions: ["怀念", "温暖"], tags: ["照片", "瞬间", "记录"], sourceType: "照片描述", importance: 3, emotionIntensity: 3 },
    { hall: "daily", keywords: ["梦", "梦到", "醒来"], title: "醒来后还没散掉的梦", emotions: ["迷茫", "害怕", "期待"], tags: ["梦境", "潜意识", "夜晚"], sourceType: "梦境", importance: 2, emotionIntensity: 4 },
    { hall: "daily", keywords: ["旅行", "车站", "机场", "海边", "山", "城市"], title: "路途中被保存的坐标", emotions: ["期待", "兴奋", "平静"], tags: ["旅行", "地点", "移动"], sourceType: "旅行片段", importance: 3, emotionIntensity: 3 }
  ];

  const matched = rules.find((rule) => rule.keywords.some((keyword) => text.includes(keyword))) || {
    hall: "daily",
    title: "普通日子里的发光切片",
    emotions: ["平静"],
    tags: ["日常", "片段", "记录"],
    sourceType: "日记",
    importance: 1,
    emotionIntensity: 2
  };

  const shortText = text.length > 62 ? `${text.slice(0, 62)}...` : text;
  return {
    title: matched.title,
    hall: matched.hall,
    emotions: matched.emotions,
    tags: matched.tags,
    sourceType: matched.sourceType,
    importance: matched.importance,
    emotionIntensity: matched.emotionIntensity,
    favorite: matched.importance >= 4,
    exhibitText: `这件展品来自一段私人记忆：“${shortText}”。它被放入${getHallName(matched.hall)}，因为其中有值得被保存的情绪和细节。`,
    coverImage: "",
    mediaNote: /照片|截图|语音|录音|合影|相册/.test(text)
      ? "第十阶段媒体线索：这段记忆可能适合进入时间线、主题展，并补充封面图、OCR 文本或语音转写说明。"
      : "",
    attachments: []
  };
}

async function analyzeMemory(rawContent) {
  try {
    const response = await fetch("/api/analyze", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ rawContent })
    });
    const payload = await readApiPayload(response);
    if (!response.ok) throw new Error(payload.error || "AI 分析失败。");

    const normalized = normalizeAnalysisResult(payload.data || {}, rawContent);
    return {
      result: normalized,
      mode: payload.mode || "ai",
      reason: payload.reason || "",
      workflow: normalizeAgentWorkflow(payload.workflow, normalized, rawContent, payload.mode || "ai")
    };
  } catch (error) {
    const result = mockAnalyzeMemory(rawContent);
    return {
      result,
      mode: "local-mock",
      reason: limitText(error.message, 160),
      workflow: buildLocalAgentWorkflow(rawContent, result, "local-mock")
    };
  }
}

function normalizeAgentWorkflow(workflow, result, rawContent, mode) {
  const steps = Array.isArray(workflow?.steps) ? workflow.steps : [];
  if (steps.length) {
    const normalizedSteps = steps.slice(0, 6).map((step) => ({
      id: limitText(step.id, 40) || createId(),
      agent: limitText(step.agent, 40) || "Agent",
      duty: limitText(step.duty, 120),
      status: limitText(step.status, 20) || "done",
      output: limitText(step.output, 220) || "已完成。",
      evidence: Array.isArray(step.evidence) ? step.evidence.slice(0, 8).map((item) => limitText(item, 40)).filter(Boolean) : [],
      actions: Array.isArray(step.actions) ? step.actions.slice(0, 4).map((item) => limitText(item, 30)).filter(Boolean) : []
    }));
    return {
      phase: Number(workflow.phase) || 7,
      mode: workflow.mode || mode,
      run: normalizeAgentRun(workflow.run, workflow.mode || mode),
      stateMachine: normalizeStateMachine(workflow.stateMachine),
      summary: normalizeWorkflowSummary(workflow.summary, normalizedSteps),
      steps: normalizedSteps
    };
  }
  return buildLocalAgentWorkflow(rawContent, result, mode);
}

function buildLocalAgentWorkflow(rawContent, result, mode = "local-mock") {
  const shortRaw = limitText(rawContent, 96);
  const people = result.people?.length ? result.people.slice(0, 4).join("、") : "未识别明确人物";
  const tags = result.tags?.length ? result.tags.slice(0, 4).join("、") : "暂无标签";
  const emotions = result.emotions?.length ? result.emotions.slice(0, 4).join("、") : "暂无情绪";
  const reviewItems = buildLocalReviewItems(result);
  const archiveNeedsReview = reviewItems.some((item) => ["people", "location", "date"].includes(item.field));
  const editorNeedsReview = reviewItems.some((item) => ["tags", "exhibitText"].includes(item.field));
  const needsReview = [archiveNeedsReview, editorNeedsReview].filter(Boolean).length;
  const ready = reviewItems.length ? 0 : 1;
  const done = 3 - needsReview;
  return {
    phase: 10,
    mode,
    run: createAgentRun(mode, "本地 Agent 工作流已创建"),
    stateMachine: {
      statuses: ["queued", "running", "needs_review", "approved", "rejected", "failed", "done", "ready"],
      actions: ["confirm", "reject", "retry", "save"]
    },
    summary: {
      total: 4,
      done,
      ready,
      running: 0,
      needsReview,
    approved: 0,
    rejected: 0,
      status: reviewItems.length ? "needs_review" : "ready",
      progress: Math.round(((done + ready) / 4) * 100),
      requiresHumanReview: reviewItems.length > 0,
      confirmationItems: reviewItems.map((item) => ({ ...item, state: "pending", action: "confirm" })),
      reviewItems,
      nextAction: reviewItems.length ? "补全复核项并确认后，可以保存展品" : "保存展品后可进入讲解员检索池"
    },
    steps: [
      { id: "archivist", agent: "档案员 Agent", duty: "提取人物、地点、时间、来源和原始线索", status: archiveNeedsReview ? "needs_review" : "done", output: `已读取原始片段“${shortRaw}”，提取人物：${people}。`, evidence: ["rawContent", "people", "location", "date"], actions: ["confirm", "reject", "retry"] },
      { id: "curator", agent: "策展人 Agent", duty: "判断展厅、情绪和珍藏级别", status: "done", output: `已归入${getHallName(result.hall)}，情绪为 ${emotions}，强度 ${result.emotionIntensity} / 5。`, evidence: ["hall", "emotions", "importance"], actions: ["confirm", "reject"] },
      { id: "editor", agent: "编辑 Agent", duty: "生成标题、标签和展品说明", status: editorNeedsReview ? "needs_review" : "done", output: `已生成标题《${result.title}》，标签：${tags}。`, evidence: ["title", "tags", "exhibitText"], actions: ["confirm", "reject", "retry"] },
      { id: "guide", agent: "讲解员 Agent", duty: "整理面向参观者的导览提示", status: reviewItems.length ? "queued" : "ready", output: reviewItems.length ? "等待人工确认完成后，再进入讲解员检索池。" : "保存后会进入讲解员检索池，参与后续 RAG 回答。", evidence: ["savedMemory", "ragCandidate"], actions: reviewItems.length ? [] : ["confirm"] }
    ]
  };
}

function normalizeStateMachine(stateMachine = {}) {
  const statuses = Array.isArray(stateMachine.statuses)
    ? stateMachine.statuses.map((item) => limitText(item, 30)).filter(Boolean)
    : ["queued", "running", "needs_review", "approved", "rejected", "failed", "done", "ready"];
  const actions = Array.isArray(stateMachine.actions)
    ? stateMachine.actions.map((item) => limitText(item, 30)).filter(Boolean)
    : ["confirm", "retry", "save"];
  return { statuses, actions };
}

function createAgentRun(mode = "local", label = "Agent 工作流已创建") {
  const now = new Date().toISOString();
  return {
    id: createId(),
    phase: 10,
    mode,
    createdAt: now,
    eventCount: 1,
    events: [{ type: "workflow_created", label, at: now }]
  };
}

function normalizeAgentRun(run, mode = "local") {
  const fallback = createAgentRun(mode);
  if (!run || typeof run !== "object") return fallback;
  const events = Array.isArray(run.events)
    ? run.events.slice(-6).map((event) => ({
      type: limitText(event.type, 40) || "event",
      label: limitText(event.label, 80) || "已记录工作流事件",
      at: limitText(event.at, 40) || new Date().toISOString(),
      step: limitText(event.step, 40)
    }))
    : fallback.events;
  return {
    id: limitText(run.id, 80) || fallback.id,
    phase: Number(run.phase) || 6,
    mode: limitText(run.mode, 40) || mode,
    createdAt: limitText(run.createdAt, 40) || fallback.createdAt,
    eventCount: Math.max(Number(run.eventCount) || events.length, events.length),
    events
  };
}

function appendAgentEvent(type, label, stepId = "") {
  if (!latestAgentWorkflow) return;
  const run = normalizeAgentRun(latestAgentWorkflow.run, latestAgentWorkflow.mode || "local");
  const event = {
    type: limitText(type, 40) || "event",
    label: limitText(label, 80) || "已记录工作流事件",
    step: limitText(stepId, 40),
    at: new Date().toISOString()
  };
  run.events = [...(run.events || []), event].slice(-6);
  run.eventCount = Math.max(Number(run.eventCount) || 0, 0) + 1;
  latestAgentWorkflow.run = run;
}

function formatAgentRunTime(value) {
  if (!value) return "刚刚";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "刚刚";
  return date.toLocaleString("zh-CN", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" });
}

function normalizeWorkflowSummary(summary, steps) {
  const total = steps.length;
  const done = steps.filter((step) => step.status === "done").length;
  const ready = steps.filter((step) => step.status === "ready").length;
  const running = steps.filter((step) => step.status === "running").length;
  return {
    total: Number(summary?.total) || total,
    done: Number(summary?.done) || done,
    ready: Number(summary?.ready) || ready,
    running: Number(summary?.running) || running,
    needsReview: Number(summary?.needsReview) || steps.filter((step) => step.status === "needs_review").length,
    approved: Number(summary?.approved) || steps.filter((step) => step.status === "approved").length,
    rejected: Number(summary?.rejected) || steps.filter((step) => step.status === "rejected").length,
    status: limitText(summary?.status, 20) || (running > 0 ? "running" : ready > 0 ? "ready" : "pending"),
    progress: Math.min(100, Math.max(0, Number(summary?.progress) || Math.round(((done + ready) / Math.max(total, 1)) * 100))),
    requiresHumanReview: Boolean(summary?.requiresHumanReview),
    confirmationItems: normalizeReviewItems(summary?.confirmationItems),
    reviewItems: normalizeReviewItems(summary?.reviewItems),
    nextAction: limitText(summary?.nextAction, 80) || (ready > 0 ? "保存展品后可进入讲解员检索池" : "继续等待 Agent 工作流完成")
  };
}

function buildLocalReviewItems(result = {}) {
  const items = [];
  if (!result.people?.length) items.push({ field: "people", label: "人物未明确" });
  if (!result.location) items.push({ field: "location", label: "地点可补充" });
  if (!result.date) items.push({ field: "date", label: "时间可补充" });
  if (!result.tags?.length || result.tags.length < 2) items.push({ field: "tags", label: "标签可再丰富" });
  if (!result.exhibitText || result.exhibitText.length < 40) items.push({ field: "exhibitText", label: "展品说明偏短" });
  return items.slice(0, 4);
}

function normalizeReviewItems(items) {
  if (!Array.isArray(items)) return [];
  return items.map((item) => {
    if (typeof item === "string") {
      return { field: getReviewFieldFromLabel(item), label: limitText(item, 40) };
    }
    return {
      field: limitText(item.field, 40) || getReviewFieldFromLabel(item.label),
      label: limitText(item.label, 40)
    };
  }).filter((item) => item.label).slice(0, 4);
}

function getReviewFieldFromLabel(label = "") {
  const text = String(label);
  if (text.includes("人物")) return "people";
  if (text.includes("地点")) return "location";
  if (text.includes("时间")) return "date";
  if (text.includes("标签")) return "tags";
  if (text.includes("说明")) return "exhibitText";
  return "";
}

function renderAgentWorkflow(workflow) {
  const steps = Array.isArray(workflow?.steps) ? workflow.steps : [];
  if (!steps.length) {
    elements.agentWorkflow.innerHTML = "";
    elements.agentWorkflow.setAttribute("aria-busy", "false");
    latestAgentWorkflow = null;
    updateAgentActionState();
    renderDraftPreview();
    return;
  }
  if (!workflow.run) workflow.run = createAgentRun(workflow.mode || "local");
  latestAgentWorkflow = workflow;
  const summary = workflow.summary || null;
  const progress = summary ? Math.min(100, Math.max(0, Number(summary.progress) || 0)) : 0;
  const isBusy = Boolean(summary && summary.status !== "ready" && summary.status !== "done" && progress < 100);
  elements.agentWorkflow.setAttribute("aria-busy", isBusy ? "true" : "false");

  elements.agentWorkflow.innerHTML = `
    <div class="agent-workflow-heading">
      <span>Agent 工作流</span>
      <small>${escapeHtml(workflow.mode || "local")}</small>
    </div>
    ${renderAgentStateMachine(workflow.stateMachine, summary?.status)}
    ${summary ? `
      <div class="agent-summary">
        <span>${escapeHtml(`${summary.done}/${summary.total} 已完成`)}</span>
        <span>${escapeHtml(`${summary.ready} 个就绪`)}</span>
        ${Number(summary.needsReview) > 0 ? `<span data-summary-state="needs-review">${escapeHtml(`${summary.needsReview} 个待确认`)}</span>` : ""}
        ${Number(summary.approved) > 0 ? `<span data-summary-state="approved">${escapeHtml(`${summary.approved} 个已确认`)}</span>` : ""}
        ${Number(summary.rejected) > 0 ? `<span data-summary-state="rejected">${escapeHtml(`${summary.rejected} 个已驳回`)}</span>` : ""}
        <span>${escapeHtml(`${progress}%`)}</span>
        <strong>${escapeHtml(summary.nextAction || "")}</strong>
      </div>
      <div class="agent-progress" role="progressbar" aria-label="Agent 工作流进度" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${progress}">
        <span style="width: ${progress}%"></span>
      </div>
      ${renderAgentReview(summary)}
    ` : ""}
    ${renderAgentRunSummary(workflow.run)}
    <div class="agent-steps">
      ${steps.map((step, index) => `
        <article class="agent-step" data-status="${escapeHtml(step.status || "done")}">
          <span class="agent-step-index">${index + 1}</span>
          <div>
            <div class="agent-step-title">
              <strong>${escapeHtml(step.agent || "Agent")}</strong>
              <span>${escapeHtml(getAgentStatusLabel(step.status))}</span>
            </div>
            ${step.duty ? `<small>${escapeHtml(step.duty)}</small>` : ""}
            <p>${escapeHtml(step.output || "已完成。")}</p>
            ${renderAgentStepEvidence(step)}
            ${renderAgentStepActions(step)}
          </div>
        </article>
      `).join("")}
    </div>
  `;
  updateAgentActionState();
  renderDraftPreview();
}

function renderAgentRunSummary(run) {
  const normalizedRun = normalizeAgentRun(run);
  const recentEvents = Array.isArray(normalizedRun.events) ? normalizedRun.events.slice(-3).reverse() : [];
  const shortId = normalizedRun.id.length > 8 ? normalizedRun.id.slice(0, 8) : normalizedRun.id;
  return `
    <div class="agent-run-summary" aria-label="Agent 运行摘要">
      <div class="agent-run-meta">
        <span>运行 ${escapeHtml(shortId)}</span>
        <span>阶段 ${escapeHtml(String(normalizedRun.phase))}</span>
        <span>${escapeHtml(`${normalizedRun.eventCount} 条事件`)}</span>
        <span>${escapeHtml(formatAgentRunTime(normalizedRun.createdAt))}</span>
      </div>
      ${recentEvents.length ? `
        <div class="agent-run-events">
          ${recentEvents.map((event) => `
            <span title="${escapeHtml(formatAgentRunTime(event.at))}">
              ${escapeHtml(event.label)}
            </span>
          `).join("")}
        </div>
      ` : ""}
    </div>
  `;
}

function renderAgentStateMachine(stateMachine, currentStatus = "") {
  const statuses = Array.isArray(stateMachine?.statuses) ? stateMachine.statuses : [];
  if (!statuses.length) return "";
  return `
    <div class="agent-state-machine" aria-label="Agent 状态机">
      ${statuses.map((status) => `
        <span data-state="${escapeHtml(status)}" ${status === currentStatus ? "data-current" : ""}>
          ${escapeHtml(getAgentStatusLabel(status))}
        </span>
      `).join("")}
    </div>
  `;
}

function renderAgentStepEvidence(step) {
  const evidence = Array.isArray(step.evidence) ? step.evidence.filter(Boolean).slice(0, 6) : [];
  if (!evidence.length) return "";
  return `
    <div class="agent-step-evidence" aria-label="Agent 依据字段">
      ${evidence.map((item) => `<span>${escapeHtml(getEvidenceLabel(item))}</span>`).join("")}
    </div>
  `;
}

function getEvidenceLabel(key = "") {
  return {
    rawContent: "原始记忆",
    people: "人物",
    location: "地点",
    date: "时间",
    hall: "展厅",
    emotions: "情绪",
    emotionIntensity: "情绪强度",
    importance: "珍藏级别",
    title: "标题",
    tags: "标签",
    exhibitText: "展品说明",
    savedMemory: "保存展品",
    ragCandidate: "讲解员检索"
  }[key] || key;
}

function renderAgentStepActions(step) {
  const actions = new Set(Array.isArray(step.actions) ? step.actions : []);
  if (step.status === "needs_review") {
    actions.add("confirm");
    actions.add("reject");
    actions.add("retry");
  }
  if (!actions.size) return "";

  return `
    <div class="agent-step-actions">
      ${actions.has("confirm") ? `<button type="button" data-agent-approve-step="${escapeHtml(step.id || "")}">确认本步</button>` : ""}
      ${actions.has("reject") ? `<button type="button" data-agent-reject-step="${escapeHtml(step.id || "")}">驳回本步</button>` : ""}
      ${actions.has("retry") ? `<button type="button" data-agent-retry-step="${escapeHtml(step.id || "")}">重试整理</button>` : ""}
    </div>
  `;
}

function renderAgentReview(summary = {}) {
  const items = Array.isArray(summary.reviewItems) ? summary.reviewItems : [];
  const isComplete = summary.status === "ready" || summary.status === "done" || Number(summary.progress) >= 100;
  if (!isComplete && items.length === 0) {
    return `
      <div class="agent-review agent-review-waiting">
        <span>复核状态</span>
        <strong class="agent-review-pending">等待 Agent 输出</strong>
      </div>
    `;
  }

  if (items.length === 0) {
    const completeLabel = summary.reviewConfirmed ? "人工确认完成，可以保存" : "无需复核，可以保存";
    return `
      <div class="agent-review agent-review-empty" data-complete>
        <span>复核状态</span>
        <strong class="agent-review-complete">${escapeHtml(completeLabel)}</strong>
      </div>
    `;
  }

  return `
    <div class="agent-review">
      <span>建议复核</span>
      ${items.map((item) => `<button type="button" data-review-field="${escapeHtml(item.field || "")}" aria-label="${escapeHtml(`定位到${item.label}`)}" title="${escapeHtml(`定位到${item.label}`)}">${escapeHtml(item.label)}</button>`).join("")}
      <button type="button" class="agent-review-confirm" data-confirm-review hidden>确认复核完成</button>
      <strong class="agent-review-complete" hidden>人工确认完成，可以保存</strong>
    </div>
  `;
}

function refreshAgentReviewItems() {
  if (!latestAgentDraft || !elements.agentWorkflow.innerHTML.trim()) return;
  const reviewItems = buildCurrentReviewItemsFromForm();
  elements.agentWorkflow.querySelectorAll("[data-review-field]").forEach((button) => {
    const field = button.dataset.reviewField;
    const stillNeedsReview = reviewItems.some((item) => item.field === field);
    button.toggleAttribute("hidden", !stillNeedsReview);
  });
  const reviewBox = elements.agentWorkflow.querySelector(".agent-review");
  if (reviewBox) {
    if (reviewBox.classList.contains("agent-review-waiting")) return;
    const visibleButtons = Array.from(reviewBox.querySelectorAll("[data-review-field]")).filter((button) => !button.hidden);
    const completeMessage = reviewBox.querySelector(".agent-review-complete");
    const confirmButton = reviewBox.querySelector(".agent-review-confirm");
    const readyToConfirm = visibleButtons.length === 0;
    if (!readyToConfirm) reviewBox.removeAttribute("data-confirmed");
    const isConfirmed = reviewBox.hasAttribute("data-confirmed");
    if (confirmButton) confirmButton.toggleAttribute("hidden", !readyToConfirm || isConfirmed);
    if (completeMessage) completeMessage.toggleAttribute("hidden", !readyToConfirm || !isConfirmed);
    reviewBox.toggleAttribute("data-complete", readyToConfirm && isConfirmed);
  }
}

function buildCurrentReviewItemsFromForm() {
  const result = {
    people: splitList(elements.peopleInput.value),
    location: elements.locationInput.value.trim(),
    date: elements.memoryDate.value,
    tags: splitList(elements.tagsInput.value),
    exhibitText: elements.exhibitText.value.trim()
  };
  return buildLocalReviewItems(result);
}

function getAgentStatusLabel(status = "done") {
  return {
    done: "已完成",
    approved: "已确认",
    rejected: "已驳回",
    needs_review: "待确认",
    ready: "已就绪",
    running: "运行中",
    queued: "等待中",
    failed: "需处理"
  }[status] || status;
}

function approveAgentStep(stepId) {
  if (!latestAgentWorkflow || !stepId) return;
  const step = latestAgentWorkflow.steps?.find((item) => item.id === stepId);
  if (!step || step.status === "approved") return;
  step.status = "approved";
  step.output = `${step.output || "已完成。"} 已由用户确认。`;
  appendAgentEvent("step_approved", `${step.agent || "Agent"}已确认`, step.id);
  latestAgentWorkflow.summary = rebuildClientWorkflowSummary(latestAgentWorkflow);
  renderAgentWorkflow(latestAgentWorkflow);
  setAiStatus("已确认该 Agent 步骤。", "success");
}

function rejectAgentStep(stepId) {
  if (!latestAgentWorkflow || !stepId) return;
  const step = latestAgentWorkflow.steps?.find((item) => item.id === stepId);
  if (!step || step.status === "rejected") return;
  step.status = "rejected";
  step.output = `${step.output || "已完成。"} 用户已驳回本步建议，可手动调整表单后保存。`;
  appendAgentEvent("step_rejected", `${step.agent || "Agent"}已驳回`, step.id);
  latestAgentWorkflow.summary = rebuildClientWorkflowSummary(latestAgentWorkflow);
  renderAgentWorkflow(latestAgentWorkflow);
  setAiStatus("已驳回该 Agent 步骤，可手动调整后保存。", "warning");
}

function retryAgentStep(stepId) {
  if (!stepId || isAnalyzing) return;
  const step = latestAgentWorkflow?.steps?.find((item) => item.id === stepId);
  if (step && latestAgentWorkflow) {
    step.status = "running";
    step.output = `${step.agent || "Agent"}正在重新整理这一部分。`;
    appendAgentEvent("step_retry", `${step.agent || "Agent"}已触发重试`, step.id);
    latestAgentWorkflow.summary = rebuildClientWorkflowSummary(latestAgentWorkflow);
    renderAgentWorkflow(latestAgentWorkflow);
  }
  setAiStatus(`正在重新运行${step?.agent || "Agent"}整理...`, "loading");
  window.setTimeout(() => {
    handleMockAi({
      type: "step_retry_completed",
      label: `${step?.agent || "Agent"}重试后已生成新结果`,
      step: stepId
    });
  }, 180);
}

function confirmAgentReview(button) {
  const reviewBox = button.closest(".agent-review");
  if (!reviewBox) return;
  const remainingReviewItems = buildCurrentReviewItemsFromForm();
  if (remainingReviewItems.length > 0) {
    setAiStatus("还有复核项未补全，请先补完后再确认。", "warning");
    return;
  }
  reviewBox.setAttribute("data-confirmed", "true");
  if (latestAgentWorkflow?.summary) {
    latestAgentWorkflow.steps = (latestAgentWorkflow.steps || []).map((step) => {
      if (step.status === "needs_review") {
        return {
          ...step,
          status: "approved",
          output: `${step.output || "已完成。"} 已由用户确认。`
        };
      }
      if (step.status === "queued" && step.id === "guide") {
        return {
          ...step,
          status: "ready",
          actions: ["confirm"],
          output: "人工复核完成，保存后可进入讲解员检索池。"
        };
      }
      return step;
    });
    latestAgentWorkflow.summary.requiresHumanReview = false;
    latestAgentWorkflow.summary.confirmationItems = (latestAgentWorkflow.summary.confirmationItems || []).map((item) => ({
      ...item,
      state: "confirmed"
    }));
    latestAgentWorkflow.summary.reviewItems = [];
    latestAgentWorkflow.summary.reviewConfirmed = true;
    latestAgentWorkflow.summary.nextAction = "人工复核已确认，可以保存展品";
    latestAgentWorkflow.summary = {
      ...latestAgentWorkflow.summary,
      ...rebuildClientWorkflowSummary(latestAgentWorkflow),
      requiresHumanReview: false,
      confirmationItems: latestAgentWorkflow.summary.confirmationItems,
      reviewItems: [],
      reviewConfirmed: true,
      nextAction: "人工复核已确认，可以保存展品"
    };
    appendAgentEvent("review_confirmed", "人工复核已确认");
  }
  renderAgentWorkflow(latestAgentWorkflow);
  setAiStatus("人工复核已确认，可以保存展品。", "success");
}

function rebuildClientWorkflowSummary(workflow) {
  const steps = Array.isArray(workflow?.steps) ? workflow.steps : [];
  const done = steps.filter((step) => step.status === "done").length;
  const ready = steps.filter((step) => step.status === "ready").length;
  const running = steps.filter((step) => step.status === "running").length;
  const needsReview = steps.filter((step) => step.status === "needs_review").length;
  const approved = steps.filter((step) => step.status === "approved").length;
  const rejected = steps.filter((step) => step.status === "rejected").length;
  const completed = done + ready + approved + rejected;
  return {
    ...(workflow.summary || {}),
    total: steps.length,
    done,
    ready,
    running,
    needsReview,
    approved,
    rejected,
    status: running > 0 ? "running" : needsReview > 0 ? "needs_review" : ready > 0 ? "ready" : "done",
    progress: steps.length ? Math.round((completed / steps.length) * 100) : 0
  };
}

function updateAgentActionState() {
  const hasWorkflow = elements.agentWorkflow.innerHTML.trim().length > 0;
  elements.useAgentSuggestionButton.disabled = isAnalyzing || !latestAgentDraft || !hasWorkflow;
  elements.clearAgentWorkflowButton.disabled = isAnalyzing || !hasWorkflow;
}

function useAgentSuggestion() {
  if (!latestAgentDraft || isAnalyzing) return;
  const manualFields = {
    people: elements.peopleInput.value,
    location: elements.locationInput.value,
    date: elements.memoryDate.value,
    tags: elements.tagsInput.value,
    exhibitText: elements.exhibitText.value
  };
  applyAnalysisToForm(latestAgentDraft);
  if (manualFields.people.trim()) elements.peopleInput.value = manualFields.people;
  if (manualFields.location.trim()) elements.locationInput.value = manualFields.location;
  if (manualFields.date) elements.memoryDate.value = manualFields.date;
  if (manualFields.tags.trim()) elements.tagsInput.value = manualFields.tags;
  if (manualFields.exhibitText.trim()) elements.exhibitText.value = manualFields.exhibitText;
  appendAgentEvent("suggestion_applied", "Agent 建议已重新应用");
  refreshAgentReviewItems();
  setAiStatus("已重新应用 Agent 建议，你可以继续微调后保存。", "success");
  renderGuide(`已把 Agent 建议重新应用到表单。\n\n标题：${latestAgentDraft.title}\n展厅：${getHallName(latestAgentDraft.hall)}\n情绪：${latestAgentDraft.emotions.join("、")}`);
}

function clearAgentWorkflow() {
  if (isAnalyzing) return;
  renderAgentWorkflow();
}

function focusReviewField(field) {
  const target = {
    people: elements.peopleInput,
    location: elements.locationInput,
    date: elements.memoryDate,
    tags: elements.tagsInput,
    exhibitText: elements.exhibitText
  }[field];
  if (!target) return;
  target.focus();
  target.scrollIntoView({ behavior: "smooth", block: "center" });
  setAiStatus("已定位到建议复核的字段。", "loading");
}

function handleReviewFieldInput() {
  refreshAgentReviewItems();
}

async function askGuide(question) {
  try {
    const payload = await requestJson("/api/guide", {
      method: "POST",
      body: JSON.stringify({ question })
    });
    return {
      mode: payload.mode || "mock",
      answer: payload.answer || "讲解员暂时没有生成回答。",
      memories: Array.isArray(payload.memories) ? payload.memories : [],
      citations: Array.isArray(payload.citations) ? payload.citations : [],
      followUps: Array.isArray(payload.followUps) ? payload.followUps.slice(0, 3) : [],
      retrievalMode: payload.retrievalMode || "keyword",
      query: payload.query || "",
      reason: payload.reason || ""
    };
  } catch (error) {
    return {
      mode: "local",
      answer: buildLocalGuideAnswer(question),
      memories: filteredMemories().slice(0, 4).map(buildStructuredMemory),
      citations: [],
      followUps: ["检查后端服务是否已启动", "换一个更具体的问题", "先从当前展厅导览开始"],
      retrievalMode: "local-page",
      query: question,
      reason: limitText(error.message, 160)
    };
  }
}

function buildLocalGuideAnswer(question) {
  const candidates = filteredMemories().slice(0, 4);
  if (candidates.length === 0) {
    return `你的问题是：“${question}”。\n\n当前页面没有可用展品。可以先同步数据库、清空筛选，或新增几件展品后再问我。`;
  }

  const titles = candidates.map((memory) => `《${memory.title}》`).join("、");
  const emotions = [...new Set(candidates.flatMap((memory) => memory.emotions || []))].slice(0, 5).join("、") || "暂未记录";
  return `你的问题是：“${question}”。\n\n当前未能连接后端 RAG 接口，我先基于页面里可见的展品回答。可以参考 ${titles}。\n\n这些展品共同呈现出的情绪包括：${emotions}。其中《${candidates[0].title}》最适合作为第一件讲解展品：${candidates[0].exhibitText}`;
}

function formatGuideEvidence(memories) {
  if (!memories.length) return "暂无候选展品";

  return memories.slice(0, 4).map((memory) => {
    const hallId = typeof memory.hall === "object" ? memory.hall?.id : memory.hall;
    const hallName = typeof memory.hall === "object" ? memory.hall?.name : getHallName(hallId);
    const emotions = (memory.emotions || []).slice(0, 2).join("、");
    const importance = memory.importanceLabel || getImportanceLabel(memory.importance);
    const attachmentCount = normalizeAttachments(memory.attachments).length;
    const media = attachmentCount ? `附件 ${attachmentCount}` : memory.mediaNote ? "多模态线索" : "";
    const meta = [hallName, emotions, importance, media].filter(Boolean).join(" / ");
    return `《${memory.title}》${meta ? `（${meta}）` : ""}`;
  }).join("、");
}

function formatGuideCitations(citations) {
  if (!citations.length) return "";
  return citations.slice(0, 4).map((citation) => {
    const title = citation.memory?.title || "未命名展品";
    const score = Number(citation.score) ? `分数 ${citation.score}` : "";
    const fields = (citation.matchedFields || []).slice(0, 3).join("、");
    const semantic = (citation.semanticTerms || []).slice(0, 3).join("、");
    const confidence = citation.confidence?.label ? `可信度 ${citation.confidence.label}` : "";
    const reason = citation.reason ? `原因 ${citation.reason}` : "";
    const parts = [confidence, score, fields ? `字段 ${fields}` : "", semantic ? `语义 ${semantic}` : "", reason].filter(Boolean).join(" / ");
    return `《${title}》${parts ? `（${parts}）` : ""}`;
  }).join("、");
}

function formatGuideFollowUps(followUps) {
  if (!followUps.length) return "";
  return followUps.slice(0, 3).map((item, index) => `${index + 1}. ${item}`).join("\n");
}

function normalizeAnalysisResult(value, rawContent) {
  const draft = mockAnalyzeMemory(rawContent);
  const hallId = typeof value.hall === "object" ? value.hall?.id : value.hall;
  const hallExists = halls.some((hall) => hall.id === hallId && hall.id !== "all");
  const tags = normalizeList(value.tags).length ? normalizeList(value.tags) : draft.tags;
  const people = normalizeList(value.people);
  const emotions = normalizeList(value.emotions).filter((emotion) => emotionOptions.includes(emotion));

  return {
    title: limitText(value.title || draft.title, fieldLimits.title),
    hall: hallExists ? hallId : draft.hall,
    emotions: emotions.length ? emotions.slice(0, 4) : draft.emotions,
    tags: tags.slice(0, 8),
    people: people.slice(0, 8),
    location: limitText(value.location || "", fieldLimits.location),
    date: limitText(value.date || "", fieldLimits.date),
    sourceType: sourceTypes.includes(value.sourceType) ? value.sourceType : draft.sourceType,
    importance: Math.min(5, Math.max(1, Number(value.importance) || draft.importance)),
    emotionIntensity: Math.min(5, Math.max(1, Number(value.emotionIntensity) || draft.emotionIntensity)),
    favorite: parseBoolean(value.favorite) || Number(value.importance) >= 4,
    exhibitText: limitText(value.exhibitText || draft.exhibitText, fieldLimits.exhibitText),
    coverImage: limitText(value.coverImage || "", fieldLimits.coverImage),
    mediaNote: limitText(value.mediaNote || draft.mediaNote || "", fieldLimits.mediaNote),
    attachments: normalizeAttachments(value.attachments)
  };
}

function applyAnalysisToForm(result) {
  elements.titleInput.value = result.title;
  elements.hallSelect.value = result.hall;
  elements.sourceType.value = result.sourceType;
  elements.importanceSelect.value = String(result.importance);
  elements.emotionIntensity.value = String(result.emotionIntensity);
  elements.favoriteInput.checked = result.favorite;
  elements.locationInput.value = result.location || elements.locationInput.value;
  elements.memoryDate.value = result.date && /^\d{4}-\d{2}-\d{2}$/.test(result.date) ? result.date : elements.memoryDate.value;
  elements.peopleInput.value = result.people?.length ? result.people.join("，") : elements.peopleInput.value;
  elements.exhibitText.value = result.exhibitText;
  elements.coverImageInput.value = result.coverImage || elements.coverImageInput.value;
  elements.mediaNoteInput.value = result.mediaNote || elements.mediaNoteInput.value;
  elements.attachmentsInput.value = result.attachments?.length ? formatAttachmentsInput(result.attachments) : elements.attachmentsInput.value;
  elements.tagsInput.value = result.tags.join("，");
  setSelectedEmotions(result.emotions);
  updateEmotionIntensityOutput();
  renderDraftPreview();
}

async function handleMockAi(runEvent = null) {
  const raw = elements.rawContent.value.trim();
  if (!raw) {
    alert("先写一段原始记忆，再让 Agent 工作流整理。");
    return;
  }
  if (!confirmAiDataScope(raw)) return;

  setAnalyzePending(true);
  try {
    setAiStatus("正在运行多 Agent 工作流...", "loading");
    renderAgentWorkflow({
      mode: "running",
      phase: 10,
      run: createAgentRun("running", "Agent 工作流开始运行"),
      stateMachine: {
        statuses: ["queued", "running", "needs_review", "approved", "rejected", "failed", "done", "ready"],
        actions: ["confirm", "reject", "retry", "save"]
      },
      summary: {
        total: 4,
        done: 0,
        ready: 0,
        running: 1,
        status: "running",
        progress: 25,
        reviewItems: [],
        nextAction: "等待多 Agent 工作流完成"
      },
      steps: [
        { agent: "档案员 Agent", duty: "提取人物、地点、时间、来源和原始线索", status: "running", output: "正在读取原始记忆，提取人物、地点、时间和来源线索。" },
        { agent: "策展人 Agent", duty: "判断展厅、情绪和珍藏级别", status: "queued", output: "等待判断展厅、情绪和珍藏级别。" },
        { agent: "编辑 Agent", duty: "生成标题、标签和展品说明", status: "queued", output: "等待生成标题、标签和展品说明。" },
        { agent: "讲解员 Agent", duty: "整理面向参观者的导览提示", status: "queued", output: "等待把展品接入后续检索讲解。" }
      ]
    });
    const { result, mode, reason, workflow } = await analyzeMemory(raw);
    latestAgentDraft = result;
    applyAnalysisToForm(result);
    if (runEvent) {
      workflow.run = normalizeAgentRun(workflow.run, workflow.mode || mode);
      latestAgentWorkflow = workflow;
      appendAgentEvent(runEvent.type, runEvent.label, runEvent.step);
    }
    renderAgentWorkflow(workflow);

    if (mode === "ai") {
      setAiStatus("Agent 工作流完成：已使用后端真实 AI 接口。", "success");
    } else if (mode === "mock") {
      setAiStatus("AI API 未配置或不可用：后端已使用 Mock Agent 工作流回退。", "warning");
    } else {
      setAiStatus(`无法连接后端：已使用前端本地 Agent 工作流。${reason ? `原因：${reason}` : ""}`, "warning");
    }

    renderGuide(`Agent 工作流已经把这段记忆整理成展品草稿。\n\n标题：${result.title}\n展厅：${getHallName(result.hall)}\n情绪：${result.emotions.join("、")}\n模式：${mode}\n\n下一步你可以手动修改，然后保存为展品。`);
  } finally {
    setAnalyzePending(false);
  }
}

async function fillSampleMemory() {
  const sample = samplePrompts[Math.floor(Math.random() * samplePrompts.length)];
  resetFormState();
  elements.rawContent.value = sample;
  updateRawCount();
  await handleMockAi();
}

function clearCollectionFilters() {
  activeHall = "all";
  searchKeyword = "";
  sortMode = "newest";
  emotionFilter = "all";
  elements.searchInput.value = "";
  elements.sortSelect.value = sortMode;
  elements.emotionFilterSelect.value = emotionFilter;
  render();
}

function buildMemoryFromForm(existing = {}) {
  const rawContent = elements.rawContent.value.trim();
  const draft = mockAnalyzeMemory(rawContent);
  const tags = splitList(elements.tagsInput.value);
  const emotions = selectedEmotions();

  return normalizeMemory({
    ...existing,
    title: elements.titleInput.value.trim() || draft.title,
    hall: elements.hallSelect.value || draft.hall,
    rawContent,
    exhibitText: elements.exhibitText.value.trim() || draft.exhibitText,
    date: elements.memoryDate.value,
    location: elements.locationInput.value.trim(),
    sourceType: elements.sourceType.value || draft.sourceType,
    importance: Number(elements.importanceSelect.value) || draft.importance,
    emotionIntensity: Number(elements.emotionIntensity.value) || draft.emotionIntensity,
    favorite: elements.favoriteInput.checked,
    agentRunId: latestAgentWorkflow?.run?.id || existing.agentRunId || "",
    people: splitList(elements.peopleInput.value),
    tags: tags.length ? tags : draft.tags,
    emotions: emotions.length ? emotions : draft.emotions,
    coverImage: elements.coverImageInput.value.trim(),
    mediaNote: elements.mediaNoteInput.value.trim(),
    attachments: normalizeAttachments(elements.attachmentsInput.value),
    createdAt: existing.createdAt || new Date().toISOString(),
    updatedAt: existing.id ? new Date().toISOString() : ""
  });
}

function resetFormState() {
  editingId = null;
  latestAgentDraft = null;
  elements.memoryForm.reset();
  renderAgentWorkflow();
  elements.emotionIntensity.value = "3";
  updateEmotionIntensityOutput();
  updateRawCount();
  renderFormMode();
}

function agentReviewBlocksSave() {
  if (!latestAgentWorkflow?.summary?.requiresHumanReview) return false;
  return buildCurrentReviewItemsFromForm().length > 0 || !latestAgentWorkflow.summary.reviewConfirmed;
}

async function handleSubmit(event) {
  event.preventDefault();
  if (isAnalyzing || isPersisting) return;
  const rawContent = elements.rawContent.value.trim();
  if (!rawContent) return;
  if (agentReviewBlocksSave()) {
    refreshAgentReviewItems();
    setAiStatus("保存前需要先补全复核项，并点击“确认复核完成”。", "warning");
    return;
  }

  const existing = memories.find((item) => item.id === editingId);
  const memory = buildMemoryFromForm(existing);
  setPersistencePending(true);

  try {
    if (databaseNeedsMigration) {
      setStorageStatus("保存前正在先迁移本地备份，避免旧展品遗漏到数据库外。", "loading");
      await migrateCurrentLocalBackup({ quiet: true });
    }

    const saved = await saveMemoryToDatabase(memory, existing?.id || "");
    databaseAvailable = true;
    databaseNeedsMigration = false;
    const nextMemories = existing
      ? memories.map((item) => item.id === existing.id ? saved : item)
      : [saved, ...memories];

    memories = nextMemories;
    persistMemories(nextMemories);
    setStorageStatus(`数据库已保存：${saved.title} 已写入 SQLite。`, "success");
    await syncWorkflowBlueprint({ quiet: true });
    await syncPrivacyPolicy({ quiet: true });
    await syncVersionInfo({ quiet: true });
    resetFormState();
    activeHall = saved.hall;
    searchKeyword = "";
    elements.searchInput.value = "";
    render();
    renderGuide(existing ? `《${saved.title}》已经更新到数据库。` : `新展品《${saved.title}》已经进入${getHallName(saved.hall)}，并保存到 SQLite。`);
  } catch (error) {
    databaseAvailable = false;
    databaseNeedsMigration = false;
    const nextMemories = existing
      ? memories.map((item) => item.id === existing.id ? memory : item)
      : [memory, ...memories];

    if (!saveMemories(nextMemories)) {
      return;
    }
    memories = nextMemories;
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = "local";
    privacyPolicy = null;
    privacyPolicySource = "local";
    versionInfo = null;
    operationsSource = "local";
    setStorageStatus(`数据库保存失败：已改用浏览器本地备份。原因：${error.message}`, "warning");
    resetFormState();
    activeHall = memory.hall;
    searchKeyword = "";
    elements.searchInput.value = "";
    render();
    renderGuide(existing ? `《${memory.title}》已经更新到本地备份。` : `新展品《${memory.title}》已经保存到本地备份。`);
  } finally {
    setPersistencePending(false);
  }
}

function startEdit(id) {
  const memory = memories.find((item) => item.id === id);
  if (!memory) return;

  editingId = id;
  elements.rawContent.value = memory.rawContent;
  updateRawCount();
  elements.titleInput.value = memory.title;
  elements.hallSelect.value = memory.hall;
  elements.memoryDate.value = memory.date;
  elements.locationInput.value = memory.location;
  elements.sourceType.value = memory.sourceType;
  elements.importanceSelect.value = String(memory.importance);
  elements.emotionIntensity.value = String(memory.emotionIntensity);
  elements.favoriteInput.checked = memory.favorite;
  elements.peopleInput.value = (memory.people || []).join("，");
  elements.exhibitText.value = memory.exhibitText;
  elements.coverImageInput.value = memory.coverImage || "";
  elements.mediaNoteInput.value = memory.mediaNote || "";
  elements.attachmentsInput.value = formatAttachmentsInput(memory.attachments);
  elements.tagsInput.value = (memory.tags || []).join("，");
  setSelectedEmotions(memory.emotions || []);
  updateEmotionIntensityOutput();
  render();
  elements.memoryForm.scrollIntoView({ behavior: "smooth", block: "start" });
}

async function openMemory(id) {
  const memory = memories.find((item) => item.id === id);
  if (!memory) return;

  const people = memory.people?.length ? memory.people.join("、") : "未记录";
  const tags = [...(memory.emotions || []), ...(memory.tags || [])].map((tag) => pillHtml(tag)).join("");
  const created = formatDate(memory.createdAt) || "未记录";
  const updated = formatDate(memory.updatedAt);
  const archiveNumber = getArchiveNumber(memory);
  const attachments = normalizeAttachments(memory.attachments);
  const attachmentTypeSummary = formatAttachmentTypeSummary(attachments, 6);

  elements.dialogContent.innerHTML = `
    <div class="dialog-body">
      <p class="eyebrow">${escapeHtml(getHallName(memory.hall))} / ${escapeHtml(archiveNumber)}</p>
      <h3>${escapeHtml(memory.title)}</h3>
      <div class="tag-row">${tags}</div>
      <p><strong>展品说明：</strong>${escapeHtml(memory.exhibitText)}</p>
      <p><strong>原始记忆：</strong>${escapeHtml(memory.rawContent)}</p>
      <p><strong>来源：</strong>${escapeHtml(memory.sourceType)}；<strong>珍藏级别：</strong>${escapeHtml(getImportanceLabel(memory.importance))}；<strong>情绪强度：</strong>${escapeHtml(memory.emotionIntensity)} / 5${memory.favorite ? "；重点展品" : ""}</p>
      <p><strong>封面图线索：</strong>${escapeHtml(memory.coverImage || "未记录")}</p>
      <p><strong>图片/OCR/语音线索：</strong>${escapeHtml(memory.mediaNote || "未记录")}</p>
      <div class="attachment-list">
        <strong>附件清单：</strong>
        ${attachmentTypeSummary ? `<div class="attachment-type-summary">类型分布：${escapeHtml(attachmentTypeSummary)}</div>` : ""}
        ${attachments.length
          ? attachments.map((item) => `<span><b>${escapeHtml(getAttachmentCategory(item))}</b>${escapeHtml(item.name)}${item.type ? ` / ${escapeHtml(item.type)}` : ""}${item.note ? ` / ${escapeHtml(item.note)}` : ""}</span>`).join("")
          : "<span>未记录附件</span>"}
        <small>${attachments.length ? "当前保存的是附件清单和线索，原文件上传将在后续迭代接入。" : "可以在编辑展品时补充图片、截图、语音转写或其他附件线索。"}</small>
      </div>
      <p><strong>时间：</strong>${escapeHtml(memory.date || "未记录")}</p>
      <p><strong>地点：</strong>${escapeHtml(memory.location || "未记录")}</p>
      <p><strong>相关人物：</strong>${escapeHtml(people)}</p>
      <p><strong>入馆日期：</strong>${escapeHtml(created)}${updated ? `，最后编辑：${escapeHtml(updated)}` : ""}</p>
      <div id="dialog-agent-run" class="dialog-agent-run">
        ${memory.agentRunId
          ? `<p><strong>整理历史：</strong>正在读取 Agent run ${escapeHtml(memory.agentRunId.slice(0, 8))}...</p>`
          : "<p><strong>整理历史：</strong>这件展品暂未关联 Agent run。</p>"}
      </div>
    </div>
  `;
  elements.memoryDialog.showModal();
  if (!memory.agentRunId) return;

  try {
    const run = await fetchAgentRunForMemory(memory.id);
    const target = document.getElementById("dialog-agent-run");
    if (target && run) target.innerHTML = renderAgentRunDetail(run);
  } catch (error) {
    const target = document.getElementById("dialog-agent-run");
    if (target) target.innerHTML = `<p><strong>整理历史：</strong>暂时无法读取。${escapeHtml(error.message || "")}</p>`;
  }
}

function renderAgentRunDetail(run) {
  const steps = Array.isArray(run.steps) ? run.steps : [];
  const events = Array.isArray(run.events) ? run.events : [];
  const shortId = run.id?.length > 8 ? run.id.slice(0, 8) : run.id;
  const eventLabels = events.slice(-3).map((event) => event.label).filter(Boolean).join("；");
  const progress = Number(run.summary?.progress) || 0;
  const nextAction = limitText(run.summary?.nextAction || "", 80);
  return `
    <div class="agent-run-detail">
      <p><strong>整理历史：</strong>run ${escapeHtml(shortId || "未记录")}；${escapeHtml(run.mode || "mock")}；${escapeHtml(getAgentStatusLabel(run.status || "done"))}；${escapeHtml(formatAgentRunTime(run.createdAt))}</p>
      <p><strong>运行摘要：</strong>${escapeHtml(progress ? `${progress}%` : "未记录进度")}；${escapeHtml(String(run.eventCount || events.length || 0))} 条事件${nextAction ? `；${escapeHtml(nextAction)}` : ""}</p>
      ${run.rawPreview ? `<p><strong>原始线索：</strong>${escapeHtml(limitText(run.rawPreview, 96))}</p>` : ""}
      <div class="agent-run-detail-grid">
        ${steps.slice(0, 4).map((step) => `
          <div>
            <strong>${escapeHtml(step.agent || "Agent")}：${escapeHtml(getAgentStatusLabel(step.status || "done"))}</strong>
            <small>${escapeHtml((step.evidence || []).map(getEvidenceLabel).join("、") || "无依据字段")}</small>
            <span>${escapeHtml(limitText(step.output || "已完成。", 72))}</span>
          </div>
        `).join("")}
      </div>
      ${eventLabels ? `<p><strong>最近事件：</strong>${escapeHtml(eventLabels)}</p>` : ""}
    </div>
  `;
}

async function deleteMemory(id) {
  const memory = memories.find((item) => item.id === id);
  if (!memory) return;
  if (!confirm(`确定删除《${memory.title}》吗？`)) return;

  setPersistencePending(true);
  try {
    await deleteMemoryFromDatabase(id);
    databaseAvailable = true;
    setStorageStatus(`数据库已删除：《${memory.title}》已从 SQLite 移除。`, "success");
  } catch (error) {
    databaseAvailable = false;
    setStorageStatus(`数据库删除失败：已只删除本地备份。原因：${error.message}`, "warning");
  } finally {
    const nextMemories = memories.filter((item) => item.id !== id);
    if (saveMemories(nextMemories)) memories = nextMemories;
    if (databaseAvailable) await syncWorkflowBlueprint({ quiet: true });
    if (databaseAvailable) await syncPrivacyPolicy({ quiet: true });
    if (databaseAvailable) await syncVersionInfo({ quiet: true });
    else {
      backendWorkflowBlueprint = null;
      workflowBlueprintSource = "local";
      privacyPolicy = null;
      privacyPolicySource = "local";
      versionInfo = null;
      operationsSource = "local";
    }
    if (editingId === id) resetFormState();
    render();
    setPersistencePending(false);
  }
}

function answerGuideQuestion(type) {
  const list = filteredMemories();
  const presetQuestions = {
    tour: "带我参观当前展厅",
    mood: "总结我的记忆情绪",
    recent: "推荐一件最近展品",
    schema: "查看结构化 JSON"
  };

  elements.guideQuestionInput.value = presetQuestions[type] || "";
  updateGuideAskState();

  if (type === "tour") {
    const hall = halls.find((item) => item.id === activeHall) || halls[0];
    const names = list.slice(0, 4).map((memory) => `《${memory.title}》`).join("、") || "暂无展品";
    renderGuide(`${hall.name}导览：\n\n${hall.description}\n\n当前可以先看 ${names}。你也可以在上方直接提问，讲解员会从 SQLite 中检索候选展品再回答。`);
  }

  if (type === "mood") {
    const counts = list.flatMap((memory) => memory.emotions || []).reduce((map, emotion) => {
      map[emotion] = (map[emotion] || 0) + 1;
      return map;
    }, {});
    const summary = Object.entries(counts).sort((a, b) => b[1] - a[1]).map(([emotion, count]) => `${emotion} ${count} 次`).join("，") || "暂无情绪数据";
    const strongest = [...list].sort((a, b) => (b.emotionIntensity || 1) - (a.emotionIntensity || 1))[0];
    const sourceCounts = list.reduce((map, memory) => {
      map[memory.sourceType] = (map[memory.sourceType] || 0) + 1;
      return map;
    }, {});
    const sourceSummary = Object.entries(sourceCounts).sort((a, b) => b[1] - a[1]).map(([source, count]) => `${source} ${count} 件`).join("，") || "暂无来源数据";
    renderGuide(`情绪统计：\n\n${summary}\n\n来源分布：${sourceSummary}\n\n${strongest ? `情绪强度最高的是《${strongest.title}》，强度 ${strongest.emotionIntensity} / 5。` : "暂无强度数据。"}\n\n这些结构化字段现在已经会进入简单 RAG 检索，讲解员会基于召回的候选展品回答。`);
  }

  if (type === "recent") {
    const recent = [...list].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))[0];
    renderGuide(recent ? `今日推荐展品：\n\n《${recent.title}》\n${recent.exhibitText}` : "还没有展品可以推荐。");
  }

  if (type === "schema") {
    const sample = list[0] || memories[0];
    renderGuide(sample
      ? `结构化 JSON 样例：\n\n${JSON.stringify(buildStructuredMemory(sample), null, 2)}`
      : "还没有展品可以生成结构化 JSON。");
  }
}

async function handleGuideAsk(event) {
  event.preventDefault();
  if (isGuideAsking) return;

  const question = elements.guideQuestionInput.value.trim();
  if (!question) {
    renderGuide("先写下你想问讲解员的问题。");
    return;
  }

  setGuidePending(true);
  renderGuide("讲解员正在从数据库里检索相关展品...");
  try {
    const result = await askGuide(question);
    const evidence = formatGuideEvidence(result.memories);
    const citations = formatGuideCitations(result.citations);
    const followUps = formatGuideFollowUps(result.followUps);
    const reason = result.reason ? `\n\n回退原因：${result.reason}` : "";
    const evidenceCount = result.memories.length;
    const queryText = result.query ? `\n检索词：${result.query}` : "";
    const retrievalHint = result.retrievalMode === "recent-fallback" ? "（未命中关键词，已回看最近展品）" : "";
    renderGuide(`${result.answer}\n\n候选展品（${evidenceCount} 件）：${evidence}${citations ? `\n引用依据：${citations}` : ""}${followUps ? `\n可继续追问：\n${followUps}` : ""}${queryText}\n检索模式：${result.mode} / ${result.retrievalMode}${retrievalHint}${reason}`);
  } finally {
    setGuidePending(false);
  }
}

async function exportMemories() {
  let payload = buildCollectionExport();
  if (databaseAvailable && !databaseNeedsMigration) {
    try {
      payload = {
        ...await requestJson(`${API_MEMORIES}/export`),
        ...buildPhase15AssetExportReview()
      };
      setStorageStatus("已从 SQLite 导出数据库备份。", "success");
    } catch (error) {
      setStorageStatus(`数据库导出失败：已导出浏览器本地备份。原因：${error.message}`, "warning");
    }
  } else if (databaseNeedsMigration) {
    setStorageStatus("当前还有本地备份未迁移：已先导出页面中的本地数据。", "warning");
  }

  if (!confirmExportPackageRisk(payload)) return;
  recordAssetAuditEvent({
    action: "full-export",
    label: "导出完整记忆博物馆包",
    detail: `${memories.length} 件展品，${buildAssetPackageItems().length} 个专题资产`
  });
  captureAssetSnapshot("完整导出");
  payload = {
    ...payload,
    phase16Sync: buildPhase16SyncManifest(memories, { exportedAt: payload.exportedAt || new Date().toISOString() }),
    ...buildPhase15AssetExportReview()
  };
  downloadJsonPayload(payload, `memory-museum-${new Date().toISOString().slice(0, 10)}.json`);
}

function downloadJsonPayload(payload, filename) {
  const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = filename;
  link.click();
  URL.revokeObjectURL(url);
}

async function exportRedactedMemories() {
  let payload = buildRedactedCollectionExport();
  if (databaseAvailable && !databaseNeedsMigration) {
    try {
      payload = await requestJson(`${API_MEMORIES}/export?mode=redacted`);
      setStorageStatus("已从 SQLite 导出脱敏同步包。", "success");
    } catch (error) {
      setStorageStatus(`数据库脱敏导出失败：已导出浏览器本地脱敏包。原因：${error.message}`, "warning");
    }
  }
  payload = {
    ...payload,
    phase16Sync: buildPhase16SyncManifest((payload.memories || []).map(normalizeMemory), { exportedAt: payload.exportedAt || new Date().toISOString() }),
    phase17SyncAdapter: buildPhase17SyncAdapter((payload.memories || []).map(normalizeMemory), { exportedAt: payload.exportedAt || new Date().toISOString() })
  };
  downloadJsonPayload(payload, `memory-museum-redacted-${new Date().toISOString().slice(0, 10)}.json`);
}

function confirmExportPackageRisk(payload) {
  const sensitivity = payload.privacyPolicy?.sensitiveData || payload.phase12Sovereignty?.sensitivity;
  const assetQuality = payload.phase15AssetQuality || buildAssetQualitySummary(getAssetCollection());
  const repairSuggestions = payload.phase15RepairSuggestions || buildAssetRepairSuggestions(getAssetCollection());
  const warnings = [];
  if (sensitivity && sensitivity.riskLevel !== "none") {
    warnings.push(`导出包敏感线索风险：${sensitivity.riskLevel}`);
    warnings.push(sensitivity.recommendation || "请确认导出包保存位置可信。");
  }
  if (assetQuality.totalIssues) {
    warnings.push(`专题资产还有 ${assetQuality.totalIssues} 个质量缺口。`);
    warnings.push(repairSuggestions.slice(0, 3).map((item) => `${item.type}《${item.title}》：${item.detail}`).join("\n"));
  }
  if (!warnings.length) return true;
  warnings.push("当前导出包未加密，会包含展品文本、结构化字段、洞察、工作流蓝图和专题资产信息。");
  return confirm(warnings.filter(Boolean).join("\n\n"));
}

function getMemorySyncChecksum(memory) {
  const structured = buildStructuredMemory(normalizeMemory(memory));
  return simpleChecksum(stableStringify({
    title: structured.title,
    hall: structured.hall?.id || structured.hall,
    rawContent: structured.rawContent,
    exhibitText: structured.exhibitText,
    date: structured.date,
    location: structured.location,
    people: structured.people,
    emotions: structured.emotions,
    tags: structured.tags,
    importance: structured.importance,
    attachments: structured.attachments
  }));
}

function compareMemoryVersion(imported, existing) {
  const importedTime = new Date(imported.updatedAt || imported.createdAt || 0).getTime() || 0;
  const existingTime = new Date(existing.updatedAt || existing.createdAt || 0).getTime() || 0;
  if (importedTime > existingTime) return "update";
  if (importedTime < existingTime) return "skip";
  return "conflict";
}

function getPhase16DefaultDecision(action) {
  if (action === "conflict") return "copy";
  if (action === "update") return "update";
  if (action === "create") return "create";
  return "skip";
}

function getPhase16Decision(entry) {
  return entry.decision || getPhase16DefaultDecision(entry.action);
}

function getPhase16DecisionLabel(decision) {
  const labels = {
    create: "新增写入",
    update: "导入覆盖",
    copy: "复制为新展品",
    "keep-local": "保留本地",
    skip: "跳过"
  };
  return labels[decision] || decision;
}

function summarizePhase16ImportPlan(plan) {
  const summary = { create: 0, update: 0, copy: 0, skip: 0, keepLocal: 0, conflict: 0, write: 0 };
  (plan?.entries || []).forEach((entry) => {
    const decision = getPhase16Decision(entry);
    if (entry.action === "conflict") summary.conflict += 1;
    if (decision === "create") summary.create += 1;
    if (decision === "update") summary.update += 1;
    if (decision === "copy") summary.copy += 1;
    if (decision === "keep-local") summary.keepLocal += 1;
    if (decision === "skip") summary.skip += 1;
    if (["create", "update", "copy"].includes(decision)) summary.write += 1;
  });
  return summary;
}

function buildPhase16DiffLabels(entry) {
  if (!entry.existing) return ["本地无同 ID 展品"];
  const imported = entry.memory;
  const existing = entry.existing;
  const checks = [
    ["标题", imported.title, existing.title],
    ["展厅", getHallName(imported.hall), getHallName(existing.hall)],
    ["日期", imported.date, existing.date],
    ["情绪", (imported.emotions || []).join(" / "), (existing.emotions || []).join(" / ")],
    ["标签", (imported.tags || []).join(" / "), (existing.tags || []).join(" / ")],
    ["珍藏级别", getImportanceLabel(imported.importance), getImportanceLabel(existing.importance)],
    ["附件", String((imported.attachments || []).length), String((existing.attachments || []).length)]
  ];
  return checks
    .filter(([, importedValue, existingValue]) => String(importedValue || "") !== String(existingValue || ""))
    .slice(0, 4)
    .map(([label, importedValue, existingValue]) => `${label}：本地 ${existingValue || "空"} → 导入 ${importedValue || "空"}`);
}

function renderPhase16DecisionControl(entry, index) {
  const decision = getPhase16Decision(entry);
  if (entry.action === "conflict") {
    return `
      <select data-phase16-decision="${index}" aria-label="冲突处理方式">
        <option value="copy" ${decision === "copy" ? "selected" : ""}>复制为新展品</option>
        <option value="keep-local" ${decision === "keep-local" ? "selected" : ""}>保留本地</option>
        <option value="update" ${decision === "update" ? "selected" : ""}>使用导入覆盖</option>
      </select>
    `;
  }
  if (entry.action === "update") {
    return `
      <select data-phase16-decision="${index}" aria-label="更新处理方式">
        <option value="update" ${decision === "update" ? "selected" : ""}>使用导入覆盖</option>
        <option value="keep-local" ${decision === "keep-local" ? "selected" : ""}>保留本地</option>
        <option value="copy" ${decision === "copy" ? "selected" : ""}>复制为新展品</option>
      </select>
    `;
  }
  if (entry.action === "create") {
    return `
      <select data-phase16-decision="${index}" aria-label="新增处理方式">
        <option value="create" ${decision === "create" ? "selected" : ""}>新增写入</option>
        <option value="skip" ${decision === "skip" ? "selected" : ""}>跳过</option>
      </select>
    `;
  }
  return `<span class="phase16-decision-static">${escapeHtml(getPhase16DecisionLabel(decision))}</span>`;
}

function renderPhase16SyncAudit() {
  const allAudit = loadPhase16SyncAudit();
  const audit = filterPhase16SyncAudit(allAudit).slice(0, 5);
  const filters = [
    ["all", "全部"],
    ["export", "导出"],
    ["preview", "预览"],
    ["drill", "演练"],
    ["apply", "导入"],
    ["cancel", "取消"]
  ];
  const filterBar = `
    <div class="phase16-audit-filters">
      ${filters.map(([id, label]) => `<button type="button" data-phase16-audit-filter="${id}" data-active="${phase16AuditFilter === id}">${label}</button>`).join("")}
    </div>
  `;
  if (!allAudit.length) return `${filterBar}<small>还没有同步记录。导出、预览、演练报告、确认导入都会写入这里。</small>`;
  if (!audit.length) return `${filterBar}<small>当前筛选下没有同步记录。</small>`;
  return `${filterBar}${audit.map((entry) => `
    <div class="phase16-audit-row">
      <span>${escapeHtml(entry.label)}</span>
      <small>${escapeHtml(formatDate(entry.at))} / ${escapeHtml(entry.batchId || localProfile.deviceLabel)}</small>
      <small>${escapeHtml(entry.detail || "")}</small>
    </div>
  `).join("")}`;
}

function renderPhase17SyncAdapterPanel() {
  const adapter = buildPhase17SyncAdapter(memories);
  return `
    <div class="phase17-adapter-panel">
      <div class="phase17-adapter-heading">
        <div>
          <span>Phase 17 Adapter</span>
          <strong>真实多设备同步适配层</strong>
        </div>
        <small>${escapeHtml(adapter.deviceRegistry.deviceCount)} 台设备 / ${escapeHtml(adapter.queue.pending)} 个待处理任务</small>
      </div>
      <div class="phase17-device-strip">
        ${adapter.deviceRegistry.devices.map((device) => `
          <span data-device-status="${escapeHtml(device.status || "trusted")}">
            <b>${escapeHtml(device.label || "未命名设备")}</b>
            <small>${escapeHtml(device.trustLevel || device.status || "trusted")} / ${escapeHtml(device.role || "device")} / ${escapeHtml(device.syncMode || "manual-json")}</small>
            ${device.id !== localProfile.deviceId ? `
              <em>${escapeHtml(device.note || "需要人工确认设备来源。")}</em>
              <div class="phase17-device-actions">
                <button type="button" data-phase17-device-trust="trusted" data-phase17-device-id="${escapeHtml(device.id)}">信任</button>
                <button type="button" data-phase17-device-trust="review" data-phase17-device-id="${escapeHtml(device.id)}">复核</button>
                <button type="button" data-phase17-device-trust="blocked" data-phase17-device-id="${escapeHtml(device.id)}">阻止</button>
              </div>
            ` : ""}
          </span>
        `).join("")}
      </div>
      <div class="phase17-trust-policy">
        <div>
          <strong>设备信任策略</strong>
          <small>可信 ${escapeHtml(adapter.deviceTrustPolicy.trusted)} / 待复核 ${escapeHtml(adapter.deviceTrustPolicy.review)} / 已阻止 ${escapeHtml(adapter.deviceTrustPolicy.blocked)}</small>
          <button type="button" data-phase17-add-review-device>登记待复核设备</button>
        </div>
        ${adapter.deviceTrustPolicy.rules.map((rule) => `<small>${escapeHtml(rule)}</small>`).join("")}
      </div>
      <div class="phase17-adapter-grid">
        ${adapter.adapters.map((item) => `
          <div data-adapter-status="${escapeHtml(item.status)}">
            <strong>${escapeHtml(item.label)}</strong>
            <small>${escapeHtml(item.detail)}</small>
          </div>
        `).join("")}
      </div>
      <div class="phase17-lan-panel" data-lan-status="${escapeHtml(adapter.lanHandshake.status)}">
        <div>
          <strong>局域网只读握手模拟</strong>
          <small>${escapeHtml(adapter.lanHandshake.mode)} / 指纹 ${escapeHtml(adapter.lanHandshake.localFingerprint)}</small>
        </div>
        <div class="phase17-check-row">
          ${adapter.lanHandshake.checks.map((item) => `
            <span data-check-status="${escapeHtml(item.status)}">
              <b>${escapeHtml(item.label)}</b>
              <small>${escapeHtml(item.detail)}</small>
            </span>
          `).join("")}
        </div>
        <small>${adapter.lanHandshake.peerCandidates.length ? `发现 ${escapeHtml(adapter.lanHandshake.peerCandidates.length)} 个历史设备候选，仍需人工确认。` : "暂无其他历史设备候选；当前只保留本机握手指纹。"}</small>
      </div>
      <div class="phase17-cloud-panel" data-cloud-status="${escapeHtml(adapter.privateCloudBoundary.status)}">
        <div>
          <strong>私有云适配边界</strong>
          <small>${escapeHtml(adapter.privateCloudBoundary.provider)} / ${escapeHtml(adapter.privateCloudBoundary.policy)}</small>
          <button type="button" data-phase17-cloud-draft>生成配置草案</button>
          <button type="button" data-phase17-cloud-clear>清除草案</button>
        </div>
        <div class="phase17-check-row">
          ${adapter.privateCloudBoundary.checks.map((item) => `
            <span data-check-status="${escapeHtml(item.status)}">
              <b>${escapeHtml(item.label)}</b>
              <small>${escapeHtml(item.detail)}</small>
            </span>
          `).join("")}
        </div>
        <small>当前状态：${escapeHtml(adapter.privateCloudBoundary.status)}；不会连接 WebDAV、S3 或自托管 API。</small>
      </div>
      <div class="phase17-health-panel" data-health-status="${escapeHtml(adapter.syncHealth.status)}">
        <div>
          <strong>同步健康度</strong>
          <small>${escapeHtml(adapter.syncHealth.score)} 分 / ${escapeHtml(adapter.syncHealth.recommendation)}</small>
        </div>
        <div class="phase17-health-grid">
          ${adapter.syncHealth.checks.map((item) => `
            <span data-health-check="${escapeHtml(item.status)}">
              <b>${escapeHtml(item.label)}</b>
              <small>${escapeHtml(item.detail)}</small>
            </span>
          `).join("")}
        </div>
        <div class="phase17-maintenance-panel">
          <strong>健康度解释</strong>
          <small>${escapeHtml(adapter.healthExplanation.summary)}</small>
          ${adapter.healthExplanation.reasons.length ? adapter.healthExplanation.reasons.map((item) => `
            <span><b>${escapeHtml(item.label)}</b><small>${escapeHtml(item.impact)}</small></span>
          `).join("") : "<small>暂无扣分项。</small>"}
        </div>
      </div>
      <div class="phase17-queue-panel">
        <div>
          <strong>同步队列</strong>
          <small>待处理 ${escapeHtml(adapter.queue.pending)} / 失败 ${escapeHtml(adapter.queue.failures)} / 已完成 ${escapeHtml(adapter.queue.resolved)}</small>
          <button type="button" data-phase17-queue-sync>加入一次手动同步任务</button>
        </div>
        <div class="phase17-recovery-panel">
          <strong>失败恢复建议</strong>
          <small>${escapeHtml(adapter.failureRecovery.recommendation)}</small>
          ${adapter.failureRecovery.actions.length ? adapter.failureRecovery.actions.map((item) => `
            <span><b>${escapeHtml(item.label)}</b><small>${escapeHtml(item.action)}</small></span>
          `).join("") : "<small>暂无失败任务需要恢复。</small>"}
        </div>
        ${adapter.queue.recent.length ? adapter.queue.recent.map((item) => `
          <div class="phase17-queue-row" data-queue-status="${escapeHtml(item.status)}">
            <span>${escapeHtml(item.label)}</span>
            <small>${escapeHtml(getPhase17QueueStatusLabel(item.status))} / ${escapeHtml(item.channel)} / ${escapeHtml(formatDate(item.updatedAt || item.createdAt))}</small>
            <small>${escapeHtml(item.detail || "")}</small>
            ${getPhase17QueueNextActions(item.status).length ? `
              <div class="phase17-queue-actions">
                ${getPhase17QueueNextActions(item.status).map((action) => `
                  <button type="button" data-phase17-queue-status="${escapeHtml(action.status)}" data-phase17-queue-id="${escapeHtml(item.id)}">${escapeHtml(action.label)}</button>
                `).join("")}
              </div>
            ` : ""}
          </div>
        `).join("") : "<small>当前还没有待处理同步任务。</small>"}
      </div>
      <div class="phase17-phase18-advisory" data-advisory-status="${escapeHtml(adapter.phase18SyncAdvisory.readiness)}">
        <strong>第十八阶段资产同步提示</strong>
        <small>专题展 ${escapeHtml(adapter.phase18SyncAdvisory.exhibitions)} / 报告 ${escapeHtml(adapter.phase18SyncAdvisory.reports)} / 活跃长期任务 ${escapeHtml(adapter.phase18SyncAdvisory.activePhase18Tasks)}</small>
        <small>可入包 ${escapeHtml(adapter.phase18SyncAdvisory.packageCandidates || 0)} / 已发布 ${escapeHtml(adapter.phase18SyncAdvisory.readyAssets || 0)} / 风险资产 ${escapeHtml(adapter.phase18SyncAdvisory.riskAssets || 0)}</small>
        <small>${escapeHtml(adapter.phase18SyncAdvisory.recommendation)}</small>
      </div>
      <small>第十七阶段第六版仍坚持本地优先：不自动上传、不自动发现设备，也不绕过第十六阶段风险确认。</small>
    </div>
  `;
}

function countPhase16PayloadAssets(payload = {}) {
  return {
    savedExhibitions: Array.isArray(payload.savedExhibitions) ? payload.savedExhibitions.length : 0,
    reportDrafts: Array.isArray(payload.reportDrafts) ? payload.reportDrafts.length : 0,
    auditRecords: Array.isArray(payload.phase15AssetAuditLog) ? payload.phase15AssetAuditLog.length : 0,
    snapshots: Array.isArray(payload.phase15AssetSnapshots) ? payload.phase15AssetSnapshots.length : 0
  };
}

function buildPhase16AssetBoundary(payload = {}) {
  const counts = countPhase16PayloadAssets(payload);
  const totalAssets = counts.savedExhibitions + counts.reportDrafts;
  return {
    counts,
    totalAssets,
    status: totalAssets > 0 ? "review" : "clear",
    policy: "memory-first-assets-preview",
    note: totalAssets > 0
      ? "同步包包含专题展或报告草稿。本版先同步展品，专题资产只进入边界提示和发布元数据恢复，不会静默合并到当前资产库。"
      : "同步包未发现专题展或报告草稿，可按展品同步流程继续。"
  };
}

function buildPhase16LegacyCompatibility(payload = {}) {
  const sync = payload.phase16Sync || null;
  if (sync?.phase >= 16) {
    return {
      mode: "phase16",
      status: "ready",
      label: "第十六阶段同步包",
      note: "同步包包含 phase16Sync，可使用逐项决策、质量检查和恢复演练。"
    };
  }
  const phase = Number(payload.phase || payload.phase12Sovereignty?.phase || payload.privacyPolicy?.phase || 0);
  return {
    mode: phase >= 15 ? "phase15-legacy" : phase >= 12 ? "phase12-legacy" : "legacy-json",
    status: "review",
    label: phase ? `旧版阶段 ${phase} 备份包` : "旧版 JSON 备份包",
    note: "未发现 phase16Sync。本版会按旧备份兼容模式导入展品，并建议先导出演练报告留档。"
  };
}

function buildPhase16AssetMergePreview(payload = {}) {
  const exhibitions = Array.isArray(payload.savedExhibitions) ? payload.savedExhibitions : [];
  const reports = Array.isArray(payload.reportDrafts) ? payload.reportDrafts : [];
  const current = getAssetCollection();
  const localExhibitionIds = new Set((current.savedExhibitions || []).map((item) => item.id));
  const localReportIds = new Set((current.reportDrafts || []).map((item) => item.id));
  const entries = [
    ...exhibitions.map((asset, index) => ({
      id: asset.id || `imported-exhibition-${index}`,
      type: "exhibition",
      title: asset.title || "未命名专题展",
      status: localExhibitionIds.has(asset.id) ? "conflict" : "new",
      decision: "skip",
      reason: localExhibitionIds.has(asset.id) ? "本地已有同 ID 专题展" : "本地没有同 ID 专题展"
    })),
    ...reports.map((asset, index) => ({
      id: asset.id || `imported-report-${index}`,
      type: "report",
      title: asset.title || "未命名报告草稿",
      status: localReportIds.has(asset.id) ? "conflict" : "new",
      decision: "skip",
      reason: localReportIds.has(asset.id) ? "本地已有同 ID 报告草稿" : "本地没有同 ID 报告草稿"
    }))
  ];
  return {
    entries,
    summary: {
      total: entries.length,
      exhibitions: exhibitions.length,
      reports: reports.length,
      conflicts: entries.filter((entry) => entry.status === "conflict").length
    },
    policy: "preview-only-no-silent-merge"
  };
}

function buildPhase16SyncPackageQuality(payload = {}, normalized = []) {
  const warnings = [];
  const blockers = [];
  const sync = payload.phase16Sync || {};
  const legacy = buildPhase16LegacyCompatibility(payload);
  if (!sync.phase) warnings.push("未发现 phase16Sync 元数据，已进入旧备份兼容模式。");
  if (sync.phase && Number(sync.phase) < 16) warnings.push(`同步包阶段较旧：${sync.phase}`);
  if (sync.mode && sync.mode !== "manual-json-local-first") warnings.push(`同步模式不是当前默认模式：${sync.mode}`);
  if (!payload.app) warnings.push("同步包缺少 app 来源字段。");
  if (!normalized.length) blockers.push("同步包没有可导入展品。");
  const ids = normalized.map((memory) => memory.id);
  const duplicateIds = ids.filter((id, index) => ids.indexOf(id) !== index);
  const missingTime = normalized.filter((memory) => !memory.updatedAt && !memory.createdAt).length;
  const missingContent = normalized.filter((memory) => !memory.rawContent && !memory.exhibitText).length;
  if (duplicateIds.length) warnings.push(`同步包内有 ${new Set(duplicateIds).size} 个重复 ID，会按新展品处理重复项。`);
  if (missingTime) warnings.push(`${missingTime} 件展品缺少更新时间，冲突判断会更保守。`);
  if (missingContent) warnings.push(`${missingContent} 件展品缺少正文或展品说明。`);
  const assetBoundary = buildPhase16AssetBoundary(payload);
  if (assetBoundary.totalAssets) warnings.push(`同步包包含 ${assetBoundary.totalAssets} 个专题资产，本版不会静默合并这些资产。`);
  const score = Math.max(0, 100 - warnings.length * 12 - blockers.length * 35);
  return {
    status: blockers.length ? "blocked" : warnings.length ? "review" : "ready",
    score,
    warnings,
    blockers,
    checks: [
      { label: "phase16Sync 元数据", status: sync.phase ? "ready" : "review" },
      { label: "展品数组", status: normalized.length ? "ready" : "blocked" },
      { label: "重复 ID", status: duplicateIds.length ? "review" : "ready" },
      { label: "时间戳", status: missingTime ? "review" : "ready" },
      { label: "旧包兼容", status: legacy.mode === "phase16" ? "ready" : "review" },
      { label: "专题资产边界", status: assetBoundary.totalAssets ? "review" : "ready" }
    ],
    assetBoundary,
    legacy
  };
}

function renderPhase16QualityPanel(plan) {
  const quality = plan?.quality;
  if (!quality) return "";
  const notes = [...(quality.blockers || []), ...(quality.warnings || [])].slice(0, 4);
  return `
    <div class="phase16-quality-panel" data-quality-status="${escapeHtml(quality.status)}">
      <div>
        <strong>同步包质量检查</strong>
        <span>${escapeHtml(quality.status)} / ${escapeHtml(quality.score)} 分</span>
      </div>
      <div class="phase16-quality-grid">
        ${(quality.checks || []).map((item) => `<small data-quality-status="${escapeHtml(item.status)}">${escapeHtml(item.label)}</small>`).join("")}
      </div>
      ${notes.length ? `<p>${notes.map(escapeHtml).join("；")}</p>` : "<p>同步包结构完整，可以继续做恢复演练或确认导入。</p>"}
    </div>
  `;
}

function renderPhase16AssetBoundary(boundary) {
  if (!boundary) return "";
  return `
    <div class="phase16-boundary-panel" data-boundary-status="${escapeHtml(boundary.status)}">
      <strong>专题资产同步边界</strong>
      <div>
        <span>专题展 ${escapeHtml(boundary.counts.savedExhibitions)}</span>
        <span>报告草稿 ${escapeHtml(boundary.counts.reportDrafts)}</span>
        <span>审计 ${escapeHtml(boundary.counts.auditRecords)}</span>
        <span>快照 ${escapeHtml(boundary.counts.snapshots)}</span>
      </div>
      <small>${escapeHtml(boundary.note)}</small>
    </div>
  `;
}

function renderPhase16LegacyCompatibility(legacy) {
  if (!legacy) return "";
  return `
    <div class="phase16-legacy-panel" data-legacy-status="${escapeHtml(legacy.status)}">
      <strong>${escapeHtml(legacy.label)}</strong>
      <small>${escapeHtml(legacy.note)}</small>
    </div>
  `;
}

function getPhase16AssetDecisionLabel(decision) {
  const labels = {
    skip: "暂不合并",
    import: "标记为待合并",
    replace: "标记为替换本地"
  };
  return labels[decision] || decision;
}

function renderPhase16AssetDecisionControl(entry, index) {
  return `
    <select data-phase16-asset-decision="${index}" aria-label="专题资产合并方式">
      <option value="skip" ${entry.decision === "skip" ? "selected" : ""}>暂不合并</option>
      <option value="import" ${entry.decision === "import" ? "selected" : ""}>标记为待合并</option>
      <option value="replace" ${entry.decision === "replace" ? "selected" : ""}>标记为替换本地</option>
    </select>
  `;
}

function renderPhase16AssetMergePreview(preview) {
  if (!preview?.entries?.length) return "";
  return `
    <div class="phase16-asset-merge-panel">
      <div>
        <strong>专题资产逐项合并预览</strong>
        <small>${preview.summary.exhibitions} 个专题展 / ${preview.summary.reports} 个报告草稿 / ${preview.summary.conflicts} 个同 ID 冲突</small>
      </div>
      <small>第五版先保存合并决策和演练报告，不会静默写入专题资产库。</small>
      ${preview.entries.slice(0, 8).map((entry, index) => `
        <div class="phase16-asset-merge-row" data-asset-sync-status="${escapeHtml(entry.status)}">
          <span>${escapeHtml(entry.type === "report" ? "报告" : "专题展")}</span>
          <b>${escapeHtml(entry.title)}</b>
          <small>${escapeHtml(entry.reason)} / 当前：${escapeHtml(getPhase16AssetDecisionLabel(entry.decision))}</small>
          ${renderPhase16AssetDecisionControl(entry, index)}
        </div>
      `).join("")}
      ${preview.entries.length > 8 ? `<small>还有 ${preview.entries.length - 8} 个专题资产未展开。</small>` : ""}
    </div>
  `;
}

function renderPhase16RecoveryDrill(plan, summary) {
  if (!plan) return "";
  const quality = plan.quality || {};
  return `
    <div class="phase16-drill-panel">
      <strong>恢复演练报告</strong>
      <small>本次预演不会写入数据：预计写入 ${summary.write || 0} 件，覆盖 ${summary.update || 0} 件，复制 ${summary.copy || 0} 件，保留或跳过 ${(summary.skip || 0) + (summary.keepLocal || 0)} 件；质量状态 ${escapeHtml(quality.status || "unknown")}。</small>
      <button type="button" data-phase16-export-drill-report>导出演练报告</button>
    </div>
  `;
}

function exportPhase16RecoveryReport() {
  if (!pendingSyncImportPlan) return;
  const summary = summarizePhase16ImportPlan(pendingSyncImportPlan);
  const payload = {
    app: "AI 记忆博物馆",
    phase: 16,
    exportMode: "phase16-recovery-drill-report",
    generatedAt: new Date().toISOString(),
    source: pendingSyncImportPlan.source,
    quality: pendingSyncImportPlan.quality,
    assetBoundary: pendingSyncImportPlan.assetBoundary,
    legacyCompatibility: pendingSyncImportPlan.legacyCompatibility,
    assetMergePreview: pendingSyncImportPlan.assetMergePreview,
    summary,
    entries: pendingSyncImportPlan.entries.map((entry) => ({
      id: entry.memory.id,
      title: entry.memory.title,
      action: entry.action,
      decision: getPhase16Decision(entry),
      reason: entry.reason,
      diffs: buildPhase16DiffLabels(entry)
    }))
  };
  downloadJsonPayload(payload, `memory-museum-recovery-drill-${new Date().toISOString().slice(0, 10)}.json`);
  recordPhase16SyncAuditEvent({
    action: "drill-export",
    label: "导出恢复演练报告",
    detail: `预演写入 ${summary.write} 件；质量 ${pendingSyncImportPlan.quality?.status || "unknown"}`,
    batchId: pendingSyncImportPlan.source.batchId,
    summary
  });
  enqueuePhase17SyncTask({
    action: "recovery-drill",
    label: "恢复演练报告已生成",
    detail: `${pendingSyncImportPlan.source.batchId || "未知批次"} 进入跨设备同步前复核队列。`
  });
  renderPhase16SyncPanel();
}

function buildPhase16ImportPlan(payload = {}) {
  const imported = Array.isArray(payload) ? payload : payload.memories;
  if (!Array.isArray(imported)) throw new Error("导入文件里没有 memories 数组。");
  const normalized = imported.map(normalizeMemory);
  const quality = buildPhase16SyncPackageQuality(payload, normalized);
  const assetBoundary = quality.assetBoundary;
  const legacyCompatibility = quality.legacy;
  const assetMergePreview = buildPhase16AssetMergePreview(payload);
  const currentById = new Map(memories.map((memory) => [memory.id, memory]));
  const seenIncoming = new Set();
  const entries = normalized.map((memory, index) => {
    const existing = currentById.get(memory.id);
    const incomingDuplicate = seenIncoming.has(memory.id);
    seenIncoming.add(memory.id);
    const baseEntry = { id: `sync-entry-${index}-${memory.id}`, memory, existing, duplicateId: incomingDuplicate };
    if (!existing || incomingDuplicate) {
      return { ...baseEntry, action: "create", decision: "create", reason: incomingDuplicate ? "导入包内 ID 重复，作为新展品写入" : "本地没有同 ID 展品" };
    }
    const same = getMemorySyncChecksum(memory) === getMemorySyncChecksum(existing);
    if (same) return { ...baseEntry, action: "skip", decision: "skip", reason: "内容一致" };
    const versionAction = compareMemoryVersion(memory, existing);
    if (versionAction === "update") return { ...baseEntry, action: "update", decision: "update", reason: "导入版本较新" };
    if (versionAction === "skip") return { ...baseEntry, action: "skip", decision: "skip", reason: "本地版本较新" };
    return { ...baseEntry, action: "conflict", decision: "copy", reason: "同 ID 内容不同且时间相同" };
  });
  const summary = entries.reduce((result, entry) => {
    result[entry.action] = (result[entry.action] || 0) + 1;
    return result;
  }, { create: 0, update: 0, conflict: 0, skip: 0 });
  return {
    id: `phase16-import-${Date.now()}`,
    createdAt: new Date().toISOString(),
    source: {
      app: payload.app || "未知来源",
      batchId: payload.phase16Sync?.batchId || "",
      deviceLabel: payload.phase16Sync?.device?.label || "",
      exportedAt: payload.exportedAt || payload.phase16Sync?.exportedAt || ""
    },
    payload,
    entries,
    summary,
    decisionSummary: summarizePhase16ImportPlan({ entries }),
    quality,
    assetBoundary,
    legacyCompatibility,
    assetMergePreview,
    total: entries.length
  };
}

function renderPhase16SyncPanel() {
  if (!elements.phase16SyncPanel) return;
  const plan = pendingSyncImportPlan;
  const summary = plan ? summarizePhase16ImportPlan(plan) : { create: 0, update: 0, copy: 0, skip: 0, keepLocal: 0, conflict: 0, write: 0 };
  const sourceSummary = plan?.summary || { create: 0, update: 0, conflict: 0, skip: 0 };
  elements.phase16SyncPanel.innerHTML = `
    <div class="phase16-sync-heading">
      <div>
        <span>Phase 16 Sync</span>
        <strong>手动同步包、逐项决策与同步审计</strong>
      </div>
      <small>${escapeHtml(localProfile.deviceLabel)} / ${escapeHtml(localProfile.deviceId.slice(0, 12))}</small>
    </div>
    <div class="phase16-sync-actions">
      <button type="button" data-phase16-export-sync>导出同步包</button>
      <span>导入 JSON 文件会先生成预览；冲突和更新都可以逐项选择，不会静默覆盖本地数据。</span>
    </div>
    <div class="phase16-sync-grid">
      <span><b>${memories.length}</b><small>本地展品</small></span>
      <span><b>${summary.create || 0}</b><small>新增写入</small></span>
      <span><b>${summary.update || 0}</b><small>导入覆盖</small></span>
      <span><b>${summary.copy || 0}</b><small>复制新展品</small></span>
      <span><b>${(summary.skip || 0) + (summary.keepLocal || 0)}</b><small>不写入</small></span>
    </div>
    ${plan ? `
      <div class="phase16-import-preview">
        <strong>${escapeHtml(plan.source.batchId || "导入预览")}</strong>
        <small>${escapeHtml(plan.source.deviceLabel || "未知设备")} / ${escapeHtml(formatDate(plan.source.exportedAt) || "未知时间")} / 原始判断：新增 ${sourceSummary.create || 0}，更新 ${sourceSummary.update || 0}，冲突 ${sourceSummary.conflict || 0}，跳过 ${sourceSummary.skip || 0}</small>
        ${renderPhase16QualityPanel(plan)}
        ${renderPhase16LegacyCompatibility(plan.legacyCompatibility)}
        ${renderPhase16AssetBoundary(plan.assetBoundary)}
        ${renderPhase16RecoveryDrill(plan, summary)}
        ${renderPhase16AssetMergePreview(plan.assetMergePreview)}
        <div class="phase16-batch-actions">
          <button type="button" data-phase16-batch-decision="copy">冲突全部复制</button>
          <button type="button" data-phase16-batch-decision="keep-local">冲突全部保留本地</button>
          <button type="button" data-phase16-batch-decision="update">冲突全部导入覆盖</button>
        </div>
        ${plan.entries.slice(0, 10).map((entry, index) => {
          const diffs = buildPhase16DiffLabels(entry);
          return `
          <div class="phase16-import-row" data-sync-action="${escapeHtml(entry.action)}">
            <span>${escapeHtml(entry.action)}</span>
            <b>${escapeHtml(entry.memory.title)}</b>
            <div class="phase16-import-detail">
              <small>${escapeHtml(entry.reason)} / 当前：${escapeHtml(getPhase16DecisionLabel(getPhase16Decision(entry)))}</small>
              ${diffs.length ? `<small>${diffs.map(escapeHtml).join("；")}</small>` : ""}
            </div>
            ${renderPhase16DecisionControl(entry, index)}
          </div>
        `; }).join("")}
        ${plan.entries.length > 10 ? `<small>还有 ${plan.entries.length - 10} 条预览未展开；确认导入时仍会按默认或已选择策略处理。</small>` : ""}
        <div class="phase16-sync-actions">
          <button type="button" data-phase16-apply-import>按当前决策导入 ${summary.write || 0} 件</button>
          <button type="button" data-phase16-cancel-import>取消预览</button>
        </div>
      </div>
    ` : "<small>选择上方“导入”文件后，这里会显示同步预览和冲突处理方式。</small>"}
    <div class="phase16-audit-panel">
      <strong>最近同步记录</strong>
      ${renderPhase16SyncAudit()}
    </div>
    ${renderPhase17SyncAdapterPanel()}
  `;
}

function exportPhase16SyncPackage() {
  const exportedAt = new Date().toISOString();
  const payload = {
    ...buildCollectionExport(),
    exportMode: "phase16-sync-package",
    exportedAt,
    phase16Sync: buildPhase16SyncManifest(memories, { exportedAt })
  };
  downloadJsonPayload(payload, `memory-museum-sync-${new Date().toISOString().slice(0, 10)}.json`);
  recordPhase16SyncAuditEvent({
    action: "export",
    label: "导出同步包",
    detail: `${memories.length} 件展品，${buildAssetPackageItems().length} 个专题资产`,
    batchId: payload.phase16Sync.batchId,
    summary: { itemCount: memories.length, assetCount: buildAssetPackageItems().length }
  });
  enqueuePhase17SyncTask({
    action: "export-sync-package",
    label: "手动同步包待分发",
    detail: `${payload.phase16Sync.batchId} 已生成，可交给其他设备导入。`
  });
  setStorageStatus("已导出第十六阶段手动同步包。", "success");
  renderPhase16SyncPanel();
}

function buildPhase16EntriesToWrite(plan) {
  return (plan?.entries || [])
    .map((entry) => {
      const decision = getPhase16Decision(entry);
      if (["skip", "keep-local"].includes(decision)) return null;
      if (decision === "update") return { ...entry, writeMode: "update", memoryToSave: entry.memory, existingId: entry.existing?.id || entry.memory.id };
      if (decision === "copy") return { ...entry, writeMode: "copy", memoryToSave: { ...entry.memory, id: createId() }, existingId: "" };
      if (decision === "create") {
        const shouldCreateNewId = entry.duplicateId || memories.some((memory) => memory.id === entry.memory.id);
        return { ...entry, writeMode: "create", memoryToSave: shouldCreateNewId ? { ...entry.memory, id: createId() } : entry.memory, existingId: "" };
      }
      return null;
    })
    .filter(Boolean);
}

function confirmPhase16ImportRisk(plan, summary) {
  const quality = plan?.quality || {};
  if (quality.status === "blocked") {
    const blockers = (quality.blockers || []).join("；") || "同步包结构不完整";
    setStorageStatus(`同步包被阻止导入：${blockers}`, "warning");
    renderGuide(`第十六阶段同步已阻止导入。原因：${blockers}。`);
    recordPhase16SyncAuditEvent({
      action: "blocked-import",
      label: "阻止高风险同步导入",
      detail: blockers,
      batchId: plan?.source?.batchId,
      summary
    });
    renderPhase16SyncPanel();
    return false;
  }
  if (quality.status === "review") {
    const warnings = (quality.warnings || []).slice(0, 4).join("\n");
    return confirm(`这个同步包需要复核后再导入：\n\n${warnings || "存在需要人工确认的同步风险。"}\n\n预计写入 ${summary.write} 件，其中覆盖 ${summary.update} 件、复制 ${summary.copy} 件。确认继续吗？`);
  }
  return true;
}

async function applyPhase16ImportPlan() {
  const plan = pendingSyncImportPlan;
  if (!plan) return;
  const summary = summarizePhase16ImportPlan(plan);
  const entriesToWrite = buildPhase16EntriesToWrite(plan);
  if (!entriesToWrite.length) {
    setStorageStatus("同步预览没有需要写入的展品。", "warning");
    recordPhase16SyncAuditEvent({
      action: "apply-empty",
      label: "同步预览未写入",
      detail: "当前决策全部为跳过或保留本地",
      batchId: plan.source.batchId,
      summary
    });
    pendingSyncImportPlan = null;
    renderPhase16SyncPanel();
    return;
  }
  if (!confirmPhase16ImportRisk(plan, summary)) return;
  const restoredPhase15 = Array.isArray(plan.payload) ? { auditCount: 0, snapshotCount: 0 } : restorePhase15AssetMetadata(plan.payload);
  const signatureText = restoredPhase15.signature?.checked ? `，签名${restoredPhase15.signature.ok ? "通过" : "异常"}` : "";
  setPersistencePending(true);
  try {
    const savedItems = [];
    if (databaseAvailable) {
      for (const entry of entriesToWrite) {
        const saved = await saveMemoryToDatabase(entry.memoryToSave, entry.writeMode === "update" ? entry.existingId : "");
        savedItems.push(saved);
      }
      await loadMemoriesFromDatabase({ silent: true });
      await syncWorkflowBlueprint({ quiet: true });
      await syncPrivacyPolicy({ quiet: true });
      await syncVersionInfo({ quiet: true });
      await syncAssetCollection({ quiet: true });
      setStorageStatus(`同步导入完成：新增 ${summary.create}，覆盖 ${summary.update}，复制 ${summary.copy}，不写入 ${summary.skip + summary.keepLocal}${signatureText}。`, "success");
    } else {
      const byId = new Map(memories.map((memory) => [memory.id, memory]));
      entriesToWrite.forEach((entry) => {
        byId.set(entry.memoryToSave.id, normalizeMemory(entry.memoryToSave));
      });
      memories = [...byId.values()].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));
      persistMemories(memories);
      setStorageStatus(`已导入到浏览器本地备份：新增 ${summary.create}，覆盖 ${summary.update}，复制 ${summary.copy}，不写入 ${summary.skip + summary.keepLocal}${signatureText}。`, "success");
    }
    recordPhase16SyncAuditEvent({
      action: "apply",
      label: "应用同步预览",
      detail: `写入 ${entriesToWrite.length} 件；新增 ${summary.create}，覆盖 ${summary.update}，复制 ${summary.copy}，不写入 ${summary.skip + summary.keepLocal}`,
      batchId: plan.source.batchId,
      summary
    });
    pendingSyncImportPlan = null;
    activeHall = "all";
    searchKeyword = "";
    elements.searchInput.value = "";
    render();
    renderGuide(`第十六阶段同步导入完成。按当前决策写入 ${entriesToWrite.length} 件；复制 ${summary.copy} 件，覆盖 ${summary.update} 件，保留或跳过 ${summary.skip + summary.keepLocal} 件。`);
  } catch (error) {
    setStorageStatus(`同步导入失败：${error.message}`, "warning");
  } finally {
    setPersistencePending(false);
    renderPhase16SyncPanel();
  }
}

function saveLocalProfileFromForm() {
  const saved = persistLocalProfile({
    displayName: elements.profileNameInput.value,
    deviceLabel: elements.profileDeviceInput.value,
    syncPreference: elements.profileSyncSelect.value,
    aiConsent: elements.profileAiConsentInput.checked
  });
  renderPrivacyPanel();
  renderGuide(saved
    ? `本地馆主配置已保存：${localProfile.displayName} / ${localProfile.deviceLabel}。`
    : "本地馆主配置保存失败，可能是浏览器存储不可用。");
}

async function handlePurgeDatabase() {
  const confirmed = confirm("确定清空 SQLite 中的所有展品和 Agent 整理历史吗？建议先导出 JSON 备份。");
  if (!confirmed) return;
  const typed = prompt("请输入 DELETE 确认清空数据库展品。");
  if (typed !== "DELETE") {
    renderGuide("已取消清空数据库。");
    return;
  }

  setPersistencePending(true);
  try {
    const result = await purgeDatabaseMemories();
    memories = [];
    persistMemories([]);
    databaseAvailable = true;
    databaseNeedsMigration = false;
    backendWorkflowBlueprint = null;
    privacyPolicy = null;
    versionInfo = null;
    await syncWorkflowBlueprint({ quiet: true });
    await syncPrivacyPolicy({ quiet: true });
    await syncVersionInfo({ quiet: true });
    setStorageStatus(`数据库已清空：删除 ${result.purge?.memoriesDeleted || 0} 件展品和 ${result.purge?.agentRunsDeleted || 0} 条整理历史。`, "success");
    render();
    renderGuide("数据库展品和整理历史已清空。本地馆主配置仍保留在浏览器中。");
  } catch (error) {
    setStorageStatus(`清空数据库失败：${error.message}`, "warning");
  } finally {
    setPersistencePending(false);
  }
}

function importMemories(file) {
  if (!file) return;
  const reader = new FileReader();
  reader.addEventListener("load", async () => {
    try {
      const parsed = JSON.parse(reader.result);
      pendingSyncImportPlan = buildPhase16ImportPlan(parsed);
      const decisionSummary = summarizePhase16ImportPlan(pendingSyncImportPlan);
      recordPhase16SyncAuditEvent({
        action: "preview",
        label: "生成同步预览",
        detail: `新增 ${decisionSummary.create}，覆盖 ${decisionSummary.update}，复制 ${decisionSummary.copy}，不写入 ${decisionSummary.skip + decisionSummary.keepLocal}；质量 ${pendingSyncImportPlan.quality.status}`,
        batchId: pendingSyncImportPlan.source.batchId,
        summary: decisionSummary
      });
      renderPhase16SyncPanel();
      elements.phase16SyncPanel?.scrollIntoView({ behavior: "smooth", block: "start" });
      setStorageStatus(`已生成同步预览：新增 ${decisionSummary.create}，覆盖 ${decisionSummary.update}，复制 ${decisionSummary.copy}，不写入 ${decisionSummary.skip + decisionSummary.keepLocal}；质量 ${pendingSyncImportPlan.quality.status}。确认后才会写入。`, "success");
      renderGuide("第十六阶段同步预览已生成。请在“数据主权”面板确认导入或取消预览。");
    } catch (error) {
      alert(`导入失败：${error.message}`);
    } finally {
      elements.importFile.value = "";
    }
  });
  reader.readAsText(file, "utf-8");
}

async function syncDatabase() {
  setPersistencePending(true);
  try {
    await loadMemoriesFromDatabase();
    await syncWorkflowBlueprint({ quiet: true });
    await syncPrivacyPolicy({ quiet: true });
    await syncVersionInfo({ quiet: true });
    renderGuide(databaseNeedsMigration
      ? "数据库已经连接，但仍有只在浏览器本地备份中的展品。当前页面已合并显示两边数据；点击“迁移本地”可以把这些展品补写入 SQLite。"
      : "已经从 SQLite 重新同步展品墙。");
  } catch (error) {
    databaseAvailable = false;
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = "local";
    privacyPolicy = null;
    privacyPolicySource = "local";
    versionInfo = null;
    operationsSource = "local";
    setStorageStatus(`同步数据库失败：继续显示浏览器本地备份。原因：${error.message}`, "warning");
  } finally {
    setPersistencePending(false);
  }
}

async function migrateLocalToDatabase() {
  const localMemories = memories.length ? memories : loadMemories();
  if (localMemories.length === 0) {
    renderGuide("本地备份里没有可迁移的展品。");
    return;
  }

  setPersistencePending(true);
  try {
    const result = await migrateCurrentLocalBackup();
    await syncWorkflowBlueprint({ quiet: true });
    await syncPrivacyPolicy({ quiet: true });
    await syncVersionInfo({ quiet: true });
    setStorageStatus(`本地迁移完成：已向 SQLite 写入 ${result.imported} 件展品。`, "success");
    activeHall = "all";
    searchKeyword = "";
    elements.searchInput.value = "";
    render();
    renderGuide(`已经把浏览器本地备份写入 SQLite。当前数据库中可展示 ${memories.length} 件展品。`);
  } catch (error) {
    databaseAvailable = false;
    backendWorkflowBlueprint = null;
    workflowBlueprintSource = "local";
    privacyPolicy = null;
    privacyPolicySource = "local";
    versionInfo = null;
    operationsSource = "local";
    setStorageStatus(`本地迁移失败：${error.message}`, "warning");
  } finally {
    setPersistencePending(false);
  }
}

function bindEvents() {
  document.addEventListener("click", (event) => {
    const featureButton = event.target.closest("[data-feature-target]");
    if (featureButton) {
      setActiveFeaturePanel(featureButton.dataset.featureTarget, featureButton.dataset.featureScrollTo || "");
      return;
    }
    const homeScrollButton = event.target.closest("[data-home-scroll]");
    if (homeScrollButton) {
      scrollHomeTarget(homeScrollButton.dataset.homeScroll);
      return;
    }
    if (event.target.closest("[data-feature-home]")) {
      setActiveFeaturePanel("home", "homeGateway");
    }
  });
  elements.memoryForm.addEventListener("submit", handleSubmit);
  elements.memoryForm.addEventListener("input", renderDraftPreview);
  elements.memoryForm.addEventListener("change", renderDraftPreview);
  elements.guideAskForm.addEventListener("submit", handleGuideAsk);
  elements.guideQuestionInput.addEventListener("input", updateGuideAskState);
  elements.rawContent.addEventListener("input", updateRawCount);
  elements.sampleButton.addEventListener("click", fillSampleMemory);
  elements.mockAiButton.addEventListener("click", handleMockAi);
  elements.useAgentSuggestionButton.addEventListener("click", useAgentSuggestion);
  elements.clearAgentWorkflowButton.addEventListener("click", clearAgentWorkflow);
  elements.agentWorkflow.addEventListener("click", (event) => {
    const reviewButton = event.target.closest("[data-review-field]");
    if (reviewButton) {
      focusReviewField(reviewButton.dataset.reviewField);
      return;
    }
    const confirmReviewButton = event.target.closest("[data-confirm-review]");
    if (confirmReviewButton) {
      confirmAgentReview(confirmReviewButton);
      return;
    }
    const approveButton = event.target.closest("[data-agent-approve-step]");
    if (approveButton) {
      approveAgentStep(approveButton.dataset.agentApproveStep);
      return;
    }
    const rejectButton = event.target.closest("[data-agent-reject-step]");
    if (rejectButton) {
      rejectAgentStep(rejectButton.dataset.agentRejectStep);
      return;
    }
    const retryButton = event.target.closest("[data-agent-retry-step]");
    if (retryButton) retryAgentStep(retryButton.dataset.agentRetryStep);
  });
  [elements.peopleInput, elements.locationInput, elements.tagsInput, elements.exhibitText].forEach((input) => {
    input.addEventListener("input", handleReviewFieldInput);
  });
  elements.memoryDate.addEventListener("change", handleReviewFieldInput);
  elements.resetButton.addEventListener("click", () => {
    resetFormState();
    renderGuide();
  });
  elements.cancelEditButton.addEventListener("click", () => {
    resetFormState();
    render();
  });
  elements.searchInput.addEventListener("input", (event) => {
    searchKeyword = event.target.value.trim();
    renderMemories();
    renderGuide();
    renderInsights();
  });
  elements.themeList.addEventListener("click", (event) => {
    const saveButton = event.target.closest("[data-theme-save]");
    if (saveButton) {
      saveThemeAsExhibition(saveButton.dataset.themeSave || "");
      return;
    }
    const searchButton = event.target.closest("[data-theme-search]");
    if (!searchButton) return;
    searchKeyword = searchButton.dataset.themeSearch || "";
    activeHall = "all";
    emotionFilter = "all";
    elements.searchInput.value = searchKeyword;
    elements.emotionFilterSelect.value = "all";
    render();
    document.getElementById("memoryGrid")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
  elements.reportPanel.addEventListener("click", (event) => {
    const button = event.target.closest("[data-report-draft]");
    if (button) saveReportDraftFromInsights();
  });
  elements.phase18AgentPanel?.addEventListener("click", (event) => {
    const openButton = event.target.closest("[data-phase18-open]");
    if (openButton) {
      openMemory(openButton.dataset.phase18Open);
      return;
    }
    if (event.target.closest("[data-phase18-create-review-task]")) {
      const task = createPhase18ReviewDashboardTask();
      renderInsights();
      setStorageStatus(`已生成第十八阶段复盘任务：${task.label}`, "success");
      return;
    }
    if (event.target.closest("[data-phase18-create-digest-task]")) {
      const task = createPhase18DigestTask();
      renderInsights();
      setStorageStatus(`已生成第十八阶段摘要任务：${task.label}`, "success");
      return;
    }
    const noiseToggleButton = event.target.closest("[data-phase18-noise-toggle]");
    if (noiseToggleButton) {
      const key = noiseToggleButton.dataset.phase18NoiseToggle;
      const config = loadPhase18NoiseRuleConfig();
      updatePhase18NoiseRuleSetting(key, !config[key]);
      renderInsights();
      setStorageStatus("第十八阶段降噪规则已更新。", "success");
      return;
    }
    const noiseScoreButton = event.target.closest("[data-phase18-noise-score]");
    if (noiseScoreButton) {
      const config = loadPhase18NoiseRuleConfig();
      const delta = Number(noiseScoreButton.dataset.phase18NoiseScore) || 0;
      updatePhase18NoiseRuleSetting("minVisibleScore", config.minVisibleScore + delta);
      renderInsights();
      setStorageStatus("第十八阶段降噪分数阈值已调整。", "success");
      return;
    }
    if (event.target.closest("[data-phase18-noise-reset]")) {
      resetPhase18NoiseRuleConfig();
      renderInsights();
      setStorageStatus("第十八阶段降噪规则已恢复默认。", "warning");
      return;
    }
    const repairDraftButton = event.target.closest("[data-phase18-repair-draft]");
    if (repairDraftButton) {
      const drafts = queuePhase18RepairDrafts(repairDraftButton.dataset.phase18RepairDraft);
      recordPhase18AgentFeedback("accepted", repairDraftButton.dataset.phase18RepairDraft, repairDraftButton.dataset.phase18Label);
      renderInsights();
      setStorageStatus(drafts.length ? `已生成 ${drafts.length} 条第十八阶段修复草案。` : "没有可生成的修复草案。", drafts.length ? "success" : "warning");
      return;
    }
    const saveReviewButton = event.target.closest("[data-phase18-save-review]");
    if (saveReviewButton) {
      savePhase18PeriodicReviewAsset(saveReviewButton.dataset.phase18SaveReview);
      return;
    }
    const saveReviewReportButton = event.target.closest("[data-phase18-save-review-report]");
    if (saveReviewReportButton) {
      savePhase18PeriodicReviewReport(saveReviewReportButton.dataset.phase18SaveReviewReport);
      return;
    }
    const openReviewAssetButton = event.target.closest("[data-phase18-open-asset]");
    if (openReviewAssetButton) {
      openSavedAsset("exhibition", openReviewAssetButton.dataset.phase18OpenAsset);
      return;
    }
    const openReviewReportButton = event.target.closest("[data-phase18-open-report]");
    if (openReviewReportButton) {
      openSavedAsset("report", openReviewReportButton.dataset.phase18OpenReport);
      return;
    }
    const graphFilterButton = event.target.closest("[data-phase18-graph-filter]");
    if (graphFilterButton) {
      phase18GraphFilter = graphFilterButton.dataset.phase18GraphFilter || "all";
      renderInsights();
      return;
    }
    const applyRepairButton = event.target.closest("[data-phase18-apply-repair]");
    if (applyRepairButton) {
      const applied = applyPhase18RepairDraft(applyRepairButton.dataset.phase18ApplyRepair);
      render();
      setStorageStatus(applied ? "已应用一条第十八阶段修复草案，请继续人工复核字段。" : "修复草案未应用，可能已处理或展品不存在。", applied ? "success" : "warning");
      return;
    }
    if (event.target.closest("[data-phase18-apply-repair-batch]")) {
      const review = buildPhase18RepairBatchReview();
      if (!review.ready) {
        setStorageStatus("当前没有可批量应用的第十八阶段修复草案。", "warning");
        return;
      }
      const confirmed = window.confirm(`即将批量应用 ${review.previewCount} 条修复草案，涉及 ${review.targetCount} 件展品。占位字段仍需要后续人工复核，是否继续？`);
      if (!confirmed) {
        recordPhase18TaskAudit("repair-drafts-batch-cancelled", "preview-drafts", `取消批量应用 ${review.previewCount} 条修复草案。`, {
          previewCount: review.previewCount,
          targetCount: review.targetCount
        });
        renderInsights();
        setStorageStatus("已取消第十八阶段批量应用。", "warning");
        return;
      }
      const applied = applyPhase18RepairDraftBatch();
      render();
      setStorageStatus(applied ? `已批量应用 ${applied} 条第十八阶段修复草案，请继续人工复核。` : "没有修复草案被应用。", applied ? "success" : "warning");
      return;
    }
    if (event.target.closest("[data-phase18-clear-repairs]")) {
      clearPhase18RepairDrafts();
      renderInsights();
      setStorageStatus("已清空第十八阶段修复草案。", "warning");
      return;
    }
    const taskFilterButton = event.target.closest("[data-phase18-task-filter]");
    if (taskFilterButton) {
      phase18TaskFilter = taskFilterButton.dataset.phase18TaskFilter || "active";
      renderInsights();
      return;
    }
    const taskSelect = event.target.closest("[data-phase18-task-select]");
    if (taskSelect) {
      togglePhase18TaskSelection(taskSelect.dataset.phase18TaskSelect, taskSelect.checked);
      renderInsights();
      return;
    }
    if (event.target.closest("[data-phase18-task-select-visible]")) {
      selectVisiblePhase18Tasks();
      renderInsights();
      setStorageStatus("已选择当前列表中的第十八阶段任务。", "success");
      return;
    }
    if (event.target.closest("[data-phase18-task-selection-clear]")) {
      clearPhase18TaskSelection();
      renderInsights();
      setStorageStatus("已清空第十八阶段任务选择。", "warning");
      return;
    }
    const taskBatchStatusButton = event.target.closest("[data-phase18-task-batch-status]");
    if (taskBatchStatusButton) {
      const review = buildPhase18TaskBatchReview();
      const nextStatus = taskBatchStatusButton.dataset.phase18TaskBatchStatus;
      if (!review.canBatch) {
        setStorageStatus("请先选择要批量处理的第十八阶段任务。", "warning");
        return;
      }
      const confirmed = window.confirm(`即将批量更新 ${review.selectedCount} 条长期任务为「${getPhase18TaskStatusLabel(nextStatus)}」，涉及 ${review.memoryCount} 件展品。是否继续？`);
      if (!confirmed) return;
      const result = updatePhase18SelectedTasksStatus(nextStatus);
      renderInsights();
      setStorageStatus(result.changed ? `已批量更新 ${result.changed} 条第十八阶段任务。` : "没有任务状态被更新。", result.changed ? "success" : "warning");
      return;
    }
    if (event.target.closest("[data-phase18-task-batch-repair]")) {
      const review = buildPhase18TaskBatchReview();
      if (!review.draftableCount) {
        setStorageStatus("当前选择的任务没有可生成的修复草案。", "warning");
        return;
      }
      const confirmed = window.confirm(`即将从 ${review.selectedCount} 条任务中批量生成 ${review.draftableCount} 组修复草案。是否继续？`);
      if (!confirmed) return;
      const result = queuePhase18RepairDraftsForSelectedTasks();
      renderInsights();
      setStorageStatus(result.generated ? `已批量生成 ${result.generated} 条第十八阶段修复草案。` : "没有新的修复草案生成，可能已存在预览草案。", result.generated ? "success" : "warning");
      return;
    }
    const taskStatusButton = event.target.closest("[data-phase18-task-status]");
    if (taskStatusButton) {
      const updated = updatePhase18TaskStatus(taskStatusButton.dataset.phase18TaskId, taskStatusButton.dataset.phase18TaskStatus);
      renderInsights();
      setStorageStatus(updated ? "第十八阶段长期任务状态已更新。" : "未找到要更新的长期任务。", updated ? "success" : "warning");
      return;
    }
    if (event.target.closest("[data-phase18-clear-resolved]")) {
      clearResolvedPhase18Tasks();
      renderInsights();
      setStorageStatus("已清理第十八阶段已完成和已忽略任务。", "warning");
      return;
    }
    const feedbackButton = event.target.closest("[data-phase18-feedback]");
    if (feedbackButton) {
      recordPhase18AgentFeedback(
        feedbackButton.dataset.phase18Feedback,
        feedbackButton.dataset.phase18Target,
        feedbackButton.dataset.phase18Label
      );
      renderInsights();
      setStorageStatus("第十八阶段长期助理反馈已记录。", "success");
    }
  });
  elements.savedAssetsPanel?.addEventListener("click", (event) => {
    const reportButton = event.target.closest("[data-report-draft]");
    if (reportButton) {
      saveReportDraftFromInsights();
      return;
    }
    const refreshButton = event.target.closest("[data-assets-refresh]");
    if (refreshButton) {
      refreshAssetCollection();
      return;
    }
    const openButton = event.target.closest("[data-asset-open]");
    if (openButton) {
      openSavedAsset(openButton.dataset.assetType || "exhibition", openButton.dataset.assetOpen || "");
      return;
    }
    const statusButton = event.target.closest("[data-asset-status]");
    if (statusButton) {
      updateSavedAssetStatus(statusButton.dataset.assetType || "exhibition", statusButton.dataset.assetStatus || "", statusButton.dataset.nextStatus || "draft");
      return;
    }
    const exportFilterButton = event.target.closest("[data-asset-export-filter]");
    if (exportFilterButton) {
      exportFilteredAssetPackage(exportFilterButton.dataset.assetExportFilter || "");
      return;
    }
    const batchStatusButton = event.target.closest("[data-asset-batch-from]");
    if (batchStatusButton) {
      batchUpdateAssetStatus(batchStatusButton.dataset.assetBatchFrom || "draft", batchStatusButton.dataset.assetBatchTo || "review");
      return;
    }
    const snapshotExportButton = event.target.closest("[data-asset-snapshot-export]");
    if (snapshotExportButton) {
      exportSelectedAssetSnapshot(snapshotExportButton.dataset.assetSnapshotExport || selectedAssetSnapshotId);
      return;
    }
    const snapshotSelectButton = event.target.closest("[data-asset-snapshot-select]");
    if (snapshotSelectButton) {
      selectedAssetSnapshotId = snapshotSelectButton.dataset.assetSnapshotSelect || "";
      setStorageStatus("已切换资产快照对比对象。", "success");
      renderAssetCollectionPanel();
      return;
    }
    const auditSearchButton = event.target.closest("[data-asset-audit-search]");
    if (auditSearchButton) {
      const input = elements.savedAssetsPanel.querySelector("[data-asset-audit-query]");
      assetAuditSearchTerm = input?.value || "";
      renderAssetCollectionPanel();
      return;
    }
    const auditClearButton = event.target.closest("[data-asset-audit-clear]");
    if (auditClearButton) {
      assetAuditSearchTerm = "";
      renderAssetCollectionPanel();
      return;
    }
    const deleteButton = event.target.closest("[data-asset-delete]");
    if (deleteButton) {
      deleteSavedAsset(deleteButton.dataset.assetType || "exhibition", deleteButton.dataset.assetDelete || "", deleteButton.dataset.assetTitle || "");
    }
  });
  elements.timelineList.addEventListener("click", (event) => {
    const button = event.target.closest("[data-timeline-search]");
    if (!button) return;
    const value = button.dataset.timelineSearch || "";
    searchKeyword = /^\d{4}-\d{2}-\d{2}$/.test(value) ? value.slice(0, 7) : value.replace(" 年 ", "-").replace(" 月", "");
    activeHall = "all";
    emotionFilter = "all";
    elements.searchInput.value = searchKeyword;
    elements.emotionFilterSelect.value = "all";
    render();
    document.getElementById("memoryGrid")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
  elements.sortSelect.addEventListener("change", (event) => {
    sortMode = event.target.value;
    renderMemories();
    renderGuide();
  });
  elements.emotionFilterSelect.addEventListener("change", (event) => {
    emotionFilter = event.target.value;
    renderMemories();
    renderGuide();
    renderInsights();
  });
  elements.insightScopeSelect.addEventListener("change", (event) => {
    insightScope = event.target.value;
    renderInsights();
  });
  elements.workflowTemplateList.addEventListener("click", (event) => {
    const card = event.target.closest("[data-template]");
    if (!card) return;
    activeWorkflowTemplateId = card.dataset.template || activeWorkflowTemplateId;
    renderWorkflowOrchestration();
  });
  elements.phase19SampleButton?.addEventListener("click", loadPhase19Sample);
  elements.phase19SaveTemplateButton?.addEventListener("click", savePhase19CustomMappingTemplate);
  elements.phase19BatchFilter?.addEventListener("change", (event) => {
    phase19BatchFilter = event.target.value || "all";
    renderPhase19ImportPreview(phase19ImportPreview);
  });
  elements.phase19AuditSearch?.addEventListener("input", (event) => {
    phase19AuditSearch = event.target.value || "";
    renderPhase19ImportPreview(phase19ImportPreview);
  });
  elements.phase19PreviewButton?.addEventListener("click", previewPhase19Import);
  elements.phase19ApplyButton?.addEventListener("click", applyPhase19ImportPreview);
  elements.phase19ImportPreview?.addEventListener("click", (event) => {
    const selectInput = event.target.closest("[data-phase19-draft-select]");
    if (selectInput) {
      setPhase19DraftSelection(Number(selectInput.dataset.phase19DraftSelect), selectInput.checked);
      return;
    }
    if (event.target.closest("[data-phase19-select-all]")) {
      setAllPhase19DraftSelection(true);
      return;
    }
    if (event.target.closest("[data-phase19-select-none]")) {
      setAllPhase19DraftSelection(false);
      return;
    }
    const batchToggleButton = event.target.closest("[data-phase19-batch-toggle]");
    if (batchToggleButton) {
      togglePhase19BatchDetail(batchToggleButton.dataset.phase19BatchToggle);
      return;
    }
    const itemRollbackButton = event.target.closest("[data-phase19-item-rollback]");
    if (itemRollbackButton) {
      rollbackPhase19ImportItem(itemRollbackButton.dataset.phase19ItemBatch, itemRollbackButton.dataset.phase19ItemRollback);
      return;
    }
    const auditButton = event.target.closest("[data-phase19-export-audit]");
    if (auditButton) {
      exportPhase19BatchAudit(auditButton.dataset.phase19ExportAudit);
      return;
    }
    const reviewStatusButton = event.target.closest("[data-phase19-review-status]");
    if (reviewStatusButton) {
      const updated = updatePhase19ConflictReviewStatus(
        reviewStatusButton.dataset.phase19ReviewBatch,
        reviewStatusButton.dataset.phase19ReviewStatus,
        reviewStatusButton.dataset.phase19ReviewNext
      );
      setStorageStatus(updated ? "第十九阶段冲突复核状态已更新。" : "未找到要更新的冲突复核项。", updated ? "success" : "warning");
      return;
    }
    const rollbackButton = event.target.closest("[data-phase19-rollback]");
    if (rollbackButton) {
      rollbackPhase19ImportBatch(rollbackButton.dataset.phase19Rollback);
    }
  });
  elements.phase19ImportPreview?.addEventListener("input", (event) => {
    const fieldInput = event.target.closest("[data-phase19-draft-field]");
    if (!fieldInput) return;
    updatePhase19DraftField(Number(fieldInput.dataset.phase19DraftIndex), fieldInput.dataset.phase19DraftField, fieldInput.value);
  });
  elements.phase19ImportPreview?.addEventListener("change", (event) => {
    const duplicateAction = event.target.closest("[data-phase19-duplicate-action]");
    if (duplicateAction) {
      setPhase19DuplicateAction(Number(duplicateAction.dataset.phase19DuplicateAction), duplicateAction.value);
    }
  });
  elements.clearFiltersButton.addEventListener("click", clearCollectionFilters);
  elements.emotionIntensity.addEventListener("input", updateEmotionIntensityOutput);
  elements.exportButton.addEventListener("click", exportMemories);
  elements.importFile.addEventListener("change", (event) => importMemories(event.target.files[0]));
  elements.syncDatabaseButton.addEventListener("click", syncDatabase);
  elements.migrateLocalButton.addEventListener("click", migrateLocalToDatabase);
  elements.saveProfileButton.addEventListener("click", saveLocalProfileFromForm);
  elements.exportRedactedButton.addEventListener("click", exportRedactedMemories);
  elements.purgeDatabaseButton.addEventListener("click", handlePurgeDatabase);
  elements.phase16SyncPanel?.addEventListener("click", (event) => {
    if (event.target.closest("[data-phase16-export-sync]")) {
      exportPhase16SyncPackage();
      return;
    }
    if (event.target.closest("[data-phase17-queue-sync]")) {
      enqueuePhase17SyncTask({
        action: "manual-sync-review",
        label: "手动同步待复核",
        detail: "已加入第十七阶段同步队列，可先导出同步包再交给其他设备导入。"
      });
      renderPhase16SyncPanel();
      setStorageStatus("已加入第十七阶段同步队列。", "success");
      return;
    }
    if (event.target.closest("[data-phase17-add-review-device]")) {
      const device = addPhase17ReviewDevice();
      enqueuePhase17SyncTask({
        action: "device-review",
        label: "设备信任复核",
        detail: `${device.label} 已登记为待复核设备，进入真实同步前需要确认来源。`,
        risk: "review"
      });
      renderPhase16SyncPanel();
      setStorageStatus("已登记一台待复核设备。", "success");
      return;
    }
    if (event.target.closest("[data-phase17-cloud-draft]")) {
      createPhase17PrivateCloudDraft();
      enqueuePhase17SyncTask({
        action: "private-cloud-boundary",
        channel: "private-cloud",
        label: "私有云配置草案",
        detail: "已生成私有云适配边界草案；不会保存密钥，也不会连接云端。",
        risk: "review"
      });
      renderPhase16SyncPanel();
      setStorageStatus("已生成私有云适配边界草案。", "success");
      return;
    }
    if (event.target.closest("[data-phase17-cloud-clear]")) {
      clearPhase17PrivateCloudDraft();
      renderPhase16SyncPanel();
      setStorageStatus("已清除私有云适配边界草案。", "warning");
      return;
    }
    const phase17TrustButton = event.target.closest("[data-phase17-device-trust]");
    if (phase17TrustButton) {
      const updated = updatePhase17DeviceTrust(
        phase17TrustButton.dataset.phase17DeviceId,
        phase17TrustButton.dataset.phase17DeviceTrust
      );
      renderPhase16SyncPanel();
      setStorageStatus(updated ? "设备信任状态已更新。" : "本机设备不能被降级，或未找到设备。", updated ? "success" : "warning");
      return;
    }
    const phase17QueueButton = event.target.closest("[data-phase17-queue-status]");
    if (phase17QueueButton) {
      const updated = updatePhase17QueueTaskStatus(
        phase17QueueButton.dataset.phase17QueueId,
        phase17QueueButton.dataset.phase17QueueStatus
      );
      renderPhase16SyncPanel();
      setStorageStatus(updated ? "第十七阶段同步任务状态已更新。" : "未找到要更新的同步任务。", updated ? "success" : "warning");
      return;
    }
    if (event.target.closest("[data-phase16-apply-import]")) {
      applyPhase16ImportPlan();
      return;
    }
    if (event.target.closest("[data-phase16-export-drill-report]")) {
      exportPhase16RecoveryReport();
      return;
    }
    const batchButton = event.target.closest("[data-phase16-batch-decision]");
    if (batchButton && pendingSyncImportPlan) {
      const decision = batchButton.dataset.phase16BatchDecision || "copy";
      pendingSyncImportPlan.entries = pendingSyncImportPlan.entries.map((entry) => (
        entry.action === "conflict" ? { ...entry, decision } : entry
      ));
      pendingSyncImportPlan.decisionSummary = summarizePhase16ImportPlan(pendingSyncImportPlan);
      renderPhase16SyncPanel();
      setStorageStatus(`已将冲突项批量设为：${getPhase16DecisionLabel(decision)}。`, "success");
      return;
    }
    if (event.target.closest("[data-phase16-cancel-import]")) {
      recordPhase16SyncAuditEvent({
        action: "cancel-preview",
        label: "取消同步预览",
        detail: "用户取消了尚未应用的导入预览",
        batchId: pendingSyncImportPlan?.source?.batchId,
        summary: pendingSyncImportPlan ? summarizePhase16ImportPlan(pendingSyncImportPlan) : null
      });
      pendingSyncImportPlan = null;
      renderPhase16SyncPanel();
      setStorageStatus("已取消第十六阶段同步预览。", "warning");
    }
  });
  elements.phase16SyncPanel?.addEventListener("change", (event) => {
    const select = event.target.closest("[data-phase16-decision]");
    if (select && pendingSyncImportPlan) {
      const index = Number(select.dataset.phase16Decision);
      if (!Number.isInteger(index) || !pendingSyncImportPlan.entries[index]) return;
      pendingSyncImportPlan.entries[index] = {
        ...pendingSyncImportPlan.entries[index],
        decision: select.value
      };
      pendingSyncImportPlan.decisionSummary = summarizePhase16ImportPlan(pendingSyncImportPlan);
      renderPhase16SyncPanel();
      return;
    }
    const assetSelect = event.target.closest("[data-phase16-asset-decision]");
    if (assetSelect && pendingSyncImportPlan?.assetMergePreview?.entries) {
      const index = Number(assetSelect.dataset.phase16AssetDecision);
      if (!Number.isInteger(index) || !pendingSyncImportPlan.assetMergePreview.entries[index]) return;
      pendingSyncImportPlan.assetMergePreview.entries[index] = {
        ...pendingSyncImportPlan.assetMergePreview.entries[index],
        decision: assetSelect.value
      };
      renderPhase16SyncPanel();
    }
  });
  elements.closeDialog.addEventListener("click", () => elements.memoryDialog.close());
  elements.memoryDialog.addEventListener("click", (event) => {
    if (event.target === elements.memoryDialog) elements.memoryDialog.close();
    const exportButton = event.target.closest("[data-asset-export]");
    if (exportButton) {
      exportActiveAsset();
      return;
    }
    const form = event.target.closest("[data-asset-edit-form]");
    if (!form) return;
    const memoryInsertButton = event.target.closest("[data-memory-insert]");
    if (memoryInsertButton) {
      insertAssetMemoryFromPicker(form);
      return;
    }
    const referenceInsertButton = event.target.closest("[data-reference-insert]");
    if (referenceInsertButton) {
      insertReportReferenceFromPicker(form);
      return;
    }
    const sortButton = event.target.closest("[data-memory-sort-index]");
    if (sortButton) {
      moveAssetMemoryLine(form, Number(sortButton.dataset.memorySortIndex), sortButton.dataset.memorySortDirection || "down");
    }
  });
  elements.memoryDialog.addEventListener("submit", (event) => {
    const form = event.target.closest("[data-asset-edit-form]");
    if (!form) return;
    event.preventDefault();
    saveSavedAssetEdits(form);
  });
  document.querySelectorAll("[data-question]").forEach((button) => {
    button.addEventListener("click", () => answerGuideQuestion(button.dataset.question));
  });
}

renderHallSelect();
renderEmotionOptions();
updateEmotionIntensityOutput();
updateRawCount();
bindEvents();
setActiveFeaturePanel("home", "", false);
updateGuideAskState();
render();
checkAiBackend();
initializeStorage();
