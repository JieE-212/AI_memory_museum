# 工程卫生收口记录

更新日期：2026-07-05

## 1. 当前结论

本轮工程卫生处理先完成运行产物隔离，不改变发布状态、不改变 Phase 29 / Phase 30 guardrail、不提交或重写 git 索引。

人工已确认接受当前 `项目工程` 作为 git 仓库根目录的扁平化形态。git 索引仍保留旧的一层目录路径，表现为旧路径删除、新根路径未跟踪；这是待执行的路径迁移提交，不是业务代码丢失。

## 2. 已处理事项

- `项目工程/.gitignore` 已补充 `.env.*`、`!.env.example`、`*.log`、`operations-debug.json` 和 `operations-export-debug.json`。
- `data/`、本地 SQLite、服务启动日志和调试导出包继续保持不入版本控制。
- 未删除任何运行日志或数据库文件。
- 未执行 `git reset`、`git checkout`、`git clean` 或 `git add -A`。
- 已完成 `server.js` 低风险中文编码清理，重点覆盖阶段 11 至 20 的用户可见文案、导出说明、隐私/同步/长期助理提示；`server.js` 与 `src/services/operations.js` 的已知乱码特征检索为 0。
- 已重写 `README.md` 当前使用说明、能力清单和 API 列表，保留 Phase 29/30 顶部 guardrail 与历史基线记录；`README.md` 的已知乱码特征检索为 0。
- 已修复 `scripts/phase13-readiness.js` 和 `scripts/phase14-readiness.js` 中由旧乱码造成的语法错误，两个脚本均可通过 `node --check`。
- 已重写 `项目规划.md` 与 `项目白皮书.md` 的历史正文部分，保留 Phase 29/30 guardrail、阶段文档链接和 readiness 脚本依赖的版本锚点；两份文档的已知乱码特征检索为 0。
- 已新增 `阶段30收口审查包.md` 和 `scripts/phase30-closure-review.js`，用于非破坏性检查 git 形态已确认、索引迁移待授权、RC 边界、人工证据执行清单和 runtime/release 保护线。
- 已新增 `阶段30候选交付说明.md` 与 `阶段30人工复核证据填写表.md`，当前候选交付结论固定为 `rc-reviewable-but-not-releasable`，证据填写表不代表人工签核完成。
- 已新增 `仓库扁平化确认记录.md`，记录人工决策：`gitPathShape=accepted-flattened-project-root`，索引迁移仍需后续人工授权提交。

## 3. 仍需人工确认的事项

目录扁平化方向已经确认：接受当前 `项目工程` 作为仓库根。

仍需人工确认并授权的事项：

- 是否现在执行路径迁移提交准备；
- 是否允许将旧的 `项目工程/...` 索引路径替换为当前仓库根文件；
- 是否允许将该迁移作为单独 commit 处理。

在索引迁移提交完成前，项目可以继续运行和检查，但不建议制作正式 release commit。

## 4. 建议顺序

1. 保持当前代码运行基线，继续要求 `npm.cmd run check` 通过。
2. 保持 `gitPathShape=accepted-flattened-project-root` 作为人工决策记录。
3. 等待维护者明确授权后，再处理索引迁移，避免误把用户文件当成删除。
4. 索引收口后，再讨论正式 release commit 和 Phase 29 exit。

## 5. 当前保护线

- 运行时仍关闭。
- 第三方插件执行仍关闭。
- Phase 30 human review execution ledger 仍为只读台账。
- 所有人工证据槽位仍为 pending。

## 6. 仓库索引迁移预检

已新增只读预检清单：`仓库索引迁移预检清单.md`。

本轮只完成迁移前观察与校验，不改变 git index：

- `gitPathShape=accepted-flattened-project-root`
- `gitIndexMigration=committed-flattened-project-root`
- `indexMutation=committed-path-migration`

预检脚本：`node scripts/git-flattening-preflight.js`。

该脚本只读取 `git status --porcelain=v1 -z -uall --ignored`，用于确认旧路径删除、当前根目录未跟踪文件和被忽略运行产物仍处于预期状态。它不执行 `git add`、`git commit`、`git reset`、`git checkout` 或 `git clean`。

## 7. 仓库索引迁移执行方案

已新增只读执行方案：`仓库索引迁移执行方案.md`。

新增脚本：`node scripts/git-flattening-migration-plan.js`。

该脚本继续保持只读，用于把迁移对象拆成两类：

- `项目工程/...` 旧索引路径：已有当前仓库根目录对应文件，可在后续授权后作为路径扁平化迁移对象。
- `项目文档/...` 旧索引路径：当前实际文档目录在 `../项目文档`，属于 `outside-current-git-root`，不能被当前 git root 直接纳入提交。

文档归属阻断已推进为 `docsOwnershipDecision=docs-in-repo`。路径迁移提交完成后，后续仍不建议制作正式 release commit，直到人工证据补齐。

## 8. 文档归属决策

已新增 `文档归属决策记录.md`，并接受 `docs-in-repo`。

当前推进方式：

- `docsOwnershipDecision=docs-in-repo`。
- 将 `../项目文档` 镜像到当前 git root 内的 `项目文档/`。
- 保留仓库外文档目录作为镜像来源：`outsideMirrorRetained=true`。
- `gitIndexMigration=committed-flattened-project-root`。
- `indexMutation=committed-path-migration`。

新增校验脚本：`node scripts/docs-ownership-check.js`。

该脚本只比较仓库外文档目录和仓库内 `项目文档/` 的 Markdown 文件是否一致，不执行任何 git index 操作。

## 9. Git index 路径迁移提交状态

已新增 `git:flattening-index-staged-check`，用于检查路径迁移提交后的索引状态。

当前结论：

- `gitPathShape=accepted-flattened-project-root`
- `gitIndexMigration=committed-flattened-project-root`
- `indexMutation=committed-path-migration`
- `commitCreated=true`
- `releaseCommit=blocked-until-human-release-approval`

本阶段只完成路径迁移 commit，不批准 release，不退出 Phase 29，不进入 Phase 30 implementation，不启用 runtime execution 或 third-party execution。
