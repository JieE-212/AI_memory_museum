# 阶段 30 收口审查包

更新日期：2026-07-05

Baseline: `2.0.7 / phase30-closure-review-package`

Phase 30 closure review package: active

本收口审查包用于把当前项目从“继续堆叠规划预览”切换到“可交付审查”。它不批准 release、不退出 Phase 29、不进入 Phase 30 implementation、不启用 runtime execution，也不允许第三方插件执行。

## 1. 审查目标

当前项目已经具备 Phase 29 冻结、Phase 30 规划基线、沙箱验收框架、密钥边界计划、审计持久化演练、Go/No-Go 看板、第一轮规划收口、人工复核证据包和人工复核执行台账。

下一步不再新增 Phase 29 preview artifact，也不继续创建新的运行时能力模型。优先完成：

- git 目录扁平化形态确认与已完成索引迁移记录；
- release candidate 边界说明；
- 人工复核证据执行清单；
- 工程卫生和编码清理验证；
- 保持所有 runtime 与 release guardrail 关闭。

## 2. 当前不可变保护线

- `PHASE=29`
- `APP_VERSION=1.9.48`
- `BUILD_LABEL=phase29-release-exit-final-archive-manifest-preview`
- `releaseReady=false`
- `phase29ExitReady=false`
- `phase30EntryReady=false`
- `runtimeExecution=false`
- `thirdPartyExecution=false`
- `persistedMutations=0`

本审查包不改变上述状态。

## 3. Git 形态审查

当前 `项目工程` 是实际 git 仓库根目录。人工已经确认接受当前扁平化目录形态，决策记录见 `仓库扁平化确认记录.md`。

索引仍保留旧的一层目录路径，表现为旧的 `项目工程/...` 路径被标记删除，而当前根目录文件被标记为未跟踪。路径迁移提交仍需后续人工授权。

在迁移提交授权前：

- 不执行 `git reset`；
- 不执行 `git checkout --`；
- 不执行 `git clean`；
- 不执行 `git add -A`；
- 不制作正式 release commit。

当前决策：

- `gitPathShape=accepted-flattened-project-root`
- `gitIndexMigration=committed-flattened-project-root`
- `releaseCommit=blocked-until-human-release-approval`

## 4. Release Candidate 边界

当前可以作为 release candidate 审查输入，但不能作为正式 release：

- `npm.cmd run check` 必须通过；
- `/api/version`、`/api/operations`、`/api/operations/export` 必须继续暴露 Phase 30 human review execution ledger；
- 10 个人工证据槽位必须继续是 `pending` 和 `evidenceRef=missing`；
- runtime 和 third-party execution 必须继续关闭；
- 工程卫生记录必须说明 git 目录扁平化已被接受，索引迁移提交已完成，但 release commit 仍需发布批准。

RC 审查说明必须明确：

- 当前是 Phase 29 frozen review state；
- Phase 30 entry 尚未激活；
- 任何 check pass 都不能转换为人工批准；
- 任何 dry-run pass 都不能转换为运行时启用。

## 5. 人工证据执行清单

进入真正 Phase 30 entry 前，以下 evidence slots 必须由人工填入外部证据引用、reviewer、reviewedAt、decisionReason 和 residualRisk：

| evidenceId | owner | 当前状态 | 阻断 |
| --- | --- | --- | --- |
| `release-blocker-disposition` | release owner | pending | release / Phase 29 exit / Phase 30 entry |
| `transition-redline-disposition` | release owner | pending | release / Phase 29 exit / Phase 30 entry |
| `signoff-evidence-reconciliation` | release owner | pending | release |
| `release-runtime-separation` | release owner | pending | release / runtime go-no-go |
| `runtime-owner-go-no-go` | runtime owner | pending | Phase 30 entry / runtime |
| `rollback-strategy-review` | runtime owner | pending | runtime |
| `sandbox-acceptance-review` | security reviewer | pending | Phase 30 entry / runtime |
| `secret-boundary-review` | security reviewer | pending | Phase 30 entry / runtime |
| `private-memory-boundary-review` | data steward | pending | Phase 30 entry / runtime |
| `audit-dry-run-review` | audit reviewer | pending | Phase 30 entry / runtime |

## 6. 禁止事项

- auto-approve-human-signoff
- convert-check-pass-to-human-evidence
- convert-dry-run-pass-to-human-evidence
- convert-closure-review-to-release-approval
- mark-releaseReady-true
- mark-phase29ExitReady-true
- mark-phase30EntryReady-true
- enable-runtimeExecution
- enable-thirdPartyExecution
- execute-real-third-party-plugin
- persist-runtime-state
- mutate-git-index-without-human-confirmation
- create-release-commit-before-index-migration

## 7. 完成标准

本收口审查包完成时，应满足：

- `阶段30收口审查包.md` 存在；
- `phase30:closure-review` 脚本存在；
- `npm.cmd run check` 包含并通过 `phase30:closure-review`；
- `README.md`、`项目规划.md`、`项目白皮书.md` 说明收口审查状态；
- `工程卫生收口记录.md` 继续记录 git 形态已接受、索引迁移提交已完成；
- Phase 30 human review execution ledger 仍为只读 pending；
- release、Phase 29 exit、Phase 30 entry、runtime execution 和 third-party execution 仍保持阻断。

Phase 30 closure review package is not release approval.

## 8. 仓库索引迁移预检

本轮补充 `仓库索引迁移预检清单.md` 和 `git:flattening-preflight`，用于在真正迁移 git index 前做只读检查。

当前结论保持不变：

- `gitPathShape=accepted-flattened-project-root`
- `gitIndexMigration=committed-flattened-project-root`
- `indexMutation=committed-path-migration`

预检脚本只允许读取 git status，并确认旧索引路径删除、当前根目录未跟踪文件、忽略运行产物三类现象仍存在。预检通过不等于迁移授权，也不等于 release approval、Phase 29 exit、Phase 30 entry 或 runtime enablement。

## 9. 索引迁移执行方案边界

本轮补充 `仓库索引迁移执行方案.md` 和 `git:flattening-migration-plan`，用于把后续迁移拆成可审查对象，而不是直接执行。

新增关键边界：

- `docsOwnershipDecision=docs-in-repo`
- `indexMutation=committed-path-migration`
- `项目文档=outside-current-git-root`

结论：工程代码路径可以在后续授权后进入路径扁平化迁移准备，但旧索引中的 `项目文档/...` 需要先决定文档继续留在仓库外，还是迁回当前 git root 内。该决策完成前，不应制作路径迁移 commit。

## 10. 文档归属决策

本轮接受 `docs-in-repo`，并新增 `文档归属决策记录.md` 与 `docs:ownership-check`。

当前边界：

- `docsOwnershipDecision=docs-in-repo`
- `outsideMirrorRetained=true`
- `gitIndexMigration=committed-flattened-project-root`
- `indexMutation=committed-path-migration`

仓库内 `项目文档/` 作为后续版本控制位置；仓库外 `../项目文档` 暂时保留为镜像来源。本决策不等于 git index 迁移授权，也不等于 release approval、Phase 29 exit、Phase 30 entry 或 runtime enablement。

## 11. Git index 路径迁移提交检查

本轮新增 `git:flattening-index-staged-check`。该检查用于确认旧的 `项目工程/...` 索引路径已从 index 中移除，当前根目录文件与 `项目文档/` 已完成路径迁移提交。

当前边界：

- `gitIndexMigration=committed-flattened-project-root`
- `indexMutation=committed-path-migration`
- `commitCreated=true`
- `releaseCommit=blocked-until-human-release-approval`

这仍不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 12. RC 冻结与人工证据 intake

本轮新增 `阶段30RC冻结清单.md`、`阶段30人工证据收集计划.md`、`阶段30人工证据收集登记表.md`、`阶段30人工证据提交门禁.md`、`阶段30人工证据提交样例.json`、`阶段30人工Reviewer交接包.md`、`阶段30人工审查会议包.md`、`阶段30会后证据汇总包.md`、`阶段30LiveSubmission转换预检包.md`、`阶段30人工证据冲突审查包.md`、`阶段30风险处置后续台账.md`、`阶段30发布决策准备信封.md`、`阶段30入口准备红线.md`、`phase30:rc-freeze`、`phase30:evidence-intake-readiness`、`phase30:evidence-intake-register`、`phase30:evidence-submission-gate`、`phase30:evidence-submission-template`、`phase30:reviewer-handoff`、`phase30:review-session-package`、`phase30:post-review-evidence-summary`、`phase30:live-submission-conversion-preflight`、`phase30:human-evidence-conflict-review`、`phase30:risk-disposition-followup-ledger`、`phase30:release-decision-readiness-envelope` 和 `phase30:entry-readiness-redline`。

当前结论继续保持：

- `rc-reviewable-but-not-releasable`
- `releaseReady=false`
- `phase29ExitReady=false`
- `phase30EntryReady=false`
- `runtimeExecution=false`
- `thirdPartyExecution=false`
- `pendingSlots=10`
- `approvedSlots=0`

RC 冻结只表示当前材料可进入人工审查，不代表 release approval。人工证据 intake 计划只定义证据填写门槛，不自动填充 reviewer、evidenceRef 或 disposition。
人工证据 intake 登记表只建立 pending 登记入口，继续保持 `pendingSlots=10` 与 `approvedSlots=0`。

## 13. 人工证据提交门禁

本轮新增 `阶段30人工证据提交门禁.md` 与 `phase30:evidence-submission-gate`。

当前 live submission 文件 `data/phase30-human-evidence-submission.json` 默认不存在，因此门禁结论是 `format-gate-ready-but-evidence-not-submitted`。未来该文件存在时，门禁只校验 10 个 evidence slots 的 reviewer、reviewedAt、evidenceRef、disposition、decisionReason、residualRisk 以及风险/阻断后续字段。

该门禁不能把 `format-valid` 转换为 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 14. 人工证据提交样例

本轮新增 `阶段30人工证据提交样例.json` 与 `phase30:evidence-submission-template`。

该样例覆盖 10 个 evidence slots，并使用 `REPLACE_WITH_...` 与 `YYYY-MM-DD` 占位符，帮助 reviewer 线下填写。它不位于 `data/phase30-human-evidence-submission.json`，不触发 live submission，不代表人工签核完成。

## 15. 人工 Reviewer 交接包

本轮新增 `阶段30人工Reviewer交接包.md` 与 `phase30:reviewer-handoff`。

该交接包按 release owner、runtime owner、security reviewer、data steward 和 audit reviewer 拆分 10 个 evidence slots。它只表示人工任务已拆分，不代表 reviewer 已经完成签核，也不代表 live submission 已经创建。

## 16. 人工审查会议包

本轮新增 `阶段30人工审查会议包.md` 与 `phase30:review-session-package`。

该会议包把 reviewer handoff 转成会议议程、参会角色确认、逐槽位会议记录模板和会后动作清单。它只表示会议可以召开，不代表任何 evidence slot 已经通过，也不代表 live submission 已经创建。

## 17. 会后证据汇总包

本轮新增 `阶段30会后证据汇总包.md` 与 `phase30:post-review-evidence-summary`。

该汇总包把人工审查会议后的 reviewer 输出整理成维护者可检查的 summary-only 流程。它只表示项目具备会后汇总方法，不自动创建 `data/phase30-human-evidence-submission.json`，不代表任何 evidence slot 已经通过，也不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 18. Live submission 转换预检包

本轮新增 `阶段30LiveSubmission转换预检包.md` 与 `phase30:live-submission-conversion-preflight`。

该预检包用于检查会后汇总是否满足创建 `data/phase30-human-evidence-submission.json` 的最低条件。它只表示项目具备转换前预检流程，不自动创建 live submission，不代表任何 evidence slot 已经通过，也不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 19. 人工证据冲突审查包

本轮新增 `阶段30人工证据冲突审查包.md` 与 `phase30:human-evidence-conflict-review`。

该审查包用于在 live submission 创建前识别 duplicate evidence、reviewer 身份冲突、disposition 冲突、evidenceRef 冲突、风险后续字段缺失、release/runtime 边界混淆和格式批准混淆。它只表示项目具备冲突审查流程，不自动创建 live submission，不代表任何 evidence slot 已经通过，也不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 20. 风险处置后续台账

本轮新增 `阶段30风险处置后续台账.md` 与 `phase30:risk-disposition-followup-ledger`。

该台账用于跟踪 `accepted-with-risk`、`deferred-with-owner` 和 `blocked` 的后续责任、恢复条件、复审日期和阻断范围。它只表示项目具备风险后续跟踪流程，不自动创建 live submission，不代表风险已经解除，也不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 21. 发布决策准备信封

本轮新增 `阶段30发布决策准备信封.md` 与 `phase30:release-decision-readiness-envelope`。

该信封用于把证据汇总、转换预检、冲突审查、风险后续台账和提交门禁合成为 release owner 可审阅材料。它只表示项目具备发布决策准备汇总流程，不自动创建 live submission，不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

## 22. 入口准备红线

本轮新增 `阶段30入口准备红线.md` 与 `phase30:entry-readiness-redline`。

该红线用于把 release approval、Phase 29 exit、Phase 30 entry、runtime go/no-go 和 third-party execution 拆成独立 gate。它只表示项目具备 entry-readiness-redline-only 的反级联边界，不自动创建 live submission，不代表 release approval、Phase 29 exit、Phase 30 entry、runtime execution 或 third-party execution。

当前关键边界：

- `gateCascadeAllowed=false`
- `releaseApprovalGate=blocked-until-independent-release-approval`
- `phase29ExitGate=blocked-until-independent-exit-approval`
- `phase30EntryGate=blocked-until-independent-entry-approval`
- `runtimeGoNoGoGate=no-go-blocked`
- `thirdPartyExecutionGate=blocked`
