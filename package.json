{
  "name": "ai-memory-museum",
  "version": "2.0.1",
  "private": true,
  "description": "时屿（TIME ISLE）：本地优先的 AI 私人记忆策展工具",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node --check server.js && node --check database.js && node --check api/index.js && node --check public/assets/app.js && node scripts/frontend-check.js",
    "smoke": "node scripts/api-smoke.js",
    "check": "node --check server.js && node --check database.js && node --check api/index.js && node --check public/assets/app.js && node scripts/frontend-check.js && node scripts/api-smoke.js",
    "test": "npm run check"
  }
}
