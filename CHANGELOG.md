# 发布记录

本文件只记录已经确认的版本事实。候选版在完成本地验收、两个远端 `main`/Annotated Tag 拓扑、生产部署和线上探针前，不会写成“已发布”。

## 17.1.2 — Candidate (local only)

目标版本：`17.1.2 / schema 19`；合同状态为 `candidate-local-only`。本地验收不会自动触发提交或部署；发布需按运行时 A → Tag → 生产探针 → 证据 B 的授权流程执行。

- 手机展品详情在关闭后重开同一件展品、或在详情内切换展品时，从照片顶部开始；同一次打开期间保留阅读位置，关闭后仍恢复馆藏滚动与触发按钮焦点。
- 馆藏卡片只在展示层去除与情绪同名的重复标签，优先保留情绪并为情绪、标签保留各自的可访问名称；底层数据不删除。
- 本地讲解员引用统一清理已有引导语和重复句尾标点，不再出现“记录了这件展品记录了……”或“。。”。
- 定时媒体维护把安全年龄截止条件同时用于图片与声音的 `ready` 孤儿回收，不再误删仍在当前记录会话中等待最终关联的新附件。
- 390×844、320×700 详情重开、跨字段标签去重和讲解员中文句式均加入永久回归；仓库新增 LF 换行策略，但不执行整仓 renormalize。
- 当前合同为 [`release/v17.1.2.json`](./release/v17.1.2.json)。通过本地测试不会自动扩大为发布事实。

## 17.1.1 — Candidate (superseded locally)

历史目标版本：`17.1.1 / schema 19`。它从未作为独立运行时版本创建 Tag、推送或部署，现已由 17.1.2 候选取代，不能写成已发布。

- 保存、整理与附件关联统一使用带 revision 的操作会话；旧异步回调不能清空新输入，媒体失败可继续关联同一展品。
- 馆藏、记录、找回、我的与移动全屏详情进一步按普通用户旅程收束，加入窄屏筛选抽屉、可信状态降级、焦点恢复和 44×44px 触控门禁。
- 馆藏与搜索增加向后兼容的服务端筛选、排序和 30 件 cursor 分页；卡片响应不再携带原文与完整媒体列表。
- 启动改为核心请求优先、非关键模块渐进加载；展品详情与高级回看脚本首次展开才加载，冷开馆藏固定不超过 722 KiB 原始 JS、275 KiB 压缩 HTML/CSS/JS 和 57 个 CSS/JS 资源；静态响应支持 gzip、表示专属 ETag、304 与大文件流式传输。
- 浏览器门禁覆盖 1265×720、390×844、320×700、844×390，以及 Demo/可写竞态、搜索恢复、无障碍和 500 件分页性能合同。
- 历史合同保留在 [`release/v17.1.1.json`](./release/v17.1.1.json)，仅作未发布候选快照，不再代表当前候选。

## 17.1.0 — Candidate (superseded locally)

历史目标版本：`17.1.0 / schema 19`。它从未作为独立运行时版本创建 Tag 或部署，随后被 17.1.1、17.1.2 候选取代。

### 普通用户体验

- 手机馆藏改为内容优先：搜索、最近展品和照片先出现，品牌说明与统计退到次级区域。
- 记录流程改为“先保存原文，再选择整理”。文字先创建，图片或声音失败不会带走原文；整理确认后以并发条件更新同一件展品，不产生重复项。
- 草稿默认只展示标题、展签、展厅和日期；人物、标签、情绪与重要度进入“更多信息”。
- 底栏显示“馆藏 / 记录 / 找回 / 我的”，内部 hash 仍为 `collection / compose / reflect / data`。
- 找回页先解释字段检索、设备语义与讲解员的区别，高级回看工具收进“更多回看方式”。
- 我的页先展示数据位置、备份和安装；安全合同、恢复演练和技术证据收进“项目幕后”。
- 展品详情先展示照片、展签、原文和普通字段，再展示来源护照、年轮、多视角与实验工具。

### 安全与可信

- 公开 Demo 改为全局零写入：所有非 `GET/HEAD` 请求在读取正文前统一拒绝，四件固定中文虚构样例由本地播种数据提供。
- 四个主视图持续显示运行时信任状态：部署类型、保存位置、写能力、AI 引擎、是否外发和静态加密状态。
- 新增 `GET /api/runtime/trust`，只返回可安全公开的运行时合同，不返回 Key、认证头或完整认证 URL。
- 外部 AI 只有在 UI 先展示提供方、模型、发送字段和用途，且用户对本次输入明确同意后才可调用。合同以内容哈希绑定，整理结果使用服务端签名执行回执。
- 录音中切换主视图会先让用户选择保留、继续或放弃；页面进入后台会停止采集并保留待确认片段。
- 所有智能模块使用真实标签：本地规则、公开 Mock、设备 embedding、外部模型或确定性策展工作流。
- SQLite、图片、声音和备份仍未实现静态加密；V18 门禁继续为 **NO-GO**。

### 质量与发布证据

- 增加隔离可写 Playwright 旅程，覆盖文字先存、媒体关联、可选整理、搜索、重开详情和刷新持久化，以及三档视口。
- 增加永久假麦克风夹具，覆盖录音切页、权限迟到、后台停止、放弃零资产和旧声音保留。
- 增加完全虚构的 100/500 件中文语义质量门禁，输出两档 Recall@3、MRR@3、误召回切片、100 件 FTS 对照与索引/查询性能。
- 增加 Windows/Linux 根检查、浏览器旅程、语义质量、Docker 构建与 evidence artifact 的 GitHub Actions。
- 历史合同保留在 [`release/v17.1.0.json`](./release/v17.1.0.json)，仅作未发布候选快照，不再代表当前候选。

## 17.0.0 — Released 2026-07-22

- 发布 V15 一次性真实隔离恢复、V16 多视角记忆对照和 V17 设备内中文语义找回，schema 保持 19。
- 功能提交：`413f78640baad7eae6324ef14bc291f05325fbf6`；静态资产披露热修复：`38d3450b9f26efdc59df4860317cdf9513e77e65`。
- GitHub/Gitee `main`、Vercel 与 CloudBase 部署 `002` 完成当时的生产核验。
- CloudBase 静态唤醒应用 `time-isle-wakeup-002` 完成桌面及手机 Wi-Fi/蜂窝入口链路验收。该结果只证明入口可达，不扩大为全部移动功能或私人云存储。
- 当时的发布证据、地址和生产探针保留在 [`DEPLOYMENT.md`](./DEPLOYMENT.md) 的历史记录中。

## 14.0.0 — Released 2026-07-20

- 共忆信笺、设备内确定性镜片、schema 19 写门禁与诚实恢复演练形成阶段性发布。
- 功能提交：`2dcce402b13f1d43c54c6a196b8e2273c9483eb3`。

## 10.0.0 — Released 2026-07-19

- 固定预算、四项只读工具和逐项人工决定的确定性策展工作流首次发布。
- 最终提交：`7107ede`。

## 更早版本

V4–V9 的影像、声音、展览、胶囊、回访、年轮、时间校准和口述史演进记录保留在 [`项目文档/V5-V10扩展路线.md`](./项目文档/V5-V10扩展路线.md) 与产品说明中，不再占用 README 首屏。
