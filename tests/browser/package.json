{
  "name": "ai-memory-museum-browser-gate",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated Playwright release gate for TIME ISLE",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node scripts/run-gate.cjs",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
